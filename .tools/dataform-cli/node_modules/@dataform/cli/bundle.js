#!/usr/bin/env node
// Copyright 2023 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

'use strict';

var chokidar = require('chokidar');
var fs$1 = require('fs');
var glob = require('glob');
var parseDuration = require('parse-duration');
var path = require('path');
var yargs = require('yargs');
var jsYaml = require('js-yaml');
var protobufjs = require('protobufjs');
var $protobuf = require('protobufjs/minimal');
var semver = require('semver');
var childProcess = require('child_process');
var fs = require('fs-extra');
var tmp = require('tmp');
var util = require('util');
var EventEmitter = require('events');
var Long = require('long');
var bigquery = require('@google-cloud/bigquery');
var promisePoolExecutor = require('promise-pool-executor');
var sizeof = require('object-sizeof');
var readlineSync = require('readline-sync');
var untildify = require('untildify');
var googleSqlSyntaxTs = require('google-sql-syntax-ts');
var jsBeautify = require('js-beautify');
var typeidJs = require('typeid-js');
var moo = require('moo');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

function _interopNamespace(e) {
    if (e && e.__esModule) return e;
    var n = Object.create(null);
    if (e) {
        Object.keys(e).forEach(function (k) {
            if (k !== 'default') {
                var d = Object.getOwnPropertyDescriptor(e, k);
                Object.defineProperty(n, k, d.get ? d : {
                    enumerable: true,
                    get: function () { return e[k]; }
                });
            }
        });
    }
    n["default"] = e;
    return Object.freeze(n);
}

var chokidar__namespace = /*#__PURE__*/_interopNamespace(chokidar);
var fs__namespace$1 = /*#__PURE__*/_interopNamespace(fs$1);
var glob__namespace = /*#__PURE__*/_interopNamespace(glob);
var parseDuration__default = /*#__PURE__*/_interopDefaultLegacy(parseDuration);
var path__namespace = /*#__PURE__*/_interopNamespace(path);
var yargs__default = /*#__PURE__*/_interopDefaultLegacy(yargs);
var $protobuf__namespace = /*#__PURE__*/_interopNamespace($protobuf);
var semver__namespace = /*#__PURE__*/_interopNamespace(semver);
var childProcess__namespace = /*#__PURE__*/_interopNamespace(childProcess);
var fs__namespace = /*#__PURE__*/_interopNamespace(fs);
var tmp__namespace = /*#__PURE__*/_interopNamespace(tmp);
var EventEmitter__default = /*#__PURE__*/_interopDefaultLegacy(EventEmitter);
var Long__default = /*#__PURE__*/_interopDefaultLegacy(Long);
var sizeof__default = /*#__PURE__*/_interopDefaultLegacy(sizeof);
var readlineSync__namespace = /*#__PURE__*/_interopNamespace(readlineSync);
var untildify__default = /*#__PURE__*/_interopDefaultLegacy(untildify);
var jsBeautify__namespace = /*#__PURE__*/_interopNamespace(jsBeautify);
var moo__namespace = /*#__PURE__*/_interopNamespace(moo);

class JSONObjectStringifier {
    stringify(value) {
        return JSON.stringify(Object.keys(value)
            .sort()
            .reduce((acc, curr) => (Object.assign(Object.assign({}, acc), { [curr]: value[curr] })), {}));
    }
    parse(value) {
        return JSON.parse(value);
    }
}

const targetStringifier = new JSONObjectStringifier();
function targetAsReadableString(target) {
    const nameParts = [target.name, target.schema];
    if (!!target.database) {
        nameParts.push(target.database);
    }
    return nameParts.reverse().join(".");
}

/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars*/

// Common aliases
const $Reader = $protobuf__namespace.Reader, $Writer = $protobuf__namespace.Writer, $util = $protobuf__namespace.util;

// Exported root namespace
const $root = $protobuf__namespace.roots["default"] || ($protobuf__namespace.roots["default"] = {});

const dataform = $root.dataform = (() => {

    /**
     * Namespace dataform.
     * @exports dataform
     * @namespace
     */
    const dataform = {};

    dataform.WorkflowSettings = (function() {

        /**
         * Properties of a WorkflowSettings.
         * @memberof dataform
         * @interface IWorkflowSettings
         * @property {string|null} [dataformCoreVersion] WorkflowSettings dataformCoreVersion
         * @property {string|null} [defaultProject] WorkflowSettings defaultProject
         * @property {string|null} [defaultDataset] WorkflowSettings defaultDataset
         * @property {string|null} [defaultLocation] WorkflowSettings defaultLocation
         * @property {string|null} [defaultAssertionDataset] WorkflowSettings defaultAssertionDataset
         * @property {Object.<string,string>|null} [vars] WorkflowSettings vars
         * @property {string|null} [projectSuffix] WorkflowSettings projectSuffix
         * @property {string|null} [datasetSuffix] WorkflowSettings datasetSuffix
         * @property {string|null} [namePrefix] WorkflowSettings namePrefix
         * @property {dataform.INotebookRuntimeOptionsConfig|null} [defaultNotebookRuntimeOptions] WorkflowSettings defaultNotebookRuntimeOptions
         * @property {string|null} [builtinAssertionNamePrefix] WorkflowSettings builtinAssertionNamePrefix
         * @property {dataform.IDefaultIcebergConfig|null} [defaultIcebergConfig] WorkflowSettings defaultIcebergConfig
         * @property {boolean|null} [disableAssertions] WorkflowSettings disableAssertions
         * @property {string|null} [defaultReservation] WorkflowSettings defaultReservation
         * @property {dataform.IExtension|null} [extension] WorkflowSettings extension
         * @property {boolean|null} [includeTestsInCompiledGraph] WorkflowSettings includeTestsInCompiledGraph
         * @property {boolean|null} [preserveGovernanceControls] WorkflowSettings preserveGovernanceControls
         */

        /**
         * Constructs a new WorkflowSettings.
         * @memberof dataform
         * @classdesc Represents a WorkflowSettings.
         * @implements IWorkflowSettings
         * @constructor
         * @param {dataform.IWorkflowSettings=} [properties] Properties to set
         */
        function WorkflowSettings(properties) {
            this.vars = {};
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WorkflowSettings dataformCoreVersion.
         * @member {string} dataformCoreVersion
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.dataformCoreVersion = "";

        /**
         * WorkflowSettings defaultProject.
         * @member {string} defaultProject
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.defaultProject = "";

        /**
         * WorkflowSettings defaultDataset.
         * @member {string} defaultDataset
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.defaultDataset = "";

        /**
         * WorkflowSettings defaultLocation.
         * @member {string} defaultLocation
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.defaultLocation = "";

        /**
         * WorkflowSettings defaultAssertionDataset.
         * @member {string} defaultAssertionDataset
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.defaultAssertionDataset = "";

        /**
         * WorkflowSettings vars.
         * @member {Object.<string,string>} vars
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.vars = $util.emptyObject;

        /**
         * WorkflowSettings projectSuffix.
         * @member {string} projectSuffix
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.projectSuffix = "";

        /**
         * WorkflowSettings datasetSuffix.
         * @member {string} datasetSuffix
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.datasetSuffix = "";

        /**
         * WorkflowSettings namePrefix.
         * @member {string} namePrefix
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.namePrefix = "";

        /**
         * WorkflowSettings defaultNotebookRuntimeOptions.
         * @member {dataform.INotebookRuntimeOptionsConfig|null|undefined} defaultNotebookRuntimeOptions
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.defaultNotebookRuntimeOptions = null;

        /**
         * WorkflowSettings builtinAssertionNamePrefix.
         * @member {string} builtinAssertionNamePrefix
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.builtinAssertionNamePrefix = "";

        /**
         * WorkflowSettings defaultIcebergConfig.
         * @member {dataform.IDefaultIcebergConfig|null|undefined} defaultIcebergConfig
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.defaultIcebergConfig = null;

        /**
         * WorkflowSettings disableAssertions.
         * @member {boolean} disableAssertions
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.disableAssertions = false;

        /**
         * WorkflowSettings defaultReservation.
         * @member {string} defaultReservation
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.defaultReservation = "";

        /**
         * WorkflowSettings extension.
         * @member {dataform.IExtension|null|undefined} extension
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.extension = null;

        /**
         * WorkflowSettings includeTestsInCompiledGraph.
         * @member {boolean} includeTestsInCompiledGraph
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.includeTestsInCompiledGraph = false;

        /**
         * WorkflowSettings preserveGovernanceControls.
         * @member {boolean} preserveGovernanceControls
         * @memberof dataform.WorkflowSettings
         * @instance
         */
        WorkflowSettings.prototype.preserveGovernanceControls = false;

        /**
         * Creates a new WorkflowSettings instance using the specified properties.
         * @function create
         * @memberof dataform.WorkflowSettings
         * @static
         * @param {dataform.IWorkflowSettings=} [properties] Properties to set
         * @returns {dataform.WorkflowSettings} WorkflowSettings instance
         */
        WorkflowSettings.create = function create(properties) {
            return new WorkflowSettings(properties);
        };

        /**
         * Encodes the specified WorkflowSettings message. Does not implicitly {@link dataform.WorkflowSettings.verify|verify} messages.
         * @function encode
         * @memberof dataform.WorkflowSettings
         * @static
         * @param {dataform.IWorkflowSettings} message WorkflowSettings message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WorkflowSettings.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.dataformCoreVersion != null && Object.hasOwnProperty.call(message, "dataformCoreVersion"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.dataformCoreVersion);
            if (message.defaultProject != null && Object.hasOwnProperty.call(message, "defaultProject"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.defaultProject);
            if (message.defaultDataset != null && Object.hasOwnProperty.call(message, "defaultDataset"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.defaultDataset);
            if (message.defaultLocation != null && Object.hasOwnProperty.call(message, "defaultLocation"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.defaultLocation);
            if (message.defaultAssertionDataset != null && Object.hasOwnProperty.call(message, "defaultAssertionDataset"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.defaultAssertionDataset);
            if (message.vars != null && Object.hasOwnProperty.call(message, "vars"))
                for (let keys = Object.keys(message.vars), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.vars[keys[i]]).ldelim();
            if (message.projectSuffix != null && Object.hasOwnProperty.call(message, "projectSuffix"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.projectSuffix);
            if (message.datasetSuffix != null && Object.hasOwnProperty.call(message, "datasetSuffix"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.datasetSuffix);
            if (message.namePrefix != null && Object.hasOwnProperty.call(message, "namePrefix"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.namePrefix);
            if (message.defaultNotebookRuntimeOptions != null && Object.hasOwnProperty.call(message, "defaultNotebookRuntimeOptions"))
                $root.dataform.NotebookRuntimeOptionsConfig.encode(message.defaultNotebookRuntimeOptions, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
            if (message.builtinAssertionNamePrefix != null && Object.hasOwnProperty.call(message, "builtinAssertionNamePrefix"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.builtinAssertionNamePrefix);
            if (message.defaultIcebergConfig != null && Object.hasOwnProperty.call(message, "defaultIcebergConfig"))
                $root.dataform.DefaultIcebergConfig.encode(message.defaultIcebergConfig, writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
            if (message.disableAssertions != null && Object.hasOwnProperty.call(message, "disableAssertions"))
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.disableAssertions);
            if (message.defaultReservation != null && Object.hasOwnProperty.call(message, "defaultReservation"))
                writer.uint32(/* id 14, wireType 2 =*/114).string(message.defaultReservation);
            if (message.extension != null && Object.hasOwnProperty.call(message, "extension"))
                $root.dataform.Extension.encode(message.extension, writer.uint32(/* id 15, wireType 2 =*/122).fork(), q + 1).ldelim();
            if (message.includeTestsInCompiledGraph != null && Object.hasOwnProperty.call(message, "includeTestsInCompiledGraph"))
                writer.uint32(/* id 16, wireType 0 =*/128).bool(message.includeTestsInCompiledGraph);
            if (message.preserveGovernanceControls != null && Object.hasOwnProperty.call(message, "preserveGovernanceControls"))
                writer.uint32(/* id 18, wireType 0 =*/144).bool(message.preserveGovernanceControls);
            return writer;
        };

        /**
         * Encodes the specified WorkflowSettings message, length delimited. Does not implicitly {@link dataform.WorkflowSettings.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.WorkflowSettings
         * @static
         * @param {dataform.IWorkflowSettings} message WorkflowSettings message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WorkflowSettings.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a WorkflowSettings message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.WorkflowSettings
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.WorkflowSettings} WorkflowSettings
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WorkflowSettings.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.WorkflowSettings(), key, value;
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.dataformCoreVersion = reader.string();
                        break;
                    }
                case 2: {
                        message.defaultProject = reader.string();
                        break;
                    }
                case 3: {
                        message.defaultDataset = reader.string();
                        break;
                    }
                case 4: {
                        message.defaultLocation = reader.string();
                        break;
                    }
                case 5: {
                        message.defaultAssertionDataset = reader.string();
                        break;
                    }
                case 6: {
                        if (message.vars === $util.emptyObject)
                            message.vars = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = "";
                        while (reader.pos < end2) {
                            let tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.string();
                                break;
                            case 2:
                                value = reader.string();
                                break;
                            default:
                                reader.skipType(tag2 & 7, long);
                                break;
                            }
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.vars, key);
                        message.vars[key] = value;
                        break;
                    }
                case 7: {
                        message.projectSuffix = reader.string();
                        break;
                    }
                case 8: {
                        message.datasetSuffix = reader.string();
                        break;
                    }
                case 9: {
                        message.namePrefix = reader.string();
                        break;
                    }
                case 10: {
                        message.defaultNotebookRuntimeOptions = $root.dataform.NotebookRuntimeOptionsConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 11: {
                        message.builtinAssertionNamePrefix = reader.string();
                        break;
                    }
                case 12: {
                        message.defaultIcebergConfig = $root.dataform.DefaultIcebergConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 13: {
                        message.disableAssertions = reader.bool();
                        break;
                    }
                case 14: {
                        message.defaultReservation = reader.string();
                        break;
                    }
                case 15: {
                        message.extension = $root.dataform.Extension.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 16: {
                        message.includeTestsInCompiledGraph = reader.bool();
                        break;
                    }
                case 18: {
                        message.preserveGovernanceControls = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WorkflowSettings message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.WorkflowSettings
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.WorkflowSettings} WorkflowSettings
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WorkflowSettings.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WorkflowSettings message.
         * @function verify
         * @memberof dataform.WorkflowSettings
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WorkflowSettings.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.dataformCoreVersion != null && Object.hasOwnProperty.call(message, "dataformCoreVersion"))
                if (!$util.isString(message.dataformCoreVersion))
                    return "dataformCoreVersion: string expected";
            if (message.defaultProject != null && Object.hasOwnProperty.call(message, "defaultProject"))
                if (!$util.isString(message.defaultProject))
                    return "defaultProject: string expected";
            if (message.defaultDataset != null && Object.hasOwnProperty.call(message, "defaultDataset"))
                if (!$util.isString(message.defaultDataset))
                    return "defaultDataset: string expected";
            if (message.defaultLocation != null && Object.hasOwnProperty.call(message, "defaultLocation"))
                if (!$util.isString(message.defaultLocation))
                    return "defaultLocation: string expected";
            if (message.defaultAssertionDataset != null && Object.hasOwnProperty.call(message, "defaultAssertionDataset"))
                if (!$util.isString(message.defaultAssertionDataset))
                    return "defaultAssertionDataset: string expected";
            if (message.vars != null && Object.hasOwnProperty.call(message, "vars")) {
                if (!$util.isObject(message.vars))
                    return "vars: object expected";
                let key = Object.keys(message.vars);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isString(message.vars[key[i]]))
                        return "vars: string{k:string} expected";
            }
            if (message.projectSuffix != null && Object.hasOwnProperty.call(message, "projectSuffix"))
                if (!$util.isString(message.projectSuffix))
                    return "projectSuffix: string expected";
            if (message.datasetSuffix != null && Object.hasOwnProperty.call(message, "datasetSuffix"))
                if (!$util.isString(message.datasetSuffix))
                    return "datasetSuffix: string expected";
            if (message.namePrefix != null && Object.hasOwnProperty.call(message, "namePrefix"))
                if (!$util.isString(message.namePrefix))
                    return "namePrefix: string expected";
            if (message.defaultNotebookRuntimeOptions != null && Object.hasOwnProperty.call(message, "defaultNotebookRuntimeOptions")) {
                let error = $root.dataform.NotebookRuntimeOptionsConfig.verify(message.defaultNotebookRuntimeOptions, long + 1);
                if (error)
                    return "defaultNotebookRuntimeOptions." + error;
            }
            if (message.builtinAssertionNamePrefix != null && Object.hasOwnProperty.call(message, "builtinAssertionNamePrefix"))
                if (!$util.isString(message.builtinAssertionNamePrefix))
                    return "builtinAssertionNamePrefix: string expected";
            if (message.defaultIcebergConfig != null && Object.hasOwnProperty.call(message, "defaultIcebergConfig")) {
                let error = $root.dataform.DefaultIcebergConfig.verify(message.defaultIcebergConfig, long + 1);
                if (error)
                    return "defaultIcebergConfig." + error;
            }
            if (message.disableAssertions != null && Object.hasOwnProperty.call(message, "disableAssertions"))
                if (typeof message.disableAssertions !== "boolean")
                    return "disableAssertions: boolean expected";
            if (message.defaultReservation != null && Object.hasOwnProperty.call(message, "defaultReservation"))
                if (!$util.isString(message.defaultReservation))
                    return "defaultReservation: string expected";
            if (message.extension != null && Object.hasOwnProperty.call(message, "extension")) {
                let error = $root.dataform.Extension.verify(message.extension, long + 1);
                if (error)
                    return "extension." + error;
            }
            if (message.includeTestsInCompiledGraph != null && Object.hasOwnProperty.call(message, "includeTestsInCompiledGraph"))
                if (typeof message.includeTestsInCompiledGraph !== "boolean")
                    return "includeTestsInCompiledGraph: boolean expected";
            if (message.preserveGovernanceControls != null && Object.hasOwnProperty.call(message, "preserveGovernanceControls"))
                if (typeof message.preserveGovernanceControls !== "boolean")
                    return "preserveGovernanceControls: boolean expected";
            return null;
        };

        /**
         * Creates a WorkflowSettings message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.WorkflowSettings
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.WorkflowSettings} WorkflowSettings
         */
        WorkflowSettings.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.WorkflowSettings)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.WorkflowSettings: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.WorkflowSettings();
            if (object.dataformCoreVersion != null)
                message.dataformCoreVersion = String(object.dataformCoreVersion);
            if (object.defaultProject != null)
                message.defaultProject = String(object.defaultProject);
            if (object.defaultDataset != null)
                message.defaultDataset = String(object.defaultDataset);
            if (object.defaultLocation != null)
                message.defaultLocation = String(object.defaultLocation);
            if (object.defaultAssertionDataset != null)
                message.defaultAssertionDataset = String(object.defaultAssertionDataset);
            if (object.vars) {
                if (!$util.isObject(object.vars))
                    throw TypeError(".dataform.WorkflowSettings.vars: object expected");
                message.vars = {};
                for (let keys = Object.keys(object.vars), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.vars, keys[i]);
                    message.vars[keys[i]] = String(object.vars[keys[i]]);
                }
            }
            if (object.projectSuffix != null)
                message.projectSuffix = String(object.projectSuffix);
            if (object.datasetSuffix != null)
                message.datasetSuffix = String(object.datasetSuffix);
            if (object.namePrefix != null)
                message.namePrefix = String(object.namePrefix);
            if (object.defaultNotebookRuntimeOptions != null) {
                if (!$util.isObject(object.defaultNotebookRuntimeOptions))
                    throw TypeError(".dataform.WorkflowSettings.defaultNotebookRuntimeOptions: object expected");
                message.defaultNotebookRuntimeOptions = $root.dataform.NotebookRuntimeOptionsConfig.fromObject(object.defaultNotebookRuntimeOptions, long + 1);
            }
            if (object.builtinAssertionNamePrefix != null)
                message.builtinAssertionNamePrefix = String(object.builtinAssertionNamePrefix);
            if (object.defaultIcebergConfig != null) {
                if (!$util.isObject(object.defaultIcebergConfig))
                    throw TypeError(".dataform.WorkflowSettings.defaultIcebergConfig: object expected");
                message.defaultIcebergConfig = $root.dataform.DefaultIcebergConfig.fromObject(object.defaultIcebergConfig, long + 1);
            }
            if (object.disableAssertions != null)
                message.disableAssertions = Boolean(object.disableAssertions);
            if (object.defaultReservation != null)
                message.defaultReservation = String(object.defaultReservation);
            if (object.extension != null) {
                if (!$util.isObject(object.extension))
                    throw TypeError(".dataform.WorkflowSettings.extension: object expected");
                message.extension = $root.dataform.Extension.fromObject(object.extension, long + 1);
            }
            if (object.includeTestsInCompiledGraph != null)
                message.includeTestsInCompiledGraph = Boolean(object.includeTestsInCompiledGraph);
            if (object.preserveGovernanceControls != null)
                message.preserveGovernanceControls = Boolean(object.preserveGovernanceControls);
            return message;
        };

        /**
         * Creates a plain object from a WorkflowSettings message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.WorkflowSettings
         * @static
         * @param {dataform.WorkflowSettings} message WorkflowSettings
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WorkflowSettings.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.vars = {};
            if (options.defaults) {
                object.dataformCoreVersion = "";
                object.defaultProject = "";
                object.defaultDataset = "";
                object.defaultLocation = "";
                object.defaultAssertionDataset = "";
                object.projectSuffix = "";
                object.datasetSuffix = "";
                object.namePrefix = "";
                object.defaultNotebookRuntimeOptions = null;
                object.builtinAssertionNamePrefix = "";
                object.defaultIcebergConfig = null;
                object.disableAssertions = false;
                object.defaultReservation = "";
                object.extension = null;
                object.includeTestsInCompiledGraph = false;
                object.preserveGovernanceControls = false;
            }
            if (message.dataformCoreVersion != null && Object.hasOwnProperty.call(message, "dataformCoreVersion"))
                object.dataformCoreVersion = message.dataformCoreVersion;
            if (message.defaultProject != null && Object.hasOwnProperty.call(message, "defaultProject"))
                object.defaultProject = message.defaultProject;
            if (message.defaultDataset != null && Object.hasOwnProperty.call(message, "defaultDataset"))
                object.defaultDataset = message.defaultDataset;
            if (message.defaultLocation != null && Object.hasOwnProperty.call(message, "defaultLocation"))
                object.defaultLocation = message.defaultLocation;
            if (message.defaultAssertionDataset != null && Object.hasOwnProperty.call(message, "defaultAssertionDataset"))
                object.defaultAssertionDataset = message.defaultAssertionDataset;
            let keys2;
            if (message.vars && (keys2 = Object.keys(message.vars)).length) {
                object.vars = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.vars, keys2[j]);
                    object.vars[keys2[j]] = message.vars[keys2[j]];
                }
            }
            if (message.projectSuffix != null && Object.hasOwnProperty.call(message, "projectSuffix"))
                object.projectSuffix = message.projectSuffix;
            if (message.datasetSuffix != null && Object.hasOwnProperty.call(message, "datasetSuffix"))
                object.datasetSuffix = message.datasetSuffix;
            if (message.namePrefix != null && Object.hasOwnProperty.call(message, "namePrefix"))
                object.namePrefix = message.namePrefix;
            if (message.defaultNotebookRuntimeOptions != null && Object.hasOwnProperty.call(message, "defaultNotebookRuntimeOptions"))
                object.defaultNotebookRuntimeOptions = $root.dataform.NotebookRuntimeOptionsConfig.toObject(message.defaultNotebookRuntimeOptions, options, q + 1);
            if (message.builtinAssertionNamePrefix != null && Object.hasOwnProperty.call(message, "builtinAssertionNamePrefix"))
                object.builtinAssertionNamePrefix = message.builtinAssertionNamePrefix;
            if (message.defaultIcebergConfig != null && Object.hasOwnProperty.call(message, "defaultIcebergConfig"))
                object.defaultIcebergConfig = $root.dataform.DefaultIcebergConfig.toObject(message.defaultIcebergConfig, options, q + 1);
            if (message.disableAssertions != null && Object.hasOwnProperty.call(message, "disableAssertions"))
                object.disableAssertions = message.disableAssertions;
            if (message.defaultReservation != null && Object.hasOwnProperty.call(message, "defaultReservation"))
                object.defaultReservation = message.defaultReservation;
            if (message.extension != null && Object.hasOwnProperty.call(message, "extension"))
                object.extension = $root.dataform.Extension.toObject(message.extension, options, q + 1);
            if (message.includeTestsInCompiledGraph != null && Object.hasOwnProperty.call(message, "includeTestsInCompiledGraph"))
                object.includeTestsInCompiledGraph = message.includeTestsInCompiledGraph;
            if (message.preserveGovernanceControls != null && Object.hasOwnProperty.call(message, "preserveGovernanceControls"))
                object.preserveGovernanceControls = message.preserveGovernanceControls;
            return object;
        };

        /**
         * Converts this WorkflowSettings to JSON.
         * @function toJSON
         * @memberof dataform.WorkflowSettings
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WorkflowSettings.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WorkflowSettings
         * @function getTypeUrl
         * @memberof dataform.WorkflowSettings
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WorkflowSettings.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.WorkflowSettings";
        };

        return WorkflowSettings;
    })();

    dataform.ActionConfigs = (function() {

        /**
         * Properties of an ActionConfigs.
         * @memberof dataform
         * @interface IActionConfigs
         * @property {Array.<dataform.IActionConfig>|null} [actions] ActionConfigs actions
         */

        /**
         * Constructs a new ActionConfigs.
         * @memberof dataform
         * @classdesc Represents an ActionConfigs.
         * @implements IActionConfigs
         * @constructor
         * @param {dataform.IActionConfigs=} [properties] Properties to set
         */
        function ActionConfigs(properties) {
            this.actions = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ActionConfigs actions.
         * @member {Array.<dataform.IActionConfig>} actions
         * @memberof dataform.ActionConfigs
         * @instance
         */
        ActionConfigs.prototype.actions = $util.emptyArray;

        /**
         * Creates a new ActionConfigs instance using the specified properties.
         * @function create
         * @memberof dataform.ActionConfigs
         * @static
         * @param {dataform.IActionConfigs=} [properties] Properties to set
         * @returns {dataform.ActionConfigs} ActionConfigs instance
         */
        ActionConfigs.create = function create(properties) {
            return new ActionConfigs(properties);
        };

        /**
         * Encodes the specified ActionConfigs message. Does not implicitly {@link dataform.ActionConfigs.verify|verify} messages.
         * @function encode
         * @memberof dataform.ActionConfigs
         * @static
         * @param {dataform.IActionConfigs} message ActionConfigs message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ActionConfigs.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.actions != null && message.actions.length)
                for (let i = 0; i < message.actions.length; ++i)
                    $root.dataform.ActionConfig.encode(message.actions[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ActionConfigs message, length delimited. Does not implicitly {@link dataform.ActionConfigs.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.ActionConfigs
         * @static
         * @param {dataform.IActionConfigs} message ActionConfigs message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ActionConfigs.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ActionConfigs message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.ActionConfigs
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.ActionConfigs} ActionConfigs
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ActionConfigs.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfigs();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.actions && message.actions.length))
                            message.actions = [];
                        message.actions.push($root.dataform.ActionConfig.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ActionConfigs message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.ActionConfigs
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.ActionConfigs} ActionConfigs
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ActionConfigs.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ActionConfigs message.
         * @function verify
         * @memberof dataform.ActionConfigs
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ActionConfigs.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.actions != null && Object.hasOwnProperty.call(message, "actions")) {
                if (!Array.isArray(message.actions))
                    return "actions: array expected";
                for (let i = 0; i < message.actions.length; ++i) {
                    let error = $root.dataform.ActionConfig.verify(message.actions[i], long + 1);
                    if (error)
                        return "actions." + error;
                }
            }
            return null;
        };

        /**
         * Creates an ActionConfigs message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.ActionConfigs
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.ActionConfigs} ActionConfigs
         */
        ActionConfigs.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.ActionConfigs)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.ActionConfigs: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.ActionConfigs();
            if (object.actions) {
                if (!Array.isArray(object.actions))
                    throw TypeError(".dataform.ActionConfigs.actions: array expected");
                message.actions = [];
                for (let i = 0; i < object.actions.length; ++i) {
                    if (!$util.isObject(object.actions[i]))
                        throw TypeError(".dataform.ActionConfigs.actions: object expected");
                    message.actions[i] = $root.dataform.ActionConfig.fromObject(object.actions[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an ActionConfigs message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.ActionConfigs
         * @static
         * @param {dataform.ActionConfigs} message ActionConfigs
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ActionConfigs.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.actions = [];
            if (message.actions && message.actions.length) {
                object.actions = [];
                for (let j = 0; j < message.actions.length; ++j)
                    object.actions[j] = $root.dataform.ActionConfig.toObject(message.actions[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this ActionConfigs to JSON.
         * @function toJSON
         * @memberof dataform.ActionConfigs
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ActionConfigs.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ActionConfigs
         * @function getTypeUrl
         * @memberof dataform.ActionConfigs
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ActionConfigs.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.ActionConfigs";
        };

        return ActionConfigs;
    })();

    dataform.ActionConfig = (function() {

        /**
         * Properties of an ActionConfig.
         * @memberof dataform
         * @interface IActionConfig
         * @property {dataform.ActionConfig.ITableConfig|null} [table] ActionConfig table
         * @property {dataform.ActionConfig.IViewConfig|null} [view] ActionConfig view
         * @property {dataform.ActionConfig.IIncrementalTableConfig|null} [incrementalTable] ActionConfig incrementalTable
         * @property {dataform.ActionConfig.IAssertionConfig|null} [assertion] ActionConfig assertion
         * @property {dataform.ActionConfig.IOperationConfig|null} [operation] ActionConfig operation
         * @property {dataform.ActionConfig.IDeclarationConfig|null} [declaration] ActionConfig declaration
         * @property {dataform.ActionConfig.INotebookConfig|null} [notebook] ActionConfig notebook
         * @property {dataform.ActionConfig.IDataPreparationConfig|null} [dataPreparation] ActionConfig dataPreparation
         */

        /**
         * Constructs a new ActionConfig.
         * @memberof dataform
         * @classdesc Represents an ActionConfig.
         * @implements IActionConfig
         * @constructor
         * @param {dataform.IActionConfig=} [properties] Properties to set
         */
        function ActionConfig(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ActionConfig table.
         * @member {dataform.ActionConfig.ITableConfig|null|undefined} table
         * @memberof dataform.ActionConfig
         * @instance
         */
        ActionConfig.prototype.table = null;

        /**
         * ActionConfig view.
         * @member {dataform.ActionConfig.IViewConfig|null|undefined} view
         * @memberof dataform.ActionConfig
         * @instance
         */
        ActionConfig.prototype.view = null;

        /**
         * ActionConfig incrementalTable.
         * @member {dataform.ActionConfig.IIncrementalTableConfig|null|undefined} incrementalTable
         * @memberof dataform.ActionConfig
         * @instance
         */
        ActionConfig.prototype.incrementalTable = null;

        /**
         * ActionConfig assertion.
         * @member {dataform.ActionConfig.IAssertionConfig|null|undefined} assertion
         * @memberof dataform.ActionConfig
         * @instance
         */
        ActionConfig.prototype.assertion = null;

        /**
         * ActionConfig operation.
         * @member {dataform.ActionConfig.IOperationConfig|null|undefined} operation
         * @memberof dataform.ActionConfig
         * @instance
         */
        ActionConfig.prototype.operation = null;

        /**
         * ActionConfig declaration.
         * @member {dataform.ActionConfig.IDeclarationConfig|null|undefined} declaration
         * @memberof dataform.ActionConfig
         * @instance
         */
        ActionConfig.prototype.declaration = null;

        /**
         * ActionConfig notebook.
         * @member {dataform.ActionConfig.INotebookConfig|null|undefined} notebook
         * @memberof dataform.ActionConfig
         * @instance
         */
        ActionConfig.prototype.notebook = null;

        /**
         * ActionConfig dataPreparation.
         * @member {dataform.ActionConfig.IDataPreparationConfig|null|undefined} dataPreparation
         * @memberof dataform.ActionConfig
         * @instance
         */
        ActionConfig.prototype.dataPreparation = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * ActionConfig action.
         * @member {"table"|"view"|"incrementalTable"|"assertion"|"operation"|"declaration"|"notebook"|"dataPreparation"|undefined} action
         * @memberof dataform.ActionConfig
         * @instance
         */
        Object.defineProperty(ActionConfig.prototype, "action", {
            get: $util.oneOfGetter($oneOfFields = ["table", "view", "incrementalTable", "assertion", "operation", "declaration", "notebook", "dataPreparation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new ActionConfig instance using the specified properties.
         * @function create
         * @memberof dataform.ActionConfig
         * @static
         * @param {dataform.IActionConfig=} [properties] Properties to set
         * @returns {dataform.ActionConfig} ActionConfig instance
         */
        ActionConfig.create = function create(properties) {
            return new ActionConfig(properties);
        };

        /**
         * Encodes the specified ActionConfig message. Does not implicitly {@link dataform.ActionConfig.verify|verify} messages.
         * @function encode
         * @memberof dataform.ActionConfig
         * @static
         * @param {dataform.IActionConfig} message ActionConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ActionConfig.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.table != null && Object.hasOwnProperty.call(message, "table"))
                $root.dataform.ActionConfig.TableConfig.encode(message.table, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.view != null && Object.hasOwnProperty.call(message, "view"))
                $root.dataform.ActionConfig.ViewConfig.encode(message.view, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.incrementalTable != null && Object.hasOwnProperty.call(message, "incrementalTable"))
                $root.dataform.ActionConfig.IncrementalTableConfig.encode(message.incrementalTable, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.assertion != null && Object.hasOwnProperty.call(message, "assertion"))
                $root.dataform.ActionConfig.AssertionConfig.encode(message.assertion, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.dataform.ActionConfig.OperationConfig.encode(message.operation, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.declaration != null && Object.hasOwnProperty.call(message, "declaration"))
                $root.dataform.ActionConfig.DeclarationConfig.encode(message.declaration, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            if (message.notebook != null && Object.hasOwnProperty.call(message, "notebook"))
                $root.dataform.ActionConfig.NotebookConfig.encode(message.notebook, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.dataPreparation != null && Object.hasOwnProperty.call(message, "dataPreparation"))
                $root.dataform.ActionConfig.DataPreparationConfig.encode(message.dataPreparation, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ActionConfig message, length delimited. Does not implicitly {@link dataform.ActionConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.ActionConfig
         * @static
         * @param {dataform.IActionConfig} message ActionConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ActionConfig.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ActionConfig message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.ActionConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.ActionConfig} ActionConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ActionConfig.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.table = $root.dataform.ActionConfig.TableConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.view = $root.dataform.ActionConfig.ViewConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 3: {
                        message.incrementalTable = $root.dataform.ActionConfig.IncrementalTableConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 4: {
                        message.assertion = $root.dataform.ActionConfig.AssertionConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 5: {
                        message.operation = $root.dataform.ActionConfig.OperationConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 6: {
                        message.declaration = $root.dataform.ActionConfig.DeclarationConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 7: {
                        message.notebook = $root.dataform.ActionConfig.NotebookConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 8: {
                        message.dataPreparation = $root.dataform.ActionConfig.DataPreparationConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ActionConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.ActionConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.ActionConfig} ActionConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ActionConfig.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ActionConfig message.
         * @function verify
         * @memberof dataform.ActionConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ActionConfig.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            let properties = {};
            if (message.table != null && Object.hasOwnProperty.call(message, "table")) {
                properties.action = 1;
                {
                    let error = $root.dataform.ActionConfig.TableConfig.verify(message.table, long + 1);
                    if (error)
                        return "table." + error;
                }
            }
            if (message.view != null && Object.hasOwnProperty.call(message, "view")) {
                if (properties.action === 1)
                    return "action: multiple values";
                properties.action = 1;
                {
                    let error = $root.dataform.ActionConfig.ViewConfig.verify(message.view, long + 1);
                    if (error)
                        return "view." + error;
                }
            }
            if (message.incrementalTable != null && Object.hasOwnProperty.call(message, "incrementalTable")) {
                if (properties.action === 1)
                    return "action: multiple values";
                properties.action = 1;
                {
                    let error = $root.dataform.ActionConfig.IncrementalTableConfig.verify(message.incrementalTable, long + 1);
                    if (error)
                        return "incrementalTable." + error;
                }
            }
            if (message.assertion != null && Object.hasOwnProperty.call(message, "assertion")) {
                if (properties.action === 1)
                    return "action: multiple values";
                properties.action = 1;
                {
                    let error = $root.dataform.ActionConfig.AssertionConfig.verify(message.assertion, long + 1);
                    if (error)
                        return "assertion." + error;
                }
            }
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation")) {
                if (properties.action === 1)
                    return "action: multiple values";
                properties.action = 1;
                {
                    let error = $root.dataform.ActionConfig.OperationConfig.verify(message.operation, long + 1);
                    if (error)
                        return "operation." + error;
                }
            }
            if (message.declaration != null && Object.hasOwnProperty.call(message, "declaration")) {
                if (properties.action === 1)
                    return "action: multiple values";
                properties.action = 1;
                {
                    let error = $root.dataform.ActionConfig.DeclarationConfig.verify(message.declaration, long + 1);
                    if (error)
                        return "declaration." + error;
                }
            }
            if (message.notebook != null && Object.hasOwnProperty.call(message, "notebook")) {
                if (properties.action === 1)
                    return "action: multiple values";
                properties.action = 1;
                {
                    let error = $root.dataform.ActionConfig.NotebookConfig.verify(message.notebook, long + 1);
                    if (error)
                        return "notebook." + error;
                }
            }
            if (message.dataPreparation != null && Object.hasOwnProperty.call(message, "dataPreparation")) {
                if (properties.action === 1)
                    return "action: multiple values";
                properties.action = 1;
                {
                    let error = $root.dataform.ActionConfig.DataPreparationConfig.verify(message.dataPreparation, long + 1);
                    if (error)
                        return "dataPreparation." + error;
                }
            }
            return null;
        };

        /**
         * Creates an ActionConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.ActionConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.ActionConfig} ActionConfig
         */
        ActionConfig.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.ActionConfig)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.ActionConfig: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.ActionConfig();
            if (object.table != null) {
                if (!$util.isObject(object.table))
                    throw TypeError(".dataform.ActionConfig.table: object expected");
                message.table = $root.dataform.ActionConfig.TableConfig.fromObject(object.table, long + 1);
            }
            if (object.view != null) {
                if (!$util.isObject(object.view))
                    throw TypeError(".dataform.ActionConfig.view: object expected");
                message.view = $root.dataform.ActionConfig.ViewConfig.fromObject(object.view, long + 1);
            }
            if (object.incrementalTable != null) {
                if (!$util.isObject(object.incrementalTable))
                    throw TypeError(".dataform.ActionConfig.incrementalTable: object expected");
                message.incrementalTable = $root.dataform.ActionConfig.IncrementalTableConfig.fromObject(object.incrementalTable, long + 1);
            }
            if (object.assertion != null) {
                if (!$util.isObject(object.assertion))
                    throw TypeError(".dataform.ActionConfig.assertion: object expected");
                message.assertion = $root.dataform.ActionConfig.AssertionConfig.fromObject(object.assertion, long + 1);
            }
            if (object.operation != null) {
                if (!$util.isObject(object.operation))
                    throw TypeError(".dataform.ActionConfig.operation: object expected");
                message.operation = $root.dataform.ActionConfig.OperationConfig.fromObject(object.operation, long + 1);
            }
            if (object.declaration != null) {
                if (!$util.isObject(object.declaration))
                    throw TypeError(".dataform.ActionConfig.declaration: object expected");
                message.declaration = $root.dataform.ActionConfig.DeclarationConfig.fromObject(object.declaration, long + 1);
            }
            if (object.notebook != null) {
                if (!$util.isObject(object.notebook))
                    throw TypeError(".dataform.ActionConfig.notebook: object expected");
                message.notebook = $root.dataform.ActionConfig.NotebookConfig.fromObject(object.notebook, long + 1);
            }
            if (object.dataPreparation != null) {
                if (!$util.isObject(object.dataPreparation))
                    throw TypeError(".dataform.ActionConfig.dataPreparation: object expected");
                message.dataPreparation = $root.dataform.ActionConfig.DataPreparationConfig.fromObject(object.dataPreparation, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an ActionConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.ActionConfig
         * @static
         * @param {dataform.ActionConfig} message ActionConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ActionConfig.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (message.table != null && Object.hasOwnProperty.call(message, "table")) {
                object.table = $root.dataform.ActionConfig.TableConfig.toObject(message.table, options, q + 1);
                if (options.oneofs)
                    object.action = "table";
            }
            if (message.view != null && Object.hasOwnProperty.call(message, "view")) {
                object.view = $root.dataform.ActionConfig.ViewConfig.toObject(message.view, options, q + 1);
                if (options.oneofs)
                    object.action = "view";
            }
            if (message.incrementalTable != null && Object.hasOwnProperty.call(message, "incrementalTable")) {
                object.incrementalTable = $root.dataform.ActionConfig.IncrementalTableConfig.toObject(message.incrementalTable, options, q + 1);
                if (options.oneofs)
                    object.action = "incrementalTable";
            }
            if (message.assertion != null && Object.hasOwnProperty.call(message, "assertion")) {
                object.assertion = $root.dataform.ActionConfig.AssertionConfig.toObject(message.assertion, options, q + 1);
                if (options.oneofs)
                    object.action = "assertion";
            }
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation")) {
                object.operation = $root.dataform.ActionConfig.OperationConfig.toObject(message.operation, options, q + 1);
                if (options.oneofs)
                    object.action = "operation";
            }
            if (message.declaration != null && Object.hasOwnProperty.call(message, "declaration")) {
                object.declaration = $root.dataform.ActionConfig.DeclarationConfig.toObject(message.declaration, options, q + 1);
                if (options.oneofs)
                    object.action = "declaration";
            }
            if (message.notebook != null && Object.hasOwnProperty.call(message, "notebook")) {
                object.notebook = $root.dataform.ActionConfig.NotebookConfig.toObject(message.notebook, options, q + 1);
                if (options.oneofs)
                    object.action = "notebook";
            }
            if (message.dataPreparation != null && Object.hasOwnProperty.call(message, "dataPreparation")) {
                object.dataPreparation = $root.dataform.ActionConfig.DataPreparationConfig.toObject(message.dataPreparation, options, q + 1);
                if (options.oneofs)
                    object.action = "dataPreparation";
            }
            return object;
        };

        /**
         * Converts this ActionConfig to JSON.
         * @function toJSON
         * @memberof dataform.ActionConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ActionConfig.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ActionConfig
         * @function getTypeUrl
         * @memberof dataform.ActionConfig
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ActionConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.ActionConfig";
        };

        ActionConfig.Target = (function() {

            /**
             * Properties of a Target.
             * @memberof dataform.ActionConfig
             * @interface ITarget
             * @property {string|null} [project] Target project
             * @property {string|null} [dataset] Target dataset
             * @property {string|null} [name] Target name
             * @property {boolean|null} [includeDependentAssertions] Target includeDependentAssertions
             */

            /**
             * Constructs a new Target.
             * @memberof dataform.ActionConfig
             * @classdesc Represents a Target.
             * @implements ITarget
             * @constructor
             * @param {dataform.ActionConfig.ITarget=} [properties] Properties to set
             */
            function Target(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Target project.
             * @member {string} project
             * @memberof dataform.ActionConfig.Target
             * @instance
             */
            Target.prototype.project = "";

            /**
             * Target dataset.
             * @member {string} dataset
             * @memberof dataform.ActionConfig.Target
             * @instance
             */
            Target.prototype.dataset = "";

            /**
             * Target name.
             * @member {string} name
             * @memberof dataform.ActionConfig.Target
             * @instance
             */
            Target.prototype.name = "";

            /**
             * Target includeDependentAssertions.
             * @member {boolean} includeDependentAssertions
             * @memberof dataform.ActionConfig.Target
             * @instance
             */
            Target.prototype.includeDependentAssertions = false;

            /**
             * Creates a new Target instance using the specified properties.
             * @function create
             * @memberof dataform.ActionConfig.Target
             * @static
             * @param {dataform.ActionConfig.ITarget=} [properties] Properties to set
             * @returns {dataform.ActionConfig.Target} Target instance
             */
            Target.create = function create(properties) {
                return new Target(properties);
            };

            /**
             * Encodes the specified Target message. Does not implicitly {@link dataform.ActionConfig.Target.verify|verify} messages.
             * @function encode
             * @memberof dataform.ActionConfig.Target
             * @static
             * @param {dataform.ActionConfig.ITarget} message Target message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Target.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.project);
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.dataset);
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.name);
                if (message.includeDependentAssertions != null && Object.hasOwnProperty.call(message, "includeDependentAssertions"))
                    writer.uint32(/* id 5, wireType 0 =*/40).bool(message.includeDependentAssertions);
                return writer;
            };

            /**
             * Encodes the specified Target message, length delimited. Does not implicitly {@link dataform.ActionConfig.Target.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.ActionConfig.Target
             * @static
             * @param {dataform.ActionConfig.ITarget} message Target message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Target.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a Target message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.ActionConfig.Target
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.ActionConfig.Target} Target
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Target.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig.Target();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.project = reader.string();
                            break;
                        }
                    case 2: {
                            message.dataset = reader.string();
                            break;
                        }
                    case 4: {
                            message.name = reader.string();
                            break;
                        }
                    case 5: {
                            message.includeDependentAssertions = reader.bool();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Target message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.ActionConfig.Target
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.ActionConfig.Target} Target
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Target.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Target message.
             * @function verify
             * @memberof dataform.ActionConfig.Target
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Target.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    if (!$util.isString(message.project))
                        return "project: string expected";
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    if (!$util.isString(message.dataset))
                        return "dataset: string expected";
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.includeDependentAssertions != null && Object.hasOwnProperty.call(message, "includeDependentAssertions"))
                    if (typeof message.includeDependentAssertions !== "boolean")
                        return "includeDependentAssertions: boolean expected";
                return null;
            };

            /**
             * Creates a Target message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.ActionConfig.Target
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.ActionConfig.Target} Target
             */
            Target.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.ActionConfig.Target)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.ActionConfig.Target: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.ActionConfig.Target();
                if (object.project != null)
                    message.project = String(object.project);
                if (object.dataset != null)
                    message.dataset = String(object.dataset);
                if (object.name != null)
                    message.name = String(object.name);
                if (object.includeDependentAssertions != null)
                    message.includeDependentAssertions = Boolean(object.includeDependentAssertions);
                return message;
            };

            /**
             * Creates a plain object from a Target message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.ActionConfig.Target
             * @static
             * @param {dataform.ActionConfig.Target} message Target
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Target.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.project = "";
                    object.dataset = "";
                    object.name = "";
                    object.includeDependentAssertions = false;
                }
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    object.project = message.project;
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    object.dataset = message.dataset;
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.includeDependentAssertions != null && Object.hasOwnProperty.call(message, "includeDependentAssertions"))
                    object.includeDependentAssertions = message.includeDependentAssertions;
                return object;
            };

            /**
             * Converts this Target to JSON.
             * @function toJSON
             * @memberof dataform.ActionConfig.Target
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Target.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Target
             * @function getTypeUrl
             * @memberof dataform.ActionConfig.Target
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Target.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.ActionConfig.Target";
            };

            return Target;
        })();

        ActionConfig.ColumnDescriptor = (function() {

            /**
             * Properties of a ColumnDescriptor.
             * @memberof dataform.ActionConfig
             * @interface IColumnDescriptor
             * @property {Array.<string>|null} [path] ColumnDescriptor path
             * @property {string|null} [description] ColumnDescriptor description
             * @property {Array.<string>|null} [bigqueryPolicyTags] ColumnDescriptor bigqueryPolicyTags
             * @property {Array.<string>|null} [tags] ColumnDescriptor tags
             */

            /**
             * Constructs a new ColumnDescriptor.
             * @memberof dataform.ActionConfig
             * @classdesc Represents a ColumnDescriptor.
             * @implements IColumnDescriptor
             * @constructor
             * @param {dataform.ActionConfig.IColumnDescriptor=} [properties] Properties to set
             */
            function ColumnDescriptor(properties) {
                this.path = [];
                this.bigqueryPolicyTags = [];
                this.tags = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ColumnDescriptor path.
             * @member {Array.<string>} path
             * @memberof dataform.ActionConfig.ColumnDescriptor
             * @instance
             */
            ColumnDescriptor.prototype.path = $util.emptyArray;

            /**
             * ColumnDescriptor description.
             * @member {string} description
             * @memberof dataform.ActionConfig.ColumnDescriptor
             * @instance
             */
            ColumnDescriptor.prototype.description = "";

            /**
             * ColumnDescriptor bigqueryPolicyTags.
             * @member {Array.<string>} bigqueryPolicyTags
             * @memberof dataform.ActionConfig.ColumnDescriptor
             * @instance
             */
            ColumnDescriptor.prototype.bigqueryPolicyTags = $util.emptyArray;

            /**
             * ColumnDescriptor tags.
             * @member {Array.<string>} tags
             * @memberof dataform.ActionConfig.ColumnDescriptor
             * @instance
             */
            ColumnDescriptor.prototype.tags = $util.emptyArray;

            /**
             * Creates a new ColumnDescriptor instance using the specified properties.
             * @function create
             * @memberof dataform.ActionConfig.ColumnDescriptor
             * @static
             * @param {dataform.ActionConfig.IColumnDescriptor=} [properties] Properties to set
             * @returns {dataform.ActionConfig.ColumnDescriptor} ColumnDescriptor instance
             */
            ColumnDescriptor.create = function create(properties) {
                return new ColumnDescriptor(properties);
            };

            /**
             * Encodes the specified ColumnDescriptor message. Does not implicitly {@link dataform.ActionConfig.ColumnDescriptor.verify|verify} messages.
             * @function encode
             * @memberof dataform.ActionConfig.ColumnDescriptor
             * @static
             * @param {dataform.ActionConfig.IColumnDescriptor} message ColumnDescriptor message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ColumnDescriptor.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.path != null && message.path.length)
                    for (let i = 0; i < message.path.length; ++i)
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.path[i]);
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.description);
                if (message.bigqueryPolicyTags != null && message.bigqueryPolicyTags.length)
                    for (let i = 0; i < message.bigqueryPolicyTags.length; ++i)
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.bigqueryPolicyTags[i]);
                if (message.tags != null && message.tags.length)
                    for (let i = 0; i < message.tags.length; ++i)
                        writer.uint32(/* id 4, wireType 2 =*/34).string(message.tags[i]);
                return writer;
            };

            /**
             * Encodes the specified ColumnDescriptor message, length delimited. Does not implicitly {@link dataform.ActionConfig.ColumnDescriptor.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.ActionConfig.ColumnDescriptor
             * @static
             * @param {dataform.ActionConfig.IColumnDescriptor} message ColumnDescriptor message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ColumnDescriptor.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ColumnDescriptor message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.ActionConfig.ColumnDescriptor
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.ActionConfig.ColumnDescriptor} ColumnDescriptor
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ColumnDescriptor.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig.ColumnDescriptor();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            if (!(message.path && message.path.length))
                                message.path = [];
                            message.path.push(reader.string());
                            break;
                        }
                    case 2: {
                            message.description = reader.string();
                            break;
                        }
                    case 3: {
                            if (!(message.bigqueryPolicyTags && message.bigqueryPolicyTags.length))
                                message.bigqueryPolicyTags = [];
                            message.bigqueryPolicyTags.push(reader.string());
                            break;
                        }
                    case 4: {
                            if (!(message.tags && message.tags.length))
                                message.tags = [];
                            message.tags.push(reader.string());
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a ColumnDescriptor message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.ActionConfig.ColumnDescriptor
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.ActionConfig.ColumnDescriptor} ColumnDescriptor
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ColumnDescriptor.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ColumnDescriptor message.
             * @function verify
             * @memberof dataform.ActionConfig.ColumnDescriptor
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ColumnDescriptor.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.path != null && Object.hasOwnProperty.call(message, "path")) {
                    if (!Array.isArray(message.path))
                        return "path: array expected";
                    for (let i = 0; i < message.path.length; ++i)
                        if (!$util.isString(message.path[i]))
                            return "path: string[] expected";
                }
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    if (!$util.isString(message.description))
                        return "description: string expected";
                if (message.bigqueryPolicyTags != null && Object.hasOwnProperty.call(message, "bigqueryPolicyTags")) {
                    if (!Array.isArray(message.bigqueryPolicyTags))
                        return "bigqueryPolicyTags: array expected";
                    for (let i = 0; i < message.bigqueryPolicyTags.length; ++i)
                        if (!$util.isString(message.bigqueryPolicyTags[i]))
                            return "bigqueryPolicyTags: string[] expected";
                }
                if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                    if (!Array.isArray(message.tags))
                        return "tags: array expected";
                    for (let i = 0; i < message.tags.length; ++i)
                        if (!$util.isString(message.tags[i]))
                            return "tags: string[] expected";
                }
                return null;
            };

            /**
             * Creates a ColumnDescriptor message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.ActionConfig.ColumnDescriptor
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.ActionConfig.ColumnDescriptor} ColumnDescriptor
             */
            ColumnDescriptor.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.ActionConfig.ColumnDescriptor)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.ActionConfig.ColumnDescriptor: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.ActionConfig.ColumnDescriptor();
                if (object.path) {
                    if (!Array.isArray(object.path))
                        throw TypeError(".dataform.ActionConfig.ColumnDescriptor.path: array expected");
                    message.path = [];
                    for (let i = 0; i < object.path.length; ++i)
                        message.path[i] = String(object.path[i]);
                }
                if (object.description != null)
                    message.description = String(object.description);
                if (object.bigqueryPolicyTags) {
                    if (!Array.isArray(object.bigqueryPolicyTags))
                        throw TypeError(".dataform.ActionConfig.ColumnDescriptor.bigqueryPolicyTags: array expected");
                    message.bigqueryPolicyTags = [];
                    for (let i = 0; i < object.bigqueryPolicyTags.length; ++i)
                        message.bigqueryPolicyTags[i] = String(object.bigqueryPolicyTags[i]);
                }
                if (object.tags) {
                    if (!Array.isArray(object.tags))
                        throw TypeError(".dataform.ActionConfig.ColumnDescriptor.tags: array expected");
                    message.tags = [];
                    for (let i = 0; i < object.tags.length; ++i)
                        message.tags[i] = String(object.tags[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a ColumnDescriptor message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.ActionConfig.ColumnDescriptor
             * @static
             * @param {dataform.ActionConfig.ColumnDescriptor} message ColumnDescriptor
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ColumnDescriptor.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.path = [];
                    object.bigqueryPolicyTags = [];
                    object.tags = [];
                }
                if (options.defaults)
                    object.description = "";
                if (message.path && message.path.length) {
                    object.path = [];
                    for (let j = 0; j < message.path.length; ++j)
                        object.path[j] = message.path[j];
                }
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    object.description = message.description;
                if (message.bigqueryPolicyTags && message.bigqueryPolicyTags.length) {
                    object.bigqueryPolicyTags = [];
                    for (let j = 0; j < message.bigqueryPolicyTags.length; ++j)
                        object.bigqueryPolicyTags[j] = message.bigqueryPolicyTags[j];
                }
                if (message.tags && message.tags.length) {
                    object.tags = [];
                    for (let j = 0; j < message.tags.length; ++j)
                        object.tags[j] = message.tags[j];
                }
                return object;
            };

            /**
             * Converts this ColumnDescriptor to JSON.
             * @function toJSON
             * @memberof dataform.ActionConfig.ColumnDescriptor
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ColumnDescriptor.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ColumnDescriptor
             * @function getTypeUrl
             * @memberof dataform.ActionConfig.ColumnDescriptor
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ColumnDescriptor.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.ActionConfig.ColumnDescriptor";
            };

            return ColumnDescriptor;
        })();

        ActionConfig.TableAssertionsConfig = (function() {

            /**
             * Properties of a TableAssertionsConfig.
             * @memberof dataform.ActionConfig
             * @interface ITableAssertionsConfig
             * @property {Array.<string>|null} [uniqueKey] TableAssertionsConfig uniqueKey
             * @property {Array.<dataform.ActionConfig.TableAssertionsConfig.IUniqueKey>|null} [uniqueKeys] TableAssertionsConfig uniqueKeys
             * @property {Array.<string>|null} [nonNull] TableAssertionsConfig nonNull
             * @property {Array.<string>|null} [rowConditions] TableAssertionsConfig rowConditions
             */

            /**
             * Constructs a new TableAssertionsConfig.
             * @memberof dataform.ActionConfig
             * @classdesc Represents a TableAssertionsConfig.
             * @implements ITableAssertionsConfig
             * @constructor
             * @param {dataform.ActionConfig.ITableAssertionsConfig=} [properties] Properties to set
             */
            function TableAssertionsConfig(properties) {
                this.uniqueKey = [];
                this.uniqueKeys = [];
                this.nonNull = [];
                this.rowConditions = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * TableAssertionsConfig uniqueKey.
             * @member {Array.<string>} uniqueKey
             * @memberof dataform.ActionConfig.TableAssertionsConfig
             * @instance
             */
            TableAssertionsConfig.prototype.uniqueKey = $util.emptyArray;

            /**
             * TableAssertionsConfig uniqueKeys.
             * @member {Array.<dataform.ActionConfig.TableAssertionsConfig.IUniqueKey>} uniqueKeys
             * @memberof dataform.ActionConfig.TableAssertionsConfig
             * @instance
             */
            TableAssertionsConfig.prototype.uniqueKeys = $util.emptyArray;

            /**
             * TableAssertionsConfig nonNull.
             * @member {Array.<string>} nonNull
             * @memberof dataform.ActionConfig.TableAssertionsConfig
             * @instance
             */
            TableAssertionsConfig.prototype.nonNull = $util.emptyArray;

            /**
             * TableAssertionsConfig rowConditions.
             * @member {Array.<string>} rowConditions
             * @memberof dataform.ActionConfig.TableAssertionsConfig
             * @instance
             */
            TableAssertionsConfig.prototype.rowConditions = $util.emptyArray;

            /**
             * Creates a new TableAssertionsConfig instance using the specified properties.
             * @function create
             * @memberof dataform.ActionConfig.TableAssertionsConfig
             * @static
             * @param {dataform.ActionConfig.ITableAssertionsConfig=} [properties] Properties to set
             * @returns {dataform.ActionConfig.TableAssertionsConfig} TableAssertionsConfig instance
             */
            TableAssertionsConfig.create = function create(properties) {
                return new TableAssertionsConfig(properties);
            };

            /**
             * Encodes the specified TableAssertionsConfig message. Does not implicitly {@link dataform.ActionConfig.TableAssertionsConfig.verify|verify} messages.
             * @function encode
             * @memberof dataform.ActionConfig.TableAssertionsConfig
             * @static
             * @param {dataform.ActionConfig.ITableAssertionsConfig} message TableAssertionsConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TableAssertionsConfig.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.uniqueKey != null && message.uniqueKey.length)
                    for (let i = 0; i < message.uniqueKey.length; ++i)
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.uniqueKey[i]);
                if (message.uniqueKeys != null && message.uniqueKeys.length)
                    for (let i = 0; i < message.uniqueKeys.length; ++i)
                        $root.dataform.ActionConfig.TableAssertionsConfig.UniqueKey.encode(message.uniqueKeys[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.nonNull != null && message.nonNull.length)
                    for (let i = 0; i < message.nonNull.length; ++i)
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.nonNull[i]);
                if (message.rowConditions != null && message.rowConditions.length)
                    for (let i = 0; i < message.rowConditions.length; ++i)
                        writer.uint32(/* id 4, wireType 2 =*/34).string(message.rowConditions[i]);
                return writer;
            };

            /**
             * Encodes the specified TableAssertionsConfig message, length delimited. Does not implicitly {@link dataform.ActionConfig.TableAssertionsConfig.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.ActionConfig.TableAssertionsConfig
             * @static
             * @param {dataform.ActionConfig.ITableAssertionsConfig} message TableAssertionsConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TableAssertionsConfig.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a TableAssertionsConfig message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.ActionConfig.TableAssertionsConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.ActionConfig.TableAssertionsConfig} TableAssertionsConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TableAssertionsConfig.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig.TableAssertionsConfig();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            if (!(message.uniqueKey && message.uniqueKey.length))
                                message.uniqueKey = [];
                            message.uniqueKey.push(reader.string());
                            break;
                        }
                    case 2: {
                            if (!(message.uniqueKeys && message.uniqueKeys.length))
                                message.uniqueKeys = [];
                            message.uniqueKeys.push($root.dataform.ActionConfig.TableAssertionsConfig.UniqueKey.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 3: {
                            if (!(message.nonNull && message.nonNull.length))
                                message.nonNull = [];
                            message.nonNull.push(reader.string());
                            break;
                        }
                    case 4: {
                            if (!(message.rowConditions && message.rowConditions.length))
                                message.rowConditions = [];
                            message.rowConditions.push(reader.string());
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a TableAssertionsConfig message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.ActionConfig.TableAssertionsConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.ActionConfig.TableAssertionsConfig} TableAssertionsConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TableAssertionsConfig.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a TableAssertionsConfig message.
             * @function verify
             * @memberof dataform.ActionConfig.TableAssertionsConfig
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            TableAssertionsConfig.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.uniqueKey != null && Object.hasOwnProperty.call(message, "uniqueKey")) {
                    if (!Array.isArray(message.uniqueKey))
                        return "uniqueKey: array expected";
                    for (let i = 0; i < message.uniqueKey.length; ++i)
                        if (!$util.isString(message.uniqueKey[i]))
                            return "uniqueKey: string[] expected";
                }
                if (message.uniqueKeys != null && Object.hasOwnProperty.call(message, "uniqueKeys")) {
                    if (!Array.isArray(message.uniqueKeys))
                        return "uniqueKeys: array expected";
                    for (let i = 0; i < message.uniqueKeys.length; ++i) {
                        let error = $root.dataform.ActionConfig.TableAssertionsConfig.UniqueKey.verify(message.uniqueKeys[i], long + 1);
                        if (error)
                            return "uniqueKeys." + error;
                    }
                }
                if (message.nonNull != null && Object.hasOwnProperty.call(message, "nonNull")) {
                    if (!Array.isArray(message.nonNull))
                        return "nonNull: array expected";
                    for (let i = 0; i < message.nonNull.length; ++i)
                        if (!$util.isString(message.nonNull[i]))
                            return "nonNull: string[] expected";
                }
                if (message.rowConditions != null && Object.hasOwnProperty.call(message, "rowConditions")) {
                    if (!Array.isArray(message.rowConditions))
                        return "rowConditions: array expected";
                    for (let i = 0; i < message.rowConditions.length; ++i)
                        if (!$util.isString(message.rowConditions[i]))
                            return "rowConditions: string[] expected";
                }
                return null;
            };

            /**
             * Creates a TableAssertionsConfig message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.ActionConfig.TableAssertionsConfig
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.ActionConfig.TableAssertionsConfig} TableAssertionsConfig
             */
            TableAssertionsConfig.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.ActionConfig.TableAssertionsConfig)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.ActionConfig.TableAssertionsConfig: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.ActionConfig.TableAssertionsConfig();
                if (object.uniqueKey) {
                    if (!Array.isArray(object.uniqueKey))
                        throw TypeError(".dataform.ActionConfig.TableAssertionsConfig.uniqueKey: array expected");
                    message.uniqueKey = [];
                    for (let i = 0; i < object.uniqueKey.length; ++i)
                        message.uniqueKey[i] = String(object.uniqueKey[i]);
                }
                if (object.uniqueKeys) {
                    if (!Array.isArray(object.uniqueKeys))
                        throw TypeError(".dataform.ActionConfig.TableAssertionsConfig.uniqueKeys: array expected");
                    message.uniqueKeys = [];
                    for (let i = 0; i < object.uniqueKeys.length; ++i) {
                        if (!$util.isObject(object.uniqueKeys[i]))
                            throw TypeError(".dataform.ActionConfig.TableAssertionsConfig.uniqueKeys: object expected");
                        message.uniqueKeys[i] = $root.dataform.ActionConfig.TableAssertionsConfig.UniqueKey.fromObject(object.uniqueKeys[i], long + 1);
                    }
                }
                if (object.nonNull) {
                    if (!Array.isArray(object.nonNull))
                        throw TypeError(".dataform.ActionConfig.TableAssertionsConfig.nonNull: array expected");
                    message.nonNull = [];
                    for (let i = 0; i < object.nonNull.length; ++i)
                        message.nonNull[i] = String(object.nonNull[i]);
                }
                if (object.rowConditions) {
                    if (!Array.isArray(object.rowConditions))
                        throw TypeError(".dataform.ActionConfig.TableAssertionsConfig.rowConditions: array expected");
                    message.rowConditions = [];
                    for (let i = 0; i < object.rowConditions.length; ++i)
                        message.rowConditions[i] = String(object.rowConditions[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a TableAssertionsConfig message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.ActionConfig.TableAssertionsConfig
             * @static
             * @param {dataform.ActionConfig.TableAssertionsConfig} message TableAssertionsConfig
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            TableAssertionsConfig.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.uniqueKey = [];
                    object.uniqueKeys = [];
                    object.nonNull = [];
                    object.rowConditions = [];
                }
                if (message.uniqueKey && message.uniqueKey.length) {
                    object.uniqueKey = [];
                    for (let j = 0; j < message.uniqueKey.length; ++j)
                        object.uniqueKey[j] = message.uniqueKey[j];
                }
                if (message.uniqueKeys && message.uniqueKeys.length) {
                    object.uniqueKeys = [];
                    for (let j = 0; j < message.uniqueKeys.length; ++j)
                        object.uniqueKeys[j] = $root.dataform.ActionConfig.TableAssertionsConfig.UniqueKey.toObject(message.uniqueKeys[j], options, q + 1);
                }
                if (message.nonNull && message.nonNull.length) {
                    object.nonNull = [];
                    for (let j = 0; j < message.nonNull.length; ++j)
                        object.nonNull[j] = message.nonNull[j];
                }
                if (message.rowConditions && message.rowConditions.length) {
                    object.rowConditions = [];
                    for (let j = 0; j < message.rowConditions.length; ++j)
                        object.rowConditions[j] = message.rowConditions[j];
                }
                return object;
            };

            /**
             * Converts this TableAssertionsConfig to JSON.
             * @function toJSON
             * @memberof dataform.ActionConfig.TableAssertionsConfig
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            TableAssertionsConfig.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for TableAssertionsConfig
             * @function getTypeUrl
             * @memberof dataform.ActionConfig.TableAssertionsConfig
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            TableAssertionsConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.ActionConfig.TableAssertionsConfig";
            };

            TableAssertionsConfig.UniqueKey = (function() {

                /**
                 * Properties of an UniqueKey.
                 * @memberof dataform.ActionConfig.TableAssertionsConfig
                 * @interface IUniqueKey
                 * @property {Array.<string>|null} [uniqueKey] UniqueKey uniqueKey
                 */

                /**
                 * Constructs a new UniqueKey.
                 * @memberof dataform.ActionConfig.TableAssertionsConfig
                 * @classdesc Represents an UniqueKey.
                 * @implements IUniqueKey
                 * @constructor
                 * @param {dataform.ActionConfig.TableAssertionsConfig.IUniqueKey=} [properties] Properties to set
                 */
                function UniqueKey(properties) {
                    this.uniqueKey = [];
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * UniqueKey uniqueKey.
                 * @member {Array.<string>} uniqueKey
                 * @memberof dataform.ActionConfig.TableAssertionsConfig.UniqueKey
                 * @instance
                 */
                UniqueKey.prototype.uniqueKey = $util.emptyArray;

                /**
                 * Creates a new UniqueKey instance using the specified properties.
                 * @function create
                 * @memberof dataform.ActionConfig.TableAssertionsConfig.UniqueKey
                 * @static
                 * @param {dataform.ActionConfig.TableAssertionsConfig.IUniqueKey=} [properties] Properties to set
                 * @returns {dataform.ActionConfig.TableAssertionsConfig.UniqueKey} UniqueKey instance
                 */
                UniqueKey.create = function create(properties) {
                    return new UniqueKey(properties);
                };

                /**
                 * Encodes the specified UniqueKey message. Does not implicitly {@link dataform.ActionConfig.TableAssertionsConfig.UniqueKey.verify|verify} messages.
                 * @function encode
                 * @memberof dataform.ActionConfig.TableAssertionsConfig.UniqueKey
                 * @static
                 * @param {dataform.ActionConfig.TableAssertionsConfig.IUniqueKey} message UniqueKey message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                UniqueKey.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.uniqueKey != null && message.uniqueKey.length)
                        for (let i = 0; i < message.uniqueKey.length; ++i)
                            writer.uint32(/* id 1, wireType 2 =*/10).string(message.uniqueKey[i]);
                    return writer;
                };

                /**
                 * Encodes the specified UniqueKey message, length delimited. Does not implicitly {@link dataform.ActionConfig.TableAssertionsConfig.UniqueKey.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof dataform.ActionConfig.TableAssertionsConfig.UniqueKey
                 * @static
                 * @param {dataform.ActionConfig.TableAssertionsConfig.IUniqueKey} message UniqueKey message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                UniqueKey.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes an UniqueKey message from the specified reader or buffer.
                 * @function decode
                 * @memberof dataform.ActionConfig.TableAssertionsConfig.UniqueKey
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {dataform.ActionConfig.TableAssertionsConfig.UniqueKey} UniqueKey
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                UniqueKey.decode = function decode(reader, length, error, long) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (long === undefined)
                        long = 0;
                    if (long > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig.TableAssertionsConfig.UniqueKey();
                    while (reader.pos < end) {
                        let tag = reader.uint32();
                        if (tag === error)
                            break;
                        switch (tag >>> 3) {
                        case 1: {
                                if (!(message.uniqueKey && message.uniqueKey.length))
                                    message.uniqueKey = [];
                                message.uniqueKey.push(reader.string());
                                break;
                            }
                        default:
                            reader.skipType(tag & 7, long);
                            break;
                        }
                    }
                    return message;
                };

                /**
                 * Decodes an UniqueKey message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof dataform.ActionConfig.TableAssertionsConfig.UniqueKey
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {dataform.ActionConfig.TableAssertionsConfig.UniqueKey} UniqueKey
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                UniqueKey.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies an UniqueKey message.
                 * @function verify
                 * @memberof dataform.ActionConfig.TableAssertionsConfig.UniqueKey
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                UniqueKey.verify = function verify(message, long) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (long === undefined)
                        long = 0;
                    if (long > $util.recursionLimit)
                        return "maximum nesting depth exceeded";
                    if (message.uniqueKey != null && Object.hasOwnProperty.call(message, "uniqueKey")) {
                        if (!Array.isArray(message.uniqueKey))
                            return "uniqueKey: array expected";
                        for (let i = 0; i < message.uniqueKey.length; ++i)
                            if (!$util.isString(message.uniqueKey[i]))
                                return "uniqueKey: string[] expected";
                    }
                    return null;
                };

                /**
                 * Creates an UniqueKey message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof dataform.ActionConfig.TableAssertionsConfig.UniqueKey
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {dataform.ActionConfig.TableAssertionsConfig.UniqueKey} UniqueKey
                 */
                UniqueKey.fromObject = function fromObject(object, long) {
                    if (object instanceof $root.dataform.ActionConfig.TableAssertionsConfig.UniqueKey)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".dataform.ActionConfig.TableAssertionsConfig.UniqueKey: object expected");
                    if (long === undefined)
                        long = 0;
                    if (long > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    let message = new $root.dataform.ActionConfig.TableAssertionsConfig.UniqueKey();
                    if (object.uniqueKey) {
                        if (!Array.isArray(object.uniqueKey))
                            throw TypeError(".dataform.ActionConfig.TableAssertionsConfig.UniqueKey.uniqueKey: array expected");
                        message.uniqueKey = [];
                        for (let i = 0; i < object.uniqueKey.length; ++i)
                            message.uniqueKey[i] = String(object.uniqueKey[i]);
                    }
                    return message;
                };

                /**
                 * Creates a plain object from an UniqueKey message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof dataform.ActionConfig.TableAssertionsConfig.UniqueKey
                 * @static
                 * @param {dataform.ActionConfig.TableAssertionsConfig.UniqueKey} message UniqueKey
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                UniqueKey.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let object = {};
                    if (options.arrays || options.defaults)
                        object.uniqueKey = [];
                    if (message.uniqueKey && message.uniqueKey.length) {
                        object.uniqueKey = [];
                        for (let j = 0; j < message.uniqueKey.length; ++j)
                            object.uniqueKey[j] = message.uniqueKey[j];
                    }
                    return object;
                };

                /**
                 * Converts this UniqueKey to JSON.
                 * @function toJSON
                 * @memberof dataform.ActionConfig.TableAssertionsConfig.UniqueKey
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                UniqueKey.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for UniqueKey
                 * @function getTypeUrl
                 * @memberof dataform.ActionConfig.TableAssertionsConfig.UniqueKey
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                UniqueKey.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/dataform.ActionConfig.TableAssertionsConfig.UniqueKey";
                };

                return UniqueKey;
            })();

            return TableAssertionsConfig;
        })();

        ActionConfig.IcebergTableConfig = (function() {

            /**
             * Properties of an IcebergTableConfig.
             * @memberof dataform.ActionConfig
             * @interface IIcebergTableConfig
             * @property {dataform.ActionConfig.IcebergTableConfig.FileFormat|null} [fileFormat] IcebergTableConfig fileFormat
             * @property {string|null} [connection] IcebergTableConfig connection
             * @property {string|null} [bucketName] IcebergTableConfig bucketName
             * @property {string|null} [tableFolderRoot] IcebergTableConfig tableFolderRoot
             * @property {string|null} [tableFolderSubpath] IcebergTableConfig tableFolderSubpath
             */

            /**
             * Constructs a new IcebergTableConfig.
             * @memberof dataform.ActionConfig
             * @classdesc Represents an IcebergTableConfig.
             * @implements IIcebergTableConfig
             * @constructor
             * @param {dataform.ActionConfig.IIcebergTableConfig=} [properties] Properties to set
             */
            function IcebergTableConfig(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * IcebergTableConfig fileFormat.
             * @member {dataform.ActionConfig.IcebergTableConfig.FileFormat} fileFormat
             * @memberof dataform.ActionConfig.IcebergTableConfig
             * @instance
             */
            IcebergTableConfig.prototype.fileFormat = 0;

            /**
             * IcebergTableConfig connection.
             * @member {string} connection
             * @memberof dataform.ActionConfig.IcebergTableConfig
             * @instance
             */
            IcebergTableConfig.prototype.connection = "";

            /**
             * IcebergTableConfig bucketName.
             * @member {string} bucketName
             * @memberof dataform.ActionConfig.IcebergTableConfig
             * @instance
             */
            IcebergTableConfig.prototype.bucketName = "";

            /**
             * IcebergTableConfig tableFolderRoot.
             * @member {string} tableFolderRoot
             * @memberof dataform.ActionConfig.IcebergTableConfig
             * @instance
             */
            IcebergTableConfig.prototype.tableFolderRoot = "";

            /**
             * IcebergTableConfig tableFolderSubpath.
             * @member {string} tableFolderSubpath
             * @memberof dataform.ActionConfig.IcebergTableConfig
             * @instance
             */
            IcebergTableConfig.prototype.tableFolderSubpath = "";

            /**
             * Creates a new IcebergTableConfig instance using the specified properties.
             * @function create
             * @memberof dataform.ActionConfig.IcebergTableConfig
             * @static
             * @param {dataform.ActionConfig.IIcebergTableConfig=} [properties] Properties to set
             * @returns {dataform.ActionConfig.IcebergTableConfig} IcebergTableConfig instance
             */
            IcebergTableConfig.create = function create(properties) {
                return new IcebergTableConfig(properties);
            };

            /**
             * Encodes the specified IcebergTableConfig message. Does not implicitly {@link dataform.ActionConfig.IcebergTableConfig.verify|verify} messages.
             * @function encode
             * @memberof dataform.ActionConfig.IcebergTableConfig
             * @static
             * @param {dataform.ActionConfig.IIcebergTableConfig} message IcebergTableConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            IcebergTableConfig.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.fileFormat != null && Object.hasOwnProperty.call(message, "fileFormat"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.fileFormat);
                if (message.connection != null && Object.hasOwnProperty.call(message, "connection"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.connection);
                if (message.bucketName != null && Object.hasOwnProperty.call(message, "bucketName"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.bucketName);
                if (message.tableFolderRoot != null && Object.hasOwnProperty.call(message, "tableFolderRoot"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.tableFolderRoot);
                if (message.tableFolderSubpath != null && Object.hasOwnProperty.call(message, "tableFolderSubpath"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.tableFolderSubpath);
                return writer;
            };

            /**
             * Encodes the specified IcebergTableConfig message, length delimited. Does not implicitly {@link dataform.ActionConfig.IcebergTableConfig.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.ActionConfig.IcebergTableConfig
             * @static
             * @param {dataform.ActionConfig.IIcebergTableConfig} message IcebergTableConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            IcebergTableConfig.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an IcebergTableConfig message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.ActionConfig.IcebergTableConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.ActionConfig.IcebergTableConfig} IcebergTableConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            IcebergTableConfig.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig.IcebergTableConfig();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.fileFormat = reader.int32();
                            break;
                        }
                    case 2: {
                            message.connection = reader.string();
                            break;
                        }
                    case 3: {
                            message.bucketName = reader.string();
                            break;
                        }
                    case 4: {
                            message.tableFolderRoot = reader.string();
                            break;
                        }
                    case 5: {
                            message.tableFolderSubpath = reader.string();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an IcebergTableConfig message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.ActionConfig.IcebergTableConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.ActionConfig.IcebergTableConfig} IcebergTableConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            IcebergTableConfig.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an IcebergTableConfig message.
             * @function verify
             * @memberof dataform.ActionConfig.IcebergTableConfig
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            IcebergTableConfig.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.fileFormat != null && Object.hasOwnProperty.call(message, "fileFormat"))
                    switch (message.fileFormat) {
                    default:
                        return "fileFormat: enum value expected";
                    case 0:
                    case 1:
                        break;
                    }
                if (message.connection != null && Object.hasOwnProperty.call(message, "connection"))
                    if (!$util.isString(message.connection))
                        return "connection: string expected";
                if (message.bucketName != null && Object.hasOwnProperty.call(message, "bucketName"))
                    if (!$util.isString(message.bucketName))
                        return "bucketName: string expected";
                if (message.tableFolderRoot != null && Object.hasOwnProperty.call(message, "tableFolderRoot"))
                    if (!$util.isString(message.tableFolderRoot))
                        return "tableFolderRoot: string expected";
                if (message.tableFolderSubpath != null && Object.hasOwnProperty.call(message, "tableFolderSubpath"))
                    if (!$util.isString(message.tableFolderSubpath))
                        return "tableFolderSubpath: string expected";
                return null;
            };

            /**
             * Creates an IcebergTableConfig message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.ActionConfig.IcebergTableConfig
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.ActionConfig.IcebergTableConfig} IcebergTableConfig
             */
            IcebergTableConfig.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.ActionConfig.IcebergTableConfig)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.ActionConfig.IcebergTableConfig: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.ActionConfig.IcebergTableConfig();
                switch (object.fileFormat) {
                default:
                    if (typeof object.fileFormat === "number") {
                        message.fileFormat = object.fileFormat;
                        break;
                    }
                    break;
                case "FILE_FORMAT_UNSPECIFIED":
                case 0:
                    message.fileFormat = 0;
                    break;
                case "PARQUET":
                case 1:
                    message.fileFormat = 1;
                    break;
                }
                if (object.connection != null)
                    message.connection = String(object.connection);
                if (object.bucketName != null)
                    message.bucketName = String(object.bucketName);
                if (object.tableFolderRoot != null)
                    message.tableFolderRoot = String(object.tableFolderRoot);
                if (object.tableFolderSubpath != null)
                    message.tableFolderSubpath = String(object.tableFolderSubpath);
                return message;
            };

            /**
             * Creates a plain object from an IcebergTableConfig message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.ActionConfig.IcebergTableConfig
             * @static
             * @param {dataform.ActionConfig.IcebergTableConfig} message IcebergTableConfig
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            IcebergTableConfig.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.fileFormat = options.enums === String ? "FILE_FORMAT_UNSPECIFIED" : 0;
                    object.connection = "";
                    object.bucketName = "";
                    object.tableFolderRoot = "";
                    object.tableFolderSubpath = "";
                }
                if (message.fileFormat != null && Object.hasOwnProperty.call(message, "fileFormat"))
                    object.fileFormat = options.enums === String ? $root.dataform.ActionConfig.IcebergTableConfig.FileFormat[message.fileFormat] === undefined ? message.fileFormat : $root.dataform.ActionConfig.IcebergTableConfig.FileFormat[message.fileFormat] : message.fileFormat;
                if (message.connection != null && Object.hasOwnProperty.call(message, "connection"))
                    object.connection = message.connection;
                if (message.bucketName != null && Object.hasOwnProperty.call(message, "bucketName"))
                    object.bucketName = message.bucketName;
                if (message.tableFolderRoot != null && Object.hasOwnProperty.call(message, "tableFolderRoot"))
                    object.tableFolderRoot = message.tableFolderRoot;
                if (message.tableFolderSubpath != null && Object.hasOwnProperty.call(message, "tableFolderSubpath"))
                    object.tableFolderSubpath = message.tableFolderSubpath;
                return object;
            };

            /**
             * Converts this IcebergTableConfig to JSON.
             * @function toJSON
             * @memberof dataform.ActionConfig.IcebergTableConfig
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            IcebergTableConfig.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for IcebergTableConfig
             * @function getTypeUrl
             * @memberof dataform.ActionConfig.IcebergTableConfig
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            IcebergTableConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.ActionConfig.IcebergTableConfig";
            };

            /**
             * FileFormat enum.
             * @name dataform.ActionConfig.IcebergTableConfig.FileFormat
             * @enum {number}
             * @property {number} FILE_FORMAT_UNSPECIFIED=0 FILE_FORMAT_UNSPECIFIED value
             * @property {number} PARQUET=1 PARQUET value
             */
            IcebergTableConfig.FileFormat = (function() {
                const valuesById = {}, values = Object.create(valuesById);
                values[valuesById[0] = "FILE_FORMAT_UNSPECIFIED"] = 0;
                values[valuesById[1] = "PARQUET"] = 1;
                return values;
            })();

            return IcebergTableConfig;
        })();

        ActionConfig.TableConfig = (function() {

            /**
             * Properties of a TableConfig.
             * @memberof dataform.ActionConfig
             * @interface ITableConfig
             * @property {string|null} [name] TableConfig name
             * @property {string|null} [dataset] TableConfig dataset
             * @property {string|null} [project] TableConfig project
             * @property {Array.<dataform.ActionConfig.ITarget>|null} [dependencyTargets] TableConfig dependencyTargets
             * @property {string|null} [filename] TableConfig filename
             * @property {Array.<string>|null} [tags] TableConfig tags
             * @property {boolean|null} [disabled] TableConfig disabled
             * @property {Array.<string>|null} [preOperations] TableConfig preOperations
             * @property {Array.<string>|null} [postOperations] TableConfig postOperations
             * @property {string|null} [description] TableConfig description
             * @property {Array.<dataform.ActionConfig.IColumnDescriptor>|null} [columns] TableConfig columns
             * @property {string|null} [partitionBy] TableConfig partitionBy
             * @property {number|null} [partitionExpirationDays] TableConfig partitionExpirationDays
             * @property {boolean|null} [requirePartitionFilter] TableConfig requirePartitionFilter
             * @property {Array.<string>|null} [clusterBy] TableConfig clusterBy
             * @property {Object.<string,string>|null} [labels] TableConfig labels
             * @property {Object.<string,string>|null} [additionalOptions] TableConfig additionalOptions
             * @property {boolean|null} [dependOnDependencyAssertions] TableConfig dependOnDependencyAssertions
             * @property {dataform.ActionConfig.ITableAssertionsConfig|null} [assertions] TableConfig assertions
             * @property {boolean|null} [hermetic] TableConfig hermetic
             * @property {dataform.ActionConfig.IIcebergTableConfig|null} [iceberg] TableConfig iceberg
             * @property {dataform.ActionConfig.IMetadata|null} [metadata] TableConfig metadata
             * @property {string|null} [reservation] TableConfig reservation
             * @property {boolean|null} [preserveGovernanceControls] TableConfig preserveGovernanceControls
             */

            /**
             * Constructs a new TableConfig.
             * @memberof dataform.ActionConfig
             * @classdesc Represents a TableConfig.
             * @implements ITableConfig
             * @constructor
             * @param {dataform.ActionConfig.ITableConfig=} [properties] Properties to set
             */
            function TableConfig(properties) {
                this.dependencyTargets = [];
                this.tags = [];
                this.preOperations = [];
                this.postOperations = [];
                this.columns = [];
                this.clusterBy = [];
                this.labels = {};
                this.additionalOptions = {};
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * TableConfig name.
             * @member {string} name
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.name = "";

            /**
             * TableConfig dataset.
             * @member {string} dataset
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.dataset = "";

            /**
             * TableConfig project.
             * @member {string} project
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.project = "";

            /**
             * TableConfig dependencyTargets.
             * @member {Array.<dataform.ActionConfig.ITarget>} dependencyTargets
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.dependencyTargets = $util.emptyArray;

            /**
             * TableConfig filename.
             * @member {string} filename
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.filename = "";

            /**
             * TableConfig tags.
             * @member {Array.<string>} tags
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.tags = $util.emptyArray;

            /**
             * TableConfig disabled.
             * @member {boolean} disabled
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.disabled = false;

            /**
             * TableConfig preOperations.
             * @member {Array.<string>} preOperations
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.preOperations = $util.emptyArray;

            /**
             * TableConfig postOperations.
             * @member {Array.<string>} postOperations
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.postOperations = $util.emptyArray;

            /**
             * TableConfig description.
             * @member {string} description
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.description = "";

            /**
             * TableConfig columns.
             * @member {Array.<dataform.ActionConfig.IColumnDescriptor>} columns
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.columns = $util.emptyArray;

            /**
             * TableConfig partitionBy.
             * @member {string} partitionBy
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.partitionBy = "";

            /**
             * TableConfig partitionExpirationDays.
             * @member {number} partitionExpirationDays
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.partitionExpirationDays = 0;

            /**
             * TableConfig requirePartitionFilter.
             * @member {boolean} requirePartitionFilter
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.requirePartitionFilter = false;

            /**
             * TableConfig clusterBy.
             * @member {Array.<string>} clusterBy
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.clusterBy = $util.emptyArray;

            /**
             * TableConfig labels.
             * @member {Object.<string,string>} labels
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.labels = $util.emptyObject;

            /**
             * TableConfig additionalOptions.
             * @member {Object.<string,string>} additionalOptions
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.additionalOptions = $util.emptyObject;

            /**
             * TableConfig dependOnDependencyAssertions.
             * @member {boolean} dependOnDependencyAssertions
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.dependOnDependencyAssertions = false;

            /**
             * TableConfig assertions.
             * @member {dataform.ActionConfig.ITableAssertionsConfig|null|undefined} assertions
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.assertions = null;

            /**
             * TableConfig hermetic.
             * @member {boolean} hermetic
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.hermetic = false;

            /**
             * TableConfig iceberg.
             * @member {dataform.ActionConfig.IIcebergTableConfig|null|undefined} iceberg
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.iceberg = null;

            /**
             * TableConfig metadata.
             * @member {dataform.ActionConfig.IMetadata|null|undefined} metadata
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.metadata = null;

            /**
             * TableConfig reservation.
             * @member {string} reservation
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.reservation = "";

            /**
             * TableConfig preserveGovernanceControls.
             * @member {boolean|null|undefined} preserveGovernanceControls
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             */
            TableConfig.prototype.preserveGovernanceControls = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            Object.defineProperty(TableConfig.prototype, "_preserveGovernanceControls", {
                get: $util.oneOfGetter($oneOfFields = ["preserveGovernanceControls"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new TableConfig instance using the specified properties.
             * @function create
             * @memberof dataform.ActionConfig.TableConfig
             * @static
             * @param {dataform.ActionConfig.ITableConfig=} [properties] Properties to set
             * @returns {dataform.ActionConfig.TableConfig} TableConfig instance
             */
            TableConfig.create = function create(properties) {
                return new TableConfig(properties);
            };

            /**
             * Encodes the specified TableConfig message. Does not implicitly {@link dataform.ActionConfig.TableConfig.verify|verify} messages.
             * @function encode
             * @memberof dataform.ActionConfig.TableConfig
             * @static
             * @param {dataform.ActionConfig.ITableConfig} message TableConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TableConfig.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.dataset);
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.project);
                if (message.dependencyTargets != null && message.dependencyTargets.length)
                    for (let i = 0; i < message.dependencyTargets.length; ++i)
                        $root.dataform.ActionConfig.Target.encode(message.dependencyTargets[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.filename);
                if (message.tags != null && message.tags.length)
                    for (let i = 0; i < message.tags.length; ++i)
                        writer.uint32(/* id 6, wireType 2 =*/50).string(message.tags[i]);
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    writer.uint32(/* id 7, wireType 0 =*/56).bool(message.disabled);
                if (message.preOperations != null && message.preOperations.length)
                    for (let i = 0; i < message.preOperations.length; ++i)
                        writer.uint32(/* id 8, wireType 2 =*/66).string(message.preOperations[i]);
                if (message.postOperations != null && message.postOperations.length)
                    for (let i = 0; i < message.postOperations.length; ++i)
                        writer.uint32(/* id 9, wireType 2 =*/74).string(message.postOperations[i]);
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 10, wireType 2 =*/82).string(message.description);
                if (message.columns != null && message.columns.length)
                    for (let i = 0; i < message.columns.length; ++i)
                        $root.dataform.ActionConfig.ColumnDescriptor.encode(message.columns[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
                if (message.partitionBy != null && Object.hasOwnProperty.call(message, "partitionBy"))
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.partitionBy);
                if (message.partitionExpirationDays != null && Object.hasOwnProperty.call(message, "partitionExpirationDays"))
                    writer.uint32(/* id 13, wireType 0 =*/104).int32(message.partitionExpirationDays);
                if (message.requirePartitionFilter != null && Object.hasOwnProperty.call(message, "requirePartitionFilter"))
                    writer.uint32(/* id 14, wireType 0 =*/112).bool(message.requirePartitionFilter);
                if (message.clusterBy != null && message.clusterBy.length)
                    for (let i = 0; i < message.clusterBy.length; ++i)
                        writer.uint32(/* id 15, wireType 2 =*/122).string(message.clusterBy[i]);
                if (message.labels != null && Object.hasOwnProperty.call(message, "labels"))
                    for (let keys = Object.keys(message.labels), i = 0; i < keys.length; ++i)
                        writer.uint32(/* id 16, wireType 2 =*/130).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.labels[keys[i]]).ldelim();
                if (message.additionalOptions != null && Object.hasOwnProperty.call(message, "additionalOptions"))
                    for (let keys = Object.keys(message.additionalOptions), i = 0; i < keys.length; ++i)
                        writer.uint32(/* id 17, wireType 2 =*/138).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.additionalOptions[keys[i]]).ldelim();
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    writer.uint32(/* id 18, wireType 0 =*/144).bool(message.dependOnDependencyAssertions);
                if (message.assertions != null && Object.hasOwnProperty.call(message, "assertions"))
                    $root.dataform.ActionConfig.TableAssertionsConfig.encode(message.assertions, writer.uint32(/* id 19, wireType 2 =*/154).fork(), q + 1).ldelim();
                if (message.hermetic != null && Object.hasOwnProperty.call(message, "hermetic"))
                    writer.uint32(/* id 20, wireType 0 =*/160).bool(message.hermetic);
                if (message.iceberg != null && Object.hasOwnProperty.call(message, "iceberg"))
                    $root.dataform.ActionConfig.IcebergTableConfig.encode(message.iceberg, writer.uint32(/* id 22, wireType 2 =*/178).fork(), q + 1).ldelim();
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                    $root.dataform.ActionConfig.Metadata.encode(message.metadata, writer.uint32(/* id 23, wireType 2 =*/186).fork(), q + 1).ldelim();
                if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                    writer.uint32(/* id 24, wireType 2 =*/194).string(message.reservation);
                if (message.preserveGovernanceControls != null && Object.hasOwnProperty.call(message, "preserveGovernanceControls"))
                    writer.uint32(/* id 25, wireType 0 =*/200).bool(message.preserveGovernanceControls);
                return writer;
            };

            /**
             * Encodes the specified TableConfig message, length delimited. Does not implicitly {@link dataform.ActionConfig.TableConfig.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.ActionConfig.TableConfig
             * @static
             * @param {dataform.ActionConfig.ITableConfig} message TableConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            TableConfig.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a TableConfig message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.ActionConfig.TableConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.ActionConfig.TableConfig} TableConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TableConfig.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig.TableConfig(), key, value;
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.name = reader.string();
                            break;
                        }
                    case 2: {
                            message.dataset = reader.string();
                            break;
                        }
                    case 3: {
                            message.project = reader.string();
                            break;
                        }
                    case 4: {
                            if (!(message.dependencyTargets && message.dependencyTargets.length))
                                message.dependencyTargets = [];
                            message.dependencyTargets.push($root.dataform.ActionConfig.Target.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 5: {
                            message.filename = reader.string();
                            break;
                        }
                    case 6: {
                            if (!(message.tags && message.tags.length))
                                message.tags = [];
                            message.tags.push(reader.string());
                            break;
                        }
                    case 7: {
                            message.disabled = reader.bool();
                            break;
                        }
                    case 8: {
                            if (!(message.preOperations && message.preOperations.length))
                                message.preOperations = [];
                            message.preOperations.push(reader.string());
                            break;
                        }
                    case 9: {
                            if (!(message.postOperations && message.postOperations.length))
                                message.postOperations = [];
                            message.postOperations.push(reader.string());
                            break;
                        }
                    case 10: {
                            message.description = reader.string();
                            break;
                        }
                    case 11: {
                            if (!(message.columns && message.columns.length))
                                message.columns = [];
                            message.columns.push($root.dataform.ActionConfig.ColumnDescriptor.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 12: {
                            message.partitionBy = reader.string();
                            break;
                        }
                    case 13: {
                            message.partitionExpirationDays = reader.int32();
                            break;
                        }
                    case 14: {
                            message.requirePartitionFilter = reader.bool();
                            break;
                        }
                    case 15: {
                            if (!(message.clusterBy && message.clusterBy.length))
                                message.clusterBy = [];
                            message.clusterBy.push(reader.string());
                            break;
                        }
                    case 16: {
                            if (message.labels === $util.emptyObject)
                                message.labels = {};
                            let end2 = reader.uint32() + reader.pos;
                            key = "";
                            value = "";
                            while (reader.pos < end2) {
                                let tag2 = reader.uint32();
                                switch (tag2 >>> 3) {
                                case 1:
                                    key = reader.string();
                                    break;
                                case 2:
                                    value = reader.string();
                                    break;
                                default:
                                    reader.skipType(tag2 & 7, long);
                                    break;
                                }
                            }
                            if (key === "__proto__")
                                $util.makeProp(message.labels, key);
                            message.labels[key] = value;
                            break;
                        }
                    case 17: {
                            if (message.additionalOptions === $util.emptyObject)
                                message.additionalOptions = {};
                            let end2 = reader.uint32() + reader.pos;
                            key = "";
                            value = "";
                            while (reader.pos < end2) {
                                let tag2 = reader.uint32();
                                switch (tag2 >>> 3) {
                                case 1:
                                    key = reader.string();
                                    break;
                                case 2:
                                    value = reader.string();
                                    break;
                                default:
                                    reader.skipType(tag2 & 7, long);
                                    break;
                                }
                            }
                            if (key === "__proto__")
                                $util.makeProp(message.additionalOptions, key);
                            message.additionalOptions[key] = value;
                            break;
                        }
                    case 18: {
                            message.dependOnDependencyAssertions = reader.bool();
                            break;
                        }
                    case 19: {
                            message.assertions = $root.dataform.ActionConfig.TableAssertionsConfig.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 20: {
                            message.hermetic = reader.bool();
                            break;
                        }
                    case 22: {
                            message.iceberg = $root.dataform.ActionConfig.IcebergTableConfig.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 23: {
                            message.metadata = $root.dataform.ActionConfig.Metadata.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 24: {
                            message.reservation = reader.string();
                            break;
                        }
                    case 25: {
                            message.preserveGovernanceControls = reader.bool();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a TableConfig message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.ActionConfig.TableConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.ActionConfig.TableConfig} TableConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            TableConfig.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a TableConfig message.
             * @function verify
             * @memberof dataform.ActionConfig.TableConfig
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            TableConfig.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    if (!$util.isString(message.dataset))
                        return "dataset: string expected";
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    if (!$util.isString(message.project))
                        return "project: string expected";
                if (message.dependencyTargets != null && Object.hasOwnProperty.call(message, "dependencyTargets")) {
                    if (!Array.isArray(message.dependencyTargets))
                        return "dependencyTargets: array expected";
                    for (let i = 0; i < message.dependencyTargets.length; ++i) {
                        let error = $root.dataform.ActionConfig.Target.verify(message.dependencyTargets[i], long + 1);
                        if (error)
                            return "dependencyTargets." + error;
                    }
                }
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    if (!$util.isString(message.filename))
                        return "filename: string expected";
                if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                    if (!Array.isArray(message.tags))
                        return "tags: array expected";
                    for (let i = 0; i < message.tags.length; ++i)
                        if (!$util.isString(message.tags[i]))
                            return "tags: string[] expected";
                }
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    if (typeof message.disabled !== "boolean")
                        return "disabled: boolean expected";
                if (message.preOperations != null && Object.hasOwnProperty.call(message, "preOperations")) {
                    if (!Array.isArray(message.preOperations))
                        return "preOperations: array expected";
                    for (let i = 0; i < message.preOperations.length; ++i)
                        if (!$util.isString(message.preOperations[i]))
                            return "preOperations: string[] expected";
                }
                if (message.postOperations != null && Object.hasOwnProperty.call(message, "postOperations")) {
                    if (!Array.isArray(message.postOperations))
                        return "postOperations: array expected";
                    for (let i = 0; i < message.postOperations.length; ++i)
                        if (!$util.isString(message.postOperations[i]))
                            return "postOperations: string[] expected";
                }
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    if (!$util.isString(message.description))
                        return "description: string expected";
                if (message.columns != null && Object.hasOwnProperty.call(message, "columns")) {
                    if (!Array.isArray(message.columns))
                        return "columns: array expected";
                    for (let i = 0; i < message.columns.length; ++i) {
                        let error = $root.dataform.ActionConfig.ColumnDescriptor.verify(message.columns[i], long + 1);
                        if (error)
                            return "columns." + error;
                    }
                }
                if (message.partitionBy != null && Object.hasOwnProperty.call(message, "partitionBy"))
                    if (!$util.isString(message.partitionBy))
                        return "partitionBy: string expected";
                if (message.partitionExpirationDays != null && Object.hasOwnProperty.call(message, "partitionExpirationDays"))
                    if (!$util.isInteger(message.partitionExpirationDays))
                        return "partitionExpirationDays: integer expected";
                if (message.requirePartitionFilter != null && Object.hasOwnProperty.call(message, "requirePartitionFilter"))
                    if (typeof message.requirePartitionFilter !== "boolean")
                        return "requirePartitionFilter: boolean expected";
                if (message.clusterBy != null && Object.hasOwnProperty.call(message, "clusterBy")) {
                    if (!Array.isArray(message.clusterBy))
                        return "clusterBy: array expected";
                    for (let i = 0; i < message.clusterBy.length; ++i)
                        if (!$util.isString(message.clusterBy[i]))
                            return "clusterBy: string[] expected";
                }
                if (message.labels != null && Object.hasOwnProperty.call(message, "labels")) {
                    if (!$util.isObject(message.labels))
                        return "labels: object expected";
                    let key = Object.keys(message.labels);
                    for (let i = 0; i < key.length; ++i)
                        if (!$util.isString(message.labels[key[i]]))
                            return "labels: string{k:string} expected";
                }
                if (message.additionalOptions != null && Object.hasOwnProperty.call(message, "additionalOptions")) {
                    if (!$util.isObject(message.additionalOptions))
                        return "additionalOptions: object expected";
                    let key = Object.keys(message.additionalOptions);
                    for (let i = 0; i < key.length; ++i)
                        if (!$util.isString(message.additionalOptions[key[i]]))
                            return "additionalOptions: string{k:string} expected";
                }
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    if (typeof message.dependOnDependencyAssertions !== "boolean")
                        return "dependOnDependencyAssertions: boolean expected";
                if (message.assertions != null && Object.hasOwnProperty.call(message, "assertions")) {
                    let error = $root.dataform.ActionConfig.TableAssertionsConfig.verify(message.assertions, long + 1);
                    if (error)
                        return "assertions." + error;
                }
                if (message.hermetic != null && Object.hasOwnProperty.call(message, "hermetic"))
                    if (typeof message.hermetic !== "boolean")
                        return "hermetic: boolean expected";
                if (message.iceberg != null && Object.hasOwnProperty.call(message, "iceberg")) {
                    let error = $root.dataform.ActionConfig.IcebergTableConfig.verify(message.iceberg, long + 1);
                    if (error)
                        return "iceberg." + error;
                }
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata")) {
                    let error = $root.dataform.ActionConfig.Metadata.verify(message.metadata, long + 1);
                    if (error)
                        return "metadata." + error;
                }
                if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                    if (!$util.isString(message.reservation))
                        return "reservation: string expected";
                if (message.preserveGovernanceControls != null && Object.hasOwnProperty.call(message, "preserveGovernanceControls")) {
                    if (typeof message.preserveGovernanceControls !== "boolean")
                        return "preserveGovernanceControls: boolean expected";
                }
                return null;
            };

            /**
             * Creates a TableConfig message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.ActionConfig.TableConfig
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.ActionConfig.TableConfig} TableConfig
             */
            TableConfig.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.ActionConfig.TableConfig)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.ActionConfig.TableConfig: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.ActionConfig.TableConfig();
                if (object.name != null)
                    message.name = String(object.name);
                if (object.dataset != null)
                    message.dataset = String(object.dataset);
                if (object.project != null)
                    message.project = String(object.project);
                if (object.dependencyTargets) {
                    if (!Array.isArray(object.dependencyTargets))
                        throw TypeError(".dataform.ActionConfig.TableConfig.dependencyTargets: array expected");
                    message.dependencyTargets = [];
                    for (let i = 0; i < object.dependencyTargets.length; ++i) {
                        if (!$util.isObject(object.dependencyTargets[i]))
                            throw TypeError(".dataform.ActionConfig.TableConfig.dependencyTargets: object expected");
                        message.dependencyTargets[i] = $root.dataform.ActionConfig.Target.fromObject(object.dependencyTargets[i], long + 1);
                    }
                }
                if (object.filename != null)
                    message.filename = String(object.filename);
                if (object.tags) {
                    if (!Array.isArray(object.tags))
                        throw TypeError(".dataform.ActionConfig.TableConfig.tags: array expected");
                    message.tags = [];
                    for (let i = 0; i < object.tags.length; ++i)
                        message.tags[i] = String(object.tags[i]);
                }
                if (object.disabled != null)
                    message.disabled = Boolean(object.disabled);
                if (object.preOperations) {
                    if (!Array.isArray(object.preOperations))
                        throw TypeError(".dataform.ActionConfig.TableConfig.preOperations: array expected");
                    message.preOperations = [];
                    for (let i = 0; i < object.preOperations.length; ++i)
                        message.preOperations[i] = String(object.preOperations[i]);
                }
                if (object.postOperations) {
                    if (!Array.isArray(object.postOperations))
                        throw TypeError(".dataform.ActionConfig.TableConfig.postOperations: array expected");
                    message.postOperations = [];
                    for (let i = 0; i < object.postOperations.length; ++i)
                        message.postOperations[i] = String(object.postOperations[i]);
                }
                if (object.description != null)
                    message.description = String(object.description);
                if (object.columns) {
                    if (!Array.isArray(object.columns))
                        throw TypeError(".dataform.ActionConfig.TableConfig.columns: array expected");
                    message.columns = [];
                    for (let i = 0; i < object.columns.length; ++i) {
                        if (!$util.isObject(object.columns[i]))
                            throw TypeError(".dataform.ActionConfig.TableConfig.columns: object expected");
                        message.columns[i] = $root.dataform.ActionConfig.ColumnDescriptor.fromObject(object.columns[i], long + 1);
                    }
                }
                if (object.partitionBy != null)
                    message.partitionBy = String(object.partitionBy);
                if (object.partitionExpirationDays != null)
                    message.partitionExpirationDays = object.partitionExpirationDays | 0;
                if (object.requirePartitionFilter != null)
                    message.requirePartitionFilter = Boolean(object.requirePartitionFilter);
                if (object.clusterBy) {
                    if (!Array.isArray(object.clusterBy))
                        throw TypeError(".dataform.ActionConfig.TableConfig.clusterBy: array expected");
                    message.clusterBy = [];
                    for (let i = 0; i < object.clusterBy.length; ++i)
                        message.clusterBy[i] = String(object.clusterBy[i]);
                }
                if (object.labels) {
                    if (!$util.isObject(object.labels))
                        throw TypeError(".dataform.ActionConfig.TableConfig.labels: object expected");
                    message.labels = {};
                    for (let keys = Object.keys(object.labels), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.labels, keys[i]);
                        message.labels[keys[i]] = String(object.labels[keys[i]]);
                    }
                }
                if (object.additionalOptions) {
                    if (!$util.isObject(object.additionalOptions))
                        throw TypeError(".dataform.ActionConfig.TableConfig.additionalOptions: object expected");
                    message.additionalOptions = {};
                    for (let keys = Object.keys(object.additionalOptions), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.additionalOptions, keys[i]);
                        message.additionalOptions[keys[i]] = String(object.additionalOptions[keys[i]]);
                    }
                }
                if (object.dependOnDependencyAssertions != null)
                    message.dependOnDependencyAssertions = Boolean(object.dependOnDependencyAssertions);
                if (object.assertions != null) {
                    if (!$util.isObject(object.assertions))
                        throw TypeError(".dataform.ActionConfig.TableConfig.assertions: object expected");
                    message.assertions = $root.dataform.ActionConfig.TableAssertionsConfig.fromObject(object.assertions, long + 1);
                }
                if (object.hermetic != null)
                    message.hermetic = Boolean(object.hermetic);
                if (object.iceberg != null) {
                    if (!$util.isObject(object.iceberg))
                        throw TypeError(".dataform.ActionConfig.TableConfig.iceberg: object expected");
                    message.iceberg = $root.dataform.ActionConfig.IcebergTableConfig.fromObject(object.iceberg, long + 1);
                }
                if (object.metadata != null) {
                    if (!$util.isObject(object.metadata))
                        throw TypeError(".dataform.ActionConfig.TableConfig.metadata: object expected");
                    message.metadata = $root.dataform.ActionConfig.Metadata.fromObject(object.metadata, long + 1);
                }
                if (object.reservation != null)
                    message.reservation = String(object.reservation);
                if (object.preserveGovernanceControls != null)
                    message.preserveGovernanceControls = Boolean(object.preserveGovernanceControls);
                return message;
            };

            /**
             * Creates a plain object from a TableConfig message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.ActionConfig.TableConfig
             * @static
             * @param {dataform.ActionConfig.TableConfig} message TableConfig
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            TableConfig.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.dependencyTargets = [];
                    object.tags = [];
                    object.preOperations = [];
                    object.postOperations = [];
                    object.columns = [];
                    object.clusterBy = [];
                }
                if (options.objects || options.defaults) {
                    object.labels = {};
                    object.additionalOptions = {};
                }
                if (options.defaults) {
                    object.name = "";
                    object.dataset = "";
                    object.project = "";
                    object.filename = "";
                    object.disabled = false;
                    object.description = "";
                    object.partitionBy = "";
                    object.partitionExpirationDays = 0;
                    object.requirePartitionFilter = false;
                    object.dependOnDependencyAssertions = false;
                    object.assertions = null;
                    object.hermetic = false;
                    object.iceberg = null;
                    object.metadata = null;
                    object.reservation = "";
                }
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    object.dataset = message.dataset;
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    object.project = message.project;
                if (message.dependencyTargets && message.dependencyTargets.length) {
                    object.dependencyTargets = [];
                    for (let j = 0; j < message.dependencyTargets.length; ++j)
                        object.dependencyTargets[j] = $root.dataform.ActionConfig.Target.toObject(message.dependencyTargets[j], options, q + 1);
                }
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    object.filename = message.filename;
                if (message.tags && message.tags.length) {
                    object.tags = [];
                    for (let j = 0; j < message.tags.length; ++j)
                        object.tags[j] = message.tags[j];
                }
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    object.disabled = message.disabled;
                if (message.preOperations && message.preOperations.length) {
                    object.preOperations = [];
                    for (let j = 0; j < message.preOperations.length; ++j)
                        object.preOperations[j] = message.preOperations[j];
                }
                if (message.postOperations && message.postOperations.length) {
                    object.postOperations = [];
                    for (let j = 0; j < message.postOperations.length; ++j)
                        object.postOperations[j] = message.postOperations[j];
                }
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    object.description = message.description;
                if (message.columns && message.columns.length) {
                    object.columns = [];
                    for (let j = 0; j < message.columns.length; ++j)
                        object.columns[j] = $root.dataform.ActionConfig.ColumnDescriptor.toObject(message.columns[j], options, q + 1);
                }
                if (message.partitionBy != null && Object.hasOwnProperty.call(message, "partitionBy"))
                    object.partitionBy = message.partitionBy;
                if (message.partitionExpirationDays != null && Object.hasOwnProperty.call(message, "partitionExpirationDays"))
                    object.partitionExpirationDays = message.partitionExpirationDays;
                if (message.requirePartitionFilter != null && Object.hasOwnProperty.call(message, "requirePartitionFilter"))
                    object.requirePartitionFilter = message.requirePartitionFilter;
                if (message.clusterBy && message.clusterBy.length) {
                    object.clusterBy = [];
                    for (let j = 0; j < message.clusterBy.length; ++j)
                        object.clusterBy[j] = message.clusterBy[j];
                }
                let keys2;
                if (message.labels && (keys2 = Object.keys(message.labels)).length) {
                    object.labels = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.labels, keys2[j]);
                        object.labels[keys2[j]] = message.labels[keys2[j]];
                    }
                }
                if (message.additionalOptions && (keys2 = Object.keys(message.additionalOptions)).length) {
                    object.additionalOptions = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.additionalOptions, keys2[j]);
                        object.additionalOptions[keys2[j]] = message.additionalOptions[keys2[j]];
                    }
                }
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    object.dependOnDependencyAssertions = message.dependOnDependencyAssertions;
                if (message.assertions != null && Object.hasOwnProperty.call(message, "assertions"))
                    object.assertions = $root.dataform.ActionConfig.TableAssertionsConfig.toObject(message.assertions, options, q + 1);
                if (message.hermetic != null && Object.hasOwnProperty.call(message, "hermetic"))
                    object.hermetic = message.hermetic;
                if (message.iceberg != null && Object.hasOwnProperty.call(message, "iceberg"))
                    object.iceberg = $root.dataform.ActionConfig.IcebergTableConfig.toObject(message.iceberg, options, q + 1);
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                    object.metadata = $root.dataform.ActionConfig.Metadata.toObject(message.metadata, options, q + 1);
                if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                    object.reservation = message.reservation;
                if (message.preserveGovernanceControls != null && Object.hasOwnProperty.call(message, "preserveGovernanceControls")) {
                    object.preserveGovernanceControls = message.preserveGovernanceControls;
                    if (options.oneofs)
                        object._preserveGovernanceControls = "preserveGovernanceControls";
                }
                return object;
            };

            /**
             * Converts this TableConfig to JSON.
             * @function toJSON
             * @memberof dataform.ActionConfig.TableConfig
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            TableConfig.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for TableConfig
             * @function getTypeUrl
             * @memberof dataform.ActionConfig.TableConfig
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            TableConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.ActionConfig.TableConfig";
            };

            return TableConfig;
        })();

        ActionConfig.Metadata = (function() {

            /**
             * Properties of a Metadata.
             * @memberof dataform.ActionConfig
             * @interface IMetadata
             * @property {string|null} [overview] Metadata overview
             * @property {google.protobuf.IStruct|null} [extraProperties] Metadata extraProperties
             */

            /**
             * Constructs a new Metadata.
             * @memberof dataform.ActionConfig
             * @classdesc Represents a Metadata.
             * @implements IMetadata
             * @constructor
             * @param {dataform.ActionConfig.IMetadata=} [properties] Properties to set
             */
            function Metadata(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Metadata overview.
             * @member {string} overview
             * @memberof dataform.ActionConfig.Metadata
             * @instance
             */
            Metadata.prototype.overview = "";

            /**
             * Metadata extraProperties.
             * @member {google.protobuf.IStruct|null|undefined} extraProperties
             * @memberof dataform.ActionConfig.Metadata
             * @instance
             */
            Metadata.prototype.extraProperties = null;

            /**
             * Creates a new Metadata instance using the specified properties.
             * @function create
             * @memberof dataform.ActionConfig.Metadata
             * @static
             * @param {dataform.ActionConfig.IMetadata=} [properties] Properties to set
             * @returns {dataform.ActionConfig.Metadata} Metadata instance
             */
            Metadata.create = function create(properties) {
                return new Metadata(properties);
            };

            /**
             * Encodes the specified Metadata message. Does not implicitly {@link dataform.ActionConfig.Metadata.verify|verify} messages.
             * @function encode
             * @memberof dataform.ActionConfig.Metadata
             * @static
             * @param {dataform.ActionConfig.IMetadata} message Metadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Metadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.overview != null && Object.hasOwnProperty.call(message, "overview"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.overview);
                if (message.extraProperties != null && Object.hasOwnProperty.call(message, "extraProperties"))
                    $root.google.protobuf.Struct.encode(message.extraProperties, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified Metadata message, length delimited. Does not implicitly {@link dataform.ActionConfig.Metadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.ActionConfig.Metadata
             * @static
             * @param {dataform.ActionConfig.IMetadata} message Metadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Metadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a Metadata message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.ActionConfig.Metadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.ActionConfig.Metadata} Metadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Metadata.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig.Metadata();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.overview = reader.string();
                            break;
                        }
                    case 2: {
                            message.extraProperties = $root.google.protobuf.Struct.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Metadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.ActionConfig.Metadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.ActionConfig.Metadata} Metadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Metadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Metadata message.
             * @function verify
             * @memberof dataform.ActionConfig.Metadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Metadata.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.overview != null && Object.hasOwnProperty.call(message, "overview"))
                    if (!$util.isString(message.overview))
                        return "overview: string expected";
                if (message.extraProperties != null && Object.hasOwnProperty.call(message, "extraProperties")) {
                    let error = $root.google.protobuf.Struct.verify(message.extraProperties, long + 1);
                    if (error)
                        return "extraProperties." + error;
                }
                return null;
            };

            /**
             * Creates a Metadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.ActionConfig.Metadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.ActionConfig.Metadata} Metadata
             */
            Metadata.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.ActionConfig.Metadata)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.ActionConfig.Metadata: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.ActionConfig.Metadata();
                if (object.overview != null)
                    message.overview = String(object.overview);
                if (object.extraProperties != null) {
                    if (!$util.isObject(object.extraProperties))
                        throw TypeError(".dataform.ActionConfig.Metadata.extraProperties: object expected");
                    message.extraProperties = $root.google.protobuf.Struct.fromObject(object.extraProperties, long + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a Metadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.ActionConfig.Metadata
             * @static
             * @param {dataform.ActionConfig.Metadata} message Metadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Metadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.overview = "";
                    object.extraProperties = null;
                }
                if (message.overview != null && Object.hasOwnProperty.call(message, "overview"))
                    object.overview = message.overview;
                if (message.extraProperties != null && Object.hasOwnProperty.call(message, "extraProperties"))
                    object.extraProperties = $root.google.protobuf.Struct.toObject(message.extraProperties, options, q + 1);
                return object;
            };

            /**
             * Converts this Metadata to JSON.
             * @function toJSON
             * @memberof dataform.ActionConfig.Metadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Metadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Metadata
             * @function getTypeUrl
             * @memberof dataform.ActionConfig.Metadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Metadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.ActionConfig.Metadata";
            };

            return Metadata;
        })();

        ActionConfig.ViewConfig = (function() {

            /**
             * Properties of a ViewConfig.
             * @memberof dataform.ActionConfig
             * @interface IViewConfig
             * @property {string|null} [name] ViewConfig name
             * @property {string|null} [dataset] ViewConfig dataset
             * @property {string|null} [project] ViewConfig project
             * @property {Array.<dataform.ActionConfig.ITarget>|null} [dependencyTargets] ViewConfig dependencyTargets
             * @property {string|null} [filename] ViewConfig filename
             * @property {Array.<string>|null} [tags] ViewConfig tags
             * @property {boolean|null} [disabled] ViewConfig disabled
             * @property {Array.<string>|null} [preOperations] ViewConfig preOperations
             * @property {Array.<string>|null} [postOperations] ViewConfig postOperations
             * @property {boolean|null} [materialized] ViewConfig materialized
             * @property {string|null} [partitionBy] ViewConfig partitionBy
             * @property {Array.<string>|null} [clusterBy] ViewConfig clusterBy
             * @property {string|null} [description] ViewConfig description
             * @property {Array.<dataform.ActionConfig.IColumnDescriptor>|null} [columns] ViewConfig columns
             * @property {Object.<string,string>|null} [labels] ViewConfig labels
             * @property {Object.<string,string>|null} [additionalOptions] ViewConfig additionalOptions
             * @property {boolean|null} [dependOnDependencyAssertions] ViewConfig dependOnDependencyAssertions
             * @property {boolean|null} [hermetic] ViewConfig hermetic
             * @property {dataform.ActionConfig.ITableAssertionsConfig|null} [assertions] ViewConfig assertions
             * @property {dataform.ActionConfig.IMetadata|null} [metadata] ViewConfig metadata
             * @property {string|null} [reservation] ViewConfig reservation
             */

            /**
             * Constructs a new ViewConfig.
             * @memberof dataform.ActionConfig
             * @classdesc Represents a ViewConfig.
             * @implements IViewConfig
             * @constructor
             * @param {dataform.ActionConfig.IViewConfig=} [properties] Properties to set
             */
            function ViewConfig(properties) {
                this.dependencyTargets = [];
                this.tags = [];
                this.preOperations = [];
                this.postOperations = [];
                this.clusterBy = [];
                this.columns = [];
                this.labels = {};
                this.additionalOptions = {};
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ViewConfig name.
             * @member {string} name
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.name = "";

            /**
             * ViewConfig dataset.
             * @member {string} dataset
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.dataset = "";

            /**
             * ViewConfig project.
             * @member {string} project
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.project = "";

            /**
             * ViewConfig dependencyTargets.
             * @member {Array.<dataform.ActionConfig.ITarget>} dependencyTargets
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.dependencyTargets = $util.emptyArray;

            /**
             * ViewConfig filename.
             * @member {string} filename
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.filename = "";

            /**
             * ViewConfig tags.
             * @member {Array.<string>} tags
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.tags = $util.emptyArray;

            /**
             * ViewConfig disabled.
             * @member {boolean} disabled
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.disabled = false;

            /**
             * ViewConfig preOperations.
             * @member {Array.<string>} preOperations
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.preOperations = $util.emptyArray;

            /**
             * ViewConfig postOperations.
             * @member {Array.<string>} postOperations
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.postOperations = $util.emptyArray;

            /**
             * ViewConfig materialized.
             * @member {boolean} materialized
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.materialized = false;

            /**
             * ViewConfig partitionBy.
             * @member {string} partitionBy
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.partitionBy = "";

            /**
             * ViewConfig clusterBy.
             * @member {Array.<string>} clusterBy
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.clusterBy = $util.emptyArray;

            /**
             * ViewConfig description.
             * @member {string} description
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.description = "";

            /**
             * ViewConfig columns.
             * @member {Array.<dataform.ActionConfig.IColumnDescriptor>} columns
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.columns = $util.emptyArray;

            /**
             * ViewConfig labels.
             * @member {Object.<string,string>} labels
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.labels = $util.emptyObject;

            /**
             * ViewConfig additionalOptions.
             * @member {Object.<string,string>} additionalOptions
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.additionalOptions = $util.emptyObject;

            /**
             * ViewConfig dependOnDependencyAssertions.
             * @member {boolean} dependOnDependencyAssertions
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.dependOnDependencyAssertions = false;

            /**
             * ViewConfig hermetic.
             * @member {boolean} hermetic
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.hermetic = false;

            /**
             * ViewConfig assertions.
             * @member {dataform.ActionConfig.ITableAssertionsConfig|null|undefined} assertions
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.assertions = null;

            /**
             * ViewConfig metadata.
             * @member {dataform.ActionConfig.IMetadata|null|undefined} metadata
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.metadata = null;

            /**
             * ViewConfig reservation.
             * @member {string} reservation
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             */
            ViewConfig.prototype.reservation = "";

            /**
             * Creates a new ViewConfig instance using the specified properties.
             * @function create
             * @memberof dataform.ActionConfig.ViewConfig
             * @static
             * @param {dataform.ActionConfig.IViewConfig=} [properties] Properties to set
             * @returns {dataform.ActionConfig.ViewConfig} ViewConfig instance
             */
            ViewConfig.create = function create(properties) {
                return new ViewConfig(properties);
            };

            /**
             * Encodes the specified ViewConfig message. Does not implicitly {@link dataform.ActionConfig.ViewConfig.verify|verify} messages.
             * @function encode
             * @memberof dataform.ActionConfig.ViewConfig
             * @static
             * @param {dataform.ActionConfig.IViewConfig} message ViewConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ViewConfig.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.dataset);
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.project);
                if (message.dependencyTargets != null && message.dependencyTargets.length)
                    for (let i = 0; i < message.dependencyTargets.length; ++i)
                        $root.dataform.ActionConfig.Target.encode(message.dependencyTargets[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.filename);
                if (message.tags != null && message.tags.length)
                    for (let i = 0; i < message.tags.length; ++i)
                        writer.uint32(/* id 6, wireType 2 =*/50).string(message.tags[i]);
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    writer.uint32(/* id 7, wireType 0 =*/56).bool(message.disabled);
                if (message.preOperations != null && message.preOperations.length)
                    for (let i = 0; i < message.preOperations.length; ++i)
                        writer.uint32(/* id 8, wireType 2 =*/66).string(message.preOperations[i]);
                if (message.postOperations != null && message.postOperations.length)
                    for (let i = 0; i < message.postOperations.length; ++i)
                        writer.uint32(/* id 9, wireType 2 =*/74).string(message.postOperations[i]);
                if (message.materialized != null && Object.hasOwnProperty.call(message, "materialized"))
                    writer.uint32(/* id 10, wireType 0 =*/80).bool(message.materialized);
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.description);
                if (message.columns != null && message.columns.length)
                    for (let i = 0; i < message.columns.length; ++i)
                        $root.dataform.ActionConfig.ColumnDescriptor.encode(message.columns[i], writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
                if (message.labels != null && Object.hasOwnProperty.call(message, "labels"))
                    for (let keys = Object.keys(message.labels), i = 0; i < keys.length; ++i)
                        writer.uint32(/* id 13, wireType 2 =*/106).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.labels[keys[i]]).ldelim();
                if (message.additionalOptions != null && Object.hasOwnProperty.call(message, "additionalOptions"))
                    for (let keys = Object.keys(message.additionalOptions), i = 0; i < keys.length; ++i)
                        writer.uint32(/* id 14, wireType 2 =*/114).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.additionalOptions[keys[i]]).ldelim();
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    writer.uint32(/* id 15, wireType 0 =*/120).bool(message.dependOnDependencyAssertions);
                if (message.hermetic != null && Object.hasOwnProperty.call(message, "hermetic"))
                    writer.uint32(/* id 16, wireType 0 =*/128).bool(message.hermetic);
                if (message.assertions != null && Object.hasOwnProperty.call(message, "assertions"))
                    $root.dataform.ActionConfig.TableAssertionsConfig.encode(message.assertions, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
                if (message.partitionBy != null && Object.hasOwnProperty.call(message, "partitionBy"))
                    writer.uint32(/* id 18, wireType 2 =*/146).string(message.partitionBy);
                if (message.clusterBy != null && message.clusterBy.length)
                    for (let i = 0; i < message.clusterBy.length; ++i)
                        writer.uint32(/* id 19, wireType 2 =*/154).string(message.clusterBy[i]);
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                    $root.dataform.ActionConfig.Metadata.encode(message.metadata, writer.uint32(/* id 20, wireType 2 =*/162).fork(), q + 1).ldelim();
                if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                    writer.uint32(/* id 21, wireType 2 =*/170).string(message.reservation);
                return writer;
            };

            /**
             * Encodes the specified ViewConfig message, length delimited. Does not implicitly {@link dataform.ActionConfig.ViewConfig.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.ActionConfig.ViewConfig
             * @static
             * @param {dataform.ActionConfig.IViewConfig} message ViewConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ViewConfig.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ViewConfig message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.ActionConfig.ViewConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.ActionConfig.ViewConfig} ViewConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ViewConfig.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig.ViewConfig(), key, value;
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.name = reader.string();
                            break;
                        }
                    case 2: {
                            message.dataset = reader.string();
                            break;
                        }
                    case 3: {
                            message.project = reader.string();
                            break;
                        }
                    case 4: {
                            if (!(message.dependencyTargets && message.dependencyTargets.length))
                                message.dependencyTargets = [];
                            message.dependencyTargets.push($root.dataform.ActionConfig.Target.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 5: {
                            message.filename = reader.string();
                            break;
                        }
                    case 6: {
                            if (!(message.tags && message.tags.length))
                                message.tags = [];
                            message.tags.push(reader.string());
                            break;
                        }
                    case 7: {
                            message.disabled = reader.bool();
                            break;
                        }
                    case 8: {
                            if (!(message.preOperations && message.preOperations.length))
                                message.preOperations = [];
                            message.preOperations.push(reader.string());
                            break;
                        }
                    case 9: {
                            if (!(message.postOperations && message.postOperations.length))
                                message.postOperations = [];
                            message.postOperations.push(reader.string());
                            break;
                        }
                    case 10: {
                            message.materialized = reader.bool();
                            break;
                        }
                    case 18: {
                            message.partitionBy = reader.string();
                            break;
                        }
                    case 19: {
                            if (!(message.clusterBy && message.clusterBy.length))
                                message.clusterBy = [];
                            message.clusterBy.push(reader.string());
                            break;
                        }
                    case 11: {
                            message.description = reader.string();
                            break;
                        }
                    case 12: {
                            if (!(message.columns && message.columns.length))
                                message.columns = [];
                            message.columns.push($root.dataform.ActionConfig.ColumnDescriptor.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 13: {
                            if (message.labels === $util.emptyObject)
                                message.labels = {};
                            let end2 = reader.uint32() + reader.pos;
                            key = "";
                            value = "";
                            while (reader.pos < end2) {
                                let tag2 = reader.uint32();
                                switch (tag2 >>> 3) {
                                case 1:
                                    key = reader.string();
                                    break;
                                case 2:
                                    value = reader.string();
                                    break;
                                default:
                                    reader.skipType(tag2 & 7, long);
                                    break;
                                }
                            }
                            if (key === "__proto__")
                                $util.makeProp(message.labels, key);
                            message.labels[key] = value;
                            break;
                        }
                    case 14: {
                            if (message.additionalOptions === $util.emptyObject)
                                message.additionalOptions = {};
                            let end2 = reader.uint32() + reader.pos;
                            key = "";
                            value = "";
                            while (reader.pos < end2) {
                                let tag2 = reader.uint32();
                                switch (tag2 >>> 3) {
                                case 1:
                                    key = reader.string();
                                    break;
                                case 2:
                                    value = reader.string();
                                    break;
                                default:
                                    reader.skipType(tag2 & 7, long);
                                    break;
                                }
                            }
                            if (key === "__proto__")
                                $util.makeProp(message.additionalOptions, key);
                            message.additionalOptions[key] = value;
                            break;
                        }
                    case 15: {
                            message.dependOnDependencyAssertions = reader.bool();
                            break;
                        }
                    case 16: {
                            message.hermetic = reader.bool();
                            break;
                        }
                    case 17: {
                            message.assertions = $root.dataform.ActionConfig.TableAssertionsConfig.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 20: {
                            message.metadata = $root.dataform.ActionConfig.Metadata.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 21: {
                            message.reservation = reader.string();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a ViewConfig message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.ActionConfig.ViewConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.ActionConfig.ViewConfig} ViewConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ViewConfig.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ViewConfig message.
             * @function verify
             * @memberof dataform.ActionConfig.ViewConfig
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ViewConfig.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    if (!$util.isString(message.dataset))
                        return "dataset: string expected";
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    if (!$util.isString(message.project))
                        return "project: string expected";
                if (message.dependencyTargets != null && Object.hasOwnProperty.call(message, "dependencyTargets")) {
                    if (!Array.isArray(message.dependencyTargets))
                        return "dependencyTargets: array expected";
                    for (let i = 0; i < message.dependencyTargets.length; ++i) {
                        let error = $root.dataform.ActionConfig.Target.verify(message.dependencyTargets[i], long + 1);
                        if (error)
                            return "dependencyTargets." + error;
                    }
                }
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    if (!$util.isString(message.filename))
                        return "filename: string expected";
                if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                    if (!Array.isArray(message.tags))
                        return "tags: array expected";
                    for (let i = 0; i < message.tags.length; ++i)
                        if (!$util.isString(message.tags[i]))
                            return "tags: string[] expected";
                }
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    if (typeof message.disabled !== "boolean")
                        return "disabled: boolean expected";
                if (message.preOperations != null && Object.hasOwnProperty.call(message, "preOperations")) {
                    if (!Array.isArray(message.preOperations))
                        return "preOperations: array expected";
                    for (let i = 0; i < message.preOperations.length; ++i)
                        if (!$util.isString(message.preOperations[i]))
                            return "preOperations: string[] expected";
                }
                if (message.postOperations != null && Object.hasOwnProperty.call(message, "postOperations")) {
                    if (!Array.isArray(message.postOperations))
                        return "postOperations: array expected";
                    for (let i = 0; i < message.postOperations.length; ++i)
                        if (!$util.isString(message.postOperations[i]))
                            return "postOperations: string[] expected";
                }
                if (message.materialized != null && Object.hasOwnProperty.call(message, "materialized"))
                    if (typeof message.materialized !== "boolean")
                        return "materialized: boolean expected";
                if (message.partitionBy != null && Object.hasOwnProperty.call(message, "partitionBy"))
                    if (!$util.isString(message.partitionBy))
                        return "partitionBy: string expected";
                if (message.clusterBy != null && Object.hasOwnProperty.call(message, "clusterBy")) {
                    if (!Array.isArray(message.clusterBy))
                        return "clusterBy: array expected";
                    for (let i = 0; i < message.clusterBy.length; ++i)
                        if (!$util.isString(message.clusterBy[i]))
                            return "clusterBy: string[] expected";
                }
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    if (!$util.isString(message.description))
                        return "description: string expected";
                if (message.columns != null && Object.hasOwnProperty.call(message, "columns")) {
                    if (!Array.isArray(message.columns))
                        return "columns: array expected";
                    for (let i = 0; i < message.columns.length; ++i) {
                        let error = $root.dataform.ActionConfig.ColumnDescriptor.verify(message.columns[i], long + 1);
                        if (error)
                            return "columns." + error;
                    }
                }
                if (message.labels != null && Object.hasOwnProperty.call(message, "labels")) {
                    if (!$util.isObject(message.labels))
                        return "labels: object expected";
                    let key = Object.keys(message.labels);
                    for (let i = 0; i < key.length; ++i)
                        if (!$util.isString(message.labels[key[i]]))
                            return "labels: string{k:string} expected";
                }
                if (message.additionalOptions != null && Object.hasOwnProperty.call(message, "additionalOptions")) {
                    if (!$util.isObject(message.additionalOptions))
                        return "additionalOptions: object expected";
                    let key = Object.keys(message.additionalOptions);
                    for (let i = 0; i < key.length; ++i)
                        if (!$util.isString(message.additionalOptions[key[i]]))
                            return "additionalOptions: string{k:string} expected";
                }
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    if (typeof message.dependOnDependencyAssertions !== "boolean")
                        return "dependOnDependencyAssertions: boolean expected";
                if (message.hermetic != null && Object.hasOwnProperty.call(message, "hermetic"))
                    if (typeof message.hermetic !== "boolean")
                        return "hermetic: boolean expected";
                if (message.assertions != null && Object.hasOwnProperty.call(message, "assertions")) {
                    let error = $root.dataform.ActionConfig.TableAssertionsConfig.verify(message.assertions, long + 1);
                    if (error)
                        return "assertions." + error;
                }
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata")) {
                    let error = $root.dataform.ActionConfig.Metadata.verify(message.metadata, long + 1);
                    if (error)
                        return "metadata." + error;
                }
                if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                    if (!$util.isString(message.reservation))
                        return "reservation: string expected";
                return null;
            };

            /**
             * Creates a ViewConfig message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.ActionConfig.ViewConfig
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.ActionConfig.ViewConfig} ViewConfig
             */
            ViewConfig.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.ActionConfig.ViewConfig)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.ActionConfig.ViewConfig: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.ActionConfig.ViewConfig();
                if (object.name != null)
                    message.name = String(object.name);
                if (object.dataset != null)
                    message.dataset = String(object.dataset);
                if (object.project != null)
                    message.project = String(object.project);
                if (object.dependencyTargets) {
                    if (!Array.isArray(object.dependencyTargets))
                        throw TypeError(".dataform.ActionConfig.ViewConfig.dependencyTargets: array expected");
                    message.dependencyTargets = [];
                    for (let i = 0; i < object.dependencyTargets.length; ++i) {
                        if (!$util.isObject(object.dependencyTargets[i]))
                            throw TypeError(".dataform.ActionConfig.ViewConfig.dependencyTargets: object expected");
                        message.dependencyTargets[i] = $root.dataform.ActionConfig.Target.fromObject(object.dependencyTargets[i], long + 1);
                    }
                }
                if (object.filename != null)
                    message.filename = String(object.filename);
                if (object.tags) {
                    if (!Array.isArray(object.tags))
                        throw TypeError(".dataform.ActionConfig.ViewConfig.tags: array expected");
                    message.tags = [];
                    for (let i = 0; i < object.tags.length; ++i)
                        message.tags[i] = String(object.tags[i]);
                }
                if (object.disabled != null)
                    message.disabled = Boolean(object.disabled);
                if (object.preOperations) {
                    if (!Array.isArray(object.preOperations))
                        throw TypeError(".dataform.ActionConfig.ViewConfig.preOperations: array expected");
                    message.preOperations = [];
                    for (let i = 0; i < object.preOperations.length; ++i)
                        message.preOperations[i] = String(object.preOperations[i]);
                }
                if (object.postOperations) {
                    if (!Array.isArray(object.postOperations))
                        throw TypeError(".dataform.ActionConfig.ViewConfig.postOperations: array expected");
                    message.postOperations = [];
                    for (let i = 0; i < object.postOperations.length; ++i)
                        message.postOperations[i] = String(object.postOperations[i]);
                }
                if (object.materialized != null)
                    message.materialized = Boolean(object.materialized);
                if (object.partitionBy != null)
                    message.partitionBy = String(object.partitionBy);
                if (object.clusterBy) {
                    if (!Array.isArray(object.clusterBy))
                        throw TypeError(".dataform.ActionConfig.ViewConfig.clusterBy: array expected");
                    message.clusterBy = [];
                    for (let i = 0; i < object.clusterBy.length; ++i)
                        message.clusterBy[i] = String(object.clusterBy[i]);
                }
                if (object.description != null)
                    message.description = String(object.description);
                if (object.columns) {
                    if (!Array.isArray(object.columns))
                        throw TypeError(".dataform.ActionConfig.ViewConfig.columns: array expected");
                    message.columns = [];
                    for (let i = 0; i < object.columns.length; ++i) {
                        if (!$util.isObject(object.columns[i]))
                            throw TypeError(".dataform.ActionConfig.ViewConfig.columns: object expected");
                        message.columns[i] = $root.dataform.ActionConfig.ColumnDescriptor.fromObject(object.columns[i], long + 1);
                    }
                }
                if (object.labels) {
                    if (!$util.isObject(object.labels))
                        throw TypeError(".dataform.ActionConfig.ViewConfig.labels: object expected");
                    message.labels = {};
                    for (let keys = Object.keys(object.labels), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.labels, keys[i]);
                        message.labels[keys[i]] = String(object.labels[keys[i]]);
                    }
                }
                if (object.additionalOptions) {
                    if (!$util.isObject(object.additionalOptions))
                        throw TypeError(".dataform.ActionConfig.ViewConfig.additionalOptions: object expected");
                    message.additionalOptions = {};
                    for (let keys = Object.keys(object.additionalOptions), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.additionalOptions, keys[i]);
                        message.additionalOptions[keys[i]] = String(object.additionalOptions[keys[i]]);
                    }
                }
                if (object.dependOnDependencyAssertions != null)
                    message.dependOnDependencyAssertions = Boolean(object.dependOnDependencyAssertions);
                if (object.hermetic != null)
                    message.hermetic = Boolean(object.hermetic);
                if (object.assertions != null) {
                    if (!$util.isObject(object.assertions))
                        throw TypeError(".dataform.ActionConfig.ViewConfig.assertions: object expected");
                    message.assertions = $root.dataform.ActionConfig.TableAssertionsConfig.fromObject(object.assertions, long + 1);
                }
                if (object.metadata != null) {
                    if (!$util.isObject(object.metadata))
                        throw TypeError(".dataform.ActionConfig.ViewConfig.metadata: object expected");
                    message.metadata = $root.dataform.ActionConfig.Metadata.fromObject(object.metadata, long + 1);
                }
                if (object.reservation != null)
                    message.reservation = String(object.reservation);
                return message;
            };

            /**
             * Creates a plain object from a ViewConfig message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.ActionConfig.ViewConfig
             * @static
             * @param {dataform.ActionConfig.ViewConfig} message ViewConfig
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ViewConfig.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.dependencyTargets = [];
                    object.tags = [];
                    object.preOperations = [];
                    object.postOperations = [];
                    object.columns = [];
                    object.clusterBy = [];
                }
                if (options.objects || options.defaults) {
                    object.labels = {};
                    object.additionalOptions = {};
                }
                if (options.defaults) {
                    object.name = "";
                    object.dataset = "";
                    object.project = "";
                    object.filename = "";
                    object.disabled = false;
                    object.materialized = false;
                    object.description = "";
                    object.dependOnDependencyAssertions = false;
                    object.hermetic = false;
                    object.assertions = null;
                    object.partitionBy = "";
                    object.metadata = null;
                    object.reservation = "";
                }
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    object.dataset = message.dataset;
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    object.project = message.project;
                if (message.dependencyTargets && message.dependencyTargets.length) {
                    object.dependencyTargets = [];
                    for (let j = 0; j < message.dependencyTargets.length; ++j)
                        object.dependencyTargets[j] = $root.dataform.ActionConfig.Target.toObject(message.dependencyTargets[j], options, q + 1);
                }
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    object.filename = message.filename;
                if (message.tags && message.tags.length) {
                    object.tags = [];
                    for (let j = 0; j < message.tags.length; ++j)
                        object.tags[j] = message.tags[j];
                }
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    object.disabled = message.disabled;
                if (message.preOperations && message.preOperations.length) {
                    object.preOperations = [];
                    for (let j = 0; j < message.preOperations.length; ++j)
                        object.preOperations[j] = message.preOperations[j];
                }
                if (message.postOperations && message.postOperations.length) {
                    object.postOperations = [];
                    for (let j = 0; j < message.postOperations.length; ++j)
                        object.postOperations[j] = message.postOperations[j];
                }
                if (message.materialized != null && Object.hasOwnProperty.call(message, "materialized"))
                    object.materialized = message.materialized;
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    object.description = message.description;
                if (message.columns && message.columns.length) {
                    object.columns = [];
                    for (let j = 0; j < message.columns.length; ++j)
                        object.columns[j] = $root.dataform.ActionConfig.ColumnDescriptor.toObject(message.columns[j], options, q + 1);
                }
                let keys2;
                if (message.labels && (keys2 = Object.keys(message.labels)).length) {
                    object.labels = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.labels, keys2[j]);
                        object.labels[keys2[j]] = message.labels[keys2[j]];
                    }
                }
                if (message.additionalOptions && (keys2 = Object.keys(message.additionalOptions)).length) {
                    object.additionalOptions = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.additionalOptions, keys2[j]);
                        object.additionalOptions[keys2[j]] = message.additionalOptions[keys2[j]];
                    }
                }
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    object.dependOnDependencyAssertions = message.dependOnDependencyAssertions;
                if (message.hermetic != null && Object.hasOwnProperty.call(message, "hermetic"))
                    object.hermetic = message.hermetic;
                if (message.assertions != null && Object.hasOwnProperty.call(message, "assertions"))
                    object.assertions = $root.dataform.ActionConfig.TableAssertionsConfig.toObject(message.assertions, options, q + 1);
                if (message.partitionBy != null && Object.hasOwnProperty.call(message, "partitionBy"))
                    object.partitionBy = message.partitionBy;
                if (message.clusterBy && message.clusterBy.length) {
                    object.clusterBy = [];
                    for (let j = 0; j < message.clusterBy.length; ++j)
                        object.clusterBy[j] = message.clusterBy[j];
                }
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                    object.metadata = $root.dataform.ActionConfig.Metadata.toObject(message.metadata, options, q + 1);
                if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                    object.reservation = message.reservation;
                return object;
            };

            /**
             * Converts this ViewConfig to JSON.
             * @function toJSON
             * @memberof dataform.ActionConfig.ViewConfig
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ViewConfig.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ViewConfig
             * @function getTypeUrl
             * @memberof dataform.ActionConfig.ViewConfig
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ViewConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.ActionConfig.ViewConfig";
            };

            return ViewConfig;
        })();

        /**
         * OnSchemaChange enum.
         * @name dataform.ActionConfig.OnSchemaChange
         * @enum {number}
         * @property {number} IGNORE=0 IGNORE value
         * @property {number} FAIL=1 FAIL value
         * @property {number} EXTEND=2 EXTEND value
         * @property {number} SYNCHRONIZE=3 SYNCHRONIZE value
         */
        ActionConfig.OnSchemaChange = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "IGNORE"] = 0;
            values[valuesById[1] = "FAIL"] = 1;
            values[valuesById[2] = "EXTEND"] = 2;
            values[valuesById[3] = "SYNCHRONIZE"] = 3;
            return values;
        })();

        /**
         * IncrementalStrategy enum.
         * @name dataform.ActionConfig.IncrementalStrategy
         * @enum {number}
         * @property {number} INCREMENTAL_STRATEGY_UNSPECIFIED=0 INCREMENTAL_STRATEGY_UNSPECIFIED value
         * @property {number} INCREMENTAL_STRATEGY_MERGE=1 INCREMENTAL_STRATEGY_MERGE value
         * @property {number} INCREMENTAL_STRATEGY_INSERT_OVERWRITE=2 INCREMENTAL_STRATEGY_INSERT_OVERWRITE value
         */
        ActionConfig.IncrementalStrategy = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "INCREMENTAL_STRATEGY_UNSPECIFIED"] = 0;
            values[valuesById[1] = "INCREMENTAL_STRATEGY_MERGE"] = 1;
            values[valuesById[2] = "INCREMENTAL_STRATEGY_INSERT_OVERWRITE"] = 2;
            return values;
        })();

        ActionConfig.IncrementalTableConfig = (function() {

            /**
             * Properties of an IncrementalTableConfig.
             * @memberof dataform.ActionConfig
             * @interface IIncrementalTableConfig
             * @property {string|null} [name] IncrementalTableConfig name
             * @property {string|null} [dataset] IncrementalTableConfig dataset
             * @property {string|null} [project] IncrementalTableConfig project
             * @property {Array.<dataform.ActionConfig.ITarget>|null} [dependencyTargets] IncrementalTableConfig dependencyTargets
             * @property {string|null} [filename] IncrementalTableConfig filename
             * @property {Array.<string>|null} [tags] IncrementalTableConfig tags
             * @property {boolean|null} [disabled] IncrementalTableConfig disabled
             * @property {Array.<string>|null} [preOperations] IncrementalTableConfig preOperations
             * @property {Array.<string>|null} [postOperations] IncrementalTableConfig postOperations
             * @property {boolean|null} ["protected"] IncrementalTableConfig protected
             * @property {Array.<string>|null} [uniqueKey] IncrementalTableConfig uniqueKey
             * @property {string|null} [description] IncrementalTableConfig description
             * @property {Array.<dataform.ActionConfig.IColumnDescriptor>|null} [columns] IncrementalTableConfig columns
             * @property {string|null} [partitionBy] IncrementalTableConfig partitionBy
             * @property {number|null} [partitionExpirationDays] IncrementalTableConfig partitionExpirationDays
             * @property {boolean|null} [requirePartitionFilter] IncrementalTableConfig requirePartitionFilter
             * @property {string|null} [updatePartitionFilter] IncrementalTableConfig updatePartitionFilter
             * @property {Array.<string>|null} [clusterBy] IncrementalTableConfig clusterBy
             * @property {Object.<string,string>|null} [labels] IncrementalTableConfig labels
             * @property {Object.<string,string>|null} [additionalOptions] IncrementalTableConfig additionalOptions
             * @property {boolean|null} [dependOnDependencyAssertions] IncrementalTableConfig dependOnDependencyAssertions
             * @property {dataform.ActionConfig.ITableAssertionsConfig|null} [assertions] IncrementalTableConfig assertions
             * @property {boolean|null} [hermetic] IncrementalTableConfig hermetic
             * @property {dataform.ActionConfig.OnSchemaChange|null} [onSchemaChange] IncrementalTableConfig onSchemaChange
             * @property {dataform.ActionConfig.IIcebergTableConfig|null} [iceberg] IncrementalTableConfig iceberg
             * @property {dataform.ActionConfig.IMetadata|null} [metadata] IncrementalTableConfig metadata
             * @property {string|null} [reservation] IncrementalTableConfig reservation
             * @property {dataform.ActionConfig.IncrementalStrategy|null} [incrementalStrategy] IncrementalTableConfig incrementalStrategy
             * @property {Array.<string>|null} [incrementalPredicates] IncrementalTableConfig incrementalPredicates
             */

            /**
             * Constructs a new IncrementalTableConfig.
             * @memberof dataform.ActionConfig
             * @classdesc Represents an IncrementalTableConfig.
             * @implements IIncrementalTableConfig
             * @constructor
             * @param {dataform.ActionConfig.IIncrementalTableConfig=} [properties] Properties to set
             */
            function IncrementalTableConfig(properties) {
                this.dependencyTargets = [];
                this.tags = [];
                this.preOperations = [];
                this.postOperations = [];
                this.uniqueKey = [];
                this.columns = [];
                this.clusterBy = [];
                this.labels = {};
                this.additionalOptions = {};
                this.incrementalPredicates = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * IncrementalTableConfig name.
             * @member {string} name
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.name = "";

            /**
             * IncrementalTableConfig dataset.
             * @member {string} dataset
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.dataset = "";

            /**
             * IncrementalTableConfig project.
             * @member {string} project
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.project = "";

            /**
             * IncrementalTableConfig dependencyTargets.
             * @member {Array.<dataform.ActionConfig.ITarget>} dependencyTargets
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.dependencyTargets = $util.emptyArray;

            /**
             * IncrementalTableConfig filename.
             * @member {string} filename
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.filename = "";

            /**
             * IncrementalTableConfig tags.
             * @member {Array.<string>} tags
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.tags = $util.emptyArray;

            /**
             * IncrementalTableConfig disabled.
             * @member {boolean} disabled
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.disabled = false;

            /**
             * IncrementalTableConfig preOperations.
             * @member {Array.<string>} preOperations
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.preOperations = $util.emptyArray;

            /**
             * IncrementalTableConfig postOperations.
             * @member {Array.<string>} postOperations
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.postOperations = $util.emptyArray;

            /**
             * IncrementalTableConfig protected.
             * @member {boolean} protected
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype["protected"] = false;

            /**
             * IncrementalTableConfig uniqueKey.
             * @member {Array.<string>} uniqueKey
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.uniqueKey = $util.emptyArray;

            /**
             * IncrementalTableConfig description.
             * @member {string} description
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.description = "";

            /**
             * IncrementalTableConfig columns.
             * @member {Array.<dataform.ActionConfig.IColumnDescriptor>} columns
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.columns = $util.emptyArray;

            /**
             * IncrementalTableConfig partitionBy.
             * @member {string} partitionBy
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.partitionBy = "";

            /**
             * IncrementalTableConfig partitionExpirationDays.
             * @member {number} partitionExpirationDays
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.partitionExpirationDays = 0;

            /**
             * IncrementalTableConfig requirePartitionFilter.
             * @member {boolean} requirePartitionFilter
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.requirePartitionFilter = false;

            /**
             * IncrementalTableConfig updatePartitionFilter.
             * @member {string} updatePartitionFilter
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.updatePartitionFilter = "";

            /**
             * IncrementalTableConfig clusterBy.
             * @member {Array.<string>} clusterBy
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.clusterBy = $util.emptyArray;

            /**
             * IncrementalTableConfig labels.
             * @member {Object.<string,string>} labels
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.labels = $util.emptyObject;

            /**
             * IncrementalTableConfig additionalOptions.
             * @member {Object.<string,string>} additionalOptions
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.additionalOptions = $util.emptyObject;

            /**
             * IncrementalTableConfig dependOnDependencyAssertions.
             * @member {boolean} dependOnDependencyAssertions
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.dependOnDependencyAssertions = false;

            /**
             * IncrementalTableConfig assertions.
             * @member {dataform.ActionConfig.ITableAssertionsConfig|null|undefined} assertions
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.assertions = null;

            /**
             * IncrementalTableConfig hermetic.
             * @member {boolean} hermetic
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.hermetic = false;

            /**
             * IncrementalTableConfig onSchemaChange.
             * @member {dataform.ActionConfig.OnSchemaChange} onSchemaChange
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.onSchemaChange = 0;

            /**
             * IncrementalTableConfig iceberg.
             * @member {dataform.ActionConfig.IIcebergTableConfig|null|undefined} iceberg
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.iceberg = null;

            /**
             * IncrementalTableConfig metadata.
             * @member {dataform.ActionConfig.IMetadata|null|undefined} metadata
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.metadata = null;

            /**
             * IncrementalTableConfig reservation.
             * @member {string} reservation
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.reservation = "";

            /**
             * IncrementalTableConfig incrementalStrategy.
             * @member {dataform.ActionConfig.IncrementalStrategy} incrementalStrategy
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.incrementalStrategy = 0;

            /**
             * IncrementalTableConfig incrementalPredicates.
             * @member {Array.<string>} incrementalPredicates
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             */
            IncrementalTableConfig.prototype.incrementalPredicates = $util.emptyArray;

            /**
             * Creates a new IncrementalTableConfig instance using the specified properties.
             * @function create
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @static
             * @param {dataform.ActionConfig.IIncrementalTableConfig=} [properties] Properties to set
             * @returns {dataform.ActionConfig.IncrementalTableConfig} IncrementalTableConfig instance
             */
            IncrementalTableConfig.create = function create(properties) {
                return new IncrementalTableConfig(properties);
            };

            /**
             * Encodes the specified IncrementalTableConfig message. Does not implicitly {@link dataform.ActionConfig.IncrementalTableConfig.verify|verify} messages.
             * @function encode
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @static
             * @param {dataform.ActionConfig.IIncrementalTableConfig} message IncrementalTableConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            IncrementalTableConfig.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.dataset);
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.project);
                if (message.dependencyTargets != null && message.dependencyTargets.length)
                    for (let i = 0; i < message.dependencyTargets.length; ++i)
                        $root.dataform.ActionConfig.Target.encode(message.dependencyTargets[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.filename);
                if (message.tags != null && message.tags.length)
                    for (let i = 0; i < message.tags.length; ++i)
                        writer.uint32(/* id 6, wireType 2 =*/50).string(message.tags[i]);
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    writer.uint32(/* id 7, wireType 0 =*/56).bool(message.disabled);
                if (message.preOperations != null && message.preOperations.length)
                    for (let i = 0; i < message.preOperations.length; ++i)
                        writer.uint32(/* id 8, wireType 2 =*/66).string(message.preOperations[i]);
                if (message.postOperations != null && message.postOperations.length)
                    for (let i = 0; i < message.postOperations.length; ++i)
                        writer.uint32(/* id 9, wireType 2 =*/74).string(message.postOperations[i]);
                if (message["protected"] != null && Object.hasOwnProperty.call(message, "protected"))
                    writer.uint32(/* id 10, wireType 0 =*/80).bool(message["protected"]);
                if (message.uniqueKey != null && message.uniqueKey.length)
                    for (let i = 0; i < message.uniqueKey.length; ++i)
                        writer.uint32(/* id 11, wireType 2 =*/90).string(message.uniqueKey[i]);
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.description);
                if (message.columns != null && message.columns.length)
                    for (let i = 0; i < message.columns.length; ++i)
                        $root.dataform.ActionConfig.ColumnDescriptor.encode(message.columns[i], writer.uint32(/* id 13, wireType 2 =*/106).fork(), q + 1).ldelim();
                if (message.partitionBy != null && Object.hasOwnProperty.call(message, "partitionBy"))
                    writer.uint32(/* id 14, wireType 2 =*/114).string(message.partitionBy);
                if (message.partitionExpirationDays != null && Object.hasOwnProperty.call(message, "partitionExpirationDays"))
                    writer.uint32(/* id 15, wireType 0 =*/120).int32(message.partitionExpirationDays);
                if (message.requirePartitionFilter != null && Object.hasOwnProperty.call(message, "requirePartitionFilter"))
                    writer.uint32(/* id 16, wireType 0 =*/128).bool(message.requirePartitionFilter);
                if (message.updatePartitionFilter != null && Object.hasOwnProperty.call(message, "updatePartitionFilter"))
                    writer.uint32(/* id 17, wireType 2 =*/138).string(message.updatePartitionFilter);
                if (message.clusterBy != null && message.clusterBy.length)
                    for (let i = 0; i < message.clusterBy.length; ++i)
                        writer.uint32(/* id 18, wireType 2 =*/146).string(message.clusterBy[i]);
                if (message.labels != null && Object.hasOwnProperty.call(message, "labels"))
                    for (let keys = Object.keys(message.labels), i = 0; i < keys.length; ++i)
                        writer.uint32(/* id 19, wireType 2 =*/154).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.labels[keys[i]]).ldelim();
                if (message.additionalOptions != null && Object.hasOwnProperty.call(message, "additionalOptions"))
                    for (let keys = Object.keys(message.additionalOptions), i = 0; i < keys.length; ++i)
                        writer.uint32(/* id 20, wireType 2 =*/162).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.additionalOptions[keys[i]]).ldelim();
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    writer.uint32(/* id 21, wireType 0 =*/168).bool(message.dependOnDependencyAssertions);
                if (message.assertions != null && Object.hasOwnProperty.call(message, "assertions"))
                    $root.dataform.ActionConfig.TableAssertionsConfig.encode(message.assertions, writer.uint32(/* id 22, wireType 2 =*/178).fork(), q + 1).ldelim();
                if (message.hermetic != null && Object.hasOwnProperty.call(message, "hermetic"))
                    writer.uint32(/* id 23, wireType 0 =*/184).bool(message.hermetic);
                if (message.onSchemaChange != null && Object.hasOwnProperty.call(message, "onSchemaChange"))
                    writer.uint32(/* id 24, wireType 0 =*/192).int32(message.onSchemaChange);
                if (message.iceberg != null && Object.hasOwnProperty.call(message, "iceberg"))
                    $root.dataform.ActionConfig.IcebergTableConfig.encode(message.iceberg, writer.uint32(/* id 25, wireType 2 =*/202).fork(), q + 1).ldelim();
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                    $root.dataform.ActionConfig.Metadata.encode(message.metadata, writer.uint32(/* id 26, wireType 2 =*/210).fork(), q + 1).ldelim();
                if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                    writer.uint32(/* id 27, wireType 2 =*/218).string(message.reservation);
                if (message.incrementalStrategy != null && Object.hasOwnProperty.call(message, "incrementalStrategy"))
                    writer.uint32(/* id 28, wireType 0 =*/224).int32(message.incrementalStrategy);
                if (message.incrementalPredicates != null && message.incrementalPredicates.length)
                    for (let i = 0; i < message.incrementalPredicates.length; ++i)
                        writer.uint32(/* id 29, wireType 2 =*/234).string(message.incrementalPredicates[i]);
                return writer;
            };

            /**
             * Encodes the specified IncrementalTableConfig message, length delimited. Does not implicitly {@link dataform.ActionConfig.IncrementalTableConfig.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @static
             * @param {dataform.ActionConfig.IIncrementalTableConfig} message IncrementalTableConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            IncrementalTableConfig.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an IncrementalTableConfig message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.ActionConfig.IncrementalTableConfig} IncrementalTableConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            IncrementalTableConfig.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig.IncrementalTableConfig(), key, value;
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.name = reader.string();
                            break;
                        }
                    case 2: {
                            message.dataset = reader.string();
                            break;
                        }
                    case 3: {
                            message.project = reader.string();
                            break;
                        }
                    case 4: {
                            if (!(message.dependencyTargets && message.dependencyTargets.length))
                                message.dependencyTargets = [];
                            message.dependencyTargets.push($root.dataform.ActionConfig.Target.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 5: {
                            message.filename = reader.string();
                            break;
                        }
                    case 6: {
                            if (!(message.tags && message.tags.length))
                                message.tags = [];
                            message.tags.push(reader.string());
                            break;
                        }
                    case 7: {
                            message.disabled = reader.bool();
                            break;
                        }
                    case 8: {
                            if (!(message.preOperations && message.preOperations.length))
                                message.preOperations = [];
                            message.preOperations.push(reader.string());
                            break;
                        }
                    case 9: {
                            if (!(message.postOperations && message.postOperations.length))
                                message.postOperations = [];
                            message.postOperations.push(reader.string());
                            break;
                        }
                    case 10: {
                            message["protected"] = reader.bool();
                            break;
                        }
                    case 11: {
                            if (!(message.uniqueKey && message.uniqueKey.length))
                                message.uniqueKey = [];
                            message.uniqueKey.push(reader.string());
                            break;
                        }
                    case 12: {
                            message.description = reader.string();
                            break;
                        }
                    case 13: {
                            if (!(message.columns && message.columns.length))
                                message.columns = [];
                            message.columns.push($root.dataform.ActionConfig.ColumnDescriptor.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 14: {
                            message.partitionBy = reader.string();
                            break;
                        }
                    case 15: {
                            message.partitionExpirationDays = reader.int32();
                            break;
                        }
                    case 16: {
                            message.requirePartitionFilter = reader.bool();
                            break;
                        }
                    case 17: {
                            message.updatePartitionFilter = reader.string();
                            break;
                        }
                    case 18: {
                            if (!(message.clusterBy && message.clusterBy.length))
                                message.clusterBy = [];
                            message.clusterBy.push(reader.string());
                            break;
                        }
                    case 19: {
                            if (message.labels === $util.emptyObject)
                                message.labels = {};
                            let end2 = reader.uint32() + reader.pos;
                            key = "";
                            value = "";
                            while (reader.pos < end2) {
                                let tag2 = reader.uint32();
                                switch (tag2 >>> 3) {
                                case 1:
                                    key = reader.string();
                                    break;
                                case 2:
                                    value = reader.string();
                                    break;
                                default:
                                    reader.skipType(tag2 & 7, long);
                                    break;
                                }
                            }
                            if (key === "__proto__")
                                $util.makeProp(message.labels, key);
                            message.labels[key] = value;
                            break;
                        }
                    case 20: {
                            if (message.additionalOptions === $util.emptyObject)
                                message.additionalOptions = {};
                            let end2 = reader.uint32() + reader.pos;
                            key = "";
                            value = "";
                            while (reader.pos < end2) {
                                let tag2 = reader.uint32();
                                switch (tag2 >>> 3) {
                                case 1:
                                    key = reader.string();
                                    break;
                                case 2:
                                    value = reader.string();
                                    break;
                                default:
                                    reader.skipType(tag2 & 7, long);
                                    break;
                                }
                            }
                            if (key === "__proto__")
                                $util.makeProp(message.additionalOptions, key);
                            message.additionalOptions[key] = value;
                            break;
                        }
                    case 21: {
                            message.dependOnDependencyAssertions = reader.bool();
                            break;
                        }
                    case 22: {
                            message.assertions = $root.dataform.ActionConfig.TableAssertionsConfig.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 23: {
                            message.hermetic = reader.bool();
                            break;
                        }
                    case 24: {
                            message.onSchemaChange = reader.int32();
                            break;
                        }
                    case 25: {
                            message.iceberg = $root.dataform.ActionConfig.IcebergTableConfig.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 26: {
                            message.metadata = $root.dataform.ActionConfig.Metadata.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 27: {
                            message.reservation = reader.string();
                            break;
                        }
                    case 28: {
                            message.incrementalStrategy = reader.int32();
                            break;
                        }
                    case 29: {
                            if (!(message.incrementalPredicates && message.incrementalPredicates.length))
                                message.incrementalPredicates = [];
                            message.incrementalPredicates.push(reader.string());
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an IncrementalTableConfig message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.ActionConfig.IncrementalTableConfig} IncrementalTableConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            IncrementalTableConfig.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an IncrementalTableConfig message.
             * @function verify
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            IncrementalTableConfig.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    if (!$util.isString(message.dataset))
                        return "dataset: string expected";
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    if (!$util.isString(message.project))
                        return "project: string expected";
                if (message.dependencyTargets != null && Object.hasOwnProperty.call(message, "dependencyTargets")) {
                    if (!Array.isArray(message.dependencyTargets))
                        return "dependencyTargets: array expected";
                    for (let i = 0; i < message.dependencyTargets.length; ++i) {
                        let error = $root.dataform.ActionConfig.Target.verify(message.dependencyTargets[i], long + 1);
                        if (error)
                            return "dependencyTargets." + error;
                    }
                }
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    if (!$util.isString(message.filename))
                        return "filename: string expected";
                if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                    if (!Array.isArray(message.tags))
                        return "tags: array expected";
                    for (let i = 0; i < message.tags.length; ++i)
                        if (!$util.isString(message.tags[i]))
                            return "tags: string[] expected";
                }
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    if (typeof message.disabled !== "boolean")
                        return "disabled: boolean expected";
                if (message.preOperations != null && Object.hasOwnProperty.call(message, "preOperations")) {
                    if (!Array.isArray(message.preOperations))
                        return "preOperations: array expected";
                    for (let i = 0; i < message.preOperations.length; ++i)
                        if (!$util.isString(message.preOperations[i]))
                            return "preOperations: string[] expected";
                }
                if (message.postOperations != null && Object.hasOwnProperty.call(message, "postOperations")) {
                    if (!Array.isArray(message.postOperations))
                        return "postOperations: array expected";
                    for (let i = 0; i < message.postOperations.length; ++i)
                        if (!$util.isString(message.postOperations[i]))
                            return "postOperations: string[] expected";
                }
                if (message["protected"] != null && Object.hasOwnProperty.call(message, "protected"))
                    if (typeof message["protected"] !== "boolean")
                        return "protected: boolean expected";
                if (message.uniqueKey != null && Object.hasOwnProperty.call(message, "uniqueKey")) {
                    if (!Array.isArray(message.uniqueKey))
                        return "uniqueKey: array expected";
                    for (let i = 0; i < message.uniqueKey.length; ++i)
                        if (!$util.isString(message.uniqueKey[i]))
                            return "uniqueKey: string[] expected";
                }
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    if (!$util.isString(message.description))
                        return "description: string expected";
                if (message.columns != null && Object.hasOwnProperty.call(message, "columns")) {
                    if (!Array.isArray(message.columns))
                        return "columns: array expected";
                    for (let i = 0; i < message.columns.length; ++i) {
                        let error = $root.dataform.ActionConfig.ColumnDescriptor.verify(message.columns[i], long + 1);
                        if (error)
                            return "columns." + error;
                    }
                }
                if (message.partitionBy != null && Object.hasOwnProperty.call(message, "partitionBy"))
                    if (!$util.isString(message.partitionBy))
                        return "partitionBy: string expected";
                if (message.partitionExpirationDays != null && Object.hasOwnProperty.call(message, "partitionExpirationDays"))
                    if (!$util.isInteger(message.partitionExpirationDays))
                        return "partitionExpirationDays: integer expected";
                if (message.requirePartitionFilter != null && Object.hasOwnProperty.call(message, "requirePartitionFilter"))
                    if (typeof message.requirePartitionFilter !== "boolean")
                        return "requirePartitionFilter: boolean expected";
                if (message.updatePartitionFilter != null && Object.hasOwnProperty.call(message, "updatePartitionFilter"))
                    if (!$util.isString(message.updatePartitionFilter))
                        return "updatePartitionFilter: string expected";
                if (message.clusterBy != null && Object.hasOwnProperty.call(message, "clusterBy")) {
                    if (!Array.isArray(message.clusterBy))
                        return "clusterBy: array expected";
                    for (let i = 0; i < message.clusterBy.length; ++i)
                        if (!$util.isString(message.clusterBy[i]))
                            return "clusterBy: string[] expected";
                }
                if (message.labels != null && Object.hasOwnProperty.call(message, "labels")) {
                    if (!$util.isObject(message.labels))
                        return "labels: object expected";
                    let key = Object.keys(message.labels);
                    for (let i = 0; i < key.length; ++i)
                        if (!$util.isString(message.labels[key[i]]))
                            return "labels: string{k:string} expected";
                }
                if (message.additionalOptions != null && Object.hasOwnProperty.call(message, "additionalOptions")) {
                    if (!$util.isObject(message.additionalOptions))
                        return "additionalOptions: object expected";
                    let key = Object.keys(message.additionalOptions);
                    for (let i = 0; i < key.length; ++i)
                        if (!$util.isString(message.additionalOptions[key[i]]))
                            return "additionalOptions: string{k:string} expected";
                }
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    if (typeof message.dependOnDependencyAssertions !== "boolean")
                        return "dependOnDependencyAssertions: boolean expected";
                if (message.assertions != null && Object.hasOwnProperty.call(message, "assertions")) {
                    let error = $root.dataform.ActionConfig.TableAssertionsConfig.verify(message.assertions, long + 1);
                    if (error)
                        return "assertions." + error;
                }
                if (message.hermetic != null && Object.hasOwnProperty.call(message, "hermetic"))
                    if (typeof message.hermetic !== "boolean")
                        return "hermetic: boolean expected";
                if (message.onSchemaChange != null && Object.hasOwnProperty.call(message, "onSchemaChange"))
                    switch (message.onSchemaChange) {
                    default:
                        return "onSchemaChange: enum value expected";
                    case 0:
                    case 1:
                    case 2:
                    case 3:
                        break;
                    }
                if (message.iceberg != null && Object.hasOwnProperty.call(message, "iceberg")) {
                    let error = $root.dataform.ActionConfig.IcebergTableConfig.verify(message.iceberg, long + 1);
                    if (error)
                        return "iceberg." + error;
                }
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata")) {
                    let error = $root.dataform.ActionConfig.Metadata.verify(message.metadata, long + 1);
                    if (error)
                        return "metadata." + error;
                }
                if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                    if (!$util.isString(message.reservation))
                        return "reservation: string expected";
                if (message.incrementalStrategy != null && Object.hasOwnProperty.call(message, "incrementalStrategy"))
                    switch (message.incrementalStrategy) {
                    default:
                        return "incrementalStrategy: enum value expected";
                    case 0:
                    case 1:
                    case 2:
                        break;
                    }
                if (message.incrementalPredicates != null && Object.hasOwnProperty.call(message, "incrementalPredicates")) {
                    if (!Array.isArray(message.incrementalPredicates))
                        return "incrementalPredicates: array expected";
                    for (let i = 0; i < message.incrementalPredicates.length; ++i)
                        if (!$util.isString(message.incrementalPredicates[i]))
                            return "incrementalPredicates: string[] expected";
                }
                return null;
            };

            /**
             * Creates an IncrementalTableConfig message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.ActionConfig.IncrementalTableConfig} IncrementalTableConfig
             */
            IncrementalTableConfig.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.ActionConfig.IncrementalTableConfig)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.ActionConfig.IncrementalTableConfig: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.ActionConfig.IncrementalTableConfig();
                if (object.name != null)
                    message.name = String(object.name);
                if (object.dataset != null)
                    message.dataset = String(object.dataset);
                if (object.project != null)
                    message.project = String(object.project);
                if (object.dependencyTargets) {
                    if (!Array.isArray(object.dependencyTargets))
                        throw TypeError(".dataform.ActionConfig.IncrementalTableConfig.dependencyTargets: array expected");
                    message.dependencyTargets = [];
                    for (let i = 0; i < object.dependencyTargets.length; ++i) {
                        if (!$util.isObject(object.dependencyTargets[i]))
                            throw TypeError(".dataform.ActionConfig.IncrementalTableConfig.dependencyTargets: object expected");
                        message.dependencyTargets[i] = $root.dataform.ActionConfig.Target.fromObject(object.dependencyTargets[i], long + 1);
                    }
                }
                if (object.filename != null)
                    message.filename = String(object.filename);
                if (object.tags) {
                    if (!Array.isArray(object.tags))
                        throw TypeError(".dataform.ActionConfig.IncrementalTableConfig.tags: array expected");
                    message.tags = [];
                    for (let i = 0; i < object.tags.length; ++i)
                        message.tags[i] = String(object.tags[i]);
                }
                if (object.disabled != null)
                    message.disabled = Boolean(object.disabled);
                if (object.preOperations) {
                    if (!Array.isArray(object.preOperations))
                        throw TypeError(".dataform.ActionConfig.IncrementalTableConfig.preOperations: array expected");
                    message.preOperations = [];
                    for (let i = 0; i < object.preOperations.length; ++i)
                        message.preOperations[i] = String(object.preOperations[i]);
                }
                if (object.postOperations) {
                    if (!Array.isArray(object.postOperations))
                        throw TypeError(".dataform.ActionConfig.IncrementalTableConfig.postOperations: array expected");
                    message.postOperations = [];
                    for (let i = 0; i < object.postOperations.length; ++i)
                        message.postOperations[i] = String(object.postOperations[i]);
                }
                if (object["protected"] != null)
                    message["protected"] = Boolean(object["protected"]);
                if (object.uniqueKey) {
                    if (!Array.isArray(object.uniqueKey))
                        throw TypeError(".dataform.ActionConfig.IncrementalTableConfig.uniqueKey: array expected");
                    message.uniqueKey = [];
                    for (let i = 0; i < object.uniqueKey.length; ++i)
                        message.uniqueKey[i] = String(object.uniqueKey[i]);
                }
                if (object.description != null)
                    message.description = String(object.description);
                if (object.columns) {
                    if (!Array.isArray(object.columns))
                        throw TypeError(".dataform.ActionConfig.IncrementalTableConfig.columns: array expected");
                    message.columns = [];
                    for (let i = 0; i < object.columns.length; ++i) {
                        if (!$util.isObject(object.columns[i]))
                            throw TypeError(".dataform.ActionConfig.IncrementalTableConfig.columns: object expected");
                        message.columns[i] = $root.dataform.ActionConfig.ColumnDescriptor.fromObject(object.columns[i], long + 1);
                    }
                }
                if (object.partitionBy != null)
                    message.partitionBy = String(object.partitionBy);
                if (object.partitionExpirationDays != null)
                    message.partitionExpirationDays = object.partitionExpirationDays | 0;
                if (object.requirePartitionFilter != null)
                    message.requirePartitionFilter = Boolean(object.requirePartitionFilter);
                if (object.updatePartitionFilter != null)
                    message.updatePartitionFilter = String(object.updatePartitionFilter);
                if (object.clusterBy) {
                    if (!Array.isArray(object.clusterBy))
                        throw TypeError(".dataform.ActionConfig.IncrementalTableConfig.clusterBy: array expected");
                    message.clusterBy = [];
                    for (let i = 0; i < object.clusterBy.length; ++i)
                        message.clusterBy[i] = String(object.clusterBy[i]);
                }
                if (object.labels) {
                    if (!$util.isObject(object.labels))
                        throw TypeError(".dataform.ActionConfig.IncrementalTableConfig.labels: object expected");
                    message.labels = {};
                    for (let keys = Object.keys(object.labels), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.labels, keys[i]);
                        message.labels[keys[i]] = String(object.labels[keys[i]]);
                    }
                }
                if (object.additionalOptions) {
                    if (!$util.isObject(object.additionalOptions))
                        throw TypeError(".dataform.ActionConfig.IncrementalTableConfig.additionalOptions: object expected");
                    message.additionalOptions = {};
                    for (let keys = Object.keys(object.additionalOptions), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.additionalOptions, keys[i]);
                        message.additionalOptions[keys[i]] = String(object.additionalOptions[keys[i]]);
                    }
                }
                if (object.dependOnDependencyAssertions != null)
                    message.dependOnDependencyAssertions = Boolean(object.dependOnDependencyAssertions);
                if (object.assertions != null) {
                    if (!$util.isObject(object.assertions))
                        throw TypeError(".dataform.ActionConfig.IncrementalTableConfig.assertions: object expected");
                    message.assertions = $root.dataform.ActionConfig.TableAssertionsConfig.fromObject(object.assertions, long + 1);
                }
                if (object.hermetic != null)
                    message.hermetic = Boolean(object.hermetic);
                switch (object.onSchemaChange) {
                default:
                    if (typeof object.onSchemaChange === "number") {
                        message.onSchemaChange = object.onSchemaChange;
                        break;
                    }
                    break;
                case "IGNORE":
                case 0:
                    message.onSchemaChange = 0;
                    break;
                case "FAIL":
                case 1:
                    message.onSchemaChange = 1;
                    break;
                case "EXTEND":
                case 2:
                    message.onSchemaChange = 2;
                    break;
                case "SYNCHRONIZE":
                case 3:
                    message.onSchemaChange = 3;
                    break;
                }
                if (object.iceberg != null) {
                    if (!$util.isObject(object.iceberg))
                        throw TypeError(".dataform.ActionConfig.IncrementalTableConfig.iceberg: object expected");
                    message.iceberg = $root.dataform.ActionConfig.IcebergTableConfig.fromObject(object.iceberg, long + 1);
                }
                if (object.metadata != null) {
                    if (!$util.isObject(object.metadata))
                        throw TypeError(".dataform.ActionConfig.IncrementalTableConfig.metadata: object expected");
                    message.metadata = $root.dataform.ActionConfig.Metadata.fromObject(object.metadata, long + 1);
                }
                if (object.reservation != null)
                    message.reservation = String(object.reservation);
                switch (object.incrementalStrategy) {
                default:
                    if (typeof object.incrementalStrategy === "number") {
                        message.incrementalStrategy = object.incrementalStrategy;
                        break;
                    }
                    break;
                case "INCREMENTAL_STRATEGY_UNSPECIFIED":
                case 0:
                    message.incrementalStrategy = 0;
                    break;
                case "INCREMENTAL_STRATEGY_MERGE":
                case 1:
                    message.incrementalStrategy = 1;
                    break;
                case "INCREMENTAL_STRATEGY_INSERT_OVERWRITE":
                case 2:
                    message.incrementalStrategy = 2;
                    break;
                }
                if (object.incrementalPredicates) {
                    if (!Array.isArray(object.incrementalPredicates))
                        throw TypeError(".dataform.ActionConfig.IncrementalTableConfig.incrementalPredicates: array expected");
                    message.incrementalPredicates = [];
                    for (let i = 0; i < object.incrementalPredicates.length; ++i)
                        message.incrementalPredicates[i] = String(object.incrementalPredicates[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from an IncrementalTableConfig message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @static
             * @param {dataform.ActionConfig.IncrementalTableConfig} message IncrementalTableConfig
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            IncrementalTableConfig.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.dependencyTargets = [];
                    object.tags = [];
                    object.preOperations = [];
                    object.postOperations = [];
                    object.uniqueKey = [];
                    object.columns = [];
                    object.clusterBy = [];
                    object.incrementalPredicates = [];
                }
                if (options.objects || options.defaults) {
                    object.labels = {};
                    object.additionalOptions = {};
                }
                if (options.defaults) {
                    object.name = "";
                    object.dataset = "";
                    object.project = "";
                    object.filename = "";
                    object.disabled = false;
                    object["protected"] = false;
                    object.description = "";
                    object.partitionBy = "";
                    object.partitionExpirationDays = 0;
                    object.requirePartitionFilter = false;
                    object.updatePartitionFilter = "";
                    object.dependOnDependencyAssertions = false;
                    object.assertions = null;
                    object.hermetic = false;
                    object.onSchemaChange = options.enums === String ? "IGNORE" : 0;
                    object.iceberg = null;
                    object.metadata = null;
                    object.reservation = "";
                    object.incrementalStrategy = options.enums === String ? "INCREMENTAL_STRATEGY_UNSPECIFIED" : 0;
                }
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    object.dataset = message.dataset;
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    object.project = message.project;
                if (message.dependencyTargets && message.dependencyTargets.length) {
                    object.dependencyTargets = [];
                    for (let j = 0; j < message.dependencyTargets.length; ++j)
                        object.dependencyTargets[j] = $root.dataform.ActionConfig.Target.toObject(message.dependencyTargets[j], options, q + 1);
                }
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    object.filename = message.filename;
                if (message.tags && message.tags.length) {
                    object.tags = [];
                    for (let j = 0; j < message.tags.length; ++j)
                        object.tags[j] = message.tags[j];
                }
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    object.disabled = message.disabled;
                if (message.preOperations && message.preOperations.length) {
                    object.preOperations = [];
                    for (let j = 0; j < message.preOperations.length; ++j)
                        object.preOperations[j] = message.preOperations[j];
                }
                if (message.postOperations && message.postOperations.length) {
                    object.postOperations = [];
                    for (let j = 0; j < message.postOperations.length; ++j)
                        object.postOperations[j] = message.postOperations[j];
                }
                if (message["protected"] != null && Object.hasOwnProperty.call(message, "protected"))
                    object["protected"] = message["protected"];
                if (message.uniqueKey && message.uniqueKey.length) {
                    object.uniqueKey = [];
                    for (let j = 0; j < message.uniqueKey.length; ++j)
                        object.uniqueKey[j] = message.uniqueKey[j];
                }
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    object.description = message.description;
                if (message.columns && message.columns.length) {
                    object.columns = [];
                    for (let j = 0; j < message.columns.length; ++j)
                        object.columns[j] = $root.dataform.ActionConfig.ColumnDescriptor.toObject(message.columns[j], options, q + 1);
                }
                if (message.partitionBy != null && Object.hasOwnProperty.call(message, "partitionBy"))
                    object.partitionBy = message.partitionBy;
                if (message.partitionExpirationDays != null && Object.hasOwnProperty.call(message, "partitionExpirationDays"))
                    object.partitionExpirationDays = message.partitionExpirationDays;
                if (message.requirePartitionFilter != null && Object.hasOwnProperty.call(message, "requirePartitionFilter"))
                    object.requirePartitionFilter = message.requirePartitionFilter;
                if (message.updatePartitionFilter != null && Object.hasOwnProperty.call(message, "updatePartitionFilter"))
                    object.updatePartitionFilter = message.updatePartitionFilter;
                if (message.clusterBy && message.clusterBy.length) {
                    object.clusterBy = [];
                    for (let j = 0; j < message.clusterBy.length; ++j)
                        object.clusterBy[j] = message.clusterBy[j];
                }
                let keys2;
                if (message.labels && (keys2 = Object.keys(message.labels)).length) {
                    object.labels = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.labels, keys2[j]);
                        object.labels[keys2[j]] = message.labels[keys2[j]];
                    }
                }
                if (message.additionalOptions && (keys2 = Object.keys(message.additionalOptions)).length) {
                    object.additionalOptions = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.additionalOptions, keys2[j]);
                        object.additionalOptions[keys2[j]] = message.additionalOptions[keys2[j]];
                    }
                }
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    object.dependOnDependencyAssertions = message.dependOnDependencyAssertions;
                if (message.assertions != null && Object.hasOwnProperty.call(message, "assertions"))
                    object.assertions = $root.dataform.ActionConfig.TableAssertionsConfig.toObject(message.assertions, options, q + 1);
                if (message.hermetic != null && Object.hasOwnProperty.call(message, "hermetic"))
                    object.hermetic = message.hermetic;
                if (message.onSchemaChange != null && Object.hasOwnProperty.call(message, "onSchemaChange"))
                    object.onSchemaChange = options.enums === String ? $root.dataform.ActionConfig.OnSchemaChange[message.onSchemaChange] === undefined ? message.onSchemaChange : $root.dataform.ActionConfig.OnSchemaChange[message.onSchemaChange] : message.onSchemaChange;
                if (message.iceberg != null && Object.hasOwnProperty.call(message, "iceberg"))
                    object.iceberg = $root.dataform.ActionConfig.IcebergTableConfig.toObject(message.iceberg, options, q + 1);
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                    object.metadata = $root.dataform.ActionConfig.Metadata.toObject(message.metadata, options, q + 1);
                if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                    object.reservation = message.reservation;
                if (message.incrementalStrategy != null && Object.hasOwnProperty.call(message, "incrementalStrategy"))
                    object.incrementalStrategy = options.enums === String ? $root.dataform.ActionConfig.IncrementalStrategy[message.incrementalStrategy] === undefined ? message.incrementalStrategy : $root.dataform.ActionConfig.IncrementalStrategy[message.incrementalStrategy] : message.incrementalStrategy;
                if (message.incrementalPredicates && message.incrementalPredicates.length) {
                    object.incrementalPredicates = [];
                    for (let j = 0; j < message.incrementalPredicates.length; ++j)
                        object.incrementalPredicates[j] = message.incrementalPredicates[j];
                }
                return object;
            };

            /**
             * Converts this IncrementalTableConfig to JSON.
             * @function toJSON
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            IncrementalTableConfig.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for IncrementalTableConfig
             * @function getTypeUrl
             * @memberof dataform.ActionConfig.IncrementalTableConfig
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            IncrementalTableConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.ActionConfig.IncrementalTableConfig";
            };

            return IncrementalTableConfig;
        })();

        ActionConfig.AssertionConfig = (function() {

            /**
             * Properties of an AssertionConfig.
             * @memberof dataform.ActionConfig
             * @interface IAssertionConfig
             * @property {string|null} [name] AssertionConfig name
             * @property {string|null} [dataset] AssertionConfig dataset
             * @property {string|null} [project] AssertionConfig project
             * @property {Array.<dataform.ActionConfig.ITarget>|null} [dependencyTargets] AssertionConfig dependencyTargets
             * @property {string|null} [filename] AssertionConfig filename
             * @property {Array.<string>|null} [tags] AssertionConfig tags
             * @property {boolean|null} [disabled] AssertionConfig disabled
             * @property {string|null} [description] AssertionConfig description
             * @property {boolean|null} [hermetic] AssertionConfig hermetic
             * @property {boolean|null} [dependOnDependencyAssertions] AssertionConfig dependOnDependencyAssertions
             * @property {string|null} [reservation] AssertionConfig reservation
             * @property {dataform.ActionConfig.IMetadata|null} [metadata] AssertionConfig metadata
             */

            /**
             * Constructs a new AssertionConfig.
             * @memberof dataform.ActionConfig
             * @classdesc Represents an AssertionConfig.
             * @implements IAssertionConfig
             * @constructor
             * @param {dataform.ActionConfig.IAssertionConfig=} [properties] Properties to set
             */
            function AssertionConfig(properties) {
                this.dependencyTargets = [];
                this.tags = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * AssertionConfig name.
             * @member {string} name
             * @memberof dataform.ActionConfig.AssertionConfig
             * @instance
             */
            AssertionConfig.prototype.name = "";

            /**
             * AssertionConfig dataset.
             * @member {string} dataset
             * @memberof dataform.ActionConfig.AssertionConfig
             * @instance
             */
            AssertionConfig.prototype.dataset = "";

            /**
             * AssertionConfig project.
             * @member {string} project
             * @memberof dataform.ActionConfig.AssertionConfig
             * @instance
             */
            AssertionConfig.prototype.project = "";

            /**
             * AssertionConfig dependencyTargets.
             * @member {Array.<dataform.ActionConfig.ITarget>} dependencyTargets
             * @memberof dataform.ActionConfig.AssertionConfig
             * @instance
             */
            AssertionConfig.prototype.dependencyTargets = $util.emptyArray;

            /**
             * AssertionConfig filename.
             * @member {string} filename
             * @memberof dataform.ActionConfig.AssertionConfig
             * @instance
             */
            AssertionConfig.prototype.filename = "";

            /**
             * AssertionConfig tags.
             * @member {Array.<string>} tags
             * @memberof dataform.ActionConfig.AssertionConfig
             * @instance
             */
            AssertionConfig.prototype.tags = $util.emptyArray;

            /**
             * AssertionConfig disabled.
             * @member {boolean} disabled
             * @memberof dataform.ActionConfig.AssertionConfig
             * @instance
             */
            AssertionConfig.prototype.disabled = false;

            /**
             * AssertionConfig description.
             * @member {string} description
             * @memberof dataform.ActionConfig.AssertionConfig
             * @instance
             */
            AssertionConfig.prototype.description = "";

            /**
             * AssertionConfig hermetic.
             * @member {boolean} hermetic
             * @memberof dataform.ActionConfig.AssertionConfig
             * @instance
             */
            AssertionConfig.prototype.hermetic = false;

            /**
             * AssertionConfig dependOnDependencyAssertions.
             * @member {boolean} dependOnDependencyAssertions
             * @memberof dataform.ActionConfig.AssertionConfig
             * @instance
             */
            AssertionConfig.prototype.dependOnDependencyAssertions = false;

            /**
             * AssertionConfig reservation.
             * @member {string} reservation
             * @memberof dataform.ActionConfig.AssertionConfig
             * @instance
             */
            AssertionConfig.prototype.reservation = "";

            /**
             * AssertionConfig metadata.
             * @member {dataform.ActionConfig.IMetadata|null|undefined} metadata
             * @memberof dataform.ActionConfig.AssertionConfig
             * @instance
             */
            AssertionConfig.prototype.metadata = null;

            /**
             * Creates a new AssertionConfig instance using the specified properties.
             * @function create
             * @memberof dataform.ActionConfig.AssertionConfig
             * @static
             * @param {dataform.ActionConfig.IAssertionConfig=} [properties] Properties to set
             * @returns {dataform.ActionConfig.AssertionConfig} AssertionConfig instance
             */
            AssertionConfig.create = function create(properties) {
                return new AssertionConfig(properties);
            };

            /**
             * Encodes the specified AssertionConfig message. Does not implicitly {@link dataform.ActionConfig.AssertionConfig.verify|verify} messages.
             * @function encode
             * @memberof dataform.ActionConfig.AssertionConfig
             * @static
             * @param {dataform.ActionConfig.IAssertionConfig} message AssertionConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AssertionConfig.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.dataset);
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.project);
                if (message.dependencyTargets != null && message.dependencyTargets.length)
                    for (let i = 0; i < message.dependencyTargets.length; ++i)
                        $root.dataform.ActionConfig.Target.encode(message.dependencyTargets[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.filename);
                if (message.tags != null && message.tags.length)
                    for (let i = 0; i < message.tags.length; ++i)
                        writer.uint32(/* id 6, wireType 2 =*/50).string(message.tags[i]);
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    writer.uint32(/* id 7, wireType 0 =*/56).bool(message.disabled);
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.description);
                if (message.hermetic != null && Object.hasOwnProperty.call(message, "hermetic"))
                    writer.uint32(/* id 9, wireType 0 =*/72).bool(message.hermetic);
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    writer.uint32(/* id 10, wireType 0 =*/80).bool(message.dependOnDependencyAssertions);
                if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                    writer.uint32(/* id 11, wireType 2 =*/90).string(message.reservation);
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                    $root.dataform.ActionConfig.Metadata.encode(message.metadata, writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified AssertionConfig message, length delimited. Does not implicitly {@link dataform.ActionConfig.AssertionConfig.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.ActionConfig.AssertionConfig
             * @static
             * @param {dataform.ActionConfig.IAssertionConfig} message AssertionConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            AssertionConfig.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an AssertionConfig message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.ActionConfig.AssertionConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.ActionConfig.AssertionConfig} AssertionConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AssertionConfig.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig.AssertionConfig();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.name = reader.string();
                            break;
                        }
                    case 2: {
                            message.dataset = reader.string();
                            break;
                        }
                    case 3: {
                            message.project = reader.string();
                            break;
                        }
                    case 4: {
                            if (!(message.dependencyTargets && message.dependencyTargets.length))
                                message.dependencyTargets = [];
                            message.dependencyTargets.push($root.dataform.ActionConfig.Target.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 5: {
                            message.filename = reader.string();
                            break;
                        }
                    case 6: {
                            if (!(message.tags && message.tags.length))
                                message.tags = [];
                            message.tags.push(reader.string());
                            break;
                        }
                    case 7: {
                            message.disabled = reader.bool();
                            break;
                        }
                    case 8: {
                            message.description = reader.string();
                            break;
                        }
                    case 9: {
                            message.hermetic = reader.bool();
                            break;
                        }
                    case 10: {
                            message.dependOnDependencyAssertions = reader.bool();
                            break;
                        }
                    case 11: {
                            message.reservation = reader.string();
                            break;
                        }
                    case 12: {
                            message.metadata = $root.dataform.ActionConfig.Metadata.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an AssertionConfig message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.ActionConfig.AssertionConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.ActionConfig.AssertionConfig} AssertionConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            AssertionConfig.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an AssertionConfig message.
             * @function verify
             * @memberof dataform.ActionConfig.AssertionConfig
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            AssertionConfig.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    if (!$util.isString(message.dataset))
                        return "dataset: string expected";
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    if (!$util.isString(message.project))
                        return "project: string expected";
                if (message.dependencyTargets != null && Object.hasOwnProperty.call(message, "dependencyTargets")) {
                    if (!Array.isArray(message.dependencyTargets))
                        return "dependencyTargets: array expected";
                    for (let i = 0; i < message.dependencyTargets.length; ++i) {
                        let error = $root.dataform.ActionConfig.Target.verify(message.dependencyTargets[i], long + 1);
                        if (error)
                            return "dependencyTargets." + error;
                    }
                }
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    if (!$util.isString(message.filename))
                        return "filename: string expected";
                if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                    if (!Array.isArray(message.tags))
                        return "tags: array expected";
                    for (let i = 0; i < message.tags.length; ++i)
                        if (!$util.isString(message.tags[i]))
                            return "tags: string[] expected";
                }
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    if (typeof message.disabled !== "boolean")
                        return "disabled: boolean expected";
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    if (!$util.isString(message.description))
                        return "description: string expected";
                if (message.hermetic != null && Object.hasOwnProperty.call(message, "hermetic"))
                    if (typeof message.hermetic !== "boolean")
                        return "hermetic: boolean expected";
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    if (typeof message.dependOnDependencyAssertions !== "boolean")
                        return "dependOnDependencyAssertions: boolean expected";
                if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                    if (!$util.isString(message.reservation))
                        return "reservation: string expected";
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata")) {
                    let error = $root.dataform.ActionConfig.Metadata.verify(message.metadata, long + 1);
                    if (error)
                        return "metadata." + error;
                }
                return null;
            };

            /**
             * Creates an AssertionConfig message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.ActionConfig.AssertionConfig
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.ActionConfig.AssertionConfig} AssertionConfig
             */
            AssertionConfig.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.ActionConfig.AssertionConfig)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.ActionConfig.AssertionConfig: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.ActionConfig.AssertionConfig();
                if (object.name != null)
                    message.name = String(object.name);
                if (object.dataset != null)
                    message.dataset = String(object.dataset);
                if (object.project != null)
                    message.project = String(object.project);
                if (object.dependencyTargets) {
                    if (!Array.isArray(object.dependencyTargets))
                        throw TypeError(".dataform.ActionConfig.AssertionConfig.dependencyTargets: array expected");
                    message.dependencyTargets = [];
                    for (let i = 0; i < object.dependencyTargets.length; ++i) {
                        if (!$util.isObject(object.dependencyTargets[i]))
                            throw TypeError(".dataform.ActionConfig.AssertionConfig.dependencyTargets: object expected");
                        message.dependencyTargets[i] = $root.dataform.ActionConfig.Target.fromObject(object.dependencyTargets[i], long + 1);
                    }
                }
                if (object.filename != null)
                    message.filename = String(object.filename);
                if (object.tags) {
                    if (!Array.isArray(object.tags))
                        throw TypeError(".dataform.ActionConfig.AssertionConfig.tags: array expected");
                    message.tags = [];
                    for (let i = 0; i < object.tags.length; ++i)
                        message.tags[i] = String(object.tags[i]);
                }
                if (object.disabled != null)
                    message.disabled = Boolean(object.disabled);
                if (object.description != null)
                    message.description = String(object.description);
                if (object.hermetic != null)
                    message.hermetic = Boolean(object.hermetic);
                if (object.dependOnDependencyAssertions != null)
                    message.dependOnDependencyAssertions = Boolean(object.dependOnDependencyAssertions);
                if (object.reservation != null)
                    message.reservation = String(object.reservation);
                if (object.metadata != null) {
                    if (!$util.isObject(object.metadata))
                        throw TypeError(".dataform.ActionConfig.AssertionConfig.metadata: object expected");
                    message.metadata = $root.dataform.ActionConfig.Metadata.fromObject(object.metadata, long + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from an AssertionConfig message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.ActionConfig.AssertionConfig
             * @static
             * @param {dataform.ActionConfig.AssertionConfig} message AssertionConfig
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            AssertionConfig.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.dependencyTargets = [];
                    object.tags = [];
                }
                if (options.defaults) {
                    object.name = "";
                    object.dataset = "";
                    object.project = "";
                    object.filename = "";
                    object.disabled = false;
                    object.description = "";
                    object.hermetic = false;
                    object.dependOnDependencyAssertions = false;
                    object.reservation = "";
                    object.metadata = null;
                }
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    object.dataset = message.dataset;
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    object.project = message.project;
                if (message.dependencyTargets && message.dependencyTargets.length) {
                    object.dependencyTargets = [];
                    for (let j = 0; j < message.dependencyTargets.length; ++j)
                        object.dependencyTargets[j] = $root.dataform.ActionConfig.Target.toObject(message.dependencyTargets[j], options, q + 1);
                }
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    object.filename = message.filename;
                if (message.tags && message.tags.length) {
                    object.tags = [];
                    for (let j = 0; j < message.tags.length; ++j)
                        object.tags[j] = message.tags[j];
                }
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    object.disabled = message.disabled;
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    object.description = message.description;
                if (message.hermetic != null && Object.hasOwnProperty.call(message, "hermetic"))
                    object.hermetic = message.hermetic;
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    object.dependOnDependencyAssertions = message.dependOnDependencyAssertions;
                if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                    object.reservation = message.reservation;
                if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                    object.metadata = $root.dataform.ActionConfig.Metadata.toObject(message.metadata, options, q + 1);
                return object;
            };

            /**
             * Converts this AssertionConfig to JSON.
             * @function toJSON
             * @memberof dataform.ActionConfig.AssertionConfig
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            AssertionConfig.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for AssertionConfig
             * @function getTypeUrl
             * @memberof dataform.ActionConfig.AssertionConfig
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            AssertionConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.ActionConfig.AssertionConfig";
            };

            return AssertionConfig;
        })();

        ActionConfig.OperationConfig = (function() {

            /**
             * Properties of an OperationConfig.
             * @memberof dataform.ActionConfig
             * @interface IOperationConfig
             * @property {string|null} [name] OperationConfig name
             * @property {string|null} [dataset] OperationConfig dataset
             * @property {string|null} [project] OperationConfig project
             * @property {Array.<dataform.ActionConfig.ITarget>|null} [dependencyTargets] OperationConfig dependencyTargets
             * @property {string|null} [filename] OperationConfig filename
             * @property {Array.<string>|null} [tags] OperationConfig tags
             * @property {boolean|null} [disabled] OperationConfig disabled
             * @property {boolean|null} [hasOutput] OperationConfig hasOutput
             * @property {string|null} [description] OperationConfig description
             * @property {Array.<dataform.ActionConfig.IColumnDescriptor>|null} [columns] OperationConfig columns
             * @property {boolean|null} [dependOnDependencyAssertions] OperationConfig dependOnDependencyAssertions
             * @property {boolean|null} [hermetic] OperationConfig hermetic
             * @property {string|null} [reservation] OperationConfig reservation
             */

            /**
             * Constructs a new OperationConfig.
             * @memberof dataform.ActionConfig
             * @classdesc Represents an OperationConfig.
             * @implements IOperationConfig
             * @constructor
             * @param {dataform.ActionConfig.IOperationConfig=} [properties] Properties to set
             */
            function OperationConfig(properties) {
                this.dependencyTargets = [];
                this.tags = [];
                this.columns = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * OperationConfig name.
             * @member {string} name
             * @memberof dataform.ActionConfig.OperationConfig
             * @instance
             */
            OperationConfig.prototype.name = "";

            /**
             * OperationConfig dataset.
             * @member {string} dataset
             * @memberof dataform.ActionConfig.OperationConfig
             * @instance
             */
            OperationConfig.prototype.dataset = "";

            /**
             * OperationConfig project.
             * @member {string} project
             * @memberof dataform.ActionConfig.OperationConfig
             * @instance
             */
            OperationConfig.prototype.project = "";

            /**
             * OperationConfig dependencyTargets.
             * @member {Array.<dataform.ActionConfig.ITarget>} dependencyTargets
             * @memberof dataform.ActionConfig.OperationConfig
             * @instance
             */
            OperationConfig.prototype.dependencyTargets = $util.emptyArray;

            /**
             * OperationConfig filename.
             * @member {string} filename
             * @memberof dataform.ActionConfig.OperationConfig
             * @instance
             */
            OperationConfig.prototype.filename = "";

            /**
             * OperationConfig tags.
             * @member {Array.<string>} tags
             * @memberof dataform.ActionConfig.OperationConfig
             * @instance
             */
            OperationConfig.prototype.tags = $util.emptyArray;

            /**
             * OperationConfig disabled.
             * @member {boolean} disabled
             * @memberof dataform.ActionConfig.OperationConfig
             * @instance
             */
            OperationConfig.prototype.disabled = false;

            /**
             * OperationConfig hasOutput.
             * @member {boolean} hasOutput
             * @memberof dataform.ActionConfig.OperationConfig
             * @instance
             */
            OperationConfig.prototype.hasOutput = false;

            /**
             * OperationConfig description.
             * @member {string} description
             * @memberof dataform.ActionConfig.OperationConfig
             * @instance
             */
            OperationConfig.prototype.description = "";

            /**
             * OperationConfig columns.
             * @member {Array.<dataform.ActionConfig.IColumnDescriptor>} columns
             * @memberof dataform.ActionConfig.OperationConfig
             * @instance
             */
            OperationConfig.prototype.columns = $util.emptyArray;

            /**
             * OperationConfig dependOnDependencyAssertions.
             * @member {boolean} dependOnDependencyAssertions
             * @memberof dataform.ActionConfig.OperationConfig
             * @instance
             */
            OperationConfig.prototype.dependOnDependencyAssertions = false;

            /**
             * OperationConfig hermetic.
             * @member {boolean} hermetic
             * @memberof dataform.ActionConfig.OperationConfig
             * @instance
             */
            OperationConfig.prototype.hermetic = false;

            /**
             * OperationConfig reservation.
             * @member {string} reservation
             * @memberof dataform.ActionConfig.OperationConfig
             * @instance
             */
            OperationConfig.prototype.reservation = "";

            /**
             * Creates a new OperationConfig instance using the specified properties.
             * @function create
             * @memberof dataform.ActionConfig.OperationConfig
             * @static
             * @param {dataform.ActionConfig.IOperationConfig=} [properties] Properties to set
             * @returns {dataform.ActionConfig.OperationConfig} OperationConfig instance
             */
            OperationConfig.create = function create(properties) {
                return new OperationConfig(properties);
            };

            /**
             * Encodes the specified OperationConfig message. Does not implicitly {@link dataform.ActionConfig.OperationConfig.verify|verify} messages.
             * @function encode
             * @memberof dataform.ActionConfig.OperationConfig
             * @static
             * @param {dataform.ActionConfig.IOperationConfig} message OperationConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OperationConfig.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.dataset);
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.project);
                if (message.dependencyTargets != null && message.dependencyTargets.length)
                    for (let i = 0; i < message.dependencyTargets.length; ++i)
                        $root.dataform.ActionConfig.Target.encode(message.dependencyTargets[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.filename);
                if (message.tags != null && message.tags.length)
                    for (let i = 0; i < message.tags.length; ++i)
                        writer.uint32(/* id 6, wireType 2 =*/50).string(message.tags[i]);
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    writer.uint32(/* id 7, wireType 0 =*/56).bool(message.disabled);
                if (message.hasOutput != null && Object.hasOwnProperty.call(message, "hasOutput"))
                    writer.uint32(/* id 8, wireType 0 =*/64).bool(message.hasOutput);
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.description);
                if (message.columns != null && message.columns.length)
                    for (let i = 0; i < message.columns.length; ++i)
                        $root.dataform.ActionConfig.ColumnDescriptor.encode(message.columns[i], writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    writer.uint32(/* id 11, wireType 0 =*/88).bool(message.dependOnDependencyAssertions);
                if (message.hermetic != null && Object.hasOwnProperty.call(message, "hermetic"))
                    writer.uint32(/* id 12, wireType 0 =*/96).bool(message.hermetic);
                if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                    writer.uint32(/* id 13, wireType 2 =*/106).string(message.reservation);
                return writer;
            };

            /**
             * Encodes the specified OperationConfig message, length delimited. Does not implicitly {@link dataform.ActionConfig.OperationConfig.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.ActionConfig.OperationConfig
             * @static
             * @param {dataform.ActionConfig.IOperationConfig} message OperationConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            OperationConfig.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an OperationConfig message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.ActionConfig.OperationConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.ActionConfig.OperationConfig} OperationConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OperationConfig.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig.OperationConfig();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.name = reader.string();
                            break;
                        }
                    case 2: {
                            message.dataset = reader.string();
                            break;
                        }
                    case 3: {
                            message.project = reader.string();
                            break;
                        }
                    case 4: {
                            if (!(message.dependencyTargets && message.dependencyTargets.length))
                                message.dependencyTargets = [];
                            message.dependencyTargets.push($root.dataform.ActionConfig.Target.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 5: {
                            message.filename = reader.string();
                            break;
                        }
                    case 6: {
                            if (!(message.tags && message.tags.length))
                                message.tags = [];
                            message.tags.push(reader.string());
                            break;
                        }
                    case 7: {
                            message.disabled = reader.bool();
                            break;
                        }
                    case 8: {
                            message.hasOutput = reader.bool();
                            break;
                        }
                    case 9: {
                            message.description = reader.string();
                            break;
                        }
                    case 10: {
                            if (!(message.columns && message.columns.length))
                                message.columns = [];
                            message.columns.push($root.dataform.ActionConfig.ColumnDescriptor.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 11: {
                            message.dependOnDependencyAssertions = reader.bool();
                            break;
                        }
                    case 12: {
                            message.hermetic = reader.bool();
                            break;
                        }
                    case 13: {
                            message.reservation = reader.string();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an OperationConfig message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.ActionConfig.OperationConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.ActionConfig.OperationConfig} OperationConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            OperationConfig.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an OperationConfig message.
             * @function verify
             * @memberof dataform.ActionConfig.OperationConfig
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            OperationConfig.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    if (!$util.isString(message.dataset))
                        return "dataset: string expected";
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    if (!$util.isString(message.project))
                        return "project: string expected";
                if (message.dependencyTargets != null && Object.hasOwnProperty.call(message, "dependencyTargets")) {
                    if (!Array.isArray(message.dependencyTargets))
                        return "dependencyTargets: array expected";
                    for (let i = 0; i < message.dependencyTargets.length; ++i) {
                        let error = $root.dataform.ActionConfig.Target.verify(message.dependencyTargets[i], long + 1);
                        if (error)
                            return "dependencyTargets." + error;
                    }
                }
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    if (!$util.isString(message.filename))
                        return "filename: string expected";
                if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                    if (!Array.isArray(message.tags))
                        return "tags: array expected";
                    for (let i = 0; i < message.tags.length; ++i)
                        if (!$util.isString(message.tags[i]))
                            return "tags: string[] expected";
                }
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    if (typeof message.disabled !== "boolean")
                        return "disabled: boolean expected";
                if (message.hasOutput != null && Object.hasOwnProperty.call(message, "hasOutput"))
                    if (typeof message.hasOutput !== "boolean")
                        return "hasOutput: boolean expected";
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    if (!$util.isString(message.description))
                        return "description: string expected";
                if (message.columns != null && Object.hasOwnProperty.call(message, "columns")) {
                    if (!Array.isArray(message.columns))
                        return "columns: array expected";
                    for (let i = 0; i < message.columns.length; ++i) {
                        let error = $root.dataform.ActionConfig.ColumnDescriptor.verify(message.columns[i], long + 1);
                        if (error)
                            return "columns." + error;
                    }
                }
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    if (typeof message.dependOnDependencyAssertions !== "boolean")
                        return "dependOnDependencyAssertions: boolean expected";
                if (message.hermetic != null && Object.hasOwnProperty.call(message, "hermetic"))
                    if (typeof message.hermetic !== "boolean")
                        return "hermetic: boolean expected";
                if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                    if (!$util.isString(message.reservation))
                        return "reservation: string expected";
                return null;
            };

            /**
             * Creates an OperationConfig message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.ActionConfig.OperationConfig
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.ActionConfig.OperationConfig} OperationConfig
             */
            OperationConfig.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.ActionConfig.OperationConfig)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.ActionConfig.OperationConfig: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.ActionConfig.OperationConfig();
                if (object.name != null)
                    message.name = String(object.name);
                if (object.dataset != null)
                    message.dataset = String(object.dataset);
                if (object.project != null)
                    message.project = String(object.project);
                if (object.dependencyTargets) {
                    if (!Array.isArray(object.dependencyTargets))
                        throw TypeError(".dataform.ActionConfig.OperationConfig.dependencyTargets: array expected");
                    message.dependencyTargets = [];
                    for (let i = 0; i < object.dependencyTargets.length; ++i) {
                        if (!$util.isObject(object.dependencyTargets[i]))
                            throw TypeError(".dataform.ActionConfig.OperationConfig.dependencyTargets: object expected");
                        message.dependencyTargets[i] = $root.dataform.ActionConfig.Target.fromObject(object.dependencyTargets[i], long + 1);
                    }
                }
                if (object.filename != null)
                    message.filename = String(object.filename);
                if (object.tags) {
                    if (!Array.isArray(object.tags))
                        throw TypeError(".dataform.ActionConfig.OperationConfig.tags: array expected");
                    message.tags = [];
                    for (let i = 0; i < object.tags.length; ++i)
                        message.tags[i] = String(object.tags[i]);
                }
                if (object.disabled != null)
                    message.disabled = Boolean(object.disabled);
                if (object.hasOutput != null)
                    message.hasOutput = Boolean(object.hasOutput);
                if (object.description != null)
                    message.description = String(object.description);
                if (object.columns) {
                    if (!Array.isArray(object.columns))
                        throw TypeError(".dataform.ActionConfig.OperationConfig.columns: array expected");
                    message.columns = [];
                    for (let i = 0; i < object.columns.length; ++i) {
                        if (!$util.isObject(object.columns[i]))
                            throw TypeError(".dataform.ActionConfig.OperationConfig.columns: object expected");
                        message.columns[i] = $root.dataform.ActionConfig.ColumnDescriptor.fromObject(object.columns[i], long + 1);
                    }
                }
                if (object.dependOnDependencyAssertions != null)
                    message.dependOnDependencyAssertions = Boolean(object.dependOnDependencyAssertions);
                if (object.hermetic != null)
                    message.hermetic = Boolean(object.hermetic);
                if (object.reservation != null)
                    message.reservation = String(object.reservation);
                return message;
            };

            /**
             * Creates a plain object from an OperationConfig message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.ActionConfig.OperationConfig
             * @static
             * @param {dataform.ActionConfig.OperationConfig} message OperationConfig
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            OperationConfig.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.dependencyTargets = [];
                    object.tags = [];
                    object.columns = [];
                }
                if (options.defaults) {
                    object.name = "";
                    object.dataset = "";
                    object.project = "";
                    object.filename = "";
                    object.disabled = false;
                    object.hasOutput = false;
                    object.description = "";
                    object.dependOnDependencyAssertions = false;
                    object.hermetic = false;
                    object.reservation = "";
                }
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    object.dataset = message.dataset;
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    object.project = message.project;
                if (message.dependencyTargets && message.dependencyTargets.length) {
                    object.dependencyTargets = [];
                    for (let j = 0; j < message.dependencyTargets.length; ++j)
                        object.dependencyTargets[j] = $root.dataform.ActionConfig.Target.toObject(message.dependencyTargets[j], options, q + 1);
                }
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    object.filename = message.filename;
                if (message.tags && message.tags.length) {
                    object.tags = [];
                    for (let j = 0; j < message.tags.length; ++j)
                        object.tags[j] = message.tags[j];
                }
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    object.disabled = message.disabled;
                if (message.hasOutput != null && Object.hasOwnProperty.call(message, "hasOutput"))
                    object.hasOutput = message.hasOutput;
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    object.description = message.description;
                if (message.columns && message.columns.length) {
                    object.columns = [];
                    for (let j = 0; j < message.columns.length; ++j)
                        object.columns[j] = $root.dataform.ActionConfig.ColumnDescriptor.toObject(message.columns[j], options, q + 1);
                }
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    object.dependOnDependencyAssertions = message.dependOnDependencyAssertions;
                if (message.hermetic != null && Object.hasOwnProperty.call(message, "hermetic"))
                    object.hermetic = message.hermetic;
                if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                    object.reservation = message.reservation;
                return object;
            };

            /**
             * Converts this OperationConfig to JSON.
             * @function toJSON
             * @memberof dataform.ActionConfig.OperationConfig
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            OperationConfig.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for OperationConfig
             * @function getTypeUrl
             * @memberof dataform.ActionConfig.OperationConfig
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            OperationConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.ActionConfig.OperationConfig";
            };

            return OperationConfig;
        })();

        ActionConfig.DeclarationConfig = (function() {

            /**
             * Properties of a DeclarationConfig.
             * @memberof dataform.ActionConfig
             * @interface IDeclarationConfig
             * @property {string|null} [name] DeclarationConfig name
             * @property {string|null} [dataset] DeclarationConfig dataset
             * @property {string|null} [project] DeclarationConfig project
             * @property {string|null} [description] DeclarationConfig description
             * @property {Array.<dataform.ActionConfig.IColumnDescriptor>|null} [columns] DeclarationConfig columns
             * @property {string|null} [filename] DeclarationConfig filename
             * @property {Array.<string>|null} [tags] DeclarationConfig tags
             */

            /**
             * Constructs a new DeclarationConfig.
             * @memberof dataform.ActionConfig
             * @classdesc Represents a DeclarationConfig.
             * @implements IDeclarationConfig
             * @constructor
             * @param {dataform.ActionConfig.IDeclarationConfig=} [properties] Properties to set
             */
            function DeclarationConfig(properties) {
                this.columns = [];
                this.tags = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * DeclarationConfig name.
             * @member {string} name
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @instance
             */
            DeclarationConfig.prototype.name = "";

            /**
             * DeclarationConfig dataset.
             * @member {string} dataset
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @instance
             */
            DeclarationConfig.prototype.dataset = "";

            /**
             * DeclarationConfig project.
             * @member {string} project
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @instance
             */
            DeclarationConfig.prototype.project = "";

            /**
             * DeclarationConfig description.
             * @member {string} description
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @instance
             */
            DeclarationConfig.prototype.description = "";

            /**
             * DeclarationConfig columns.
             * @member {Array.<dataform.ActionConfig.IColumnDescriptor>} columns
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @instance
             */
            DeclarationConfig.prototype.columns = $util.emptyArray;

            /**
             * DeclarationConfig filename.
             * @member {string} filename
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @instance
             */
            DeclarationConfig.prototype.filename = "";

            /**
             * DeclarationConfig tags.
             * @member {Array.<string>} tags
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @instance
             */
            DeclarationConfig.prototype.tags = $util.emptyArray;

            /**
             * Creates a new DeclarationConfig instance using the specified properties.
             * @function create
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @static
             * @param {dataform.ActionConfig.IDeclarationConfig=} [properties] Properties to set
             * @returns {dataform.ActionConfig.DeclarationConfig} DeclarationConfig instance
             */
            DeclarationConfig.create = function create(properties) {
                return new DeclarationConfig(properties);
            };

            /**
             * Encodes the specified DeclarationConfig message. Does not implicitly {@link dataform.ActionConfig.DeclarationConfig.verify|verify} messages.
             * @function encode
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @static
             * @param {dataform.ActionConfig.IDeclarationConfig} message DeclarationConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeclarationConfig.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.dataset);
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.project);
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.description);
                if (message.columns != null && message.columns.length)
                    for (let i = 0; i < message.columns.length; ++i)
                        $root.dataform.ActionConfig.ColumnDescriptor.encode(message.columns[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.filename);
                if (message.tags != null && message.tags.length)
                    for (let i = 0; i < message.tags.length; ++i)
                        writer.uint32(/* id 7, wireType 2 =*/58).string(message.tags[i]);
                return writer;
            };

            /**
             * Encodes the specified DeclarationConfig message, length delimited. Does not implicitly {@link dataform.ActionConfig.DeclarationConfig.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @static
             * @param {dataform.ActionConfig.IDeclarationConfig} message DeclarationConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DeclarationConfig.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a DeclarationConfig message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.ActionConfig.DeclarationConfig} DeclarationConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeclarationConfig.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig.DeclarationConfig();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.name = reader.string();
                            break;
                        }
                    case 2: {
                            message.dataset = reader.string();
                            break;
                        }
                    case 3: {
                            message.project = reader.string();
                            break;
                        }
                    case 4: {
                            message.description = reader.string();
                            break;
                        }
                    case 5: {
                            if (!(message.columns && message.columns.length))
                                message.columns = [];
                            message.columns.push($root.dataform.ActionConfig.ColumnDescriptor.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 6: {
                            message.filename = reader.string();
                            break;
                        }
                    case 7: {
                            if (!(message.tags && message.tags.length))
                                message.tags = [];
                            message.tags.push(reader.string());
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a DeclarationConfig message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.ActionConfig.DeclarationConfig} DeclarationConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DeclarationConfig.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DeclarationConfig message.
             * @function verify
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DeclarationConfig.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    if (!$util.isString(message.dataset))
                        return "dataset: string expected";
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    if (!$util.isString(message.project))
                        return "project: string expected";
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    if (!$util.isString(message.description))
                        return "description: string expected";
                if (message.columns != null && Object.hasOwnProperty.call(message, "columns")) {
                    if (!Array.isArray(message.columns))
                        return "columns: array expected";
                    for (let i = 0; i < message.columns.length; ++i) {
                        let error = $root.dataform.ActionConfig.ColumnDescriptor.verify(message.columns[i], long + 1);
                        if (error)
                            return "columns." + error;
                    }
                }
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    if (!$util.isString(message.filename))
                        return "filename: string expected";
                if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                    if (!Array.isArray(message.tags))
                        return "tags: array expected";
                    for (let i = 0; i < message.tags.length; ++i)
                        if (!$util.isString(message.tags[i]))
                            return "tags: string[] expected";
                }
                return null;
            };

            /**
             * Creates a DeclarationConfig message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.ActionConfig.DeclarationConfig} DeclarationConfig
             */
            DeclarationConfig.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.ActionConfig.DeclarationConfig)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.ActionConfig.DeclarationConfig: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.ActionConfig.DeclarationConfig();
                if (object.name != null)
                    message.name = String(object.name);
                if (object.dataset != null)
                    message.dataset = String(object.dataset);
                if (object.project != null)
                    message.project = String(object.project);
                if (object.description != null)
                    message.description = String(object.description);
                if (object.columns) {
                    if (!Array.isArray(object.columns))
                        throw TypeError(".dataform.ActionConfig.DeclarationConfig.columns: array expected");
                    message.columns = [];
                    for (let i = 0; i < object.columns.length; ++i) {
                        if (!$util.isObject(object.columns[i]))
                            throw TypeError(".dataform.ActionConfig.DeclarationConfig.columns: object expected");
                        message.columns[i] = $root.dataform.ActionConfig.ColumnDescriptor.fromObject(object.columns[i], long + 1);
                    }
                }
                if (object.filename != null)
                    message.filename = String(object.filename);
                if (object.tags) {
                    if (!Array.isArray(object.tags))
                        throw TypeError(".dataform.ActionConfig.DeclarationConfig.tags: array expected");
                    message.tags = [];
                    for (let i = 0; i < object.tags.length; ++i)
                        message.tags[i] = String(object.tags[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a DeclarationConfig message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @static
             * @param {dataform.ActionConfig.DeclarationConfig} message DeclarationConfig
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DeclarationConfig.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.columns = [];
                    object.tags = [];
                }
                if (options.defaults) {
                    object.name = "";
                    object.dataset = "";
                    object.project = "";
                    object.description = "";
                    object.filename = "";
                }
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    object.dataset = message.dataset;
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    object.project = message.project;
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    object.description = message.description;
                if (message.columns && message.columns.length) {
                    object.columns = [];
                    for (let j = 0; j < message.columns.length; ++j)
                        object.columns[j] = $root.dataform.ActionConfig.ColumnDescriptor.toObject(message.columns[j], options, q + 1);
                }
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    object.filename = message.filename;
                if (message.tags && message.tags.length) {
                    object.tags = [];
                    for (let j = 0; j < message.tags.length; ++j)
                        object.tags[j] = message.tags[j];
                }
                return object;
            };

            /**
             * Converts this DeclarationConfig to JSON.
             * @function toJSON
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DeclarationConfig.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for DeclarationConfig
             * @function getTypeUrl
             * @memberof dataform.ActionConfig.DeclarationConfig
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            DeclarationConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.ActionConfig.DeclarationConfig";
            };

            return DeclarationConfig;
        })();

        ActionConfig.NotebookConfig = (function() {

            /**
             * Properties of a NotebookConfig.
             * @memberof dataform.ActionConfig
             * @interface INotebookConfig
             * @property {string|null} [name] NotebookConfig name
             * @property {string|null} [location] NotebookConfig location
             * @property {string|null} [project] NotebookConfig project
             * @property {Array.<dataform.ActionConfig.ITarget>|null} [dependencyTargets] NotebookConfig dependencyTargets
             * @property {string|null} [filename] NotebookConfig filename
             * @property {Array.<string>|null} [tags] NotebookConfig tags
             * @property {boolean|null} [disabled] NotebookConfig disabled
             * @property {string|null} [description] NotebookConfig description
             * @property {boolean|null} [dependOnDependencyAssertions] NotebookConfig dependOnDependencyAssertions
             */

            /**
             * Constructs a new NotebookConfig.
             * @memberof dataform.ActionConfig
             * @classdesc Represents a NotebookConfig.
             * @implements INotebookConfig
             * @constructor
             * @param {dataform.ActionConfig.INotebookConfig=} [properties] Properties to set
             */
            function NotebookConfig(properties) {
                this.dependencyTargets = [];
                this.tags = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * NotebookConfig name.
             * @member {string} name
             * @memberof dataform.ActionConfig.NotebookConfig
             * @instance
             */
            NotebookConfig.prototype.name = "";

            /**
             * NotebookConfig location.
             * @member {string} location
             * @memberof dataform.ActionConfig.NotebookConfig
             * @instance
             */
            NotebookConfig.prototype.location = "";

            /**
             * NotebookConfig project.
             * @member {string} project
             * @memberof dataform.ActionConfig.NotebookConfig
             * @instance
             */
            NotebookConfig.prototype.project = "";

            /**
             * NotebookConfig dependencyTargets.
             * @member {Array.<dataform.ActionConfig.ITarget>} dependencyTargets
             * @memberof dataform.ActionConfig.NotebookConfig
             * @instance
             */
            NotebookConfig.prototype.dependencyTargets = $util.emptyArray;

            /**
             * NotebookConfig filename.
             * @member {string} filename
             * @memberof dataform.ActionConfig.NotebookConfig
             * @instance
             */
            NotebookConfig.prototype.filename = "";

            /**
             * NotebookConfig tags.
             * @member {Array.<string>} tags
             * @memberof dataform.ActionConfig.NotebookConfig
             * @instance
             */
            NotebookConfig.prototype.tags = $util.emptyArray;

            /**
             * NotebookConfig disabled.
             * @member {boolean} disabled
             * @memberof dataform.ActionConfig.NotebookConfig
             * @instance
             */
            NotebookConfig.prototype.disabled = false;

            /**
             * NotebookConfig description.
             * @member {string} description
             * @memberof dataform.ActionConfig.NotebookConfig
             * @instance
             */
            NotebookConfig.prototype.description = "";

            /**
             * NotebookConfig dependOnDependencyAssertions.
             * @member {boolean} dependOnDependencyAssertions
             * @memberof dataform.ActionConfig.NotebookConfig
             * @instance
             */
            NotebookConfig.prototype.dependOnDependencyAssertions = false;

            /**
             * Creates a new NotebookConfig instance using the specified properties.
             * @function create
             * @memberof dataform.ActionConfig.NotebookConfig
             * @static
             * @param {dataform.ActionConfig.INotebookConfig=} [properties] Properties to set
             * @returns {dataform.ActionConfig.NotebookConfig} NotebookConfig instance
             */
            NotebookConfig.create = function create(properties) {
                return new NotebookConfig(properties);
            };

            /**
             * Encodes the specified NotebookConfig message. Does not implicitly {@link dataform.ActionConfig.NotebookConfig.verify|verify} messages.
             * @function encode
             * @memberof dataform.ActionConfig.NotebookConfig
             * @static
             * @param {dataform.ActionConfig.INotebookConfig} message NotebookConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NotebookConfig.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.location);
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.project);
                if (message.dependencyTargets != null && message.dependencyTargets.length)
                    for (let i = 0; i < message.dependencyTargets.length; ++i)
                        $root.dataform.ActionConfig.Target.encode(message.dependencyTargets[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.filename);
                if (message.tags != null && message.tags.length)
                    for (let i = 0; i < message.tags.length; ++i)
                        writer.uint32(/* id 6, wireType 2 =*/50).string(message.tags[i]);
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    writer.uint32(/* id 7, wireType 0 =*/56).bool(message.disabled);
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.description);
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    writer.uint32(/* id 9, wireType 0 =*/72).bool(message.dependOnDependencyAssertions);
                return writer;
            };

            /**
             * Encodes the specified NotebookConfig message, length delimited. Does not implicitly {@link dataform.ActionConfig.NotebookConfig.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.ActionConfig.NotebookConfig
             * @static
             * @param {dataform.ActionConfig.INotebookConfig} message NotebookConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            NotebookConfig.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a NotebookConfig message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.ActionConfig.NotebookConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.ActionConfig.NotebookConfig} NotebookConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NotebookConfig.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig.NotebookConfig();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.name = reader.string();
                            break;
                        }
                    case 2: {
                            message.location = reader.string();
                            break;
                        }
                    case 3: {
                            message.project = reader.string();
                            break;
                        }
                    case 4: {
                            if (!(message.dependencyTargets && message.dependencyTargets.length))
                                message.dependencyTargets = [];
                            message.dependencyTargets.push($root.dataform.ActionConfig.Target.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 5: {
                            message.filename = reader.string();
                            break;
                        }
                    case 6: {
                            if (!(message.tags && message.tags.length))
                                message.tags = [];
                            message.tags.push(reader.string());
                            break;
                        }
                    case 7: {
                            message.disabled = reader.bool();
                            break;
                        }
                    case 8: {
                            message.description = reader.string();
                            break;
                        }
                    case 9: {
                            message.dependOnDependencyAssertions = reader.bool();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a NotebookConfig message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.ActionConfig.NotebookConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.ActionConfig.NotebookConfig} NotebookConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            NotebookConfig.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a NotebookConfig message.
             * @function verify
             * @memberof dataform.ActionConfig.NotebookConfig
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            NotebookConfig.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                    if (!$util.isString(message.location))
                        return "location: string expected";
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    if (!$util.isString(message.project))
                        return "project: string expected";
                if (message.dependencyTargets != null && Object.hasOwnProperty.call(message, "dependencyTargets")) {
                    if (!Array.isArray(message.dependencyTargets))
                        return "dependencyTargets: array expected";
                    for (let i = 0; i < message.dependencyTargets.length; ++i) {
                        let error = $root.dataform.ActionConfig.Target.verify(message.dependencyTargets[i], long + 1);
                        if (error)
                            return "dependencyTargets." + error;
                    }
                }
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    if (!$util.isString(message.filename))
                        return "filename: string expected";
                if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                    if (!Array.isArray(message.tags))
                        return "tags: array expected";
                    for (let i = 0; i < message.tags.length; ++i)
                        if (!$util.isString(message.tags[i]))
                            return "tags: string[] expected";
                }
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    if (typeof message.disabled !== "boolean")
                        return "disabled: boolean expected";
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    if (!$util.isString(message.description))
                        return "description: string expected";
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    if (typeof message.dependOnDependencyAssertions !== "boolean")
                        return "dependOnDependencyAssertions: boolean expected";
                return null;
            };

            /**
             * Creates a NotebookConfig message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.ActionConfig.NotebookConfig
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.ActionConfig.NotebookConfig} NotebookConfig
             */
            NotebookConfig.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.ActionConfig.NotebookConfig)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.ActionConfig.NotebookConfig: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.ActionConfig.NotebookConfig();
                if (object.name != null)
                    message.name = String(object.name);
                if (object.location != null)
                    message.location = String(object.location);
                if (object.project != null)
                    message.project = String(object.project);
                if (object.dependencyTargets) {
                    if (!Array.isArray(object.dependencyTargets))
                        throw TypeError(".dataform.ActionConfig.NotebookConfig.dependencyTargets: array expected");
                    message.dependencyTargets = [];
                    for (let i = 0; i < object.dependencyTargets.length; ++i) {
                        if (!$util.isObject(object.dependencyTargets[i]))
                            throw TypeError(".dataform.ActionConfig.NotebookConfig.dependencyTargets: object expected");
                        message.dependencyTargets[i] = $root.dataform.ActionConfig.Target.fromObject(object.dependencyTargets[i], long + 1);
                    }
                }
                if (object.filename != null)
                    message.filename = String(object.filename);
                if (object.tags) {
                    if (!Array.isArray(object.tags))
                        throw TypeError(".dataform.ActionConfig.NotebookConfig.tags: array expected");
                    message.tags = [];
                    for (let i = 0; i < object.tags.length; ++i)
                        message.tags[i] = String(object.tags[i]);
                }
                if (object.disabled != null)
                    message.disabled = Boolean(object.disabled);
                if (object.description != null)
                    message.description = String(object.description);
                if (object.dependOnDependencyAssertions != null)
                    message.dependOnDependencyAssertions = Boolean(object.dependOnDependencyAssertions);
                return message;
            };

            /**
             * Creates a plain object from a NotebookConfig message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.ActionConfig.NotebookConfig
             * @static
             * @param {dataform.ActionConfig.NotebookConfig} message NotebookConfig
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            NotebookConfig.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.dependencyTargets = [];
                    object.tags = [];
                }
                if (options.defaults) {
                    object.name = "";
                    object.location = "";
                    object.project = "";
                    object.filename = "";
                    object.disabled = false;
                    object.description = "";
                    object.dependOnDependencyAssertions = false;
                }
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                    object.location = message.location;
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    object.project = message.project;
                if (message.dependencyTargets && message.dependencyTargets.length) {
                    object.dependencyTargets = [];
                    for (let j = 0; j < message.dependencyTargets.length; ++j)
                        object.dependencyTargets[j] = $root.dataform.ActionConfig.Target.toObject(message.dependencyTargets[j], options, q + 1);
                }
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    object.filename = message.filename;
                if (message.tags && message.tags.length) {
                    object.tags = [];
                    for (let j = 0; j < message.tags.length; ++j)
                        object.tags[j] = message.tags[j];
                }
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    object.disabled = message.disabled;
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    object.description = message.description;
                if (message.dependOnDependencyAssertions != null && Object.hasOwnProperty.call(message, "dependOnDependencyAssertions"))
                    object.dependOnDependencyAssertions = message.dependOnDependencyAssertions;
                return object;
            };

            /**
             * Converts this NotebookConfig to JSON.
             * @function toJSON
             * @memberof dataform.ActionConfig.NotebookConfig
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            NotebookConfig.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for NotebookConfig
             * @function getTypeUrl
             * @memberof dataform.ActionConfig.NotebookConfig
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            NotebookConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.ActionConfig.NotebookConfig";
            };

            return NotebookConfig;
        })();

        ActionConfig.DataPreparationConfig = (function() {

            /**
             * Properties of a DataPreparationConfig.
             * @memberof dataform.ActionConfig
             * @interface IDataPreparationConfig
             * @property {string|null} [name] DataPreparationConfig name
             * @property {string|null} [dataset] DataPreparationConfig dataset
             * @property {string|null} [project] DataPreparationConfig project
             * @property {Array.<dataform.ActionConfig.ITarget>|null} [dependencyTargets] DataPreparationConfig dependencyTargets
             * @property {string|null} [filename] DataPreparationConfig filename
             * @property {Array.<string>|null} [tags] DataPreparationConfig tags
             * @property {boolean|null} [disabled] DataPreparationConfig disabled
             * @property {string|null} [description] DataPreparationConfig description
             * @property {dataform.ActionConfig.DataPreparationConfig.IErrorTableConfig|null} [errorTable] DataPreparationConfig errorTable
             * @property {dataform.ActionConfig.ILoadModeConfig|null} [loadMode] DataPreparationConfig loadMode
             */

            /**
             * Constructs a new DataPreparationConfig.
             * @memberof dataform.ActionConfig
             * @classdesc Represents a DataPreparationConfig.
             * @implements IDataPreparationConfig
             * @constructor
             * @param {dataform.ActionConfig.IDataPreparationConfig=} [properties] Properties to set
             */
            function DataPreparationConfig(properties) {
                this.dependencyTargets = [];
                this.tags = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * DataPreparationConfig name.
             * @member {string} name
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @instance
             */
            DataPreparationConfig.prototype.name = "";

            /**
             * DataPreparationConfig dataset.
             * @member {string} dataset
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @instance
             */
            DataPreparationConfig.prototype.dataset = "";

            /**
             * DataPreparationConfig project.
             * @member {string} project
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @instance
             */
            DataPreparationConfig.prototype.project = "";

            /**
             * DataPreparationConfig dependencyTargets.
             * @member {Array.<dataform.ActionConfig.ITarget>} dependencyTargets
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @instance
             */
            DataPreparationConfig.prototype.dependencyTargets = $util.emptyArray;

            /**
             * DataPreparationConfig filename.
             * @member {string} filename
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @instance
             */
            DataPreparationConfig.prototype.filename = "";

            /**
             * DataPreparationConfig tags.
             * @member {Array.<string>} tags
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @instance
             */
            DataPreparationConfig.prototype.tags = $util.emptyArray;

            /**
             * DataPreparationConfig disabled.
             * @member {boolean} disabled
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @instance
             */
            DataPreparationConfig.prototype.disabled = false;

            /**
             * DataPreparationConfig description.
             * @member {string} description
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @instance
             */
            DataPreparationConfig.prototype.description = "";

            /**
             * DataPreparationConfig errorTable.
             * @member {dataform.ActionConfig.DataPreparationConfig.IErrorTableConfig|null|undefined} errorTable
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @instance
             */
            DataPreparationConfig.prototype.errorTable = null;

            /**
             * DataPreparationConfig loadMode.
             * @member {dataform.ActionConfig.ILoadModeConfig|null|undefined} loadMode
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @instance
             */
            DataPreparationConfig.prototype.loadMode = null;

            /**
             * Creates a new DataPreparationConfig instance using the specified properties.
             * @function create
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @static
             * @param {dataform.ActionConfig.IDataPreparationConfig=} [properties] Properties to set
             * @returns {dataform.ActionConfig.DataPreparationConfig} DataPreparationConfig instance
             */
            DataPreparationConfig.create = function create(properties) {
                return new DataPreparationConfig(properties);
            };

            /**
             * Encodes the specified DataPreparationConfig message. Does not implicitly {@link dataform.ActionConfig.DataPreparationConfig.verify|verify} messages.
             * @function encode
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @static
             * @param {dataform.ActionConfig.IDataPreparationConfig} message DataPreparationConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DataPreparationConfig.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                if (message.dependencyTargets != null && message.dependencyTargets.length)
                    for (let i = 0; i < message.dependencyTargets.length; ++i)
                        $root.dataform.ActionConfig.Target.encode(message.dependencyTargets[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.filename);
                if (message.tags != null && message.tags.length)
                    for (let i = 0; i < message.tags.length; ++i)
                        writer.uint32(/* id 4, wireType 2 =*/34).string(message.tags[i]);
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    writer.uint32(/* id 7, wireType 0 =*/56).bool(message.disabled);
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.description);
                if (message.errorTable != null && Object.hasOwnProperty.call(message, "errorTable"))
                    $root.dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig.encode(message.errorTable, writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
                if (message.loadMode != null && Object.hasOwnProperty.call(message, "loadMode"))
                    $root.dataform.ActionConfig.LoadModeConfig.encode(message.loadMode, writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    writer.uint32(/* id 13, wireType 2 =*/106).string(message.dataset);
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    writer.uint32(/* id 14, wireType 2 =*/114).string(message.project);
                return writer;
            };

            /**
             * Encodes the specified DataPreparationConfig message, length delimited. Does not implicitly {@link dataform.ActionConfig.DataPreparationConfig.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @static
             * @param {dataform.ActionConfig.IDataPreparationConfig} message DataPreparationConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            DataPreparationConfig.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a DataPreparationConfig message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.ActionConfig.DataPreparationConfig} DataPreparationConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DataPreparationConfig.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig.DataPreparationConfig();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.name = reader.string();
                            break;
                        }
                    case 13: {
                            message.dataset = reader.string();
                            break;
                        }
                    case 14: {
                            message.project = reader.string();
                            break;
                        }
                    case 2: {
                            if (!(message.dependencyTargets && message.dependencyTargets.length))
                                message.dependencyTargets = [];
                            message.dependencyTargets.push($root.dataform.ActionConfig.Target.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    case 3: {
                            message.filename = reader.string();
                            break;
                        }
                    case 4: {
                            if (!(message.tags && message.tags.length))
                                message.tags = [];
                            message.tags.push(reader.string());
                            break;
                        }
                    case 7: {
                            message.disabled = reader.bool();
                            break;
                        }
                    case 8: {
                            message.description = reader.string();
                            break;
                        }
                    case 11: {
                            message.errorTable = $root.dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 12: {
                            message.loadMode = $root.dataform.ActionConfig.LoadModeConfig.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a DataPreparationConfig message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.ActionConfig.DataPreparationConfig} DataPreparationConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            DataPreparationConfig.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a DataPreparationConfig message.
             * @function verify
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            DataPreparationConfig.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    if (!$util.isString(message.name))
                        return "name: string expected";
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    if (!$util.isString(message.dataset))
                        return "dataset: string expected";
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    if (!$util.isString(message.project))
                        return "project: string expected";
                if (message.dependencyTargets != null && Object.hasOwnProperty.call(message, "dependencyTargets")) {
                    if (!Array.isArray(message.dependencyTargets))
                        return "dependencyTargets: array expected";
                    for (let i = 0; i < message.dependencyTargets.length; ++i) {
                        let error = $root.dataform.ActionConfig.Target.verify(message.dependencyTargets[i], long + 1);
                        if (error)
                            return "dependencyTargets." + error;
                    }
                }
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    if (!$util.isString(message.filename))
                        return "filename: string expected";
                if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                    if (!Array.isArray(message.tags))
                        return "tags: array expected";
                    for (let i = 0; i < message.tags.length; ++i)
                        if (!$util.isString(message.tags[i]))
                            return "tags: string[] expected";
                }
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    if (typeof message.disabled !== "boolean")
                        return "disabled: boolean expected";
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    if (!$util.isString(message.description))
                        return "description: string expected";
                if (message.errorTable != null && Object.hasOwnProperty.call(message, "errorTable")) {
                    let error = $root.dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig.verify(message.errorTable, long + 1);
                    if (error)
                        return "errorTable." + error;
                }
                if (message.loadMode != null && Object.hasOwnProperty.call(message, "loadMode")) {
                    let error = $root.dataform.ActionConfig.LoadModeConfig.verify(message.loadMode, long + 1);
                    if (error)
                        return "loadMode." + error;
                }
                return null;
            };

            /**
             * Creates a DataPreparationConfig message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.ActionConfig.DataPreparationConfig} DataPreparationConfig
             */
            DataPreparationConfig.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.ActionConfig.DataPreparationConfig)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.ActionConfig.DataPreparationConfig: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.ActionConfig.DataPreparationConfig();
                if (object.name != null)
                    message.name = String(object.name);
                if (object.dataset != null)
                    message.dataset = String(object.dataset);
                if (object.project != null)
                    message.project = String(object.project);
                if (object.dependencyTargets) {
                    if (!Array.isArray(object.dependencyTargets))
                        throw TypeError(".dataform.ActionConfig.DataPreparationConfig.dependencyTargets: array expected");
                    message.dependencyTargets = [];
                    for (let i = 0; i < object.dependencyTargets.length; ++i) {
                        if (!$util.isObject(object.dependencyTargets[i]))
                            throw TypeError(".dataform.ActionConfig.DataPreparationConfig.dependencyTargets: object expected");
                        message.dependencyTargets[i] = $root.dataform.ActionConfig.Target.fromObject(object.dependencyTargets[i], long + 1);
                    }
                }
                if (object.filename != null)
                    message.filename = String(object.filename);
                if (object.tags) {
                    if (!Array.isArray(object.tags))
                        throw TypeError(".dataform.ActionConfig.DataPreparationConfig.tags: array expected");
                    message.tags = [];
                    for (let i = 0; i < object.tags.length; ++i)
                        message.tags[i] = String(object.tags[i]);
                }
                if (object.disabled != null)
                    message.disabled = Boolean(object.disabled);
                if (object.description != null)
                    message.description = String(object.description);
                if (object.errorTable != null) {
                    if (!$util.isObject(object.errorTable))
                        throw TypeError(".dataform.ActionConfig.DataPreparationConfig.errorTable: object expected");
                    message.errorTable = $root.dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig.fromObject(object.errorTable, long + 1);
                }
                if (object.loadMode != null) {
                    if (!$util.isObject(object.loadMode))
                        throw TypeError(".dataform.ActionConfig.DataPreparationConfig.loadMode: object expected");
                    message.loadMode = $root.dataform.ActionConfig.LoadModeConfig.fromObject(object.loadMode, long + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a DataPreparationConfig message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @static
             * @param {dataform.ActionConfig.DataPreparationConfig} message DataPreparationConfig
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            DataPreparationConfig.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults) {
                    object.dependencyTargets = [];
                    object.tags = [];
                }
                if (options.defaults) {
                    object.name = "";
                    object.filename = "";
                    object.disabled = false;
                    object.description = "";
                    object.errorTable = null;
                    object.loadMode = null;
                    object.dataset = "";
                    object.project = "";
                }
                if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                    object.name = message.name;
                if (message.dependencyTargets && message.dependencyTargets.length) {
                    object.dependencyTargets = [];
                    for (let j = 0; j < message.dependencyTargets.length; ++j)
                        object.dependencyTargets[j] = $root.dataform.ActionConfig.Target.toObject(message.dependencyTargets[j], options, q + 1);
                }
                if (message.filename != null && Object.hasOwnProperty.call(message, "filename"))
                    object.filename = message.filename;
                if (message.tags && message.tags.length) {
                    object.tags = [];
                    for (let j = 0; j < message.tags.length; ++j)
                        object.tags[j] = message.tags[j];
                }
                if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                    object.disabled = message.disabled;
                if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                    object.description = message.description;
                if (message.errorTable != null && Object.hasOwnProperty.call(message, "errorTable"))
                    object.errorTable = $root.dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig.toObject(message.errorTable, options, q + 1);
                if (message.loadMode != null && Object.hasOwnProperty.call(message, "loadMode"))
                    object.loadMode = $root.dataform.ActionConfig.LoadModeConfig.toObject(message.loadMode, options, q + 1);
                if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                    object.dataset = message.dataset;
                if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                    object.project = message.project;
                return object;
            };

            /**
             * Converts this DataPreparationConfig to JSON.
             * @function toJSON
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            DataPreparationConfig.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for DataPreparationConfig
             * @function getTypeUrl
             * @memberof dataform.ActionConfig.DataPreparationConfig
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            DataPreparationConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.ActionConfig.DataPreparationConfig";
            };

            DataPreparationConfig.ErrorTableConfig = (function() {

                /**
                 * Properties of an ErrorTableConfig.
                 * @memberof dataform.ActionConfig.DataPreparationConfig
                 * @interface IErrorTableConfig
                 * @property {string|null} [name] ErrorTableConfig name
                 * @property {string|null} [dataset] ErrorTableConfig dataset
                 * @property {string|null} [project] ErrorTableConfig project
                 * @property {number|null} [retentionDays] ErrorTableConfig retentionDays
                 */

                /**
                 * Constructs a new ErrorTableConfig.
                 * @memberof dataform.ActionConfig.DataPreparationConfig
                 * @classdesc Represents an ErrorTableConfig.
                 * @implements IErrorTableConfig
                 * @constructor
                 * @param {dataform.ActionConfig.DataPreparationConfig.IErrorTableConfig=} [properties] Properties to set
                 */
                function ErrorTableConfig(properties) {
                    if (properties)
                        for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                            if (properties[keys[i]] != null && keys[i] !== "__proto__")
                                this[keys[i]] = properties[keys[i]];
                }

                /**
                 * ErrorTableConfig name.
                 * @member {string} name
                 * @memberof dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig
                 * @instance
                 */
                ErrorTableConfig.prototype.name = "";

                /**
                 * ErrorTableConfig dataset.
                 * @member {string} dataset
                 * @memberof dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig
                 * @instance
                 */
                ErrorTableConfig.prototype.dataset = "";

                /**
                 * ErrorTableConfig project.
                 * @member {string} project
                 * @memberof dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig
                 * @instance
                 */
                ErrorTableConfig.prototype.project = "";

                /**
                 * ErrorTableConfig retentionDays.
                 * @member {number} retentionDays
                 * @memberof dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig
                 * @instance
                 */
                ErrorTableConfig.prototype.retentionDays = 0;

                /**
                 * Creates a new ErrorTableConfig instance using the specified properties.
                 * @function create
                 * @memberof dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig
                 * @static
                 * @param {dataform.ActionConfig.DataPreparationConfig.IErrorTableConfig=} [properties] Properties to set
                 * @returns {dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig} ErrorTableConfig instance
                 */
                ErrorTableConfig.create = function create(properties) {
                    return new ErrorTableConfig(properties);
                };

                /**
                 * Encodes the specified ErrorTableConfig message. Does not implicitly {@link dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig.verify|verify} messages.
                 * @function encode
                 * @memberof dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig
                 * @static
                 * @param {dataform.ActionConfig.DataPreparationConfig.IErrorTableConfig} message ErrorTableConfig message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ErrorTableConfig.encode = function encode(message, writer, q) {
                    if (!writer)
                        writer = $Writer.create();
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                        writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
                    if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                        writer.uint32(/* id 2, wireType 2 =*/18).string(message.dataset);
                    if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.project);
                    if (message.retentionDays != null && Object.hasOwnProperty.call(message, "retentionDays"))
                        writer.uint32(/* id 4, wireType 0 =*/32).int32(message.retentionDays);
                    return writer;
                };

                /**
                 * Encodes the specified ErrorTableConfig message, length delimited. Does not implicitly {@link dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig.verify|verify} messages.
                 * @function encodeDelimited
                 * @memberof dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig
                 * @static
                 * @param {dataform.ActionConfig.DataPreparationConfig.IErrorTableConfig} message ErrorTableConfig message or plain object to encode
                 * @param {$protobuf.Writer} [writer] Writer to encode to
                 * @returns {$protobuf.Writer} Writer
                 */
                ErrorTableConfig.encodeDelimited = function encodeDelimited(message, writer) {
                    return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
                };

                /**
                 * Decodes an ErrorTableConfig message from the specified reader or buffer.
                 * @function decode
                 * @memberof dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @param {number} [length] Message length if known beforehand
                 * @returns {dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig} ErrorTableConfig
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ErrorTableConfig.decode = function decode(reader, length, error, long) {
                    if (!(reader instanceof $Reader))
                        reader = $Reader.create(reader);
                    if (long === undefined)
                        long = 0;
                    if (long > $Reader.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig();
                    while (reader.pos < end) {
                        let tag = reader.uint32();
                        if (tag === error)
                            break;
                        switch (tag >>> 3) {
                        case 1: {
                                message.name = reader.string();
                                break;
                            }
                        case 2: {
                                message.dataset = reader.string();
                                break;
                            }
                        case 3: {
                                message.project = reader.string();
                                break;
                            }
                        case 4: {
                                message.retentionDays = reader.int32();
                                break;
                            }
                        default:
                            reader.skipType(tag & 7, long);
                            break;
                        }
                    }
                    return message;
                };

                /**
                 * Decodes an ErrorTableConfig message from the specified reader or buffer, length delimited.
                 * @function decodeDelimited
                 * @memberof dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig
                 * @static
                 * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
                 * @returns {dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig} ErrorTableConfig
                 * @throws {Error} If the payload is not a reader or valid buffer
                 * @throws {$protobuf.util.ProtocolError} If required fields are missing
                 */
                ErrorTableConfig.decodeDelimited = function decodeDelimited(reader) {
                    if (!(reader instanceof $Reader))
                        reader = new $Reader(reader);
                    return this.decode(reader, reader.uint32());
                };

                /**
                 * Verifies an ErrorTableConfig message.
                 * @function verify
                 * @memberof dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig
                 * @static
                 * @param {Object.<string,*>} message Plain object to verify
                 * @returns {string|null} `null` if valid, otherwise the reason why it is not
                 */
                ErrorTableConfig.verify = function verify(message, long) {
                    if (typeof message !== "object" || message === null)
                        return "object expected";
                    if (long === undefined)
                        long = 0;
                    if (long > $util.recursionLimit)
                        return "maximum nesting depth exceeded";
                    if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                        if (!$util.isString(message.name))
                            return "name: string expected";
                    if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                        if (!$util.isString(message.dataset))
                            return "dataset: string expected";
                    if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                        if (!$util.isString(message.project))
                            return "project: string expected";
                    if (message.retentionDays != null && Object.hasOwnProperty.call(message, "retentionDays"))
                        if (!$util.isInteger(message.retentionDays))
                            return "retentionDays: integer expected";
                    return null;
                };

                /**
                 * Creates an ErrorTableConfig message from a plain object. Also converts values to their respective internal types.
                 * @function fromObject
                 * @memberof dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig
                 * @static
                 * @param {Object.<string,*>} object Plain object
                 * @returns {dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig} ErrorTableConfig
                 */
                ErrorTableConfig.fromObject = function fromObject(object, long) {
                    if (object instanceof $root.dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig)
                        return object;
                    if (!$util.isObject(object))
                        throw TypeError(".dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig: object expected");
                    if (long === undefined)
                        long = 0;
                    if (long > $util.recursionLimit)
                        throw Error("maximum nesting depth exceeded");
                    let message = new $root.dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig();
                    if (object.name != null)
                        message.name = String(object.name);
                    if (object.dataset != null)
                        message.dataset = String(object.dataset);
                    if (object.project != null)
                        message.project = String(object.project);
                    if (object.retentionDays != null)
                        message.retentionDays = object.retentionDays | 0;
                    return message;
                };

                /**
                 * Creates a plain object from an ErrorTableConfig message. Also converts values to other types if specified.
                 * @function toObject
                 * @memberof dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig
                 * @static
                 * @param {dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig} message ErrorTableConfig
                 * @param {$protobuf.IConversionOptions} [options] Conversion options
                 * @returns {Object.<string,*>} Plain object
                 */
                ErrorTableConfig.toObject = function toObject(message, options, q) {
                    if (!options)
                        options = {};
                    if (q === undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw Error("max depth exceeded");
                    let object = {};
                    if (options.defaults) {
                        object.name = "";
                        object.dataset = "";
                        object.project = "";
                        object.retentionDays = 0;
                    }
                    if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                        object.name = message.name;
                    if (message.dataset != null && Object.hasOwnProperty.call(message, "dataset"))
                        object.dataset = message.dataset;
                    if (message.project != null && Object.hasOwnProperty.call(message, "project"))
                        object.project = message.project;
                    if (message.retentionDays != null && Object.hasOwnProperty.call(message, "retentionDays"))
                        object.retentionDays = message.retentionDays;
                    return object;
                };

                /**
                 * Converts this ErrorTableConfig to JSON.
                 * @function toJSON
                 * @memberof dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig
                 * @instance
                 * @returns {Object.<string,*>} JSON object
                 */
                ErrorTableConfig.prototype.toJSON = function toJSON() {
                    return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
                };

                /**
                 * Gets the default type url for ErrorTableConfig
                 * @function getTypeUrl
                 * @memberof dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig
                 * @static
                 * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
                 * @returns {string} The default type url
                 */
                ErrorTableConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                    if (typeUrlPrefix === undefined) {
                        typeUrlPrefix = "type.googleapis.com";
                    }
                    return typeUrlPrefix + "/dataform.ActionConfig.DataPreparationConfig.ErrorTableConfig";
                };

                return ErrorTableConfig;
            })();

            return DataPreparationConfig;
        })();

        ActionConfig.LoadModeConfig = (function() {

            /**
             * Properties of a LoadModeConfig.
             * @memberof dataform.ActionConfig
             * @interface ILoadModeConfig
             * @property {dataform.ActionConfig.LoadMode|null} [mode] LoadModeConfig mode
             * @property {string|null} [incrementalColumn] LoadModeConfig incrementalColumn
             * @property {Array.<string>|null} [uniqueKey] LoadModeConfig uniqueKey
             */

            /**
             * Constructs a new LoadModeConfig.
             * @memberof dataform.ActionConfig
             * @classdesc Represents a LoadModeConfig.
             * @implements ILoadModeConfig
             * @constructor
             * @param {dataform.ActionConfig.ILoadModeConfig=} [properties] Properties to set
             */
            function LoadModeConfig(properties) {
                this.uniqueKey = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * LoadModeConfig mode.
             * @member {dataform.ActionConfig.LoadMode} mode
             * @memberof dataform.ActionConfig.LoadModeConfig
             * @instance
             */
            LoadModeConfig.prototype.mode = 0;

            /**
             * LoadModeConfig incrementalColumn.
             * @member {string} incrementalColumn
             * @memberof dataform.ActionConfig.LoadModeConfig
             * @instance
             */
            LoadModeConfig.prototype.incrementalColumn = "";

            /**
             * LoadModeConfig uniqueKey.
             * @member {Array.<string>} uniqueKey
             * @memberof dataform.ActionConfig.LoadModeConfig
             * @instance
             */
            LoadModeConfig.prototype.uniqueKey = $util.emptyArray;

            /**
             * Creates a new LoadModeConfig instance using the specified properties.
             * @function create
             * @memberof dataform.ActionConfig.LoadModeConfig
             * @static
             * @param {dataform.ActionConfig.ILoadModeConfig=} [properties] Properties to set
             * @returns {dataform.ActionConfig.LoadModeConfig} LoadModeConfig instance
             */
            LoadModeConfig.create = function create(properties) {
                return new LoadModeConfig(properties);
            };

            /**
             * Encodes the specified LoadModeConfig message. Does not implicitly {@link dataform.ActionConfig.LoadModeConfig.verify|verify} messages.
             * @function encode
             * @memberof dataform.ActionConfig.LoadModeConfig
             * @static
             * @param {dataform.ActionConfig.ILoadModeConfig} message LoadModeConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadModeConfig.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.mode);
                if (message.incrementalColumn != null && Object.hasOwnProperty.call(message, "incrementalColumn"))
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.incrementalColumn);
                if (message.uniqueKey != null && message.uniqueKey.length)
                    for (let i = 0; i < message.uniqueKey.length; ++i)
                        writer.uint32(/* id 3, wireType 2 =*/26).string(message.uniqueKey[i]);
                return writer;
            };

            /**
             * Encodes the specified LoadModeConfig message, length delimited. Does not implicitly {@link dataform.ActionConfig.LoadModeConfig.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.ActionConfig.LoadModeConfig
             * @static
             * @param {dataform.ActionConfig.ILoadModeConfig} message LoadModeConfig message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            LoadModeConfig.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a LoadModeConfig message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.ActionConfig.LoadModeConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.ActionConfig.LoadModeConfig} LoadModeConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadModeConfig.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionConfig.LoadModeConfig();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.mode = reader.int32();
                            break;
                        }
                    case 2: {
                            message.incrementalColumn = reader.string();
                            break;
                        }
                    case 3: {
                            if (!(message.uniqueKey && message.uniqueKey.length))
                                message.uniqueKey = [];
                            message.uniqueKey.push(reader.string());
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a LoadModeConfig message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.ActionConfig.LoadModeConfig
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.ActionConfig.LoadModeConfig} LoadModeConfig
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            LoadModeConfig.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a LoadModeConfig message.
             * @function verify
             * @memberof dataform.ActionConfig.LoadModeConfig
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            LoadModeConfig.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                    switch (message.mode) {
                    default:
                        return "mode: enum value expected";
                    case 0:
                    case 1:
                    case 2:
                    case 3:
                    case 4:
                        break;
                    }
                if (message.incrementalColumn != null && Object.hasOwnProperty.call(message, "incrementalColumn"))
                    if (!$util.isString(message.incrementalColumn))
                        return "incrementalColumn: string expected";
                if (message.uniqueKey != null && Object.hasOwnProperty.call(message, "uniqueKey")) {
                    if (!Array.isArray(message.uniqueKey))
                        return "uniqueKey: array expected";
                    for (let i = 0; i < message.uniqueKey.length; ++i)
                        if (!$util.isString(message.uniqueKey[i]))
                            return "uniqueKey: string[] expected";
                }
                return null;
            };

            /**
             * Creates a LoadModeConfig message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.ActionConfig.LoadModeConfig
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.ActionConfig.LoadModeConfig} LoadModeConfig
             */
            LoadModeConfig.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.ActionConfig.LoadModeConfig)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.ActionConfig.LoadModeConfig: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.ActionConfig.LoadModeConfig();
                switch (object.mode) {
                default:
                    if (typeof object.mode === "number") {
                        message.mode = object.mode;
                        break;
                    }
                    break;
                case "REPLACE_TABLE":
                case 0:
                    message.mode = 0;
                    break;
                case "APPEND":
                case 1:
                    message.mode = 1;
                    break;
                case "MAXIMUM":
                case 2:
                    message.mode = 2;
                    break;
                case "UNIQUE":
                case 3:
                    message.mode = 3;
                    break;
                case "MERGE":
                case 4:
                    message.mode = 4;
                    break;
                }
                if (object.incrementalColumn != null)
                    message.incrementalColumn = String(object.incrementalColumn);
                if (object.uniqueKey) {
                    if (!Array.isArray(object.uniqueKey))
                        throw TypeError(".dataform.ActionConfig.LoadModeConfig.uniqueKey: array expected");
                    message.uniqueKey = [];
                    for (let i = 0; i < object.uniqueKey.length; ++i)
                        message.uniqueKey[i] = String(object.uniqueKey[i]);
                }
                return message;
            };

            /**
             * Creates a plain object from a LoadModeConfig message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.ActionConfig.LoadModeConfig
             * @static
             * @param {dataform.ActionConfig.LoadModeConfig} message LoadModeConfig
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            LoadModeConfig.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.uniqueKey = [];
                if (options.defaults) {
                    object.mode = options.enums === String ? "REPLACE_TABLE" : 0;
                    object.incrementalColumn = "";
                }
                if (message.mode != null && Object.hasOwnProperty.call(message, "mode"))
                    object.mode = options.enums === String ? $root.dataform.ActionConfig.LoadMode[message.mode] === undefined ? message.mode : $root.dataform.ActionConfig.LoadMode[message.mode] : message.mode;
                if (message.incrementalColumn != null && Object.hasOwnProperty.call(message, "incrementalColumn"))
                    object.incrementalColumn = message.incrementalColumn;
                if (message.uniqueKey && message.uniqueKey.length) {
                    object.uniqueKey = [];
                    for (let j = 0; j < message.uniqueKey.length; ++j)
                        object.uniqueKey[j] = message.uniqueKey[j];
                }
                return object;
            };

            /**
             * Converts this LoadModeConfig to JSON.
             * @function toJSON
             * @memberof dataform.ActionConfig.LoadModeConfig
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            LoadModeConfig.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for LoadModeConfig
             * @function getTypeUrl
             * @memberof dataform.ActionConfig.LoadModeConfig
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            LoadModeConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.ActionConfig.LoadModeConfig";
            };

            return LoadModeConfig;
        })();

        /**
         * LoadMode enum.
         * @name dataform.ActionConfig.LoadMode
         * @enum {number}
         * @property {number} REPLACE_TABLE=0 REPLACE_TABLE value
         * @property {number} APPEND=1 APPEND value
         * @property {number} MAXIMUM=2 MAXIMUM value
         * @property {number} UNIQUE=3 UNIQUE value
         * @property {number} MERGE=4 MERGE value
         */
        ActionConfig.LoadMode = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "REPLACE_TABLE"] = 0;
            values[valuesById[1] = "APPEND"] = 1;
            values[valuesById[2] = "MAXIMUM"] = 2;
            values[valuesById[3] = "UNIQUE"] = 3;
            values[valuesById[4] = "MERGE"] = 4;
            return values;
        })();

        return ActionConfig;
    })();

    dataform.RepositorySnapshotDestinationConfig = (function() {

        /**
         * Properties of a RepositorySnapshotDestinationConfig.
         * @memberof dataform
         * @interface IRepositorySnapshotDestinationConfig
         * @property {string|null} [repositorySnapshotUri] RepositorySnapshotDestinationConfig repositorySnapshotUri
         */

        /**
         * Constructs a new RepositorySnapshotDestinationConfig.
         * @memberof dataform
         * @classdesc Represents a RepositorySnapshotDestinationConfig.
         * @implements IRepositorySnapshotDestinationConfig
         * @constructor
         * @param {dataform.IRepositorySnapshotDestinationConfig=} [properties] Properties to set
         */
        function RepositorySnapshotDestinationConfig(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RepositorySnapshotDestinationConfig repositorySnapshotUri.
         * @member {string} repositorySnapshotUri
         * @memberof dataform.RepositorySnapshotDestinationConfig
         * @instance
         */
        RepositorySnapshotDestinationConfig.prototype.repositorySnapshotUri = "";

        /**
         * Creates a new RepositorySnapshotDestinationConfig instance using the specified properties.
         * @function create
         * @memberof dataform.RepositorySnapshotDestinationConfig
         * @static
         * @param {dataform.IRepositorySnapshotDestinationConfig=} [properties] Properties to set
         * @returns {dataform.RepositorySnapshotDestinationConfig} RepositorySnapshotDestinationConfig instance
         */
        RepositorySnapshotDestinationConfig.create = function create(properties) {
            return new RepositorySnapshotDestinationConfig(properties);
        };

        /**
         * Encodes the specified RepositorySnapshotDestinationConfig message. Does not implicitly {@link dataform.RepositorySnapshotDestinationConfig.verify|verify} messages.
         * @function encode
         * @memberof dataform.RepositorySnapshotDestinationConfig
         * @static
         * @param {dataform.IRepositorySnapshotDestinationConfig} message RepositorySnapshotDestinationConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RepositorySnapshotDestinationConfig.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.repositorySnapshotUri != null && Object.hasOwnProperty.call(message, "repositorySnapshotUri"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.repositorySnapshotUri);
            return writer;
        };

        /**
         * Encodes the specified RepositorySnapshotDestinationConfig message, length delimited. Does not implicitly {@link dataform.RepositorySnapshotDestinationConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.RepositorySnapshotDestinationConfig
         * @static
         * @param {dataform.IRepositorySnapshotDestinationConfig} message RepositorySnapshotDestinationConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RepositorySnapshotDestinationConfig.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RepositorySnapshotDestinationConfig message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.RepositorySnapshotDestinationConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.RepositorySnapshotDestinationConfig} RepositorySnapshotDestinationConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RepositorySnapshotDestinationConfig.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.RepositorySnapshotDestinationConfig();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.repositorySnapshotUri = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RepositorySnapshotDestinationConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.RepositorySnapshotDestinationConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.RepositorySnapshotDestinationConfig} RepositorySnapshotDestinationConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RepositorySnapshotDestinationConfig.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RepositorySnapshotDestinationConfig message.
         * @function verify
         * @memberof dataform.RepositorySnapshotDestinationConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RepositorySnapshotDestinationConfig.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.repositorySnapshotUri != null && Object.hasOwnProperty.call(message, "repositorySnapshotUri"))
                if (!$util.isString(message.repositorySnapshotUri))
                    return "repositorySnapshotUri: string expected";
            return null;
        };

        /**
         * Creates a RepositorySnapshotDestinationConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.RepositorySnapshotDestinationConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.RepositorySnapshotDestinationConfig} RepositorySnapshotDestinationConfig
         */
        RepositorySnapshotDestinationConfig.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.RepositorySnapshotDestinationConfig)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.RepositorySnapshotDestinationConfig: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.RepositorySnapshotDestinationConfig();
            if (object.repositorySnapshotUri != null)
                message.repositorySnapshotUri = String(object.repositorySnapshotUri);
            return message;
        };

        /**
         * Creates a plain object from a RepositorySnapshotDestinationConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.RepositorySnapshotDestinationConfig
         * @static
         * @param {dataform.RepositorySnapshotDestinationConfig} message RepositorySnapshotDestinationConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RepositorySnapshotDestinationConfig.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.repositorySnapshotUri = "";
            if (message.repositorySnapshotUri != null && Object.hasOwnProperty.call(message, "repositorySnapshotUri"))
                object.repositorySnapshotUri = message.repositorySnapshotUri;
            return object;
        };

        /**
         * Converts this RepositorySnapshotDestinationConfig to JSON.
         * @function toJSON
         * @memberof dataform.RepositorySnapshotDestinationConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RepositorySnapshotDestinationConfig.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RepositorySnapshotDestinationConfig
         * @function getTypeUrl
         * @memberof dataform.RepositorySnapshotDestinationConfig
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RepositorySnapshotDestinationConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.RepositorySnapshotDestinationConfig";
        };

        return RepositorySnapshotDestinationConfig;
    })();

    dataform.NotebookRuntimeOptionsConfig = (function() {

        /**
         * Properties of a NotebookRuntimeOptionsConfig.
         * @memberof dataform
         * @interface INotebookRuntimeOptionsConfig
         * @property {string|null} [outputBucket] NotebookRuntimeOptionsConfig outputBucket
         * @property {string|null} [runtimeTemplateName] NotebookRuntimeOptionsConfig runtimeTemplateName
         * @property {dataform.IRepositorySnapshotDestinationConfig|null} [repositorySnapshotDestination] NotebookRuntimeOptionsConfig repositorySnapshotDestination
         */

        /**
         * Constructs a new NotebookRuntimeOptionsConfig.
         * @memberof dataform
         * @classdesc Represents a NotebookRuntimeOptionsConfig.
         * @implements INotebookRuntimeOptionsConfig
         * @constructor
         * @param {dataform.INotebookRuntimeOptionsConfig=} [properties] Properties to set
         */
        function NotebookRuntimeOptionsConfig(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NotebookRuntimeOptionsConfig outputBucket.
         * @member {string|null|undefined} outputBucket
         * @memberof dataform.NotebookRuntimeOptionsConfig
         * @instance
         */
        NotebookRuntimeOptionsConfig.prototype.outputBucket = null;

        /**
         * NotebookRuntimeOptionsConfig runtimeTemplateName.
         * @member {string} runtimeTemplateName
         * @memberof dataform.NotebookRuntimeOptionsConfig
         * @instance
         */
        NotebookRuntimeOptionsConfig.prototype.runtimeTemplateName = "";

        /**
         * NotebookRuntimeOptionsConfig repositorySnapshotDestination.
         * @member {dataform.IRepositorySnapshotDestinationConfig|null|undefined} repositorySnapshotDestination
         * @memberof dataform.NotebookRuntimeOptionsConfig
         * @instance
         */
        NotebookRuntimeOptionsConfig.prototype.repositorySnapshotDestination = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * NotebookRuntimeOptionsConfig outputSink.
         * @member {"outputBucket"|undefined} outputSink
         * @memberof dataform.NotebookRuntimeOptionsConfig
         * @instance
         */
        Object.defineProperty(NotebookRuntimeOptionsConfig.prototype, "outputSink", {
            get: $util.oneOfGetter($oneOfFields = ["outputBucket"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * NotebookRuntimeOptionsConfig repositorySnapshotStorage.
         * @member {"repositorySnapshotDestination"|undefined} repositorySnapshotStorage
         * @memberof dataform.NotebookRuntimeOptionsConfig
         * @instance
         */
        Object.defineProperty(NotebookRuntimeOptionsConfig.prototype, "repositorySnapshotStorage", {
            get: $util.oneOfGetter($oneOfFields = ["repositorySnapshotDestination"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new NotebookRuntimeOptionsConfig instance using the specified properties.
         * @function create
         * @memberof dataform.NotebookRuntimeOptionsConfig
         * @static
         * @param {dataform.INotebookRuntimeOptionsConfig=} [properties] Properties to set
         * @returns {dataform.NotebookRuntimeOptionsConfig} NotebookRuntimeOptionsConfig instance
         */
        NotebookRuntimeOptionsConfig.create = function create(properties) {
            return new NotebookRuntimeOptionsConfig(properties);
        };

        /**
         * Encodes the specified NotebookRuntimeOptionsConfig message. Does not implicitly {@link dataform.NotebookRuntimeOptionsConfig.verify|verify} messages.
         * @function encode
         * @memberof dataform.NotebookRuntimeOptionsConfig
         * @static
         * @param {dataform.INotebookRuntimeOptionsConfig} message NotebookRuntimeOptionsConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotebookRuntimeOptionsConfig.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.outputBucket != null && Object.hasOwnProperty.call(message, "outputBucket"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.outputBucket);
            if (message.runtimeTemplateName != null && Object.hasOwnProperty.call(message, "runtimeTemplateName"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.runtimeTemplateName);
            if (message.repositorySnapshotDestination != null && Object.hasOwnProperty.call(message, "repositorySnapshotDestination"))
                $root.dataform.RepositorySnapshotDestinationConfig.encode(message.repositorySnapshotDestination, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified NotebookRuntimeOptionsConfig message, length delimited. Does not implicitly {@link dataform.NotebookRuntimeOptionsConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.NotebookRuntimeOptionsConfig
         * @static
         * @param {dataform.INotebookRuntimeOptionsConfig} message NotebookRuntimeOptionsConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotebookRuntimeOptionsConfig.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NotebookRuntimeOptionsConfig message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.NotebookRuntimeOptionsConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.NotebookRuntimeOptionsConfig} NotebookRuntimeOptionsConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotebookRuntimeOptionsConfig.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.NotebookRuntimeOptionsConfig();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.outputBucket = reader.string();
                        break;
                    }
                case 2: {
                        message.runtimeTemplateName = reader.string();
                        break;
                    }
                case 3: {
                        message.repositorySnapshotDestination = $root.dataform.RepositorySnapshotDestinationConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a NotebookRuntimeOptionsConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.NotebookRuntimeOptionsConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.NotebookRuntimeOptionsConfig} NotebookRuntimeOptionsConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotebookRuntimeOptionsConfig.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NotebookRuntimeOptionsConfig message.
         * @function verify
         * @memberof dataform.NotebookRuntimeOptionsConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NotebookRuntimeOptionsConfig.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.outputBucket != null && Object.hasOwnProperty.call(message, "outputBucket")) {
                if (!$util.isString(message.outputBucket))
                    return "outputBucket: string expected";
            }
            if (message.runtimeTemplateName != null && Object.hasOwnProperty.call(message, "runtimeTemplateName"))
                if (!$util.isString(message.runtimeTemplateName))
                    return "runtimeTemplateName: string expected";
            if (message.repositorySnapshotDestination != null && Object.hasOwnProperty.call(message, "repositorySnapshotDestination")) {
                {
                    let error = $root.dataform.RepositorySnapshotDestinationConfig.verify(message.repositorySnapshotDestination, long + 1);
                    if (error)
                        return "repositorySnapshotDestination." + error;
                }
            }
            return null;
        };

        /**
         * Creates a NotebookRuntimeOptionsConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.NotebookRuntimeOptionsConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.NotebookRuntimeOptionsConfig} NotebookRuntimeOptionsConfig
         */
        NotebookRuntimeOptionsConfig.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.NotebookRuntimeOptionsConfig)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.NotebookRuntimeOptionsConfig: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.NotebookRuntimeOptionsConfig();
            if (object.outputBucket != null)
                message.outputBucket = String(object.outputBucket);
            if (object.runtimeTemplateName != null)
                message.runtimeTemplateName = String(object.runtimeTemplateName);
            if (object.repositorySnapshotDestination != null) {
                if (!$util.isObject(object.repositorySnapshotDestination))
                    throw TypeError(".dataform.NotebookRuntimeOptionsConfig.repositorySnapshotDestination: object expected");
                message.repositorySnapshotDestination = $root.dataform.RepositorySnapshotDestinationConfig.fromObject(object.repositorySnapshotDestination, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a NotebookRuntimeOptionsConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.NotebookRuntimeOptionsConfig
         * @static
         * @param {dataform.NotebookRuntimeOptionsConfig} message NotebookRuntimeOptionsConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NotebookRuntimeOptionsConfig.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.runtimeTemplateName = "";
            if (message.outputBucket != null && Object.hasOwnProperty.call(message, "outputBucket")) {
                object.outputBucket = message.outputBucket;
                if (options.oneofs)
                    object.outputSink = "outputBucket";
            }
            if (message.runtimeTemplateName != null && Object.hasOwnProperty.call(message, "runtimeTemplateName"))
                object.runtimeTemplateName = message.runtimeTemplateName;
            if (message.repositorySnapshotDestination != null && Object.hasOwnProperty.call(message, "repositorySnapshotDestination")) {
                object.repositorySnapshotDestination = $root.dataform.RepositorySnapshotDestinationConfig.toObject(message.repositorySnapshotDestination, options, q + 1);
                if (options.oneofs)
                    object.repositorySnapshotStorage = "repositorySnapshotDestination";
            }
            return object;
        };

        /**
         * Converts this NotebookRuntimeOptionsConfig to JSON.
         * @function toJSON
         * @memberof dataform.NotebookRuntimeOptionsConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NotebookRuntimeOptionsConfig.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NotebookRuntimeOptionsConfig
         * @function getTypeUrl
         * @memberof dataform.NotebookRuntimeOptionsConfig
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NotebookRuntimeOptionsConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.NotebookRuntimeOptionsConfig";
        };

        return NotebookRuntimeOptionsConfig;
    })();

    /**
     * ExtensionCompilationMode enum.
     * @name dataform.ExtensionCompilationMode
     * @enum {number}
     * @property {number} COMPILATION_MODE_UNSPECIFIED=0 COMPILATION_MODE_UNSPECIFIED value
     * @property {number} PROLOGUE=1 PROLOGUE value
     * @property {number} APPLICATION_CODE=2 APPLICATION_CODE value
     */
    dataform.ExtensionCompilationMode = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "COMPILATION_MODE_UNSPECIFIED"] = 0;
        values[valuesById[1] = "PROLOGUE"] = 1;
        values[valuesById[2] = "APPLICATION_CODE"] = 2;
        return values;
    })();

    dataform.Extension = (function() {

        /**
         * Properties of an Extension.
         * @memberof dataform
         * @interface IExtension
         * @property {string|null} [name] Extension name
         * @property {dataform.ExtensionCompilationMode|null} [compilationMode] Extension compilationMode
         * @property {Object.<string,string>|null} [options] Extension options
         */

        /**
         * Constructs a new Extension.
         * @memberof dataform
         * @classdesc Represents an Extension.
         * @implements IExtension
         * @constructor
         * @param {dataform.IExtension=} [properties] Properties to set
         */
        function Extension(properties) {
            this.options = {};
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Extension name.
         * @member {string} name
         * @memberof dataform.Extension
         * @instance
         */
        Extension.prototype.name = "";

        /**
         * Extension compilationMode.
         * @member {dataform.ExtensionCompilationMode} compilationMode
         * @memberof dataform.Extension
         * @instance
         */
        Extension.prototype.compilationMode = 0;

        /**
         * Extension options.
         * @member {Object.<string,string>} options
         * @memberof dataform.Extension
         * @instance
         */
        Extension.prototype.options = $util.emptyObject;

        /**
         * Creates a new Extension instance using the specified properties.
         * @function create
         * @memberof dataform.Extension
         * @static
         * @param {dataform.IExtension=} [properties] Properties to set
         * @returns {dataform.Extension} Extension instance
         */
        Extension.create = function create(properties) {
            return new Extension(properties);
        };

        /**
         * Encodes the specified Extension message. Does not implicitly {@link dataform.Extension.verify|verify} messages.
         * @function encode
         * @memberof dataform.Extension
         * @static
         * @param {dataform.IExtension} message Extension message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Extension.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.compilationMode != null && Object.hasOwnProperty.call(message, "compilationMode"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.compilationMode);
            if (message.options != null && Object.hasOwnProperty.call(message, "options"))
                for (let keys = Object.keys(message.options), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.options[keys[i]]).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Extension message, length delimited. Does not implicitly {@link dataform.Extension.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.Extension
         * @static
         * @param {dataform.IExtension} message Extension message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Extension.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an Extension message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.Extension
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.Extension} Extension
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Extension.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.Extension(), key, value;
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 2: {
                        message.compilationMode = reader.int32();
                        break;
                    }
                case 3: {
                        if (message.options === $util.emptyObject)
                            message.options = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = "";
                        while (reader.pos < end2) {
                            let tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.string();
                                break;
                            case 2:
                                value = reader.string();
                                break;
                            default:
                                reader.skipType(tag2 & 7, long);
                                break;
                            }
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.options, key);
                        message.options[key] = value;
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an Extension message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.Extension
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.Extension} Extension
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Extension.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Extension message.
         * @function verify
         * @memberof dataform.Extension
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Extension.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.compilationMode != null && Object.hasOwnProperty.call(message, "compilationMode"))
                switch (message.compilationMode) {
                default:
                    return "compilationMode: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            if (message.options != null && Object.hasOwnProperty.call(message, "options")) {
                if (!$util.isObject(message.options))
                    return "options: object expected";
                let key = Object.keys(message.options);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isString(message.options[key[i]]))
                        return "options: string{k:string} expected";
            }
            return null;
        };

        /**
         * Creates an Extension message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.Extension
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.Extension} Extension
         */
        Extension.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.Extension)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.Extension: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.Extension();
            if (object.name != null)
                message.name = String(object.name);
            switch (object.compilationMode) {
            default:
                if (typeof object.compilationMode === "number") {
                    message.compilationMode = object.compilationMode;
                    break;
                }
                break;
            case "COMPILATION_MODE_UNSPECIFIED":
            case 0:
                message.compilationMode = 0;
                break;
            case "PROLOGUE":
            case 1:
                message.compilationMode = 1;
                break;
            case "APPLICATION_CODE":
            case 2:
                message.compilationMode = 2;
                break;
            }
            if (object.options) {
                if (!$util.isObject(object.options))
                    throw TypeError(".dataform.Extension.options: object expected");
                message.options = {};
                for (let keys = Object.keys(object.options), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.options, keys[i]);
                    message.options[keys[i]] = String(object.options[keys[i]]);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an Extension message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.Extension
         * @static
         * @param {dataform.Extension} message Extension
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Extension.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.options = {};
            if (options.defaults) {
                object.name = "";
                object.compilationMode = options.enums === String ? "COMPILATION_MODE_UNSPECIFIED" : 0;
            }
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.compilationMode != null && Object.hasOwnProperty.call(message, "compilationMode"))
                object.compilationMode = options.enums === String ? $root.dataform.ExtensionCompilationMode[message.compilationMode] === undefined ? message.compilationMode : $root.dataform.ExtensionCompilationMode[message.compilationMode] : message.compilationMode;
            let keys2;
            if (message.options && (keys2 = Object.keys(message.options)).length) {
                object.options = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.options, keys2[j]);
                    object.options[keys2[j]] = message.options[keys2[j]];
                }
            }
            return object;
        };

        /**
         * Converts this Extension to JSON.
         * @function toJSON
         * @memberof dataform.Extension
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Extension.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Extension
         * @function getTypeUrl
         * @memberof dataform.Extension
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Extension.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.Extension";
        };

        return Extension;
    })();

    dataform.ProjectConfig = (function() {

        /**
         * Properties of a ProjectConfig.
         * @memberof dataform
         * @interface IProjectConfig
         * @property {string|null} [warehouse] ProjectConfig warehouse
         * @property {string|null} [defaultDatabase] ProjectConfig defaultDatabase
         * @property {string|null} [defaultSchema] ProjectConfig defaultSchema
         * @property {string|null} [defaultLocation] ProjectConfig defaultLocation
         * @property {string|null} [assertionSchema] ProjectConfig assertionSchema
         * @property {Object.<string,string>|null} [vars] ProjectConfig vars
         * @property {string|null} [databaseSuffix] ProjectConfig databaseSuffix
         * @property {string|null} [schemaSuffix] ProjectConfig schemaSuffix
         * @property {string|null} [tablePrefix] ProjectConfig tablePrefix
         * @property {string|null} [builtinAssertionNamePrefix] ProjectConfig builtinAssertionNamePrefix
         * @property {dataform.INotebookRuntimeOptions|null} [defaultNotebookRuntimeOptions] ProjectConfig defaultNotebookRuntimeOptions
         * @property {dataform.IDefaultIcebergConfig|null} [defaultIcebergConfig] ProjectConfig defaultIcebergConfig
         * @property {boolean|null} [disableAssertions] ProjectConfig disableAssertions
         * @property {string|null} [defaultReservation] ProjectConfig defaultReservation
         * @property {boolean|null} [includeTestsInCompiledGraph] ProjectConfig includeTestsInCompiledGraph
         * @property {boolean|null} [preserveGovernanceControls] ProjectConfig preserveGovernanceControls
         */

        /**
         * Constructs a new ProjectConfig.
         * @memberof dataform
         * @classdesc Represents a ProjectConfig.
         * @implements IProjectConfig
         * @constructor
         * @param {dataform.IProjectConfig=} [properties] Properties to set
         */
        function ProjectConfig(properties) {
            this.vars = {};
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ProjectConfig warehouse.
         * @member {string} warehouse
         * @memberof dataform.ProjectConfig
         * @instance
         */
        ProjectConfig.prototype.warehouse = "";

        /**
         * ProjectConfig defaultDatabase.
         * @member {string} defaultDatabase
         * @memberof dataform.ProjectConfig
         * @instance
         */
        ProjectConfig.prototype.defaultDatabase = "";

        /**
         * ProjectConfig defaultSchema.
         * @member {string} defaultSchema
         * @memberof dataform.ProjectConfig
         * @instance
         */
        ProjectConfig.prototype.defaultSchema = "";

        /**
         * ProjectConfig defaultLocation.
         * @member {string} defaultLocation
         * @memberof dataform.ProjectConfig
         * @instance
         */
        ProjectConfig.prototype.defaultLocation = "";

        /**
         * ProjectConfig assertionSchema.
         * @member {string} assertionSchema
         * @memberof dataform.ProjectConfig
         * @instance
         */
        ProjectConfig.prototype.assertionSchema = "";

        /**
         * ProjectConfig vars.
         * @member {Object.<string,string>} vars
         * @memberof dataform.ProjectConfig
         * @instance
         */
        ProjectConfig.prototype.vars = $util.emptyObject;

        /**
         * ProjectConfig databaseSuffix.
         * @member {string} databaseSuffix
         * @memberof dataform.ProjectConfig
         * @instance
         */
        ProjectConfig.prototype.databaseSuffix = "";

        /**
         * ProjectConfig schemaSuffix.
         * @member {string} schemaSuffix
         * @memberof dataform.ProjectConfig
         * @instance
         */
        ProjectConfig.prototype.schemaSuffix = "";

        /**
         * ProjectConfig tablePrefix.
         * @member {string} tablePrefix
         * @memberof dataform.ProjectConfig
         * @instance
         */
        ProjectConfig.prototype.tablePrefix = "";

        /**
         * ProjectConfig builtinAssertionNamePrefix.
         * @member {string} builtinAssertionNamePrefix
         * @memberof dataform.ProjectConfig
         * @instance
         */
        ProjectConfig.prototype.builtinAssertionNamePrefix = "";

        /**
         * ProjectConfig defaultNotebookRuntimeOptions.
         * @member {dataform.INotebookRuntimeOptions|null|undefined} defaultNotebookRuntimeOptions
         * @memberof dataform.ProjectConfig
         * @instance
         */
        ProjectConfig.prototype.defaultNotebookRuntimeOptions = null;

        /**
         * ProjectConfig defaultIcebergConfig.
         * @member {dataform.IDefaultIcebergConfig|null|undefined} defaultIcebergConfig
         * @memberof dataform.ProjectConfig
         * @instance
         */
        ProjectConfig.prototype.defaultIcebergConfig = null;

        /**
         * ProjectConfig disableAssertions.
         * @member {boolean} disableAssertions
         * @memberof dataform.ProjectConfig
         * @instance
         */
        ProjectConfig.prototype.disableAssertions = false;

        /**
         * ProjectConfig defaultReservation.
         * @member {string} defaultReservation
         * @memberof dataform.ProjectConfig
         * @instance
         */
        ProjectConfig.prototype.defaultReservation = "";

        /**
         * ProjectConfig includeTestsInCompiledGraph.
         * @member {boolean} includeTestsInCompiledGraph
         * @memberof dataform.ProjectConfig
         * @instance
         */
        ProjectConfig.prototype.includeTestsInCompiledGraph = false;

        /**
         * ProjectConfig preserveGovernanceControls.
         * @member {boolean} preserveGovernanceControls
         * @memberof dataform.ProjectConfig
         * @instance
         */
        ProjectConfig.prototype.preserveGovernanceControls = false;

        /**
         * Creates a new ProjectConfig instance using the specified properties.
         * @function create
         * @memberof dataform.ProjectConfig
         * @static
         * @param {dataform.IProjectConfig=} [properties] Properties to set
         * @returns {dataform.ProjectConfig} ProjectConfig instance
         */
        ProjectConfig.create = function create(properties) {
            return new ProjectConfig(properties);
        };

        /**
         * Encodes the specified ProjectConfig message. Does not implicitly {@link dataform.ProjectConfig.verify|verify} messages.
         * @function encode
         * @memberof dataform.ProjectConfig
         * @static
         * @param {dataform.IProjectConfig} message ProjectConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProjectConfig.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.warehouse != null && Object.hasOwnProperty.call(message, "warehouse"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.warehouse);
            if (message.defaultSchema != null && Object.hasOwnProperty.call(message, "defaultSchema"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.defaultSchema);
            if (message.assertionSchema != null && Object.hasOwnProperty.call(message, "assertionSchema"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.assertionSchema);
            if (message.schemaSuffix != null && Object.hasOwnProperty.call(message, "schemaSuffix"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.schemaSuffix);
            if (message.defaultDatabase != null && Object.hasOwnProperty.call(message, "defaultDatabase"))
                writer.uint32(/* id 9, wireType 2 =*/74).string(message.defaultDatabase);
            if (message.tablePrefix != null && Object.hasOwnProperty.call(message, "tablePrefix"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.tablePrefix);
            if (message.vars != null && Object.hasOwnProperty.call(message, "vars"))
                for (let keys = Object.keys(message.vars), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 14, wireType 2 =*/114).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.vars[keys[i]]).ldelim();
            if (message.databaseSuffix != null && Object.hasOwnProperty.call(message, "databaseSuffix"))
                writer.uint32(/* id 15, wireType 2 =*/122).string(message.databaseSuffix);
            if (message.defaultLocation != null && Object.hasOwnProperty.call(message, "defaultLocation"))
                writer.uint32(/* id 16, wireType 2 =*/130).string(message.defaultLocation);
            if (message.defaultNotebookRuntimeOptions != null && Object.hasOwnProperty.call(message, "defaultNotebookRuntimeOptions"))
                $root.dataform.NotebookRuntimeOptions.encode(message.defaultNotebookRuntimeOptions, writer.uint32(/* id 17, wireType 2 =*/138).fork(), q + 1).ldelim();
            if (message.builtinAssertionNamePrefix != null && Object.hasOwnProperty.call(message, "builtinAssertionNamePrefix"))
                writer.uint32(/* id 18, wireType 2 =*/146).string(message.builtinAssertionNamePrefix);
            if (message.defaultIcebergConfig != null && Object.hasOwnProperty.call(message, "defaultIcebergConfig"))
                $root.dataform.DefaultIcebergConfig.encode(message.defaultIcebergConfig, writer.uint32(/* id 19, wireType 2 =*/154).fork(), q + 1).ldelim();
            if (message.disableAssertions != null && Object.hasOwnProperty.call(message, "disableAssertions"))
                writer.uint32(/* id 20, wireType 0 =*/160).bool(message.disableAssertions);
            if (message.defaultReservation != null && Object.hasOwnProperty.call(message, "defaultReservation"))
                writer.uint32(/* id 21, wireType 2 =*/170).string(message.defaultReservation);
            if (message.includeTestsInCompiledGraph != null && Object.hasOwnProperty.call(message, "includeTestsInCompiledGraph"))
                writer.uint32(/* id 22, wireType 0 =*/176).bool(message.includeTestsInCompiledGraph);
            if (message.preserveGovernanceControls != null && Object.hasOwnProperty.call(message, "preserveGovernanceControls"))
                writer.uint32(/* id 24, wireType 0 =*/192).bool(message.preserveGovernanceControls);
            return writer;
        };

        /**
         * Encodes the specified ProjectConfig message, length delimited. Does not implicitly {@link dataform.ProjectConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.ProjectConfig
         * @static
         * @param {dataform.IProjectConfig} message ProjectConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ProjectConfig.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ProjectConfig message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.ProjectConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.ProjectConfig} ProjectConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProjectConfig.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ProjectConfig(), key, value;
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.warehouse = reader.string();
                        break;
                    }
                case 9: {
                        message.defaultDatabase = reader.string();
                        break;
                    }
                case 2: {
                        message.defaultSchema = reader.string();
                        break;
                    }
                case 16: {
                        message.defaultLocation = reader.string();
                        break;
                    }
                case 5: {
                        message.assertionSchema = reader.string();
                        break;
                    }
                case 14: {
                        if (message.vars === $util.emptyObject)
                            message.vars = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = "";
                        while (reader.pos < end2) {
                            let tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.string();
                                break;
                            case 2:
                                value = reader.string();
                                break;
                            default:
                                reader.skipType(tag2 & 7, long);
                                break;
                            }
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.vars, key);
                        message.vars[key] = value;
                        break;
                    }
                case 15: {
                        message.databaseSuffix = reader.string();
                        break;
                    }
                case 7: {
                        message.schemaSuffix = reader.string();
                        break;
                    }
                case 11: {
                        message.tablePrefix = reader.string();
                        break;
                    }
                case 18: {
                        message.builtinAssertionNamePrefix = reader.string();
                        break;
                    }
                case 17: {
                        message.defaultNotebookRuntimeOptions = $root.dataform.NotebookRuntimeOptions.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 19: {
                        message.defaultIcebergConfig = $root.dataform.DefaultIcebergConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 20: {
                        message.disableAssertions = reader.bool();
                        break;
                    }
                case 21: {
                        message.defaultReservation = reader.string();
                        break;
                    }
                case 22: {
                        message.includeTestsInCompiledGraph = reader.bool();
                        break;
                    }
                case 24: {
                        message.preserveGovernanceControls = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ProjectConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.ProjectConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.ProjectConfig} ProjectConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ProjectConfig.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ProjectConfig message.
         * @function verify
         * @memberof dataform.ProjectConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ProjectConfig.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.warehouse != null && Object.hasOwnProperty.call(message, "warehouse"))
                if (!$util.isString(message.warehouse))
                    return "warehouse: string expected";
            if (message.defaultDatabase != null && Object.hasOwnProperty.call(message, "defaultDatabase"))
                if (!$util.isString(message.defaultDatabase))
                    return "defaultDatabase: string expected";
            if (message.defaultSchema != null && Object.hasOwnProperty.call(message, "defaultSchema"))
                if (!$util.isString(message.defaultSchema))
                    return "defaultSchema: string expected";
            if (message.defaultLocation != null && Object.hasOwnProperty.call(message, "defaultLocation"))
                if (!$util.isString(message.defaultLocation))
                    return "defaultLocation: string expected";
            if (message.assertionSchema != null && Object.hasOwnProperty.call(message, "assertionSchema"))
                if (!$util.isString(message.assertionSchema))
                    return "assertionSchema: string expected";
            if (message.vars != null && Object.hasOwnProperty.call(message, "vars")) {
                if (!$util.isObject(message.vars))
                    return "vars: object expected";
                let key = Object.keys(message.vars);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isString(message.vars[key[i]]))
                        return "vars: string{k:string} expected";
            }
            if (message.databaseSuffix != null && Object.hasOwnProperty.call(message, "databaseSuffix"))
                if (!$util.isString(message.databaseSuffix))
                    return "databaseSuffix: string expected";
            if (message.schemaSuffix != null && Object.hasOwnProperty.call(message, "schemaSuffix"))
                if (!$util.isString(message.schemaSuffix))
                    return "schemaSuffix: string expected";
            if (message.tablePrefix != null && Object.hasOwnProperty.call(message, "tablePrefix"))
                if (!$util.isString(message.tablePrefix))
                    return "tablePrefix: string expected";
            if (message.builtinAssertionNamePrefix != null && Object.hasOwnProperty.call(message, "builtinAssertionNamePrefix"))
                if (!$util.isString(message.builtinAssertionNamePrefix))
                    return "builtinAssertionNamePrefix: string expected";
            if (message.defaultNotebookRuntimeOptions != null && Object.hasOwnProperty.call(message, "defaultNotebookRuntimeOptions")) {
                let error = $root.dataform.NotebookRuntimeOptions.verify(message.defaultNotebookRuntimeOptions, long + 1);
                if (error)
                    return "defaultNotebookRuntimeOptions." + error;
            }
            if (message.defaultIcebergConfig != null && Object.hasOwnProperty.call(message, "defaultIcebergConfig")) {
                let error = $root.dataform.DefaultIcebergConfig.verify(message.defaultIcebergConfig, long + 1);
                if (error)
                    return "defaultIcebergConfig." + error;
            }
            if (message.disableAssertions != null && Object.hasOwnProperty.call(message, "disableAssertions"))
                if (typeof message.disableAssertions !== "boolean")
                    return "disableAssertions: boolean expected";
            if (message.defaultReservation != null && Object.hasOwnProperty.call(message, "defaultReservation"))
                if (!$util.isString(message.defaultReservation))
                    return "defaultReservation: string expected";
            if (message.includeTestsInCompiledGraph != null && Object.hasOwnProperty.call(message, "includeTestsInCompiledGraph"))
                if (typeof message.includeTestsInCompiledGraph !== "boolean")
                    return "includeTestsInCompiledGraph: boolean expected";
            if (message.preserveGovernanceControls != null && Object.hasOwnProperty.call(message, "preserveGovernanceControls"))
                if (typeof message.preserveGovernanceControls !== "boolean")
                    return "preserveGovernanceControls: boolean expected";
            return null;
        };

        /**
         * Creates a ProjectConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.ProjectConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.ProjectConfig} ProjectConfig
         */
        ProjectConfig.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.ProjectConfig)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.ProjectConfig: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.ProjectConfig();
            if (object.warehouse != null)
                message.warehouse = String(object.warehouse);
            if (object.defaultDatabase != null)
                message.defaultDatabase = String(object.defaultDatabase);
            if (object.defaultSchema != null)
                message.defaultSchema = String(object.defaultSchema);
            if (object.defaultLocation != null)
                message.defaultLocation = String(object.defaultLocation);
            if (object.assertionSchema != null)
                message.assertionSchema = String(object.assertionSchema);
            if (object.vars) {
                if (!$util.isObject(object.vars))
                    throw TypeError(".dataform.ProjectConfig.vars: object expected");
                message.vars = {};
                for (let keys = Object.keys(object.vars), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.vars, keys[i]);
                    message.vars[keys[i]] = String(object.vars[keys[i]]);
                }
            }
            if (object.databaseSuffix != null)
                message.databaseSuffix = String(object.databaseSuffix);
            if (object.schemaSuffix != null)
                message.schemaSuffix = String(object.schemaSuffix);
            if (object.tablePrefix != null)
                message.tablePrefix = String(object.tablePrefix);
            if (object.builtinAssertionNamePrefix != null)
                message.builtinAssertionNamePrefix = String(object.builtinAssertionNamePrefix);
            if (object.defaultNotebookRuntimeOptions != null) {
                if (!$util.isObject(object.defaultNotebookRuntimeOptions))
                    throw TypeError(".dataform.ProjectConfig.defaultNotebookRuntimeOptions: object expected");
                message.defaultNotebookRuntimeOptions = $root.dataform.NotebookRuntimeOptions.fromObject(object.defaultNotebookRuntimeOptions, long + 1);
            }
            if (object.defaultIcebergConfig != null) {
                if (!$util.isObject(object.defaultIcebergConfig))
                    throw TypeError(".dataform.ProjectConfig.defaultIcebergConfig: object expected");
                message.defaultIcebergConfig = $root.dataform.DefaultIcebergConfig.fromObject(object.defaultIcebergConfig, long + 1);
            }
            if (object.disableAssertions != null)
                message.disableAssertions = Boolean(object.disableAssertions);
            if (object.defaultReservation != null)
                message.defaultReservation = String(object.defaultReservation);
            if (object.includeTestsInCompiledGraph != null)
                message.includeTestsInCompiledGraph = Boolean(object.includeTestsInCompiledGraph);
            if (object.preserveGovernanceControls != null)
                message.preserveGovernanceControls = Boolean(object.preserveGovernanceControls);
            return message;
        };

        /**
         * Creates a plain object from a ProjectConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.ProjectConfig
         * @static
         * @param {dataform.ProjectConfig} message ProjectConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ProjectConfig.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.vars = {};
            if (options.defaults) {
                object.warehouse = "";
                object.defaultSchema = "";
                object.assertionSchema = "";
                object.schemaSuffix = "";
                object.defaultDatabase = "";
                object.tablePrefix = "";
                object.databaseSuffix = "";
                object.defaultLocation = "";
                object.defaultNotebookRuntimeOptions = null;
                object.builtinAssertionNamePrefix = "";
                object.defaultIcebergConfig = null;
                object.disableAssertions = false;
                object.defaultReservation = "";
                object.includeTestsInCompiledGraph = false;
                object.preserveGovernanceControls = false;
            }
            if (message.warehouse != null && Object.hasOwnProperty.call(message, "warehouse"))
                object.warehouse = message.warehouse;
            if (message.defaultSchema != null && Object.hasOwnProperty.call(message, "defaultSchema"))
                object.defaultSchema = message.defaultSchema;
            if (message.assertionSchema != null && Object.hasOwnProperty.call(message, "assertionSchema"))
                object.assertionSchema = message.assertionSchema;
            if (message.schemaSuffix != null && Object.hasOwnProperty.call(message, "schemaSuffix"))
                object.schemaSuffix = message.schemaSuffix;
            if (message.defaultDatabase != null && Object.hasOwnProperty.call(message, "defaultDatabase"))
                object.defaultDatabase = message.defaultDatabase;
            if (message.tablePrefix != null && Object.hasOwnProperty.call(message, "tablePrefix"))
                object.tablePrefix = message.tablePrefix;
            let keys2;
            if (message.vars && (keys2 = Object.keys(message.vars)).length) {
                object.vars = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.vars, keys2[j]);
                    object.vars[keys2[j]] = message.vars[keys2[j]];
                }
            }
            if (message.databaseSuffix != null && Object.hasOwnProperty.call(message, "databaseSuffix"))
                object.databaseSuffix = message.databaseSuffix;
            if (message.defaultLocation != null && Object.hasOwnProperty.call(message, "defaultLocation"))
                object.defaultLocation = message.defaultLocation;
            if (message.defaultNotebookRuntimeOptions != null && Object.hasOwnProperty.call(message, "defaultNotebookRuntimeOptions"))
                object.defaultNotebookRuntimeOptions = $root.dataform.NotebookRuntimeOptions.toObject(message.defaultNotebookRuntimeOptions, options, q + 1);
            if (message.builtinAssertionNamePrefix != null && Object.hasOwnProperty.call(message, "builtinAssertionNamePrefix"))
                object.builtinAssertionNamePrefix = message.builtinAssertionNamePrefix;
            if (message.defaultIcebergConfig != null && Object.hasOwnProperty.call(message, "defaultIcebergConfig"))
                object.defaultIcebergConfig = $root.dataform.DefaultIcebergConfig.toObject(message.defaultIcebergConfig, options, q + 1);
            if (message.disableAssertions != null && Object.hasOwnProperty.call(message, "disableAssertions"))
                object.disableAssertions = message.disableAssertions;
            if (message.defaultReservation != null && Object.hasOwnProperty.call(message, "defaultReservation"))
                object.defaultReservation = message.defaultReservation;
            if (message.includeTestsInCompiledGraph != null && Object.hasOwnProperty.call(message, "includeTestsInCompiledGraph"))
                object.includeTestsInCompiledGraph = message.includeTestsInCompiledGraph;
            if (message.preserveGovernanceControls != null && Object.hasOwnProperty.call(message, "preserveGovernanceControls"))
                object.preserveGovernanceControls = message.preserveGovernanceControls;
            return object;
        };

        /**
         * Converts this ProjectConfig to JSON.
         * @function toJSON
         * @memberof dataform.ProjectConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ProjectConfig.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ProjectConfig
         * @function getTypeUrl
         * @memberof dataform.ProjectConfig
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ProjectConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.ProjectConfig";
        };

        return ProjectConfig;
    })();

    dataform.DefaultIcebergConfig = (function() {

        /**
         * Properties of a DefaultIcebergConfig.
         * @memberof dataform
         * @interface IDefaultIcebergConfig
         * @property {string|null} [bucketName] DefaultIcebergConfig bucketName
         * @property {string|null} [tableFolderRoot] DefaultIcebergConfig tableFolderRoot
         * @property {string|null} [tableFolderSubpath] DefaultIcebergConfig tableFolderSubpath
         * @property {string|null} [connection] DefaultIcebergConfig connection
         */

        /**
         * Constructs a new DefaultIcebergConfig.
         * @memberof dataform
         * @classdesc Represents a DefaultIcebergConfig.
         * @implements IDefaultIcebergConfig
         * @constructor
         * @param {dataform.IDefaultIcebergConfig=} [properties] Properties to set
         */
        function DefaultIcebergConfig(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DefaultIcebergConfig bucketName.
         * @member {string} bucketName
         * @memberof dataform.DefaultIcebergConfig
         * @instance
         */
        DefaultIcebergConfig.prototype.bucketName = "";

        /**
         * DefaultIcebergConfig tableFolderRoot.
         * @member {string} tableFolderRoot
         * @memberof dataform.DefaultIcebergConfig
         * @instance
         */
        DefaultIcebergConfig.prototype.tableFolderRoot = "";

        /**
         * DefaultIcebergConfig tableFolderSubpath.
         * @member {string} tableFolderSubpath
         * @memberof dataform.DefaultIcebergConfig
         * @instance
         */
        DefaultIcebergConfig.prototype.tableFolderSubpath = "";

        /**
         * DefaultIcebergConfig connection.
         * @member {string} connection
         * @memberof dataform.DefaultIcebergConfig
         * @instance
         */
        DefaultIcebergConfig.prototype.connection = "";

        /**
         * Creates a new DefaultIcebergConfig instance using the specified properties.
         * @function create
         * @memberof dataform.DefaultIcebergConfig
         * @static
         * @param {dataform.IDefaultIcebergConfig=} [properties] Properties to set
         * @returns {dataform.DefaultIcebergConfig} DefaultIcebergConfig instance
         */
        DefaultIcebergConfig.create = function create(properties) {
            return new DefaultIcebergConfig(properties);
        };

        /**
         * Encodes the specified DefaultIcebergConfig message. Does not implicitly {@link dataform.DefaultIcebergConfig.verify|verify} messages.
         * @function encode
         * @memberof dataform.DefaultIcebergConfig
         * @static
         * @param {dataform.IDefaultIcebergConfig} message DefaultIcebergConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DefaultIcebergConfig.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.bucketName != null && Object.hasOwnProperty.call(message, "bucketName"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.bucketName);
            if (message.tableFolderRoot != null && Object.hasOwnProperty.call(message, "tableFolderRoot"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.tableFolderRoot);
            if (message.tableFolderSubpath != null && Object.hasOwnProperty.call(message, "tableFolderSubpath"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.tableFolderSubpath);
            if (message.connection != null && Object.hasOwnProperty.call(message, "connection"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.connection);
            return writer;
        };

        /**
         * Encodes the specified DefaultIcebergConfig message, length delimited. Does not implicitly {@link dataform.DefaultIcebergConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.DefaultIcebergConfig
         * @static
         * @param {dataform.IDefaultIcebergConfig} message DefaultIcebergConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DefaultIcebergConfig.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DefaultIcebergConfig message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.DefaultIcebergConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.DefaultIcebergConfig} DefaultIcebergConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DefaultIcebergConfig.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.DefaultIcebergConfig();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.bucketName = reader.string();
                        break;
                    }
                case 2: {
                        message.tableFolderRoot = reader.string();
                        break;
                    }
                case 3: {
                        message.tableFolderSubpath = reader.string();
                        break;
                    }
                case 4: {
                        message.connection = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DefaultIcebergConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.DefaultIcebergConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.DefaultIcebergConfig} DefaultIcebergConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DefaultIcebergConfig.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DefaultIcebergConfig message.
         * @function verify
         * @memberof dataform.DefaultIcebergConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DefaultIcebergConfig.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.bucketName != null && Object.hasOwnProperty.call(message, "bucketName"))
                if (!$util.isString(message.bucketName))
                    return "bucketName: string expected";
            if (message.tableFolderRoot != null && Object.hasOwnProperty.call(message, "tableFolderRoot"))
                if (!$util.isString(message.tableFolderRoot))
                    return "tableFolderRoot: string expected";
            if (message.tableFolderSubpath != null && Object.hasOwnProperty.call(message, "tableFolderSubpath"))
                if (!$util.isString(message.tableFolderSubpath))
                    return "tableFolderSubpath: string expected";
            if (message.connection != null && Object.hasOwnProperty.call(message, "connection"))
                if (!$util.isString(message.connection))
                    return "connection: string expected";
            return null;
        };

        /**
         * Creates a DefaultIcebergConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.DefaultIcebergConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.DefaultIcebergConfig} DefaultIcebergConfig
         */
        DefaultIcebergConfig.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.DefaultIcebergConfig)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.DefaultIcebergConfig: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.DefaultIcebergConfig();
            if (object.bucketName != null)
                message.bucketName = String(object.bucketName);
            if (object.tableFolderRoot != null)
                message.tableFolderRoot = String(object.tableFolderRoot);
            if (object.tableFolderSubpath != null)
                message.tableFolderSubpath = String(object.tableFolderSubpath);
            if (object.connection != null)
                message.connection = String(object.connection);
            return message;
        };

        /**
         * Creates a plain object from a DefaultIcebergConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.DefaultIcebergConfig
         * @static
         * @param {dataform.DefaultIcebergConfig} message DefaultIcebergConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DefaultIcebergConfig.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.bucketName = "";
                object.tableFolderRoot = "";
                object.tableFolderSubpath = "";
                object.connection = "";
            }
            if (message.bucketName != null && Object.hasOwnProperty.call(message, "bucketName"))
                object.bucketName = message.bucketName;
            if (message.tableFolderRoot != null && Object.hasOwnProperty.call(message, "tableFolderRoot"))
                object.tableFolderRoot = message.tableFolderRoot;
            if (message.tableFolderSubpath != null && Object.hasOwnProperty.call(message, "tableFolderSubpath"))
                object.tableFolderSubpath = message.tableFolderSubpath;
            if (message.connection != null && Object.hasOwnProperty.call(message, "connection"))
                object.connection = message.connection;
            return object;
        };

        /**
         * Converts this DefaultIcebergConfig to JSON.
         * @function toJSON
         * @memberof dataform.DefaultIcebergConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DefaultIcebergConfig.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DefaultIcebergConfig
         * @function getTypeUrl
         * @memberof dataform.DefaultIcebergConfig
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DefaultIcebergConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.DefaultIcebergConfig";
        };

        return DefaultIcebergConfig;
    })();

    dataform.CompileConfig = (function() {

        /**
         * Properties of a CompileConfig.
         * @memberof dataform
         * @interface ICompileConfig
         * @property {string|null} [projectDir] CompileConfig projectDir
         * @property {Array.<string>|null} [filePaths] CompileConfig filePaths
         * @property {dataform.IProjectConfig|null} [projectConfigOverride] CompileConfig projectConfigOverride
         * @property {number|null} [timeoutMillis] CompileConfig timeoutMillis
         * @property {dataform.IExtension|null} [extension] CompileConfig extension
         * @property {boolean|null} [verbose] CompileConfig verbose
         */

        /**
         * Constructs a new CompileConfig.
         * @memberof dataform
         * @classdesc Represents a CompileConfig.
         * @implements ICompileConfig
         * @constructor
         * @param {dataform.ICompileConfig=} [properties] Properties to set
         */
        function CompileConfig(properties) {
            this.filePaths = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CompileConfig projectDir.
         * @member {string} projectDir
         * @memberof dataform.CompileConfig
         * @instance
         */
        CompileConfig.prototype.projectDir = "";

        /**
         * CompileConfig filePaths.
         * @member {Array.<string>} filePaths
         * @memberof dataform.CompileConfig
         * @instance
         */
        CompileConfig.prototype.filePaths = $util.emptyArray;

        /**
         * CompileConfig projectConfigOverride.
         * @member {dataform.IProjectConfig|null|undefined} projectConfigOverride
         * @memberof dataform.CompileConfig
         * @instance
         */
        CompileConfig.prototype.projectConfigOverride = null;

        /**
         * CompileConfig timeoutMillis.
         * @member {number} timeoutMillis
         * @memberof dataform.CompileConfig
         * @instance
         */
        CompileConfig.prototype.timeoutMillis = 0;

        /**
         * CompileConfig extension.
         * @member {dataform.IExtension|null|undefined} extension
         * @memberof dataform.CompileConfig
         * @instance
         */
        CompileConfig.prototype.extension = null;

        /**
         * CompileConfig verbose.
         * @member {boolean} verbose
         * @memberof dataform.CompileConfig
         * @instance
         */
        CompileConfig.prototype.verbose = false;

        /**
         * Creates a new CompileConfig instance using the specified properties.
         * @function create
         * @memberof dataform.CompileConfig
         * @static
         * @param {dataform.ICompileConfig=} [properties] Properties to set
         * @returns {dataform.CompileConfig} CompileConfig instance
         */
        CompileConfig.create = function create(properties) {
            return new CompileConfig(properties);
        };

        /**
         * Encodes the specified CompileConfig message. Does not implicitly {@link dataform.CompileConfig.verify|verify} messages.
         * @function encode
         * @memberof dataform.CompileConfig
         * @static
         * @param {dataform.ICompileConfig} message CompileConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompileConfig.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.projectDir != null && Object.hasOwnProperty.call(message, "projectDir"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.projectDir);
            if (message.projectConfigOverride != null && Object.hasOwnProperty.call(message, "projectConfigOverride"))
                $root.dataform.ProjectConfig.encode(message.projectConfigOverride, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.timeoutMillis != null && Object.hasOwnProperty.call(message, "timeoutMillis"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.timeoutMillis);
            if (message.filePaths != null && message.filePaths.length)
                for (let i = 0; i < message.filePaths.length; ++i)
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.filePaths[i]);
            if (message.extension != null && Object.hasOwnProperty.call(message, "extension"))
                $root.dataform.Extension.encode(message.extension, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
            if (message.verbose != null && Object.hasOwnProperty.call(message, "verbose"))
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.verbose);
            return writer;
        };

        /**
         * Encodes the specified CompileConfig message, length delimited. Does not implicitly {@link dataform.CompileConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.CompileConfig
         * @static
         * @param {dataform.ICompileConfig} message CompileConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompileConfig.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CompileConfig message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.CompileConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.CompileConfig} CompileConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompileConfig.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.CompileConfig();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.projectDir = reader.string();
                        break;
                    }
                case 8: {
                        if (!(message.filePaths && message.filePaths.length))
                            message.filePaths = [];
                        message.filePaths.push(reader.string());
                        break;
                    }
                case 3: {
                        message.projectConfigOverride = $root.dataform.ProjectConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 6: {
                        message.timeoutMillis = reader.int32();
                        break;
                    }
                case 10: {
                        message.extension = $root.dataform.Extension.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 11: {
                        message.verbose = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CompileConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.CompileConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.CompileConfig} CompileConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompileConfig.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CompileConfig message.
         * @function verify
         * @memberof dataform.CompileConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CompileConfig.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.projectDir != null && Object.hasOwnProperty.call(message, "projectDir"))
                if (!$util.isString(message.projectDir))
                    return "projectDir: string expected";
            if (message.filePaths != null && Object.hasOwnProperty.call(message, "filePaths")) {
                if (!Array.isArray(message.filePaths))
                    return "filePaths: array expected";
                for (let i = 0; i < message.filePaths.length; ++i)
                    if (!$util.isString(message.filePaths[i]))
                        return "filePaths: string[] expected";
            }
            if (message.projectConfigOverride != null && Object.hasOwnProperty.call(message, "projectConfigOverride")) {
                let error = $root.dataform.ProjectConfig.verify(message.projectConfigOverride, long + 1);
                if (error)
                    return "projectConfigOverride." + error;
            }
            if (message.timeoutMillis != null && Object.hasOwnProperty.call(message, "timeoutMillis"))
                if (!$util.isInteger(message.timeoutMillis))
                    return "timeoutMillis: integer expected";
            if (message.extension != null && Object.hasOwnProperty.call(message, "extension")) {
                let error = $root.dataform.Extension.verify(message.extension, long + 1);
                if (error)
                    return "extension." + error;
            }
            if (message.verbose != null && Object.hasOwnProperty.call(message, "verbose"))
                if (typeof message.verbose !== "boolean")
                    return "verbose: boolean expected";
            return null;
        };

        /**
         * Creates a CompileConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.CompileConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.CompileConfig} CompileConfig
         */
        CompileConfig.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.CompileConfig)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.CompileConfig: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.CompileConfig();
            if (object.projectDir != null)
                message.projectDir = String(object.projectDir);
            if (object.filePaths) {
                if (!Array.isArray(object.filePaths))
                    throw TypeError(".dataform.CompileConfig.filePaths: array expected");
                message.filePaths = [];
                for (let i = 0; i < object.filePaths.length; ++i)
                    message.filePaths[i] = String(object.filePaths[i]);
            }
            if (object.projectConfigOverride != null) {
                if (!$util.isObject(object.projectConfigOverride))
                    throw TypeError(".dataform.CompileConfig.projectConfigOverride: object expected");
                message.projectConfigOverride = $root.dataform.ProjectConfig.fromObject(object.projectConfigOverride, long + 1);
            }
            if (object.timeoutMillis != null)
                message.timeoutMillis = object.timeoutMillis | 0;
            if (object.extension != null) {
                if (!$util.isObject(object.extension))
                    throw TypeError(".dataform.CompileConfig.extension: object expected");
                message.extension = $root.dataform.Extension.fromObject(object.extension, long + 1);
            }
            if (object.verbose != null)
                message.verbose = Boolean(object.verbose);
            return message;
        };

        /**
         * Creates a plain object from a CompileConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.CompileConfig
         * @static
         * @param {dataform.CompileConfig} message CompileConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CompileConfig.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.filePaths = [];
            if (options.defaults) {
                object.projectDir = "";
                object.projectConfigOverride = null;
                object.timeoutMillis = 0;
                object.extension = null;
                object.verbose = false;
            }
            if (message.projectDir != null && Object.hasOwnProperty.call(message, "projectDir"))
                object.projectDir = message.projectDir;
            if (message.projectConfigOverride != null && Object.hasOwnProperty.call(message, "projectConfigOverride"))
                object.projectConfigOverride = $root.dataform.ProjectConfig.toObject(message.projectConfigOverride, options, q + 1);
            if (message.timeoutMillis != null && Object.hasOwnProperty.call(message, "timeoutMillis"))
                object.timeoutMillis = message.timeoutMillis;
            if (message.filePaths && message.filePaths.length) {
                object.filePaths = [];
                for (let j = 0; j < message.filePaths.length; ++j)
                    object.filePaths[j] = message.filePaths[j];
            }
            if (message.extension != null && Object.hasOwnProperty.call(message, "extension"))
                object.extension = $root.dataform.Extension.toObject(message.extension, options, q + 1);
            if (message.verbose != null && Object.hasOwnProperty.call(message, "verbose"))
                object.verbose = message.verbose;
            return object;
        };

        /**
         * Converts this CompileConfig to JSON.
         * @function toJSON
         * @memberof dataform.CompileConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CompileConfig.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CompileConfig
         * @function getTypeUrl
         * @memberof dataform.CompileConfig
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CompileConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.CompileConfig";
        };

        return CompileConfig;
    })();

    dataform.Target = (function() {

        /**
         * Properties of a Target.
         * @memberof dataform
         * @interface ITarget
         * @property {string|null} [database] Target database
         * @property {string|null} [schema] Target schema
         * @property {string|null} [name] Target name
         * @property {boolean|null} [includeDependentAssertions] Target includeDependentAssertions
         */

        /**
         * Constructs a new Target.
         * @memberof dataform
         * @classdesc Represents a Target.
         * @implements ITarget
         * @constructor
         * @param {dataform.ITarget=} [properties] Properties to set
         */
        function Target(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Target database.
         * @member {string} database
         * @memberof dataform.Target
         * @instance
         */
        Target.prototype.database = "";

        /**
         * Target schema.
         * @member {string} schema
         * @memberof dataform.Target
         * @instance
         */
        Target.prototype.schema = "";

        /**
         * Target name.
         * @member {string} name
         * @memberof dataform.Target
         * @instance
         */
        Target.prototype.name = "";

        /**
         * Target includeDependentAssertions.
         * @member {boolean} includeDependentAssertions
         * @memberof dataform.Target
         * @instance
         */
        Target.prototype.includeDependentAssertions = false;

        /**
         * Creates a new Target instance using the specified properties.
         * @function create
         * @memberof dataform.Target
         * @static
         * @param {dataform.ITarget=} [properties] Properties to set
         * @returns {dataform.Target} Target instance
         */
        Target.create = function create(properties) {
            return new Target(properties);
        };

        /**
         * Encodes the specified Target message. Does not implicitly {@link dataform.Target.verify|verify} messages.
         * @function encode
         * @memberof dataform.Target
         * @static
         * @param {dataform.ITarget} message Target message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Target.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.schema != null && Object.hasOwnProperty.call(message, "schema"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.schema);
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
            if (message.database != null && Object.hasOwnProperty.call(message, "database"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.database);
            if (message.includeDependentAssertions != null && Object.hasOwnProperty.call(message, "includeDependentAssertions"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.includeDependentAssertions);
            return writer;
        };

        /**
         * Encodes the specified Target message, length delimited. Does not implicitly {@link dataform.Target.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.Target
         * @static
         * @param {dataform.ITarget} message Target message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Target.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Target message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.Target
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.Target} Target
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Target.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.Target();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 3: {
                        message.database = reader.string();
                        break;
                    }
                case 1: {
                        message.schema = reader.string();
                        break;
                    }
                case 2: {
                        message.name = reader.string();
                        break;
                    }
                case 4: {
                        message.includeDependentAssertions = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Target message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.Target
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.Target} Target
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Target.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Target message.
         * @function verify
         * @memberof dataform.Target
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Target.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.database != null && Object.hasOwnProperty.call(message, "database"))
                if (!$util.isString(message.database))
                    return "database: string expected";
            if (message.schema != null && Object.hasOwnProperty.call(message, "schema"))
                if (!$util.isString(message.schema))
                    return "schema: string expected";
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.includeDependentAssertions != null && Object.hasOwnProperty.call(message, "includeDependentAssertions"))
                if (typeof message.includeDependentAssertions !== "boolean")
                    return "includeDependentAssertions: boolean expected";
            return null;
        };

        /**
         * Creates a Target message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.Target
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.Target} Target
         */
        Target.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.Target)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.Target: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.Target();
            if (object.database != null)
                message.database = String(object.database);
            if (object.schema != null)
                message.schema = String(object.schema);
            if (object.name != null)
                message.name = String(object.name);
            if (object.includeDependentAssertions != null)
                message.includeDependentAssertions = Boolean(object.includeDependentAssertions);
            return message;
        };

        /**
         * Creates a plain object from a Target message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.Target
         * @static
         * @param {dataform.Target} message Target
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Target.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.schema = "";
                object.name = "";
                object.database = "";
                object.includeDependentAssertions = false;
            }
            if (message.schema != null && Object.hasOwnProperty.call(message, "schema"))
                object.schema = message.schema;
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.database != null && Object.hasOwnProperty.call(message, "database"))
                object.database = message.database;
            if (message.includeDependentAssertions != null && Object.hasOwnProperty.call(message, "includeDependentAssertions"))
                object.includeDependentAssertions = message.includeDependentAssertions;
            return object;
        };

        /**
         * Converts this Target to JSON.
         * @function toJSON
         * @memberof dataform.Target
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Target.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Target
         * @function getTypeUrl
         * @memberof dataform.Target
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Target.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.Target";
        };

        return Target;
    })();

    /**
     * FileFormat enum.
     * @name dataform.FileFormat
     * @enum {number}
     * @property {number} FILE_FORMAT_UNSPECIFIED=0 FILE_FORMAT_UNSPECIFIED value
     * @property {number} PARQUET=1 PARQUET value
     */
    dataform.FileFormat = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "FILE_FORMAT_UNSPECIFIED"] = 0;
        values[valuesById[1] = "PARQUET"] = 1;
        return values;
    })();

    /**
     * TableFormat enum.
     * @name dataform.TableFormat
     * @enum {number}
     * @property {number} TABLE_FORMAT_UNSPECIFIED=0 TABLE_FORMAT_UNSPECIFIED value
     * @property {number} ICEBERG=1 ICEBERG value
     */
    dataform.TableFormat = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "TABLE_FORMAT_UNSPECIFIED"] = 0;
        values[valuesById[1] = "ICEBERG"] = 1;
        return values;
    })();

    dataform.BigQueryOptions = (function() {

        /**
         * Properties of a BigQueryOptions.
         * @memberof dataform
         * @interface IBigQueryOptions
         * @property {string|null} [partitionBy] BigQueryOptions partitionBy
         * @property {Array.<string>|null} [clusterBy] BigQueryOptions clusterBy
         * @property {string|null} [updatePartitionFilter] BigQueryOptions updatePartitionFilter
         * @property {Object.<string,string>|null} [labels] BigQueryOptions labels
         * @property {number|null} [partitionExpirationDays] BigQueryOptions partitionExpirationDays
         * @property {boolean|null} [requirePartitionFilter] BigQueryOptions requirePartitionFilter
         * @property {Object.<string,string>|null} [additionalOptions] BigQueryOptions additionalOptions
         * @property {string|null} [connection] BigQueryOptions connection
         * @property {dataform.TableFormat|null} [tableFormat] BigQueryOptions tableFormat
         * @property {dataform.FileFormat|null} [fileFormat] BigQueryOptions fileFormat
         * @property {string|null} [storageUri] BigQueryOptions storageUri
         * @property {Array.<string>|null} [incrementalPredicates] BigQueryOptions incrementalPredicates
         * @property {boolean|null} [preserveGovernanceControls] BigQueryOptions preserveGovernanceControls
         */

        /**
         * Constructs a new BigQueryOptions.
         * @memberof dataform
         * @classdesc Represents a BigQueryOptions.
         * @implements IBigQueryOptions
         * @constructor
         * @param {dataform.IBigQueryOptions=} [properties] Properties to set
         */
        function BigQueryOptions(properties) {
            this.clusterBy = [];
            this.labels = {};
            this.additionalOptions = {};
            this.incrementalPredicates = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BigQueryOptions partitionBy.
         * @member {string} partitionBy
         * @memberof dataform.BigQueryOptions
         * @instance
         */
        BigQueryOptions.prototype.partitionBy = "";

        /**
         * BigQueryOptions clusterBy.
         * @member {Array.<string>} clusterBy
         * @memberof dataform.BigQueryOptions
         * @instance
         */
        BigQueryOptions.prototype.clusterBy = $util.emptyArray;

        /**
         * BigQueryOptions updatePartitionFilter.
         * @member {string} updatePartitionFilter
         * @memberof dataform.BigQueryOptions
         * @instance
         */
        BigQueryOptions.prototype.updatePartitionFilter = "";

        /**
         * BigQueryOptions labels.
         * @member {Object.<string,string>} labels
         * @memberof dataform.BigQueryOptions
         * @instance
         */
        BigQueryOptions.prototype.labels = $util.emptyObject;

        /**
         * BigQueryOptions partitionExpirationDays.
         * @member {number} partitionExpirationDays
         * @memberof dataform.BigQueryOptions
         * @instance
         */
        BigQueryOptions.prototype.partitionExpirationDays = 0;

        /**
         * BigQueryOptions requirePartitionFilter.
         * @member {boolean} requirePartitionFilter
         * @memberof dataform.BigQueryOptions
         * @instance
         */
        BigQueryOptions.prototype.requirePartitionFilter = false;

        /**
         * BigQueryOptions additionalOptions.
         * @member {Object.<string,string>} additionalOptions
         * @memberof dataform.BigQueryOptions
         * @instance
         */
        BigQueryOptions.prototype.additionalOptions = $util.emptyObject;

        /**
         * BigQueryOptions connection.
         * @member {string} connection
         * @memberof dataform.BigQueryOptions
         * @instance
         */
        BigQueryOptions.prototype.connection = "";

        /**
         * BigQueryOptions tableFormat.
         * @member {dataform.TableFormat} tableFormat
         * @memberof dataform.BigQueryOptions
         * @instance
         */
        BigQueryOptions.prototype.tableFormat = 0;

        /**
         * BigQueryOptions fileFormat.
         * @member {dataform.FileFormat} fileFormat
         * @memberof dataform.BigQueryOptions
         * @instance
         */
        BigQueryOptions.prototype.fileFormat = 0;

        /**
         * BigQueryOptions storageUri.
         * @member {string} storageUri
         * @memberof dataform.BigQueryOptions
         * @instance
         */
        BigQueryOptions.prototype.storageUri = "";

        /**
         * BigQueryOptions incrementalPredicates.
         * @member {Array.<string>} incrementalPredicates
         * @memberof dataform.BigQueryOptions
         * @instance
         */
        BigQueryOptions.prototype.incrementalPredicates = $util.emptyArray;

        /**
         * BigQueryOptions preserveGovernanceControls.
         * @member {boolean} preserveGovernanceControls
         * @memberof dataform.BigQueryOptions
         * @instance
         */
        BigQueryOptions.prototype.preserveGovernanceControls = false;

        /**
         * Creates a new BigQueryOptions instance using the specified properties.
         * @function create
         * @memberof dataform.BigQueryOptions
         * @static
         * @param {dataform.IBigQueryOptions=} [properties] Properties to set
         * @returns {dataform.BigQueryOptions} BigQueryOptions instance
         */
        BigQueryOptions.create = function create(properties) {
            return new BigQueryOptions(properties);
        };

        /**
         * Encodes the specified BigQueryOptions message. Does not implicitly {@link dataform.BigQueryOptions.verify|verify} messages.
         * @function encode
         * @memberof dataform.BigQueryOptions
         * @static
         * @param {dataform.IBigQueryOptions} message BigQueryOptions message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BigQueryOptions.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.partitionBy != null && Object.hasOwnProperty.call(message, "partitionBy"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.partitionBy);
            if (message.clusterBy != null && message.clusterBy.length)
                for (let i = 0; i < message.clusterBy.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.clusterBy[i]);
            if (message.updatePartitionFilter != null && Object.hasOwnProperty.call(message, "updatePartitionFilter"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.updatePartitionFilter);
            if (message.labels != null && Object.hasOwnProperty.call(message, "labels"))
                for (let keys = Object.keys(message.labels), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 4, wireType 2 =*/34).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.labels[keys[i]]).ldelim();
            if (message.partitionExpirationDays != null && Object.hasOwnProperty.call(message, "partitionExpirationDays"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.partitionExpirationDays);
            if (message.requirePartitionFilter != null && Object.hasOwnProperty.call(message, "requirePartitionFilter"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.requirePartitionFilter);
            if (message.additionalOptions != null && Object.hasOwnProperty.call(message, "additionalOptions"))
                for (let keys = Object.keys(message.additionalOptions), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 7, wireType 2 =*/58).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.additionalOptions[keys[i]]).ldelim();
            if (message.connection != null && Object.hasOwnProperty.call(message, "connection"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.connection);
            if (message.tableFormat != null && Object.hasOwnProperty.call(message, "tableFormat"))
                writer.uint32(/* id 9, wireType 0 =*/72).int32(message.tableFormat);
            if (message.fileFormat != null && Object.hasOwnProperty.call(message, "fileFormat"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.fileFormat);
            if (message.storageUri != null && Object.hasOwnProperty.call(message, "storageUri"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.storageUri);
            if (message.incrementalPredicates != null && message.incrementalPredicates.length)
                for (let i = 0; i < message.incrementalPredicates.length; ++i)
                    writer.uint32(/* id 12, wireType 2 =*/98).string(message.incrementalPredicates[i]);
            if (message.preserveGovernanceControls != null && Object.hasOwnProperty.call(message, "preserveGovernanceControls"))
                writer.uint32(/* id 13, wireType 0 =*/104).bool(message.preserveGovernanceControls);
            return writer;
        };

        /**
         * Encodes the specified BigQueryOptions message, length delimited. Does not implicitly {@link dataform.BigQueryOptions.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.BigQueryOptions
         * @static
         * @param {dataform.IBigQueryOptions} message BigQueryOptions message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BigQueryOptions.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BigQueryOptions message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.BigQueryOptions
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.BigQueryOptions} BigQueryOptions
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BigQueryOptions.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.BigQueryOptions(), key, value;
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.partitionBy = reader.string();
                        break;
                    }
                case 2: {
                        if (!(message.clusterBy && message.clusterBy.length))
                            message.clusterBy = [];
                        message.clusterBy.push(reader.string());
                        break;
                    }
                case 3: {
                        message.updatePartitionFilter = reader.string();
                        break;
                    }
                case 4: {
                        if (message.labels === $util.emptyObject)
                            message.labels = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = "";
                        while (reader.pos < end2) {
                            let tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.string();
                                break;
                            case 2:
                                value = reader.string();
                                break;
                            default:
                                reader.skipType(tag2 & 7, long);
                                break;
                            }
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.labels, key);
                        message.labels[key] = value;
                        break;
                    }
                case 5: {
                        message.partitionExpirationDays = reader.int32();
                        break;
                    }
                case 6: {
                        message.requirePartitionFilter = reader.bool();
                        break;
                    }
                case 7: {
                        if (message.additionalOptions === $util.emptyObject)
                            message.additionalOptions = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = "";
                        while (reader.pos < end2) {
                            let tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.string();
                                break;
                            case 2:
                                value = reader.string();
                                break;
                            default:
                                reader.skipType(tag2 & 7, long);
                                break;
                            }
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.additionalOptions, key);
                        message.additionalOptions[key] = value;
                        break;
                    }
                case 8: {
                        message.connection = reader.string();
                        break;
                    }
                case 9: {
                        message.tableFormat = reader.int32();
                        break;
                    }
                case 10: {
                        message.fileFormat = reader.int32();
                        break;
                    }
                case 11: {
                        message.storageUri = reader.string();
                        break;
                    }
                case 12: {
                        if (!(message.incrementalPredicates && message.incrementalPredicates.length))
                            message.incrementalPredicates = [];
                        message.incrementalPredicates.push(reader.string());
                        break;
                    }
                case 13: {
                        message.preserveGovernanceControls = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BigQueryOptions message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.BigQueryOptions
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.BigQueryOptions} BigQueryOptions
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BigQueryOptions.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BigQueryOptions message.
         * @function verify
         * @memberof dataform.BigQueryOptions
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BigQueryOptions.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.partitionBy != null && Object.hasOwnProperty.call(message, "partitionBy"))
                if (!$util.isString(message.partitionBy))
                    return "partitionBy: string expected";
            if (message.clusterBy != null && Object.hasOwnProperty.call(message, "clusterBy")) {
                if (!Array.isArray(message.clusterBy))
                    return "clusterBy: array expected";
                for (let i = 0; i < message.clusterBy.length; ++i)
                    if (!$util.isString(message.clusterBy[i]))
                        return "clusterBy: string[] expected";
            }
            if (message.updatePartitionFilter != null && Object.hasOwnProperty.call(message, "updatePartitionFilter"))
                if (!$util.isString(message.updatePartitionFilter))
                    return "updatePartitionFilter: string expected";
            if (message.labels != null && Object.hasOwnProperty.call(message, "labels")) {
                if (!$util.isObject(message.labels))
                    return "labels: object expected";
                let key = Object.keys(message.labels);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isString(message.labels[key[i]]))
                        return "labels: string{k:string} expected";
            }
            if (message.partitionExpirationDays != null && Object.hasOwnProperty.call(message, "partitionExpirationDays"))
                if (!$util.isInteger(message.partitionExpirationDays))
                    return "partitionExpirationDays: integer expected";
            if (message.requirePartitionFilter != null && Object.hasOwnProperty.call(message, "requirePartitionFilter"))
                if (typeof message.requirePartitionFilter !== "boolean")
                    return "requirePartitionFilter: boolean expected";
            if (message.additionalOptions != null && Object.hasOwnProperty.call(message, "additionalOptions")) {
                if (!$util.isObject(message.additionalOptions))
                    return "additionalOptions: object expected";
                let key = Object.keys(message.additionalOptions);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isString(message.additionalOptions[key[i]]))
                        return "additionalOptions: string{k:string} expected";
            }
            if (message.connection != null && Object.hasOwnProperty.call(message, "connection"))
                if (!$util.isString(message.connection))
                    return "connection: string expected";
            if (message.tableFormat != null && Object.hasOwnProperty.call(message, "tableFormat"))
                switch (message.tableFormat) {
                default:
                    return "tableFormat: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.fileFormat != null && Object.hasOwnProperty.call(message, "fileFormat"))
                switch (message.fileFormat) {
                default:
                    return "fileFormat: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.storageUri != null && Object.hasOwnProperty.call(message, "storageUri"))
                if (!$util.isString(message.storageUri))
                    return "storageUri: string expected";
            if (message.incrementalPredicates != null && Object.hasOwnProperty.call(message, "incrementalPredicates")) {
                if (!Array.isArray(message.incrementalPredicates))
                    return "incrementalPredicates: array expected";
                for (let i = 0; i < message.incrementalPredicates.length; ++i)
                    if (!$util.isString(message.incrementalPredicates[i]))
                        return "incrementalPredicates: string[] expected";
            }
            if (message.preserveGovernanceControls != null && Object.hasOwnProperty.call(message, "preserveGovernanceControls"))
                if (typeof message.preserveGovernanceControls !== "boolean")
                    return "preserveGovernanceControls: boolean expected";
            return null;
        };

        /**
         * Creates a BigQueryOptions message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.BigQueryOptions
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.BigQueryOptions} BigQueryOptions
         */
        BigQueryOptions.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.BigQueryOptions)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.BigQueryOptions: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.BigQueryOptions();
            if (object.partitionBy != null)
                message.partitionBy = String(object.partitionBy);
            if (object.clusterBy) {
                if (!Array.isArray(object.clusterBy))
                    throw TypeError(".dataform.BigQueryOptions.clusterBy: array expected");
                message.clusterBy = [];
                for (let i = 0; i < object.clusterBy.length; ++i)
                    message.clusterBy[i] = String(object.clusterBy[i]);
            }
            if (object.updatePartitionFilter != null)
                message.updatePartitionFilter = String(object.updatePartitionFilter);
            if (object.labels) {
                if (!$util.isObject(object.labels))
                    throw TypeError(".dataform.BigQueryOptions.labels: object expected");
                message.labels = {};
                for (let keys = Object.keys(object.labels), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.labels, keys[i]);
                    message.labels[keys[i]] = String(object.labels[keys[i]]);
                }
            }
            if (object.partitionExpirationDays != null)
                message.partitionExpirationDays = object.partitionExpirationDays | 0;
            if (object.requirePartitionFilter != null)
                message.requirePartitionFilter = Boolean(object.requirePartitionFilter);
            if (object.additionalOptions) {
                if (!$util.isObject(object.additionalOptions))
                    throw TypeError(".dataform.BigQueryOptions.additionalOptions: object expected");
                message.additionalOptions = {};
                for (let keys = Object.keys(object.additionalOptions), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.additionalOptions, keys[i]);
                    message.additionalOptions[keys[i]] = String(object.additionalOptions[keys[i]]);
                }
            }
            if (object.connection != null)
                message.connection = String(object.connection);
            switch (object.tableFormat) {
            default:
                if (typeof object.tableFormat === "number") {
                    message.tableFormat = object.tableFormat;
                    break;
                }
                break;
            case "TABLE_FORMAT_UNSPECIFIED":
            case 0:
                message.tableFormat = 0;
                break;
            case "ICEBERG":
            case 1:
                message.tableFormat = 1;
                break;
            }
            switch (object.fileFormat) {
            default:
                if (typeof object.fileFormat === "number") {
                    message.fileFormat = object.fileFormat;
                    break;
                }
                break;
            case "FILE_FORMAT_UNSPECIFIED":
            case 0:
                message.fileFormat = 0;
                break;
            case "PARQUET":
            case 1:
                message.fileFormat = 1;
                break;
            }
            if (object.storageUri != null)
                message.storageUri = String(object.storageUri);
            if (object.incrementalPredicates) {
                if (!Array.isArray(object.incrementalPredicates))
                    throw TypeError(".dataform.BigQueryOptions.incrementalPredicates: array expected");
                message.incrementalPredicates = [];
                for (let i = 0; i < object.incrementalPredicates.length; ++i)
                    message.incrementalPredicates[i] = String(object.incrementalPredicates[i]);
            }
            if (object.preserveGovernanceControls != null)
                message.preserveGovernanceControls = Boolean(object.preserveGovernanceControls);
            return message;
        };

        /**
         * Creates a plain object from a BigQueryOptions message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.BigQueryOptions
         * @static
         * @param {dataform.BigQueryOptions} message BigQueryOptions
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BigQueryOptions.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.clusterBy = [];
                object.incrementalPredicates = [];
            }
            if (options.objects || options.defaults) {
                object.labels = {};
                object.additionalOptions = {};
            }
            if (options.defaults) {
                object.partitionBy = "";
                object.updatePartitionFilter = "";
                object.partitionExpirationDays = 0;
                object.requirePartitionFilter = false;
                object.connection = "";
                object.tableFormat = options.enums === String ? "TABLE_FORMAT_UNSPECIFIED" : 0;
                object.fileFormat = options.enums === String ? "FILE_FORMAT_UNSPECIFIED" : 0;
                object.storageUri = "";
                object.preserveGovernanceControls = false;
            }
            if (message.partitionBy != null && Object.hasOwnProperty.call(message, "partitionBy"))
                object.partitionBy = message.partitionBy;
            if (message.clusterBy && message.clusterBy.length) {
                object.clusterBy = [];
                for (let j = 0; j < message.clusterBy.length; ++j)
                    object.clusterBy[j] = message.clusterBy[j];
            }
            if (message.updatePartitionFilter != null && Object.hasOwnProperty.call(message, "updatePartitionFilter"))
                object.updatePartitionFilter = message.updatePartitionFilter;
            let keys2;
            if (message.labels && (keys2 = Object.keys(message.labels)).length) {
                object.labels = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.labels, keys2[j]);
                    object.labels[keys2[j]] = message.labels[keys2[j]];
                }
            }
            if (message.partitionExpirationDays != null && Object.hasOwnProperty.call(message, "partitionExpirationDays"))
                object.partitionExpirationDays = message.partitionExpirationDays;
            if (message.requirePartitionFilter != null && Object.hasOwnProperty.call(message, "requirePartitionFilter"))
                object.requirePartitionFilter = message.requirePartitionFilter;
            if (message.additionalOptions && (keys2 = Object.keys(message.additionalOptions)).length) {
                object.additionalOptions = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.additionalOptions, keys2[j]);
                    object.additionalOptions[keys2[j]] = message.additionalOptions[keys2[j]];
                }
            }
            if (message.connection != null && Object.hasOwnProperty.call(message, "connection"))
                object.connection = message.connection;
            if (message.tableFormat != null && Object.hasOwnProperty.call(message, "tableFormat"))
                object.tableFormat = options.enums === String ? $root.dataform.TableFormat[message.tableFormat] === undefined ? message.tableFormat : $root.dataform.TableFormat[message.tableFormat] : message.tableFormat;
            if (message.fileFormat != null && Object.hasOwnProperty.call(message, "fileFormat"))
                object.fileFormat = options.enums === String ? $root.dataform.FileFormat[message.fileFormat] === undefined ? message.fileFormat : $root.dataform.FileFormat[message.fileFormat] : message.fileFormat;
            if (message.storageUri != null && Object.hasOwnProperty.call(message, "storageUri"))
                object.storageUri = message.storageUri;
            if (message.incrementalPredicates && message.incrementalPredicates.length) {
                object.incrementalPredicates = [];
                for (let j = 0; j < message.incrementalPredicates.length; ++j)
                    object.incrementalPredicates[j] = message.incrementalPredicates[j];
            }
            if (message.preserveGovernanceControls != null && Object.hasOwnProperty.call(message, "preserveGovernanceControls"))
                object.preserveGovernanceControls = message.preserveGovernanceControls;
            return object;
        };

        /**
         * Converts this BigQueryOptions to JSON.
         * @function toJSON
         * @memberof dataform.BigQueryOptions
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BigQueryOptions.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BigQueryOptions
         * @function getTypeUrl
         * @memberof dataform.BigQueryOptions
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BigQueryOptions.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.BigQueryOptions";
        };

        return BigQueryOptions;
    })();

    dataform.GraphErrors = (function() {

        /**
         * Properties of a GraphErrors.
         * @memberof dataform
         * @interface IGraphErrors
         * @property {Array.<dataform.ICompilationError>|null} [compilationErrors] GraphErrors compilationErrors
         */

        /**
         * Constructs a new GraphErrors.
         * @memberof dataform
         * @classdesc Represents a GraphErrors.
         * @implements IGraphErrors
         * @constructor
         * @param {dataform.IGraphErrors=} [properties] Properties to set
         */
        function GraphErrors(properties) {
            this.compilationErrors = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GraphErrors compilationErrors.
         * @member {Array.<dataform.ICompilationError>} compilationErrors
         * @memberof dataform.GraphErrors
         * @instance
         */
        GraphErrors.prototype.compilationErrors = $util.emptyArray;

        /**
         * Creates a new GraphErrors instance using the specified properties.
         * @function create
         * @memberof dataform.GraphErrors
         * @static
         * @param {dataform.IGraphErrors=} [properties] Properties to set
         * @returns {dataform.GraphErrors} GraphErrors instance
         */
        GraphErrors.create = function create(properties) {
            return new GraphErrors(properties);
        };

        /**
         * Encodes the specified GraphErrors message. Does not implicitly {@link dataform.GraphErrors.verify|verify} messages.
         * @function encode
         * @memberof dataform.GraphErrors
         * @static
         * @param {dataform.IGraphErrors} message GraphErrors message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GraphErrors.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.compilationErrors != null && message.compilationErrors.length)
                for (let i = 0; i < message.compilationErrors.length; ++i)
                    $root.dataform.CompilationError.encode(message.compilationErrors[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GraphErrors message, length delimited. Does not implicitly {@link dataform.GraphErrors.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.GraphErrors
         * @static
         * @param {dataform.IGraphErrors} message GraphErrors message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GraphErrors.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GraphErrors message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.GraphErrors
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.GraphErrors} GraphErrors
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GraphErrors.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.GraphErrors();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.compilationErrors && message.compilationErrors.length))
                            message.compilationErrors = [];
                        message.compilationErrors.push($root.dataform.CompilationError.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GraphErrors message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.GraphErrors
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.GraphErrors} GraphErrors
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GraphErrors.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GraphErrors message.
         * @function verify
         * @memberof dataform.GraphErrors
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GraphErrors.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.compilationErrors != null && Object.hasOwnProperty.call(message, "compilationErrors")) {
                if (!Array.isArray(message.compilationErrors))
                    return "compilationErrors: array expected";
                for (let i = 0; i < message.compilationErrors.length; ++i) {
                    let error = $root.dataform.CompilationError.verify(message.compilationErrors[i], long + 1);
                    if (error)
                        return "compilationErrors." + error;
                }
            }
            return null;
        };

        /**
         * Creates a GraphErrors message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.GraphErrors
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.GraphErrors} GraphErrors
         */
        GraphErrors.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.GraphErrors)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.GraphErrors: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.GraphErrors();
            if (object.compilationErrors) {
                if (!Array.isArray(object.compilationErrors))
                    throw TypeError(".dataform.GraphErrors.compilationErrors: array expected");
                message.compilationErrors = [];
                for (let i = 0; i < object.compilationErrors.length; ++i) {
                    if (!$util.isObject(object.compilationErrors[i]))
                        throw TypeError(".dataform.GraphErrors.compilationErrors: object expected");
                    message.compilationErrors[i] = $root.dataform.CompilationError.fromObject(object.compilationErrors[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a GraphErrors message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.GraphErrors
         * @static
         * @param {dataform.GraphErrors} message GraphErrors
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GraphErrors.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.compilationErrors = [];
            if (message.compilationErrors && message.compilationErrors.length) {
                object.compilationErrors = [];
                for (let j = 0; j < message.compilationErrors.length; ++j)
                    object.compilationErrors[j] = $root.dataform.CompilationError.toObject(message.compilationErrors[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this GraphErrors to JSON.
         * @function toJSON
         * @memberof dataform.GraphErrors
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GraphErrors.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GraphErrors
         * @function getTypeUrl
         * @memberof dataform.GraphErrors
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GraphErrors.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.GraphErrors";
        };

        return GraphErrors;
    })();

    dataform.CompilationError = (function() {

        /**
         * Properties of a CompilationError.
         * @memberof dataform
         * @interface ICompilationError
         * @property {string|null} [fileName] CompilationError fileName
         * @property {string|null} [actionName] CompilationError actionName
         * @property {dataform.ITarget|null} [actionTarget] CompilationError actionTarget
         * @property {string|null} [message] CompilationError message
         * @property {string|null} [stack] CompilationError stack
         */

        /**
         * Constructs a new CompilationError.
         * @memberof dataform
         * @classdesc Represents a CompilationError.
         * @implements ICompilationError
         * @constructor
         * @param {dataform.ICompilationError=} [properties] Properties to set
         */
        function CompilationError(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CompilationError fileName.
         * @member {string} fileName
         * @memberof dataform.CompilationError
         * @instance
         */
        CompilationError.prototype.fileName = "";

        /**
         * CompilationError actionName.
         * @member {string} actionName
         * @memberof dataform.CompilationError
         * @instance
         */
        CompilationError.prototype.actionName = "";

        /**
         * CompilationError actionTarget.
         * @member {dataform.ITarget|null|undefined} actionTarget
         * @memberof dataform.CompilationError
         * @instance
         */
        CompilationError.prototype.actionTarget = null;

        /**
         * CompilationError message.
         * @member {string} message
         * @memberof dataform.CompilationError
         * @instance
         */
        CompilationError.prototype.message = "";

        /**
         * CompilationError stack.
         * @member {string} stack
         * @memberof dataform.CompilationError
         * @instance
         */
        CompilationError.prototype.stack = "";

        /**
         * Creates a new CompilationError instance using the specified properties.
         * @function create
         * @memberof dataform.CompilationError
         * @static
         * @param {dataform.ICompilationError=} [properties] Properties to set
         * @returns {dataform.CompilationError} CompilationError instance
         */
        CompilationError.create = function create(properties) {
            return new CompilationError(properties);
        };

        /**
         * Encodes the specified CompilationError message. Does not implicitly {@link dataform.CompilationError.verify|verify} messages.
         * @function encode
         * @memberof dataform.CompilationError
         * @static
         * @param {dataform.ICompilationError} message CompilationError message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompilationError.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.fileName);
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.message);
            if (message.stack != null && Object.hasOwnProperty.call(message, "stack"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.stack);
            if (message.actionName != null && Object.hasOwnProperty.call(message, "actionName"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.actionName);
            if (message.actionTarget != null && Object.hasOwnProperty.call(message, "actionTarget"))
                $root.dataform.Target.encode(message.actionTarget, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CompilationError message, length delimited. Does not implicitly {@link dataform.CompilationError.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.CompilationError
         * @static
         * @param {dataform.ICompilationError} message CompilationError message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompilationError.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CompilationError message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.CompilationError
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.CompilationError} CompilationError
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompilationError.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.CompilationError();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.fileName = reader.string();
                        break;
                    }
                case 4: {
                        message.actionName = reader.string();
                        break;
                    }
                case 5: {
                        message.actionTarget = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.message = reader.string();
                        break;
                    }
                case 3: {
                        message.stack = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CompilationError message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.CompilationError
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.CompilationError} CompilationError
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompilationError.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CompilationError message.
         * @function verify
         * @memberof dataform.CompilationError
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CompilationError.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                if (!$util.isString(message.fileName))
                    return "fileName: string expected";
            if (message.actionName != null && Object.hasOwnProperty.call(message, "actionName"))
                if (!$util.isString(message.actionName))
                    return "actionName: string expected";
            if (message.actionTarget != null && Object.hasOwnProperty.call(message, "actionTarget")) {
                let error = $root.dataform.Target.verify(message.actionTarget, long + 1);
                if (error)
                    return "actionTarget." + error;
            }
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            if (message.stack != null && Object.hasOwnProperty.call(message, "stack"))
                if (!$util.isString(message.stack))
                    return "stack: string expected";
            return null;
        };

        /**
         * Creates a CompilationError message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.CompilationError
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.CompilationError} CompilationError
         */
        CompilationError.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.CompilationError)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.CompilationError: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.CompilationError();
            if (object.fileName != null)
                message.fileName = String(object.fileName);
            if (object.actionName != null)
                message.actionName = String(object.actionName);
            if (object.actionTarget != null) {
                if (!$util.isObject(object.actionTarget))
                    throw TypeError(".dataform.CompilationError.actionTarget: object expected");
                message.actionTarget = $root.dataform.Target.fromObject(object.actionTarget, long + 1);
            }
            if (object.message != null)
                message.message = String(object.message);
            if (object.stack != null)
                message.stack = String(object.stack);
            return message;
        };

        /**
         * Creates a plain object from a CompilationError message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.CompilationError
         * @static
         * @param {dataform.CompilationError} message CompilationError
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CompilationError.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.fileName = "";
                object.message = "";
                object.stack = "";
                object.actionName = "";
                object.actionTarget = null;
            }
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                object.fileName = message.fileName;
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                object.message = message.message;
            if (message.stack != null && Object.hasOwnProperty.call(message, "stack"))
                object.stack = message.stack;
            if (message.actionName != null && Object.hasOwnProperty.call(message, "actionName"))
                object.actionName = message.actionName;
            if (message.actionTarget != null && Object.hasOwnProperty.call(message, "actionTarget"))
                object.actionTarget = $root.dataform.Target.toObject(message.actionTarget, options, q + 1);
            return object;
        };

        /**
         * Converts this CompilationError to JSON.
         * @function toJSON
         * @memberof dataform.CompilationError
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CompilationError.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CompilationError
         * @function getTypeUrl
         * @memberof dataform.CompilationError
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CompilationError.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.CompilationError";
        };

        return CompilationError;
    })();

    /**
     * ActionCompilationMode enum.
     * @name dataform.ActionCompilationMode
     * @enum {number}
     * @property {number} ACTION_COMPILATION_MODE_UNSPECIFIED=0 ACTION_COMPILATION_MODE_UNSPECIFIED value
     * @property {number} ACTION_COMPILATION_MODE_AOT=1 ACTION_COMPILATION_MODE_AOT value
     * @property {number} ACTION_COMPILATION_MODE_JIT=2 ACTION_COMPILATION_MODE_JIT value
     */
    dataform.ActionCompilationMode = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "ACTION_COMPILATION_MODE_UNSPECIFIED"] = 0;
        values[valuesById[1] = "ACTION_COMPILATION_MODE_AOT"] = 1;
        values[valuesById[2] = "ACTION_COMPILATION_MODE_JIT"] = 2;
        return values;
    })();

    dataform.ActionDescriptor = (function() {

        /**
         * Properties of an ActionDescriptor.
         * @memberof dataform
         * @interface IActionDescriptor
         * @property {string|null} [description] ActionDescriptor description
         * @property {Array.<dataform.IColumnDescriptor>|null} [columns] ActionDescriptor columns
         * @property {Object.<string,string>|null} [bigqueryLabels] ActionDescriptor bigqueryLabels
         * @property {dataform.IMetadata|null} [metadata] ActionDescriptor metadata
         * @property {dataform.ActionCompilationMode|null} [compilationMode] ActionDescriptor compilationMode
         * @property {string|null} [reservation] ActionDescriptor reservation
         */

        /**
         * Constructs a new ActionDescriptor.
         * @memberof dataform
         * @classdesc Represents an ActionDescriptor.
         * @implements IActionDescriptor
         * @constructor
         * @param {dataform.IActionDescriptor=} [properties] Properties to set
         */
        function ActionDescriptor(properties) {
            this.columns = [];
            this.bigqueryLabels = {};
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ActionDescriptor description.
         * @member {string} description
         * @memberof dataform.ActionDescriptor
         * @instance
         */
        ActionDescriptor.prototype.description = "";

        /**
         * ActionDescriptor columns.
         * @member {Array.<dataform.IColumnDescriptor>} columns
         * @memberof dataform.ActionDescriptor
         * @instance
         */
        ActionDescriptor.prototype.columns = $util.emptyArray;

        /**
         * ActionDescriptor bigqueryLabels.
         * @member {Object.<string,string>} bigqueryLabels
         * @memberof dataform.ActionDescriptor
         * @instance
         */
        ActionDescriptor.prototype.bigqueryLabels = $util.emptyObject;

        /**
         * ActionDescriptor metadata.
         * @member {dataform.IMetadata|null|undefined} metadata
         * @memberof dataform.ActionDescriptor
         * @instance
         */
        ActionDescriptor.prototype.metadata = null;

        /**
         * ActionDescriptor compilationMode.
         * @member {dataform.ActionCompilationMode} compilationMode
         * @memberof dataform.ActionDescriptor
         * @instance
         */
        ActionDescriptor.prototype.compilationMode = 0;

        /**
         * ActionDescriptor reservation.
         * @member {string} reservation
         * @memberof dataform.ActionDescriptor
         * @instance
         */
        ActionDescriptor.prototype.reservation = "";

        /**
         * Creates a new ActionDescriptor instance using the specified properties.
         * @function create
         * @memberof dataform.ActionDescriptor
         * @static
         * @param {dataform.IActionDescriptor=} [properties] Properties to set
         * @returns {dataform.ActionDescriptor} ActionDescriptor instance
         */
        ActionDescriptor.create = function create(properties) {
            return new ActionDescriptor(properties);
        };

        /**
         * Encodes the specified ActionDescriptor message. Does not implicitly {@link dataform.ActionDescriptor.verify|verify} messages.
         * @function encode
         * @memberof dataform.ActionDescriptor
         * @static
         * @param {dataform.IActionDescriptor} message ActionDescriptor message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ActionDescriptor.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.description);
            if (message.columns != null && message.columns.length)
                for (let i = 0; i < message.columns.length; ++i)
                    $root.dataform.ColumnDescriptor.encode(message.columns[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.bigqueryLabels != null && Object.hasOwnProperty.call(message, "bigqueryLabels"))
                for (let keys = Object.keys(message.bigqueryLabels), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.bigqueryLabels[keys[i]]).ldelim();
            if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                $root.dataform.Metadata.encode(message.metadata, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.compilationMode != null && Object.hasOwnProperty.call(message, "compilationMode"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.compilationMode);
            if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.reservation);
            return writer;
        };

        /**
         * Encodes the specified ActionDescriptor message, length delimited. Does not implicitly {@link dataform.ActionDescriptor.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.ActionDescriptor
         * @static
         * @param {dataform.IActionDescriptor} message ActionDescriptor message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ActionDescriptor.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ActionDescriptor message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.ActionDescriptor
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.ActionDescriptor} ActionDescriptor
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ActionDescriptor.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionDescriptor(), key, value;
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.description = reader.string();
                        break;
                    }
                case 2: {
                        if (!(message.columns && message.columns.length))
                            message.columns = [];
                        message.columns.push($root.dataform.ColumnDescriptor.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 3: {
                        if (message.bigqueryLabels === $util.emptyObject)
                            message.bigqueryLabels = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = "";
                        while (reader.pos < end2) {
                            let tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.string();
                                break;
                            case 2:
                                value = reader.string();
                                break;
                            default:
                                reader.skipType(tag2 & 7, long);
                                break;
                            }
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.bigqueryLabels, key);
                        message.bigqueryLabels[key] = value;
                        break;
                    }
                case 4: {
                        message.metadata = $root.dataform.Metadata.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 5: {
                        message.compilationMode = reader.int32();
                        break;
                    }
                case 6: {
                        message.reservation = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ActionDescriptor message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.ActionDescriptor
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.ActionDescriptor} ActionDescriptor
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ActionDescriptor.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ActionDescriptor message.
         * @function verify
         * @memberof dataform.ActionDescriptor
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ActionDescriptor.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                if (!$util.isString(message.description))
                    return "description: string expected";
            if (message.columns != null && Object.hasOwnProperty.call(message, "columns")) {
                if (!Array.isArray(message.columns))
                    return "columns: array expected";
                for (let i = 0; i < message.columns.length; ++i) {
                    let error = $root.dataform.ColumnDescriptor.verify(message.columns[i], long + 1);
                    if (error)
                        return "columns." + error;
                }
            }
            if (message.bigqueryLabels != null && Object.hasOwnProperty.call(message, "bigqueryLabels")) {
                if (!$util.isObject(message.bigqueryLabels))
                    return "bigqueryLabels: object expected";
                let key = Object.keys(message.bigqueryLabels);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isString(message.bigqueryLabels[key[i]]))
                        return "bigqueryLabels: string{k:string} expected";
            }
            if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata")) {
                let error = $root.dataform.Metadata.verify(message.metadata, long + 1);
                if (error)
                    return "metadata." + error;
            }
            if (message.compilationMode != null && Object.hasOwnProperty.call(message, "compilationMode"))
                switch (message.compilationMode) {
                default:
                    return "compilationMode: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                if (!$util.isString(message.reservation))
                    return "reservation: string expected";
            return null;
        };

        /**
         * Creates an ActionDescriptor message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.ActionDescriptor
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.ActionDescriptor} ActionDescriptor
         */
        ActionDescriptor.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.ActionDescriptor)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.ActionDescriptor: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.ActionDescriptor();
            if (object.description != null)
                message.description = String(object.description);
            if (object.columns) {
                if (!Array.isArray(object.columns))
                    throw TypeError(".dataform.ActionDescriptor.columns: array expected");
                message.columns = [];
                for (let i = 0; i < object.columns.length; ++i) {
                    if (!$util.isObject(object.columns[i]))
                        throw TypeError(".dataform.ActionDescriptor.columns: object expected");
                    message.columns[i] = $root.dataform.ColumnDescriptor.fromObject(object.columns[i], long + 1);
                }
            }
            if (object.bigqueryLabels) {
                if (!$util.isObject(object.bigqueryLabels))
                    throw TypeError(".dataform.ActionDescriptor.bigqueryLabels: object expected");
                message.bigqueryLabels = {};
                for (let keys = Object.keys(object.bigqueryLabels), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.bigqueryLabels, keys[i]);
                    message.bigqueryLabels[keys[i]] = String(object.bigqueryLabels[keys[i]]);
                }
            }
            if (object.metadata != null) {
                if (!$util.isObject(object.metadata))
                    throw TypeError(".dataform.ActionDescriptor.metadata: object expected");
                message.metadata = $root.dataform.Metadata.fromObject(object.metadata, long + 1);
            }
            switch (object.compilationMode) {
            default:
                if (typeof object.compilationMode === "number") {
                    message.compilationMode = object.compilationMode;
                    break;
                }
                break;
            case "ACTION_COMPILATION_MODE_UNSPECIFIED":
            case 0:
                message.compilationMode = 0;
                break;
            case "ACTION_COMPILATION_MODE_AOT":
            case 1:
                message.compilationMode = 1;
                break;
            case "ACTION_COMPILATION_MODE_JIT":
            case 2:
                message.compilationMode = 2;
                break;
            }
            if (object.reservation != null)
                message.reservation = String(object.reservation);
            return message;
        };

        /**
         * Creates a plain object from an ActionDescriptor message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.ActionDescriptor
         * @static
         * @param {dataform.ActionDescriptor} message ActionDescriptor
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ActionDescriptor.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.columns = [];
            if (options.objects || options.defaults)
                object.bigqueryLabels = {};
            if (options.defaults) {
                object.description = "";
                object.metadata = null;
                object.compilationMode = options.enums === String ? "ACTION_COMPILATION_MODE_UNSPECIFIED" : 0;
                object.reservation = "";
            }
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                object.description = message.description;
            if (message.columns && message.columns.length) {
                object.columns = [];
                for (let j = 0; j < message.columns.length; ++j)
                    object.columns[j] = $root.dataform.ColumnDescriptor.toObject(message.columns[j], options, q + 1);
            }
            let keys2;
            if (message.bigqueryLabels && (keys2 = Object.keys(message.bigqueryLabels)).length) {
                object.bigqueryLabels = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.bigqueryLabels, keys2[j]);
                    object.bigqueryLabels[keys2[j]] = message.bigqueryLabels[keys2[j]];
                }
            }
            if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                object.metadata = $root.dataform.Metadata.toObject(message.metadata, options, q + 1);
            if (message.compilationMode != null && Object.hasOwnProperty.call(message, "compilationMode"))
                object.compilationMode = options.enums === String ? $root.dataform.ActionCompilationMode[message.compilationMode] === undefined ? message.compilationMode : $root.dataform.ActionCompilationMode[message.compilationMode] : message.compilationMode;
            if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                object.reservation = message.reservation;
            return object;
        };

        /**
         * Converts this ActionDescriptor to JSON.
         * @function toJSON
         * @memberof dataform.ActionDescriptor
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ActionDescriptor.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ActionDescriptor
         * @function getTypeUrl
         * @memberof dataform.ActionDescriptor
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ActionDescriptor.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.ActionDescriptor";
        };

        return ActionDescriptor;
    })();

    dataform.Metadata = (function() {

        /**
         * Properties of a Metadata.
         * @memberof dataform
         * @interface IMetadata
         * @property {string|null} [overview] Metadata overview
         * @property {google.protobuf.IStruct|null} [extraProperties] Metadata extraProperties
         */

        /**
         * Constructs a new Metadata.
         * @memberof dataform
         * @classdesc Represents a Metadata.
         * @implements IMetadata
         * @constructor
         * @param {dataform.IMetadata=} [properties] Properties to set
         */
        function Metadata(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Metadata overview.
         * @member {string} overview
         * @memberof dataform.Metadata
         * @instance
         */
        Metadata.prototype.overview = "";

        /**
         * Metadata extraProperties.
         * @member {google.protobuf.IStruct|null|undefined} extraProperties
         * @memberof dataform.Metadata
         * @instance
         */
        Metadata.prototype.extraProperties = null;

        /**
         * Creates a new Metadata instance using the specified properties.
         * @function create
         * @memberof dataform.Metadata
         * @static
         * @param {dataform.IMetadata=} [properties] Properties to set
         * @returns {dataform.Metadata} Metadata instance
         */
        Metadata.create = function create(properties) {
            return new Metadata(properties);
        };

        /**
         * Encodes the specified Metadata message. Does not implicitly {@link dataform.Metadata.verify|verify} messages.
         * @function encode
         * @memberof dataform.Metadata
         * @static
         * @param {dataform.IMetadata} message Metadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Metadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.overview != null && Object.hasOwnProperty.call(message, "overview"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.overview);
            if (message.extraProperties != null && Object.hasOwnProperty.call(message, "extraProperties"))
                $root.google.protobuf.Struct.encode(message.extraProperties, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Metadata message, length delimited. Does not implicitly {@link dataform.Metadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.Metadata
         * @static
         * @param {dataform.IMetadata} message Metadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Metadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Metadata message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.Metadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.Metadata} Metadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Metadata.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.Metadata();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.overview = reader.string();
                        break;
                    }
                case 2: {
                        message.extraProperties = $root.google.protobuf.Struct.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Metadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.Metadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.Metadata} Metadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Metadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Metadata message.
         * @function verify
         * @memberof dataform.Metadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Metadata.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.overview != null && Object.hasOwnProperty.call(message, "overview"))
                if (!$util.isString(message.overview))
                    return "overview: string expected";
            if (message.extraProperties != null && Object.hasOwnProperty.call(message, "extraProperties")) {
                let error = $root.google.protobuf.Struct.verify(message.extraProperties, long + 1);
                if (error)
                    return "extraProperties." + error;
            }
            return null;
        };

        /**
         * Creates a Metadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.Metadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.Metadata} Metadata
         */
        Metadata.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.Metadata)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.Metadata: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.Metadata();
            if (object.overview != null)
                message.overview = String(object.overview);
            if (object.extraProperties != null) {
                if (!$util.isObject(object.extraProperties))
                    throw TypeError(".dataform.Metadata.extraProperties: object expected");
                message.extraProperties = $root.google.protobuf.Struct.fromObject(object.extraProperties, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a Metadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.Metadata
         * @static
         * @param {dataform.Metadata} message Metadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Metadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.overview = "";
                object.extraProperties = null;
            }
            if (message.overview != null && Object.hasOwnProperty.call(message, "overview"))
                object.overview = message.overview;
            if (message.extraProperties != null && Object.hasOwnProperty.call(message, "extraProperties"))
                object.extraProperties = $root.google.protobuf.Struct.toObject(message.extraProperties, options, q + 1);
            return object;
        };

        /**
         * Converts this Metadata to JSON.
         * @function toJSON
         * @memberof dataform.Metadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Metadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Metadata
         * @function getTypeUrl
         * @memberof dataform.Metadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Metadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.Metadata";
        };

        return Metadata;
    })();

    dataform.ColumnDescriptor = (function() {

        /**
         * Properties of a ColumnDescriptor.
         * @memberof dataform
         * @interface IColumnDescriptor
         * @property {string|null} [description] ColumnDescriptor description
         * @property {Array.<string>|null} [path] ColumnDescriptor path
         * @property {string|null} [displayName] ColumnDescriptor displayName
         * @property {Array.<string>|null} [tags] ColumnDescriptor tags
         * @property {Array.<string>|null} [bigqueryPolicyTags] ColumnDescriptor bigqueryPolicyTags
         */

        /**
         * Constructs a new ColumnDescriptor.
         * @memberof dataform
         * @classdesc Represents a ColumnDescriptor.
         * @implements IColumnDescriptor
         * @constructor
         * @param {dataform.IColumnDescriptor=} [properties] Properties to set
         */
        function ColumnDescriptor(properties) {
            this.path = [];
            this.tags = [];
            this.bigqueryPolicyTags = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ColumnDescriptor description.
         * @member {string} description
         * @memberof dataform.ColumnDescriptor
         * @instance
         */
        ColumnDescriptor.prototype.description = "";

        /**
         * ColumnDescriptor path.
         * @member {Array.<string>} path
         * @memberof dataform.ColumnDescriptor
         * @instance
         */
        ColumnDescriptor.prototype.path = $util.emptyArray;

        /**
         * ColumnDescriptor displayName.
         * @member {string} displayName
         * @memberof dataform.ColumnDescriptor
         * @instance
         */
        ColumnDescriptor.prototype.displayName = "";

        /**
         * ColumnDescriptor tags.
         * @member {Array.<string>} tags
         * @memberof dataform.ColumnDescriptor
         * @instance
         */
        ColumnDescriptor.prototype.tags = $util.emptyArray;

        /**
         * ColumnDescriptor bigqueryPolicyTags.
         * @member {Array.<string>} bigqueryPolicyTags
         * @memberof dataform.ColumnDescriptor
         * @instance
         */
        ColumnDescriptor.prototype.bigqueryPolicyTags = $util.emptyArray;

        /**
         * Creates a new ColumnDescriptor instance using the specified properties.
         * @function create
         * @memberof dataform.ColumnDescriptor
         * @static
         * @param {dataform.IColumnDescriptor=} [properties] Properties to set
         * @returns {dataform.ColumnDescriptor} ColumnDescriptor instance
         */
        ColumnDescriptor.create = function create(properties) {
            return new ColumnDescriptor(properties);
        };

        /**
         * Encodes the specified ColumnDescriptor message. Does not implicitly {@link dataform.ColumnDescriptor.verify|verify} messages.
         * @function encode
         * @memberof dataform.ColumnDescriptor
         * @static
         * @param {dataform.IColumnDescriptor} message ColumnDescriptor message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnDescriptor.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.description);
            if (message.path != null && message.path.length)
                for (let i = 0; i < message.path.length; ++i)
                    writer.uint32(/* id 2, wireType 2 =*/18).string(message.path[i]);
            if (message.displayName != null && Object.hasOwnProperty.call(message, "displayName"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.displayName);
            if (message.tags != null && message.tags.length)
                for (let i = 0; i < message.tags.length; ++i)
                    writer.uint32(/* id 7, wireType 2 =*/58).string(message.tags[i]);
            if (message.bigqueryPolicyTags != null && message.bigqueryPolicyTags.length)
                for (let i = 0; i < message.bigqueryPolicyTags.length; ++i)
                    writer.uint32(/* id 8, wireType 2 =*/66).string(message.bigqueryPolicyTags[i]);
            return writer;
        };

        /**
         * Encodes the specified ColumnDescriptor message, length delimited. Does not implicitly {@link dataform.ColumnDescriptor.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.ColumnDescriptor
         * @static
         * @param {dataform.IColumnDescriptor} message ColumnDescriptor message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ColumnDescriptor.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ColumnDescriptor message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.ColumnDescriptor
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.ColumnDescriptor} ColumnDescriptor
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnDescriptor.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ColumnDescriptor();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.description = reader.string();
                        break;
                    }
                case 2: {
                        if (!(message.path && message.path.length))
                            message.path = [];
                        message.path.push(reader.string());
                        break;
                    }
                case 3: {
                        message.displayName = reader.string();
                        break;
                    }
                case 7: {
                        if (!(message.tags && message.tags.length))
                            message.tags = [];
                        message.tags.push(reader.string());
                        break;
                    }
                case 8: {
                        if (!(message.bigqueryPolicyTags && message.bigqueryPolicyTags.length))
                            message.bigqueryPolicyTags = [];
                        message.bigqueryPolicyTags.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ColumnDescriptor message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.ColumnDescriptor
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.ColumnDescriptor} ColumnDescriptor
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ColumnDescriptor.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ColumnDescriptor message.
         * @function verify
         * @memberof dataform.ColumnDescriptor
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ColumnDescriptor.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                if (!$util.isString(message.description))
                    return "description: string expected";
            if (message.path != null && Object.hasOwnProperty.call(message, "path")) {
                if (!Array.isArray(message.path))
                    return "path: array expected";
                for (let i = 0; i < message.path.length; ++i)
                    if (!$util.isString(message.path[i]))
                        return "path: string[] expected";
            }
            if (message.displayName != null && Object.hasOwnProperty.call(message, "displayName"))
                if (!$util.isString(message.displayName))
                    return "displayName: string expected";
            if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                if (!Array.isArray(message.tags))
                    return "tags: array expected";
                for (let i = 0; i < message.tags.length; ++i)
                    if (!$util.isString(message.tags[i]))
                        return "tags: string[] expected";
            }
            if (message.bigqueryPolicyTags != null && Object.hasOwnProperty.call(message, "bigqueryPolicyTags")) {
                if (!Array.isArray(message.bigqueryPolicyTags))
                    return "bigqueryPolicyTags: array expected";
                for (let i = 0; i < message.bigqueryPolicyTags.length; ++i)
                    if (!$util.isString(message.bigqueryPolicyTags[i]))
                        return "bigqueryPolicyTags: string[] expected";
            }
            return null;
        };

        /**
         * Creates a ColumnDescriptor message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.ColumnDescriptor
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.ColumnDescriptor} ColumnDescriptor
         */
        ColumnDescriptor.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.ColumnDescriptor)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.ColumnDescriptor: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.ColumnDescriptor();
            if (object.description != null)
                message.description = String(object.description);
            if (object.path) {
                if (!Array.isArray(object.path))
                    throw TypeError(".dataform.ColumnDescriptor.path: array expected");
                message.path = [];
                for (let i = 0; i < object.path.length; ++i)
                    message.path[i] = String(object.path[i]);
            }
            if (object.displayName != null)
                message.displayName = String(object.displayName);
            if (object.tags) {
                if (!Array.isArray(object.tags))
                    throw TypeError(".dataform.ColumnDescriptor.tags: array expected");
                message.tags = [];
                for (let i = 0; i < object.tags.length; ++i)
                    message.tags[i] = String(object.tags[i]);
            }
            if (object.bigqueryPolicyTags) {
                if (!Array.isArray(object.bigqueryPolicyTags))
                    throw TypeError(".dataform.ColumnDescriptor.bigqueryPolicyTags: array expected");
                message.bigqueryPolicyTags = [];
                for (let i = 0; i < object.bigqueryPolicyTags.length; ++i)
                    message.bigqueryPolicyTags[i] = String(object.bigqueryPolicyTags[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a ColumnDescriptor message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.ColumnDescriptor
         * @static
         * @param {dataform.ColumnDescriptor} message ColumnDescriptor
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ColumnDescriptor.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.path = [];
                object.tags = [];
                object.bigqueryPolicyTags = [];
            }
            if (options.defaults) {
                object.description = "";
                object.displayName = "";
            }
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                object.description = message.description;
            if (message.path && message.path.length) {
                object.path = [];
                for (let j = 0; j < message.path.length; ++j)
                    object.path[j] = message.path[j];
            }
            if (message.displayName != null && Object.hasOwnProperty.call(message, "displayName"))
                object.displayName = message.displayName;
            if (message.tags && message.tags.length) {
                object.tags = [];
                for (let j = 0; j < message.tags.length; ++j)
                    object.tags[j] = message.tags[j];
            }
            if (message.bigqueryPolicyTags && message.bigqueryPolicyTags.length) {
                object.bigqueryPolicyTags = [];
                for (let j = 0; j < message.bigqueryPolicyTags.length; ++j)
                    object.bigqueryPolicyTags[j] = message.bigqueryPolicyTags[j];
            }
            return object;
        };

        /**
         * Converts this ColumnDescriptor to JSON.
         * @function toJSON
         * @memberof dataform.ColumnDescriptor
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ColumnDescriptor.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ColumnDescriptor
         * @function getTypeUrl
         * @memberof dataform.ColumnDescriptor
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ColumnDescriptor.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.ColumnDescriptor";
        };

        return ColumnDescriptor;
    })();

    /**
     * TableType enum.
     * @name dataform.TableType
     * @enum {number}
     * @property {number} UNKNOWN_TYPE=0 UNKNOWN_TYPE value
     * @property {number} TABLE=1 TABLE value
     * @property {number} INCREMENTAL=2 INCREMENTAL value
     * @property {number} VIEW=3 VIEW value
     */
    dataform.TableType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN_TYPE"] = 0;
        values[valuesById[1] = "TABLE"] = 1;
        values[valuesById[2] = "INCREMENTAL"] = 2;
        values[valuesById[3] = "VIEW"] = 3;
        return values;
    })();

    /**
     * OnSchemaChange enum.
     * @name dataform.OnSchemaChange
     * @enum {number}
     * @property {number} IGNORE=0 IGNORE value
     * @property {number} FAIL=1 FAIL value
     * @property {number} EXTEND=2 EXTEND value
     * @property {number} SYNCHRONIZE=3 SYNCHRONIZE value
     */
    dataform.OnSchemaChange = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "IGNORE"] = 0;
        values[valuesById[1] = "FAIL"] = 1;
        values[valuesById[2] = "EXTEND"] = 2;
        values[valuesById[3] = "SYNCHRONIZE"] = 3;
        return values;
    })();

    /**
     * IncrementalStrategy enum.
     * @name dataform.IncrementalStrategy
     * @enum {number}
     * @property {number} INCREMENTAL_STRATEGY_UNSPECIFIED=0 INCREMENTAL_STRATEGY_UNSPECIFIED value
     * @property {number} MERGE=1 MERGE value
     * @property {number} INSERT_OVERWRITE=2 INSERT_OVERWRITE value
     */
    dataform.IncrementalStrategy = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "INCREMENTAL_STRATEGY_UNSPECIFIED"] = 0;
        values[valuesById[1] = "MERGE"] = 1;
        values[valuesById[2] = "INSERT_OVERWRITE"] = 2;
        return values;
    })();

    dataform.Table = (function() {

        /**
         * Properties of a Table.
         * @memberof dataform
         * @interface ITable
         * @property {dataform.TableType|null} [enumType] Table enumType
         * @property {dataform.ITarget|null} [target] Table target
         * @property {dataform.ITarget|null} [canonicalTarget] Table canonicalTarget
         * @property {Array.<dataform.ITarget>|null} [dependencyTargets] Table dependencyTargets
         * @property {dataform.ActionHermeticity|null} [hermeticity] Table hermeticity
         * @property {boolean|null} [disabled] Table disabled
         * @property {string|null} [type] Table type
         * @property {string|null} [query] Table query
         * @property {boolean|null} ["protected"] Table protected
         * @property {boolean|null} [materialized] Table materialized
         * @property {dataform.IActionDescriptor|null} [actionDescriptor] Table actionDescriptor
         * @property {Array.<string>|null} [tags] Table tags
         * @property {string|null} [where] Table where
         * @property {string|null} [incrementalQuery] Table incrementalQuery
         * @property {Array.<string>|null} [uniqueKey] Table uniqueKey
         * @property {dataform.OnSchemaChange|null} [onSchemaChange] Table onSchemaChange
         * @property {Array.<string>|null} [preOps] Table preOps
         * @property {Array.<string>|null} [postOps] Table postOps
         * @property {Array.<string>|null} [incrementalPreOps] Table incrementalPreOps
         * @property {Array.<string>|null} [incrementalPostOps] Table incrementalPostOps
         * @property {string|null} [jitCode] Table jitCode
         * @property {dataform.IncrementalStrategy|null} [incrementalStrategy] Table incrementalStrategy
         * @property {dataform.IBigQueryOptions|null} [bigquery] Table bigquery
         * @property {string|null} [fileName] Table fileName
         */

        /**
         * Constructs a new Table.
         * @memberof dataform
         * @classdesc Represents a Table.
         * @implements ITable
         * @constructor
         * @param {dataform.ITable=} [properties] Properties to set
         */
        function Table(properties) {
            this.dependencyTargets = [];
            this.tags = [];
            this.uniqueKey = [];
            this.preOps = [];
            this.postOps = [];
            this.incrementalPreOps = [];
            this.incrementalPostOps = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Table enumType.
         * @member {dataform.TableType} enumType
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.enumType = 0;

        /**
         * Table target.
         * @member {dataform.ITarget|null|undefined} target
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.target = null;

        /**
         * Table canonicalTarget.
         * @member {dataform.ITarget|null|undefined} canonicalTarget
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.canonicalTarget = null;

        /**
         * Table dependencyTargets.
         * @member {Array.<dataform.ITarget>} dependencyTargets
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.dependencyTargets = $util.emptyArray;

        /**
         * Table hermeticity.
         * @member {dataform.ActionHermeticity} hermeticity
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.hermeticity = 0;

        /**
         * Table disabled.
         * @member {boolean} disabled
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.disabled = false;

        /**
         * Table type.
         * @member {string} type
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.type = "";

        /**
         * Table query.
         * @member {string} query
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.query = "";

        /**
         * Table protected.
         * @member {boolean} protected
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype["protected"] = false;

        /**
         * Table materialized.
         * @member {boolean} materialized
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.materialized = false;

        /**
         * Table actionDescriptor.
         * @member {dataform.IActionDescriptor|null|undefined} actionDescriptor
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.actionDescriptor = null;

        /**
         * Table tags.
         * @member {Array.<string>} tags
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.tags = $util.emptyArray;

        /**
         * Table where.
         * @member {string} where
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.where = "";

        /**
         * Table incrementalQuery.
         * @member {string} incrementalQuery
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.incrementalQuery = "";

        /**
         * Table uniqueKey.
         * @member {Array.<string>} uniqueKey
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.uniqueKey = $util.emptyArray;

        /**
         * Table onSchemaChange.
         * @member {dataform.OnSchemaChange} onSchemaChange
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.onSchemaChange = 0;

        /**
         * Table preOps.
         * @member {Array.<string>} preOps
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.preOps = $util.emptyArray;

        /**
         * Table postOps.
         * @member {Array.<string>} postOps
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.postOps = $util.emptyArray;

        /**
         * Table incrementalPreOps.
         * @member {Array.<string>} incrementalPreOps
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.incrementalPreOps = $util.emptyArray;

        /**
         * Table incrementalPostOps.
         * @member {Array.<string>} incrementalPostOps
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.incrementalPostOps = $util.emptyArray;

        /**
         * Table jitCode.
         * @member {string} jitCode
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.jitCode = "";

        /**
         * Table incrementalStrategy.
         * @member {dataform.IncrementalStrategy} incrementalStrategy
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.incrementalStrategy = 0;

        /**
         * Table bigquery.
         * @member {dataform.IBigQueryOptions|null|undefined} bigquery
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.bigquery = null;

        /**
         * Table fileName.
         * @member {string} fileName
         * @memberof dataform.Table
         * @instance
         */
        Table.prototype.fileName = "";

        /**
         * Creates a new Table instance using the specified properties.
         * @function create
         * @memberof dataform.Table
         * @static
         * @param {dataform.ITable=} [properties] Properties to set
         * @returns {dataform.Table} Table instance
         */
        Table.create = function create(properties) {
            return new Table(properties);
        };

        /**
         * Encodes the specified Table message. Does not implicitly {@link dataform.Table.verify|verify} messages.
         * @function encode
         * @memberof dataform.Table
         * @static
         * @param {dataform.ITable} message Table message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Table.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.type);
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                $root.dataform.Target.encode(message.target, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.query != null && Object.hasOwnProperty.call(message, "query"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.query);
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.disabled);
            if (message.where != null && Object.hasOwnProperty.call(message, "where"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.where);
            if (message["protected"] != null && Object.hasOwnProperty.call(message, "protected"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message["protected"]);
            if (message.preOps != null && message.preOps.length)
                for (let i = 0; i < message.preOps.length; ++i)
                    writer.uint32(/* id 13, wireType 2 =*/106).string(message.preOps[i]);
            if (message.postOps != null && message.postOps.length)
                for (let i = 0; i < message.postOps.length; ++i)
                    writer.uint32(/* id 14, wireType 2 =*/114).string(message.postOps[i]);
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                writer.uint32(/* id 18, wireType 2 =*/146).string(message.fileName);
            if (message.bigquery != null && Object.hasOwnProperty.call(message, "bigquery"))
                $root.dataform.BigQueryOptions.encode(message.bigquery, writer.uint32(/* id 22, wireType 2 =*/178).fork(), q + 1).ldelim();
            if (message.tags != null && message.tags.length)
                for (let i = 0; i < message.tags.length; ++i)
                    writer.uint32(/* id 23, wireType 2 =*/186).string(message.tags[i]);
            if (message.actionDescriptor != null && Object.hasOwnProperty.call(message, "actionDescriptor"))
                $root.dataform.ActionDescriptor.encode(message.actionDescriptor, writer.uint32(/* id 24, wireType 2 =*/194).fork(), q + 1).ldelim();
            if (message.incrementalQuery != null && Object.hasOwnProperty.call(message, "incrementalQuery"))
                writer.uint32(/* id 26, wireType 2 =*/210).string(message.incrementalQuery);
            if (message.dependencyTargets != null && message.dependencyTargets.length)
                for (let i = 0; i < message.dependencyTargets.length; ++i)
                    $root.dataform.Target.encode(message.dependencyTargets[i], writer.uint32(/* id 27, wireType 2 =*/218).fork(), q + 1).ldelim();
            if (message.incrementalPreOps != null && message.incrementalPreOps.length)
                for (let i = 0; i < message.incrementalPreOps.length; ++i)
                    writer.uint32(/* id 28, wireType 2 =*/226).string(message.incrementalPreOps[i]);
            if (message.incrementalPostOps != null && message.incrementalPostOps.length)
                for (let i = 0; i < message.incrementalPostOps.length; ++i)
                    writer.uint32(/* id 29, wireType 2 =*/234).string(message.incrementalPostOps[i]);
            if (message.uniqueKey != null && message.uniqueKey.length)
                for (let i = 0; i < message.uniqueKey.length; ++i)
                    writer.uint32(/* id 30, wireType 2 =*/242).string(message.uniqueKey[i]);
            if (message.hermeticity != null && Object.hasOwnProperty.call(message, "hermeticity"))
                writer.uint32(/* id 31, wireType 0 =*/248).int32(message.hermeticity);
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget"))
                $root.dataform.Target.encode(message.canonicalTarget, writer.uint32(/* id 32, wireType 2 =*/258).fork(), q + 1).ldelim();
            if (message.materialized != null && Object.hasOwnProperty.call(message, "materialized"))
                writer.uint32(/* id 35, wireType 0 =*/280).bool(message.materialized);
            if (message.enumType != null && Object.hasOwnProperty.call(message, "enumType"))
                writer.uint32(/* id 36, wireType 0 =*/288).int32(message.enumType);
            if (message.onSchemaChange != null && Object.hasOwnProperty.call(message, "onSchemaChange"))
                writer.uint32(/* id 37, wireType 0 =*/296).int32(message.onSchemaChange);
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                writer.uint32(/* id 38, wireType 2 =*/306).string(message.jitCode);
            if (message.incrementalStrategy != null && Object.hasOwnProperty.call(message, "incrementalStrategy"))
                writer.uint32(/* id 39, wireType 0 =*/312).int32(message.incrementalStrategy);
            return writer;
        };

        /**
         * Encodes the specified Table message, length delimited. Does not implicitly {@link dataform.Table.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.Table
         * @static
         * @param {dataform.ITable} message Table message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Table.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Table message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.Table
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.Table} Table
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Table.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.Table();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 36: {
                        message.enumType = reader.int32();
                        break;
                    }
                case 4: {
                        message.target = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 32: {
                        message.canonicalTarget = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 27: {
                        if (!(message.dependencyTargets && message.dependencyTargets.length))
                            message.dependencyTargets = [];
                        message.dependencyTargets.push($root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 31: {
                        message.hermeticity = reader.int32();
                        break;
                    }
                case 6: {
                        message.disabled = reader.bool();
                        break;
                    }
                case 3: {
                        message.type = reader.string();
                        break;
                    }
                case 5: {
                        message.query = reader.string();
                        break;
                    }
                case 9: {
                        message["protected"] = reader.bool();
                        break;
                    }
                case 35: {
                        message.materialized = reader.bool();
                        break;
                    }
                case 24: {
                        message.actionDescriptor = $root.dataform.ActionDescriptor.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 23: {
                        if (!(message.tags && message.tags.length))
                            message.tags = [];
                        message.tags.push(reader.string());
                        break;
                    }
                case 8: {
                        message.where = reader.string();
                        break;
                    }
                case 26: {
                        message.incrementalQuery = reader.string();
                        break;
                    }
                case 30: {
                        if (!(message.uniqueKey && message.uniqueKey.length))
                            message.uniqueKey = [];
                        message.uniqueKey.push(reader.string());
                        break;
                    }
                case 37: {
                        message.onSchemaChange = reader.int32();
                        break;
                    }
                case 13: {
                        if (!(message.preOps && message.preOps.length))
                            message.preOps = [];
                        message.preOps.push(reader.string());
                        break;
                    }
                case 14: {
                        if (!(message.postOps && message.postOps.length))
                            message.postOps = [];
                        message.postOps.push(reader.string());
                        break;
                    }
                case 28: {
                        if (!(message.incrementalPreOps && message.incrementalPreOps.length))
                            message.incrementalPreOps = [];
                        message.incrementalPreOps.push(reader.string());
                        break;
                    }
                case 29: {
                        if (!(message.incrementalPostOps && message.incrementalPostOps.length))
                            message.incrementalPostOps = [];
                        message.incrementalPostOps.push(reader.string());
                        break;
                    }
                case 38: {
                        message.jitCode = reader.string();
                        break;
                    }
                case 39: {
                        message.incrementalStrategy = reader.int32();
                        break;
                    }
                case 22: {
                        message.bigquery = $root.dataform.BigQueryOptions.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 18: {
                        message.fileName = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Table message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.Table
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.Table} Table
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Table.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Table message.
         * @function verify
         * @memberof dataform.Table
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Table.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.enumType != null && Object.hasOwnProperty.call(message, "enumType"))
                switch (message.enumType) {
                default:
                    return "enumType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                    break;
                }
            if (message.target != null && Object.hasOwnProperty.call(message, "target")) {
                let error = $root.dataform.Target.verify(message.target, long + 1);
                if (error)
                    return "target." + error;
            }
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget")) {
                let error = $root.dataform.Target.verify(message.canonicalTarget, long + 1);
                if (error)
                    return "canonicalTarget." + error;
            }
            if (message.dependencyTargets != null && Object.hasOwnProperty.call(message, "dependencyTargets")) {
                if (!Array.isArray(message.dependencyTargets))
                    return "dependencyTargets: array expected";
                for (let i = 0; i < message.dependencyTargets.length; ++i) {
                    let error = $root.dataform.Target.verify(message.dependencyTargets[i], long + 1);
                    if (error)
                        return "dependencyTargets." + error;
                }
            }
            if (message.hermeticity != null && Object.hasOwnProperty.call(message, "hermeticity"))
                switch (message.hermeticity) {
                default:
                    return "hermeticity: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                if (typeof message.disabled !== "boolean")
                    return "disabled: boolean expected";
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            if (message.query != null && Object.hasOwnProperty.call(message, "query"))
                if (!$util.isString(message.query))
                    return "query: string expected";
            if (message["protected"] != null && Object.hasOwnProperty.call(message, "protected"))
                if (typeof message["protected"] !== "boolean")
                    return "protected: boolean expected";
            if (message.materialized != null && Object.hasOwnProperty.call(message, "materialized"))
                if (typeof message.materialized !== "boolean")
                    return "materialized: boolean expected";
            if (message.actionDescriptor != null && Object.hasOwnProperty.call(message, "actionDescriptor")) {
                let error = $root.dataform.ActionDescriptor.verify(message.actionDescriptor, long + 1);
                if (error)
                    return "actionDescriptor." + error;
            }
            if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                if (!Array.isArray(message.tags))
                    return "tags: array expected";
                for (let i = 0; i < message.tags.length; ++i)
                    if (!$util.isString(message.tags[i]))
                        return "tags: string[] expected";
            }
            if (message.where != null && Object.hasOwnProperty.call(message, "where"))
                if (!$util.isString(message.where))
                    return "where: string expected";
            if (message.incrementalQuery != null && Object.hasOwnProperty.call(message, "incrementalQuery"))
                if (!$util.isString(message.incrementalQuery))
                    return "incrementalQuery: string expected";
            if (message.uniqueKey != null && Object.hasOwnProperty.call(message, "uniqueKey")) {
                if (!Array.isArray(message.uniqueKey))
                    return "uniqueKey: array expected";
                for (let i = 0; i < message.uniqueKey.length; ++i)
                    if (!$util.isString(message.uniqueKey[i]))
                        return "uniqueKey: string[] expected";
            }
            if (message.onSchemaChange != null && Object.hasOwnProperty.call(message, "onSchemaChange"))
                switch (message.onSchemaChange) {
                default:
                    return "onSchemaChange: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                    break;
                }
            if (message.preOps != null && Object.hasOwnProperty.call(message, "preOps")) {
                if (!Array.isArray(message.preOps))
                    return "preOps: array expected";
                for (let i = 0; i < message.preOps.length; ++i)
                    if (!$util.isString(message.preOps[i]))
                        return "preOps: string[] expected";
            }
            if (message.postOps != null && Object.hasOwnProperty.call(message, "postOps")) {
                if (!Array.isArray(message.postOps))
                    return "postOps: array expected";
                for (let i = 0; i < message.postOps.length; ++i)
                    if (!$util.isString(message.postOps[i]))
                        return "postOps: string[] expected";
            }
            if (message.incrementalPreOps != null && Object.hasOwnProperty.call(message, "incrementalPreOps")) {
                if (!Array.isArray(message.incrementalPreOps))
                    return "incrementalPreOps: array expected";
                for (let i = 0; i < message.incrementalPreOps.length; ++i)
                    if (!$util.isString(message.incrementalPreOps[i]))
                        return "incrementalPreOps: string[] expected";
            }
            if (message.incrementalPostOps != null && Object.hasOwnProperty.call(message, "incrementalPostOps")) {
                if (!Array.isArray(message.incrementalPostOps))
                    return "incrementalPostOps: array expected";
                for (let i = 0; i < message.incrementalPostOps.length; ++i)
                    if (!$util.isString(message.incrementalPostOps[i]))
                        return "incrementalPostOps: string[] expected";
            }
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                if (!$util.isString(message.jitCode))
                    return "jitCode: string expected";
            if (message.incrementalStrategy != null && Object.hasOwnProperty.call(message, "incrementalStrategy"))
                switch (message.incrementalStrategy) {
                default:
                    return "incrementalStrategy: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            if (message.bigquery != null && Object.hasOwnProperty.call(message, "bigquery")) {
                let error = $root.dataform.BigQueryOptions.verify(message.bigquery, long + 1);
                if (error)
                    return "bigquery." + error;
            }
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                if (!$util.isString(message.fileName))
                    return "fileName: string expected";
            return null;
        };

        /**
         * Creates a Table message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.Table
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.Table} Table
         */
        Table.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.Table)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.Table: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.Table();
            switch (object.enumType) {
            default:
                if (typeof object.enumType === "number") {
                    message.enumType = object.enumType;
                    break;
                }
                break;
            case "UNKNOWN_TYPE":
            case 0:
                message.enumType = 0;
                break;
            case "TABLE":
            case 1:
                message.enumType = 1;
                break;
            case "INCREMENTAL":
            case 2:
                message.enumType = 2;
                break;
            case "VIEW":
            case 3:
                message.enumType = 3;
                break;
            }
            if (object.target != null) {
                if (!$util.isObject(object.target))
                    throw TypeError(".dataform.Table.target: object expected");
                message.target = $root.dataform.Target.fromObject(object.target, long + 1);
            }
            if (object.canonicalTarget != null) {
                if (!$util.isObject(object.canonicalTarget))
                    throw TypeError(".dataform.Table.canonicalTarget: object expected");
                message.canonicalTarget = $root.dataform.Target.fromObject(object.canonicalTarget, long + 1);
            }
            if (object.dependencyTargets) {
                if (!Array.isArray(object.dependencyTargets))
                    throw TypeError(".dataform.Table.dependencyTargets: array expected");
                message.dependencyTargets = [];
                for (let i = 0; i < object.dependencyTargets.length; ++i) {
                    if (!$util.isObject(object.dependencyTargets[i]))
                        throw TypeError(".dataform.Table.dependencyTargets: object expected");
                    message.dependencyTargets[i] = $root.dataform.Target.fromObject(object.dependencyTargets[i], long + 1);
                }
            }
            switch (object.hermeticity) {
            default:
                if (typeof object.hermeticity === "number") {
                    message.hermeticity = object.hermeticity;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                message.hermeticity = 0;
                break;
            case "HERMETIC":
            case 1:
                message.hermeticity = 1;
                break;
            case "NON_HERMETIC":
            case 2:
                message.hermeticity = 2;
                break;
            }
            if (object.disabled != null)
                message.disabled = Boolean(object.disabled);
            if (object.type != null)
                message.type = String(object.type);
            if (object.query != null)
                message.query = String(object.query);
            if (object["protected"] != null)
                message["protected"] = Boolean(object["protected"]);
            if (object.materialized != null)
                message.materialized = Boolean(object.materialized);
            if (object.actionDescriptor != null) {
                if (!$util.isObject(object.actionDescriptor))
                    throw TypeError(".dataform.Table.actionDescriptor: object expected");
                message.actionDescriptor = $root.dataform.ActionDescriptor.fromObject(object.actionDescriptor, long + 1);
            }
            if (object.tags) {
                if (!Array.isArray(object.tags))
                    throw TypeError(".dataform.Table.tags: array expected");
                message.tags = [];
                for (let i = 0; i < object.tags.length; ++i)
                    message.tags[i] = String(object.tags[i]);
            }
            if (object.where != null)
                message.where = String(object.where);
            if (object.incrementalQuery != null)
                message.incrementalQuery = String(object.incrementalQuery);
            if (object.uniqueKey) {
                if (!Array.isArray(object.uniqueKey))
                    throw TypeError(".dataform.Table.uniqueKey: array expected");
                message.uniqueKey = [];
                for (let i = 0; i < object.uniqueKey.length; ++i)
                    message.uniqueKey[i] = String(object.uniqueKey[i]);
            }
            switch (object.onSchemaChange) {
            default:
                if (typeof object.onSchemaChange === "number") {
                    message.onSchemaChange = object.onSchemaChange;
                    break;
                }
                break;
            case "IGNORE":
            case 0:
                message.onSchemaChange = 0;
                break;
            case "FAIL":
            case 1:
                message.onSchemaChange = 1;
                break;
            case "EXTEND":
            case 2:
                message.onSchemaChange = 2;
                break;
            case "SYNCHRONIZE":
            case 3:
                message.onSchemaChange = 3;
                break;
            }
            if (object.preOps) {
                if (!Array.isArray(object.preOps))
                    throw TypeError(".dataform.Table.preOps: array expected");
                message.preOps = [];
                for (let i = 0; i < object.preOps.length; ++i)
                    message.preOps[i] = String(object.preOps[i]);
            }
            if (object.postOps) {
                if (!Array.isArray(object.postOps))
                    throw TypeError(".dataform.Table.postOps: array expected");
                message.postOps = [];
                for (let i = 0; i < object.postOps.length; ++i)
                    message.postOps[i] = String(object.postOps[i]);
            }
            if (object.incrementalPreOps) {
                if (!Array.isArray(object.incrementalPreOps))
                    throw TypeError(".dataform.Table.incrementalPreOps: array expected");
                message.incrementalPreOps = [];
                for (let i = 0; i < object.incrementalPreOps.length; ++i)
                    message.incrementalPreOps[i] = String(object.incrementalPreOps[i]);
            }
            if (object.incrementalPostOps) {
                if (!Array.isArray(object.incrementalPostOps))
                    throw TypeError(".dataform.Table.incrementalPostOps: array expected");
                message.incrementalPostOps = [];
                for (let i = 0; i < object.incrementalPostOps.length; ++i)
                    message.incrementalPostOps[i] = String(object.incrementalPostOps[i]);
            }
            if (object.jitCode != null)
                message.jitCode = String(object.jitCode);
            switch (object.incrementalStrategy) {
            default:
                if (typeof object.incrementalStrategy === "number") {
                    message.incrementalStrategy = object.incrementalStrategy;
                    break;
                }
                break;
            case "INCREMENTAL_STRATEGY_UNSPECIFIED":
            case 0:
                message.incrementalStrategy = 0;
                break;
            case "MERGE":
            case 1:
                message.incrementalStrategy = 1;
                break;
            case "INSERT_OVERWRITE":
            case 2:
                message.incrementalStrategy = 2;
                break;
            }
            if (object.bigquery != null) {
                if (!$util.isObject(object.bigquery))
                    throw TypeError(".dataform.Table.bigquery: object expected");
                message.bigquery = $root.dataform.BigQueryOptions.fromObject(object.bigquery, long + 1);
            }
            if (object.fileName != null)
                message.fileName = String(object.fileName);
            return message;
        };

        /**
         * Creates a plain object from a Table message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.Table
         * @static
         * @param {dataform.Table} message Table
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Table.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.preOps = [];
                object.postOps = [];
                object.tags = [];
                object.dependencyTargets = [];
                object.incrementalPreOps = [];
                object.incrementalPostOps = [];
                object.uniqueKey = [];
            }
            if (options.defaults) {
                object.type = "";
                object.target = null;
                object.query = "";
                object.disabled = false;
                object.where = "";
                object["protected"] = false;
                object.fileName = "";
                object.bigquery = null;
                object.actionDescriptor = null;
                object.incrementalQuery = "";
                object.hermeticity = options.enums === String ? "UNKNOWN" : 0;
                object.canonicalTarget = null;
                object.materialized = false;
                object.enumType = options.enums === String ? "UNKNOWN_TYPE" : 0;
                object.onSchemaChange = options.enums === String ? "IGNORE" : 0;
                object.jitCode = "";
                object.incrementalStrategy = options.enums === String ? "INCREMENTAL_STRATEGY_UNSPECIFIED" : 0;
            }
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                object.type = message.type;
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                object.target = $root.dataform.Target.toObject(message.target, options, q + 1);
            if (message.query != null && Object.hasOwnProperty.call(message, "query"))
                object.query = message.query;
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                object.disabled = message.disabled;
            if (message.where != null && Object.hasOwnProperty.call(message, "where"))
                object.where = message.where;
            if (message["protected"] != null && Object.hasOwnProperty.call(message, "protected"))
                object["protected"] = message["protected"];
            if (message.preOps && message.preOps.length) {
                object.preOps = [];
                for (let j = 0; j < message.preOps.length; ++j)
                    object.preOps[j] = message.preOps[j];
            }
            if (message.postOps && message.postOps.length) {
                object.postOps = [];
                for (let j = 0; j < message.postOps.length; ++j)
                    object.postOps[j] = message.postOps[j];
            }
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                object.fileName = message.fileName;
            if (message.bigquery != null && Object.hasOwnProperty.call(message, "bigquery"))
                object.bigquery = $root.dataform.BigQueryOptions.toObject(message.bigquery, options, q + 1);
            if (message.tags && message.tags.length) {
                object.tags = [];
                for (let j = 0; j < message.tags.length; ++j)
                    object.tags[j] = message.tags[j];
            }
            if (message.actionDescriptor != null && Object.hasOwnProperty.call(message, "actionDescriptor"))
                object.actionDescriptor = $root.dataform.ActionDescriptor.toObject(message.actionDescriptor, options, q + 1);
            if (message.incrementalQuery != null && Object.hasOwnProperty.call(message, "incrementalQuery"))
                object.incrementalQuery = message.incrementalQuery;
            if (message.dependencyTargets && message.dependencyTargets.length) {
                object.dependencyTargets = [];
                for (let j = 0; j < message.dependencyTargets.length; ++j)
                    object.dependencyTargets[j] = $root.dataform.Target.toObject(message.dependencyTargets[j], options, q + 1);
            }
            if (message.incrementalPreOps && message.incrementalPreOps.length) {
                object.incrementalPreOps = [];
                for (let j = 0; j < message.incrementalPreOps.length; ++j)
                    object.incrementalPreOps[j] = message.incrementalPreOps[j];
            }
            if (message.incrementalPostOps && message.incrementalPostOps.length) {
                object.incrementalPostOps = [];
                for (let j = 0; j < message.incrementalPostOps.length; ++j)
                    object.incrementalPostOps[j] = message.incrementalPostOps[j];
            }
            if (message.uniqueKey && message.uniqueKey.length) {
                object.uniqueKey = [];
                for (let j = 0; j < message.uniqueKey.length; ++j)
                    object.uniqueKey[j] = message.uniqueKey[j];
            }
            if (message.hermeticity != null && Object.hasOwnProperty.call(message, "hermeticity"))
                object.hermeticity = options.enums === String ? $root.dataform.ActionHermeticity[message.hermeticity] === undefined ? message.hermeticity : $root.dataform.ActionHermeticity[message.hermeticity] : message.hermeticity;
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget"))
                object.canonicalTarget = $root.dataform.Target.toObject(message.canonicalTarget, options, q + 1);
            if (message.materialized != null && Object.hasOwnProperty.call(message, "materialized"))
                object.materialized = message.materialized;
            if (message.enumType != null && Object.hasOwnProperty.call(message, "enumType"))
                object.enumType = options.enums === String ? $root.dataform.TableType[message.enumType] === undefined ? message.enumType : $root.dataform.TableType[message.enumType] : message.enumType;
            if (message.onSchemaChange != null && Object.hasOwnProperty.call(message, "onSchemaChange"))
                object.onSchemaChange = options.enums === String ? $root.dataform.OnSchemaChange[message.onSchemaChange] === undefined ? message.onSchemaChange : $root.dataform.OnSchemaChange[message.onSchemaChange] : message.onSchemaChange;
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                object.jitCode = message.jitCode;
            if (message.incrementalStrategy != null && Object.hasOwnProperty.call(message, "incrementalStrategy"))
                object.incrementalStrategy = options.enums === String ? $root.dataform.IncrementalStrategy[message.incrementalStrategy] === undefined ? message.incrementalStrategy : $root.dataform.IncrementalStrategy[message.incrementalStrategy] : message.incrementalStrategy;
            return object;
        };

        /**
         * Converts this Table to JSON.
         * @function toJSON
         * @memberof dataform.Table
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Table.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Table
         * @function getTypeUrl
         * @memberof dataform.Table
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Table.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.Table";
        };

        return Table;
    })();

    dataform.Operation = (function() {

        /**
         * Properties of an Operation.
         * @memberof dataform
         * @interface IOperation
         * @property {dataform.ITarget|null} [target] Operation target
         * @property {dataform.ITarget|null} [canonicalTarget] Operation canonicalTarget
         * @property {Array.<dataform.ITarget>|null} [dependencyTargets] Operation dependencyTargets
         * @property {dataform.ActionHermeticity|null} [hermeticity] Operation hermeticity
         * @property {boolean|null} [disabled] Operation disabled
         * @property {Array.<string>|null} [queries] Operation queries
         * @property {string|null} [jitCode] Operation jitCode
         * @property {boolean|null} [hasOutput] Operation hasOutput
         * @property {Array.<string>|null} [tags] Operation tags
         * @property {dataform.IActionDescriptor|null} [actionDescriptor] Operation actionDescriptor
         * @property {string|null} [fileName] Operation fileName
         */

        /**
         * Constructs a new Operation.
         * @memberof dataform
         * @classdesc Represents an Operation.
         * @implements IOperation
         * @constructor
         * @param {dataform.IOperation=} [properties] Properties to set
         */
        function Operation(properties) {
            this.dependencyTargets = [];
            this.queries = [];
            this.tags = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Operation target.
         * @member {dataform.ITarget|null|undefined} target
         * @memberof dataform.Operation
         * @instance
         */
        Operation.prototype.target = null;

        /**
         * Operation canonicalTarget.
         * @member {dataform.ITarget|null|undefined} canonicalTarget
         * @memberof dataform.Operation
         * @instance
         */
        Operation.prototype.canonicalTarget = null;

        /**
         * Operation dependencyTargets.
         * @member {Array.<dataform.ITarget>} dependencyTargets
         * @memberof dataform.Operation
         * @instance
         */
        Operation.prototype.dependencyTargets = $util.emptyArray;

        /**
         * Operation hermeticity.
         * @member {dataform.ActionHermeticity} hermeticity
         * @memberof dataform.Operation
         * @instance
         */
        Operation.prototype.hermeticity = 0;

        /**
         * Operation disabled.
         * @member {boolean} disabled
         * @memberof dataform.Operation
         * @instance
         */
        Operation.prototype.disabled = false;

        /**
         * Operation queries.
         * @member {Array.<string>} queries
         * @memberof dataform.Operation
         * @instance
         */
        Operation.prototype.queries = $util.emptyArray;

        /**
         * Operation jitCode.
         * @member {string} jitCode
         * @memberof dataform.Operation
         * @instance
         */
        Operation.prototype.jitCode = "";

        /**
         * Operation hasOutput.
         * @member {boolean} hasOutput
         * @memberof dataform.Operation
         * @instance
         */
        Operation.prototype.hasOutput = false;

        /**
         * Operation tags.
         * @member {Array.<string>} tags
         * @memberof dataform.Operation
         * @instance
         */
        Operation.prototype.tags = $util.emptyArray;

        /**
         * Operation actionDescriptor.
         * @member {dataform.IActionDescriptor|null|undefined} actionDescriptor
         * @memberof dataform.Operation
         * @instance
         */
        Operation.prototype.actionDescriptor = null;

        /**
         * Operation fileName.
         * @member {string} fileName
         * @memberof dataform.Operation
         * @instance
         */
        Operation.prototype.fileName = "";

        /**
         * Creates a new Operation instance using the specified properties.
         * @function create
         * @memberof dataform.Operation
         * @static
         * @param {dataform.IOperation=} [properties] Properties to set
         * @returns {dataform.Operation} Operation instance
         */
        Operation.create = function create(properties) {
            return new Operation(properties);
        };

        /**
         * Encodes the specified Operation message. Does not implicitly {@link dataform.Operation.verify|verify} messages.
         * @function encode
         * @memberof dataform.Operation
         * @static
         * @param {dataform.IOperation} message Operation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Operation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                $root.dataform.Target.encode(message.target, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.queries != null && message.queries.length)
                for (let i = 0; i < message.queries.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.queries[i]);
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.fileName);
            if (message.hasOutput != null && Object.hasOwnProperty.call(message, "hasOutput"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.hasOutput);
            if (message.tags != null && message.tags.length)
                for (let i = 0; i < message.tags.length; ++i)
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.tags[i]);
            if (message.actionDescriptor != null && Object.hasOwnProperty.call(message, "actionDescriptor"))
                $root.dataform.ActionDescriptor.encode(message.actionDescriptor, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
            if (message.dependencyTargets != null && message.dependencyTargets.length)
                for (let i = 0; i < message.dependencyTargets.length; ++i)
                    $root.dataform.Target.encode(message.dependencyTargets[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
            if (message.hermeticity != null && Object.hasOwnProperty.call(message, "hermeticity"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.hermeticity);
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget"))
                $root.dataform.Target.encode(message.canonicalTarget, writer.uint32(/* id 13, wireType 2 =*/106).fork(), q + 1).ldelim();
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.disabled);
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                writer.uint32(/* id 15, wireType 2 =*/122).string(message.jitCode);
            return writer;
        };

        /**
         * Encodes the specified Operation message, length delimited. Does not implicitly {@link dataform.Operation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.Operation
         * @static
         * @param {dataform.IOperation} message Operation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Operation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an Operation message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.Operation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.Operation} Operation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Operation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.Operation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 3: {
                        message.target = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 13: {
                        message.canonicalTarget = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 11: {
                        if (!(message.dependencyTargets && message.dependencyTargets.length))
                            message.dependencyTargets = [];
                        message.dependencyTargets.push($root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 12: {
                        message.hermeticity = reader.int32();
                        break;
                    }
                case 14: {
                        message.disabled = reader.bool();
                        break;
                    }
                case 6: {
                        if (!(message.queries && message.queries.length))
                            message.queries = [];
                        message.queries.push(reader.string());
                        break;
                    }
                case 15: {
                        message.jitCode = reader.string();
                        break;
                    }
                case 8: {
                        message.hasOutput = reader.bool();
                        break;
                    }
                case 9: {
                        if (!(message.tags && message.tags.length))
                            message.tags = [];
                        message.tags.push(reader.string());
                        break;
                    }
                case 10: {
                        message.actionDescriptor = $root.dataform.ActionDescriptor.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 7: {
                        message.fileName = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an Operation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.Operation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.Operation} Operation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Operation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Operation message.
         * @function verify
         * @memberof dataform.Operation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Operation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.target != null && Object.hasOwnProperty.call(message, "target")) {
                let error = $root.dataform.Target.verify(message.target, long + 1);
                if (error)
                    return "target." + error;
            }
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget")) {
                let error = $root.dataform.Target.verify(message.canonicalTarget, long + 1);
                if (error)
                    return "canonicalTarget." + error;
            }
            if (message.dependencyTargets != null && Object.hasOwnProperty.call(message, "dependencyTargets")) {
                if (!Array.isArray(message.dependencyTargets))
                    return "dependencyTargets: array expected";
                for (let i = 0; i < message.dependencyTargets.length; ++i) {
                    let error = $root.dataform.Target.verify(message.dependencyTargets[i], long + 1);
                    if (error)
                        return "dependencyTargets." + error;
                }
            }
            if (message.hermeticity != null && Object.hasOwnProperty.call(message, "hermeticity"))
                switch (message.hermeticity) {
                default:
                    return "hermeticity: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                if (typeof message.disabled !== "boolean")
                    return "disabled: boolean expected";
            if (message.queries != null && Object.hasOwnProperty.call(message, "queries")) {
                if (!Array.isArray(message.queries))
                    return "queries: array expected";
                for (let i = 0; i < message.queries.length; ++i)
                    if (!$util.isString(message.queries[i]))
                        return "queries: string[] expected";
            }
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                if (!$util.isString(message.jitCode))
                    return "jitCode: string expected";
            if (message.hasOutput != null && Object.hasOwnProperty.call(message, "hasOutput"))
                if (typeof message.hasOutput !== "boolean")
                    return "hasOutput: boolean expected";
            if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                if (!Array.isArray(message.tags))
                    return "tags: array expected";
                for (let i = 0; i < message.tags.length; ++i)
                    if (!$util.isString(message.tags[i]))
                        return "tags: string[] expected";
            }
            if (message.actionDescriptor != null && Object.hasOwnProperty.call(message, "actionDescriptor")) {
                let error = $root.dataform.ActionDescriptor.verify(message.actionDescriptor, long + 1);
                if (error)
                    return "actionDescriptor." + error;
            }
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                if (!$util.isString(message.fileName))
                    return "fileName: string expected";
            return null;
        };

        /**
         * Creates an Operation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.Operation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.Operation} Operation
         */
        Operation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.Operation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.Operation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.Operation();
            if (object.target != null) {
                if (!$util.isObject(object.target))
                    throw TypeError(".dataform.Operation.target: object expected");
                message.target = $root.dataform.Target.fromObject(object.target, long + 1);
            }
            if (object.canonicalTarget != null) {
                if (!$util.isObject(object.canonicalTarget))
                    throw TypeError(".dataform.Operation.canonicalTarget: object expected");
                message.canonicalTarget = $root.dataform.Target.fromObject(object.canonicalTarget, long + 1);
            }
            if (object.dependencyTargets) {
                if (!Array.isArray(object.dependencyTargets))
                    throw TypeError(".dataform.Operation.dependencyTargets: array expected");
                message.dependencyTargets = [];
                for (let i = 0; i < object.dependencyTargets.length; ++i) {
                    if (!$util.isObject(object.dependencyTargets[i]))
                        throw TypeError(".dataform.Operation.dependencyTargets: object expected");
                    message.dependencyTargets[i] = $root.dataform.Target.fromObject(object.dependencyTargets[i], long + 1);
                }
            }
            switch (object.hermeticity) {
            default:
                if (typeof object.hermeticity === "number") {
                    message.hermeticity = object.hermeticity;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                message.hermeticity = 0;
                break;
            case "HERMETIC":
            case 1:
                message.hermeticity = 1;
                break;
            case "NON_HERMETIC":
            case 2:
                message.hermeticity = 2;
                break;
            }
            if (object.disabled != null)
                message.disabled = Boolean(object.disabled);
            if (object.queries) {
                if (!Array.isArray(object.queries))
                    throw TypeError(".dataform.Operation.queries: array expected");
                message.queries = [];
                for (let i = 0; i < object.queries.length; ++i)
                    message.queries[i] = String(object.queries[i]);
            }
            if (object.jitCode != null)
                message.jitCode = String(object.jitCode);
            if (object.hasOutput != null)
                message.hasOutput = Boolean(object.hasOutput);
            if (object.tags) {
                if (!Array.isArray(object.tags))
                    throw TypeError(".dataform.Operation.tags: array expected");
                message.tags = [];
                for (let i = 0; i < object.tags.length; ++i)
                    message.tags[i] = String(object.tags[i]);
            }
            if (object.actionDescriptor != null) {
                if (!$util.isObject(object.actionDescriptor))
                    throw TypeError(".dataform.Operation.actionDescriptor: object expected");
                message.actionDescriptor = $root.dataform.ActionDescriptor.fromObject(object.actionDescriptor, long + 1);
            }
            if (object.fileName != null)
                message.fileName = String(object.fileName);
            return message;
        };

        /**
         * Creates a plain object from an Operation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.Operation
         * @static
         * @param {dataform.Operation} message Operation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Operation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.queries = [];
                object.tags = [];
                object.dependencyTargets = [];
            }
            if (options.defaults) {
                object.target = null;
                object.fileName = "";
                object.hasOutput = false;
                object.actionDescriptor = null;
                object.hermeticity = options.enums === String ? "UNKNOWN" : 0;
                object.canonicalTarget = null;
                object.disabled = false;
                object.jitCode = "";
            }
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                object.target = $root.dataform.Target.toObject(message.target, options, q + 1);
            if (message.queries && message.queries.length) {
                object.queries = [];
                for (let j = 0; j < message.queries.length; ++j)
                    object.queries[j] = message.queries[j];
            }
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                object.fileName = message.fileName;
            if (message.hasOutput != null && Object.hasOwnProperty.call(message, "hasOutput"))
                object.hasOutput = message.hasOutput;
            if (message.tags && message.tags.length) {
                object.tags = [];
                for (let j = 0; j < message.tags.length; ++j)
                    object.tags[j] = message.tags[j];
            }
            if (message.actionDescriptor != null && Object.hasOwnProperty.call(message, "actionDescriptor"))
                object.actionDescriptor = $root.dataform.ActionDescriptor.toObject(message.actionDescriptor, options, q + 1);
            if (message.dependencyTargets && message.dependencyTargets.length) {
                object.dependencyTargets = [];
                for (let j = 0; j < message.dependencyTargets.length; ++j)
                    object.dependencyTargets[j] = $root.dataform.Target.toObject(message.dependencyTargets[j], options, q + 1);
            }
            if (message.hermeticity != null && Object.hasOwnProperty.call(message, "hermeticity"))
                object.hermeticity = options.enums === String ? $root.dataform.ActionHermeticity[message.hermeticity] === undefined ? message.hermeticity : $root.dataform.ActionHermeticity[message.hermeticity] : message.hermeticity;
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget"))
                object.canonicalTarget = $root.dataform.Target.toObject(message.canonicalTarget, options, q + 1);
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                object.disabled = message.disabled;
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                object.jitCode = message.jitCode;
            return object;
        };

        /**
         * Converts this Operation to JSON.
         * @function toJSON
         * @memberof dataform.Operation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Operation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Operation
         * @function getTypeUrl
         * @memberof dataform.Operation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Operation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.Operation";
        };

        return Operation;
    })();

    dataform.Assertion = (function() {

        /**
         * Properties of an Assertion.
         * @memberof dataform
         * @interface IAssertion
         * @property {dataform.ITarget|null} [target] Assertion target
         * @property {dataform.ITarget|null} [canonicalTarget] Assertion canonicalTarget
         * @property {Array.<dataform.ITarget>|null} [dependencyTargets] Assertion dependencyTargets
         * @property {dataform.ActionHermeticity|null} [hermeticity] Assertion hermeticity
         * @property {boolean|null} [disabled] Assertion disabled
         * @property {string|null} [query] Assertion query
         * @property {Array.<string>|null} [tags] Assertion tags
         * @property {dataform.IActionDescriptor|null} [actionDescriptor] Assertion actionDescriptor
         * @property {dataform.ITarget|null} [parentAction] Assertion parentAction
         * @property {string|null} [jitCode] Assertion jitCode
         * @property {string|null} [fileName] Assertion fileName
         */

        /**
         * Constructs a new Assertion.
         * @memberof dataform
         * @classdesc Represents an Assertion.
         * @implements IAssertion
         * @constructor
         * @param {dataform.IAssertion=} [properties] Properties to set
         */
        function Assertion(properties) {
            this.dependencyTargets = [];
            this.tags = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Assertion target.
         * @member {dataform.ITarget|null|undefined} target
         * @memberof dataform.Assertion
         * @instance
         */
        Assertion.prototype.target = null;

        /**
         * Assertion canonicalTarget.
         * @member {dataform.ITarget|null|undefined} canonicalTarget
         * @memberof dataform.Assertion
         * @instance
         */
        Assertion.prototype.canonicalTarget = null;

        /**
         * Assertion dependencyTargets.
         * @member {Array.<dataform.ITarget>} dependencyTargets
         * @memberof dataform.Assertion
         * @instance
         */
        Assertion.prototype.dependencyTargets = $util.emptyArray;

        /**
         * Assertion hermeticity.
         * @member {dataform.ActionHermeticity} hermeticity
         * @memberof dataform.Assertion
         * @instance
         */
        Assertion.prototype.hermeticity = 0;

        /**
         * Assertion disabled.
         * @member {boolean} disabled
         * @memberof dataform.Assertion
         * @instance
         */
        Assertion.prototype.disabled = false;

        /**
         * Assertion query.
         * @member {string} query
         * @memberof dataform.Assertion
         * @instance
         */
        Assertion.prototype.query = "";

        /**
         * Assertion tags.
         * @member {Array.<string>} tags
         * @memberof dataform.Assertion
         * @instance
         */
        Assertion.prototype.tags = $util.emptyArray;

        /**
         * Assertion actionDescriptor.
         * @member {dataform.IActionDescriptor|null|undefined} actionDescriptor
         * @memberof dataform.Assertion
         * @instance
         */
        Assertion.prototype.actionDescriptor = null;

        /**
         * Assertion parentAction.
         * @member {dataform.ITarget|null|undefined} parentAction
         * @memberof dataform.Assertion
         * @instance
         */
        Assertion.prototype.parentAction = null;

        /**
         * Assertion jitCode.
         * @member {string} jitCode
         * @memberof dataform.Assertion
         * @instance
         */
        Assertion.prototype.jitCode = "";

        /**
         * Assertion fileName.
         * @member {string} fileName
         * @memberof dataform.Assertion
         * @instance
         */
        Assertion.prototype.fileName = "";

        /**
         * Creates a new Assertion instance using the specified properties.
         * @function create
         * @memberof dataform.Assertion
         * @static
         * @param {dataform.IAssertion=} [properties] Properties to set
         * @returns {dataform.Assertion} Assertion instance
         */
        Assertion.create = function create(properties) {
            return new Assertion(properties);
        };

        /**
         * Encodes the specified Assertion message. Does not implicitly {@link dataform.Assertion.verify|verify} messages.
         * @function encode
         * @memberof dataform.Assertion
         * @static
         * @param {dataform.IAssertion} message Assertion message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Assertion.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.query != null && Object.hasOwnProperty.call(message, "query"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.query);
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.fileName);
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                $root.dataform.Target.encode(message.target, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
            if (message.tags != null && message.tags.length)
                for (let i = 0; i < message.tags.length; ++i)
                    writer.uint32(/* id 9, wireType 2 =*/74).string(message.tags[i]);
            if (message.actionDescriptor != null && Object.hasOwnProperty.call(message, "actionDescriptor"))
                $root.dataform.ActionDescriptor.encode(message.actionDescriptor, writer.uint32(/* id 10, wireType 2 =*/82).fork(), q + 1).ldelim();
            if (message.dependencyTargets != null && message.dependencyTargets.length)
                for (let i = 0; i < message.dependencyTargets.length; ++i)
                    $root.dataform.Target.encode(message.dependencyTargets[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
            if (message.hermeticity != null && Object.hasOwnProperty.call(message, "hermeticity"))
                writer.uint32(/* id 12, wireType 0 =*/96).int32(message.hermeticity);
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget"))
                $root.dataform.Target.encode(message.canonicalTarget, writer.uint32(/* id 13, wireType 2 =*/106).fork(), q + 1).ldelim();
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                writer.uint32(/* id 14, wireType 0 =*/112).bool(message.disabled);
            if (message.parentAction != null && Object.hasOwnProperty.call(message, "parentAction"))
                $root.dataform.Target.encode(message.parentAction, writer.uint32(/* id 15, wireType 2 =*/122).fork(), q + 1).ldelim();
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                writer.uint32(/* id 16, wireType 2 =*/130).string(message.jitCode);
            return writer;
        };

        /**
         * Encodes the specified Assertion message, length delimited. Does not implicitly {@link dataform.Assertion.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.Assertion
         * @static
         * @param {dataform.IAssertion} message Assertion message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Assertion.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an Assertion message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.Assertion
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.Assertion} Assertion
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Assertion.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.Assertion();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 8: {
                        message.target = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 13: {
                        message.canonicalTarget = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 11: {
                        if (!(message.dependencyTargets && message.dependencyTargets.length))
                            message.dependencyTargets = [];
                        message.dependencyTargets.push($root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 12: {
                        message.hermeticity = reader.int32();
                        break;
                    }
                case 14: {
                        message.disabled = reader.bool();
                        break;
                    }
                case 3: {
                        message.query = reader.string();
                        break;
                    }
                case 9: {
                        if (!(message.tags && message.tags.length))
                            message.tags = [];
                        message.tags.push(reader.string());
                        break;
                    }
                case 10: {
                        message.actionDescriptor = $root.dataform.ActionDescriptor.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 15: {
                        message.parentAction = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 16: {
                        message.jitCode = reader.string();
                        break;
                    }
                case 7: {
                        message.fileName = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an Assertion message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.Assertion
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.Assertion} Assertion
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Assertion.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an Assertion message.
         * @function verify
         * @memberof dataform.Assertion
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Assertion.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.target != null && Object.hasOwnProperty.call(message, "target")) {
                let error = $root.dataform.Target.verify(message.target, long + 1);
                if (error)
                    return "target." + error;
            }
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget")) {
                let error = $root.dataform.Target.verify(message.canonicalTarget, long + 1);
                if (error)
                    return "canonicalTarget." + error;
            }
            if (message.dependencyTargets != null && Object.hasOwnProperty.call(message, "dependencyTargets")) {
                if (!Array.isArray(message.dependencyTargets))
                    return "dependencyTargets: array expected";
                for (let i = 0; i < message.dependencyTargets.length; ++i) {
                    let error = $root.dataform.Target.verify(message.dependencyTargets[i], long + 1);
                    if (error)
                        return "dependencyTargets." + error;
                }
            }
            if (message.hermeticity != null && Object.hasOwnProperty.call(message, "hermeticity"))
                switch (message.hermeticity) {
                default:
                    return "hermeticity: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                if (typeof message.disabled !== "boolean")
                    return "disabled: boolean expected";
            if (message.query != null && Object.hasOwnProperty.call(message, "query"))
                if (!$util.isString(message.query))
                    return "query: string expected";
            if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                if (!Array.isArray(message.tags))
                    return "tags: array expected";
                for (let i = 0; i < message.tags.length; ++i)
                    if (!$util.isString(message.tags[i]))
                        return "tags: string[] expected";
            }
            if (message.actionDescriptor != null && Object.hasOwnProperty.call(message, "actionDescriptor")) {
                let error = $root.dataform.ActionDescriptor.verify(message.actionDescriptor, long + 1);
                if (error)
                    return "actionDescriptor." + error;
            }
            if (message.parentAction != null && Object.hasOwnProperty.call(message, "parentAction")) {
                let error = $root.dataform.Target.verify(message.parentAction, long + 1);
                if (error)
                    return "parentAction." + error;
            }
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                if (!$util.isString(message.jitCode))
                    return "jitCode: string expected";
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                if (!$util.isString(message.fileName))
                    return "fileName: string expected";
            return null;
        };

        /**
         * Creates an Assertion message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.Assertion
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.Assertion} Assertion
         */
        Assertion.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.Assertion)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.Assertion: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.Assertion();
            if (object.target != null) {
                if (!$util.isObject(object.target))
                    throw TypeError(".dataform.Assertion.target: object expected");
                message.target = $root.dataform.Target.fromObject(object.target, long + 1);
            }
            if (object.canonicalTarget != null) {
                if (!$util.isObject(object.canonicalTarget))
                    throw TypeError(".dataform.Assertion.canonicalTarget: object expected");
                message.canonicalTarget = $root.dataform.Target.fromObject(object.canonicalTarget, long + 1);
            }
            if (object.dependencyTargets) {
                if (!Array.isArray(object.dependencyTargets))
                    throw TypeError(".dataform.Assertion.dependencyTargets: array expected");
                message.dependencyTargets = [];
                for (let i = 0; i < object.dependencyTargets.length; ++i) {
                    if (!$util.isObject(object.dependencyTargets[i]))
                        throw TypeError(".dataform.Assertion.dependencyTargets: object expected");
                    message.dependencyTargets[i] = $root.dataform.Target.fromObject(object.dependencyTargets[i], long + 1);
                }
            }
            switch (object.hermeticity) {
            default:
                if (typeof object.hermeticity === "number") {
                    message.hermeticity = object.hermeticity;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                message.hermeticity = 0;
                break;
            case "HERMETIC":
            case 1:
                message.hermeticity = 1;
                break;
            case "NON_HERMETIC":
            case 2:
                message.hermeticity = 2;
                break;
            }
            if (object.disabled != null)
                message.disabled = Boolean(object.disabled);
            if (object.query != null)
                message.query = String(object.query);
            if (object.tags) {
                if (!Array.isArray(object.tags))
                    throw TypeError(".dataform.Assertion.tags: array expected");
                message.tags = [];
                for (let i = 0; i < object.tags.length; ++i)
                    message.tags[i] = String(object.tags[i]);
            }
            if (object.actionDescriptor != null) {
                if (!$util.isObject(object.actionDescriptor))
                    throw TypeError(".dataform.Assertion.actionDescriptor: object expected");
                message.actionDescriptor = $root.dataform.ActionDescriptor.fromObject(object.actionDescriptor, long + 1);
            }
            if (object.parentAction != null) {
                if (!$util.isObject(object.parentAction))
                    throw TypeError(".dataform.Assertion.parentAction: object expected");
                message.parentAction = $root.dataform.Target.fromObject(object.parentAction, long + 1);
            }
            if (object.jitCode != null)
                message.jitCode = String(object.jitCode);
            if (object.fileName != null)
                message.fileName = String(object.fileName);
            return message;
        };

        /**
         * Creates a plain object from an Assertion message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.Assertion
         * @static
         * @param {dataform.Assertion} message Assertion
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Assertion.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.tags = [];
                object.dependencyTargets = [];
            }
            if (options.defaults) {
                object.query = "";
                object.fileName = "";
                object.target = null;
                object.actionDescriptor = null;
                object.hermeticity = options.enums === String ? "UNKNOWN" : 0;
                object.canonicalTarget = null;
                object.disabled = false;
                object.parentAction = null;
                object.jitCode = "";
            }
            if (message.query != null && Object.hasOwnProperty.call(message, "query"))
                object.query = message.query;
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                object.fileName = message.fileName;
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                object.target = $root.dataform.Target.toObject(message.target, options, q + 1);
            if (message.tags && message.tags.length) {
                object.tags = [];
                for (let j = 0; j < message.tags.length; ++j)
                    object.tags[j] = message.tags[j];
            }
            if (message.actionDescriptor != null && Object.hasOwnProperty.call(message, "actionDescriptor"))
                object.actionDescriptor = $root.dataform.ActionDescriptor.toObject(message.actionDescriptor, options, q + 1);
            if (message.dependencyTargets && message.dependencyTargets.length) {
                object.dependencyTargets = [];
                for (let j = 0; j < message.dependencyTargets.length; ++j)
                    object.dependencyTargets[j] = $root.dataform.Target.toObject(message.dependencyTargets[j], options, q + 1);
            }
            if (message.hermeticity != null && Object.hasOwnProperty.call(message, "hermeticity"))
                object.hermeticity = options.enums === String ? $root.dataform.ActionHermeticity[message.hermeticity] === undefined ? message.hermeticity : $root.dataform.ActionHermeticity[message.hermeticity] : message.hermeticity;
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget"))
                object.canonicalTarget = $root.dataform.Target.toObject(message.canonicalTarget, options, q + 1);
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                object.disabled = message.disabled;
            if (message.parentAction != null && Object.hasOwnProperty.call(message, "parentAction"))
                object.parentAction = $root.dataform.Target.toObject(message.parentAction, options, q + 1);
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                object.jitCode = message.jitCode;
            return object;
        };

        /**
         * Converts this Assertion to JSON.
         * @function toJSON
         * @memberof dataform.Assertion
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Assertion.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Assertion
         * @function getTypeUrl
         * @memberof dataform.Assertion
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Assertion.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.Assertion";
        };

        return Assertion;
    })();

    /**
     * ActionHermeticity enum.
     * @name dataform.ActionHermeticity
     * @enum {number}
     * @property {number} UNKNOWN=0 UNKNOWN value
     * @property {number} HERMETIC=1 HERMETIC value
     * @property {number} NON_HERMETIC=2 NON_HERMETIC value
     */
    dataform.ActionHermeticity = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN"] = 0;
        values[valuesById[1] = "HERMETIC"] = 1;
        values[valuesById[2] = "NON_HERMETIC"] = 2;
        return values;
    })();

    dataform.Declaration = (function() {

        /**
         * Properties of a Declaration.
         * @memberof dataform
         * @interface IDeclaration
         * @property {dataform.ITarget|null} [target] Declaration target
         * @property {dataform.ITarget|null} [canonicalTarget] Declaration canonicalTarget
         * @property {dataform.IActionDescriptor|null} [actionDescriptor] Declaration actionDescriptor
         * @property {string|null} [fileName] Declaration fileName
         * @property {Array.<string>|null} [tags] Declaration tags
         */

        /**
         * Constructs a new Declaration.
         * @memberof dataform
         * @classdesc Represents a Declaration.
         * @implements IDeclaration
         * @constructor
         * @param {dataform.IDeclaration=} [properties] Properties to set
         */
        function Declaration(properties) {
            this.tags = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Declaration target.
         * @member {dataform.ITarget|null|undefined} target
         * @memberof dataform.Declaration
         * @instance
         */
        Declaration.prototype.target = null;

        /**
         * Declaration canonicalTarget.
         * @member {dataform.ITarget|null|undefined} canonicalTarget
         * @memberof dataform.Declaration
         * @instance
         */
        Declaration.prototype.canonicalTarget = null;

        /**
         * Declaration actionDescriptor.
         * @member {dataform.IActionDescriptor|null|undefined} actionDescriptor
         * @memberof dataform.Declaration
         * @instance
         */
        Declaration.prototype.actionDescriptor = null;

        /**
         * Declaration fileName.
         * @member {string} fileName
         * @memberof dataform.Declaration
         * @instance
         */
        Declaration.prototype.fileName = "";

        /**
         * Declaration tags.
         * @member {Array.<string>} tags
         * @memberof dataform.Declaration
         * @instance
         */
        Declaration.prototype.tags = $util.emptyArray;

        /**
         * Creates a new Declaration instance using the specified properties.
         * @function create
         * @memberof dataform.Declaration
         * @static
         * @param {dataform.IDeclaration=} [properties] Properties to set
         * @returns {dataform.Declaration} Declaration instance
         */
        Declaration.create = function create(properties) {
            return new Declaration(properties);
        };

        /**
         * Encodes the specified Declaration message. Does not implicitly {@link dataform.Declaration.verify|verify} messages.
         * @function encode
         * @memberof dataform.Declaration
         * @static
         * @param {dataform.IDeclaration} message Declaration message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Declaration.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                $root.dataform.Target.encode(message.target, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.actionDescriptor != null && Object.hasOwnProperty.call(message, "actionDescriptor"))
                $root.dataform.ActionDescriptor.encode(message.actionDescriptor, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.fileName);
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget"))
                $root.dataform.Target.encode(message.canonicalTarget, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.tags != null && message.tags.length)
                for (let i = 0; i < message.tags.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.tags[i]);
            return writer;
        };

        /**
         * Encodes the specified Declaration message, length delimited. Does not implicitly {@link dataform.Declaration.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.Declaration
         * @static
         * @param {dataform.IDeclaration} message Declaration message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Declaration.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Declaration message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.Declaration
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.Declaration} Declaration
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Declaration.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.Declaration();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 2: {
                        message.target = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 5: {
                        message.canonicalTarget = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 3: {
                        message.actionDescriptor = $root.dataform.ActionDescriptor.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 4: {
                        message.fileName = reader.string();
                        break;
                    }
                case 6: {
                        if (!(message.tags && message.tags.length))
                            message.tags = [];
                        message.tags.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Declaration message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.Declaration
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.Declaration} Declaration
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Declaration.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Declaration message.
         * @function verify
         * @memberof dataform.Declaration
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Declaration.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.target != null && Object.hasOwnProperty.call(message, "target")) {
                let error = $root.dataform.Target.verify(message.target, long + 1);
                if (error)
                    return "target." + error;
            }
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget")) {
                let error = $root.dataform.Target.verify(message.canonicalTarget, long + 1);
                if (error)
                    return "canonicalTarget." + error;
            }
            if (message.actionDescriptor != null && Object.hasOwnProperty.call(message, "actionDescriptor")) {
                let error = $root.dataform.ActionDescriptor.verify(message.actionDescriptor, long + 1);
                if (error)
                    return "actionDescriptor." + error;
            }
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                if (!$util.isString(message.fileName))
                    return "fileName: string expected";
            if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                if (!Array.isArray(message.tags))
                    return "tags: array expected";
                for (let i = 0; i < message.tags.length; ++i)
                    if (!$util.isString(message.tags[i]))
                        return "tags: string[] expected";
            }
            return null;
        };

        /**
         * Creates a Declaration message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.Declaration
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.Declaration} Declaration
         */
        Declaration.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.Declaration)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.Declaration: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.Declaration();
            if (object.target != null) {
                if (!$util.isObject(object.target))
                    throw TypeError(".dataform.Declaration.target: object expected");
                message.target = $root.dataform.Target.fromObject(object.target, long + 1);
            }
            if (object.canonicalTarget != null) {
                if (!$util.isObject(object.canonicalTarget))
                    throw TypeError(".dataform.Declaration.canonicalTarget: object expected");
                message.canonicalTarget = $root.dataform.Target.fromObject(object.canonicalTarget, long + 1);
            }
            if (object.actionDescriptor != null) {
                if (!$util.isObject(object.actionDescriptor))
                    throw TypeError(".dataform.Declaration.actionDescriptor: object expected");
                message.actionDescriptor = $root.dataform.ActionDescriptor.fromObject(object.actionDescriptor, long + 1);
            }
            if (object.fileName != null)
                message.fileName = String(object.fileName);
            if (object.tags) {
                if (!Array.isArray(object.tags))
                    throw TypeError(".dataform.Declaration.tags: array expected");
                message.tags = [];
                for (let i = 0; i < object.tags.length; ++i)
                    message.tags[i] = String(object.tags[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a Declaration message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.Declaration
         * @static
         * @param {dataform.Declaration} message Declaration
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Declaration.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.tags = [];
            if (options.defaults) {
                object.target = null;
                object.actionDescriptor = null;
                object.fileName = "";
                object.canonicalTarget = null;
            }
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                object.target = $root.dataform.Target.toObject(message.target, options, q + 1);
            if (message.actionDescriptor != null && Object.hasOwnProperty.call(message, "actionDescriptor"))
                object.actionDescriptor = $root.dataform.ActionDescriptor.toObject(message.actionDescriptor, options, q + 1);
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                object.fileName = message.fileName;
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget"))
                object.canonicalTarget = $root.dataform.Target.toObject(message.canonicalTarget, options, q + 1);
            if (message.tags && message.tags.length) {
                object.tags = [];
                for (let j = 0; j < message.tags.length; ++j)
                    object.tags[j] = message.tags[j];
            }
            return object;
        };

        /**
         * Converts this Declaration to JSON.
         * @function toJSON
         * @memberof dataform.Declaration
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Declaration.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Declaration
         * @function getTypeUrl
         * @memberof dataform.Declaration
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Declaration.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.Declaration";
        };

        return Declaration;
    })();

    dataform.Test = (function() {

        /**
         * Properties of a Test.
         * @memberof dataform
         * @interface ITest
         * @property {string|null} [name] Test name
         * @property {string|null} [testQuery] Test testQuery
         * @property {string|null} [expectedOutputQuery] Test expectedOutputQuery
         * @property {string|null} [jitCode] Test jitCode
         * @property {string|null} [fileName] Test fileName
         * @property {boolean|null} [disabled] Test disabled
         * @property {Array.<string>|null} [tags] Test tags
         * @property {dataform.ITarget|null} [target] Test target
         * @property {dataform.ITarget|null} [canonicalTarget] Test canonicalTarget
         * @property {Array.<dataform.ITarget>|null} [dependencyTargets] Test dependencyTargets
         */

        /**
         * Constructs a new Test.
         * @memberof dataform
         * @classdesc Represents a Test.
         * @implements ITest
         * @constructor
         * @param {dataform.ITest=} [properties] Properties to set
         */
        function Test(properties) {
            this.tags = [];
            this.dependencyTargets = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Test name.
         * @member {string} name
         * @memberof dataform.Test
         * @instance
         */
        Test.prototype.name = "";

        /**
         * Test testQuery.
         * @member {string} testQuery
         * @memberof dataform.Test
         * @instance
         */
        Test.prototype.testQuery = "";

        /**
         * Test expectedOutputQuery.
         * @member {string} expectedOutputQuery
         * @memberof dataform.Test
         * @instance
         */
        Test.prototype.expectedOutputQuery = "";

        /**
         * Test jitCode.
         * @member {string} jitCode
         * @memberof dataform.Test
         * @instance
         */
        Test.prototype.jitCode = "";

        /**
         * Test fileName.
         * @member {string} fileName
         * @memberof dataform.Test
         * @instance
         */
        Test.prototype.fileName = "";

        /**
         * Test disabled.
         * @member {boolean} disabled
         * @memberof dataform.Test
         * @instance
         */
        Test.prototype.disabled = false;

        /**
         * Test tags.
         * @member {Array.<string>} tags
         * @memberof dataform.Test
         * @instance
         */
        Test.prototype.tags = $util.emptyArray;

        /**
         * Test target.
         * @member {dataform.ITarget|null|undefined} target
         * @memberof dataform.Test
         * @instance
         */
        Test.prototype.target = null;

        /**
         * Test canonicalTarget.
         * @member {dataform.ITarget|null|undefined} canonicalTarget
         * @memberof dataform.Test
         * @instance
         */
        Test.prototype.canonicalTarget = null;

        /**
         * Test dependencyTargets.
         * @member {Array.<dataform.ITarget>} dependencyTargets
         * @memberof dataform.Test
         * @instance
         */
        Test.prototype.dependencyTargets = $util.emptyArray;

        /**
         * Creates a new Test instance using the specified properties.
         * @function create
         * @memberof dataform.Test
         * @static
         * @param {dataform.ITest=} [properties] Properties to set
         * @returns {dataform.Test} Test instance
         */
        Test.create = function create(properties) {
            return new Test(properties);
        };

        /**
         * Encodes the specified Test message. Does not implicitly {@link dataform.Test.verify|verify} messages.
         * @function encode
         * @memberof dataform.Test
         * @static
         * @param {dataform.ITest} message Test message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Test.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.testQuery != null && Object.hasOwnProperty.call(message, "testQuery"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.testQuery);
            if (message.expectedOutputQuery != null && Object.hasOwnProperty.call(message, "expectedOutputQuery"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.expectedOutputQuery);
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.fileName);
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                writer.uint32(/* id 5, wireType 0 =*/40).bool(message.disabled);
            if (message.tags != null && message.tags.length)
                for (let i = 0; i < message.tags.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.tags[i]);
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                $root.dataform.Target.encode(message.target, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget"))
                $root.dataform.Target.encode(message.canonicalTarget, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
            if (message.dependencyTargets != null && message.dependencyTargets.length)
                for (let i = 0; i < message.dependencyTargets.length; ++i)
                    $root.dataform.Target.encode(message.dependencyTargets[i], writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.jitCode);
            return writer;
        };

        /**
         * Encodes the specified Test message, length delimited. Does not implicitly {@link dataform.Test.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.Test
         * @static
         * @param {dataform.ITest} message Test message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Test.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Test message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.Test
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.Test} Test
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Test.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.Test();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 2: {
                        message.testQuery = reader.string();
                        break;
                    }
                case 3: {
                        message.expectedOutputQuery = reader.string();
                        break;
                    }
                case 10: {
                        message.jitCode = reader.string();
                        break;
                    }
                case 4: {
                        message.fileName = reader.string();
                        break;
                    }
                case 5: {
                        message.disabled = reader.bool();
                        break;
                    }
                case 6: {
                        if (!(message.tags && message.tags.length))
                            message.tags = [];
                        message.tags.push(reader.string());
                        break;
                    }
                case 7: {
                        message.target = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 8: {
                        message.canonicalTarget = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 9: {
                        if (!(message.dependencyTargets && message.dependencyTargets.length))
                            message.dependencyTargets = [];
                        message.dependencyTargets.push($root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Test message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.Test
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.Test} Test
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Test.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Test message.
         * @function verify
         * @memberof dataform.Test
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Test.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.testQuery != null && Object.hasOwnProperty.call(message, "testQuery"))
                if (!$util.isString(message.testQuery))
                    return "testQuery: string expected";
            if (message.expectedOutputQuery != null && Object.hasOwnProperty.call(message, "expectedOutputQuery"))
                if (!$util.isString(message.expectedOutputQuery))
                    return "expectedOutputQuery: string expected";
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                if (!$util.isString(message.jitCode))
                    return "jitCode: string expected";
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                if (!$util.isString(message.fileName))
                    return "fileName: string expected";
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                if (typeof message.disabled !== "boolean")
                    return "disabled: boolean expected";
            if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                if (!Array.isArray(message.tags))
                    return "tags: array expected";
                for (let i = 0; i < message.tags.length; ++i)
                    if (!$util.isString(message.tags[i]))
                        return "tags: string[] expected";
            }
            if (message.target != null && Object.hasOwnProperty.call(message, "target")) {
                let error = $root.dataform.Target.verify(message.target, long + 1);
                if (error)
                    return "target." + error;
            }
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget")) {
                let error = $root.dataform.Target.verify(message.canonicalTarget, long + 1);
                if (error)
                    return "canonicalTarget." + error;
            }
            if (message.dependencyTargets != null && Object.hasOwnProperty.call(message, "dependencyTargets")) {
                if (!Array.isArray(message.dependencyTargets))
                    return "dependencyTargets: array expected";
                for (let i = 0; i < message.dependencyTargets.length; ++i) {
                    let error = $root.dataform.Target.verify(message.dependencyTargets[i], long + 1);
                    if (error)
                        return "dependencyTargets." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Test message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.Test
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.Test} Test
         */
        Test.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.Test)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.Test: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.Test();
            if (object.name != null)
                message.name = String(object.name);
            if (object.testQuery != null)
                message.testQuery = String(object.testQuery);
            if (object.expectedOutputQuery != null)
                message.expectedOutputQuery = String(object.expectedOutputQuery);
            if (object.jitCode != null)
                message.jitCode = String(object.jitCode);
            if (object.fileName != null)
                message.fileName = String(object.fileName);
            if (object.disabled != null)
                message.disabled = Boolean(object.disabled);
            if (object.tags) {
                if (!Array.isArray(object.tags))
                    throw TypeError(".dataform.Test.tags: array expected");
                message.tags = [];
                for (let i = 0; i < object.tags.length; ++i)
                    message.tags[i] = String(object.tags[i]);
            }
            if (object.target != null) {
                if (!$util.isObject(object.target))
                    throw TypeError(".dataform.Test.target: object expected");
                message.target = $root.dataform.Target.fromObject(object.target, long + 1);
            }
            if (object.canonicalTarget != null) {
                if (!$util.isObject(object.canonicalTarget))
                    throw TypeError(".dataform.Test.canonicalTarget: object expected");
                message.canonicalTarget = $root.dataform.Target.fromObject(object.canonicalTarget, long + 1);
            }
            if (object.dependencyTargets) {
                if (!Array.isArray(object.dependencyTargets))
                    throw TypeError(".dataform.Test.dependencyTargets: array expected");
                message.dependencyTargets = [];
                for (let i = 0; i < object.dependencyTargets.length; ++i) {
                    if (!$util.isObject(object.dependencyTargets[i]))
                        throw TypeError(".dataform.Test.dependencyTargets: object expected");
                    message.dependencyTargets[i] = $root.dataform.Target.fromObject(object.dependencyTargets[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Test message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.Test
         * @static
         * @param {dataform.Test} message Test
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Test.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.tags = [];
                object.dependencyTargets = [];
            }
            if (options.defaults) {
                object.name = "";
                object.testQuery = "";
                object.expectedOutputQuery = "";
                object.fileName = "";
                object.disabled = false;
                object.target = null;
                object.canonicalTarget = null;
                object.jitCode = "";
            }
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.testQuery != null && Object.hasOwnProperty.call(message, "testQuery"))
                object.testQuery = message.testQuery;
            if (message.expectedOutputQuery != null && Object.hasOwnProperty.call(message, "expectedOutputQuery"))
                object.expectedOutputQuery = message.expectedOutputQuery;
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                object.fileName = message.fileName;
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                object.disabled = message.disabled;
            if (message.tags && message.tags.length) {
                object.tags = [];
                for (let j = 0; j < message.tags.length; ++j)
                    object.tags[j] = message.tags[j];
            }
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                object.target = $root.dataform.Target.toObject(message.target, options, q + 1);
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget"))
                object.canonicalTarget = $root.dataform.Target.toObject(message.canonicalTarget, options, q + 1);
            if (message.dependencyTargets && message.dependencyTargets.length) {
                object.dependencyTargets = [];
                for (let j = 0; j < message.dependencyTargets.length; ++j)
                    object.dependencyTargets[j] = $root.dataform.Target.toObject(message.dependencyTargets[j], options, q + 1);
            }
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                object.jitCode = message.jitCode;
            return object;
        };

        /**
         * Converts this Test to JSON.
         * @function toJSON
         * @memberof dataform.Test
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Test.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Test
         * @function getTypeUrl
         * @memberof dataform.Test
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Test.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.Test";
        };

        return Test;
    })();

    dataform.Notebook = (function() {

        /**
         * Properties of a Notebook.
         * @memberof dataform
         * @interface INotebook
         * @property {dataform.ITarget|null} [target] Notebook target
         * @property {dataform.ITarget|null} [canonicalTarget] Notebook canonicalTarget
         * @property {Array.<string>|null} [tags] Notebook tags
         * @property {Array.<dataform.ITarget>|null} [dependencyTargets] Notebook dependencyTargets
         * @property {string|null} [fileName] Notebook fileName
         * @property {boolean|null} [disabled] Notebook disabled
         * @property {string|null} [notebookContents] Notebook notebookContents
         */

        /**
         * Constructs a new Notebook.
         * @memberof dataform
         * @classdesc Represents a Notebook.
         * @implements INotebook
         * @constructor
         * @param {dataform.INotebook=} [properties] Properties to set
         */
        function Notebook(properties) {
            this.tags = [];
            this.dependencyTargets = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Notebook target.
         * @member {dataform.ITarget|null|undefined} target
         * @memberof dataform.Notebook
         * @instance
         */
        Notebook.prototype.target = null;

        /**
         * Notebook canonicalTarget.
         * @member {dataform.ITarget|null|undefined} canonicalTarget
         * @memberof dataform.Notebook
         * @instance
         */
        Notebook.prototype.canonicalTarget = null;

        /**
         * Notebook tags.
         * @member {Array.<string>} tags
         * @memberof dataform.Notebook
         * @instance
         */
        Notebook.prototype.tags = $util.emptyArray;

        /**
         * Notebook dependencyTargets.
         * @member {Array.<dataform.ITarget>} dependencyTargets
         * @memberof dataform.Notebook
         * @instance
         */
        Notebook.prototype.dependencyTargets = $util.emptyArray;

        /**
         * Notebook fileName.
         * @member {string} fileName
         * @memberof dataform.Notebook
         * @instance
         */
        Notebook.prototype.fileName = "";

        /**
         * Notebook disabled.
         * @member {boolean} disabled
         * @memberof dataform.Notebook
         * @instance
         */
        Notebook.prototype.disabled = false;

        /**
         * Notebook notebookContents.
         * @member {string} notebookContents
         * @memberof dataform.Notebook
         * @instance
         */
        Notebook.prototype.notebookContents = "";

        /**
         * Creates a new Notebook instance using the specified properties.
         * @function create
         * @memberof dataform.Notebook
         * @static
         * @param {dataform.INotebook=} [properties] Properties to set
         * @returns {dataform.Notebook} Notebook instance
         */
        Notebook.create = function create(properties) {
            return new Notebook(properties);
        };

        /**
         * Encodes the specified Notebook message. Does not implicitly {@link dataform.Notebook.verify|verify} messages.
         * @function encode
         * @memberof dataform.Notebook
         * @static
         * @param {dataform.INotebook} message Notebook message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Notebook.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                $root.dataform.Target.encode(message.target, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget"))
                $root.dataform.Target.encode(message.canonicalTarget, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.tags != null && message.tags.length)
                for (let i = 0; i < message.tags.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.tags[i]);
            if (message.dependencyTargets != null && message.dependencyTargets.length)
                for (let i = 0; i < message.dependencyTargets.length; ++i)
                    $root.dataform.Target.encode(message.dependencyTargets[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.fileName);
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.disabled);
            if (message.notebookContents != null && Object.hasOwnProperty.call(message, "notebookContents"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.notebookContents);
            return writer;
        };

        /**
         * Encodes the specified Notebook message, length delimited. Does not implicitly {@link dataform.Notebook.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.Notebook
         * @static
         * @param {dataform.INotebook} message Notebook message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Notebook.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Notebook message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.Notebook
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.Notebook} Notebook
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Notebook.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.Notebook();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.target = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.canonicalTarget = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 3: {
                        if (!(message.tags && message.tags.length))
                            message.tags = [];
                        message.tags.push(reader.string());
                        break;
                    }
                case 4: {
                        if (!(message.dependencyTargets && message.dependencyTargets.length))
                            message.dependencyTargets = [];
                        message.dependencyTargets.push($root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 5: {
                        message.fileName = reader.string();
                        break;
                    }
                case 6: {
                        message.disabled = reader.bool();
                        break;
                    }
                case 7: {
                        message.notebookContents = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Notebook message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.Notebook
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.Notebook} Notebook
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Notebook.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Notebook message.
         * @function verify
         * @memberof dataform.Notebook
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Notebook.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.target != null && Object.hasOwnProperty.call(message, "target")) {
                let error = $root.dataform.Target.verify(message.target, long + 1);
                if (error)
                    return "target." + error;
            }
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget")) {
                let error = $root.dataform.Target.verify(message.canonicalTarget, long + 1);
                if (error)
                    return "canonicalTarget." + error;
            }
            if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                if (!Array.isArray(message.tags))
                    return "tags: array expected";
                for (let i = 0; i < message.tags.length; ++i)
                    if (!$util.isString(message.tags[i]))
                        return "tags: string[] expected";
            }
            if (message.dependencyTargets != null && Object.hasOwnProperty.call(message, "dependencyTargets")) {
                if (!Array.isArray(message.dependencyTargets))
                    return "dependencyTargets: array expected";
                for (let i = 0; i < message.dependencyTargets.length; ++i) {
                    let error = $root.dataform.Target.verify(message.dependencyTargets[i], long + 1);
                    if (error)
                        return "dependencyTargets." + error;
                }
            }
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                if (!$util.isString(message.fileName))
                    return "fileName: string expected";
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                if (typeof message.disabled !== "boolean")
                    return "disabled: boolean expected";
            if (message.notebookContents != null && Object.hasOwnProperty.call(message, "notebookContents"))
                if (!$util.isString(message.notebookContents))
                    return "notebookContents: string expected";
            return null;
        };

        /**
         * Creates a Notebook message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.Notebook
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.Notebook} Notebook
         */
        Notebook.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.Notebook)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.Notebook: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.Notebook();
            if (object.target != null) {
                if (!$util.isObject(object.target))
                    throw TypeError(".dataform.Notebook.target: object expected");
                message.target = $root.dataform.Target.fromObject(object.target, long + 1);
            }
            if (object.canonicalTarget != null) {
                if (!$util.isObject(object.canonicalTarget))
                    throw TypeError(".dataform.Notebook.canonicalTarget: object expected");
                message.canonicalTarget = $root.dataform.Target.fromObject(object.canonicalTarget, long + 1);
            }
            if (object.tags) {
                if (!Array.isArray(object.tags))
                    throw TypeError(".dataform.Notebook.tags: array expected");
                message.tags = [];
                for (let i = 0; i < object.tags.length; ++i)
                    message.tags[i] = String(object.tags[i]);
            }
            if (object.dependencyTargets) {
                if (!Array.isArray(object.dependencyTargets))
                    throw TypeError(".dataform.Notebook.dependencyTargets: array expected");
                message.dependencyTargets = [];
                for (let i = 0; i < object.dependencyTargets.length; ++i) {
                    if (!$util.isObject(object.dependencyTargets[i]))
                        throw TypeError(".dataform.Notebook.dependencyTargets: object expected");
                    message.dependencyTargets[i] = $root.dataform.Target.fromObject(object.dependencyTargets[i], long + 1);
                }
            }
            if (object.fileName != null)
                message.fileName = String(object.fileName);
            if (object.disabled != null)
                message.disabled = Boolean(object.disabled);
            if (object.notebookContents != null)
                message.notebookContents = String(object.notebookContents);
            return message;
        };

        /**
         * Creates a plain object from a Notebook message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.Notebook
         * @static
         * @param {dataform.Notebook} message Notebook
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Notebook.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.tags = [];
                object.dependencyTargets = [];
            }
            if (options.defaults) {
                object.target = null;
                object.canonicalTarget = null;
                object.fileName = "";
                object.disabled = false;
                object.notebookContents = "";
            }
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                object.target = $root.dataform.Target.toObject(message.target, options, q + 1);
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget"))
                object.canonicalTarget = $root.dataform.Target.toObject(message.canonicalTarget, options, q + 1);
            if (message.tags && message.tags.length) {
                object.tags = [];
                for (let j = 0; j < message.tags.length; ++j)
                    object.tags[j] = message.tags[j];
            }
            if (message.dependencyTargets && message.dependencyTargets.length) {
                object.dependencyTargets = [];
                for (let j = 0; j < message.dependencyTargets.length; ++j)
                    object.dependencyTargets[j] = $root.dataform.Target.toObject(message.dependencyTargets[j], options, q + 1);
            }
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                object.fileName = message.fileName;
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                object.disabled = message.disabled;
            if (message.notebookContents != null && Object.hasOwnProperty.call(message, "notebookContents"))
                object.notebookContents = message.notebookContents;
            return object;
        };

        /**
         * Converts this Notebook to JSON.
         * @function toJSON
         * @memberof dataform.Notebook
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Notebook.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Notebook
         * @function getTypeUrl
         * @memberof dataform.Notebook
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Notebook.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.Notebook";
        };

        return Notebook;
    })();

    dataform.RepositorySnapshotDestination = (function() {

        /**
         * Properties of a RepositorySnapshotDestination.
         * @memberof dataform
         * @interface IRepositorySnapshotDestination
         * @property {string|null} [repositorySnapshotUri] RepositorySnapshotDestination repositorySnapshotUri
         */

        /**
         * Constructs a new RepositorySnapshotDestination.
         * @memberof dataform
         * @classdesc Represents a RepositorySnapshotDestination.
         * @implements IRepositorySnapshotDestination
         * @constructor
         * @param {dataform.IRepositorySnapshotDestination=} [properties] Properties to set
         */
        function RepositorySnapshotDestination(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RepositorySnapshotDestination repositorySnapshotUri.
         * @member {string} repositorySnapshotUri
         * @memberof dataform.RepositorySnapshotDestination
         * @instance
         */
        RepositorySnapshotDestination.prototype.repositorySnapshotUri = "";

        /**
         * Creates a new RepositorySnapshotDestination instance using the specified properties.
         * @function create
         * @memberof dataform.RepositorySnapshotDestination
         * @static
         * @param {dataform.IRepositorySnapshotDestination=} [properties] Properties to set
         * @returns {dataform.RepositorySnapshotDestination} RepositorySnapshotDestination instance
         */
        RepositorySnapshotDestination.create = function create(properties) {
            return new RepositorySnapshotDestination(properties);
        };

        /**
         * Encodes the specified RepositorySnapshotDestination message. Does not implicitly {@link dataform.RepositorySnapshotDestination.verify|verify} messages.
         * @function encode
         * @memberof dataform.RepositorySnapshotDestination
         * @static
         * @param {dataform.IRepositorySnapshotDestination} message RepositorySnapshotDestination message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RepositorySnapshotDestination.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.repositorySnapshotUri != null && Object.hasOwnProperty.call(message, "repositorySnapshotUri"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.repositorySnapshotUri);
            return writer;
        };

        /**
         * Encodes the specified RepositorySnapshotDestination message, length delimited. Does not implicitly {@link dataform.RepositorySnapshotDestination.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.RepositorySnapshotDestination
         * @static
         * @param {dataform.IRepositorySnapshotDestination} message RepositorySnapshotDestination message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RepositorySnapshotDestination.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RepositorySnapshotDestination message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.RepositorySnapshotDestination
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.RepositorySnapshotDestination} RepositorySnapshotDestination
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RepositorySnapshotDestination.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.RepositorySnapshotDestination();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.repositorySnapshotUri = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RepositorySnapshotDestination message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.RepositorySnapshotDestination
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.RepositorySnapshotDestination} RepositorySnapshotDestination
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RepositorySnapshotDestination.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RepositorySnapshotDestination message.
         * @function verify
         * @memberof dataform.RepositorySnapshotDestination
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RepositorySnapshotDestination.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.repositorySnapshotUri != null && Object.hasOwnProperty.call(message, "repositorySnapshotUri"))
                if (!$util.isString(message.repositorySnapshotUri))
                    return "repositorySnapshotUri: string expected";
            return null;
        };

        /**
         * Creates a RepositorySnapshotDestination message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.RepositorySnapshotDestination
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.RepositorySnapshotDestination} RepositorySnapshotDestination
         */
        RepositorySnapshotDestination.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.RepositorySnapshotDestination)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.RepositorySnapshotDestination: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.RepositorySnapshotDestination();
            if (object.repositorySnapshotUri != null)
                message.repositorySnapshotUri = String(object.repositorySnapshotUri);
            return message;
        };

        /**
         * Creates a plain object from a RepositorySnapshotDestination message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.RepositorySnapshotDestination
         * @static
         * @param {dataform.RepositorySnapshotDestination} message RepositorySnapshotDestination
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RepositorySnapshotDestination.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.repositorySnapshotUri = "";
            if (message.repositorySnapshotUri != null && Object.hasOwnProperty.call(message, "repositorySnapshotUri"))
                object.repositorySnapshotUri = message.repositorySnapshotUri;
            return object;
        };

        /**
         * Converts this RepositorySnapshotDestination to JSON.
         * @function toJSON
         * @memberof dataform.RepositorySnapshotDestination
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RepositorySnapshotDestination.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RepositorySnapshotDestination
         * @function getTypeUrl
         * @memberof dataform.RepositorySnapshotDestination
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RepositorySnapshotDestination.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.RepositorySnapshotDestination";
        };

        return RepositorySnapshotDestination;
    })();

    dataform.NotebookRuntimeOptions = (function() {

        /**
         * Properties of a NotebookRuntimeOptions.
         * @memberof dataform
         * @interface INotebookRuntimeOptions
         * @property {string|null} [outputBucket] NotebookRuntimeOptions outputBucket
         * @property {string|null} [runtimeTemplateName] NotebookRuntimeOptions runtimeTemplateName
         * @property {dataform.IRepositorySnapshotDestination|null} [repositorySnapshotDestination] NotebookRuntimeOptions repositorySnapshotDestination
         */

        /**
         * Constructs a new NotebookRuntimeOptions.
         * @memberof dataform
         * @classdesc Represents a NotebookRuntimeOptions.
         * @implements INotebookRuntimeOptions
         * @constructor
         * @param {dataform.INotebookRuntimeOptions=} [properties] Properties to set
         */
        function NotebookRuntimeOptions(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * NotebookRuntimeOptions outputBucket.
         * @member {string|null|undefined} outputBucket
         * @memberof dataform.NotebookRuntimeOptions
         * @instance
         */
        NotebookRuntimeOptions.prototype.outputBucket = null;

        /**
         * NotebookRuntimeOptions runtimeTemplateName.
         * @member {string} runtimeTemplateName
         * @memberof dataform.NotebookRuntimeOptions
         * @instance
         */
        NotebookRuntimeOptions.prototype.runtimeTemplateName = "";

        /**
         * NotebookRuntimeOptions repositorySnapshotDestination.
         * @member {dataform.IRepositorySnapshotDestination|null|undefined} repositorySnapshotDestination
         * @memberof dataform.NotebookRuntimeOptions
         * @instance
         */
        NotebookRuntimeOptions.prototype.repositorySnapshotDestination = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * NotebookRuntimeOptions outputSink.
         * @member {"outputBucket"|undefined} outputSink
         * @memberof dataform.NotebookRuntimeOptions
         * @instance
         */
        Object.defineProperty(NotebookRuntimeOptions.prototype, "outputSink", {
            get: $util.oneOfGetter($oneOfFields = ["outputBucket"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * NotebookRuntimeOptions repositorySnapshotStorage.
         * @member {"repositorySnapshotDestination"|undefined} repositorySnapshotStorage
         * @memberof dataform.NotebookRuntimeOptions
         * @instance
         */
        Object.defineProperty(NotebookRuntimeOptions.prototype, "repositorySnapshotStorage", {
            get: $util.oneOfGetter($oneOfFields = ["repositorySnapshotDestination"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new NotebookRuntimeOptions instance using the specified properties.
         * @function create
         * @memberof dataform.NotebookRuntimeOptions
         * @static
         * @param {dataform.INotebookRuntimeOptions=} [properties] Properties to set
         * @returns {dataform.NotebookRuntimeOptions} NotebookRuntimeOptions instance
         */
        NotebookRuntimeOptions.create = function create(properties) {
            return new NotebookRuntimeOptions(properties);
        };

        /**
         * Encodes the specified NotebookRuntimeOptions message. Does not implicitly {@link dataform.NotebookRuntimeOptions.verify|verify} messages.
         * @function encode
         * @memberof dataform.NotebookRuntimeOptions
         * @static
         * @param {dataform.INotebookRuntimeOptions} message NotebookRuntimeOptions message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotebookRuntimeOptions.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.outputBucket != null && Object.hasOwnProperty.call(message, "outputBucket"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.outputBucket);
            if (message.runtimeTemplateName != null && Object.hasOwnProperty.call(message, "runtimeTemplateName"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.runtimeTemplateName);
            if (message.repositorySnapshotDestination != null && Object.hasOwnProperty.call(message, "repositorySnapshotDestination"))
                $root.dataform.RepositorySnapshotDestination.encode(message.repositorySnapshotDestination, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified NotebookRuntimeOptions message, length delimited. Does not implicitly {@link dataform.NotebookRuntimeOptions.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.NotebookRuntimeOptions
         * @static
         * @param {dataform.INotebookRuntimeOptions} message NotebookRuntimeOptions message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        NotebookRuntimeOptions.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a NotebookRuntimeOptions message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.NotebookRuntimeOptions
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.NotebookRuntimeOptions} NotebookRuntimeOptions
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotebookRuntimeOptions.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.NotebookRuntimeOptions();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.outputBucket = reader.string();
                        break;
                    }
                case 2: {
                        message.runtimeTemplateName = reader.string();
                        break;
                    }
                case 3: {
                        message.repositorySnapshotDestination = $root.dataform.RepositorySnapshotDestination.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a NotebookRuntimeOptions message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.NotebookRuntimeOptions
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.NotebookRuntimeOptions} NotebookRuntimeOptions
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        NotebookRuntimeOptions.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a NotebookRuntimeOptions message.
         * @function verify
         * @memberof dataform.NotebookRuntimeOptions
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        NotebookRuntimeOptions.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.outputBucket != null && Object.hasOwnProperty.call(message, "outputBucket")) {
                if (!$util.isString(message.outputBucket))
                    return "outputBucket: string expected";
            }
            if (message.runtimeTemplateName != null && Object.hasOwnProperty.call(message, "runtimeTemplateName"))
                if (!$util.isString(message.runtimeTemplateName))
                    return "runtimeTemplateName: string expected";
            if (message.repositorySnapshotDestination != null && Object.hasOwnProperty.call(message, "repositorySnapshotDestination")) {
                {
                    let error = $root.dataform.RepositorySnapshotDestination.verify(message.repositorySnapshotDestination, long + 1);
                    if (error)
                        return "repositorySnapshotDestination." + error;
                }
            }
            return null;
        };

        /**
         * Creates a NotebookRuntimeOptions message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.NotebookRuntimeOptions
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.NotebookRuntimeOptions} NotebookRuntimeOptions
         */
        NotebookRuntimeOptions.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.NotebookRuntimeOptions)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.NotebookRuntimeOptions: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.NotebookRuntimeOptions();
            if (object.outputBucket != null)
                message.outputBucket = String(object.outputBucket);
            if (object.runtimeTemplateName != null)
                message.runtimeTemplateName = String(object.runtimeTemplateName);
            if (object.repositorySnapshotDestination != null) {
                if (!$util.isObject(object.repositorySnapshotDestination))
                    throw TypeError(".dataform.NotebookRuntimeOptions.repositorySnapshotDestination: object expected");
                message.repositorySnapshotDestination = $root.dataform.RepositorySnapshotDestination.fromObject(object.repositorySnapshotDestination, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a NotebookRuntimeOptions message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.NotebookRuntimeOptions
         * @static
         * @param {dataform.NotebookRuntimeOptions} message NotebookRuntimeOptions
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        NotebookRuntimeOptions.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.runtimeTemplateName = "";
            if (message.outputBucket != null && Object.hasOwnProperty.call(message, "outputBucket")) {
                object.outputBucket = message.outputBucket;
                if (options.oneofs)
                    object.outputSink = "outputBucket";
            }
            if (message.runtimeTemplateName != null && Object.hasOwnProperty.call(message, "runtimeTemplateName"))
                object.runtimeTemplateName = message.runtimeTemplateName;
            if (message.repositorySnapshotDestination != null && Object.hasOwnProperty.call(message, "repositorySnapshotDestination")) {
                object.repositorySnapshotDestination = $root.dataform.RepositorySnapshotDestination.toObject(message.repositorySnapshotDestination, options, q + 1);
                if (options.oneofs)
                    object.repositorySnapshotStorage = "repositorySnapshotDestination";
            }
            return object;
        };

        /**
         * Converts this NotebookRuntimeOptions to JSON.
         * @function toJSON
         * @memberof dataform.NotebookRuntimeOptions
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        NotebookRuntimeOptions.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for NotebookRuntimeOptions
         * @function getTypeUrl
         * @memberof dataform.NotebookRuntimeOptions
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        NotebookRuntimeOptions.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.NotebookRuntimeOptions";
        };

        return NotebookRuntimeOptions;
    })();

    dataform.DataPreparation = (function() {

        /**
         * Properties of a DataPreparation.
         * @memberof dataform
         * @interface IDataPreparation
         * @property {dataform.ITarget|null} [target] DataPreparation target
         * @property {dataform.ITarget|null} [canonicalTarget] DataPreparation canonicalTarget
         * @property {Array.<dataform.ITarget>|null} [targets] DataPreparation targets
         * @property {Array.<dataform.ITarget>|null} [canonicalTargets] DataPreparation canonicalTargets
         * @property {Array.<string>|null} [tags] DataPreparation tags
         * @property {Array.<dataform.ITarget>|null} [dependencyTargets] DataPreparation dependencyTargets
         * @property {string|null} [fileName] DataPreparation fileName
         * @property {boolean|null} [disabled] DataPreparation disabled
         * @property {string|null} [dataPreparationYaml] DataPreparation dataPreparationYaml
         * @property {string|null} [query] DataPreparation query
         * @property {dataform.ITarget|null} [errorTable] DataPreparation errorTable
         * @property {number|null} [errorTableRetentionDays] DataPreparation errorTableRetentionDays
         * @property {dataform.ILoadConfiguration|null} [load] DataPreparation load
         */

        /**
         * Constructs a new DataPreparation.
         * @memberof dataform
         * @classdesc Represents a DataPreparation.
         * @implements IDataPreparation
         * @constructor
         * @param {dataform.IDataPreparation=} [properties] Properties to set
         */
        function DataPreparation(properties) {
            this.targets = [];
            this.canonicalTargets = [];
            this.tags = [];
            this.dependencyTargets = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DataPreparation target.
         * @member {dataform.ITarget|null|undefined} target
         * @memberof dataform.DataPreparation
         * @instance
         */
        DataPreparation.prototype.target = null;

        /**
         * DataPreparation canonicalTarget.
         * @member {dataform.ITarget|null|undefined} canonicalTarget
         * @memberof dataform.DataPreparation
         * @instance
         */
        DataPreparation.prototype.canonicalTarget = null;

        /**
         * DataPreparation targets.
         * @member {Array.<dataform.ITarget>} targets
         * @memberof dataform.DataPreparation
         * @instance
         */
        DataPreparation.prototype.targets = $util.emptyArray;

        /**
         * DataPreparation canonicalTargets.
         * @member {Array.<dataform.ITarget>} canonicalTargets
         * @memberof dataform.DataPreparation
         * @instance
         */
        DataPreparation.prototype.canonicalTargets = $util.emptyArray;

        /**
         * DataPreparation tags.
         * @member {Array.<string>} tags
         * @memberof dataform.DataPreparation
         * @instance
         */
        DataPreparation.prototype.tags = $util.emptyArray;

        /**
         * DataPreparation dependencyTargets.
         * @member {Array.<dataform.ITarget>} dependencyTargets
         * @memberof dataform.DataPreparation
         * @instance
         */
        DataPreparation.prototype.dependencyTargets = $util.emptyArray;

        /**
         * DataPreparation fileName.
         * @member {string} fileName
         * @memberof dataform.DataPreparation
         * @instance
         */
        DataPreparation.prototype.fileName = "";

        /**
         * DataPreparation disabled.
         * @member {boolean} disabled
         * @memberof dataform.DataPreparation
         * @instance
         */
        DataPreparation.prototype.disabled = false;

        /**
         * DataPreparation dataPreparationYaml.
         * @member {string|null|undefined} dataPreparationYaml
         * @memberof dataform.DataPreparation
         * @instance
         */
        DataPreparation.prototype.dataPreparationYaml = null;

        /**
         * DataPreparation query.
         * @member {string|null|undefined} query
         * @memberof dataform.DataPreparation
         * @instance
         */
        DataPreparation.prototype.query = null;

        /**
         * DataPreparation errorTable.
         * @member {dataform.ITarget|null|undefined} errorTable
         * @memberof dataform.DataPreparation
         * @instance
         */
        DataPreparation.prototype.errorTable = null;

        /**
         * DataPreparation errorTableRetentionDays.
         * @member {number} errorTableRetentionDays
         * @memberof dataform.DataPreparation
         * @instance
         */
        DataPreparation.prototype.errorTableRetentionDays = 0;

        /**
         * DataPreparation load.
         * @member {dataform.ILoadConfiguration|null|undefined} load
         * @memberof dataform.DataPreparation
         * @instance
         */
        DataPreparation.prototype.load = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * DataPreparation definition.
         * @member {"dataPreparationYaml"|"query"|undefined} definition
         * @memberof dataform.DataPreparation
         * @instance
         */
        Object.defineProperty(DataPreparation.prototype, "definition", {
            get: $util.oneOfGetter($oneOfFields = ["dataPreparationYaml", "query"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new DataPreparation instance using the specified properties.
         * @function create
         * @memberof dataform.DataPreparation
         * @static
         * @param {dataform.IDataPreparation=} [properties] Properties to set
         * @returns {dataform.DataPreparation} DataPreparation instance
         */
        DataPreparation.create = function create(properties) {
            return new DataPreparation(properties);
        };

        /**
         * Encodes the specified DataPreparation message. Does not implicitly {@link dataform.DataPreparation.verify|verify} messages.
         * @function encode
         * @memberof dataform.DataPreparation
         * @static
         * @param {dataform.IDataPreparation} message DataPreparation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DataPreparation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.targets != null && message.targets.length)
                for (let i = 0; i < message.targets.length; ++i)
                    $root.dataform.Target.encode(message.targets[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.canonicalTargets != null && message.canonicalTargets.length)
                for (let i = 0; i < message.canonicalTargets.length; ++i)
                    $root.dataform.Target.encode(message.canonicalTargets[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.tags != null && message.tags.length)
                for (let i = 0; i < message.tags.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.tags[i]);
            if (message.dependencyTargets != null && message.dependencyTargets.length)
                for (let i = 0; i < message.dependencyTargets.length; ++i)
                    $root.dataform.Target.encode(message.dependencyTargets[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.fileName);
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.disabled);
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                $root.dataform.Target.encode(message.target, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget"))
                $root.dataform.Target.encode(message.canonicalTarget, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
            if (message.dataPreparationYaml != null && Object.hasOwnProperty.call(message, "dataPreparationYaml"))
                writer.uint32(/* id 11, wireType 2 =*/90).string(message.dataPreparationYaml);
            if (message.errorTable != null && Object.hasOwnProperty.call(message, "errorTable"))
                $root.dataform.Target.encode(message.errorTable, writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
            if (message.errorTableRetentionDays != null && Object.hasOwnProperty.call(message, "errorTableRetentionDays"))
                writer.uint32(/* id 13, wireType 0 =*/104).int32(message.errorTableRetentionDays);
            if (message.query != null && Object.hasOwnProperty.call(message, "query"))
                writer.uint32(/* id 15, wireType 2 =*/122).string(message.query);
            if (message.load != null && Object.hasOwnProperty.call(message, "load"))
                $root.dataform.LoadConfiguration.encode(message.load, writer.uint32(/* id 16, wireType 2 =*/130).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified DataPreparation message, length delimited. Does not implicitly {@link dataform.DataPreparation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.DataPreparation
         * @static
         * @param {dataform.IDataPreparation} message DataPreparation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DataPreparation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DataPreparation message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.DataPreparation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.DataPreparation} DataPreparation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DataPreparation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.DataPreparation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 8: {
                        message.target = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 9: {
                        message.canonicalTarget = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 1: {
                        if (!(message.targets && message.targets.length))
                            message.targets = [];
                        message.targets.push($root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 2: {
                        if (!(message.canonicalTargets && message.canonicalTargets.length))
                            message.canonicalTargets = [];
                        message.canonicalTargets.push($root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 3: {
                        if (!(message.tags && message.tags.length))
                            message.tags = [];
                        message.tags.push(reader.string());
                        break;
                    }
                case 4: {
                        if (!(message.dependencyTargets && message.dependencyTargets.length))
                            message.dependencyTargets = [];
                        message.dependencyTargets.push($root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 5: {
                        message.fileName = reader.string();
                        break;
                    }
                case 6: {
                        message.disabled = reader.bool();
                        break;
                    }
                case 11: {
                        message.dataPreparationYaml = reader.string();
                        break;
                    }
                case 15: {
                        message.query = reader.string();
                        break;
                    }
                case 12: {
                        message.errorTable = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 13: {
                        message.errorTableRetentionDays = reader.int32();
                        break;
                    }
                case 16: {
                        message.load = $root.dataform.LoadConfiguration.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DataPreparation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.DataPreparation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.DataPreparation} DataPreparation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DataPreparation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DataPreparation message.
         * @function verify
         * @memberof dataform.DataPreparation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DataPreparation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            let properties = {};
            if (message.target != null && Object.hasOwnProperty.call(message, "target")) {
                let error = $root.dataform.Target.verify(message.target, long + 1);
                if (error)
                    return "target." + error;
            }
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget")) {
                let error = $root.dataform.Target.verify(message.canonicalTarget, long + 1);
                if (error)
                    return "canonicalTarget." + error;
            }
            if (message.targets != null && Object.hasOwnProperty.call(message, "targets")) {
                if (!Array.isArray(message.targets))
                    return "targets: array expected";
                for (let i = 0; i < message.targets.length; ++i) {
                    let error = $root.dataform.Target.verify(message.targets[i], long + 1);
                    if (error)
                        return "targets." + error;
                }
            }
            if (message.canonicalTargets != null && Object.hasOwnProperty.call(message, "canonicalTargets")) {
                if (!Array.isArray(message.canonicalTargets))
                    return "canonicalTargets: array expected";
                for (let i = 0; i < message.canonicalTargets.length; ++i) {
                    let error = $root.dataform.Target.verify(message.canonicalTargets[i], long + 1);
                    if (error)
                        return "canonicalTargets." + error;
                }
            }
            if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                if (!Array.isArray(message.tags))
                    return "tags: array expected";
                for (let i = 0; i < message.tags.length; ++i)
                    if (!$util.isString(message.tags[i]))
                        return "tags: string[] expected";
            }
            if (message.dependencyTargets != null && Object.hasOwnProperty.call(message, "dependencyTargets")) {
                if (!Array.isArray(message.dependencyTargets))
                    return "dependencyTargets: array expected";
                for (let i = 0; i < message.dependencyTargets.length; ++i) {
                    let error = $root.dataform.Target.verify(message.dependencyTargets[i], long + 1);
                    if (error)
                        return "dependencyTargets." + error;
                }
            }
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                if (!$util.isString(message.fileName))
                    return "fileName: string expected";
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                if (typeof message.disabled !== "boolean")
                    return "disabled: boolean expected";
            if (message.dataPreparationYaml != null && Object.hasOwnProperty.call(message, "dataPreparationYaml")) {
                properties.definition = 1;
                if (!$util.isString(message.dataPreparationYaml))
                    return "dataPreparationYaml: string expected";
            }
            if (message.query != null && Object.hasOwnProperty.call(message, "query")) {
                if (properties.definition === 1)
                    return "definition: multiple values";
                properties.definition = 1;
                if (!$util.isString(message.query))
                    return "query: string expected";
            }
            if (message.errorTable != null && Object.hasOwnProperty.call(message, "errorTable")) {
                let error = $root.dataform.Target.verify(message.errorTable, long + 1);
                if (error)
                    return "errorTable." + error;
            }
            if (message.errorTableRetentionDays != null && Object.hasOwnProperty.call(message, "errorTableRetentionDays"))
                if (!$util.isInteger(message.errorTableRetentionDays))
                    return "errorTableRetentionDays: integer expected";
            if (message.load != null && Object.hasOwnProperty.call(message, "load")) {
                let error = $root.dataform.LoadConfiguration.verify(message.load, long + 1);
                if (error)
                    return "load." + error;
            }
            return null;
        };

        /**
         * Creates a DataPreparation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.DataPreparation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.DataPreparation} DataPreparation
         */
        DataPreparation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.DataPreparation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.DataPreparation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.DataPreparation();
            if (object.target != null) {
                if (!$util.isObject(object.target))
                    throw TypeError(".dataform.DataPreparation.target: object expected");
                message.target = $root.dataform.Target.fromObject(object.target, long + 1);
            }
            if (object.canonicalTarget != null) {
                if (!$util.isObject(object.canonicalTarget))
                    throw TypeError(".dataform.DataPreparation.canonicalTarget: object expected");
                message.canonicalTarget = $root.dataform.Target.fromObject(object.canonicalTarget, long + 1);
            }
            if (object.targets) {
                if (!Array.isArray(object.targets))
                    throw TypeError(".dataform.DataPreparation.targets: array expected");
                message.targets = [];
                for (let i = 0; i < object.targets.length; ++i) {
                    if (!$util.isObject(object.targets[i]))
                        throw TypeError(".dataform.DataPreparation.targets: object expected");
                    message.targets[i] = $root.dataform.Target.fromObject(object.targets[i], long + 1);
                }
            }
            if (object.canonicalTargets) {
                if (!Array.isArray(object.canonicalTargets))
                    throw TypeError(".dataform.DataPreparation.canonicalTargets: array expected");
                message.canonicalTargets = [];
                for (let i = 0; i < object.canonicalTargets.length; ++i) {
                    if (!$util.isObject(object.canonicalTargets[i]))
                        throw TypeError(".dataform.DataPreparation.canonicalTargets: object expected");
                    message.canonicalTargets[i] = $root.dataform.Target.fromObject(object.canonicalTargets[i], long + 1);
                }
            }
            if (object.tags) {
                if (!Array.isArray(object.tags))
                    throw TypeError(".dataform.DataPreparation.tags: array expected");
                message.tags = [];
                for (let i = 0; i < object.tags.length; ++i)
                    message.tags[i] = String(object.tags[i]);
            }
            if (object.dependencyTargets) {
                if (!Array.isArray(object.dependencyTargets))
                    throw TypeError(".dataform.DataPreparation.dependencyTargets: array expected");
                message.dependencyTargets = [];
                for (let i = 0; i < object.dependencyTargets.length; ++i) {
                    if (!$util.isObject(object.dependencyTargets[i]))
                        throw TypeError(".dataform.DataPreparation.dependencyTargets: object expected");
                    message.dependencyTargets[i] = $root.dataform.Target.fromObject(object.dependencyTargets[i], long + 1);
                }
            }
            if (object.fileName != null)
                message.fileName = String(object.fileName);
            if (object.disabled != null)
                message.disabled = Boolean(object.disabled);
            if (object.dataPreparationYaml != null)
                message.dataPreparationYaml = String(object.dataPreparationYaml);
            if (object.query != null)
                message.query = String(object.query);
            if (object.errorTable != null) {
                if (!$util.isObject(object.errorTable))
                    throw TypeError(".dataform.DataPreparation.errorTable: object expected");
                message.errorTable = $root.dataform.Target.fromObject(object.errorTable, long + 1);
            }
            if (object.errorTableRetentionDays != null)
                message.errorTableRetentionDays = object.errorTableRetentionDays | 0;
            if (object.load != null) {
                if (!$util.isObject(object.load))
                    throw TypeError(".dataform.DataPreparation.load: object expected");
                message.load = $root.dataform.LoadConfiguration.fromObject(object.load, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a DataPreparation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.DataPreparation
         * @static
         * @param {dataform.DataPreparation} message DataPreparation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DataPreparation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.targets = [];
                object.canonicalTargets = [];
                object.tags = [];
                object.dependencyTargets = [];
            }
            if (options.defaults) {
                object.fileName = "";
                object.disabled = false;
                object.target = null;
                object.canonicalTarget = null;
                object.errorTable = null;
                object.errorTableRetentionDays = 0;
                object.load = null;
            }
            if (message.targets && message.targets.length) {
                object.targets = [];
                for (let j = 0; j < message.targets.length; ++j)
                    object.targets[j] = $root.dataform.Target.toObject(message.targets[j], options, q + 1);
            }
            if (message.canonicalTargets && message.canonicalTargets.length) {
                object.canonicalTargets = [];
                for (let j = 0; j < message.canonicalTargets.length; ++j)
                    object.canonicalTargets[j] = $root.dataform.Target.toObject(message.canonicalTargets[j], options, q + 1);
            }
            if (message.tags && message.tags.length) {
                object.tags = [];
                for (let j = 0; j < message.tags.length; ++j)
                    object.tags[j] = message.tags[j];
            }
            if (message.dependencyTargets && message.dependencyTargets.length) {
                object.dependencyTargets = [];
                for (let j = 0; j < message.dependencyTargets.length; ++j)
                    object.dependencyTargets[j] = $root.dataform.Target.toObject(message.dependencyTargets[j], options, q + 1);
            }
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                object.fileName = message.fileName;
            if (message.disabled != null && Object.hasOwnProperty.call(message, "disabled"))
                object.disabled = message.disabled;
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                object.target = $root.dataform.Target.toObject(message.target, options, q + 1);
            if (message.canonicalTarget != null && Object.hasOwnProperty.call(message, "canonicalTarget"))
                object.canonicalTarget = $root.dataform.Target.toObject(message.canonicalTarget, options, q + 1);
            if (message.dataPreparationYaml != null && Object.hasOwnProperty.call(message, "dataPreparationYaml")) {
                object.dataPreparationYaml = message.dataPreparationYaml;
                if (options.oneofs)
                    object.definition = "dataPreparationYaml";
            }
            if (message.errorTable != null && Object.hasOwnProperty.call(message, "errorTable"))
                object.errorTable = $root.dataform.Target.toObject(message.errorTable, options, q + 1);
            if (message.errorTableRetentionDays != null && Object.hasOwnProperty.call(message, "errorTableRetentionDays"))
                object.errorTableRetentionDays = message.errorTableRetentionDays;
            if (message.query != null && Object.hasOwnProperty.call(message, "query")) {
                object.query = message.query;
                if (options.oneofs)
                    object.definition = "query";
            }
            if (message.load != null && Object.hasOwnProperty.call(message, "load"))
                object.load = $root.dataform.LoadConfiguration.toObject(message.load, options, q + 1);
            return object;
        };

        /**
         * Converts this DataPreparation to JSON.
         * @function toJSON
         * @memberof dataform.DataPreparation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DataPreparation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DataPreparation
         * @function getTypeUrl
         * @memberof dataform.DataPreparation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DataPreparation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.DataPreparation";
        };

        return DataPreparation;
    })();

    dataform.LoadConfiguration = (function() {

        /**
         * Properties of a LoadConfiguration.
         * @memberof dataform
         * @interface ILoadConfiguration
         * @property {dataform.ISimpleLoadMode|null} [replace] LoadConfiguration replace
         * @property {dataform.ISimpleLoadMode|null} [append] LoadConfiguration append
         * @property {dataform.IIncrementalLoadMode|null} [maximum] LoadConfiguration maximum
         * @property {dataform.IIncrementalLoadMode|null} [unique] LoadConfiguration unique
         * @property {dataform.IIncrementalLoadMode|null} [automatic] LoadConfiguration automatic
         * @property {dataform.IMergeLoadMode|null} [merge] LoadConfiguration merge
         */

        /**
         * Constructs a new LoadConfiguration.
         * @memberof dataform
         * @classdesc Represents a LoadConfiguration.
         * @implements ILoadConfiguration
         * @constructor
         * @param {dataform.ILoadConfiguration=} [properties] Properties to set
         */
        function LoadConfiguration(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * LoadConfiguration replace.
         * @member {dataform.ISimpleLoadMode|null|undefined} replace
         * @memberof dataform.LoadConfiguration
         * @instance
         */
        LoadConfiguration.prototype.replace = null;

        /**
         * LoadConfiguration append.
         * @member {dataform.ISimpleLoadMode|null|undefined} append
         * @memberof dataform.LoadConfiguration
         * @instance
         */
        LoadConfiguration.prototype.append = null;

        /**
         * LoadConfiguration maximum.
         * @member {dataform.IIncrementalLoadMode|null|undefined} maximum
         * @memberof dataform.LoadConfiguration
         * @instance
         */
        LoadConfiguration.prototype.maximum = null;

        /**
         * LoadConfiguration unique.
         * @member {dataform.IIncrementalLoadMode|null|undefined} unique
         * @memberof dataform.LoadConfiguration
         * @instance
         */
        LoadConfiguration.prototype.unique = null;

        /**
         * LoadConfiguration automatic.
         * @member {dataform.IIncrementalLoadMode|null|undefined} automatic
         * @memberof dataform.LoadConfiguration
         * @instance
         */
        LoadConfiguration.prototype.automatic = null;

        /**
         * LoadConfiguration merge.
         * @member {dataform.IMergeLoadMode|null|undefined} merge
         * @memberof dataform.LoadConfiguration
         * @instance
         */
        LoadConfiguration.prototype.merge = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * LoadConfiguration mode.
         * @member {"replace"|"append"|"maximum"|"unique"|"automatic"|"merge"|undefined} mode
         * @memberof dataform.LoadConfiguration
         * @instance
         */
        Object.defineProperty(LoadConfiguration.prototype, "mode", {
            get: $util.oneOfGetter($oneOfFields = ["replace", "append", "maximum", "unique", "automatic", "merge"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new LoadConfiguration instance using the specified properties.
         * @function create
         * @memberof dataform.LoadConfiguration
         * @static
         * @param {dataform.ILoadConfiguration=} [properties] Properties to set
         * @returns {dataform.LoadConfiguration} LoadConfiguration instance
         */
        LoadConfiguration.create = function create(properties) {
            return new LoadConfiguration(properties);
        };

        /**
         * Encodes the specified LoadConfiguration message. Does not implicitly {@link dataform.LoadConfiguration.verify|verify} messages.
         * @function encode
         * @memberof dataform.LoadConfiguration
         * @static
         * @param {dataform.ILoadConfiguration} message LoadConfiguration message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoadConfiguration.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.replace != null && Object.hasOwnProperty.call(message, "replace"))
                $root.dataform.SimpleLoadMode.encode(message.replace, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.append != null && Object.hasOwnProperty.call(message, "append"))
                $root.dataform.SimpleLoadMode.encode(message.append, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.maximum != null && Object.hasOwnProperty.call(message, "maximum"))
                $root.dataform.IncrementalLoadMode.encode(message.maximum, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.unique != null && Object.hasOwnProperty.call(message, "unique"))
                $root.dataform.IncrementalLoadMode.encode(message.unique, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.automatic != null && Object.hasOwnProperty.call(message, "automatic"))
                $root.dataform.IncrementalLoadMode.encode(message.automatic, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.merge != null && Object.hasOwnProperty.call(message, "merge"))
                $root.dataform.MergeLoadMode.encode(message.merge, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified LoadConfiguration message, length delimited. Does not implicitly {@link dataform.LoadConfiguration.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.LoadConfiguration
         * @static
         * @param {dataform.ILoadConfiguration} message LoadConfiguration message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        LoadConfiguration.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a LoadConfiguration message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.LoadConfiguration
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.LoadConfiguration} LoadConfiguration
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoadConfiguration.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.LoadConfiguration();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.replace = $root.dataform.SimpleLoadMode.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.append = $root.dataform.SimpleLoadMode.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 3: {
                        message.maximum = $root.dataform.IncrementalLoadMode.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 4: {
                        message.unique = $root.dataform.IncrementalLoadMode.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 5: {
                        message.automatic = $root.dataform.IncrementalLoadMode.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 6: {
                        message.merge = $root.dataform.MergeLoadMode.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a LoadConfiguration message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.LoadConfiguration
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.LoadConfiguration} LoadConfiguration
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        LoadConfiguration.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a LoadConfiguration message.
         * @function verify
         * @memberof dataform.LoadConfiguration
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        LoadConfiguration.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            let properties = {};
            if (message.replace != null && Object.hasOwnProperty.call(message, "replace")) {
                properties.mode = 1;
                {
                    let error = $root.dataform.SimpleLoadMode.verify(message.replace, long + 1);
                    if (error)
                        return "replace." + error;
                }
            }
            if (message.append != null && Object.hasOwnProperty.call(message, "append")) {
                if (properties.mode === 1)
                    return "mode: multiple values";
                properties.mode = 1;
                {
                    let error = $root.dataform.SimpleLoadMode.verify(message.append, long + 1);
                    if (error)
                        return "append." + error;
                }
            }
            if (message.maximum != null && Object.hasOwnProperty.call(message, "maximum")) {
                if (properties.mode === 1)
                    return "mode: multiple values";
                properties.mode = 1;
                {
                    let error = $root.dataform.IncrementalLoadMode.verify(message.maximum, long + 1);
                    if (error)
                        return "maximum." + error;
                }
            }
            if (message.unique != null && Object.hasOwnProperty.call(message, "unique")) {
                if (properties.mode === 1)
                    return "mode: multiple values";
                properties.mode = 1;
                {
                    let error = $root.dataform.IncrementalLoadMode.verify(message.unique, long + 1);
                    if (error)
                        return "unique." + error;
                }
            }
            if (message.automatic != null && Object.hasOwnProperty.call(message, "automatic")) {
                if (properties.mode === 1)
                    return "mode: multiple values";
                properties.mode = 1;
                {
                    let error = $root.dataform.IncrementalLoadMode.verify(message.automatic, long + 1);
                    if (error)
                        return "automatic." + error;
                }
            }
            if (message.merge != null && Object.hasOwnProperty.call(message, "merge")) {
                if (properties.mode === 1)
                    return "mode: multiple values";
                properties.mode = 1;
                {
                    let error = $root.dataform.MergeLoadMode.verify(message.merge, long + 1);
                    if (error)
                        return "merge." + error;
                }
            }
            return null;
        };

        /**
         * Creates a LoadConfiguration message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.LoadConfiguration
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.LoadConfiguration} LoadConfiguration
         */
        LoadConfiguration.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.LoadConfiguration)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.LoadConfiguration: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.LoadConfiguration();
            if (object.replace != null) {
                if (!$util.isObject(object.replace))
                    throw TypeError(".dataform.LoadConfiguration.replace: object expected");
                message.replace = $root.dataform.SimpleLoadMode.fromObject(object.replace, long + 1);
            }
            if (object.append != null) {
                if (!$util.isObject(object.append))
                    throw TypeError(".dataform.LoadConfiguration.append: object expected");
                message.append = $root.dataform.SimpleLoadMode.fromObject(object.append, long + 1);
            }
            if (object.maximum != null) {
                if (!$util.isObject(object.maximum))
                    throw TypeError(".dataform.LoadConfiguration.maximum: object expected");
                message.maximum = $root.dataform.IncrementalLoadMode.fromObject(object.maximum, long + 1);
            }
            if (object.unique != null) {
                if (!$util.isObject(object.unique))
                    throw TypeError(".dataform.LoadConfiguration.unique: object expected");
                message.unique = $root.dataform.IncrementalLoadMode.fromObject(object.unique, long + 1);
            }
            if (object.automatic != null) {
                if (!$util.isObject(object.automatic))
                    throw TypeError(".dataform.LoadConfiguration.automatic: object expected");
                message.automatic = $root.dataform.IncrementalLoadMode.fromObject(object.automatic, long + 1);
            }
            if (object.merge != null) {
                if (!$util.isObject(object.merge))
                    throw TypeError(".dataform.LoadConfiguration.merge: object expected");
                message.merge = $root.dataform.MergeLoadMode.fromObject(object.merge, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a LoadConfiguration message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.LoadConfiguration
         * @static
         * @param {dataform.LoadConfiguration} message LoadConfiguration
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        LoadConfiguration.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (message.replace != null && Object.hasOwnProperty.call(message, "replace")) {
                object.replace = $root.dataform.SimpleLoadMode.toObject(message.replace, options, q + 1);
                if (options.oneofs)
                    object.mode = "replace";
            }
            if (message.append != null && Object.hasOwnProperty.call(message, "append")) {
                object.append = $root.dataform.SimpleLoadMode.toObject(message.append, options, q + 1);
                if (options.oneofs)
                    object.mode = "append";
            }
            if (message.maximum != null && Object.hasOwnProperty.call(message, "maximum")) {
                object.maximum = $root.dataform.IncrementalLoadMode.toObject(message.maximum, options, q + 1);
                if (options.oneofs)
                    object.mode = "maximum";
            }
            if (message.unique != null && Object.hasOwnProperty.call(message, "unique")) {
                object.unique = $root.dataform.IncrementalLoadMode.toObject(message.unique, options, q + 1);
                if (options.oneofs)
                    object.mode = "unique";
            }
            if (message.automatic != null && Object.hasOwnProperty.call(message, "automatic")) {
                object.automatic = $root.dataform.IncrementalLoadMode.toObject(message.automatic, options, q + 1);
                if (options.oneofs)
                    object.mode = "automatic";
            }
            if (message.merge != null && Object.hasOwnProperty.call(message, "merge")) {
                object.merge = $root.dataform.MergeLoadMode.toObject(message.merge, options, q + 1);
                if (options.oneofs)
                    object.mode = "merge";
            }
            return object;
        };

        /**
         * Converts this LoadConfiguration to JSON.
         * @function toJSON
         * @memberof dataform.LoadConfiguration
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        LoadConfiguration.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for LoadConfiguration
         * @function getTypeUrl
         * @memberof dataform.LoadConfiguration
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        LoadConfiguration.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.LoadConfiguration";
        };

        return LoadConfiguration;
    })();

    dataform.SimpleLoadMode = (function() {

        /**
         * Properties of a SimpleLoadMode.
         * @memberof dataform
         * @interface ISimpleLoadMode
         */

        /**
         * Constructs a new SimpleLoadMode.
         * @memberof dataform
         * @classdesc Represents a SimpleLoadMode.
         * @implements ISimpleLoadMode
         * @constructor
         * @param {dataform.ISimpleLoadMode=} [properties] Properties to set
         */
        function SimpleLoadMode(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Creates a new SimpleLoadMode instance using the specified properties.
         * @function create
         * @memberof dataform.SimpleLoadMode
         * @static
         * @param {dataform.ISimpleLoadMode=} [properties] Properties to set
         * @returns {dataform.SimpleLoadMode} SimpleLoadMode instance
         */
        SimpleLoadMode.create = function create(properties) {
            return new SimpleLoadMode(properties);
        };

        /**
         * Encodes the specified SimpleLoadMode message. Does not implicitly {@link dataform.SimpleLoadMode.verify|verify} messages.
         * @function encode
         * @memberof dataform.SimpleLoadMode
         * @static
         * @param {dataform.ISimpleLoadMode} message SimpleLoadMode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SimpleLoadMode.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            return writer;
        };

        /**
         * Encodes the specified SimpleLoadMode message, length delimited. Does not implicitly {@link dataform.SimpleLoadMode.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.SimpleLoadMode
         * @static
         * @param {dataform.ISimpleLoadMode} message SimpleLoadMode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        SimpleLoadMode.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a SimpleLoadMode message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.SimpleLoadMode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.SimpleLoadMode} SimpleLoadMode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SimpleLoadMode.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.SimpleLoadMode();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a SimpleLoadMode message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.SimpleLoadMode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.SimpleLoadMode} SimpleLoadMode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        SimpleLoadMode.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a SimpleLoadMode message.
         * @function verify
         * @memberof dataform.SimpleLoadMode
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        SimpleLoadMode.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            return null;
        };

        /**
         * Creates a SimpleLoadMode message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.SimpleLoadMode
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.SimpleLoadMode} SimpleLoadMode
         */
        SimpleLoadMode.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.SimpleLoadMode)
                return object;
            return new $root.dataform.SimpleLoadMode();
        };

        /**
         * Creates a plain object from a SimpleLoadMode message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.SimpleLoadMode
         * @static
         * @param {dataform.SimpleLoadMode} message SimpleLoadMode
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        SimpleLoadMode.toObject = function toObject() {
            return {};
        };

        /**
         * Converts this SimpleLoadMode to JSON.
         * @function toJSON
         * @memberof dataform.SimpleLoadMode
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        SimpleLoadMode.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for SimpleLoadMode
         * @function getTypeUrl
         * @memberof dataform.SimpleLoadMode
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        SimpleLoadMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.SimpleLoadMode";
        };

        return SimpleLoadMode;
    })();

    dataform.IncrementalLoadMode = (function() {

        /**
         * Properties of an IncrementalLoadMode.
         * @memberof dataform
         * @interface IIncrementalLoadMode
         * @property {string|null} [columnName] IncrementalLoadMode columnName
         */

        /**
         * Constructs a new IncrementalLoadMode.
         * @memberof dataform
         * @classdesc Represents an IncrementalLoadMode.
         * @implements IIncrementalLoadMode
         * @constructor
         * @param {dataform.IIncrementalLoadMode=} [properties] Properties to set
         */
        function IncrementalLoadMode(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * IncrementalLoadMode columnName.
         * @member {string} columnName
         * @memberof dataform.IncrementalLoadMode
         * @instance
         */
        IncrementalLoadMode.prototype.columnName = "";

        /**
         * Creates a new IncrementalLoadMode instance using the specified properties.
         * @function create
         * @memberof dataform.IncrementalLoadMode
         * @static
         * @param {dataform.IIncrementalLoadMode=} [properties] Properties to set
         * @returns {dataform.IncrementalLoadMode} IncrementalLoadMode instance
         */
        IncrementalLoadMode.create = function create(properties) {
            return new IncrementalLoadMode(properties);
        };

        /**
         * Encodes the specified IncrementalLoadMode message. Does not implicitly {@link dataform.IncrementalLoadMode.verify|verify} messages.
         * @function encode
         * @memberof dataform.IncrementalLoadMode
         * @static
         * @param {dataform.IIncrementalLoadMode} message IncrementalLoadMode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IncrementalLoadMode.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.columnName != null && Object.hasOwnProperty.call(message, "columnName"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.columnName);
            return writer;
        };

        /**
         * Encodes the specified IncrementalLoadMode message, length delimited. Does not implicitly {@link dataform.IncrementalLoadMode.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.IncrementalLoadMode
         * @static
         * @param {dataform.IIncrementalLoadMode} message IncrementalLoadMode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        IncrementalLoadMode.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an IncrementalLoadMode message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.IncrementalLoadMode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.IncrementalLoadMode} IncrementalLoadMode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IncrementalLoadMode.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.IncrementalLoadMode();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.columnName = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an IncrementalLoadMode message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.IncrementalLoadMode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.IncrementalLoadMode} IncrementalLoadMode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        IncrementalLoadMode.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an IncrementalLoadMode message.
         * @function verify
         * @memberof dataform.IncrementalLoadMode
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        IncrementalLoadMode.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.columnName != null && Object.hasOwnProperty.call(message, "columnName"))
                if (!$util.isString(message.columnName))
                    return "columnName: string expected";
            return null;
        };

        /**
         * Creates an IncrementalLoadMode message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.IncrementalLoadMode
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.IncrementalLoadMode} IncrementalLoadMode
         */
        IncrementalLoadMode.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.IncrementalLoadMode)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.IncrementalLoadMode: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.IncrementalLoadMode();
            if (object.columnName != null)
                message.columnName = String(object.columnName);
            return message;
        };

        /**
         * Creates a plain object from an IncrementalLoadMode message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.IncrementalLoadMode
         * @static
         * @param {dataform.IncrementalLoadMode} message IncrementalLoadMode
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        IncrementalLoadMode.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.columnName = "";
            if (message.columnName != null && Object.hasOwnProperty.call(message, "columnName"))
                object.columnName = message.columnName;
            return object;
        };

        /**
         * Converts this IncrementalLoadMode to JSON.
         * @function toJSON
         * @memberof dataform.IncrementalLoadMode
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        IncrementalLoadMode.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for IncrementalLoadMode
         * @function getTypeUrl
         * @memberof dataform.IncrementalLoadMode
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        IncrementalLoadMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.IncrementalLoadMode";
        };

        return IncrementalLoadMode;
    })();

    dataform.MergeLoadMode = (function() {

        /**
         * Properties of a MergeLoadMode.
         * @memberof dataform
         * @interface IMergeLoadMode
         * @property {Array.<string>|null} [uniqueKey] MergeLoadMode uniqueKey
         */

        /**
         * Constructs a new MergeLoadMode.
         * @memberof dataform
         * @classdesc Represents a MergeLoadMode.
         * @implements IMergeLoadMode
         * @constructor
         * @param {dataform.IMergeLoadMode=} [properties] Properties to set
         */
        function MergeLoadMode(properties) {
            this.uniqueKey = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * MergeLoadMode uniqueKey.
         * @member {Array.<string>} uniqueKey
         * @memberof dataform.MergeLoadMode
         * @instance
         */
        MergeLoadMode.prototype.uniqueKey = $util.emptyArray;

        /**
         * Creates a new MergeLoadMode instance using the specified properties.
         * @function create
         * @memberof dataform.MergeLoadMode
         * @static
         * @param {dataform.IMergeLoadMode=} [properties] Properties to set
         * @returns {dataform.MergeLoadMode} MergeLoadMode instance
         */
        MergeLoadMode.create = function create(properties) {
            return new MergeLoadMode(properties);
        };

        /**
         * Encodes the specified MergeLoadMode message. Does not implicitly {@link dataform.MergeLoadMode.verify|verify} messages.
         * @function encode
         * @memberof dataform.MergeLoadMode
         * @static
         * @param {dataform.IMergeLoadMode} message MergeLoadMode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MergeLoadMode.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.uniqueKey != null && message.uniqueKey.length)
                for (let i = 0; i < message.uniqueKey.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.uniqueKey[i]);
            return writer;
        };

        /**
         * Encodes the specified MergeLoadMode message, length delimited. Does not implicitly {@link dataform.MergeLoadMode.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.MergeLoadMode
         * @static
         * @param {dataform.IMergeLoadMode} message MergeLoadMode message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        MergeLoadMode.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a MergeLoadMode message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.MergeLoadMode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.MergeLoadMode} MergeLoadMode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MergeLoadMode.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.MergeLoadMode();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.uniqueKey && message.uniqueKey.length))
                            message.uniqueKey = [];
                        message.uniqueKey.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a MergeLoadMode message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.MergeLoadMode
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.MergeLoadMode} MergeLoadMode
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        MergeLoadMode.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a MergeLoadMode message.
         * @function verify
         * @memberof dataform.MergeLoadMode
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        MergeLoadMode.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.uniqueKey != null && Object.hasOwnProperty.call(message, "uniqueKey")) {
                if (!Array.isArray(message.uniqueKey))
                    return "uniqueKey: array expected";
                for (let i = 0; i < message.uniqueKey.length; ++i)
                    if (!$util.isString(message.uniqueKey[i]))
                        return "uniqueKey: string[] expected";
            }
            return null;
        };

        /**
         * Creates a MergeLoadMode message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.MergeLoadMode
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.MergeLoadMode} MergeLoadMode
         */
        MergeLoadMode.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.MergeLoadMode)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.MergeLoadMode: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.MergeLoadMode();
            if (object.uniqueKey) {
                if (!Array.isArray(object.uniqueKey))
                    throw TypeError(".dataform.MergeLoadMode.uniqueKey: array expected");
                message.uniqueKey = [];
                for (let i = 0; i < object.uniqueKey.length; ++i)
                    message.uniqueKey[i] = String(object.uniqueKey[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a MergeLoadMode message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.MergeLoadMode
         * @static
         * @param {dataform.MergeLoadMode} message MergeLoadMode
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        MergeLoadMode.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.uniqueKey = [];
            if (message.uniqueKey && message.uniqueKey.length) {
                object.uniqueKey = [];
                for (let j = 0; j < message.uniqueKey.length; ++j)
                    object.uniqueKey[j] = message.uniqueKey[j];
            }
            return object;
        };

        /**
         * Converts this MergeLoadMode to JSON.
         * @function toJSON
         * @memberof dataform.MergeLoadMode
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        MergeLoadMode.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for MergeLoadMode
         * @function getTypeUrl
         * @memberof dataform.MergeLoadMode
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        MergeLoadMode.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.MergeLoadMode";
        };

        return MergeLoadMode;
    })();

    dataform.CompiledGraph = (function() {

        /**
         * Properties of a CompiledGraph.
         * @memberof dataform
         * @interface ICompiledGraph
         * @property {dataform.IProjectConfig|null} [projectConfig] CompiledGraph projectConfig
         * @property {Array.<dataform.ITable>|null} [tables] CompiledGraph tables
         * @property {Array.<dataform.IOperation>|null} [operations] CompiledGraph operations
         * @property {Array.<dataform.IAssertion>|null} [assertions] CompiledGraph assertions
         * @property {Array.<dataform.IDeclaration>|null} [declarations] CompiledGraph declarations
         * @property {Array.<dataform.ITest>|null} [tests] CompiledGraph tests
         * @property {Array.<dataform.INotebook>|null} [notebooks] CompiledGraph notebooks
         * @property {Array.<dataform.IDataPreparation>|null} [dataPreparations] CompiledGraph dataPreparations
         * @property {dataform.IGraphErrors|null} [graphErrors] CompiledGraph graphErrors
         * @property {string|null} [dataformCoreVersion] CompiledGraph dataformCoreVersion
         * @property {Array.<dataform.ITarget>|null} [targets] CompiledGraph targets
         * @property {google.protobuf.IStruct|null} [jitData] CompiledGraph jitData
         */

        /**
         * Constructs a new CompiledGraph.
         * @memberof dataform
         * @classdesc Represents a CompiledGraph.
         * @implements ICompiledGraph
         * @constructor
         * @param {dataform.ICompiledGraph=} [properties] Properties to set
         */
        function CompiledGraph(properties) {
            this.tables = [];
            this.operations = [];
            this.assertions = [];
            this.declarations = [];
            this.tests = [];
            this.notebooks = [];
            this.dataPreparations = [];
            this.targets = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CompiledGraph projectConfig.
         * @member {dataform.IProjectConfig|null|undefined} projectConfig
         * @memberof dataform.CompiledGraph
         * @instance
         */
        CompiledGraph.prototype.projectConfig = null;

        /**
         * CompiledGraph tables.
         * @member {Array.<dataform.ITable>} tables
         * @memberof dataform.CompiledGraph
         * @instance
         */
        CompiledGraph.prototype.tables = $util.emptyArray;

        /**
         * CompiledGraph operations.
         * @member {Array.<dataform.IOperation>} operations
         * @memberof dataform.CompiledGraph
         * @instance
         */
        CompiledGraph.prototype.operations = $util.emptyArray;

        /**
         * CompiledGraph assertions.
         * @member {Array.<dataform.IAssertion>} assertions
         * @memberof dataform.CompiledGraph
         * @instance
         */
        CompiledGraph.prototype.assertions = $util.emptyArray;

        /**
         * CompiledGraph declarations.
         * @member {Array.<dataform.IDeclaration>} declarations
         * @memberof dataform.CompiledGraph
         * @instance
         */
        CompiledGraph.prototype.declarations = $util.emptyArray;

        /**
         * CompiledGraph tests.
         * @member {Array.<dataform.ITest>} tests
         * @memberof dataform.CompiledGraph
         * @instance
         */
        CompiledGraph.prototype.tests = $util.emptyArray;

        /**
         * CompiledGraph notebooks.
         * @member {Array.<dataform.INotebook>} notebooks
         * @memberof dataform.CompiledGraph
         * @instance
         */
        CompiledGraph.prototype.notebooks = $util.emptyArray;

        /**
         * CompiledGraph dataPreparations.
         * @member {Array.<dataform.IDataPreparation>} dataPreparations
         * @memberof dataform.CompiledGraph
         * @instance
         */
        CompiledGraph.prototype.dataPreparations = $util.emptyArray;

        /**
         * CompiledGraph graphErrors.
         * @member {dataform.IGraphErrors|null|undefined} graphErrors
         * @memberof dataform.CompiledGraph
         * @instance
         */
        CompiledGraph.prototype.graphErrors = null;

        /**
         * CompiledGraph dataformCoreVersion.
         * @member {string} dataformCoreVersion
         * @memberof dataform.CompiledGraph
         * @instance
         */
        CompiledGraph.prototype.dataformCoreVersion = "";

        /**
         * CompiledGraph targets.
         * @member {Array.<dataform.ITarget>} targets
         * @memberof dataform.CompiledGraph
         * @instance
         */
        CompiledGraph.prototype.targets = $util.emptyArray;

        /**
         * CompiledGraph jitData.
         * @member {google.protobuf.IStruct|null|undefined} jitData
         * @memberof dataform.CompiledGraph
         * @instance
         */
        CompiledGraph.prototype.jitData = null;

        /**
         * Creates a new CompiledGraph instance using the specified properties.
         * @function create
         * @memberof dataform.CompiledGraph
         * @static
         * @param {dataform.ICompiledGraph=} [properties] Properties to set
         * @returns {dataform.CompiledGraph} CompiledGraph instance
         */
        CompiledGraph.create = function create(properties) {
            return new CompiledGraph(properties);
        };

        /**
         * Encodes the specified CompiledGraph message. Does not implicitly {@link dataform.CompiledGraph.verify|verify} messages.
         * @function encode
         * @memberof dataform.CompiledGraph
         * @static
         * @param {dataform.ICompiledGraph} message CompiledGraph message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompiledGraph.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.tables != null && message.tables.length)
                for (let i = 0; i < message.tables.length; ++i)
                    $root.dataform.Table.encode(message.tables[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.operations != null && message.operations.length)
                for (let i = 0; i < message.operations.length; ++i)
                    $root.dataform.Operation.encode(message.operations[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.assertions != null && message.assertions.length)
                for (let i = 0; i < message.assertions.length; ++i)
                    $root.dataform.Assertion.encode(message.assertions[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.projectConfig != null && Object.hasOwnProperty.call(message, "projectConfig"))
                $root.dataform.ProjectConfig.encode(message.projectConfig, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.graphErrors != null && Object.hasOwnProperty.call(message, "graphErrors"))
                $root.dataform.GraphErrors.encode(message.graphErrors, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            if (message.tests != null && message.tests.length)
                for (let i = 0; i < message.tests.length; ++i)
                    $root.dataform.Test.encode(message.tests[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
            if (message.declarations != null && message.declarations.length)
                for (let i = 0; i < message.declarations.length; ++i)
                    $root.dataform.Declaration.encode(message.declarations[i], writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
            if (message.dataformCoreVersion != null && Object.hasOwnProperty.call(message, "dataformCoreVersion"))
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.dataformCoreVersion);
            if (message.targets != null && message.targets.length)
                for (let i = 0; i < message.targets.length; ++i)
                    $root.dataform.Target.encode(message.targets[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
            if (message.notebooks != null && message.notebooks.length)
                for (let i = 0; i < message.notebooks.length; ++i)
                    $root.dataform.Notebook.encode(message.notebooks[i], writer.uint32(/* id 12, wireType 2 =*/98).fork(), q + 1).ldelim();
            if (message.dataPreparations != null && message.dataPreparations.length)
                for (let i = 0; i < message.dataPreparations.length; ++i)
                    $root.dataform.DataPreparation.encode(message.dataPreparations[i], writer.uint32(/* id 13, wireType 2 =*/106).fork(), q + 1).ldelim();
            if (message.jitData != null && Object.hasOwnProperty.call(message, "jitData"))
                $root.google.protobuf.Struct.encode(message.jitData, writer.uint32(/* id 15, wireType 2 =*/122).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CompiledGraph message, length delimited. Does not implicitly {@link dataform.CompiledGraph.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.CompiledGraph
         * @static
         * @param {dataform.ICompiledGraph} message CompiledGraph message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompiledGraph.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CompiledGraph message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.CompiledGraph
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.CompiledGraph} CompiledGraph
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompiledGraph.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.CompiledGraph();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 4: {
                        message.projectConfig = $root.dataform.ProjectConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 1: {
                        if (!(message.tables && message.tables.length))
                            message.tables = [];
                        message.tables.push($root.dataform.Table.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 2: {
                        if (!(message.operations && message.operations.length))
                            message.operations = [];
                        message.operations.push($root.dataform.Operation.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 3: {
                        if (!(message.assertions && message.assertions.length))
                            message.assertions = [];
                        message.assertions.push($root.dataform.Assertion.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 9: {
                        if (!(message.declarations && message.declarations.length))
                            message.declarations = [];
                        message.declarations.push($root.dataform.Declaration.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 8: {
                        if (!(message.tests && message.tests.length))
                            message.tests = [];
                        message.tests.push($root.dataform.Test.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 12: {
                        if (!(message.notebooks && message.notebooks.length))
                            message.notebooks = [];
                        message.notebooks.push($root.dataform.Notebook.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 13: {
                        if (!(message.dataPreparations && message.dataPreparations.length))
                            message.dataPreparations = [];
                        message.dataPreparations.push($root.dataform.DataPreparation.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 7: {
                        message.graphErrors = $root.dataform.GraphErrors.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 10: {
                        message.dataformCoreVersion = reader.string();
                        break;
                    }
                case 11: {
                        if (!(message.targets && message.targets.length))
                            message.targets = [];
                        message.targets.push($root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 15: {
                        message.jitData = $root.google.protobuf.Struct.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CompiledGraph message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.CompiledGraph
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.CompiledGraph} CompiledGraph
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompiledGraph.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CompiledGraph message.
         * @function verify
         * @memberof dataform.CompiledGraph
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CompiledGraph.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.projectConfig != null && Object.hasOwnProperty.call(message, "projectConfig")) {
                let error = $root.dataform.ProjectConfig.verify(message.projectConfig, long + 1);
                if (error)
                    return "projectConfig." + error;
            }
            if (message.tables != null && Object.hasOwnProperty.call(message, "tables")) {
                if (!Array.isArray(message.tables))
                    return "tables: array expected";
                for (let i = 0; i < message.tables.length; ++i) {
                    let error = $root.dataform.Table.verify(message.tables[i], long + 1);
                    if (error)
                        return "tables." + error;
                }
            }
            if (message.operations != null && Object.hasOwnProperty.call(message, "operations")) {
                if (!Array.isArray(message.operations))
                    return "operations: array expected";
                for (let i = 0; i < message.operations.length; ++i) {
                    let error = $root.dataform.Operation.verify(message.operations[i], long + 1);
                    if (error)
                        return "operations." + error;
                }
            }
            if (message.assertions != null && Object.hasOwnProperty.call(message, "assertions")) {
                if (!Array.isArray(message.assertions))
                    return "assertions: array expected";
                for (let i = 0; i < message.assertions.length; ++i) {
                    let error = $root.dataform.Assertion.verify(message.assertions[i], long + 1);
                    if (error)
                        return "assertions." + error;
                }
            }
            if (message.declarations != null && Object.hasOwnProperty.call(message, "declarations")) {
                if (!Array.isArray(message.declarations))
                    return "declarations: array expected";
                for (let i = 0; i < message.declarations.length; ++i) {
                    let error = $root.dataform.Declaration.verify(message.declarations[i], long + 1);
                    if (error)
                        return "declarations." + error;
                }
            }
            if (message.tests != null && Object.hasOwnProperty.call(message, "tests")) {
                if (!Array.isArray(message.tests))
                    return "tests: array expected";
                for (let i = 0; i < message.tests.length; ++i) {
                    let error = $root.dataform.Test.verify(message.tests[i], long + 1);
                    if (error)
                        return "tests." + error;
                }
            }
            if (message.notebooks != null && Object.hasOwnProperty.call(message, "notebooks")) {
                if (!Array.isArray(message.notebooks))
                    return "notebooks: array expected";
                for (let i = 0; i < message.notebooks.length; ++i) {
                    let error = $root.dataform.Notebook.verify(message.notebooks[i], long + 1);
                    if (error)
                        return "notebooks." + error;
                }
            }
            if (message.dataPreparations != null && Object.hasOwnProperty.call(message, "dataPreparations")) {
                if (!Array.isArray(message.dataPreparations))
                    return "dataPreparations: array expected";
                for (let i = 0; i < message.dataPreparations.length; ++i) {
                    let error = $root.dataform.DataPreparation.verify(message.dataPreparations[i], long + 1);
                    if (error)
                        return "dataPreparations." + error;
                }
            }
            if (message.graphErrors != null && Object.hasOwnProperty.call(message, "graphErrors")) {
                let error = $root.dataform.GraphErrors.verify(message.graphErrors, long + 1);
                if (error)
                    return "graphErrors." + error;
            }
            if (message.dataformCoreVersion != null && Object.hasOwnProperty.call(message, "dataformCoreVersion"))
                if (!$util.isString(message.dataformCoreVersion))
                    return "dataformCoreVersion: string expected";
            if (message.targets != null && Object.hasOwnProperty.call(message, "targets")) {
                if (!Array.isArray(message.targets))
                    return "targets: array expected";
                for (let i = 0; i < message.targets.length; ++i) {
                    let error = $root.dataform.Target.verify(message.targets[i], long + 1);
                    if (error)
                        return "targets." + error;
                }
            }
            if (message.jitData != null && Object.hasOwnProperty.call(message, "jitData")) {
                let error = $root.google.protobuf.Struct.verify(message.jitData, long + 1);
                if (error)
                    return "jitData." + error;
            }
            return null;
        };

        /**
         * Creates a CompiledGraph message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.CompiledGraph
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.CompiledGraph} CompiledGraph
         */
        CompiledGraph.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.CompiledGraph)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.CompiledGraph: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.CompiledGraph();
            if (object.projectConfig != null) {
                if (!$util.isObject(object.projectConfig))
                    throw TypeError(".dataform.CompiledGraph.projectConfig: object expected");
                message.projectConfig = $root.dataform.ProjectConfig.fromObject(object.projectConfig, long + 1);
            }
            if (object.tables) {
                if (!Array.isArray(object.tables))
                    throw TypeError(".dataform.CompiledGraph.tables: array expected");
                message.tables = [];
                for (let i = 0; i < object.tables.length; ++i) {
                    if (!$util.isObject(object.tables[i]))
                        throw TypeError(".dataform.CompiledGraph.tables: object expected");
                    message.tables[i] = $root.dataform.Table.fromObject(object.tables[i], long + 1);
                }
            }
            if (object.operations) {
                if (!Array.isArray(object.operations))
                    throw TypeError(".dataform.CompiledGraph.operations: array expected");
                message.operations = [];
                for (let i = 0; i < object.operations.length; ++i) {
                    if (!$util.isObject(object.operations[i]))
                        throw TypeError(".dataform.CompiledGraph.operations: object expected");
                    message.operations[i] = $root.dataform.Operation.fromObject(object.operations[i], long + 1);
                }
            }
            if (object.assertions) {
                if (!Array.isArray(object.assertions))
                    throw TypeError(".dataform.CompiledGraph.assertions: array expected");
                message.assertions = [];
                for (let i = 0; i < object.assertions.length; ++i) {
                    if (!$util.isObject(object.assertions[i]))
                        throw TypeError(".dataform.CompiledGraph.assertions: object expected");
                    message.assertions[i] = $root.dataform.Assertion.fromObject(object.assertions[i], long + 1);
                }
            }
            if (object.declarations) {
                if (!Array.isArray(object.declarations))
                    throw TypeError(".dataform.CompiledGraph.declarations: array expected");
                message.declarations = [];
                for (let i = 0; i < object.declarations.length; ++i) {
                    if (!$util.isObject(object.declarations[i]))
                        throw TypeError(".dataform.CompiledGraph.declarations: object expected");
                    message.declarations[i] = $root.dataform.Declaration.fromObject(object.declarations[i], long + 1);
                }
            }
            if (object.tests) {
                if (!Array.isArray(object.tests))
                    throw TypeError(".dataform.CompiledGraph.tests: array expected");
                message.tests = [];
                for (let i = 0; i < object.tests.length; ++i) {
                    if (!$util.isObject(object.tests[i]))
                        throw TypeError(".dataform.CompiledGraph.tests: object expected");
                    message.tests[i] = $root.dataform.Test.fromObject(object.tests[i], long + 1);
                }
            }
            if (object.notebooks) {
                if (!Array.isArray(object.notebooks))
                    throw TypeError(".dataform.CompiledGraph.notebooks: array expected");
                message.notebooks = [];
                for (let i = 0; i < object.notebooks.length; ++i) {
                    if (!$util.isObject(object.notebooks[i]))
                        throw TypeError(".dataform.CompiledGraph.notebooks: object expected");
                    message.notebooks[i] = $root.dataform.Notebook.fromObject(object.notebooks[i], long + 1);
                }
            }
            if (object.dataPreparations) {
                if (!Array.isArray(object.dataPreparations))
                    throw TypeError(".dataform.CompiledGraph.dataPreparations: array expected");
                message.dataPreparations = [];
                for (let i = 0; i < object.dataPreparations.length; ++i) {
                    if (!$util.isObject(object.dataPreparations[i]))
                        throw TypeError(".dataform.CompiledGraph.dataPreparations: object expected");
                    message.dataPreparations[i] = $root.dataform.DataPreparation.fromObject(object.dataPreparations[i], long + 1);
                }
            }
            if (object.graphErrors != null) {
                if (!$util.isObject(object.graphErrors))
                    throw TypeError(".dataform.CompiledGraph.graphErrors: object expected");
                message.graphErrors = $root.dataform.GraphErrors.fromObject(object.graphErrors, long + 1);
            }
            if (object.dataformCoreVersion != null)
                message.dataformCoreVersion = String(object.dataformCoreVersion);
            if (object.targets) {
                if (!Array.isArray(object.targets))
                    throw TypeError(".dataform.CompiledGraph.targets: array expected");
                message.targets = [];
                for (let i = 0; i < object.targets.length; ++i) {
                    if (!$util.isObject(object.targets[i]))
                        throw TypeError(".dataform.CompiledGraph.targets: object expected");
                    message.targets[i] = $root.dataform.Target.fromObject(object.targets[i], long + 1);
                }
            }
            if (object.jitData != null) {
                if (!$util.isObject(object.jitData))
                    throw TypeError(".dataform.CompiledGraph.jitData: object expected");
                message.jitData = $root.google.protobuf.Struct.fromObject(object.jitData, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CompiledGraph message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.CompiledGraph
         * @static
         * @param {dataform.CompiledGraph} message CompiledGraph
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CompiledGraph.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.tables = [];
                object.operations = [];
                object.assertions = [];
                object.tests = [];
                object.declarations = [];
                object.targets = [];
                object.notebooks = [];
                object.dataPreparations = [];
            }
            if (options.defaults) {
                object.projectConfig = null;
                object.graphErrors = null;
                object.dataformCoreVersion = "";
                object.jitData = null;
            }
            if (message.tables && message.tables.length) {
                object.tables = [];
                for (let j = 0; j < message.tables.length; ++j)
                    object.tables[j] = $root.dataform.Table.toObject(message.tables[j], options, q + 1);
            }
            if (message.operations && message.operations.length) {
                object.operations = [];
                for (let j = 0; j < message.operations.length; ++j)
                    object.operations[j] = $root.dataform.Operation.toObject(message.operations[j], options, q + 1);
            }
            if (message.assertions && message.assertions.length) {
                object.assertions = [];
                for (let j = 0; j < message.assertions.length; ++j)
                    object.assertions[j] = $root.dataform.Assertion.toObject(message.assertions[j], options, q + 1);
            }
            if (message.projectConfig != null && Object.hasOwnProperty.call(message, "projectConfig"))
                object.projectConfig = $root.dataform.ProjectConfig.toObject(message.projectConfig, options, q + 1);
            if (message.graphErrors != null && Object.hasOwnProperty.call(message, "graphErrors"))
                object.graphErrors = $root.dataform.GraphErrors.toObject(message.graphErrors, options, q + 1);
            if (message.tests && message.tests.length) {
                object.tests = [];
                for (let j = 0; j < message.tests.length; ++j)
                    object.tests[j] = $root.dataform.Test.toObject(message.tests[j], options, q + 1);
            }
            if (message.declarations && message.declarations.length) {
                object.declarations = [];
                for (let j = 0; j < message.declarations.length; ++j)
                    object.declarations[j] = $root.dataform.Declaration.toObject(message.declarations[j], options, q + 1);
            }
            if (message.dataformCoreVersion != null && Object.hasOwnProperty.call(message, "dataformCoreVersion"))
                object.dataformCoreVersion = message.dataformCoreVersion;
            if (message.targets && message.targets.length) {
                object.targets = [];
                for (let j = 0; j < message.targets.length; ++j)
                    object.targets[j] = $root.dataform.Target.toObject(message.targets[j], options, q + 1);
            }
            if (message.notebooks && message.notebooks.length) {
                object.notebooks = [];
                for (let j = 0; j < message.notebooks.length; ++j)
                    object.notebooks[j] = $root.dataform.Notebook.toObject(message.notebooks[j], options, q + 1);
            }
            if (message.dataPreparations && message.dataPreparations.length) {
                object.dataPreparations = [];
                for (let j = 0; j < message.dataPreparations.length; ++j)
                    object.dataPreparations[j] = $root.dataform.DataPreparation.toObject(message.dataPreparations[j], options, q + 1);
            }
            if (message.jitData != null && Object.hasOwnProperty.call(message, "jitData"))
                object.jitData = $root.google.protobuf.Struct.toObject(message.jitData, options, q + 1);
            return object;
        };

        /**
         * Converts this CompiledGraph to JSON.
         * @function toJSON
         * @memberof dataform.CompiledGraph
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CompiledGraph.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CompiledGraph
         * @function getTypeUrl
         * @memberof dataform.CompiledGraph
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CompiledGraph.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.CompiledGraph";
        };

        return CompiledGraph;
    })();

    dataform.CoreExecutionRequest = (function() {

        /**
         * Properties of a CoreExecutionRequest.
         * @memberof dataform
         * @interface ICoreExecutionRequest
         * @property {dataform.ICompileExecutionRequest|null} [compile] CoreExecutionRequest compile
         */

        /**
         * Constructs a new CoreExecutionRequest.
         * @memberof dataform
         * @classdesc Represents a CoreExecutionRequest.
         * @implements ICoreExecutionRequest
         * @constructor
         * @param {dataform.ICoreExecutionRequest=} [properties] Properties to set
         */
        function CoreExecutionRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CoreExecutionRequest compile.
         * @member {dataform.ICompileExecutionRequest|null|undefined} compile
         * @memberof dataform.CoreExecutionRequest
         * @instance
         */
        CoreExecutionRequest.prototype.compile = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * CoreExecutionRequest request.
         * @member {"compile"|undefined} request
         * @memberof dataform.CoreExecutionRequest
         * @instance
         */
        Object.defineProperty(CoreExecutionRequest.prototype, "request", {
            get: $util.oneOfGetter($oneOfFields = ["compile"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new CoreExecutionRequest instance using the specified properties.
         * @function create
         * @memberof dataform.CoreExecutionRequest
         * @static
         * @param {dataform.ICoreExecutionRequest=} [properties] Properties to set
         * @returns {dataform.CoreExecutionRequest} CoreExecutionRequest instance
         */
        CoreExecutionRequest.create = function create(properties) {
            return new CoreExecutionRequest(properties);
        };

        /**
         * Encodes the specified CoreExecutionRequest message. Does not implicitly {@link dataform.CoreExecutionRequest.verify|verify} messages.
         * @function encode
         * @memberof dataform.CoreExecutionRequest
         * @static
         * @param {dataform.ICoreExecutionRequest} message CoreExecutionRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CoreExecutionRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.compile != null && Object.hasOwnProperty.call(message, "compile"))
                $root.dataform.CompileExecutionRequest.encode(message.compile, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CoreExecutionRequest message, length delimited. Does not implicitly {@link dataform.CoreExecutionRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.CoreExecutionRequest
         * @static
         * @param {dataform.ICoreExecutionRequest} message CoreExecutionRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CoreExecutionRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CoreExecutionRequest message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.CoreExecutionRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.CoreExecutionRequest} CoreExecutionRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CoreExecutionRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.CoreExecutionRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.compile = $root.dataform.CompileExecutionRequest.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CoreExecutionRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.CoreExecutionRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.CoreExecutionRequest} CoreExecutionRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CoreExecutionRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CoreExecutionRequest message.
         * @function verify
         * @memberof dataform.CoreExecutionRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CoreExecutionRequest.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.compile != null && Object.hasOwnProperty.call(message, "compile")) {
                {
                    let error = $root.dataform.CompileExecutionRequest.verify(message.compile, long + 1);
                    if (error)
                        return "compile." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CoreExecutionRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.CoreExecutionRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.CoreExecutionRequest} CoreExecutionRequest
         */
        CoreExecutionRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.CoreExecutionRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.CoreExecutionRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.CoreExecutionRequest();
            if (object.compile != null) {
                if (!$util.isObject(object.compile))
                    throw TypeError(".dataform.CoreExecutionRequest.compile: object expected");
                message.compile = $root.dataform.CompileExecutionRequest.fromObject(object.compile, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CoreExecutionRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.CoreExecutionRequest
         * @static
         * @param {dataform.CoreExecutionRequest} message CoreExecutionRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CoreExecutionRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (message.compile != null && Object.hasOwnProperty.call(message, "compile")) {
                object.compile = $root.dataform.CompileExecutionRequest.toObject(message.compile, options, q + 1);
                if (options.oneofs)
                    object.request = "compile";
            }
            return object;
        };

        /**
         * Converts this CoreExecutionRequest to JSON.
         * @function toJSON
         * @memberof dataform.CoreExecutionRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CoreExecutionRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CoreExecutionRequest
         * @function getTypeUrl
         * @memberof dataform.CoreExecutionRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CoreExecutionRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.CoreExecutionRequest";
        };

        return CoreExecutionRequest;
    })();

    dataform.CoreExecutionResponse = (function() {

        /**
         * Properties of a CoreExecutionResponse.
         * @memberof dataform
         * @interface ICoreExecutionResponse
         * @property {dataform.ICompileExecutionResponse|null} [compile] CoreExecutionResponse compile
         */

        /**
         * Constructs a new CoreExecutionResponse.
         * @memberof dataform
         * @classdesc Represents a CoreExecutionResponse.
         * @implements ICoreExecutionResponse
         * @constructor
         * @param {dataform.ICoreExecutionResponse=} [properties] Properties to set
         */
        function CoreExecutionResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CoreExecutionResponse compile.
         * @member {dataform.ICompileExecutionResponse|null|undefined} compile
         * @memberof dataform.CoreExecutionResponse
         * @instance
         */
        CoreExecutionResponse.prototype.compile = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * CoreExecutionResponse response.
         * @member {"compile"|undefined} response
         * @memberof dataform.CoreExecutionResponse
         * @instance
         */
        Object.defineProperty(CoreExecutionResponse.prototype, "response", {
            get: $util.oneOfGetter($oneOfFields = ["compile"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new CoreExecutionResponse instance using the specified properties.
         * @function create
         * @memberof dataform.CoreExecutionResponse
         * @static
         * @param {dataform.ICoreExecutionResponse=} [properties] Properties to set
         * @returns {dataform.CoreExecutionResponse} CoreExecutionResponse instance
         */
        CoreExecutionResponse.create = function create(properties) {
            return new CoreExecutionResponse(properties);
        };

        /**
         * Encodes the specified CoreExecutionResponse message. Does not implicitly {@link dataform.CoreExecutionResponse.verify|verify} messages.
         * @function encode
         * @memberof dataform.CoreExecutionResponse
         * @static
         * @param {dataform.ICoreExecutionResponse} message CoreExecutionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CoreExecutionResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.compile != null && Object.hasOwnProperty.call(message, "compile"))
                $root.dataform.CompileExecutionResponse.encode(message.compile, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CoreExecutionResponse message, length delimited. Does not implicitly {@link dataform.CoreExecutionResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.CoreExecutionResponse
         * @static
         * @param {dataform.ICoreExecutionResponse} message CoreExecutionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CoreExecutionResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CoreExecutionResponse message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.CoreExecutionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.CoreExecutionResponse} CoreExecutionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CoreExecutionResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.CoreExecutionResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.compile = $root.dataform.CompileExecutionResponse.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CoreExecutionResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.CoreExecutionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.CoreExecutionResponse} CoreExecutionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CoreExecutionResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CoreExecutionResponse message.
         * @function verify
         * @memberof dataform.CoreExecutionResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CoreExecutionResponse.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.compile != null && Object.hasOwnProperty.call(message, "compile")) {
                {
                    let error = $root.dataform.CompileExecutionResponse.verify(message.compile, long + 1);
                    if (error)
                        return "compile." + error;
                }
            }
            return null;
        };

        /**
         * Creates a CoreExecutionResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.CoreExecutionResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.CoreExecutionResponse} CoreExecutionResponse
         */
        CoreExecutionResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.CoreExecutionResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.CoreExecutionResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.CoreExecutionResponse();
            if (object.compile != null) {
                if (!$util.isObject(object.compile))
                    throw TypeError(".dataform.CoreExecutionResponse.compile: object expected");
                message.compile = $root.dataform.CompileExecutionResponse.fromObject(object.compile, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CoreExecutionResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.CoreExecutionResponse
         * @static
         * @param {dataform.CoreExecutionResponse} message CoreExecutionResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CoreExecutionResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (message.compile != null && Object.hasOwnProperty.call(message, "compile")) {
                object.compile = $root.dataform.CompileExecutionResponse.toObject(message.compile, options, q + 1);
                if (options.oneofs)
                    object.response = "compile";
            }
            return object;
        };

        /**
         * Converts this CoreExecutionResponse to JSON.
         * @function toJSON
         * @memberof dataform.CoreExecutionResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CoreExecutionResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CoreExecutionResponse
         * @function getTypeUrl
         * @memberof dataform.CoreExecutionResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CoreExecutionResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.CoreExecutionResponse";
        };

        return CoreExecutionResponse;
    })();

    dataform.CompileExecutionRequest = (function() {

        /**
         * Properties of a CompileExecutionRequest.
         * @memberof dataform
         * @interface ICompileExecutionRequest
         * @property {dataform.ICompileConfig|null} [compileConfig] CompileExecutionRequest compileConfig
         */

        /**
         * Constructs a new CompileExecutionRequest.
         * @memberof dataform
         * @classdesc Represents a CompileExecutionRequest.
         * @implements ICompileExecutionRequest
         * @constructor
         * @param {dataform.ICompileExecutionRequest=} [properties] Properties to set
         */
        function CompileExecutionRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CompileExecutionRequest compileConfig.
         * @member {dataform.ICompileConfig|null|undefined} compileConfig
         * @memberof dataform.CompileExecutionRequest
         * @instance
         */
        CompileExecutionRequest.prototype.compileConfig = null;

        /**
         * Creates a new CompileExecutionRequest instance using the specified properties.
         * @function create
         * @memberof dataform.CompileExecutionRequest
         * @static
         * @param {dataform.ICompileExecutionRequest=} [properties] Properties to set
         * @returns {dataform.CompileExecutionRequest} CompileExecutionRequest instance
         */
        CompileExecutionRequest.create = function create(properties) {
            return new CompileExecutionRequest(properties);
        };

        /**
         * Encodes the specified CompileExecutionRequest message. Does not implicitly {@link dataform.CompileExecutionRequest.verify|verify} messages.
         * @function encode
         * @memberof dataform.CompileExecutionRequest
         * @static
         * @param {dataform.ICompileExecutionRequest} message CompileExecutionRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompileExecutionRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.compileConfig != null && Object.hasOwnProperty.call(message, "compileConfig"))
                $root.dataform.CompileConfig.encode(message.compileConfig, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CompileExecutionRequest message, length delimited. Does not implicitly {@link dataform.CompileExecutionRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.CompileExecutionRequest
         * @static
         * @param {dataform.ICompileExecutionRequest} message CompileExecutionRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompileExecutionRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CompileExecutionRequest message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.CompileExecutionRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.CompileExecutionRequest} CompileExecutionRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompileExecutionRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.CompileExecutionRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.compileConfig = $root.dataform.CompileConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CompileExecutionRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.CompileExecutionRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.CompileExecutionRequest} CompileExecutionRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompileExecutionRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CompileExecutionRequest message.
         * @function verify
         * @memberof dataform.CompileExecutionRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CompileExecutionRequest.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.compileConfig != null && Object.hasOwnProperty.call(message, "compileConfig")) {
                let error = $root.dataform.CompileConfig.verify(message.compileConfig, long + 1);
                if (error)
                    return "compileConfig." + error;
            }
            return null;
        };

        /**
         * Creates a CompileExecutionRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.CompileExecutionRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.CompileExecutionRequest} CompileExecutionRequest
         */
        CompileExecutionRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.CompileExecutionRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.CompileExecutionRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.CompileExecutionRequest();
            if (object.compileConfig != null) {
                if (!$util.isObject(object.compileConfig))
                    throw TypeError(".dataform.CompileExecutionRequest.compileConfig: object expected");
                message.compileConfig = $root.dataform.CompileConfig.fromObject(object.compileConfig, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CompileExecutionRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.CompileExecutionRequest
         * @static
         * @param {dataform.CompileExecutionRequest} message CompileExecutionRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CompileExecutionRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.compileConfig = null;
            if (message.compileConfig != null && Object.hasOwnProperty.call(message, "compileConfig"))
                object.compileConfig = $root.dataform.CompileConfig.toObject(message.compileConfig, options, q + 1);
            return object;
        };

        /**
         * Converts this CompileExecutionRequest to JSON.
         * @function toJSON
         * @memberof dataform.CompileExecutionRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CompileExecutionRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CompileExecutionRequest
         * @function getTypeUrl
         * @memberof dataform.CompileExecutionRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CompileExecutionRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.CompileExecutionRequest";
        };

        return CompileExecutionRequest;
    })();

    dataform.CompileExecutionResponse = (function() {

        /**
         * Properties of a CompileExecutionResponse.
         * @memberof dataform
         * @interface ICompileExecutionResponse
         * @property {dataform.ICompiledGraph|null} [compiledGraph] CompileExecutionResponse compiledGraph
         */

        /**
         * Constructs a new CompileExecutionResponse.
         * @memberof dataform
         * @classdesc Represents a CompileExecutionResponse.
         * @implements ICompileExecutionResponse
         * @constructor
         * @param {dataform.ICompileExecutionResponse=} [properties] Properties to set
         */
        function CompileExecutionResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * CompileExecutionResponse compiledGraph.
         * @member {dataform.ICompiledGraph|null|undefined} compiledGraph
         * @memberof dataform.CompileExecutionResponse
         * @instance
         */
        CompileExecutionResponse.prototype.compiledGraph = null;

        /**
         * Creates a new CompileExecutionResponse instance using the specified properties.
         * @function create
         * @memberof dataform.CompileExecutionResponse
         * @static
         * @param {dataform.ICompileExecutionResponse=} [properties] Properties to set
         * @returns {dataform.CompileExecutionResponse} CompileExecutionResponse instance
         */
        CompileExecutionResponse.create = function create(properties) {
            return new CompileExecutionResponse(properties);
        };

        /**
         * Encodes the specified CompileExecutionResponse message. Does not implicitly {@link dataform.CompileExecutionResponse.verify|verify} messages.
         * @function encode
         * @memberof dataform.CompileExecutionResponse
         * @static
         * @param {dataform.ICompileExecutionResponse} message CompileExecutionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompileExecutionResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.compiledGraph != null && Object.hasOwnProperty.call(message, "compiledGraph"))
                $root.dataform.CompiledGraph.encode(message.compiledGraph, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified CompileExecutionResponse message, length delimited. Does not implicitly {@link dataform.CompileExecutionResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.CompileExecutionResponse
         * @static
         * @param {dataform.ICompileExecutionResponse} message CompileExecutionResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        CompileExecutionResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a CompileExecutionResponse message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.CompileExecutionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.CompileExecutionResponse} CompileExecutionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompileExecutionResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.CompileExecutionResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.compiledGraph = $root.dataform.CompiledGraph.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a CompileExecutionResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.CompileExecutionResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.CompileExecutionResponse} CompileExecutionResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        CompileExecutionResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a CompileExecutionResponse message.
         * @function verify
         * @memberof dataform.CompileExecutionResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        CompileExecutionResponse.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.compiledGraph != null && Object.hasOwnProperty.call(message, "compiledGraph")) {
                let error = $root.dataform.CompiledGraph.verify(message.compiledGraph, long + 1);
                if (error)
                    return "compiledGraph." + error;
            }
            return null;
        };

        /**
         * Creates a CompileExecutionResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.CompileExecutionResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.CompileExecutionResponse} CompileExecutionResponse
         */
        CompileExecutionResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.CompileExecutionResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.CompileExecutionResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.CompileExecutionResponse();
            if (object.compiledGraph != null) {
                if (!$util.isObject(object.compiledGraph))
                    throw TypeError(".dataform.CompileExecutionResponse.compiledGraph: object expected");
                message.compiledGraph = $root.dataform.CompiledGraph.fromObject(object.compiledGraph, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a CompileExecutionResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.CompileExecutionResponse
         * @static
         * @param {dataform.CompileExecutionResponse} message CompileExecutionResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        CompileExecutionResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.compiledGraph = null;
            if (message.compiledGraph != null && Object.hasOwnProperty.call(message, "compiledGraph"))
                object.compiledGraph = $root.dataform.CompiledGraph.toObject(message.compiledGraph, options, q + 1);
            return object;
        };

        /**
         * Converts this CompileExecutionResponse to JSON.
         * @function toJSON
         * @memberof dataform.CompileExecutionResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        CompileExecutionResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for CompileExecutionResponse
         * @function getTypeUrl
         * @memberof dataform.CompileExecutionResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        CompileExecutionResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.CompileExecutionResponse";
        };

        return CompileExecutionResponse;
    })();

    /**
     * SupportedFeatures enum.
     * @name dataform.SupportedFeatures
     * @enum {number}
     * @property {number} UNKNOWN_FEATURE=0 UNKNOWN_FEATURE value
     * @property {number} ARRAY_BUFFER_IPC=1 ARRAY_BUFFER_IPC value
     */
    dataform.SupportedFeatures = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN_FEATURE"] = 0;
        values[valuesById[1] = "ARRAY_BUFFER_IPC"] = 1;
        return values;
    })();

    dataform.Field = (function() {

        /**
         * Properties of a Field.
         * @memberof dataform
         * @interface IField
         * @property {string|null} [name] Field name
         * @property {Array.<dataform.Field.Flag>|null} [flags] Field flags
         * @property {dataform.Field.Primitive|null} [primitive] Field primitive
         * @property {dataform.IFields|null} [struct] Field struct
         * @property {string|null} [description] Field description
         */

        /**
         * Constructs a new Field.
         * @memberof dataform
         * @classdesc Represents a Field.
         * @implements IField
         * @constructor
         * @param {dataform.IField=} [properties] Properties to set
         */
        function Field(properties) {
            this.flags = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Field name.
         * @member {string} name
         * @memberof dataform.Field
         * @instance
         */
        Field.prototype.name = "";

        /**
         * Field flags.
         * @member {Array.<dataform.Field.Flag>} flags
         * @memberof dataform.Field
         * @instance
         */
        Field.prototype.flags = $util.emptyArray;

        /**
         * Field primitive.
         * @member {dataform.Field.Primitive|null|undefined} primitive
         * @memberof dataform.Field
         * @instance
         */
        Field.prototype.primitive = null;

        /**
         * Field struct.
         * @member {dataform.IFields|null|undefined} struct
         * @memberof dataform.Field
         * @instance
         */
        Field.prototype.struct = null;

        /**
         * Field description.
         * @member {string} description
         * @memberof dataform.Field
         * @instance
         */
        Field.prototype.description = "";

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * Field type.
         * @member {"primitive"|"struct"|undefined} type
         * @memberof dataform.Field
         * @instance
         */
        Object.defineProperty(Field.prototype, "type", {
            get: $util.oneOfGetter($oneOfFields = ["primitive", "struct"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new Field instance using the specified properties.
         * @function create
         * @memberof dataform.Field
         * @static
         * @param {dataform.IField=} [properties] Properties to set
         * @returns {dataform.Field} Field instance
         */
        Field.create = function create(properties) {
            return new Field(properties);
        };

        /**
         * Encodes the specified Field message. Does not implicitly {@link dataform.Field.verify|verify} messages.
         * @function encode
         * @memberof dataform.Field
         * @static
         * @param {dataform.IField} message Field message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Field.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.struct != null && Object.hasOwnProperty.call(message, "struct"))
                $root.dataform.Fields.encode(message.struct, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.description);
            if (message.flags != null && message.flags.length) {
                writer.uint32(/* id 6, wireType 2 =*/50).fork();
                for (let i = 0; i < message.flags.length; ++i)
                    writer.int32(message.flags[i]);
                writer.ldelim();
            }
            if (message.primitive != null && Object.hasOwnProperty.call(message, "primitive"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.primitive);
            return writer;
        };

        /**
         * Encodes the specified Field message, length delimited. Does not implicitly {@link dataform.Field.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.Field
         * @static
         * @param {dataform.IField} message Field message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Field.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Field message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.Field
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.Field} Field
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Field.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.Field();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 6: {
                        if (!(message.flags && message.flags.length))
                            message.flags = [];
                        if ((tag & 7) === 2) {
                            let end2 = reader.uint32() + reader.pos;
                            while (reader.pos < end2)
                                message.flags.push(reader.int32());
                        } else
                            message.flags.push(reader.int32());
                        break;
                    }
                case 7: {
                        message.primitive = reader.int32();
                        break;
                    }
                case 3: {
                        message.struct = $root.dataform.Fields.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 5: {
                        message.description = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Field message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.Field
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.Field} Field
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Field.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Field message.
         * @function verify
         * @memberof dataform.Field
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Field.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            let properties = {};
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.flags != null && Object.hasOwnProperty.call(message, "flags")) {
                if (!Array.isArray(message.flags))
                    return "flags: array expected";
                for (let i = 0; i < message.flags.length; ++i)
                    switch (message.flags[i]) {
                    default:
                        return "flags: enum value[] expected";
                    case 0:
                    case 1:
                        break;
                    }
            }
            if (message.primitive != null && Object.hasOwnProperty.call(message, "primitive")) {
                properties.type = 1;
                switch (message.primitive) {
                default:
                    return "primitive: enum value expected";
                case 0:
                case 1:
                case 2:
                case 5:
                case 3:
                case 4:
                case 6:
                case 7:
                case 8:
                case 9:
                case 10:
                case 11:
                case 12:
                case 13:
                case 14:
                case 15:
                    break;
                }
            }
            if (message.struct != null && Object.hasOwnProperty.call(message, "struct")) {
                if (properties.type === 1)
                    return "type: multiple values";
                properties.type = 1;
                {
                    let error = $root.dataform.Fields.verify(message.struct, long + 1);
                    if (error)
                        return "struct." + error;
                }
            }
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                if (!$util.isString(message.description))
                    return "description: string expected";
            return null;
        };

        /**
         * Creates a Field message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.Field
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.Field} Field
         */
        Field.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.Field)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.Field: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.Field();
            if (object.name != null)
                message.name = String(object.name);
            if (object.flags) {
                if (!Array.isArray(object.flags))
                    throw TypeError(".dataform.Field.flags: array expected");
                message.flags = [];
                for (let i = 0; i < object.flags.length; ++i)
                    switch (object.flags[i]) {
                    default:
                        if (typeof object.flags[i] === "number") {
                            message.flags[i] = object.flags[i];
                            break;
                        }
                    case "UNKNOWN_FLAG":
                    case 0:
                        message.flags[i] = 0;
                        break;
                    case "REPEATED":
                    case 1:
                        message.flags[i] = 1;
                        break;
                    }
            }
            switch (object.primitive) {
            default:
                if (typeof object.primitive === "number") {
                    message.primitive = object.primitive;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                message.primitive = 0;
                break;
            case "INTEGER":
            case 1:
                message.primitive = 1;
                break;
            case "FLOAT":
            case 2:
                message.primitive = 2;
                break;
            case "NUMERIC":
            case 5:
                message.primitive = 5;
                break;
            case "BOOLEAN":
            case 3:
                message.primitive = 3;
                break;
            case "STRING":
            case 4:
                message.primitive = 4;
                break;
            case "DATE":
            case 6:
                message.primitive = 6;
                break;
            case "DATETIME":
            case 7:
                message.primitive = 7;
                break;
            case "TIMESTAMP":
            case 8:
                message.primitive = 8;
                break;
            case "TIME":
            case 9:
                message.primitive = 9;
                break;
            case "BYTES":
            case 10:
                message.primitive = 10;
                break;
            case "ANY":
            case 11:
                message.primitive = 11;
                break;
            case "GEOGRAPHY":
            case 12:
                message.primitive = 12;
                break;
            case "BIGNUMERIC":
            case 13:
                message.primitive = 13;
                break;
            case "JSON":
            case 14:
                message.primitive = 14;
                break;
            case "INTERVAL":
            case 15:
                message.primitive = 15;
                break;
            }
            if (object.struct != null) {
                if (!$util.isObject(object.struct))
                    throw TypeError(".dataform.Field.struct: object expected");
                message.struct = $root.dataform.Fields.fromObject(object.struct, long + 1);
            }
            if (object.description != null)
                message.description = String(object.description);
            return message;
        };

        /**
         * Creates a plain object from a Field message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.Field
         * @static
         * @param {dataform.Field} message Field
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Field.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.flags = [];
            if (options.defaults) {
                object.name = "";
                object.description = "";
            }
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.struct != null && Object.hasOwnProperty.call(message, "struct")) {
                object.struct = $root.dataform.Fields.toObject(message.struct, options, q + 1);
                if (options.oneofs)
                    object.type = "struct";
            }
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                object.description = message.description;
            if (message.flags && message.flags.length) {
                object.flags = [];
                for (let j = 0; j < message.flags.length; ++j)
                    object.flags[j] = options.enums === String ? $root.dataform.Field.Flag[message.flags[j]] === undefined ? message.flags[j] : $root.dataform.Field.Flag[message.flags[j]] : message.flags[j];
            }
            if (message.primitive != null && Object.hasOwnProperty.call(message, "primitive")) {
                object.primitive = options.enums === String ? $root.dataform.Field.Primitive[message.primitive] === undefined ? message.primitive : $root.dataform.Field.Primitive[message.primitive] : message.primitive;
                if (options.oneofs)
                    object.type = "primitive";
            }
            return object;
        };

        /**
         * Converts this Field to JSON.
         * @function toJSON
         * @memberof dataform.Field
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Field.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Field
         * @function getTypeUrl
         * @memberof dataform.Field
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Field.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.Field";
        };

        /**
         * Primitive enum.
         * @name dataform.Field.Primitive
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} INTEGER=1 INTEGER value
         * @property {number} FLOAT=2 FLOAT value
         * @property {number} NUMERIC=5 NUMERIC value
         * @property {number} BOOLEAN=3 BOOLEAN value
         * @property {number} STRING=4 STRING value
         * @property {number} DATE=6 DATE value
         * @property {number} DATETIME=7 DATETIME value
         * @property {number} TIMESTAMP=8 TIMESTAMP value
         * @property {number} TIME=9 TIME value
         * @property {number} BYTES=10 BYTES value
         * @property {number} ANY=11 ANY value
         * @property {number} GEOGRAPHY=12 GEOGRAPHY value
         * @property {number} BIGNUMERIC=13 BIGNUMERIC value
         * @property {number} JSON=14 JSON value
         * @property {number} INTERVAL=15 INTERVAL value
         */
        Field.Primitive = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "INTEGER"] = 1;
            values[valuesById[2] = "FLOAT"] = 2;
            values[valuesById[5] = "NUMERIC"] = 5;
            values[valuesById[3] = "BOOLEAN"] = 3;
            values[valuesById[4] = "STRING"] = 4;
            values[valuesById[6] = "DATE"] = 6;
            values[valuesById[7] = "DATETIME"] = 7;
            values[valuesById[8] = "TIMESTAMP"] = 8;
            values[valuesById[9] = "TIME"] = 9;
            values[valuesById[10] = "BYTES"] = 10;
            values[valuesById[11] = "ANY"] = 11;
            values[valuesById[12] = "GEOGRAPHY"] = 12;
            values[valuesById[13] = "BIGNUMERIC"] = 13;
            values[valuesById[14] = "JSON"] = 14;
            values[valuesById[15] = "INTERVAL"] = 15;
            return values;
        })();

        /**
         * Flag enum.
         * @name dataform.Field.Flag
         * @enum {number}
         * @property {number} UNKNOWN_FLAG=0 UNKNOWN_FLAG value
         * @property {number} REPEATED=1 REPEATED value
         */
        Field.Flag = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_FLAG"] = 0;
            values[valuesById[1] = "REPEATED"] = 1;
            return values;
        })();

        return Field;
    })();

    dataform.Fields = (function() {

        /**
         * Properties of a Fields.
         * @memberof dataform
         * @interface IFields
         * @property {Array.<dataform.IField>|null} [fields] Fields fields
         */

        /**
         * Constructs a new Fields.
         * @memberof dataform
         * @classdesc Represents a Fields.
         * @implements IFields
         * @constructor
         * @param {dataform.IFields=} [properties] Properties to set
         */
        function Fields(properties) {
            this.fields = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Fields fields.
         * @member {Array.<dataform.IField>} fields
         * @memberof dataform.Fields
         * @instance
         */
        Fields.prototype.fields = $util.emptyArray;

        /**
         * Creates a new Fields instance using the specified properties.
         * @function create
         * @memberof dataform.Fields
         * @static
         * @param {dataform.IFields=} [properties] Properties to set
         * @returns {dataform.Fields} Fields instance
         */
        Fields.create = function create(properties) {
            return new Fields(properties);
        };

        /**
         * Encodes the specified Fields message. Does not implicitly {@link dataform.Fields.verify|verify} messages.
         * @function encode
         * @memberof dataform.Fields
         * @static
         * @param {dataform.IFields} message Fields message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Fields.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.fields != null && message.fields.length)
                for (let i = 0; i < message.fields.length; ++i)
                    $root.dataform.Field.encode(message.fields[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified Fields message, length delimited. Does not implicitly {@link dataform.Fields.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.Fields
         * @static
         * @param {dataform.IFields} message Fields message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Fields.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Fields message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.Fields
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.Fields} Fields
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Fields.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.Fields();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.fields && message.fields.length))
                            message.fields = [];
                        message.fields.push($root.dataform.Field.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Fields message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.Fields
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.Fields} Fields
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Fields.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Fields message.
         * @function verify
         * @memberof dataform.Fields
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Fields.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.fields != null && Object.hasOwnProperty.call(message, "fields")) {
                if (!Array.isArray(message.fields))
                    return "fields: array expected";
                for (let i = 0; i < message.fields.length; ++i) {
                    let error = $root.dataform.Field.verify(message.fields[i], long + 1);
                    if (error)
                        return "fields." + error;
                }
            }
            return null;
        };

        /**
         * Creates a Fields message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.Fields
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.Fields} Fields
         */
        Fields.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.Fields)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.Fields: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.Fields();
            if (object.fields) {
                if (!Array.isArray(object.fields))
                    throw TypeError(".dataform.Fields.fields: array expected");
                message.fields = [];
                for (let i = 0; i < object.fields.length; ++i) {
                    if (!$util.isObject(object.fields[i]))
                        throw TypeError(".dataform.Fields.fields: object expected");
                    message.fields[i] = $root.dataform.Field.fromObject(object.fields[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a Fields message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.Fields
         * @static
         * @param {dataform.Fields} message Fields
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Fields.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.fields = [];
            if (message.fields && message.fields.length) {
                object.fields = [];
                for (let j = 0; j < message.fields.length; ++j)
                    object.fields[j] = $root.dataform.Field.toObject(message.fields[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this Fields to JSON.
         * @function toJSON
         * @memberof dataform.Fields
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Fields.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Fields
         * @function getTypeUrl
         * @memberof dataform.Fields
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Fields.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.Fields";
        };

        return Fields;
    })();

    dataform.TableMetadata = (function() {

        /**
         * Properties of a TableMetadata.
         * @memberof dataform
         * @interface ITableMetadata
         * @property {dataform.ITarget|null} [target] TableMetadata target
         * @property {dataform.TableMetadata.Type|null} [type] TableMetadata type
         * @property {Array.<dataform.IField>|null} [fields] TableMetadata fields
         * @property {string|null} [description] TableMetadata description
         * @property {Object.<string,string>|null} [labels] TableMetadata labels
         * @property {Long|null} [lastUpdatedMillis] TableMetadata lastUpdatedMillis
         * @property {dataform.TableMetadata.IBigQuery|null} [bigquery] TableMetadata bigquery
         */

        /**
         * Constructs a new TableMetadata.
         * @memberof dataform
         * @classdesc Represents a TableMetadata.
         * @implements ITableMetadata
         * @constructor
         * @param {dataform.ITableMetadata=} [properties] Properties to set
         */
        function TableMetadata(properties) {
            this.fields = [];
            this.labels = {};
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TableMetadata target.
         * @member {dataform.ITarget|null|undefined} target
         * @memberof dataform.TableMetadata
         * @instance
         */
        TableMetadata.prototype.target = null;

        /**
         * TableMetadata type.
         * @member {dataform.TableMetadata.Type} type
         * @memberof dataform.TableMetadata
         * @instance
         */
        TableMetadata.prototype.type = 0;

        /**
         * TableMetadata fields.
         * @member {Array.<dataform.IField>} fields
         * @memberof dataform.TableMetadata
         * @instance
         */
        TableMetadata.prototype.fields = $util.emptyArray;

        /**
         * TableMetadata description.
         * @member {string} description
         * @memberof dataform.TableMetadata
         * @instance
         */
        TableMetadata.prototype.description = "";

        /**
         * TableMetadata labels.
         * @member {Object.<string,string>} labels
         * @memberof dataform.TableMetadata
         * @instance
         */
        TableMetadata.prototype.labels = $util.emptyObject;

        /**
         * TableMetadata lastUpdatedMillis.
         * @member {Long} lastUpdatedMillis
         * @memberof dataform.TableMetadata
         * @instance
         */
        TableMetadata.prototype.lastUpdatedMillis = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * TableMetadata bigquery.
         * @member {dataform.TableMetadata.IBigQuery|null|undefined} bigquery
         * @memberof dataform.TableMetadata
         * @instance
         */
        TableMetadata.prototype.bigquery = null;

        /**
         * Creates a new TableMetadata instance using the specified properties.
         * @function create
         * @memberof dataform.TableMetadata
         * @static
         * @param {dataform.ITableMetadata=} [properties] Properties to set
         * @returns {dataform.TableMetadata} TableMetadata instance
         */
        TableMetadata.create = function create(properties) {
            return new TableMetadata(properties);
        };

        /**
         * Encodes the specified TableMetadata message. Does not implicitly {@link dataform.TableMetadata.verify|verify} messages.
         * @function encode
         * @memberof dataform.TableMetadata
         * @static
         * @param {dataform.ITableMetadata} message TableMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TableMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                $root.dataform.Target.encode(message.target, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.fields != null && message.fields.length)
                for (let i = 0; i < message.fields.length; ++i)
                    $root.dataform.Field.encode(message.fields[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.lastUpdatedMillis != null && Object.hasOwnProperty.call(message, "lastUpdatedMillis"))
                writer.uint32(/* id 4, wireType 0 =*/32).int64(message.lastUpdatedMillis);
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.description);
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 6, wireType 0 =*/48).int32(message.type);
            if (message.labels != null && Object.hasOwnProperty.call(message, "labels"))
                for (let keys = Object.keys(message.labels), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 7, wireType 2 =*/58).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.labels[keys[i]]).ldelim();
            if (message.bigquery != null && Object.hasOwnProperty.call(message, "bigquery"))
                $root.dataform.TableMetadata.BigQuery.encode(message.bigquery, writer.uint32(/* id 8, wireType 2 =*/66).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified TableMetadata message, length delimited. Does not implicitly {@link dataform.TableMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.TableMetadata
         * @static
         * @param {dataform.ITableMetadata} message TableMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TableMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TableMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.TableMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.TableMetadata} TableMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TableMetadata.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.TableMetadata(), key, value;
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.target = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 6: {
                        message.type = reader.int32();
                        break;
                    }
                case 3: {
                        if (!(message.fields && message.fields.length))
                            message.fields = [];
                        message.fields.push($root.dataform.Field.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 5: {
                        message.description = reader.string();
                        break;
                    }
                case 7: {
                        if (message.labels === $util.emptyObject)
                            message.labels = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = "";
                        while (reader.pos < end2) {
                            let tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.string();
                                break;
                            case 2:
                                value = reader.string();
                                break;
                            default:
                                reader.skipType(tag2 & 7, long);
                                break;
                            }
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.labels, key);
                        message.labels[key] = value;
                        break;
                    }
                case 4: {
                        message.lastUpdatedMillis = reader.int64();
                        break;
                    }
                case 8: {
                        message.bigquery = $root.dataform.TableMetadata.BigQuery.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TableMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.TableMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.TableMetadata} TableMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TableMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TableMetadata message.
         * @function verify
         * @memberof dataform.TableMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TableMetadata.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.target != null && Object.hasOwnProperty.call(message, "target")) {
                let error = $root.dataform.Target.verify(message.target, long + 1);
                if (error)
                    return "target." + error;
            }
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                switch (message.type) {
                default:
                    return "type: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            if (message.fields != null && Object.hasOwnProperty.call(message, "fields")) {
                if (!Array.isArray(message.fields))
                    return "fields: array expected";
                for (let i = 0; i < message.fields.length; ++i) {
                    let error = $root.dataform.Field.verify(message.fields[i], long + 1);
                    if (error)
                        return "fields." + error;
                }
            }
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                if (!$util.isString(message.description))
                    return "description: string expected";
            if (message.labels != null && Object.hasOwnProperty.call(message, "labels")) {
                if (!$util.isObject(message.labels))
                    return "labels: object expected";
                let key = Object.keys(message.labels);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isString(message.labels[key[i]]))
                        return "labels: string{k:string} expected";
            }
            if (message.lastUpdatedMillis != null && Object.hasOwnProperty.call(message, "lastUpdatedMillis"))
                if (!$util.isInteger(message.lastUpdatedMillis) && !(message.lastUpdatedMillis && $util.isInteger(message.lastUpdatedMillis.low) && $util.isInteger(message.lastUpdatedMillis.high)))
                    return "lastUpdatedMillis: integer|Long expected";
            if (message.bigquery != null && Object.hasOwnProperty.call(message, "bigquery")) {
                let error = $root.dataform.TableMetadata.BigQuery.verify(message.bigquery, long + 1);
                if (error)
                    return "bigquery." + error;
            }
            return null;
        };

        /**
         * Creates a TableMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.TableMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.TableMetadata} TableMetadata
         */
        TableMetadata.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.TableMetadata)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.TableMetadata: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.TableMetadata();
            if (object.target != null) {
                if (!$util.isObject(object.target))
                    throw TypeError(".dataform.TableMetadata.target: object expected");
                message.target = $root.dataform.Target.fromObject(object.target, long + 1);
            }
            switch (object.type) {
            default:
                if (typeof object.type === "number") {
                    message.type = object.type;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                message.type = 0;
                break;
            case "TABLE":
            case 1:
                message.type = 1;
                break;
            case "VIEW":
            case 2:
                message.type = 2;
                break;
            }
            if (object.fields) {
                if (!Array.isArray(object.fields))
                    throw TypeError(".dataform.TableMetadata.fields: array expected");
                message.fields = [];
                for (let i = 0; i < object.fields.length; ++i) {
                    if (!$util.isObject(object.fields[i]))
                        throw TypeError(".dataform.TableMetadata.fields: object expected");
                    message.fields[i] = $root.dataform.Field.fromObject(object.fields[i], long + 1);
                }
            }
            if (object.description != null)
                message.description = String(object.description);
            if (object.labels) {
                if (!$util.isObject(object.labels))
                    throw TypeError(".dataform.TableMetadata.labels: object expected");
                message.labels = {};
                for (let keys = Object.keys(object.labels), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.labels, keys[i]);
                    message.labels[keys[i]] = String(object.labels[keys[i]]);
                }
            }
            if (object.lastUpdatedMillis != null)
                if ($util.Long)
                    message.lastUpdatedMillis = $util.Long.fromValue(object.lastUpdatedMillis, false);
                else if (typeof object.lastUpdatedMillis === "string")
                    message.lastUpdatedMillis = parseInt(object.lastUpdatedMillis, 10);
                else if (typeof object.lastUpdatedMillis === "number")
                    message.lastUpdatedMillis = object.lastUpdatedMillis;
                else if (typeof object.lastUpdatedMillis === "object")
                    message.lastUpdatedMillis = new $util.LongBits(object.lastUpdatedMillis.low >>> 0, object.lastUpdatedMillis.high >>> 0).toNumber();
            if (object.bigquery != null) {
                if (!$util.isObject(object.bigquery))
                    throw TypeError(".dataform.TableMetadata.bigquery: object expected");
                message.bigquery = $root.dataform.TableMetadata.BigQuery.fromObject(object.bigquery, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a TableMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.TableMetadata
         * @static
         * @param {dataform.TableMetadata} message TableMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TableMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.fields = [];
            if (options.objects || options.defaults)
                object.labels = {};
            if (options.defaults) {
                object.target = null;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.lastUpdatedMillis = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.lastUpdatedMillis = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.description = "";
                object.type = options.enums === String ? "UNKNOWN" : 0;
                object.bigquery = null;
            }
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                object.target = $root.dataform.Target.toObject(message.target, options, q + 1);
            if (message.fields && message.fields.length) {
                object.fields = [];
                for (let j = 0; j < message.fields.length; ++j)
                    object.fields[j] = $root.dataform.Field.toObject(message.fields[j], options, q + 1);
            }
            if (message.lastUpdatedMillis != null && Object.hasOwnProperty.call(message, "lastUpdatedMillis"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.lastUpdatedMillis = typeof message.lastUpdatedMillis === "number" ? BigInt(message.lastUpdatedMillis) : $util.Long.fromBits(message.lastUpdatedMillis.low >>> 0, message.lastUpdatedMillis.high >>> 0, false).toBigInt();
                else if (typeof message.lastUpdatedMillis === "number")
                    object.lastUpdatedMillis = options.longs === String ? String(message.lastUpdatedMillis) : message.lastUpdatedMillis;
                else
                    object.lastUpdatedMillis = options.longs === String ? $util.Long.prototype.toString.call(message.lastUpdatedMillis) : options.longs === Number ? new $util.LongBits(message.lastUpdatedMillis.low >>> 0, message.lastUpdatedMillis.high >>> 0).toNumber() : message.lastUpdatedMillis;
            if (message.description != null && Object.hasOwnProperty.call(message, "description"))
                object.description = message.description;
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                object.type = options.enums === String ? $root.dataform.TableMetadata.Type[message.type] === undefined ? message.type : $root.dataform.TableMetadata.Type[message.type] : message.type;
            let keys2;
            if (message.labels && (keys2 = Object.keys(message.labels)).length) {
                object.labels = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.labels, keys2[j]);
                    object.labels[keys2[j]] = message.labels[keys2[j]];
                }
            }
            if (message.bigquery != null && Object.hasOwnProperty.call(message, "bigquery"))
                object.bigquery = $root.dataform.TableMetadata.BigQuery.toObject(message.bigquery, options, q + 1);
            return object;
        };

        /**
         * Converts this TableMetadata to JSON.
         * @function toJSON
         * @memberof dataform.TableMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TableMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TableMetadata
         * @function getTypeUrl
         * @memberof dataform.TableMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TableMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.TableMetadata";
        };

        /**
         * Type enum.
         * @name dataform.TableMetadata.Type
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} TABLE=1 TABLE value
         * @property {number} VIEW=2 VIEW value
         */
        TableMetadata.Type = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "TABLE"] = 1;
            values[valuesById[2] = "VIEW"] = 2;
            return values;
        })();

        TableMetadata.BigQuery = (function() {

            /**
             * Properties of a BigQuery.
             * @memberof dataform.TableMetadata
             * @interface IBigQuery
             * @property {boolean|null} [hasStreamingBuffer] BigQuery hasStreamingBuffer
             */

            /**
             * Constructs a new BigQuery.
             * @memberof dataform.TableMetadata
             * @classdesc Represents a BigQuery.
             * @implements IBigQuery
             * @constructor
             * @param {dataform.TableMetadata.IBigQuery=} [properties] Properties to set
             */
            function BigQuery(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BigQuery hasStreamingBuffer.
             * @member {boolean} hasStreamingBuffer
             * @memberof dataform.TableMetadata.BigQuery
             * @instance
             */
            BigQuery.prototype.hasStreamingBuffer = false;

            /**
             * Creates a new BigQuery instance using the specified properties.
             * @function create
             * @memberof dataform.TableMetadata.BigQuery
             * @static
             * @param {dataform.TableMetadata.IBigQuery=} [properties] Properties to set
             * @returns {dataform.TableMetadata.BigQuery} BigQuery instance
             */
            BigQuery.create = function create(properties) {
                return new BigQuery(properties);
            };

            /**
             * Encodes the specified BigQuery message. Does not implicitly {@link dataform.TableMetadata.BigQuery.verify|verify} messages.
             * @function encode
             * @memberof dataform.TableMetadata.BigQuery
             * @static
             * @param {dataform.TableMetadata.IBigQuery} message BigQuery message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BigQuery.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.hasStreamingBuffer != null && Object.hasOwnProperty.call(message, "hasStreamingBuffer"))
                    writer.uint32(/* id 1, wireType 0 =*/8).bool(message.hasStreamingBuffer);
                return writer;
            };

            /**
             * Encodes the specified BigQuery message, length delimited. Does not implicitly {@link dataform.TableMetadata.BigQuery.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.TableMetadata.BigQuery
             * @static
             * @param {dataform.TableMetadata.IBigQuery} message BigQuery message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BigQuery.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a BigQuery message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.TableMetadata.BigQuery
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.TableMetadata.BigQuery} BigQuery
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BigQuery.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.TableMetadata.BigQuery();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.hasStreamingBuffer = reader.bool();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a BigQuery message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.TableMetadata.BigQuery
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.TableMetadata.BigQuery} BigQuery
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BigQuery.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BigQuery message.
             * @function verify
             * @memberof dataform.TableMetadata.BigQuery
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BigQuery.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.hasStreamingBuffer != null && Object.hasOwnProperty.call(message, "hasStreamingBuffer"))
                    if (typeof message.hasStreamingBuffer !== "boolean")
                        return "hasStreamingBuffer: boolean expected";
                return null;
            };

            /**
             * Creates a BigQuery message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.TableMetadata.BigQuery
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.TableMetadata.BigQuery} BigQuery
             */
            BigQuery.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.TableMetadata.BigQuery)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.TableMetadata.BigQuery: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.TableMetadata.BigQuery();
                if (object.hasStreamingBuffer != null)
                    message.hasStreamingBuffer = Boolean(object.hasStreamingBuffer);
                return message;
            };

            /**
             * Creates a plain object from a BigQuery message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.TableMetadata.BigQuery
             * @static
             * @param {dataform.TableMetadata.BigQuery} message BigQuery
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BigQuery.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults)
                    object.hasStreamingBuffer = false;
                if (message.hasStreamingBuffer != null && Object.hasOwnProperty.call(message, "hasStreamingBuffer"))
                    object.hasStreamingBuffer = message.hasStreamingBuffer;
                return object;
            };

            /**
             * Converts this BigQuery to JSON.
             * @function toJSON
             * @memberof dataform.TableMetadata.BigQuery
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BigQuery.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BigQuery
             * @function getTypeUrl
             * @memberof dataform.TableMetadata.BigQuery
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BigQuery.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.TableMetadata.BigQuery";
            };

            return BigQuery;
        })();

        return TableMetadata;
    })();

    dataform.ExecutionMetadata = (function() {

        /**
         * Properties of an ExecutionMetadata.
         * @memberof dataform
         * @interface IExecutionMetadata
         * @property {dataform.ExecutionMetadata.IBigqueryMetadata|null} [bigquery] ExecutionMetadata bigquery
         */

        /**
         * Constructs a new ExecutionMetadata.
         * @memberof dataform
         * @classdesc Represents an ExecutionMetadata.
         * @implements IExecutionMetadata
         * @constructor
         * @param {dataform.IExecutionMetadata=} [properties] Properties to set
         */
        function ExecutionMetadata(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ExecutionMetadata bigquery.
         * @member {dataform.ExecutionMetadata.IBigqueryMetadata|null|undefined} bigquery
         * @memberof dataform.ExecutionMetadata
         * @instance
         */
        ExecutionMetadata.prototype.bigquery = null;

        /**
         * Creates a new ExecutionMetadata instance using the specified properties.
         * @function create
         * @memberof dataform.ExecutionMetadata
         * @static
         * @param {dataform.IExecutionMetadata=} [properties] Properties to set
         * @returns {dataform.ExecutionMetadata} ExecutionMetadata instance
         */
        ExecutionMetadata.create = function create(properties) {
            return new ExecutionMetadata(properties);
        };

        /**
         * Encodes the specified ExecutionMetadata message. Does not implicitly {@link dataform.ExecutionMetadata.verify|verify} messages.
         * @function encode
         * @memberof dataform.ExecutionMetadata
         * @static
         * @param {dataform.IExecutionMetadata} message ExecutionMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExecutionMetadata.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.bigquery != null && Object.hasOwnProperty.call(message, "bigquery"))
                $root.dataform.ExecutionMetadata.BigqueryMetadata.encode(message.bigquery, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ExecutionMetadata message, length delimited. Does not implicitly {@link dataform.ExecutionMetadata.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.ExecutionMetadata
         * @static
         * @param {dataform.IExecutionMetadata} message ExecutionMetadata message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExecutionMetadata.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ExecutionMetadata message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.ExecutionMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.ExecutionMetadata} ExecutionMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExecutionMetadata.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ExecutionMetadata();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.bigquery = $root.dataform.ExecutionMetadata.BigqueryMetadata.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ExecutionMetadata message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.ExecutionMetadata
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.ExecutionMetadata} ExecutionMetadata
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExecutionMetadata.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExecutionMetadata message.
         * @function verify
         * @memberof dataform.ExecutionMetadata
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExecutionMetadata.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.bigquery != null && Object.hasOwnProperty.call(message, "bigquery")) {
                let error = $root.dataform.ExecutionMetadata.BigqueryMetadata.verify(message.bigquery, long + 1);
                if (error)
                    return "bigquery." + error;
            }
            return null;
        };

        /**
         * Creates an ExecutionMetadata message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.ExecutionMetadata
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.ExecutionMetadata} ExecutionMetadata
         */
        ExecutionMetadata.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.ExecutionMetadata)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.ExecutionMetadata: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.ExecutionMetadata();
            if (object.bigquery != null) {
                if (!$util.isObject(object.bigquery))
                    throw TypeError(".dataform.ExecutionMetadata.bigquery: object expected");
                message.bigquery = $root.dataform.ExecutionMetadata.BigqueryMetadata.fromObject(object.bigquery, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an ExecutionMetadata message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.ExecutionMetadata
         * @static
         * @param {dataform.ExecutionMetadata} message ExecutionMetadata
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExecutionMetadata.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.bigquery = null;
            if (message.bigquery != null && Object.hasOwnProperty.call(message, "bigquery"))
                object.bigquery = $root.dataform.ExecutionMetadata.BigqueryMetadata.toObject(message.bigquery, options, q + 1);
            return object;
        };

        /**
         * Converts this ExecutionMetadata to JSON.
         * @function toJSON
         * @memberof dataform.ExecutionMetadata
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExecutionMetadata.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ExecutionMetadata
         * @function getTypeUrl
         * @memberof dataform.ExecutionMetadata
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ExecutionMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.ExecutionMetadata";
        };

        ExecutionMetadata.BigqueryMetadata = (function() {

            /**
             * Properties of a BigqueryMetadata.
             * @memberof dataform.ExecutionMetadata
             * @interface IBigqueryMetadata
             * @property {string|null} [jobId] BigqueryMetadata jobId
             * @property {Long|null} [totalBytesProcessed] BigqueryMetadata totalBytesProcessed
             * @property {Long|null} [totalBytesBilled] BigqueryMetadata totalBytesBilled
             */

            /**
             * Constructs a new BigqueryMetadata.
             * @memberof dataform.ExecutionMetadata
             * @classdesc Represents a BigqueryMetadata.
             * @implements IBigqueryMetadata
             * @constructor
             * @param {dataform.ExecutionMetadata.IBigqueryMetadata=} [properties] Properties to set
             */
            function BigqueryMetadata(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * BigqueryMetadata jobId.
             * @member {string} jobId
             * @memberof dataform.ExecutionMetadata.BigqueryMetadata
             * @instance
             */
            BigqueryMetadata.prototype.jobId = "";

            /**
             * BigqueryMetadata totalBytesProcessed.
             * @member {Long} totalBytesProcessed
             * @memberof dataform.ExecutionMetadata.BigqueryMetadata
             * @instance
             */
            BigqueryMetadata.prototype.totalBytesProcessed = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * BigqueryMetadata totalBytesBilled.
             * @member {Long} totalBytesBilled
             * @memberof dataform.ExecutionMetadata.BigqueryMetadata
             * @instance
             */
            BigqueryMetadata.prototype.totalBytesBilled = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Creates a new BigqueryMetadata instance using the specified properties.
             * @function create
             * @memberof dataform.ExecutionMetadata.BigqueryMetadata
             * @static
             * @param {dataform.ExecutionMetadata.IBigqueryMetadata=} [properties] Properties to set
             * @returns {dataform.ExecutionMetadata.BigqueryMetadata} BigqueryMetadata instance
             */
            BigqueryMetadata.create = function create(properties) {
                return new BigqueryMetadata(properties);
            };

            /**
             * Encodes the specified BigqueryMetadata message. Does not implicitly {@link dataform.ExecutionMetadata.BigqueryMetadata.verify|verify} messages.
             * @function encode
             * @memberof dataform.ExecutionMetadata.BigqueryMetadata
             * @static
             * @param {dataform.ExecutionMetadata.IBigqueryMetadata} message BigqueryMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BigqueryMetadata.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.jobId != null && Object.hasOwnProperty.call(message, "jobId"))
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.jobId);
                if (message.totalBytesProcessed != null && Object.hasOwnProperty.call(message, "totalBytesProcessed"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int64(message.totalBytesProcessed);
                if (message.totalBytesBilled != null && Object.hasOwnProperty.call(message, "totalBytesBilled"))
                    writer.uint32(/* id 3, wireType 0 =*/24).int64(message.totalBytesBilled);
                return writer;
            };

            /**
             * Encodes the specified BigqueryMetadata message, length delimited. Does not implicitly {@link dataform.ExecutionMetadata.BigqueryMetadata.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.ExecutionMetadata.BigqueryMetadata
             * @static
             * @param {dataform.ExecutionMetadata.IBigqueryMetadata} message BigqueryMetadata message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            BigqueryMetadata.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a BigqueryMetadata message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.ExecutionMetadata.BigqueryMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.ExecutionMetadata.BigqueryMetadata} BigqueryMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BigqueryMetadata.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ExecutionMetadata.BigqueryMetadata();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.jobId = reader.string();
                            break;
                        }
                    case 2: {
                            message.totalBytesProcessed = reader.int64();
                            break;
                        }
                    case 3: {
                            message.totalBytesBilled = reader.int64();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a BigqueryMetadata message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.ExecutionMetadata.BigqueryMetadata
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.ExecutionMetadata.BigqueryMetadata} BigqueryMetadata
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            BigqueryMetadata.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a BigqueryMetadata message.
             * @function verify
             * @memberof dataform.ExecutionMetadata.BigqueryMetadata
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            BigqueryMetadata.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.jobId != null && Object.hasOwnProperty.call(message, "jobId"))
                    if (!$util.isString(message.jobId))
                        return "jobId: string expected";
                if (message.totalBytesProcessed != null && Object.hasOwnProperty.call(message, "totalBytesProcessed"))
                    if (!$util.isInteger(message.totalBytesProcessed) && !(message.totalBytesProcessed && $util.isInteger(message.totalBytesProcessed.low) && $util.isInteger(message.totalBytesProcessed.high)))
                        return "totalBytesProcessed: integer|Long expected";
                if (message.totalBytesBilled != null && Object.hasOwnProperty.call(message, "totalBytesBilled"))
                    if (!$util.isInteger(message.totalBytesBilled) && !(message.totalBytesBilled && $util.isInteger(message.totalBytesBilled.low) && $util.isInteger(message.totalBytesBilled.high)))
                        return "totalBytesBilled: integer|Long expected";
                return null;
            };

            /**
             * Creates a BigqueryMetadata message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.ExecutionMetadata.BigqueryMetadata
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.ExecutionMetadata.BigqueryMetadata} BigqueryMetadata
             */
            BigqueryMetadata.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.ExecutionMetadata.BigqueryMetadata)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.ExecutionMetadata.BigqueryMetadata: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.ExecutionMetadata.BigqueryMetadata();
                if (object.jobId != null)
                    message.jobId = String(object.jobId);
                if (object.totalBytesProcessed != null)
                    if ($util.Long)
                        message.totalBytesProcessed = $util.Long.fromValue(object.totalBytesProcessed, false);
                    else if (typeof object.totalBytesProcessed === "string")
                        message.totalBytesProcessed = parseInt(object.totalBytesProcessed, 10);
                    else if (typeof object.totalBytesProcessed === "number")
                        message.totalBytesProcessed = object.totalBytesProcessed;
                    else if (typeof object.totalBytesProcessed === "object")
                        message.totalBytesProcessed = new $util.LongBits(object.totalBytesProcessed.low >>> 0, object.totalBytesProcessed.high >>> 0).toNumber();
                if (object.totalBytesBilled != null)
                    if ($util.Long)
                        message.totalBytesBilled = $util.Long.fromValue(object.totalBytesBilled, false);
                    else if (typeof object.totalBytesBilled === "string")
                        message.totalBytesBilled = parseInt(object.totalBytesBilled, 10);
                    else if (typeof object.totalBytesBilled === "number")
                        message.totalBytesBilled = object.totalBytesBilled;
                    else if (typeof object.totalBytesBilled === "object")
                        message.totalBytesBilled = new $util.LongBits(object.totalBytesBilled.low >>> 0, object.totalBytesBilled.high >>> 0).toNumber();
                return message;
            };

            /**
             * Creates a plain object from a BigqueryMetadata message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.ExecutionMetadata.BigqueryMetadata
             * @static
             * @param {dataform.ExecutionMetadata.BigqueryMetadata} message BigqueryMetadata
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            BigqueryMetadata.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.jobId = "";
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.totalBytesProcessed = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.totalBytesProcessed = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.totalBytesBilled = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.totalBytesBilled = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                }
                if (message.jobId != null && Object.hasOwnProperty.call(message, "jobId"))
                    object.jobId = message.jobId;
                if (message.totalBytesProcessed != null && Object.hasOwnProperty.call(message, "totalBytesProcessed"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.totalBytesProcessed = typeof message.totalBytesProcessed === "number" ? BigInt(message.totalBytesProcessed) : $util.Long.fromBits(message.totalBytesProcessed.low >>> 0, message.totalBytesProcessed.high >>> 0, false).toBigInt();
                    else if (typeof message.totalBytesProcessed === "number")
                        object.totalBytesProcessed = options.longs === String ? String(message.totalBytesProcessed) : message.totalBytesProcessed;
                    else
                        object.totalBytesProcessed = options.longs === String ? $util.Long.prototype.toString.call(message.totalBytesProcessed) : options.longs === Number ? new $util.LongBits(message.totalBytesProcessed.low >>> 0, message.totalBytesProcessed.high >>> 0).toNumber() : message.totalBytesProcessed;
                if (message.totalBytesBilled != null && Object.hasOwnProperty.call(message, "totalBytesBilled"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.totalBytesBilled = typeof message.totalBytesBilled === "number" ? BigInt(message.totalBytesBilled) : $util.Long.fromBits(message.totalBytesBilled.low >>> 0, message.totalBytesBilled.high >>> 0, false).toBigInt();
                    else if (typeof message.totalBytesBilled === "number")
                        object.totalBytesBilled = options.longs === String ? String(message.totalBytesBilled) : message.totalBytesBilled;
                    else
                        object.totalBytesBilled = options.longs === String ? $util.Long.prototype.toString.call(message.totalBytesBilled) : options.longs === Number ? new $util.LongBits(message.totalBytesBilled.low >>> 0, message.totalBytesBilled.high >>> 0).toNumber() : message.totalBytesBilled;
                return object;
            };

            /**
             * Converts this BigqueryMetadata to JSON.
             * @function toJSON
             * @memberof dataform.ExecutionMetadata.BigqueryMetadata
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            BigqueryMetadata.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for BigqueryMetadata
             * @function getTypeUrl
             * @memberof dataform.ExecutionMetadata.BigqueryMetadata
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            BigqueryMetadata.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.ExecutionMetadata.BigqueryMetadata";
            };

            return BigqueryMetadata;
        })();

        return ExecutionMetadata;
    })();

    dataform.QueryEvaluationError = (function() {

        /**
         * Properties of a QueryEvaluationError.
         * @memberof dataform
         * @interface IQueryEvaluationError
         * @property {string|null} [message] QueryEvaluationError message
         * @property {dataform.QueryEvaluationError.IErrorLocation|null} [errorLocation] QueryEvaluationError errorLocation
         */

        /**
         * Constructs a new QueryEvaluationError.
         * @memberof dataform
         * @classdesc Represents a QueryEvaluationError.
         * @implements IQueryEvaluationError
         * @constructor
         * @param {dataform.IQueryEvaluationError=} [properties] Properties to set
         */
        function QueryEvaluationError(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * QueryEvaluationError message.
         * @member {string} message
         * @memberof dataform.QueryEvaluationError
         * @instance
         */
        QueryEvaluationError.prototype.message = "";

        /**
         * QueryEvaluationError errorLocation.
         * @member {dataform.QueryEvaluationError.IErrorLocation|null|undefined} errorLocation
         * @memberof dataform.QueryEvaluationError
         * @instance
         */
        QueryEvaluationError.prototype.errorLocation = null;

        /**
         * Creates a new QueryEvaluationError instance using the specified properties.
         * @function create
         * @memberof dataform.QueryEvaluationError
         * @static
         * @param {dataform.IQueryEvaluationError=} [properties] Properties to set
         * @returns {dataform.QueryEvaluationError} QueryEvaluationError instance
         */
        QueryEvaluationError.create = function create(properties) {
            return new QueryEvaluationError(properties);
        };

        /**
         * Encodes the specified QueryEvaluationError message. Does not implicitly {@link dataform.QueryEvaluationError.verify|verify} messages.
         * @function encode
         * @memberof dataform.QueryEvaluationError
         * @static
         * @param {dataform.IQueryEvaluationError} message QueryEvaluationError message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryEvaluationError.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.message);
            if (message.errorLocation != null && Object.hasOwnProperty.call(message, "errorLocation"))
                $root.dataform.QueryEvaluationError.ErrorLocation.encode(message.errorLocation, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified QueryEvaluationError message, length delimited. Does not implicitly {@link dataform.QueryEvaluationError.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.QueryEvaluationError
         * @static
         * @param {dataform.IQueryEvaluationError} message QueryEvaluationError message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryEvaluationError.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a QueryEvaluationError message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.QueryEvaluationError
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.QueryEvaluationError} QueryEvaluationError
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryEvaluationError.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.QueryEvaluationError();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.message = reader.string();
                        break;
                    }
                case 2: {
                        message.errorLocation = $root.dataform.QueryEvaluationError.ErrorLocation.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a QueryEvaluationError message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.QueryEvaluationError
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.QueryEvaluationError} QueryEvaluationError
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryEvaluationError.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a QueryEvaluationError message.
         * @function verify
         * @memberof dataform.QueryEvaluationError
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        QueryEvaluationError.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                if (!$util.isString(message.message))
                    return "message: string expected";
            if (message.errorLocation != null && Object.hasOwnProperty.call(message, "errorLocation")) {
                let error = $root.dataform.QueryEvaluationError.ErrorLocation.verify(message.errorLocation, long + 1);
                if (error)
                    return "errorLocation." + error;
            }
            return null;
        };

        /**
         * Creates a QueryEvaluationError message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.QueryEvaluationError
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.QueryEvaluationError} QueryEvaluationError
         */
        QueryEvaluationError.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.QueryEvaluationError)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.QueryEvaluationError: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.QueryEvaluationError();
            if (object.message != null)
                message.message = String(object.message);
            if (object.errorLocation != null) {
                if (!$util.isObject(object.errorLocation))
                    throw TypeError(".dataform.QueryEvaluationError.errorLocation: object expected");
                message.errorLocation = $root.dataform.QueryEvaluationError.ErrorLocation.fromObject(object.errorLocation, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a QueryEvaluationError message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.QueryEvaluationError
         * @static
         * @param {dataform.QueryEvaluationError} message QueryEvaluationError
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        QueryEvaluationError.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.message = "";
                object.errorLocation = null;
            }
            if (message.message != null && Object.hasOwnProperty.call(message, "message"))
                object.message = message.message;
            if (message.errorLocation != null && Object.hasOwnProperty.call(message, "errorLocation"))
                object.errorLocation = $root.dataform.QueryEvaluationError.ErrorLocation.toObject(message.errorLocation, options, q + 1);
            return object;
        };

        /**
         * Converts this QueryEvaluationError to JSON.
         * @function toJSON
         * @memberof dataform.QueryEvaluationError
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        QueryEvaluationError.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for QueryEvaluationError
         * @function getTypeUrl
         * @memberof dataform.QueryEvaluationError
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        QueryEvaluationError.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.QueryEvaluationError";
        };

        QueryEvaluationError.ErrorLocation = (function() {

            /**
             * Properties of an ErrorLocation.
             * @memberof dataform.QueryEvaluationError
             * @interface IErrorLocation
             * @property {number|null} [line] ErrorLocation line
             * @property {number|null} [column] ErrorLocation column
             */

            /**
             * Constructs a new ErrorLocation.
             * @memberof dataform.QueryEvaluationError
             * @classdesc Represents an ErrorLocation.
             * @implements IErrorLocation
             * @constructor
             * @param {dataform.QueryEvaluationError.IErrorLocation=} [properties] Properties to set
             */
            function ErrorLocation(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ErrorLocation line.
             * @member {number} line
             * @memberof dataform.QueryEvaluationError.ErrorLocation
             * @instance
             */
            ErrorLocation.prototype.line = 0;

            /**
             * ErrorLocation column.
             * @member {number} column
             * @memberof dataform.QueryEvaluationError.ErrorLocation
             * @instance
             */
            ErrorLocation.prototype.column = 0;

            /**
             * Creates a new ErrorLocation instance using the specified properties.
             * @function create
             * @memberof dataform.QueryEvaluationError.ErrorLocation
             * @static
             * @param {dataform.QueryEvaluationError.IErrorLocation=} [properties] Properties to set
             * @returns {dataform.QueryEvaluationError.ErrorLocation} ErrorLocation instance
             */
            ErrorLocation.create = function create(properties) {
                return new ErrorLocation(properties);
            };

            /**
             * Encodes the specified ErrorLocation message. Does not implicitly {@link dataform.QueryEvaluationError.ErrorLocation.verify|verify} messages.
             * @function encode
             * @memberof dataform.QueryEvaluationError.ErrorLocation
             * @static
             * @param {dataform.QueryEvaluationError.IErrorLocation} message ErrorLocation message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ErrorLocation.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.line != null && Object.hasOwnProperty.call(message, "line"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.line);
                if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.column);
                return writer;
            };

            /**
             * Encodes the specified ErrorLocation message, length delimited. Does not implicitly {@link dataform.QueryEvaluationError.ErrorLocation.verify|verify} messages.
             * @function encodeDelimited
             * @memberof dataform.QueryEvaluationError.ErrorLocation
             * @static
             * @param {dataform.QueryEvaluationError.IErrorLocation} message ErrorLocation message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ErrorLocation.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an ErrorLocation message from the specified reader or buffer.
             * @function decode
             * @memberof dataform.QueryEvaluationError.ErrorLocation
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {dataform.QueryEvaluationError.ErrorLocation} ErrorLocation
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ErrorLocation.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.QueryEvaluationError.ErrorLocation();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.line = reader.int32();
                            break;
                        }
                    case 2: {
                            message.column = reader.int32();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an ErrorLocation message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof dataform.QueryEvaluationError.ErrorLocation
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {dataform.QueryEvaluationError.ErrorLocation} ErrorLocation
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ErrorLocation.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an ErrorLocation message.
             * @function verify
             * @memberof dataform.QueryEvaluationError.ErrorLocation
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ErrorLocation.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.line != null && Object.hasOwnProperty.call(message, "line"))
                    if (!$util.isInteger(message.line))
                        return "line: integer expected";
                if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                    if (!$util.isInteger(message.column))
                        return "column: integer expected";
                return null;
            };

            /**
             * Creates an ErrorLocation message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof dataform.QueryEvaluationError.ErrorLocation
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {dataform.QueryEvaluationError.ErrorLocation} ErrorLocation
             */
            ErrorLocation.fromObject = function fromObject(object, long) {
                if (object instanceof $root.dataform.QueryEvaluationError.ErrorLocation)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".dataform.QueryEvaluationError.ErrorLocation: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.dataform.QueryEvaluationError.ErrorLocation();
                if (object.line != null)
                    message.line = object.line | 0;
                if (object.column != null)
                    message.column = object.column | 0;
                return message;
            };

            /**
             * Creates a plain object from an ErrorLocation message. Also converts values to other types if specified.
             * @function toObject
             * @memberof dataform.QueryEvaluationError.ErrorLocation
             * @static
             * @param {dataform.QueryEvaluationError.ErrorLocation} message ErrorLocation
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ErrorLocation.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    object.line = 0;
                    object.column = 0;
                }
                if (message.line != null && Object.hasOwnProperty.call(message, "line"))
                    object.line = message.line;
                if (message.column != null && Object.hasOwnProperty.call(message, "column"))
                    object.column = message.column;
                return object;
            };

            /**
             * Converts this ErrorLocation to JSON.
             * @function toJSON
             * @memberof dataform.QueryEvaluationError.ErrorLocation
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ErrorLocation.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ErrorLocation
             * @function getTypeUrl
             * @memberof dataform.QueryEvaluationError.ErrorLocation
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ErrorLocation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/dataform.QueryEvaluationError.ErrorLocation";
            };

            return ErrorLocation;
        })();

        return QueryEvaluationError;
    })();

    dataform.QueryEvaluation = (function() {

        /**
         * Properties of a QueryEvaluation.
         * @memberof dataform
         * @interface IQueryEvaluation
         * @property {dataform.QueryEvaluation.QueryEvaluationStatus|null} [status] QueryEvaluation status
         * @property {dataform.IQueryEvaluationError|null} [error] QueryEvaluation error
         * @property {boolean|null} [incremental] QueryEvaluation incremental
         * @property {string|null} [query] QueryEvaluation query
         */

        /**
         * Constructs a new QueryEvaluation.
         * @memberof dataform
         * @classdesc Represents a QueryEvaluation.
         * @implements IQueryEvaluation
         * @constructor
         * @param {dataform.IQueryEvaluation=} [properties] Properties to set
         */
        function QueryEvaluation(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * QueryEvaluation status.
         * @member {dataform.QueryEvaluation.QueryEvaluationStatus} status
         * @memberof dataform.QueryEvaluation
         * @instance
         */
        QueryEvaluation.prototype.status = 0;

        /**
         * QueryEvaluation error.
         * @member {dataform.IQueryEvaluationError|null|undefined} error
         * @memberof dataform.QueryEvaluation
         * @instance
         */
        QueryEvaluation.prototype.error = null;

        /**
         * QueryEvaluation incremental.
         * @member {boolean} incremental
         * @memberof dataform.QueryEvaluation
         * @instance
         */
        QueryEvaluation.prototype.incremental = false;

        /**
         * QueryEvaluation query.
         * @member {string} query
         * @memberof dataform.QueryEvaluation
         * @instance
         */
        QueryEvaluation.prototype.query = "";

        /**
         * Creates a new QueryEvaluation instance using the specified properties.
         * @function create
         * @memberof dataform.QueryEvaluation
         * @static
         * @param {dataform.IQueryEvaluation=} [properties] Properties to set
         * @returns {dataform.QueryEvaluation} QueryEvaluation instance
         */
        QueryEvaluation.create = function create(properties) {
            return new QueryEvaluation(properties);
        };

        /**
         * Encodes the specified QueryEvaluation message. Does not implicitly {@link dataform.QueryEvaluation.verify|verify} messages.
         * @function encode
         * @memberof dataform.QueryEvaluation
         * @static
         * @param {dataform.IQueryEvaluation} message QueryEvaluation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryEvaluation.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.status);
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                $root.dataform.QueryEvaluationError.encode(message.error, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.incremental != null && Object.hasOwnProperty.call(message, "incremental"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.incremental);
            if (message.query != null && Object.hasOwnProperty.call(message, "query"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.query);
            return writer;
        };

        /**
         * Encodes the specified QueryEvaluation message, length delimited. Does not implicitly {@link dataform.QueryEvaluation.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.QueryEvaluation
         * @static
         * @param {dataform.IQueryEvaluation} message QueryEvaluation message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        QueryEvaluation.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a QueryEvaluation message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.QueryEvaluation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.QueryEvaluation} QueryEvaluation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryEvaluation.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.QueryEvaluation();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.status = reader.int32();
                        break;
                    }
                case 2: {
                        message.error = $root.dataform.QueryEvaluationError.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 3: {
                        message.incremental = reader.bool();
                        break;
                    }
                case 4: {
                        message.query = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a QueryEvaluation message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.QueryEvaluation
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.QueryEvaluation} QueryEvaluation
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        QueryEvaluation.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a QueryEvaluation message.
         * @function verify
         * @memberof dataform.QueryEvaluation
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        QueryEvaluation.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                switch (message.status) {
                default:
                    return "status: enum value expected";
                case 0:
                case 1:
                    break;
                }
            if (message.error != null && Object.hasOwnProperty.call(message, "error")) {
                let error = $root.dataform.QueryEvaluationError.verify(message.error, long + 1);
                if (error)
                    return "error." + error;
            }
            if (message.incremental != null && Object.hasOwnProperty.call(message, "incremental"))
                if (typeof message.incremental !== "boolean")
                    return "incremental: boolean expected";
            if (message.query != null && Object.hasOwnProperty.call(message, "query"))
                if (!$util.isString(message.query))
                    return "query: string expected";
            return null;
        };

        /**
         * Creates a QueryEvaluation message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.QueryEvaluation
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.QueryEvaluation} QueryEvaluation
         */
        QueryEvaluation.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.QueryEvaluation)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.QueryEvaluation: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.QueryEvaluation();
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "SUCCESS":
            case 0:
                message.status = 0;
                break;
            case "FAILURE":
            case 1:
                message.status = 1;
                break;
            }
            if (object.error != null) {
                if (!$util.isObject(object.error))
                    throw TypeError(".dataform.QueryEvaluation.error: object expected");
                message.error = $root.dataform.QueryEvaluationError.fromObject(object.error, long + 1);
            }
            if (object.incremental != null)
                message.incremental = Boolean(object.incremental);
            if (object.query != null)
                message.query = String(object.query);
            return message;
        };

        /**
         * Creates a plain object from a QueryEvaluation message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.QueryEvaluation
         * @static
         * @param {dataform.QueryEvaluation} message QueryEvaluation
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        QueryEvaluation.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.status = options.enums === String ? "SUCCESS" : 0;
                object.error = null;
                object.incremental = false;
                object.query = "";
            }
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === String ? $root.dataform.QueryEvaluation.QueryEvaluationStatus[message.status] === undefined ? message.status : $root.dataform.QueryEvaluation.QueryEvaluationStatus[message.status] : message.status;
            if (message.error != null && Object.hasOwnProperty.call(message, "error"))
                object.error = $root.dataform.QueryEvaluationError.toObject(message.error, options, q + 1);
            if (message.incremental != null && Object.hasOwnProperty.call(message, "incremental"))
                object.incremental = message.incremental;
            if (message.query != null && Object.hasOwnProperty.call(message, "query"))
                object.query = message.query;
            return object;
        };

        /**
         * Converts this QueryEvaluation to JSON.
         * @function toJSON
         * @memberof dataform.QueryEvaluation
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        QueryEvaluation.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for QueryEvaluation
         * @function getTypeUrl
         * @memberof dataform.QueryEvaluation
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        QueryEvaluation.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.QueryEvaluation";
        };

        /**
         * QueryEvaluationStatus enum.
         * @name dataform.QueryEvaluation.QueryEvaluationStatus
         * @enum {number}
         * @property {number} SUCCESS=0 SUCCESS value
         * @property {number} FAILURE=1 FAILURE value
         */
        QueryEvaluation.QueryEvaluationStatus = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "SUCCESS"] = 0;
            values[valuesById[1] = "FAILURE"] = 1;
            return values;
        })();

        return QueryEvaluation;
    })();

    dataform.RunConfig = (function() {

        /**
         * Properties of a RunConfig.
         * @memberof dataform
         * @interface IRunConfig
         * @property {Array.<string>|null} [actions] RunConfig actions
         * @property {Array.<string>|null} [tags] RunConfig tags
         * @property {boolean|null} [includeDependencies] RunConfig includeDependencies
         * @property {boolean|null} [includeDependents] RunConfig includeDependents
         * @property {boolean|null} [fullRefresh] RunConfig fullRefresh
         * @property {number|null} [timeoutMillis] RunConfig timeoutMillis
         * @property {number|null} [jitTimeoutMillis] RunConfig jitTimeoutMillis
         * @property {boolean|null} [disableSetMetadata] RunConfig disableSetMetadata
         * @property {boolean|null} [useRunCache] RunConfig useRunCache
         */

        /**
         * Constructs a new RunConfig.
         * @memberof dataform
         * @classdesc Represents a RunConfig.
         * @implements IRunConfig
         * @constructor
         * @param {dataform.IRunConfig=} [properties] Properties to set
         */
        function RunConfig(properties) {
            this.actions = [];
            this.tags = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RunConfig actions.
         * @member {Array.<string>} actions
         * @memberof dataform.RunConfig
         * @instance
         */
        RunConfig.prototype.actions = $util.emptyArray;

        /**
         * RunConfig tags.
         * @member {Array.<string>} tags
         * @memberof dataform.RunConfig
         * @instance
         */
        RunConfig.prototype.tags = $util.emptyArray;

        /**
         * RunConfig includeDependencies.
         * @member {boolean} includeDependencies
         * @memberof dataform.RunConfig
         * @instance
         */
        RunConfig.prototype.includeDependencies = false;

        /**
         * RunConfig includeDependents.
         * @member {boolean} includeDependents
         * @memberof dataform.RunConfig
         * @instance
         */
        RunConfig.prototype.includeDependents = false;

        /**
         * RunConfig fullRefresh.
         * @member {boolean} fullRefresh
         * @memberof dataform.RunConfig
         * @instance
         */
        RunConfig.prototype.fullRefresh = false;

        /**
         * RunConfig timeoutMillis.
         * @member {number} timeoutMillis
         * @memberof dataform.RunConfig
         * @instance
         */
        RunConfig.prototype.timeoutMillis = 0;

        /**
         * RunConfig jitTimeoutMillis.
         * @member {number} jitTimeoutMillis
         * @memberof dataform.RunConfig
         * @instance
         */
        RunConfig.prototype.jitTimeoutMillis = 0;

        /**
         * RunConfig disableSetMetadata.
         * @member {boolean} disableSetMetadata
         * @memberof dataform.RunConfig
         * @instance
         */
        RunConfig.prototype.disableSetMetadata = false;

        /**
         * RunConfig useRunCache.
         * @member {boolean} useRunCache
         * @memberof dataform.RunConfig
         * @instance
         */
        RunConfig.prototype.useRunCache = false;

        /**
         * Creates a new RunConfig instance using the specified properties.
         * @function create
         * @memberof dataform.RunConfig
         * @static
         * @param {dataform.IRunConfig=} [properties] Properties to set
         * @returns {dataform.RunConfig} RunConfig instance
         */
        RunConfig.create = function create(properties) {
            return new RunConfig(properties);
        };

        /**
         * Encodes the specified RunConfig message. Does not implicitly {@link dataform.RunConfig.verify|verify} messages.
         * @function encode
         * @memberof dataform.RunConfig
         * @static
         * @param {dataform.IRunConfig} message RunConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RunConfig.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.actions != null && message.actions.length)
                for (let i = 0; i < message.actions.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.actions[i]);
            if (message.fullRefresh != null && Object.hasOwnProperty.call(message, "fullRefresh"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.fullRefresh);
            if (message.includeDependencies != null && Object.hasOwnProperty.call(message, "includeDependencies"))
                writer.uint32(/* id 3, wireType 0 =*/24).bool(message.includeDependencies);
            if (message.tags != null && message.tags.length)
                for (let i = 0; i < message.tags.length; ++i)
                    writer.uint32(/* id 5, wireType 2 =*/42).string(message.tags[i]);
            if (message.timeoutMillis != null && Object.hasOwnProperty.call(message, "timeoutMillis"))
                writer.uint32(/* id 7, wireType 0 =*/56).int32(message.timeoutMillis);
            if (message.useRunCache != null && Object.hasOwnProperty.call(message, "useRunCache"))
                writer.uint32(/* id 8, wireType 0 =*/64).bool(message.useRunCache);
            if (message.disableSetMetadata != null && Object.hasOwnProperty.call(message, "disableSetMetadata"))
                writer.uint32(/* id 9, wireType 0 =*/72).bool(message.disableSetMetadata);
            if (message.jitTimeoutMillis != null && Object.hasOwnProperty.call(message, "jitTimeoutMillis"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.jitTimeoutMillis);
            if (message.includeDependents != null && Object.hasOwnProperty.call(message, "includeDependents"))
                writer.uint32(/* id 11, wireType 0 =*/88).bool(message.includeDependents);
            return writer;
        };

        /**
         * Encodes the specified RunConfig message, length delimited. Does not implicitly {@link dataform.RunConfig.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.RunConfig
         * @static
         * @param {dataform.IRunConfig} message RunConfig message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RunConfig.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RunConfig message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.RunConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.RunConfig} RunConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RunConfig.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.RunConfig();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.actions && message.actions.length))
                            message.actions = [];
                        message.actions.push(reader.string());
                        break;
                    }
                case 5: {
                        if (!(message.tags && message.tags.length))
                            message.tags = [];
                        message.tags.push(reader.string());
                        break;
                    }
                case 3: {
                        message.includeDependencies = reader.bool();
                        break;
                    }
                case 11: {
                        message.includeDependents = reader.bool();
                        break;
                    }
                case 2: {
                        message.fullRefresh = reader.bool();
                        break;
                    }
                case 7: {
                        message.timeoutMillis = reader.int32();
                        break;
                    }
                case 10: {
                        message.jitTimeoutMillis = reader.int32();
                        break;
                    }
                case 9: {
                        message.disableSetMetadata = reader.bool();
                        break;
                    }
                case 8: {
                        message.useRunCache = reader.bool();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RunConfig message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.RunConfig
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.RunConfig} RunConfig
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RunConfig.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RunConfig message.
         * @function verify
         * @memberof dataform.RunConfig
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RunConfig.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.actions != null && Object.hasOwnProperty.call(message, "actions")) {
                if (!Array.isArray(message.actions))
                    return "actions: array expected";
                for (let i = 0; i < message.actions.length; ++i)
                    if (!$util.isString(message.actions[i]))
                        return "actions: string[] expected";
            }
            if (message.tags != null && Object.hasOwnProperty.call(message, "tags")) {
                if (!Array.isArray(message.tags))
                    return "tags: array expected";
                for (let i = 0; i < message.tags.length; ++i)
                    if (!$util.isString(message.tags[i]))
                        return "tags: string[] expected";
            }
            if (message.includeDependencies != null && Object.hasOwnProperty.call(message, "includeDependencies"))
                if (typeof message.includeDependencies !== "boolean")
                    return "includeDependencies: boolean expected";
            if (message.includeDependents != null && Object.hasOwnProperty.call(message, "includeDependents"))
                if (typeof message.includeDependents !== "boolean")
                    return "includeDependents: boolean expected";
            if (message.fullRefresh != null && Object.hasOwnProperty.call(message, "fullRefresh"))
                if (typeof message.fullRefresh !== "boolean")
                    return "fullRefresh: boolean expected";
            if (message.timeoutMillis != null && Object.hasOwnProperty.call(message, "timeoutMillis"))
                if (!$util.isInteger(message.timeoutMillis))
                    return "timeoutMillis: integer expected";
            if (message.jitTimeoutMillis != null && Object.hasOwnProperty.call(message, "jitTimeoutMillis"))
                if (!$util.isInteger(message.jitTimeoutMillis))
                    return "jitTimeoutMillis: integer expected";
            if (message.disableSetMetadata != null && Object.hasOwnProperty.call(message, "disableSetMetadata"))
                if (typeof message.disableSetMetadata !== "boolean")
                    return "disableSetMetadata: boolean expected";
            if (message.useRunCache != null && Object.hasOwnProperty.call(message, "useRunCache"))
                if (typeof message.useRunCache !== "boolean")
                    return "useRunCache: boolean expected";
            return null;
        };

        /**
         * Creates a RunConfig message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.RunConfig
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.RunConfig} RunConfig
         */
        RunConfig.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.RunConfig)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.RunConfig: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.RunConfig();
            if (object.actions) {
                if (!Array.isArray(object.actions))
                    throw TypeError(".dataform.RunConfig.actions: array expected");
                message.actions = [];
                for (let i = 0; i < object.actions.length; ++i)
                    message.actions[i] = String(object.actions[i]);
            }
            if (object.tags) {
                if (!Array.isArray(object.tags))
                    throw TypeError(".dataform.RunConfig.tags: array expected");
                message.tags = [];
                for (let i = 0; i < object.tags.length; ++i)
                    message.tags[i] = String(object.tags[i]);
            }
            if (object.includeDependencies != null)
                message.includeDependencies = Boolean(object.includeDependencies);
            if (object.includeDependents != null)
                message.includeDependents = Boolean(object.includeDependents);
            if (object.fullRefresh != null)
                message.fullRefresh = Boolean(object.fullRefresh);
            if (object.timeoutMillis != null)
                message.timeoutMillis = object.timeoutMillis | 0;
            if (object.jitTimeoutMillis != null)
                message.jitTimeoutMillis = object.jitTimeoutMillis | 0;
            if (object.disableSetMetadata != null)
                message.disableSetMetadata = Boolean(object.disableSetMetadata);
            if (object.useRunCache != null)
                message.useRunCache = Boolean(object.useRunCache);
            return message;
        };

        /**
         * Creates a plain object from a RunConfig message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.RunConfig
         * @static
         * @param {dataform.RunConfig} message RunConfig
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RunConfig.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.actions = [];
                object.tags = [];
            }
            if (options.defaults) {
                object.fullRefresh = false;
                object.includeDependencies = false;
                object.timeoutMillis = 0;
                object.useRunCache = false;
                object.disableSetMetadata = false;
                object.jitTimeoutMillis = 0;
                object.includeDependents = false;
            }
            if (message.actions && message.actions.length) {
                object.actions = [];
                for (let j = 0; j < message.actions.length; ++j)
                    object.actions[j] = message.actions[j];
            }
            if (message.fullRefresh != null && Object.hasOwnProperty.call(message, "fullRefresh"))
                object.fullRefresh = message.fullRefresh;
            if (message.includeDependencies != null && Object.hasOwnProperty.call(message, "includeDependencies"))
                object.includeDependencies = message.includeDependencies;
            if (message.tags && message.tags.length) {
                object.tags = [];
                for (let j = 0; j < message.tags.length; ++j)
                    object.tags[j] = message.tags[j];
            }
            if (message.timeoutMillis != null && Object.hasOwnProperty.call(message, "timeoutMillis"))
                object.timeoutMillis = message.timeoutMillis;
            if (message.useRunCache != null && Object.hasOwnProperty.call(message, "useRunCache"))
                object.useRunCache = message.useRunCache;
            if (message.disableSetMetadata != null && Object.hasOwnProperty.call(message, "disableSetMetadata"))
                object.disableSetMetadata = message.disableSetMetadata;
            if (message.jitTimeoutMillis != null && Object.hasOwnProperty.call(message, "jitTimeoutMillis"))
                object.jitTimeoutMillis = message.jitTimeoutMillis;
            if (message.includeDependents != null && Object.hasOwnProperty.call(message, "includeDependents"))
                object.includeDependents = message.includeDependents;
            return object;
        };

        /**
         * Converts this RunConfig to JSON.
         * @function toJSON
         * @memberof dataform.RunConfig
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RunConfig.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RunConfig
         * @function getTypeUrl
         * @memberof dataform.RunConfig
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RunConfig.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.RunConfig";
        };

        return RunConfig;
    })();

    dataform.ExecutionTask = (function() {

        /**
         * Properties of an ExecutionTask.
         * @memberof dataform
         * @interface IExecutionTask
         * @property {string|null} [type] ExecutionTask type
         * @property {string|null} [statement] ExecutionTask statement
         */

        /**
         * Constructs a new ExecutionTask.
         * @memberof dataform
         * @classdesc Represents an ExecutionTask.
         * @implements IExecutionTask
         * @constructor
         * @param {dataform.IExecutionTask=} [properties] Properties to set
         */
        function ExecutionTask(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ExecutionTask type.
         * @member {string} type
         * @memberof dataform.ExecutionTask
         * @instance
         */
        ExecutionTask.prototype.type = "";

        /**
         * ExecutionTask statement.
         * @member {string} statement
         * @memberof dataform.ExecutionTask
         * @instance
         */
        ExecutionTask.prototype.statement = "";

        /**
         * Creates a new ExecutionTask instance using the specified properties.
         * @function create
         * @memberof dataform.ExecutionTask
         * @static
         * @param {dataform.IExecutionTask=} [properties] Properties to set
         * @returns {dataform.ExecutionTask} ExecutionTask instance
         */
        ExecutionTask.create = function create(properties) {
            return new ExecutionTask(properties);
        };

        /**
         * Encodes the specified ExecutionTask message. Does not implicitly {@link dataform.ExecutionTask.verify|verify} messages.
         * @function encode
         * @memberof dataform.ExecutionTask
         * @static
         * @param {dataform.IExecutionTask} message ExecutionTask message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExecutionTask.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.type);
            if (message.statement != null && Object.hasOwnProperty.call(message, "statement"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.statement);
            return writer;
        };

        /**
         * Encodes the specified ExecutionTask message, length delimited. Does not implicitly {@link dataform.ExecutionTask.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.ExecutionTask
         * @static
         * @param {dataform.IExecutionTask} message ExecutionTask message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExecutionTask.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ExecutionTask message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.ExecutionTask
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.ExecutionTask} ExecutionTask
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExecutionTask.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ExecutionTask();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.type = reader.string();
                        break;
                    }
                case 2: {
                        message.statement = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ExecutionTask message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.ExecutionTask
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.ExecutionTask} ExecutionTask
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExecutionTask.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExecutionTask message.
         * @function verify
         * @memberof dataform.ExecutionTask
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExecutionTask.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            if (message.statement != null && Object.hasOwnProperty.call(message, "statement"))
                if (!$util.isString(message.statement))
                    return "statement: string expected";
            return null;
        };

        /**
         * Creates an ExecutionTask message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.ExecutionTask
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.ExecutionTask} ExecutionTask
         */
        ExecutionTask.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.ExecutionTask)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.ExecutionTask: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.ExecutionTask();
            if (object.type != null)
                message.type = String(object.type);
            if (object.statement != null)
                message.statement = String(object.statement);
            return message;
        };

        /**
         * Creates a plain object from an ExecutionTask message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.ExecutionTask
         * @static
         * @param {dataform.ExecutionTask} message ExecutionTask
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExecutionTask.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.type = "";
                object.statement = "";
            }
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                object.type = message.type;
            if (message.statement != null && Object.hasOwnProperty.call(message, "statement"))
                object.statement = message.statement;
            return object;
        };

        /**
         * Converts this ExecutionTask to JSON.
         * @function toJSON
         * @memberof dataform.ExecutionTask
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExecutionTask.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ExecutionTask
         * @function getTypeUrl
         * @memberof dataform.ExecutionTask
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ExecutionTask.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.ExecutionTask";
        };

        return ExecutionTask;
    })();

    dataform.ExecutionAction = (function() {

        /**
         * Properties of an ExecutionAction.
         * @memberof dataform
         * @interface IExecutionAction
         * @property {dataform.ITarget|null} [target] ExecutionAction target
         * @property {string|null} [fileName] ExecutionAction fileName
         * @property {string|null} [type] ExecutionAction type
         * @property {string|null} [tableType] ExecutionAction tableType
         * @property {Array.<dataform.ITarget>|null} [dependencyTargets] ExecutionAction dependencyTargets
         * @property {dataform.ActionHermeticity|null} [hermeticity] ExecutionAction hermeticity
         * @property {Array.<dataform.IExecutionTask>|null} [tasks] ExecutionAction tasks
         * @property {dataform.IActionDescriptor|null} [actionDescriptor] ExecutionAction actionDescriptor
         * @property {string|null} [jitCode] ExecutionAction jitCode
         */

        /**
         * Constructs a new ExecutionAction.
         * @memberof dataform
         * @classdesc Represents an ExecutionAction.
         * @implements IExecutionAction
         * @constructor
         * @param {dataform.IExecutionAction=} [properties] Properties to set
         */
        function ExecutionAction(properties) {
            this.dependencyTargets = [];
            this.tasks = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ExecutionAction target.
         * @member {dataform.ITarget|null|undefined} target
         * @memberof dataform.ExecutionAction
         * @instance
         */
        ExecutionAction.prototype.target = null;

        /**
         * ExecutionAction fileName.
         * @member {string} fileName
         * @memberof dataform.ExecutionAction
         * @instance
         */
        ExecutionAction.prototype.fileName = "";

        /**
         * ExecutionAction type.
         * @member {string} type
         * @memberof dataform.ExecutionAction
         * @instance
         */
        ExecutionAction.prototype.type = "";

        /**
         * ExecutionAction tableType.
         * @member {string} tableType
         * @memberof dataform.ExecutionAction
         * @instance
         */
        ExecutionAction.prototype.tableType = "";

        /**
         * ExecutionAction dependencyTargets.
         * @member {Array.<dataform.ITarget>} dependencyTargets
         * @memberof dataform.ExecutionAction
         * @instance
         */
        ExecutionAction.prototype.dependencyTargets = $util.emptyArray;

        /**
         * ExecutionAction hermeticity.
         * @member {dataform.ActionHermeticity} hermeticity
         * @memberof dataform.ExecutionAction
         * @instance
         */
        ExecutionAction.prototype.hermeticity = 0;

        /**
         * ExecutionAction tasks.
         * @member {Array.<dataform.IExecutionTask>} tasks
         * @memberof dataform.ExecutionAction
         * @instance
         */
        ExecutionAction.prototype.tasks = $util.emptyArray;

        /**
         * ExecutionAction actionDescriptor.
         * @member {dataform.IActionDescriptor|null|undefined} actionDescriptor
         * @memberof dataform.ExecutionAction
         * @instance
         */
        ExecutionAction.prototype.actionDescriptor = null;

        /**
         * ExecutionAction jitCode.
         * @member {string} jitCode
         * @memberof dataform.ExecutionAction
         * @instance
         */
        ExecutionAction.prototype.jitCode = "";

        /**
         * Creates a new ExecutionAction instance using the specified properties.
         * @function create
         * @memberof dataform.ExecutionAction
         * @static
         * @param {dataform.IExecutionAction=} [properties] Properties to set
         * @returns {dataform.ExecutionAction} ExecutionAction instance
         */
        ExecutionAction.create = function create(properties) {
            return new ExecutionAction(properties);
        };

        /**
         * Encodes the specified ExecutionAction message. Does not implicitly {@link dataform.ExecutionAction.verify|verify} messages.
         * @function encode
         * @memberof dataform.ExecutionAction
         * @static
         * @param {dataform.IExecutionAction} message ExecutionAction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExecutionAction.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.tasks != null && message.tasks.length)
                for (let i = 0; i < message.tasks.length; ++i)
                    $root.dataform.ExecutionTask.encode(message.tasks[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.type);
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                $root.dataform.Target.encode(message.target, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.tableType != null && Object.hasOwnProperty.call(message, "tableType"))
                writer.uint32(/* id 6, wireType 2 =*/50).string(message.tableType);
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                writer.uint32(/* id 8, wireType 2 =*/66).string(message.fileName);
            if (message.actionDescriptor != null && Object.hasOwnProperty.call(message, "actionDescriptor"))
                $root.dataform.ActionDescriptor.encode(message.actionDescriptor, writer.uint32(/* id 9, wireType 2 =*/74).fork(), q + 1).ldelim();
            if (message.hermeticity != null && Object.hasOwnProperty.call(message, "hermeticity"))
                writer.uint32(/* id 10, wireType 0 =*/80).int32(message.hermeticity);
            if (message.dependencyTargets != null && message.dependencyTargets.length)
                for (let i = 0; i < message.dependencyTargets.length; ++i)
                    $root.dataform.Target.encode(message.dependencyTargets[i], writer.uint32(/* id 11, wireType 2 =*/90).fork(), q + 1).ldelim();
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                writer.uint32(/* id 12, wireType 2 =*/98).string(message.jitCode);
            return writer;
        };

        /**
         * Encodes the specified ExecutionAction message, length delimited. Does not implicitly {@link dataform.ExecutionAction.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.ExecutionAction
         * @static
         * @param {dataform.IExecutionAction} message ExecutionAction message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExecutionAction.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ExecutionAction message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.ExecutionAction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.ExecutionAction} ExecutionAction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExecutionAction.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ExecutionAction();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 5: {
                        message.target = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 8: {
                        message.fileName = reader.string();
                        break;
                    }
                case 4: {
                        message.type = reader.string();
                        break;
                    }
                case 6: {
                        message.tableType = reader.string();
                        break;
                    }
                case 11: {
                        if (!(message.dependencyTargets && message.dependencyTargets.length))
                            message.dependencyTargets = [];
                        message.dependencyTargets.push($root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 10: {
                        message.hermeticity = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.tasks && message.tasks.length))
                            message.tasks = [];
                        message.tasks.push($root.dataform.ExecutionTask.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 9: {
                        message.actionDescriptor = $root.dataform.ActionDescriptor.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 12: {
                        message.jitCode = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ExecutionAction message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.ExecutionAction
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.ExecutionAction} ExecutionAction
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExecutionAction.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExecutionAction message.
         * @function verify
         * @memberof dataform.ExecutionAction
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExecutionAction.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.target != null && Object.hasOwnProperty.call(message, "target")) {
                let error = $root.dataform.Target.verify(message.target, long + 1);
                if (error)
                    return "target." + error;
            }
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                if (!$util.isString(message.fileName))
                    return "fileName: string expected";
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                if (!$util.isString(message.type))
                    return "type: string expected";
            if (message.tableType != null && Object.hasOwnProperty.call(message, "tableType"))
                if (!$util.isString(message.tableType))
                    return "tableType: string expected";
            if (message.dependencyTargets != null && Object.hasOwnProperty.call(message, "dependencyTargets")) {
                if (!Array.isArray(message.dependencyTargets))
                    return "dependencyTargets: array expected";
                for (let i = 0; i < message.dependencyTargets.length; ++i) {
                    let error = $root.dataform.Target.verify(message.dependencyTargets[i], long + 1);
                    if (error)
                        return "dependencyTargets." + error;
                }
            }
            if (message.hermeticity != null && Object.hasOwnProperty.call(message, "hermeticity"))
                switch (message.hermeticity) {
                default:
                    return "hermeticity: enum value expected";
                case 0:
                case 1:
                case 2:
                    break;
                }
            if (message.tasks != null && Object.hasOwnProperty.call(message, "tasks")) {
                if (!Array.isArray(message.tasks))
                    return "tasks: array expected";
                for (let i = 0; i < message.tasks.length; ++i) {
                    let error = $root.dataform.ExecutionTask.verify(message.tasks[i], long + 1);
                    if (error)
                        return "tasks." + error;
                }
            }
            if (message.actionDescriptor != null && Object.hasOwnProperty.call(message, "actionDescriptor")) {
                let error = $root.dataform.ActionDescriptor.verify(message.actionDescriptor, long + 1);
                if (error)
                    return "actionDescriptor." + error;
            }
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                if (!$util.isString(message.jitCode))
                    return "jitCode: string expected";
            return null;
        };

        /**
         * Creates an ExecutionAction message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.ExecutionAction
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.ExecutionAction} ExecutionAction
         */
        ExecutionAction.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.ExecutionAction)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.ExecutionAction: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.ExecutionAction();
            if (object.target != null) {
                if (!$util.isObject(object.target))
                    throw TypeError(".dataform.ExecutionAction.target: object expected");
                message.target = $root.dataform.Target.fromObject(object.target, long + 1);
            }
            if (object.fileName != null)
                message.fileName = String(object.fileName);
            if (object.type != null)
                message.type = String(object.type);
            if (object.tableType != null)
                message.tableType = String(object.tableType);
            if (object.dependencyTargets) {
                if (!Array.isArray(object.dependencyTargets))
                    throw TypeError(".dataform.ExecutionAction.dependencyTargets: array expected");
                message.dependencyTargets = [];
                for (let i = 0; i < object.dependencyTargets.length; ++i) {
                    if (!$util.isObject(object.dependencyTargets[i]))
                        throw TypeError(".dataform.ExecutionAction.dependencyTargets: object expected");
                    message.dependencyTargets[i] = $root.dataform.Target.fromObject(object.dependencyTargets[i], long + 1);
                }
            }
            switch (object.hermeticity) {
            default:
                if (typeof object.hermeticity === "number") {
                    message.hermeticity = object.hermeticity;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                message.hermeticity = 0;
                break;
            case "HERMETIC":
            case 1:
                message.hermeticity = 1;
                break;
            case "NON_HERMETIC":
            case 2:
                message.hermeticity = 2;
                break;
            }
            if (object.tasks) {
                if (!Array.isArray(object.tasks))
                    throw TypeError(".dataform.ExecutionAction.tasks: array expected");
                message.tasks = [];
                for (let i = 0; i < object.tasks.length; ++i) {
                    if (!$util.isObject(object.tasks[i]))
                        throw TypeError(".dataform.ExecutionAction.tasks: object expected");
                    message.tasks[i] = $root.dataform.ExecutionTask.fromObject(object.tasks[i], long + 1);
                }
            }
            if (object.actionDescriptor != null) {
                if (!$util.isObject(object.actionDescriptor))
                    throw TypeError(".dataform.ExecutionAction.actionDescriptor: object expected");
                message.actionDescriptor = $root.dataform.ActionDescriptor.fromObject(object.actionDescriptor, long + 1);
            }
            if (object.jitCode != null)
                message.jitCode = String(object.jitCode);
            return message;
        };

        /**
         * Creates a plain object from an ExecutionAction message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.ExecutionAction
         * @static
         * @param {dataform.ExecutionAction} message ExecutionAction
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExecutionAction.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.tasks = [];
                object.dependencyTargets = [];
            }
            if (options.defaults) {
                object.type = "";
                object.target = null;
                object.tableType = "";
                object.fileName = "";
                object.actionDescriptor = null;
                object.hermeticity = options.enums === String ? "UNKNOWN" : 0;
                object.jitCode = "";
            }
            if (message.tasks && message.tasks.length) {
                object.tasks = [];
                for (let j = 0; j < message.tasks.length; ++j)
                    object.tasks[j] = $root.dataform.ExecutionTask.toObject(message.tasks[j], options, q + 1);
            }
            if (message.type != null && Object.hasOwnProperty.call(message, "type"))
                object.type = message.type;
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                object.target = $root.dataform.Target.toObject(message.target, options, q + 1);
            if (message.tableType != null && Object.hasOwnProperty.call(message, "tableType"))
                object.tableType = message.tableType;
            if (message.fileName != null && Object.hasOwnProperty.call(message, "fileName"))
                object.fileName = message.fileName;
            if (message.actionDescriptor != null && Object.hasOwnProperty.call(message, "actionDescriptor"))
                object.actionDescriptor = $root.dataform.ActionDescriptor.toObject(message.actionDescriptor, options, q + 1);
            if (message.hermeticity != null && Object.hasOwnProperty.call(message, "hermeticity"))
                object.hermeticity = options.enums === String ? $root.dataform.ActionHermeticity[message.hermeticity] === undefined ? message.hermeticity : $root.dataform.ActionHermeticity[message.hermeticity] : message.hermeticity;
            if (message.dependencyTargets && message.dependencyTargets.length) {
                object.dependencyTargets = [];
                for (let j = 0; j < message.dependencyTargets.length; ++j)
                    object.dependencyTargets[j] = $root.dataform.Target.toObject(message.dependencyTargets[j], options, q + 1);
            }
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                object.jitCode = message.jitCode;
            return object;
        };

        /**
         * Converts this ExecutionAction to JSON.
         * @function toJSON
         * @memberof dataform.ExecutionAction
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExecutionAction.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ExecutionAction
         * @function getTypeUrl
         * @memberof dataform.ExecutionAction
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ExecutionAction.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.ExecutionAction";
        };

        return ExecutionAction;
    })();

    dataform.WarehouseState = (function() {

        /**
         * Properties of a WarehouseState.
         * @memberof dataform
         * @interface IWarehouseState
         * @property {Array.<dataform.ITableMetadata>|null} [tables] WarehouseState tables
         */

        /**
         * Constructs a new WarehouseState.
         * @memberof dataform
         * @classdesc Represents a WarehouseState.
         * @implements IWarehouseState
         * @constructor
         * @param {dataform.IWarehouseState=} [properties] Properties to set
         */
        function WarehouseState(properties) {
            this.tables = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * WarehouseState tables.
         * @member {Array.<dataform.ITableMetadata>} tables
         * @memberof dataform.WarehouseState
         * @instance
         */
        WarehouseState.prototype.tables = $util.emptyArray;

        /**
         * Creates a new WarehouseState instance using the specified properties.
         * @function create
         * @memberof dataform.WarehouseState
         * @static
         * @param {dataform.IWarehouseState=} [properties] Properties to set
         * @returns {dataform.WarehouseState} WarehouseState instance
         */
        WarehouseState.create = function create(properties) {
            return new WarehouseState(properties);
        };

        /**
         * Encodes the specified WarehouseState message. Does not implicitly {@link dataform.WarehouseState.verify|verify} messages.
         * @function encode
         * @memberof dataform.WarehouseState
         * @static
         * @param {dataform.IWarehouseState} message WarehouseState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WarehouseState.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.tables != null && message.tables.length)
                for (let i = 0; i < message.tables.length; ++i)
                    $root.dataform.TableMetadata.encode(message.tables[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified WarehouseState message, length delimited. Does not implicitly {@link dataform.WarehouseState.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.WarehouseState
         * @static
         * @param {dataform.IWarehouseState} message WarehouseState message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        WarehouseState.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a WarehouseState message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.WarehouseState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.WarehouseState} WarehouseState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WarehouseState.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.WarehouseState();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.tables && message.tables.length))
                            message.tables = [];
                        message.tables.push($root.dataform.TableMetadata.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a WarehouseState message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.WarehouseState
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.WarehouseState} WarehouseState
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        WarehouseState.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a WarehouseState message.
         * @function verify
         * @memberof dataform.WarehouseState
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        WarehouseState.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.tables != null && Object.hasOwnProperty.call(message, "tables")) {
                if (!Array.isArray(message.tables))
                    return "tables: array expected";
                for (let i = 0; i < message.tables.length; ++i) {
                    let error = $root.dataform.TableMetadata.verify(message.tables[i], long + 1);
                    if (error)
                        return "tables." + error;
                }
            }
            return null;
        };

        /**
         * Creates a WarehouseState message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.WarehouseState
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.WarehouseState} WarehouseState
         */
        WarehouseState.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.WarehouseState)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.WarehouseState: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.WarehouseState();
            if (object.tables) {
                if (!Array.isArray(object.tables))
                    throw TypeError(".dataform.WarehouseState.tables: array expected");
                message.tables = [];
                for (let i = 0; i < object.tables.length; ++i) {
                    if (!$util.isObject(object.tables[i]))
                        throw TypeError(".dataform.WarehouseState.tables: object expected");
                    message.tables[i] = $root.dataform.TableMetadata.fromObject(object.tables[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a WarehouseState message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.WarehouseState
         * @static
         * @param {dataform.WarehouseState} message WarehouseState
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        WarehouseState.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.tables = [];
            if (message.tables && message.tables.length) {
                object.tables = [];
                for (let j = 0; j < message.tables.length; ++j)
                    object.tables[j] = $root.dataform.TableMetadata.toObject(message.tables[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this WarehouseState to JSON.
         * @function toJSON
         * @memberof dataform.WarehouseState
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        WarehouseState.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for WarehouseState
         * @function getTypeUrl
         * @memberof dataform.WarehouseState
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        WarehouseState.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.WarehouseState";
        };

        return WarehouseState;
    })();

    dataform.ExecutionGraph = (function() {

        /**
         * Properties of an ExecutionGraph.
         * @memberof dataform
         * @interface IExecutionGraph
         * @property {dataform.IProjectConfig|null} [projectConfig] ExecutionGraph projectConfig
         * @property {dataform.IRunConfig|null} [runConfig] ExecutionGraph runConfig
         * @property {dataform.IWarehouseState|null} [warehouseState] ExecutionGraph warehouseState
         * @property {Array.<dataform.ITarget>|null} [declarationTargets] ExecutionGraph declarationTargets
         * @property {Array.<dataform.IExecutionAction>|null} [actions] ExecutionGraph actions
         * @property {google.protobuf.IStruct|null} [jitData] ExecutionGraph jitData
         */

        /**
         * Constructs a new ExecutionGraph.
         * @memberof dataform
         * @classdesc Represents an ExecutionGraph.
         * @implements IExecutionGraph
         * @constructor
         * @param {dataform.IExecutionGraph=} [properties] Properties to set
         */
        function ExecutionGraph(properties) {
            this.declarationTargets = [];
            this.actions = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ExecutionGraph projectConfig.
         * @member {dataform.IProjectConfig|null|undefined} projectConfig
         * @memberof dataform.ExecutionGraph
         * @instance
         */
        ExecutionGraph.prototype.projectConfig = null;

        /**
         * ExecutionGraph runConfig.
         * @member {dataform.IRunConfig|null|undefined} runConfig
         * @memberof dataform.ExecutionGraph
         * @instance
         */
        ExecutionGraph.prototype.runConfig = null;

        /**
         * ExecutionGraph warehouseState.
         * @member {dataform.IWarehouseState|null|undefined} warehouseState
         * @memberof dataform.ExecutionGraph
         * @instance
         */
        ExecutionGraph.prototype.warehouseState = null;

        /**
         * ExecutionGraph declarationTargets.
         * @member {Array.<dataform.ITarget>} declarationTargets
         * @memberof dataform.ExecutionGraph
         * @instance
         */
        ExecutionGraph.prototype.declarationTargets = $util.emptyArray;

        /**
         * ExecutionGraph actions.
         * @member {Array.<dataform.IExecutionAction>} actions
         * @memberof dataform.ExecutionGraph
         * @instance
         */
        ExecutionGraph.prototype.actions = $util.emptyArray;

        /**
         * ExecutionGraph jitData.
         * @member {google.protobuf.IStruct|null|undefined} jitData
         * @memberof dataform.ExecutionGraph
         * @instance
         */
        ExecutionGraph.prototype.jitData = null;

        /**
         * Creates a new ExecutionGraph instance using the specified properties.
         * @function create
         * @memberof dataform.ExecutionGraph
         * @static
         * @param {dataform.IExecutionGraph=} [properties] Properties to set
         * @returns {dataform.ExecutionGraph} ExecutionGraph instance
         */
        ExecutionGraph.create = function create(properties) {
            return new ExecutionGraph(properties);
        };

        /**
         * Encodes the specified ExecutionGraph message. Does not implicitly {@link dataform.ExecutionGraph.verify|verify} messages.
         * @function encode
         * @memberof dataform.ExecutionGraph
         * @static
         * @param {dataform.IExecutionGraph} message ExecutionGraph message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExecutionGraph.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.projectConfig != null && Object.hasOwnProperty.call(message, "projectConfig"))
                $root.dataform.ProjectConfig.encode(message.projectConfig, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.runConfig != null && Object.hasOwnProperty.call(message, "runConfig"))
                $root.dataform.RunConfig.encode(message.runConfig, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.actions != null && message.actions.length)
                for (let i = 0; i < message.actions.length; ++i)
                    $root.dataform.ExecutionAction.encode(message.actions[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.warehouseState != null && Object.hasOwnProperty.call(message, "warehouseState"))
                $root.dataform.WarehouseState.encode(message.warehouseState, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.declarationTargets != null && message.declarationTargets.length)
                for (let i = 0; i < message.declarationTargets.length; ++i)
                    $root.dataform.Target.encode(message.declarationTargets[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.jitData != null && Object.hasOwnProperty.call(message, "jitData"))
                $root.google.protobuf.Struct.encode(message.jitData, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ExecutionGraph message, length delimited. Does not implicitly {@link dataform.ExecutionGraph.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.ExecutionGraph
         * @static
         * @param {dataform.IExecutionGraph} message ExecutionGraph message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExecutionGraph.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ExecutionGraph message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.ExecutionGraph
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.ExecutionGraph} ExecutionGraph
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExecutionGraph.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ExecutionGraph();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.projectConfig = $root.dataform.ProjectConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.runConfig = $root.dataform.RunConfig.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 4: {
                        message.warehouseState = $root.dataform.WarehouseState.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 5: {
                        if (!(message.declarationTargets && message.declarationTargets.length))
                            message.declarationTargets = [];
                        message.declarationTargets.push($root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 3: {
                        if (!(message.actions && message.actions.length))
                            message.actions = [];
                        message.actions.push($root.dataform.ExecutionAction.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 7: {
                        message.jitData = $root.google.protobuf.Struct.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ExecutionGraph message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.ExecutionGraph
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.ExecutionGraph} ExecutionGraph
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExecutionGraph.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExecutionGraph message.
         * @function verify
         * @memberof dataform.ExecutionGraph
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExecutionGraph.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.projectConfig != null && Object.hasOwnProperty.call(message, "projectConfig")) {
                let error = $root.dataform.ProjectConfig.verify(message.projectConfig, long + 1);
                if (error)
                    return "projectConfig." + error;
            }
            if (message.runConfig != null && Object.hasOwnProperty.call(message, "runConfig")) {
                let error = $root.dataform.RunConfig.verify(message.runConfig, long + 1);
                if (error)
                    return "runConfig." + error;
            }
            if (message.warehouseState != null && Object.hasOwnProperty.call(message, "warehouseState")) {
                let error = $root.dataform.WarehouseState.verify(message.warehouseState, long + 1);
                if (error)
                    return "warehouseState." + error;
            }
            if (message.declarationTargets != null && Object.hasOwnProperty.call(message, "declarationTargets")) {
                if (!Array.isArray(message.declarationTargets))
                    return "declarationTargets: array expected";
                for (let i = 0; i < message.declarationTargets.length; ++i) {
                    let error = $root.dataform.Target.verify(message.declarationTargets[i], long + 1);
                    if (error)
                        return "declarationTargets." + error;
                }
            }
            if (message.actions != null && Object.hasOwnProperty.call(message, "actions")) {
                if (!Array.isArray(message.actions))
                    return "actions: array expected";
                for (let i = 0; i < message.actions.length; ++i) {
                    let error = $root.dataform.ExecutionAction.verify(message.actions[i], long + 1);
                    if (error)
                        return "actions." + error;
                }
            }
            if (message.jitData != null && Object.hasOwnProperty.call(message, "jitData")) {
                let error = $root.google.protobuf.Struct.verify(message.jitData, long + 1);
                if (error)
                    return "jitData." + error;
            }
            return null;
        };

        /**
         * Creates an ExecutionGraph message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.ExecutionGraph
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.ExecutionGraph} ExecutionGraph
         */
        ExecutionGraph.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.ExecutionGraph)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.ExecutionGraph: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.ExecutionGraph();
            if (object.projectConfig != null) {
                if (!$util.isObject(object.projectConfig))
                    throw TypeError(".dataform.ExecutionGraph.projectConfig: object expected");
                message.projectConfig = $root.dataform.ProjectConfig.fromObject(object.projectConfig, long + 1);
            }
            if (object.runConfig != null) {
                if (!$util.isObject(object.runConfig))
                    throw TypeError(".dataform.ExecutionGraph.runConfig: object expected");
                message.runConfig = $root.dataform.RunConfig.fromObject(object.runConfig, long + 1);
            }
            if (object.warehouseState != null) {
                if (!$util.isObject(object.warehouseState))
                    throw TypeError(".dataform.ExecutionGraph.warehouseState: object expected");
                message.warehouseState = $root.dataform.WarehouseState.fromObject(object.warehouseState, long + 1);
            }
            if (object.declarationTargets) {
                if (!Array.isArray(object.declarationTargets))
                    throw TypeError(".dataform.ExecutionGraph.declarationTargets: array expected");
                message.declarationTargets = [];
                for (let i = 0; i < object.declarationTargets.length; ++i) {
                    if (!$util.isObject(object.declarationTargets[i]))
                        throw TypeError(".dataform.ExecutionGraph.declarationTargets: object expected");
                    message.declarationTargets[i] = $root.dataform.Target.fromObject(object.declarationTargets[i], long + 1);
                }
            }
            if (object.actions) {
                if (!Array.isArray(object.actions))
                    throw TypeError(".dataform.ExecutionGraph.actions: array expected");
                message.actions = [];
                for (let i = 0; i < object.actions.length; ++i) {
                    if (!$util.isObject(object.actions[i]))
                        throw TypeError(".dataform.ExecutionGraph.actions: object expected");
                    message.actions[i] = $root.dataform.ExecutionAction.fromObject(object.actions[i], long + 1);
                }
            }
            if (object.jitData != null) {
                if (!$util.isObject(object.jitData))
                    throw TypeError(".dataform.ExecutionGraph.jitData: object expected");
                message.jitData = $root.google.protobuf.Struct.fromObject(object.jitData, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an ExecutionGraph message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.ExecutionGraph
         * @static
         * @param {dataform.ExecutionGraph} message ExecutionGraph
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExecutionGraph.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.actions = [];
                object.declarationTargets = [];
            }
            if (options.defaults) {
                object.projectConfig = null;
                object.runConfig = null;
                object.warehouseState = null;
                object.jitData = null;
            }
            if (message.projectConfig != null && Object.hasOwnProperty.call(message, "projectConfig"))
                object.projectConfig = $root.dataform.ProjectConfig.toObject(message.projectConfig, options, q + 1);
            if (message.runConfig != null && Object.hasOwnProperty.call(message, "runConfig"))
                object.runConfig = $root.dataform.RunConfig.toObject(message.runConfig, options, q + 1);
            if (message.actions && message.actions.length) {
                object.actions = [];
                for (let j = 0; j < message.actions.length; ++j)
                    object.actions[j] = $root.dataform.ExecutionAction.toObject(message.actions[j], options, q + 1);
            }
            if (message.warehouseState != null && Object.hasOwnProperty.call(message, "warehouseState"))
                object.warehouseState = $root.dataform.WarehouseState.toObject(message.warehouseState, options, q + 1);
            if (message.declarationTargets && message.declarationTargets.length) {
                object.declarationTargets = [];
                for (let j = 0; j < message.declarationTargets.length; ++j)
                    object.declarationTargets[j] = $root.dataform.Target.toObject(message.declarationTargets[j], options, q + 1);
            }
            if (message.jitData != null && Object.hasOwnProperty.call(message, "jitData"))
                object.jitData = $root.google.protobuf.Struct.toObject(message.jitData, options, q + 1);
            return object;
        };

        /**
         * Converts this ExecutionGraph to JSON.
         * @function toJSON
         * @memberof dataform.ExecutionGraph
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExecutionGraph.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ExecutionGraph
         * @function getTypeUrl
         * @memberof dataform.ExecutionGraph
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ExecutionGraph.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.ExecutionGraph";
        };

        return ExecutionGraph;
    })();

    dataform.Timing = (function() {

        /**
         * Properties of a Timing.
         * @memberof dataform
         * @interface ITiming
         * @property {Long|null} [startTimeMillis] Timing startTimeMillis
         * @property {Long|null} [endTimeMillis] Timing endTimeMillis
         */

        /**
         * Constructs a new Timing.
         * @memberof dataform
         * @classdesc Represents a Timing.
         * @implements ITiming
         * @constructor
         * @param {dataform.ITiming=} [properties] Properties to set
         */
        function Timing(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * Timing startTimeMillis.
         * @member {Long} startTimeMillis
         * @memberof dataform.Timing
         * @instance
         */
        Timing.prototype.startTimeMillis = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Timing endTimeMillis.
         * @member {Long} endTimeMillis
         * @memberof dataform.Timing
         * @instance
         */
        Timing.prototype.endTimeMillis = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * Creates a new Timing instance using the specified properties.
         * @function create
         * @memberof dataform.Timing
         * @static
         * @param {dataform.ITiming=} [properties] Properties to set
         * @returns {dataform.Timing} Timing instance
         */
        Timing.create = function create(properties) {
            return new Timing(properties);
        };

        /**
         * Encodes the specified Timing message. Does not implicitly {@link dataform.Timing.verify|verify} messages.
         * @function encode
         * @memberof dataform.Timing
         * @static
         * @param {dataform.ITiming} message Timing message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Timing.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.startTimeMillis != null && Object.hasOwnProperty.call(message, "startTimeMillis"))
                writer.uint32(/* id 1, wireType 0 =*/8).int64(message.startTimeMillis);
            if (message.endTimeMillis != null && Object.hasOwnProperty.call(message, "endTimeMillis"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.endTimeMillis);
            return writer;
        };

        /**
         * Encodes the specified Timing message, length delimited. Does not implicitly {@link dataform.Timing.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.Timing
         * @static
         * @param {dataform.ITiming} message Timing message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        Timing.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a Timing message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.Timing
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.Timing} Timing
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Timing.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.Timing();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.startTimeMillis = reader.int64();
                        break;
                    }
                case 2: {
                        message.endTimeMillis = reader.int64();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a Timing message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.Timing
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.Timing} Timing
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        Timing.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a Timing message.
         * @function verify
         * @memberof dataform.Timing
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        Timing.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.startTimeMillis != null && Object.hasOwnProperty.call(message, "startTimeMillis"))
                if (!$util.isInteger(message.startTimeMillis) && !(message.startTimeMillis && $util.isInteger(message.startTimeMillis.low) && $util.isInteger(message.startTimeMillis.high)))
                    return "startTimeMillis: integer|Long expected";
            if (message.endTimeMillis != null && Object.hasOwnProperty.call(message, "endTimeMillis"))
                if (!$util.isInteger(message.endTimeMillis) && !(message.endTimeMillis && $util.isInteger(message.endTimeMillis.low) && $util.isInteger(message.endTimeMillis.high)))
                    return "endTimeMillis: integer|Long expected";
            return null;
        };

        /**
         * Creates a Timing message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.Timing
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.Timing} Timing
         */
        Timing.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.Timing)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.Timing: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.Timing();
            if (object.startTimeMillis != null)
                if ($util.Long)
                    message.startTimeMillis = $util.Long.fromValue(object.startTimeMillis, false);
                else if (typeof object.startTimeMillis === "string")
                    message.startTimeMillis = parseInt(object.startTimeMillis, 10);
                else if (typeof object.startTimeMillis === "number")
                    message.startTimeMillis = object.startTimeMillis;
                else if (typeof object.startTimeMillis === "object")
                    message.startTimeMillis = new $util.LongBits(object.startTimeMillis.low >>> 0, object.startTimeMillis.high >>> 0).toNumber();
            if (object.endTimeMillis != null)
                if ($util.Long)
                    message.endTimeMillis = $util.Long.fromValue(object.endTimeMillis, false);
                else if (typeof object.endTimeMillis === "string")
                    message.endTimeMillis = parseInt(object.endTimeMillis, 10);
                else if (typeof object.endTimeMillis === "number")
                    message.endTimeMillis = object.endTimeMillis;
                else if (typeof object.endTimeMillis === "object")
                    message.endTimeMillis = new $util.LongBits(object.endTimeMillis.low >>> 0, object.endTimeMillis.high >>> 0).toNumber();
            return message;
        };

        /**
         * Creates a plain object from a Timing message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.Timing
         * @static
         * @param {dataform.Timing} message Timing
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        Timing.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.startTimeMillis = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.startTimeMillis = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.endTimeMillis = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.endTimeMillis = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
            }
            if (message.startTimeMillis != null && Object.hasOwnProperty.call(message, "startTimeMillis"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.startTimeMillis = typeof message.startTimeMillis === "number" ? BigInt(message.startTimeMillis) : $util.Long.fromBits(message.startTimeMillis.low >>> 0, message.startTimeMillis.high >>> 0, false).toBigInt();
                else if (typeof message.startTimeMillis === "number")
                    object.startTimeMillis = options.longs === String ? String(message.startTimeMillis) : message.startTimeMillis;
                else
                    object.startTimeMillis = options.longs === String ? $util.Long.prototype.toString.call(message.startTimeMillis) : options.longs === Number ? new $util.LongBits(message.startTimeMillis.low >>> 0, message.startTimeMillis.high >>> 0).toNumber() : message.startTimeMillis;
            if (message.endTimeMillis != null && Object.hasOwnProperty.call(message, "endTimeMillis"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.endTimeMillis = typeof message.endTimeMillis === "number" ? BigInt(message.endTimeMillis) : $util.Long.fromBits(message.endTimeMillis.low >>> 0, message.endTimeMillis.high >>> 0, false).toBigInt();
                else if (typeof message.endTimeMillis === "number")
                    object.endTimeMillis = options.longs === String ? String(message.endTimeMillis) : message.endTimeMillis;
                else
                    object.endTimeMillis = options.longs === String ? $util.Long.prototype.toString.call(message.endTimeMillis) : options.longs === Number ? new $util.LongBits(message.endTimeMillis.low >>> 0, message.endTimeMillis.high >>> 0).toNumber() : message.endTimeMillis;
            return object;
        };

        /**
         * Converts this Timing to JSON.
         * @function toJSON
         * @memberof dataform.Timing
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        Timing.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for Timing
         * @function getTypeUrl
         * @memberof dataform.Timing
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        Timing.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.Timing";
        };

        return Timing;
    })();

    dataform.RunResult = (function() {

        /**
         * Properties of a RunResult.
         * @memberof dataform
         * @interface IRunResult
         * @property {dataform.RunResult.ExecutionStatus|null} [status] RunResult status
         * @property {Array.<dataform.IActionResult>|null} [actions] RunResult actions
         * @property {dataform.ITiming|null} [timing] RunResult timing
         */

        /**
         * Constructs a new RunResult.
         * @memberof dataform
         * @classdesc Represents a RunResult.
         * @implements IRunResult
         * @constructor
         * @param {dataform.IRunResult=} [properties] Properties to set
         */
        function RunResult(properties) {
            this.actions = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RunResult status.
         * @member {dataform.RunResult.ExecutionStatus} status
         * @memberof dataform.RunResult
         * @instance
         */
        RunResult.prototype.status = 0;

        /**
         * RunResult actions.
         * @member {Array.<dataform.IActionResult>} actions
         * @memberof dataform.RunResult
         * @instance
         */
        RunResult.prototype.actions = $util.emptyArray;

        /**
         * RunResult timing.
         * @member {dataform.ITiming|null|undefined} timing
         * @memberof dataform.RunResult
         * @instance
         */
        RunResult.prototype.timing = null;

        /**
         * Creates a new RunResult instance using the specified properties.
         * @function create
         * @memberof dataform.RunResult
         * @static
         * @param {dataform.IRunResult=} [properties] Properties to set
         * @returns {dataform.RunResult} RunResult instance
         */
        RunResult.create = function create(properties) {
            return new RunResult(properties);
        };

        /**
         * Encodes the specified RunResult message. Does not implicitly {@link dataform.RunResult.verify|verify} messages.
         * @function encode
         * @memberof dataform.RunResult
         * @static
         * @param {dataform.IRunResult} message RunResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RunResult.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.status);
            if (message.actions != null && message.actions.length)
                for (let i = 0; i < message.actions.length; ++i)
                    $root.dataform.ActionResult.encode(message.actions[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.timing != null && Object.hasOwnProperty.call(message, "timing"))
                $root.dataform.Timing.encode(message.timing, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RunResult message, length delimited. Does not implicitly {@link dataform.RunResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.RunResult
         * @static
         * @param {dataform.IRunResult} message RunResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RunResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RunResult message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.RunResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.RunResult} RunResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RunResult.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.RunResult();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.status = reader.int32();
                        break;
                    }
                case 2: {
                        if (!(message.actions && message.actions.length))
                            message.actions = [];
                        message.actions.push($root.dataform.ActionResult.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 3: {
                        message.timing = $root.dataform.Timing.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RunResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.RunResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.RunResult} RunResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RunResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RunResult message.
         * @function verify
         * @memberof dataform.RunResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RunResult.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                switch (message.status) {
                default:
                    return "status: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                    break;
                }
            if (message.actions != null && Object.hasOwnProperty.call(message, "actions")) {
                if (!Array.isArray(message.actions))
                    return "actions: array expected";
                for (let i = 0; i < message.actions.length; ++i) {
                    let error = $root.dataform.ActionResult.verify(message.actions[i], long + 1);
                    if (error)
                        return "actions." + error;
                }
            }
            if (message.timing != null && Object.hasOwnProperty.call(message, "timing")) {
                let error = $root.dataform.Timing.verify(message.timing, long + 1);
                if (error)
                    return "timing." + error;
            }
            return null;
        };

        /**
         * Creates a RunResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.RunResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.RunResult} RunResult
         */
        RunResult.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.RunResult)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.RunResult: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.RunResult();
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                message.status = 0;
                break;
            case "RUNNING":
            case 1:
                message.status = 1;
                break;
            case "SUCCESSFUL":
            case 2:
                message.status = 2;
                break;
            case "FAILED":
            case 3:
                message.status = 3;
                break;
            case "CANCELLED":
            case 4:
                message.status = 4;
                break;
            case "TIMED_OUT":
            case 5:
                message.status = 5;
                break;
            }
            if (object.actions) {
                if (!Array.isArray(object.actions))
                    throw TypeError(".dataform.RunResult.actions: array expected");
                message.actions = [];
                for (let i = 0; i < object.actions.length; ++i) {
                    if (!$util.isObject(object.actions[i]))
                        throw TypeError(".dataform.RunResult.actions: object expected");
                    message.actions[i] = $root.dataform.ActionResult.fromObject(object.actions[i], long + 1);
                }
            }
            if (object.timing != null) {
                if (!$util.isObject(object.timing))
                    throw TypeError(".dataform.RunResult.timing: object expected");
                message.timing = $root.dataform.Timing.fromObject(object.timing, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RunResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.RunResult
         * @static
         * @param {dataform.RunResult} message RunResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RunResult.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.actions = [];
            if (options.defaults) {
                object.status = options.enums === String ? "UNKNOWN" : 0;
                object.timing = null;
            }
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === String ? $root.dataform.RunResult.ExecutionStatus[message.status] === undefined ? message.status : $root.dataform.RunResult.ExecutionStatus[message.status] : message.status;
            if (message.actions && message.actions.length) {
                object.actions = [];
                for (let j = 0; j < message.actions.length; ++j)
                    object.actions[j] = $root.dataform.ActionResult.toObject(message.actions[j], options, q + 1);
            }
            if (message.timing != null && Object.hasOwnProperty.call(message, "timing"))
                object.timing = $root.dataform.Timing.toObject(message.timing, options, q + 1);
            return object;
        };

        /**
         * Converts this RunResult to JSON.
         * @function toJSON
         * @memberof dataform.RunResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RunResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RunResult
         * @function getTypeUrl
         * @memberof dataform.RunResult
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RunResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.RunResult";
        };

        /**
         * ExecutionStatus enum.
         * @name dataform.RunResult.ExecutionStatus
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} RUNNING=1 RUNNING value
         * @property {number} SUCCESSFUL=2 SUCCESSFUL value
         * @property {number} FAILED=3 FAILED value
         * @property {number} CANCELLED=4 CANCELLED value
         * @property {number} TIMED_OUT=5 TIMED_OUT value
         */
        RunResult.ExecutionStatus = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "RUNNING"] = 1;
            values[valuesById[2] = "SUCCESSFUL"] = 2;
            values[valuesById[3] = "FAILED"] = 3;
            values[valuesById[4] = "CANCELLED"] = 4;
            values[valuesById[5] = "TIMED_OUT"] = 5;
            return values;
        })();

        return RunResult;
    })();

    dataform.ActionResult = (function() {

        /**
         * Properties of an ActionResult.
         * @memberof dataform
         * @interface IActionResult
         * @property {dataform.ITarget|null} [target] ActionResult target
         * @property {dataform.ActionResult.ExecutionStatus|null} [status] ActionResult status
         * @property {Array.<dataform.ITaskResult>|null} [tasks] ActionResult tasks
         * @property {dataform.ITiming|null} [timing] ActionResult timing
         */

        /**
         * Constructs a new ActionResult.
         * @memberof dataform
         * @classdesc Represents an ActionResult.
         * @implements IActionResult
         * @constructor
         * @param {dataform.IActionResult=} [properties] Properties to set
         */
        function ActionResult(properties) {
            this.tasks = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ActionResult target.
         * @member {dataform.ITarget|null|undefined} target
         * @memberof dataform.ActionResult
         * @instance
         */
        ActionResult.prototype.target = null;

        /**
         * ActionResult status.
         * @member {dataform.ActionResult.ExecutionStatus} status
         * @memberof dataform.ActionResult
         * @instance
         */
        ActionResult.prototype.status = 0;

        /**
         * ActionResult tasks.
         * @member {Array.<dataform.ITaskResult>} tasks
         * @memberof dataform.ActionResult
         * @instance
         */
        ActionResult.prototype.tasks = $util.emptyArray;

        /**
         * ActionResult timing.
         * @member {dataform.ITiming|null|undefined} timing
         * @memberof dataform.ActionResult
         * @instance
         */
        ActionResult.prototype.timing = null;

        /**
         * Creates a new ActionResult instance using the specified properties.
         * @function create
         * @memberof dataform.ActionResult
         * @static
         * @param {dataform.IActionResult=} [properties] Properties to set
         * @returns {dataform.ActionResult} ActionResult instance
         */
        ActionResult.create = function create(properties) {
            return new ActionResult(properties);
        };

        /**
         * Encodes the specified ActionResult message. Does not implicitly {@link dataform.ActionResult.verify|verify} messages.
         * @function encode
         * @memberof dataform.ActionResult
         * @static
         * @param {dataform.IActionResult} message ActionResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ActionResult.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 2, wireType 0 =*/16).int32(message.status);
            if (message.tasks != null && message.tasks.length)
                for (let i = 0; i < message.tasks.length; ++i)
                    $root.dataform.TaskResult.encode(message.tasks[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.timing != null && Object.hasOwnProperty.call(message, "timing"))
                $root.dataform.Timing.encode(message.timing, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                $root.dataform.Target.encode(message.target, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ActionResult message, length delimited. Does not implicitly {@link dataform.ActionResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.ActionResult
         * @static
         * @param {dataform.IActionResult} message ActionResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ActionResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ActionResult message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.ActionResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.ActionResult} ActionResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ActionResult.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ActionResult();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 5: {
                        message.target = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.status = reader.int32();
                        break;
                    }
                case 3: {
                        if (!(message.tasks && message.tasks.length))
                            message.tasks = [];
                        message.tasks.push($root.dataform.TaskResult.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 4: {
                        message.timing = $root.dataform.Timing.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ActionResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.ActionResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.ActionResult} ActionResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ActionResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ActionResult message.
         * @function verify
         * @memberof dataform.ActionResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ActionResult.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.target != null && Object.hasOwnProperty.call(message, "target")) {
                let error = $root.dataform.Target.verify(message.target, long + 1);
                if (error)
                    return "target." + error;
            }
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                switch (message.status) {
                default:
                    return "status: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                case 6:
                    break;
                }
            if (message.tasks != null && Object.hasOwnProperty.call(message, "tasks")) {
                if (!Array.isArray(message.tasks))
                    return "tasks: array expected";
                for (let i = 0; i < message.tasks.length; ++i) {
                    let error = $root.dataform.TaskResult.verify(message.tasks[i], long + 1);
                    if (error)
                        return "tasks." + error;
                }
            }
            if (message.timing != null && Object.hasOwnProperty.call(message, "timing")) {
                let error = $root.dataform.Timing.verify(message.timing, long + 1);
                if (error)
                    return "timing." + error;
            }
            return null;
        };

        /**
         * Creates an ActionResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.ActionResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.ActionResult} ActionResult
         */
        ActionResult.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.ActionResult)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.ActionResult: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.ActionResult();
            if (object.target != null) {
                if (!$util.isObject(object.target))
                    throw TypeError(".dataform.ActionResult.target: object expected");
                message.target = $root.dataform.Target.fromObject(object.target, long + 1);
            }
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                message.status = 0;
                break;
            case "RUNNING":
            case 1:
                message.status = 1;
                break;
            case "SUCCESSFUL":
            case 2:
                message.status = 2;
                break;
            case "FAILED":
            case 3:
                message.status = 3;
                break;
            case "SKIPPED":
            case 4:
                message.status = 4;
                break;
            case "DISABLED":
            case 5:
                message.status = 5;
                break;
            case "CANCELLED":
            case 6:
                message.status = 6;
                break;
            }
            if (object.tasks) {
                if (!Array.isArray(object.tasks))
                    throw TypeError(".dataform.ActionResult.tasks: array expected");
                message.tasks = [];
                for (let i = 0; i < object.tasks.length; ++i) {
                    if (!$util.isObject(object.tasks[i]))
                        throw TypeError(".dataform.ActionResult.tasks: object expected");
                    message.tasks[i] = $root.dataform.TaskResult.fromObject(object.tasks[i], long + 1);
                }
            }
            if (object.timing != null) {
                if (!$util.isObject(object.timing))
                    throw TypeError(".dataform.ActionResult.timing: object expected");
                message.timing = $root.dataform.Timing.fromObject(object.timing, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an ActionResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.ActionResult
         * @static
         * @param {dataform.ActionResult} message ActionResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ActionResult.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.tasks = [];
            if (options.defaults) {
                object.status = options.enums === String ? "UNKNOWN" : 0;
                object.timing = null;
                object.target = null;
            }
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === String ? $root.dataform.ActionResult.ExecutionStatus[message.status] === undefined ? message.status : $root.dataform.ActionResult.ExecutionStatus[message.status] : message.status;
            if (message.tasks && message.tasks.length) {
                object.tasks = [];
                for (let j = 0; j < message.tasks.length; ++j)
                    object.tasks[j] = $root.dataform.TaskResult.toObject(message.tasks[j], options, q + 1);
            }
            if (message.timing != null && Object.hasOwnProperty.call(message, "timing"))
                object.timing = $root.dataform.Timing.toObject(message.timing, options, q + 1);
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                object.target = $root.dataform.Target.toObject(message.target, options, q + 1);
            return object;
        };

        /**
         * Converts this ActionResult to JSON.
         * @function toJSON
         * @memberof dataform.ActionResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ActionResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ActionResult
         * @function getTypeUrl
         * @memberof dataform.ActionResult
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ActionResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.ActionResult";
        };

        /**
         * ExecutionStatus enum.
         * @name dataform.ActionResult.ExecutionStatus
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} RUNNING=1 RUNNING value
         * @property {number} SUCCESSFUL=2 SUCCESSFUL value
         * @property {number} FAILED=3 FAILED value
         * @property {number} SKIPPED=4 SKIPPED value
         * @property {number} DISABLED=5 DISABLED value
         * @property {number} CANCELLED=6 CANCELLED value
         */
        ActionResult.ExecutionStatus = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "RUNNING"] = 1;
            values[valuesById[2] = "SUCCESSFUL"] = 2;
            values[valuesById[3] = "FAILED"] = 3;
            values[valuesById[4] = "SKIPPED"] = 4;
            values[valuesById[5] = "DISABLED"] = 5;
            values[valuesById[6] = "CANCELLED"] = 6;
            return values;
        })();

        return ActionResult;
    })();

    dataform.TaskResult = (function() {

        /**
         * Properties of a TaskResult.
         * @memberof dataform
         * @interface ITaskResult
         * @property {dataform.TaskResult.ExecutionStatus|null} [status] TaskResult status
         * @property {string|null} [errorMessage] TaskResult errorMessage
         * @property {dataform.ITiming|null} [timing] TaskResult timing
         * @property {dataform.IExecutionMetadata|null} [metadata] TaskResult metadata
         * @property {string|null} [compiledSql] TaskResult compiledSql
         */

        /**
         * Constructs a new TaskResult.
         * @memberof dataform
         * @classdesc Represents a TaskResult.
         * @implements ITaskResult
         * @constructor
         * @param {dataform.ITaskResult=} [properties] Properties to set
         */
        function TaskResult(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TaskResult status.
         * @member {dataform.TaskResult.ExecutionStatus} status
         * @memberof dataform.TaskResult
         * @instance
         */
        TaskResult.prototype.status = 0;

        /**
         * TaskResult errorMessage.
         * @member {string} errorMessage
         * @memberof dataform.TaskResult
         * @instance
         */
        TaskResult.prototype.errorMessage = "";

        /**
         * TaskResult timing.
         * @member {dataform.ITiming|null|undefined} timing
         * @memberof dataform.TaskResult
         * @instance
         */
        TaskResult.prototype.timing = null;

        /**
         * TaskResult metadata.
         * @member {dataform.IExecutionMetadata|null|undefined} metadata
         * @memberof dataform.TaskResult
         * @instance
         */
        TaskResult.prototype.metadata = null;

        /**
         * TaskResult compiledSql.
         * @member {string} compiledSql
         * @memberof dataform.TaskResult
         * @instance
         */
        TaskResult.prototype.compiledSql = "";

        /**
         * Creates a new TaskResult instance using the specified properties.
         * @function create
         * @memberof dataform.TaskResult
         * @static
         * @param {dataform.ITaskResult=} [properties] Properties to set
         * @returns {dataform.TaskResult} TaskResult instance
         */
        TaskResult.create = function create(properties) {
            return new TaskResult(properties);
        };

        /**
         * Encodes the specified TaskResult message. Does not implicitly {@link dataform.TaskResult.verify|verify} messages.
         * @function encode
         * @memberof dataform.TaskResult
         * @static
         * @param {dataform.ITaskResult} message TaskResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TaskResult.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                writer.uint32(/* id 1, wireType 0 =*/8).int32(message.status);
            if (message.errorMessage != null && Object.hasOwnProperty.call(message, "errorMessage"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.errorMessage);
            if (message.timing != null && Object.hasOwnProperty.call(message, "timing"))
                $root.dataform.Timing.encode(message.timing, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                $root.dataform.ExecutionMetadata.encode(message.metadata, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.compiledSql != null && Object.hasOwnProperty.call(message, "compiledSql"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.compiledSql);
            return writer;
        };

        /**
         * Encodes the specified TaskResult message, length delimited. Does not implicitly {@link dataform.TaskResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.TaskResult
         * @static
         * @param {dataform.ITaskResult} message TaskResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TaskResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TaskResult message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.TaskResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.TaskResult} TaskResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TaskResult.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.TaskResult();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.status = reader.int32();
                        break;
                    }
                case 2: {
                        message.errorMessage = reader.string();
                        break;
                    }
                case 3: {
                        message.timing = $root.dataform.Timing.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 4: {
                        message.metadata = $root.dataform.ExecutionMetadata.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 5: {
                        message.compiledSql = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TaskResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.TaskResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.TaskResult} TaskResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TaskResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TaskResult message.
         * @function verify
         * @memberof dataform.TaskResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TaskResult.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                switch (message.status) {
                default:
                    return "status: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                case 5:
                    break;
                }
            if (message.errorMessage != null && Object.hasOwnProperty.call(message, "errorMessage"))
                if (!$util.isString(message.errorMessage))
                    return "errorMessage: string expected";
            if (message.timing != null && Object.hasOwnProperty.call(message, "timing")) {
                let error = $root.dataform.Timing.verify(message.timing, long + 1);
                if (error)
                    return "timing." + error;
            }
            if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata")) {
                let error = $root.dataform.ExecutionMetadata.verify(message.metadata, long + 1);
                if (error)
                    return "metadata." + error;
            }
            if (message.compiledSql != null && Object.hasOwnProperty.call(message, "compiledSql"))
                if (!$util.isString(message.compiledSql))
                    return "compiledSql: string expected";
            return null;
        };

        /**
         * Creates a TaskResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.TaskResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.TaskResult} TaskResult
         */
        TaskResult.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.TaskResult)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.TaskResult: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.TaskResult();
            switch (object.status) {
            default:
                if (typeof object.status === "number") {
                    message.status = object.status;
                    break;
                }
                break;
            case "UNKNOWN":
            case 0:
                message.status = 0;
                break;
            case "RUNNING":
            case 1:
                message.status = 1;
                break;
            case "SUCCESSFUL":
            case 2:
                message.status = 2;
                break;
            case "FAILED":
            case 3:
                message.status = 3;
                break;
            case "SKIPPED":
            case 4:
                message.status = 4;
                break;
            case "CANCELLED":
            case 5:
                message.status = 5;
                break;
            }
            if (object.errorMessage != null)
                message.errorMessage = String(object.errorMessage);
            if (object.timing != null) {
                if (!$util.isObject(object.timing))
                    throw TypeError(".dataform.TaskResult.timing: object expected");
                message.timing = $root.dataform.Timing.fromObject(object.timing, long + 1);
            }
            if (object.metadata != null) {
                if (!$util.isObject(object.metadata))
                    throw TypeError(".dataform.TaskResult.metadata: object expected");
                message.metadata = $root.dataform.ExecutionMetadata.fromObject(object.metadata, long + 1);
            }
            if (object.compiledSql != null)
                message.compiledSql = String(object.compiledSql);
            return message;
        };

        /**
         * Creates a plain object from a TaskResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.TaskResult
         * @static
         * @param {dataform.TaskResult} message TaskResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TaskResult.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.status = options.enums === String ? "UNKNOWN" : 0;
                object.errorMessage = "";
                object.timing = null;
                object.metadata = null;
                object.compiledSql = "";
            }
            if (message.status != null && Object.hasOwnProperty.call(message, "status"))
                object.status = options.enums === String ? $root.dataform.TaskResult.ExecutionStatus[message.status] === undefined ? message.status : $root.dataform.TaskResult.ExecutionStatus[message.status] : message.status;
            if (message.errorMessage != null && Object.hasOwnProperty.call(message, "errorMessage"))
                object.errorMessage = message.errorMessage;
            if (message.timing != null && Object.hasOwnProperty.call(message, "timing"))
                object.timing = $root.dataform.Timing.toObject(message.timing, options, q + 1);
            if (message.metadata != null && Object.hasOwnProperty.call(message, "metadata"))
                object.metadata = $root.dataform.ExecutionMetadata.toObject(message.metadata, options, q + 1);
            if (message.compiledSql != null && Object.hasOwnProperty.call(message, "compiledSql"))
                object.compiledSql = message.compiledSql;
            return object;
        };

        /**
         * Converts this TaskResult to JSON.
         * @function toJSON
         * @memberof dataform.TaskResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TaskResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TaskResult
         * @function getTypeUrl
         * @memberof dataform.TaskResult
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TaskResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.TaskResult";
        };

        /**
         * ExecutionStatus enum.
         * @name dataform.TaskResult.ExecutionStatus
         * @enum {number}
         * @property {number} UNKNOWN=0 UNKNOWN value
         * @property {number} RUNNING=1 RUNNING value
         * @property {number} SUCCESSFUL=2 SUCCESSFUL value
         * @property {number} FAILED=3 FAILED value
         * @property {number} SKIPPED=4 SKIPPED value
         * @property {number} CANCELLED=5 CANCELLED value
         */
        TaskResult.ExecutionStatus = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "RUNNING"] = 1;
            values[valuesById[2] = "SUCCESSFUL"] = 2;
            values[valuesById[3] = "FAILED"] = 3;
            values[valuesById[4] = "SKIPPED"] = 4;
            values[valuesById[5] = "CANCELLED"] = 5;
            return values;
        })();

        return TaskResult;
    })();

    dataform.TestResult = (function() {

        /**
         * Properties of a TestResult.
         * @memberof dataform
         * @interface ITestResult
         * @property {string|null} [name] TestResult name
         * @property {boolean|null} [successful] TestResult successful
         * @property {Array.<string>|null} [messages] TestResult messages
         */

        /**
         * Constructs a new TestResult.
         * @memberof dataform
         * @classdesc Represents a TestResult.
         * @implements ITestResult
         * @constructor
         * @param {dataform.ITestResult=} [properties] Properties to set
         */
        function TestResult(properties) {
            this.messages = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * TestResult name.
         * @member {string} name
         * @memberof dataform.TestResult
         * @instance
         */
        TestResult.prototype.name = "";

        /**
         * TestResult successful.
         * @member {boolean} successful
         * @memberof dataform.TestResult
         * @instance
         */
        TestResult.prototype.successful = false;

        /**
         * TestResult messages.
         * @member {Array.<string>} messages
         * @memberof dataform.TestResult
         * @instance
         */
        TestResult.prototype.messages = $util.emptyArray;

        /**
         * Creates a new TestResult instance using the specified properties.
         * @function create
         * @memberof dataform.TestResult
         * @static
         * @param {dataform.ITestResult=} [properties] Properties to set
         * @returns {dataform.TestResult} TestResult instance
         */
        TestResult.create = function create(properties) {
            return new TestResult(properties);
        };

        /**
         * Encodes the specified TestResult message. Does not implicitly {@link dataform.TestResult.verify|verify} messages.
         * @function encode
         * @memberof dataform.TestResult
         * @static
         * @param {dataform.ITestResult} message TestResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TestResult.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.name);
            if (message.successful != null && Object.hasOwnProperty.call(message, "successful"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.successful);
            if (message.messages != null && message.messages.length)
                for (let i = 0; i < message.messages.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.messages[i]);
            return writer;
        };

        /**
         * Encodes the specified TestResult message, length delimited. Does not implicitly {@link dataform.TestResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.TestResult
         * @static
         * @param {dataform.ITestResult} message TestResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        TestResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a TestResult message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.TestResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.TestResult} TestResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TestResult.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.TestResult();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.name = reader.string();
                        break;
                    }
                case 2: {
                        message.successful = reader.bool();
                        break;
                    }
                case 3: {
                        if (!(message.messages && message.messages.length))
                            message.messages = [];
                        message.messages.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a TestResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.TestResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.TestResult} TestResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        TestResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a TestResult message.
         * @function verify
         * @memberof dataform.TestResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        TestResult.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                if (!$util.isString(message.name))
                    return "name: string expected";
            if (message.successful != null && Object.hasOwnProperty.call(message, "successful"))
                if (typeof message.successful !== "boolean")
                    return "successful: boolean expected";
            if (message.messages != null && Object.hasOwnProperty.call(message, "messages")) {
                if (!Array.isArray(message.messages))
                    return "messages: array expected";
                for (let i = 0; i < message.messages.length; ++i)
                    if (!$util.isString(message.messages[i]))
                        return "messages: string[] expected";
            }
            return null;
        };

        /**
         * Creates a TestResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.TestResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.TestResult} TestResult
         */
        TestResult.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.TestResult)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.TestResult: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.TestResult();
            if (object.name != null)
                message.name = String(object.name);
            if (object.successful != null)
                message.successful = Boolean(object.successful);
            if (object.messages) {
                if (!Array.isArray(object.messages))
                    throw TypeError(".dataform.TestResult.messages: array expected");
                message.messages = [];
                for (let i = 0; i < object.messages.length; ++i)
                    message.messages[i] = String(object.messages[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a TestResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.TestResult
         * @static
         * @param {dataform.TestResult} message TestResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        TestResult.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.messages = [];
            if (options.defaults) {
                object.name = "";
                object.successful = false;
            }
            if (message.name != null && Object.hasOwnProperty.call(message, "name"))
                object.name = message.name;
            if (message.successful != null && Object.hasOwnProperty.call(message, "successful"))
                object.successful = message.successful;
            if (message.messages && message.messages.length) {
                object.messages = [];
                for (let j = 0; j < message.messages.length; ++j)
                    object.messages[j] = message.messages[j];
            }
            return object;
        };

        /**
         * Converts this TestResult to JSON.
         * @function toJSON
         * @memberof dataform.TestResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        TestResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for TestResult
         * @function getTypeUrl
         * @memberof dataform.TestResult
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        TestResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.TestResult";
        };

        return TestResult;
    })();

    dataform.DbAdapter = (function() {

        /**
         * Constructs a new DbAdapter service.
         * @memberof dataform
         * @classdesc Represents a DbAdapter
         * @extends $protobuf.rpc.Service
         * @constructor
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         */
        function DbAdapter(rpcImpl, requestDelimited, responseDelimited) {
            $protobuf__namespace.rpc.Service.call(this, rpcImpl, requestDelimited, responseDelimited);
        }

        (DbAdapter.prototype = Object.create($protobuf__namespace.rpc.Service.prototype)).constructor = DbAdapter;

        /**
         * Creates new DbAdapter service using the specified rpc implementation.
         * @function create
         * @memberof dataform.DbAdapter
         * @static
         * @param {$protobuf.RPCImpl} rpcImpl RPC implementation
         * @param {boolean} [requestDelimited=false] Whether requests are length-delimited
         * @param {boolean} [responseDelimited=false] Whether responses are length-delimited
         * @returns {DbAdapter} RPC service. Useful where requests and/or responses are streamed.
         */
        DbAdapter.create = function create(rpcImpl, requestDelimited, responseDelimited) {
            return new this(rpcImpl, requestDelimited, responseDelimited);
        };

        /**
         * Callback as used by {@link dataform.DbAdapter#execute}.
         * @memberof dataform.DbAdapter
         * @typedef ExecuteCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {dataform.ExecuteResponse} [response] ExecuteResponse
         */

        /**
         * Calls Execute.
         * @function execute
         * @memberof dataform.DbAdapter
         * @instance
         * @param {dataform.IExecuteRequest} request ExecuteRequest message or plain object
         * @param {dataform.DbAdapter.ExecuteCallback} callback Node-style callback called with the error, if any, and ExecuteResponse
         * @returns {undefined}
         * @variation 1
         */
        Object.defineProperty(DbAdapter.prototype.execute = function execute(request, callback) {
            return $protobuf__namespace.rpc.Service.prototype.rpcCall.call(this, execute, $root.dataform.ExecuteRequest, $root.dataform.ExecuteResponse, request, callback);
        }, "name", { value: "Execute" });

        /**
         * Calls Execute.
         * @function execute
         * @memberof dataform.DbAdapter
         * @instance
         * @param {dataform.IExecuteRequest} request ExecuteRequest message or plain object
         * @returns {Promise<dataform.ExecuteResponse>} Promise
         * @variation 2
         */

        /**
         * Callback as used by {@link dataform.DbAdapter#listTables}.
         * @memberof dataform.DbAdapter
         * @typedef ListTablesCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {dataform.ListTablesResponse} [response] ListTablesResponse
         */

        /**
         * Calls ListTables.
         * @function listTables
         * @memberof dataform.DbAdapter
         * @instance
         * @param {dataform.IListTablesRequest} request ListTablesRequest message or plain object
         * @param {dataform.DbAdapter.ListTablesCallback} callback Node-style callback called with the error, if any, and ListTablesResponse
         * @returns {undefined}
         * @variation 1
         */
        Object.defineProperty(DbAdapter.prototype.listTables = function listTables(request, callback) {
            return $protobuf__namespace.rpc.Service.prototype.rpcCall.call(this, listTables, $root.dataform.ListTablesRequest, $root.dataform.ListTablesResponse, request, callback);
        }, "name", { value: "ListTables" });

        /**
         * Calls ListTables.
         * @function listTables
         * @memberof dataform.DbAdapter
         * @instance
         * @param {dataform.IListTablesRequest} request ListTablesRequest message or plain object
         * @returns {Promise<dataform.ListTablesResponse>} Promise
         * @variation 2
         */

        /**
         * Callback as used by {@link dataform.DbAdapter#getTable}.
         * @memberof dataform.DbAdapter
         * @typedef GetTableCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {dataform.TableMetadata} [response] TableMetadata
         */

        /**
         * Calls GetTable.
         * @function getTable
         * @memberof dataform.DbAdapter
         * @instance
         * @param {dataform.IGetTableRequest} request GetTableRequest message or plain object
         * @param {dataform.DbAdapter.GetTableCallback} callback Node-style callback called with the error, if any, and TableMetadata
         * @returns {undefined}
         * @variation 1
         */
        Object.defineProperty(DbAdapter.prototype.getTable = function getTable(request, callback) {
            return $protobuf__namespace.rpc.Service.prototype.rpcCall.call(this, getTable, $root.dataform.GetTableRequest, $root.dataform.TableMetadata, request, callback);
        }, "name", { value: "GetTable" });

        /**
         * Calls GetTable.
         * @function getTable
         * @memberof dataform.DbAdapter
         * @instance
         * @param {dataform.IGetTableRequest} request GetTableRequest message or plain object
         * @returns {Promise<dataform.TableMetadata>} Promise
         * @variation 2
         */

        /**
         * Callback as used by {@link dataform.DbAdapter#deleteTable}.
         * @memberof dataform.DbAdapter
         * @typedef DeleteTableCallback
         * @type {function}
         * @param {Error|null} error Error, if any
         * @param {google.protobuf.Empty} [response] Empty
         */

        /**
         * Calls DeleteTable.
         * @function deleteTable
         * @memberof dataform.DbAdapter
         * @instance
         * @param {dataform.IDeleteTableRequest} request DeleteTableRequest message or plain object
         * @param {dataform.DbAdapter.DeleteTableCallback} callback Node-style callback called with the error, if any, and Empty
         * @returns {undefined}
         * @variation 1
         */
        Object.defineProperty(DbAdapter.prototype.deleteTable = function deleteTable(request, callback) {
            return $protobuf__namespace.rpc.Service.prototype.rpcCall.call(this, deleteTable, $root.dataform.DeleteTableRequest, $root.google.protobuf.Empty, request, callback);
        }, "name", { value: "DeleteTable" });

        /**
         * Calls DeleteTable.
         * @function deleteTable
         * @memberof dataform.DbAdapter
         * @instance
         * @param {dataform.IDeleteTableRequest} request DeleteTableRequest message or plain object
         * @returns {Promise<google.protobuf.Empty>} Promise
         * @variation 2
         */

        return DbAdapter;
    })();

    dataform.ExecuteRequest = (function() {

        /**
         * Properties of an ExecuteRequest.
         * @memberof dataform
         * @interface IExecuteRequest
         * @property {string|null} [statement] ExecuteRequest statement
         * @property {Long|null} [rowLimit] ExecuteRequest rowLimit
         * @property {Long|null} [byteLimit] ExecuteRequest byteLimit
         * @property {boolean|null} [fetchResults] ExecuteRequest fetchResults
         * @property {dataform.IBigQueryExecuteOptions|null} [bigQueryOptions] ExecuteRequest bigQueryOptions
         * @property {google.protobuf.IStruct|null} [params] ExecuteRequest params
         */

        /**
         * Constructs a new ExecuteRequest.
         * @memberof dataform
         * @classdesc Represents an ExecuteRequest.
         * @implements IExecuteRequest
         * @constructor
         * @param {dataform.IExecuteRequest=} [properties] Properties to set
         */
        function ExecuteRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ExecuteRequest statement.
         * @member {string} statement
         * @memberof dataform.ExecuteRequest
         * @instance
         */
        ExecuteRequest.prototype.statement = "";

        /**
         * ExecuteRequest rowLimit.
         * @member {Long} rowLimit
         * @memberof dataform.ExecuteRequest
         * @instance
         */
        ExecuteRequest.prototype.rowLimit = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ExecuteRequest byteLimit.
         * @member {Long} byteLimit
         * @memberof dataform.ExecuteRequest
         * @instance
         */
        ExecuteRequest.prototype.byteLimit = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

        /**
         * ExecuteRequest fetchResults.
         * @member {boolean} fetchResults
         * @memberof dataform.ExecuteRequest
         * @instance
         */
        ExecuteRequest.prototype.fetchResults = false;

        /**
         * ExecuteRequest bigQueryOptions.
         * @member {dataform.IBigQueryExecuteOptions|null|undefined} bigQueryOptions
         * @memberof dataform.ExecuteRequest
         * @instance
         */
        ExecuteRequest.prototype.bigQueryOptions = null;

        /**
         * ExecuteRequest params.
         * @member {google.protobuf.IStruct|null|undefined} params
         * @memberof dataform.ExecuteRequest
         * @instance
         */
        ExecuteRequest.prototype.params = null;

        /**
         * Creates a new ExecuteRequest instance using the specified properties.
         * @function create
         * @memberof dataform.ExecuteRequest
         * @static
         * @param {dataform.IExecuteRequest=} [properties] Properties to set
         * @returns {dataform.ExecuteRequest} ExecuteRequest instance
         */
        ExecuteRequest.create = function create(properties) {
            return new ExecuteRequest(properties);
        };

        /**
         * Encodes the specified ExecuteRequest message. Does not implicitly {@link dataform.ExecuteRequest.verify|verify} messages.
         * @function encode
         * @memberof dataform.ExecuteRequest
         * @static
         * @param {dataform.IExecuteRequest} message ExecuteRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExecuteRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.statement != null && Object.hasOwnProperty.call(message, "statement"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.statement);
            if (message.rowLimit != null && Object.hasOwnProperty.call(message, "rowLimit"))
                writer.uint32(/* id 2, wireType 0 =*/16).int64(message.rowLimit);
            if (message.byteLimit != null && Object.hasOwnProperty.call(message, "byteLimit"))
                writer.uint32(/* id 3, wireType 0 =*/24).int64(message.byteLimit);
            if (message.fetchResults != null && Object.hasOwnProperty.call(message, "fetchResults"))
                writer.uint32(/* id 4, wireType 0 =*/32).bool(message.fetchResults);
            if (message.bigQueryOptions != null && Object.hasOwnProperty.call(message, "bigQueryOptions"))
                $root.dataform.BigQueryExecuteOptions.encode(message.bigQueryOptions, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
            if (message.params != null && Object.hasOwnProperty.call(message, "params"))
                $root.google.protobuf.Struct.encode(message.params, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ExecuteRequest message, length delimited. Does not implicitly {@link dataform.ExecuteRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.ExecuteRequest
         * @static
         * @param {dataform.IExecuteRequest} message ExecuteRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExecuteRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ExecuteRequest message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.ExecuteRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.ExecuteRequest} ExecuteRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExecuteRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ExecuteRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.statement = reader.string();
                        break;
                    }
                case 2: {
                        message.rowLimit = reader.int64();
                        break;
                    }
                case 3: {
                        message.byteLimit = reader.int64();
                        break;
                    }
                case 4: {
                        message.fetchResults = reader.bool();
                        break;
                    }
                case 5: {
                        message.bigQueryOptions = $root.dataform.BigQueryExecuteOptions.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 6: {
                        message.params = $root.google.protobuf.Struct.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ExecuteRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.ExecuteRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.ExecuteRequest} ExecuteRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExecuteRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExecuteRequest message.
         * @function verify
         * @memberof dataform.ExecuteRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExecuteRequest.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.statement != null && Object.hasOwnProperty.call(message, "statement"))
                if (!$util.isString(message.statement))
                    return "statement: string expected";
            if (message.rowLimit != null && Object.hasOwnProperty.call(message, "rowLimit"))
                if (!$util.isInteger(message.rowLimit) && !(message.rowLimit && $util.isInteger(message.rowLimit.low) && $util.isInteger(message.rowLimit.high)))
                    return "rowLimit: integer|Long expected";
            if (message.byteLimit != null && Object.hasOwnProperty.call(message, "byteLimit"))
                if (!$util.isInteger(message.byteLimit) && !(message.byteLimit && $util.isInteger(message.byteLimit.low) && $util.isInteger(message.byteLimit.high)))
                    return "byteLimit: integer|Long expected";
            if (message.fetchResults != null && Object.hasOwnProperty.call(message, "fetchResults"))
                if (typeof message.fetchResults !== "boolean")
                    return "fetchResults: boolean expected";
            if (message.bigQueryOptions != null && Object.hasOwnProperty.call(message, "bigQueryOptions")) {
                let error = $root.dataform.BigQueryExecuteOptions.verify(message.bigQueryOptions, long + 1);
                if (error)
                    return "bigQueryOptions." + error;
            }
            if (message.params != null && Object.hasOwnProperty.call(message, "params")) {
                let error = $root.google.protobuf.Struct.verify(message.params, long + 1);
                if (error)
                    return "params." + error;
            }
            return null;
        };

        /**
         * Creates an ExecuteRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.ExecuteRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.ExecuteRequest} ExecuteRequest
         */
        ExecuteRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.ExecuteRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.ExecuteRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.ExecuteRequest();
            if (object.statement != null)
                message.statement = String(object.statement);
            if (object.rowLimit != null)
                if ($util.Long)
                    message.rowLimit = $util.Long.fromValue(object.rowLimit, false);
                else if (typeof object.rowLimit === "string")
                    message.rowLimit = parseInt(object.rowLimit, 10);
                else if (typeof object.rowLimit === "number")
                    message.rowLimit = object.rowLimit;
                else if (typeof object.rowLimit === "object")
                    message.rowLimit = new $util.LongBits(object.rowLimit.low >>> 0, object.rowLimit.high >>> 0).toNumber();
            if (object.byteLimit != null)
                if ($util.Long)
                    message.byteLimit = $util.Long.fromValue(object.byteLimit, false);
                else if (typeof object.byteLimit === "string")
                    message.byteLimit = parseInt(object.byteLimit, 10);
                else if (typeof object.byteLimit === "number")
                    message.byteLimit = object.byteLimit;
                else if (typeof object.byteLimit === "object")
                    message.byteLimit = new $util.LongBits(object.byteLimit.low >>> 0, object.byteLimit.high >>> 0).toNumber();
            if (object.fetchResults != null)
                message.fetchResults = Boolean(object.fetchResults);
            if (object.bigQueryOptions != null) {
                if (!$util.isObject(object.bigQueryOptions))
                    throw TypeError(".dataform.ExecuteRequest.bigQueryOptions: object expected");
                message.bigQueryOptions = $root.dataform.BigQueryExecuteOptions.fromObject(object.bigQueryOptions, long + 1);
            }
            if (object.params != null) {
                if (!$util.isObject(object.params))
                    throw TypeError(".dataform.ExecuteRequest.params: object expected");
                message.params = $root.google.protobuf.Struct.fromObject(object.params, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from an ExecuteRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.ExecuteRequest
         * @static
         * @param {dataform.ExecuteRequest} message ExecuteRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExecuteRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.statement = "";
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.rowLimit = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.rowLimit = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                if ($util.Long) {
                    let long = new $util.Long(0, 0, false);
                    object.byteLimit = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                } else
                    object.byteLimit = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                object.fetchResults = false;
                object.bigQueryOptions = null;
                object.params = null;
            }
            if (message.statement != null && Object.hasOwnProperty.call(message, "statement"))
                object.statement = message.statement;
            if (message.rowLimit != null && Object.hasOwnProperty.call(message, "rowLimit"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.rowLimit = typeof message.rowLimit === "number" ? BigInt(message.rowLimit) : $util.Long.fromBits(message.rowLimit.low >>> 0, message.rowLimit.high >>> 0, false).toBigInt();
                else if (typeof message.rowLimit === "number")
                    object.rowLimit = options.longs === String ? String(message.rowLimit) : message.rowLimit;
                else
                    object.rowLimit = options.longs === String ? $util.Long.prototype.toString.call(message.rowLimit) : options.longs === Number ? new $util.LongBits(message.rowLimit.low >>> 0, message.rowLimit.high >>> 0).toNumber() : message.rowLimit;
            if (message.byteLimit != null && Object.hasOwnProperty.call(message, "byteLimit"))
                if (typeof BigInt !== "undefined" && options.longs === BigInt)
                    object.byteLimit = typeof message.byteLimit === "number" ? BigInt(message.byteLimit) : $util.Long.fromBits(message.byteLimit.low >>> 0, message.byteLimit.high >>> 0, false).toBigInt();
                else if (typeof message.byteLimit === "number")
                    object.byteLimit = options.longs === String ? String(message.byteLimit) : message.byteLimit;
                else
                    object.byteLimit = options.longs === String ? $util.Long.prototype.toString.call(message.byteLimit) : options.longs === Number ? new $util.LongBits(message.byteLimit.low >>> 0, message.byteLimit.high >>> 0).toNumber() : message.byteLimit;
            if (message.fetchResults != null && Object.hasOwnProperty.call(message, "fetchResults"))
                object.fetchResults = message.fetchResults;
            if (message.bigQueryOptions != null && Object.hasOwnProperty.call(message, "bigQueryOptions"))
                object.bigQueryOptions = $root.dataform.BigQueryExecuteOptions.toObject(message.bigQueryOptions, options, q + 1);
            if (message.params != null && Object.hasOwnProperty.call(message, "params"))
                object.params = $root.google.protobuf.Struct.toObject(message.params, options, q + 1);
            return object;
        };

        /**
         * Converts this ExecuteRequest to JSON.
         * @function toJSON
         * @memberof dataform.ExecuteRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExecuteRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ExecuteRequest
         * @function getTypeUrl
         * @memberof dataform.ExecuteRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ExecuteRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.ExecuteRequest";
        };

        return ExecuteRequest;
    })();

    dataform.BigQueryExecuteOptions = (function() {

        /**
         * Properties of a BigQueryExecuteOptions.
         * @memberof dataform
         * @interface IBigQueryExecuteOptions
         * @property {boolean|null} [interactive] BigQueryExecuteOptions interactive
         * @property {boolean|null} [useLegacySql] BigQueryExecuteOptions useLegacySql
         * @property {Object.<string,string>|null} [labels] BigQueryExecuteOptions labels
         * @property {string|null} [location] BigQueryExecuteOptions location
         * @property {string|null} [jobPrefix] BigQueryExecuteOptions jobPrefix
         * @property {boolean|null} [dryRun] BigQueryExecuteOptions dryRun
         * @property {string|null} [reservation] BigQueryExecuteOptions reservation
         */

        /**
         * Constructs a new BigQueryExecuteOptions.
         * @memberof dataform
         * @classdesc Represents a BigQueryExecuteOptions.
         * @implements IBigQueryExecuteOptions
         * @constructor
         * @param {dataform.IBigQueryExecuteOptions=} [properties] Properties to set
         */
        function BigQueryExecuteOptions(properties) {
            this.labels = {};
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BigQueryExecuteOptions interactive.
         * @member {boolean} interactive
         * @memberof dataform.BigQueryExecuteOptions
         * @instance
         */
        BigQueryExecuteOptions.prototype.interactive = false;

        /**
         * BigQueryExecuteOptions useLegacySql.
         * @member {boolean} useLegacySql
         * @memberof dataform.BigQueryExecuteOptions
         * @instance
         */
        BigQueryExecuteOptions.prototype.useLegacySql = false;

        /**
         * BigQueryExecuteOptions labels.
         * @member {Object.<string,string>} labels
         * @memberof dataform.BigQueryExecuteOptions
         * @instance
         */
        BigQueryExecuteOptions.prototype.labels = $util.emptyObject;

        /**
         * BigQueryExecuteOptions location.
         * @member {string} location
         * @memberof dataform.BigQueryExecuteOptions
         * @instance
         */
        BigQueryExecuteOptions.prototype.location = "";

        /**
         * BigQueryExecuteOptions jobPrefix.
         * @member {string} jobPrefix
         * @memberof dataform.BigQueryExecuteOptions
         * @instance
         */
        BigQueryExecuteOptions.prototype.jobPrefix = "";

        /**
         * BigQueryExecuteOptions dryRun.
         * @member {boolean} dryRun
         * @memberof dataform.BigQueryExecuteOptions
         * @instance
         */
        BigQueryExecuteOptions.prototype.dryRun = false;

        /**
         * BigQueryExecuteOptions reservation.
         * @member {string} reservation
         * @memberof dataform.BigQueryExecuteOptions
         * @instance
         */
        BigQueryExecuteOptions.prototype.reservation = "";

        /**
         * Creates a new BigQueryExecuteOptions instance using the specified properties.
         * @function create
         * @memberof dataform.BigQueryExecuteOptions
         * @static
         * @param {dataform.IBigQueryExecuteOptions=} [properties] Properties to set
         * @returns {dataform.BigQueryExecuteOptions} BigQueryExecuteOptions instance
         */
        BigQueryExecuteOptions.create = function create(properties) {
            return new BigQueryExecuteOptions(properties);
        };

        /**
         * Encodes the specified BigQueryExecuteOptions message. Does not implicitly {@link dataform.BigQueryExecuteOptions.verify|verify} messages.
         * @function encode
         * @memberof dataform.BigQueryExecuteOptions
         * @static
         * @param {dataform.IBigQueryExecuteOptions} message BigQueryExecuteOptions message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BigQueryExecuteOptions.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.interactive != null && Object.hasOwnProperty.call(message, "interactive"))
                writer.uint32(/* id 1, wireType 0 =*/8).bool(message.interactive);
            if (message.useLegacySql != null && Object.hasOwnProperty.call(message, "useLegacySql"))
                writer.uint32(/* id 2, wireType 0 =*/16).bool(message.useLegacySql);
            if (message.labels != null && Object.hasOwnProperty.call(message, "labels"))
                for (let keys = Object.keys(message.labels), i = 0; i < keys.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 2 =*/18).string(message.labels[keys[i]]).ldelim();
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.location);
            if (message.jobPrefix != null && Object.hasOwnProperty.call(message, "jobPrefix"))
                writer.uint32(/* id 5, wireType 2 =*/42).string(message.jobPrefix);
            if (message.dryRun != null && Object.hasOwnProperty.call(message, "dryRun"))
                writer.uint32(/* id 6, wireType 0 =*/48).bool(message.dryRun);
            if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                writer.uint32(/* id 7, wireType 2 =*/58).string(message.reservation);
            return writer;
        };

        /**
         * Encodes the specified BigQueryExecuteOptions message, length delimited. Does not implicitly {@link dataform.BigQueryExecuteOptions.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.BigQueryExecuteOptions
         * @static
         * @param {dataform.IBigQueryExecuteOptions} message BigQueryExecuteOptions message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BigQueryExecuteOptions.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BigQueryExecuteOptions message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.BigQueryExecuteOptions
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.BigQueryExecuteOptions} BigQueryExecuteOptions
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BigQueryExecuteOptions.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.BigQueryExecuteOptions(), key, value;
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.interactive = reader.bool();
                        break;
                    }
                case 2: {
                        message.useLegacySql = reader.bool();
                        break;
                    }
                case 3: {
                        if (message.labels === $util.emptyObject)
                            message.labels = {};
                        let end2 = reader.uint32() + reader.pos;
                        key = "";
                        value = "";
                        while (reader.pos < end2) {
                            let tag2 = reader.uint32();
                            switch (tag2 >>> 3) {
                            case 1:
                                key = reader.string();
                                break;
                            case 2:
                                value = reader.string();
                                break;
                            default:
                                reader.skipType(tag2 & 7, long);
                                break;
                            }
                        }
                        if (key === "__proto__")
                            $util.makeProp(message.labels, key);
                        message.labels[key] = value;
                        break;
                    }
                case 4: {
                        message.location = reader.string();
                        break;
                    }
                case 5: {
                        message.jobPrefix = reader.string();
                        break;
                    }
                case 6: {
                        message.dryRun = reader.bool();
                        break;
                    }
                case 7: {
                        message.reservation = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BigQueryExecuteOptions message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.BigQueryExecuteOptions
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.BigQueryExecuteOptions} BigQueryExecuteOptions
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BigQueryExecuteOptions.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BigQueryExecuteOptions message.
         * @function verify
         * @memberof dataform.BigQueryExecuteOptions
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BigQueryExecuteOptions.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.interactive != null && Object.hasOwnProperty.call(message, "interactive"))
                if (typeof message.interactive !== "boolean")
                    return "interactive: boolean expected";
            if (message.useLegacySql != null && Object.hasOwnProperty.call(message, "useLegacySql"))
                if (typeof message.useLegacySql !== "boolean")
                    return "useLegacySql: boolean expected";
            if (message.labels != null && Object.hasOwnProperty.call(message, "labels")) {
                if (!$util.isObject(message.labels))
                    return "labels: object expected";
                let key = Object.keys(message.labels);
                for (let i = 0; i < key.length; ++i)
                    if (!$util.isString(message.labels[key[i]]))
                        return "labels: string{k:string} expected";
            }
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                if (!$util.isString(message.location))
                    return "location: string expected";
            if (message.jobPrefix != null && Object.hasOwnProperty.call(message, "jobPrefix"))
                if (!$util.isString(message.jobPrefix))
                    return "jobPrefix: string expected";
            if (message.dryRun != null && Object.hasOwnProperty.call(message, "dryRun"))
                if (typeof message.dryRun !== "boolean")
                    return "dryRun: boolean expected";
            if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                if (!$util.isString(message.reservation))
                    return "reservation: string expected";
            return null;
        };

        /**
         * Creates a BigQueryExecuteOptions message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.BigQueryExecuteOptions
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.BigQueryExecuteOptions} BigQueryExecuteOptions
         */
        BigQueryExecuteOptions.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.BigQueryExecuteOptions)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.BigQueryExecuteOptions: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.BigQueryExecuteOptions();
            if (object.interactive != null)
                message.interactive = Boolean(object.interactive);
            if (object.useLegacySql != null)
                message.useLegacySql = Boolean(object.useLegacySql);
            if (object.labels) {
                if (!$util.isObject(object.labels))
                    throw TypeError(".dataform.BigQueryExecuteOptions.labels: object expected");
                message.labels = {};
                for (let keys = Object.keys(object.labels), i = 0; i < keys.length; ++i) {
                    if (keys[i] === "__proto__")
                        $util.makeProp(message.labels, keys[i]);
                    message.labels[keys[i]] = String(object.labels[keys[i]]);
                }
            }
            if (object.location != null)
                message.location = String(object.location);
            if (object.jobPrefix != null)
                message.jobPrefix = String(object.jobPrefix);
            if (object.dryRun != null)
                message.dryRun = Boolean(object.dryRun);
            if (object.reservation != null)
                message.reservation = String(object.reservation);
            return message;
        };

        /**
         * Creates a plain object from a BigQueryExecuteOptions message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.BigQueryExecuteOptions
         * @static
         * @param {dataform.BigQueryExecuteOptions} message BigQueryExecuteOptions
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BigQueryExecuteOptions.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.objects || options.defaults)
                object.labels = {};
            if (options.defaults) {
                object.interactive = false;
                object.useLegacySql = false;
                object.location = "";
                object.jobPrefix = "";
                object.dryRun = false;
                object.reservation = "";
            }
            if (message.interactive != null && Object.hasOwnProperty.call(message, "interactive"))
                object.interactive = message.interactive;
            if (message.useLegacySql != null && Object.hasOwnProperty.call(message, "useLegacySql"))
                object.useLegacySql = message.useLegacySql;
            let keys2;
            if (message.labels && (keys2 = Object.keys(message.labels)).length) {
                object.labels = {};
                for (let j = 0; j < keys2.length; ++j) {
                    if (keys2[j] === "__proto__")
                        $util.makeProp(object.labels, keys2[j]);
                    object.labels[keys2[j]] = message.labels[keys2[j]];
                }
            }
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                object.location = message.location;
            if (message.jobPrefix != null && Object.hasOwnProperty.call(message, "jobPrefix"))
                object.jobPrefix = message.jobPrefix;
            if (message.dryRun != null && Object.hasOwnProperty.call(message, "dryRun"))
                object.dryRun = message.dryRun;
            if (message.reservation != null && Object.hasOwnProperty.call(message, "reservation"))
                object.reservation = message.reservation;
            return object;
        };

        /**
         * Converts this BigQueryExecuteOptions to JSON.
         * @function toJSON
         * @memberof dataform.BigQueryExecuteOptions
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BigQueryExecuteOptions.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BigQueryExecuteOptions
         * @function getTypeUrl
         * @memberof dataform.BigQueryExecuteOptions
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BigQueryExecuteOptions.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.BigQueryExecuteOptions";
        };

        return BigQueryExecuteOptions;
    })();

    dataform.ExecuteResponse = (function() {

        /**
         * Properties of an ExecuteResponse.
         * @memberof dataform
         * @interface IExecuteResponse
         * @property {dataform.IExecutionMetadata|null} [executionMetadata] ExecuteResponse executionMetadata
         * @property {Array.<google.protobuf.IStruct>|null} [rows] ExecuteResponse rows
         * @property {Array.<string>|null} [errors] ExecuteResponse errors
         * @property {Array.<dataform.IField>|null} [schemaFields] ExecuteResponse schemaFields
         */

        /**
         * Constructs a new ExecuteResponse.
         * @memberof dataform
         * @classdesc Represents an ExecuteResponse.
         * @implements IExecuteResponse
         * @constructor
         * @param {dataform.IExecuteResponse=} [properties] Properties to set
         */
        function ExecuteResponse(properties) {
            this.rows = [];
            this.errors = [];
            this.schemaFields = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ExecuteResponse executionMetadata.
         * @member {dataform.IExecutionMetadata|null|undefined} executionMetadata
         * @memberof dataform.ExecuteResponse
         * @instance
         */
        ExecuteResponse.prototype.executionMetadata = null;

        /**
         * ExecuteResponse rows.
         * @member {Array.<google.protobuf.IStruct>} rows
         * @memberof dataform.ExecuteResponse
         * @instance
         */
        ExecuteResponse.prototype.rows = $util.emptyArray;

        /**
         * ExecuteResponse errors.
         * @member {Array.<string>} errors
         * @memberof dataform.ExecuteResponse
         * @instance
         */
        ExecuteResponse.prototype.errors = $util.emptyArray;

        /**
         * ExecuteResponse schemaFields.
         * @member {Array.<dataform.IField>} schemaFields
         * @memberof dataform.ExecuteResponse
         * @instance
         */
        ExecuteResponse.prototype.schemaFields = $util.emptyArray;

        /**
         * Creates a new ExecuteResponse instance using the specified properties.
         * @function create
         * @memberof dataform.ExecuteResponse
         * @static
         * @param {dataform.IExecuteResponse=} [properties] Properties to set
         * @returns {dataform.ExecuteResponse} ExecuteResponse instance
         */
        ExecuteResponse.create = function create(properties) {
            return new ExecuteResponse(properties);
        };

        /**
         * Encodes the specified ExecuteResponse message. Does not implicitly {@link dataform.ExecuteResponse.verify|verify} messages.
         * @function encode
         * @memberof dataform.ExecuteResponse
         * @static
         * @param {dataform.IExecuteResponse} message ExecuteResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExecuteResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.executionMetadata != null && Object.hasOwnProperty.call(message, "executionMetadata"))
                $root.dataform.ExecutionMetadata.encode(message.executionMetadata, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.rows != null && message.rows.length)
                for (let i = 0; i < message.rows.length; ++i)
                    $root.google.protobuf.Struct.encode(message.rows[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.errors != null && message.errors.length)
                for (let i = 0; i < message.errors.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.errors[i]);
            if (message.schemaFields != null && message.schemaFields.length)
                for (let i = 0; i < message.schemaFields.length; ++i)
                    $root.dataform.Field.encode(message.schemaFields[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ExecuteResponse message, length delimited. Does not implicitly {@link dataform.ExecuteResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.ExecuteResponse
         * @static
         * @param {dataform.IExecuteResponse} message ExecuteResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ExecuteResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes an ExecuteResponse message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.ExecuteResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.ExecuteResponse} ExecuteResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExecuteResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ExecuteResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.executionMetadata = $root.dataform.ExecutionMetadata.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        if (!(message.rows && message.rows.length))
                            message.rows = [];
                        message.rows.push($root.google.protobuf.Struct.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 3: {
                        if (!(message.errors && message.errors.length))
                            message.errors = [];
                        message.errors.push(reader.string());
                        break;
                    }
                case 4: {
                        if (!(message.schemaFields && message.schemaFields.length))
                            message.schemaFields = [];
                        message.schemaFields.push($root.dataform.Field.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes an ExecuteResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.ExecuteResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.ExecuteResponse} ExecuteResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ExecuteResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies an ExecuteResponse message.
         * @function verify
         * @memberof dataform.ExecuteResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ExecuteResponse.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.executionMetadata != null && Object.hasOwnProperty.call(message, "executionMetadata")) {
                let error = $root.dataform.ExecutionMetadata.verify(message.executionMetadata, long + 1);
                if (error)
                    return "executionMetadata." + error;
            }
            if (message.rows != null && Object.hasOwnProperty.call(message, "rows")) {
                if (!Array.isArray(message.rows))
                    return "rows: array expected";
                for (let i = 0; i < message.rows.length; ++i) {
                    let error = $root.google.protobuf.Struct.verify(message.rows[i], long + 1);
                    if (error)
                        return "rows." + error;
                }
            }
            if (message.errors != null && Object.hasOwnProperty.call(message, "errors")) {
                if (!Array.isArray(message.errors))
                    return "errors: array expected";
                for (let i = 0; i < message.errors.length; ++i)
                    if (!$util.isString(message.errors[i]))
                        return "errors: string[] expected";
            }
            if (message.schemaFields != null && Object.hasOwnProperty.call(message, "schemaFields")) {
                if (!Array.isArray(message.schemaFields))
                    return "schemaFields: array expected";
                for (let i = 0; i < message.schemaFields.length; ++i) {
                    let error = $root.dataform.Field.verify(message.schemaFields[i], long + 1);
                    if (error)
                        return "schemaFields." + error;
                }
            }
            return null;
        };

        /**
         * Creates an ExecuteResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.ExecuteResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.ExecuteResponse} ExecuteResponse
         */
        ExecuteResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.ExecuteResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.ExecuteResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.ExecuteResponse();
            if (object.executionMetadata != null) {
                if (!$util.isObject(object.executionMetadata))
                    throw TypeError(".dataform.ExecuteResponse.executionMetadata: object expected");
                message.executionMetadata = $root.dataform.ExecutionMetadata.fromObject(object.executionMetadata, long + 1);
            }
            if (object.rows) {
                if (!Array.isArray(object.rows))
                    throw TypeError(".dataform.ExecuteResponse.rows: array expected");
                message.rows = [];
                for (let i = 0; i < object.rows.length; ++i) {
                    if (!$util.isObject(object.rows[i]))
                        throw TypeError(".dataform.ExecuteResponse.rows: object expected");
                    message.rows[i] = $root.google.protobuf.Struct.fromObject(object.rows[i], long + 1);
                }
            }
            if (object.errors) {
                if (!Array.isArray(object.errors))
                    throw TypeError(".dataform.ExecuteResponse.errors: array expected");
                message.errors = [];
                for (let i = 0; i < object.errors.length; ++i)
                    message.errors[i] = String(object.errors[i]);
            }
            if (object.schemaFields) {
                if (!Array.isArray(object.schemaFields))
                    throw TypeError(".dataform.ExecuteResponse.schemaFields: array expected");
                message.schemaFields = [];
                for (let i = 0; i < object.schemaFields.length; ++i) {
                    if (!$util.isObject(object.schemaFields[i]))
                        throw TypeError(".dataform.ExecuteResponse.schemaFields: object expected");
                    message.schemaFields[i] = $root.dataform.Field.fromObject(object.schemaFields[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from an ExecuteResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.ExecuteResponse
         * @static
         * @param {dataform.ExecuteResponse} message ExecuteResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ExecuteResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.rows = [];
                object.errors = [];
                object.schemaFields = [];
            }
            if (options.defaults)
                object.executionMetadata = null;
            if (message.executionMetadata != null && Object.hasOwnProperty.call(message, "executionMetadata"))
                object.executionMetadata = $root.dataform.ExecutionMetadata.toObject(message.executionMetadata, options, q + 1);
            if (message.rows && message.rows.length) {
                object.rows = [];
                for (let j = 0; j < message.rows.length; ++j)
                    object.rows[j] = $root.google.protobuf.Struct.toObject(message.rows[j], options, q + 1);
            }
            if (message.errors && message.errors.length) {
                object.errors = [];
                for (let j = 0; j < message.errors.length; ++j)
                    object.errors[j] = message.errors[j];
            }
            if (message.schemaFields && message.schemaFields.length) {
                object.schemaFields = [];
                for (let j = 0; j < message.schemaFields.length; ++j)
                    object.schemaFields[j] = $root.dataform.Field.toObject(message.schemaFields[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this ExecuteResponse to JSON.
         * @function toJSON
         * @memberof dataform.ExecuteResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ExecuteResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ExecuteResponse
         * @function getTypeUrl
         * @memberof dataform.ExecuteResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ExecuteResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.ExecuteResponse";
        };

        return ExecuteResponse;
    })();

    dataform.ListTablesRequest = (function() {

        /**
         * Properties of a ListTablesRequest.
         * @memberof dataform
         * @interface IListTablesRequest
         * @property {string|null} [database] ListTablesRequest database
         * @property {string|null} [schema] ListTablesRequest schema
         */

        /**
         * Constructs a new ListTablesRequest.
         * @memberof dataform
         * @classdesc Represents a ListTablesRequest.
         * @implements IListTablesRequest
         * @constructor
         * @param {dataform.IListTablesRequest=} [properties] Properties to set
         */
        function ListTablesRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ListTablesRequest database.
         * @member {string} database
         * @memberof dataform.ListTablesRequest
         * @instance
         */
        ListTablesRequest.prototype.database = "";

        /**
         * ListTablesRequest schema.
         * @member {string} schema
         * @memberof dataform.ListTablesRequest
         * @instance
         */
        ListTablesRequest.prototype.schema = "";

        /**
         * Creates a new ListTablesRequest instance using the specified properties.
         * @function create
         * @memberof dataform.ListTablesRequest
         * @static
         * @param {dataform.IListTablesRequest=} [properties] Properties to set
         * @returns {dataform.ListTablesRequest} ListTablesRequest instance
         */
        ListTablesRequest.create = function create(properties) {
            return new ListTablesRequest(properties);
        };

        /**
         * Encodes the specified ListTablesRequest message. Does not implicitly {@link dataform.ListTablesRequest.verify|verify} messages.
         * @function encode
         * @memberof dataform.ListTablesRequest
         * @static
         * @param {dataform.IListTablesRequest} message ListTablesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListTablesRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.database != null && Object.hasOwnProperty.call(message, "database"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.database);
            if (message.schema != null && Object.hasOwnProperty.call(message, "schema"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.schema);
            return writer;
        };

        /**
         * Encodes the specified ListTablesRequest message, length delimited. Does not implicitly {@link dataform.ListTablesRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.ListTablesRequest
         * @static
         * @param {dataform.IListTablesRequest} message ListTablesRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListTablesRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListTablesRequest message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.ListTablesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.ListTablesRequest} ListTablesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListTablesRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ListTablesRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.database = reader.string();
                        break;
                    }
                case 2: {
                        message.schema = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ListTablesRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.ListTablesRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.ListTablesRequest} ListTablesRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListTablesRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListTablesRequest message.
         * @function verify
         * @memberof dataform.ListTablesRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListTablesRequest.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.database != null && Object.hasOwnProperty.call(message, "database"))
                if (!$util.isString(message.database))
                    return "database: string expected";
            if (message.schema != null && Object.hasOwnProperty.call(message, "schema"))
                if (!$util.isString(message.schema))
                    return "schema: string expected";
            return null;
        };

        /**
         * Creates a ListTablesRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.ListTablesRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.ListTablesRequest} ListTablesRequest
         */
        ListTablesRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.ListTablesRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.ListTablesRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.ListTablesRequest();
            if (object.database != null)
                message.database = String(object.database);
            if (object.schema != null)
                message.schema = String(object.schema);
            return message;
        };

        /**
         * Creates a plain object from a ListTablesRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.ListTablesRequest
         * @static
         * @param {dataform.ListTablesRequest} message ListTablesRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListTablesRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.database = "";
                object.schema = "";
            }
            if (message.database != null && Object.hasOwnProperty.call(message, "database"))
                object.database = message.database;
            if (message.schema != null && Object.hasOwnProperty.call(message, "schema"))
                object.schema = message.schema;
            return object;
        };

        /**
         * Converts this ListTablesRequest to JSON.
         * @function toJSON
         * @memberof dataform.ListTablesRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListTablesRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ListTablesRequest
         * @function getTypeUrl
         * @memberof dataform.ListTablesRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ListTablesRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.ListTablesRequest";
        };

        return ListTablesRequest;
    })();

    dataform.ListTablesResponse = (function() {

        /**
         * Properties of a ListTablesResponse.
         * @memberof dataform
         * @interface IListTablesResponse
         * @property {Array.<dataform.ITableMetadata>|null} [tables] ListTablesResponse tables
         */

        /**
         * Constructs a new ListTablesResponse.
         * @memberof dataform
         * @classdesc Represents a ListTablesResponse.
         * @implements IListTablesResponse
         * @constructor
         * @param {dataform.IListTablesResponse=} [properties] Properties to set
         */
        function ListTablesResponse(properties) {
            this.tables = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * ListTablesResponse tables.
         * @member {Array.<dataform.ITableMetadata>} tables
         * @memberof dataform.ListTablesResponse
         * @instance
         */
        ListTablesResponse.prototype.tables = $util.emptyArray;

        /**
         * Creates a new ListTablesResponse instance using the specified properties.
         * @function create
         * @memberof dataform.ListTablesResponse
         * @static
         * @param {dataform.IListTablesResponse=} [properties] Properties to set
         * @returns {dataform.ListTablesResponse} ListTablesResponse instance
         */
        ListTablesResponse.create = function create(properties) {
            return new ListTablesResponse(properties);
        };

        /**
         * Encodes the specified ListTablesResponse message. Does not implicitly {@link dataform.ListTablesResponse.verify|verify} messages.
         * @function encode
         * @memberof dataform.ListTablesResponse
         * @static
         * @param {dataform.IListTablesResponse} message ListTablesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListTablesResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.tables != null && message.tables.length)
                for (let i = 0; i < message.tables.length; ++i)
                    $root.dataform.TableMetadata.encode(message.tables[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified ListTablesResponse message, length delimited. Does not implicitly {@link dataform.ListTablesResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.ListTablesResponse
         * @static
         * @param {dataform.IListTablesResponse} message ListTablesResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        ListTablesResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a ListTablesResponse message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.ListTablesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.ListTablesResponse} ListTablesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListTablesResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.ListTablesResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.tables && message.tables.length))
                            message.tables = [];
                        message.tables.push($root.dataform.TableMetadata.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a ListTablesResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.ListTablesResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.ListTablesResponse} ListTablesResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        ListTablesResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a ListTablesResponse message.
         * @function verify
         * @memberof dataform.ListTablesResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        ListTablesResponse.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.tables != null && Object.hasOwnProperty.call(message, "tables")) {
                if (!Array.isArray(message.tables))
                    return "tables: array expected";
                for (let i = 0; i < message.tables.length; ++i) {
                    let error = $root.dataform.TableMetadata.verify(message.tables[i], long + 1);
                    if (error)
                        return "tables." + error;
                }
            }
            return null;
        };

        /**
         * Creates a ListTablesResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.ListTablesResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.ListTablesResponse} ListTablesResponse
         */
        ListTablesResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.ListTablesResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.ListTablesResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.ListTablesResponse();
            if (object.tables) {
                if (!Array.isArray(object.tables))
                    throw TypeError(".dataform.ListTablesResponse.tables: array expected");
                message.tables = [];
                for (let i = 0; i < object.tables.length; ++i) {
                    if (!$util.isObject(object.tables[i]))
                        throw TypeError(".dataform.ListTablesResponse.tables: object expected");
                    message.tables[i] = $root.dataform.TableMetadata.fromObject(object.tables[i], long + 1);
                }
            }
            return message;
        };

        /**
         * Creates a plain object from a ListTablesResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.ListTablesResponse
         * @static
         * @param {dataform.ListTablesResponse} message ListTablesResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        ListTablesResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.tables = [];
            if (message.tables && message.tables.length) {
                object.tables = [];
                for (let j = 0; j < message.tables.length; ++j)
                    object.tables[j] = $root.dataform.TableMetadata.toObject(message.tables[j], options, q + 1);
            }
            return object;
        };

        /**
         * Converts this ListTablesResponse to JSON.
         * @function toJSON
         * @memberof dataform.ListTablesResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        ListTablesResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for ListTablesResponse
         * @function getTypeUrl
         * @memberof dataform.ListTablesResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        ListTablesResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.ListTablesResponse";
        };

        return ListTablesResponse;
    })();

    dataform.GetTableRequest = (function() {

        /**
         * Properties of a GetTableRequest.
         * @memberof dataform
         * @interface IGetTableRequest
         * @property {dataform.ITarget|null} [target] GetTableRequest target
         */

        /**
         * Constructs a new GetTableRequest.
         * @memberof dataform
         * @classdesc Represents a GetTableRequest.
         * @implements IGetTableRequest
         * @constructor
         * @param {dataform.IGetTableRequest=} [properties] Properties to set
         */
        function GetTableRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * GetTableRequest target.
         * @member {dataform.ITarget|null|undefined} target
         * @memberof dataform.GetTableRequest
         * @instance
         */
        GetTableRequest.prototype.target = null;

        /**
         * Creates a new GetTableRequest instance using the specified properties.
         * @function create
         * @memberof dataform.GetTableRequest
         * @static
         * @param {dataform.IGetTableRequest=} [properties] Properties to set
         * @returns {dataform.GetTableRequest} GetTableRequest instance
         */
        GetTableRequest.create = function create(properties) {
            return new GetTableRequest(properties);
        };

        /**
         * Encodes the specified GetTableRequest message. Does not implicitly {@link dataform.GetTableRequest.verify|verify} messages.
         * @function encode
         * @memberof dataform.GetTableRequest
         * @static
         * @param {dataform.IGetTableRequest} message GetTableRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetTableRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                $root.dataform.Target.encode(message.target, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified GetTableRequest message, length delimited. Does not implicitly {@link dataform.GetTableRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.GetTableRequest
         * @static
         * @param {dataform.IGetTableRequest} message GetTableRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        GetTableRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a GetTableRequest message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.GetTableRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.GetTableRequest} GetTableRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetTableRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.GetTableRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.target = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a GetTableRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.GetTableRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.GetTableRequest} GetTableRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        GetTableRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a GetTableRequest message.
         * @function verify
         * @memberof dataform.GetTableRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        GetTableRequest.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.target != null && Object.hasOwnProperty.call(message, "target")) {
                let error = $root.dataform.Target.verify(message.target, long + 1);
                if (error)
                    return "target." + error;
            }
            return null;
        };

        /**
         * Creates a GetTableRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.GetTableRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.GetTableRequest} GetTableRequest
         */
        GetTableRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.GetTableRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.GetTableRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.GetTableRequest();
            if (object.target != null) {
                if (!$util.isObject(object.target))
                    throw TypeError(".dataform.GetTableRequest.target: object expected");
                message.target = $root.dataform.Target.fromObject(object.target, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a GetTableRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.GetTableRequest
         * @static
         * @param {dataform.GetTableRequest} message GetTableRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        GetTableRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.target = null;
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                object.target = $root.dataform.Target.toObject(message.target, options, q + 1);
            return object;
        };

        /**
         * Converts this GetTableRequest to JSON.
         * @function toJSON
         * @memberof dataform.GetTableRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        GetTableRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for GetTableRequest
         * @function getTypeUrl
         * @memberof dataform.GetTableRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        GetTableRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.GetTableRequest";
        };

        return GetTableRequest;
    })();

    dataform.DeleteTableRequest = (function() {

        /**
         * Properties of a DeleteTableRequest.
         * @memberof dataform
         * @interface IDeleteTableRequest
         * @property {dataform.ITarget|null} [target] DeleteTableRequest target
         */

        /**
         * Constructs a new DeleteTableRequest.
         * @memberof dataform
         * @classdesc Represents a DeleteTableRequest.
         * @implements IDeleteTableRequest
         * @constructor
         * @param {dataform.IDeleteTableRequest=} [properties] Properties to set
         */
        function DeleteTableRequest(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * DeleteTableRequest target.
         * @member {dataform.ITarget|null|undefined} target
         * @memberof dataform.DeleteTableRequest
         * @instance
         */
        DeleteTableRequest.prototype.target = null;

        /**
         * Creates a new DeleteTableRequest instance using the specified properties.
         * @function create
         * @memberof dataform.DeleteTableRequest
         * @static
         * @param {dataform.IDeleteTableRequest=} [properties] Properties to set
         * @returns {dataform.DeleteTableRequest} DeleteTableRequest instance
         */
        DeleteTableRequest.create = function create(properties) {
            return new DeleteTableRequest(properties);
        };

        /**
         * Encodes the specified DeleteTableRequest message. Does not implicitly {@link dataform.DeleteTableRequest.verify|verify} messages.
         * @function encode
         * @memberof dataform.DeleteTableRequest
         * @static
         * @param {dataform.IDeleteTableRequest} message DeleteTableRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteTableRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                $root.dataform.Target.encode(message.target, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified DeleteTableRequest message, length delimited. Does not implicitly {@link dataform.DeleteTableRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.DeleteTableRequest
         * @static
         * @param {dataform.IDeleteTableRequest} message DeleteTableRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        DeleteTableRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a DeleteTableRequest message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.DeleteTableRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.DeleteTableRequest} DeleteTableRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteTableRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.DeleteTableRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.target = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a DeleteTableRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.DeleteTableRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.DeleteTableRequest} DeleteTableRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        DeleteTableRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a DeleteTableRequest message.
         * @function verify
         * @memberof dataform.DeleteTableRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        DeleteTableRequest.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.target != null && Object.hasOwnProperty.call(message, "target")) {
                let error = $root.dataform.Target.verify(message.target, long + 1);
                if (error)
                    return "target." + error;
            }
            return null;
        };

        /**
         * Creates a DeleteTableRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.DeleteTableRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.DeleteTableRequest} DeleteTableRequest
         */
        DeleteTableRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.DeleteTableRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.DeleteTableRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.DeleteTableRequest();
            if (object.target != null) {
                if (!$util.isObject(object.target))
                    throw TypeError(".dataform.DeleteTableRequest.target: object expected");
                message.target = $root.dataform.Target.fromObject(object.target, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a DeleteTableRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.DeleteTableRequest
         * @static
         * @param {dataform.DeleteTableRequest} message DeleteTableRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        DeleteTableRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.target = null;
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                object.target = $root.dataform.Target.toObject(message.target, options, q + 1);
            return object;
        };

        /**
         * Converts this DeleteTableRequest to JSON.
         * @function toJSON
         * @memberof dataform.DeleteTableRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        DeleteTableRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for DeleteTableRequest
         * @function getTypeUrl
         * @memberof dataform.DeleteTableRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        DeleteTableRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.DeleteTableRequest";
        };

        return DeleteTableRequest;
    })();

    dataform.RunningExecutionData = (function() {

        /**
         * Properties of a RunningExecutionData.
         * @memberof dataform
         * @interface IRunningExecutionData
         * @property {Array.<dataform.ITarget>|null} [runTargets] RunningExecutionData runTargets
         * @property {string|null} [executionId] RunningExecutionData executionId
         * @property {google.protobuf.ITimestamp|null} [executionStartTime] RunningExecutionData executionStartTime
         */

        /**
         * Constructs a new RunningExecutionData.
         * @memberof dataform
         * @classdesc Represents a RunningExecutionData.
         * @implements IRunningExecutionData
         * @constructor
         * @param {dataform.IRunningExecutionData=} [properties] Properties to set
         */
        function RunningExecutionData(properties) {
            this.runTargets = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * RunningExecutionData runTargets.
         * @member {Array.<dataform.ITarget>} runTargets
         * @memberof dataform.RunningExecutionData
         * @instance
         */
        RunningExecutionData.prototype.runTargets = $util.emptyArray;

        /**
         * RunningExecutionData executionId.
         * @member {string} executionId
         * @memberof dataform.RunningExecutionData
         * @instance
         */
        RunningExecutionData.prototype.executionId = "";

        /**
         * RunningExecutionData executionStartTime.
         * @member {google.protobuf.ITimestamp|null|undefined} executionStartTime
         * @memberof dataform.RunningExecutionData
         * @instance
         */
        RunningExecutionData.prototype.executionStartTime = null;

        /**
         * Creates a new RunningExecutionData instance using the specified properties.
         * @function create
         * @memberof dataform.RunningExecutionData
         * @static
         * @param {dataform.IRunningExecutionData=} [properties] Properties to set
         * @returns {dataform.RunningExecutionData} RunningExecutionData instance
         */
        RunningExecutionData.create = function create(properties) {
            return new RunningExecutionData(properties);
        };

        /**
         * Encodes the specified RunningExecutionData message. Does not implicitly {@link dataform.RunningExecutionData.verify|verify} messages.
         * @function encode
         * @memberof dataform.RunningExecutionData
         * @static
         * @param {dataform.IRunningExecutionData} message RunningExecutionData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RunningExecutionData.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.runTargets != null && message.runTargets.length)
                for (let i = 0; i < message.runTargets.length; ++i)
                    $root.dataform.Target.encode(message.runTargets[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.executionId != null && Object.hasOwnProperty.call(message, "executionId"))
                writer.uint32(/* id 2, wireType 2 =*/18).string(message.executionId);
            if (message.executionStartTime != null && Object.hasOwnProperty.call(message, "executionStartTime"))
                $root.google.protobuf.Timestamp.encode(message.executionStartTime, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified RunningExecutionData message, length delimited. Does not implicitly {@link dataform.RunningExecutionData.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.RunningExecutionData
         * @static
         * @param {dataform.IRunningExecutionData} message RunningExecutionData message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        RunningExecutionData.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a RunningExecutionData message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.RunningExecutionData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.RunningExecutionData} RunningExecutionData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RunningExecutionData.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.RunningExecutionData();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.runTargets && message.runTargets.length))
                            message.runTargets = [];
                        message.runTargets.push($root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 2: {
                        message.executionId = reader.string();
                        break;
                    }
                case 3: {
                        message.executionStartTime = $root.google.protobuf.Timestamp.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a RunningExecutionData message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.RunningExecutionData
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.RunningExecutionData} RunningExecutionData
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        RunningExecutionData.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a RunningExecutionData message.
         * @function verify
         * @memberof dataform.RunningExecutionData
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        RunningExecutionData.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.runTargets != null && Object.hasOwnProperty.call(message, "runTargets")) {
                if (!Array.isArray(message.runTargets))
                    return "runTargets: array expected";
                for (let i = 0; i < message.runTargets.length; ++i) {
                    let error = $root.dataform.Target.verify(message.runTargets[i], long + 1);
                    if (error)
                        return "runTargets." + error;
                }
            }
            if (message.executionId != null && Object.hasOwnProperty.call(message, "executionId"))
                if (!$util.isString(message.executionId))
                    return "executionId: string expected";
            if (message.executionStartTime != null && Object.hasOwnProperty.call(message, "executionStartTime")) {
                let error = $root.google.protobuf.Timestamp.verify(message.executionStartTime, long + 1);
                if (error)
                    return "executionStartTime." + error;
            }
            return null;
        };

        /**
         * Creates a RunningExecutionData message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.RunningExecutionData
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.RunningExecutionData} RunningExecutionData
         */
        RunningExecutionData.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.RunningExecutionData)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.RunningExecutionData: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.RunningExecutionData();
            if (object.runTargets) {
                if (!Array.isArray(object.runTargets))
                    throw TypeError(".dataform.RunningExecutionData.runTargets: array expected");
                message.runTargets = [];
                for (let i = 0; i < object.runTargets.length; ++i) {
                    if (!$util.isObject(object.runTargets[i]))
                        throw TypeError(".dataform.RunningExecutionData.runTargets: object expected");
                    message.runTargets[i] = $root.dataform.Target.fromObject(object.runTargets[i], long + 1);
                }
            }
            if (object.executionId != null)
                message.executionId = String(object.executionId);
            if (object.executionStartTime != null) {
                if (!$util.isObject(object.executionStartTime))
                    throw TypeError(".dataform.RunningExecutionData.executionStartTime: object expected");
                message.executionStartTime = $root.google.protobuf.Timestamp.fromObject(object.executionStartTime, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a RunningExecutionData message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.RunningExecutionData
         * @static
         * @param {dataform.RunningExecutionData} message RunningExecutionData
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        RunningExecutionData.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.runTargets = [];
            if (options.defaults) {
                object.executionId = "";
                object.executionStartTime = null;
            }
            if (message.runTargets && message.runTargets.length) {
                object.runTargets = [];
                for (let j = 0; j < message.runTargets.length; ++j)
                    object.runTargets[j] = $root.dataform.Target.toObject(message.runTargets[j], options, q + 1);
            }
            if (message.executionId != null && Object.hasOwnProperty.call(message, "executionId"))
                object.executionId = message.executionId;
            if (message.executionStartTime != null && Object.hasOwnProperty.call(message, "executionStartTime"))
                object.executionStartTime = $root.google.protobuf.Timestamp.toObject(message.executionStartTime, options, q + 1);
            return object;
        };

        /**
         * Converts this RunningExecutionData to JSON.
         * @function toJSON
         * @memberof dataform.RunningExecutionData
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        RunningExecutionData.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for RunningExecutionData
         * @function getTypeUrl
         * @memberof dataform.RunningExecutionData
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        RunningExecutionData.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.RunningExecutionData";
        };

        return RunningExecutionData;
    })();

    /**
     * JitCompilationTargetType enum.
     * @name dataform.JitCompilationTargetType
     * @enum {number}
     * @property {number} JIT_COMPILATION_TARGET_TYPE_UNSPECIFIED=0 JIT_COMPILATION_TARGET_TYPE_UNSPECIFIED value
     * @property {number} JIT_COMPILATION_TARGET_TYPE_TABLE=1 JIT_COMPILATION_TARGET_TYPE_TABLE value
     * @property {number} JIT_COMPILATION_TARGET_TYPE_OPERATION=2 JIT_COMPILATION_TARGET_TYPE_OPERATION value
     * @property {number} JIT_COMPILATION_TARGET_TYPE_INCREMENTAL_TABLE=3 JIT_COMPILATION_TARGET_TYPE_INCREMENTAL_TABLE value
     * @property {number} JIT_COMPILATION_TARGET_TYPE_ASSERTION=4 JIT_COMPILATION_TARGET_TYPE_ASSERTION value
     */
    dataform.JitCompilationTargetType = (function() {
        const valuesById = {}, values = Object.create(valuesById);
        values[valuesById[0] = "JIT_COMPILATION_TARGET_TYPE_UNSPECIFIED"] = 0;
        values[valuesById[1] = "JIT_COMPILATION_TARGET_TYPE_TABLE"] = 1;
        values[valuesById[2] = "JIT_COMPILATION_TARGET_TYPE_OPERATION"] = 2;
        values[valuesById[3] = "JIT_COMPILATION_TARGET_TYPE_INCREMENTAL_TABLE"] = 3;
        values[valuesById[4] = "JIT_COMPILATION_TARGET_TYPE_ASSERTION"] = 4;
        return values;
    })();

    dataform.JitCompilationRequest = (function() {

        /**
         * Properties of a JitCompilationRequest.
         * @memberof dataform
         * @interface IJitCompilationRequest
         * @property {dataform.ITarget|null} [target] JitCompilationRequest target
         * @property {Array.<dataform.ITarget>|null} [dependencies] JitCompilationRequest dependencies
         * @property {string|null} [jitCode] JitCompilationRequest jitCode
         * @property {google.protobuf.IStruct|null} [jitData] JitCompilationRequest jitData
         * @property {dataform.JitCompilationTargetType|null} [compilationTargetType] JitCompilationRequest compilationTargetType
         * @property {Array.<string>|null} [filePaths] JitCompilationRequest filePaths
         * @property {dataform.IRunningExecutionData|null} [executionData] JitCompilationRequest executionData
         */

        /**
         * Constructs a new JitCompilationRequest.
         * @memberof dataform
         * @classdesc Represents a JitCompilationRequest.
         * @implements IJitCompilationRequest
         * @constructor
         * @param {dataform.IJitCompilationRequest=} [properties] Properties to set
         */
        function JitCompilationRequest(properties) {
            this.dependencies = [];
            this.filePaths = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JitCompilationRequest target.
         * @member {dataform.ITarget|null|undefined} target
         * @memberof dataform.JitCompilationRequest
         * @instance
         */
        JitCompilationRequest.prototype.target = null;

        /**
         * JitCompilationRequest dependencies.
         * @member {Array.<dataform.ITarget>} dependencies
         * @memberof dataform.JitCompilationRequest
         * @instance
         */
        JitCompilationRequest.prototype.dependencies = $util.emptyArray;

        /**
         * JitCompilationRequest jitCode.
         * @member {string} jitCode
         * @memberof dataform.JitCompilationRequest
         * @instance
         */
        JitCompilationRequest.prototype.jitCode = "";

        /**
         * JitCompilationRequest jitData.
         * @member {google.protobuf.IStruct|null|undefined} jitData
         * @memberof dataform.JitCompilationRequest
         * @instance
         */
        JitCompilationRequest.prototype.jitData = null;

        /**
         * JitCompilationRequest compilationTargetType.
         * @member {dataform.JitCompilationTargetType} compilationTargetType
         * @memberof dataform.JitCompilationRequest
         * @instance
         */
        JitCompilationRequest.prototype.compilationTargetType = 0;

        /**
         * JitCompilationRequest filePaths.
         * @member {Array.<string>} filePaths
         * @memberof dataform.JitCompilationRequest
         * @instance
         */
        JitCompilationRequest.prototype.filePaths = $util.emptyArray;

        /**
         * JitCompilationRequest executionData.
         * @member {dataform.IRunningExecutionData|null|undefined} executionData
         * @memberof dataform.JitCompilationRequest
         * @instance
         */
        JitCompilationRequest.prototype.executionData = null;

        /**
         * Creates a new JitCompilationRequest instance using the specified properties.
         * @function create
         * @memberof dataform.JitCompilationRequest
         * @static
         * @param {dataform.IJitCompilationRequest=} [properties] Properties to set
         * @returns {dataform.JitCompilationRequest} JitCompilationRequest instance
         */
        JitCompilationRequest.create = function create(properties) {
            return new JitCompilationRequest(properties);
        };

        /**
         * Encodes the specified JitCompilationRequest message. Does not implicitly {@link dataform.JitCompilationRequest.verify|verify} messages.
         * @function encode
         * @memberof dataform.JitCompilationRequest
         * @static
         * @param {dataform.IJitCompilationRequest} message JitCompilationRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JitCompilationRequest.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                $root.dataform.Target.encode(message.target, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.dependencies != null && message.dependencies.length)
                for (let i = 0; i < message.dependencies.length; ++i)
                    $root.dataform.Target.encode(message.dependencies[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.jitCode);
            if (message.jitData != null && Object.hasOwnProperty.call(message, "jitData"))
                $root.google.protobuf.Struct.encode(message.jitData, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            if (message.compilationTargetType != null && Object.hasOwnProperty.call(message, "compilationTargetType"))
                writer.uint32(/* id 5, wireType 0 =*/40).int32(message.compilationTargetType);
            if (message.filePaths != null && message.filePaths.length)
                for (let i = 0; i < message.filePaths.length; ++i)
                    writer.uint32(/* id 6, wireType 2 =*/50).string(message.filePaths[i]);
            if (message.executionData != null && Object.hasOwnProperty.call(message, "executionData"))
                $root.dataform.RunningExecutionData.encode(message.executionData, writer.uint32(/* id 7, wireType 2 =*/58).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified JitCompilationRequest message, length delimited. Does not implicitly {@link dataform.JitCompilationRequest.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.JitCompilationRequest
         * @static
         * @param {dataform.IJitCompilationRequest} message JitCompilationRequest message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JitCompilationRequest.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a JitCompilationRequest message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.JitCompilationRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.JitCompilationRequest} JitCompilationRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JitCompilationRequest.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.JitCompilationRequest();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.target = $root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        if (!(message.dependencies && message.dependencies.length))
                            message.dependencies = [];
                        message.dependencies.push($root.dataform.Target.decode(reader, reader.uint32(), undefined, long + 1));
                        break;
                    }
                case 3: {
                        message.jitCode = reader.string();
                        break;
                    }
                case 4: {
                        message.jitData = $root.google.protobuf.Struct.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 5: {
                        message.compilationTargetType = reader.int32();
                        break;
                    }
                case 6: {
                        if (!(message.filePaths && message.filePaths.length))
                            message.filePaths = [];
                        message.filePaths.push(reader.string());
                        break;
                    }
                case 7: {
                        message.executionData = $root.dataform.RunningExecutionData.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JitCompilationRequest message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.JitCompilationRequest
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.JitCompilationRequest} JitCompilationRequest
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JitCompilationRequest.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JitCompilationRequest message.
         * @function verify
         * @memberof dataform.JitCompilationRequest
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JitCompilationRequest.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.target != null && Object.hasOwnProperty.call(message, "target")) {
                let error = $root.dataform.Target.verify(message.target, long + 1);
                if (error)
                    return "target." + error;
            }
            if (message.dependencies != null && Object.hasOwnProperty.call(message, "dependencies")) {
                if (!Array.isArray(message.dependencies))
                    return "dependencies: array expected";
                for (let i = 0; i < message.dependencies.length; ++i) {
                    let error = $root.dataform.Target.verify(message.dependencies[i], long + 1);
                    if (error)
                        return "dependencies." + error;
                }
            }
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                if (!$util.isString(message.jitCode))
                    return "jitCode: string expected";
            if (message.jitData != null && Object.hasOwnProperty.call(message, "jitData")) {
                let error = $root.google.protobuf.Struct.verify(message.jitData, long + 1);
                if (error)
                    return "jitData." + error;
            }
            if (message.compilationTargetType != null && Object.hasOwnProperty.call(message, "compilationTargetType"))
                switch (message.compilationTargetType) {
                default:
                    return "compilationTargetType: enum value expected";
                case 0:
                case 1:
                case 2:
                case 3:
                case 4:
                    break;
                }
            if (message.filePaths != null && Object.hasOwnProperty.call(message, "filePaths")) {
                if (!Array.isArray(message.filePaths))
                    return "filePaths: array expected";
                for (let i = 0; i < message.filePaths.length; ++i)
                    if (!$util.isString(message.filePaths[i]))
                        return "filePaths: string[] expected";
            }
            if (message.executionData != null && Object.hasOwnProperty.call(message, "executionData")) {
                let error = $root.dataform.RunningExecutionData.verify(message.executionData, long + 1);
                if (error)
                    return "executionData." + error;
            }
            return null;
        };

        /**
         * Creates a JitCompilationRequest message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.JitCompilationRequest
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.JitCompilationRequest} JitCompilationRequest
         */
        JitCompilationRequest.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.JitCompilationRequest)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.JitCompilationRequest: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.JitCompilationRequest();
            if (object.target != null) {
                if (!$util.isObject(object.target))
                    throw TypeError(".dataform.JitCompilationRequest.target: object expected");
                message.target = $root.dataform.Target.fromObject(object.target, long + 1);
            }
            if (object.dependencies) {
                if (!Array.isArray(object.dependencies))
                    throw TypeError(".dataform.JitCompilationRequest.dependencies: array expected");
                message.dependencies = [];
                for (let i = 0; i < object.dependencies.length; ++i) {
                    if (!$util.isObject(object.dependencies[i]))
                        throw TypeError(".dataform.JitCompilationRequest.dependencies: object expected");
                    message.dependencies[i] = $root.dataform.Target.fromObject(object.dependencies[i], long + 1);
                }
            }
            if (object.jitCode != null)
                message.jitCode = String(object.jitCode);
            if (object.jitData != null) {
                if (!$util.isObject(object.jitData))
                    throw TypeError(".dataform.JitCompilationRequest.jitData: object expected");
                message.jitData = $root.google.protobuf.Struct.fromObject(object.jitData, long + 1);
            }
            switch (object.compilationTargetType) {
            default:
                if (typeof object.compilationTargetType === "number") {
                    message.compilationTargetType = object.compilationTargetType;
                    break;
                }
                break;
            case "JIT_COMPILATION_TARGET_TYPE_UNSPECIFIED":
            case 0:
                message.compilationTargetType = 0;
                break;
            case "JIT_COMPILATION_TARGET_TYPE_TABLE":
            case 1:
                message.compilationTargetType = 1;
                break;
            case "JIT_COMPILATION_TARGET_TYPE_OPERATION":
            case 2:
                message.compilationTargetType = 2;
                break;
            case "JIT_COMPILATION_TARGET_TYPE_INCREMENTAL_TABLE":
            case 3:
                message.compilationTargetType = 3;
                break;
            case "JIT_COMPILATION_TARGET_TYPE_ASSERTION":
            case 4:
                message.compilationTargetType = 4;
                break;
            }
            if (object.filePaths) {
                if (!Array.isArray(object.filePaths))
                    throw TypeError(".dataform.JitCompilationRequest.filePaths: array expected");
                message.filePaths = [];
                for (let i = 0; i < object.filePaths.length; ++i)
                    message.filePaths[i] = String(object.filePaths[i]);
            }
            if (object.executionData != null) {
                if (!$util.isObject(object.executionData))
                    throw TypeError(".dataform.JitCompilationRequest.executionData: object expected");
                message.executionData = $root.dataform.RunningExecutionData.fromObject(object.executionData, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a JitCompilationRequest message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.JitCompilationRequest
         * @static
         * @param {dataform.JitCompilationRequest} message JitCompilationRequest
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JitCompilationRequest.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.dependencies = [];
                object.filePaths = [];
            }
            if (options.defaults) {
                object.target = null;
                object.jitCode = "";
                object.jitData = null;
                object.compilationTargetType = options.enums === String ? "JIT_COMPILATION_TARGET_TYPE_UNSPECIFIED" : 0;
                object.executionData = null;
            }
            if (message.target != null && Object.hasOwnProperty.call(message, "target"))
                object.target = $root.dataform.Target.toObject(message.target, options, q + 1);
            if (message.dependencies && message.dependencies.length) {
                object.dependencies = [];
                for (let j = 0; j < message.dependencies.length; ++j)
                    object.dependencies[j] = $root.dataform.Target.toObject(message.dependencies[j], options, q + 1);
            }
            if (message.jitCode != null && Object.hasOwnProperty.call(message, "jitCode"))
                object.jitCode = message.jitCode;
            if (message.jitData != null && Object.hasOwnProperty.call(message, "jitData"))
                object.jitData = $root.google.protobuf.Struct.toObject(message.jitData, options, q + 1);
            if (message.compilationTargetType != null && Object.hasOwnProperty.call(message, "compilationTargetType"))
                object.compilationTargetType = options.enums === String ? $root.dataform.JitCompilationTargetType[message.compilationTargetType] === undefined ? message.compilationTargetType : $root.dataform.JitCompilationTargetType[message.compilationTargetType] : message.compilationTargetType;
            if (message.filePaths && message.filePaths.length) {
                object.filePaths = [];
                for (let j = 0; j < message.filePaths.length; ++j)
                    object.filePaths[j] = message.filePaths[j];
            }
            if (message.executionData != null && Object.hasOwnProperty.call(message, "executionData"))
                object.executionData = $root.dataform.RunningExecutionData.toObject(message.executionData, options, q + 1);
            return object;
        };

        /**
         * Converts this JitCompilationRequest to JSON.
         * @function toJSON
         * @memberof dataform.JitCompilationRequest
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JitCompilationRequest.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JitCompilationRequest
         * @function getTypeUrl
         * @memberof dataform.JitCompilationRequest
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JitCompilationRequest.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.JitCompilationRequest";
        };

        return JitCompilationRequest;
    })();

    dataform.JitCompilationResponse = (function() {

        /**
         * Properties of a JitCompilationResponse.
         * @memberof dataform
         * @interface IJitCompilationResponse
         * @property {dataform.IJitTableResult|null} [table] JitCompilationResponse table
         * @property {dataform.IJitOperationResult|null} [operation] JitCompilationResponse operation
         * @property {dataform.IJitIncrementalTableResult|null} [incrementalTable] JitCompilationResponse incrementalTable
         * @property {dataform.IJitAssertionResult|null} [assertion] JitCompilationResponse assertion
         */

        /**
         * Constructs a new JitCompilationResponse.
         * @memberof dataform
         * @classdesc Represents a JitCompilationResponse.
         * @implements IJitCompilationResponse
         * @constructor
         * @param {dataform.IJitCompilationResponse=} [properties] Properties to set
         */
        function JitCompilationResponse(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JitCompilationResponse table.
         * @member {dataform.IJitTableResult|null|undefined} table
         * @memberof dataform.JitCompilationResponse
         * @instance
         */
        JitCompilationResponse.prototype.table = null;

        /**
         * JitCompilationResponse operation.
         * @member {dataform.IJitOperationResult|null|undefined} operation
         * @memberof dataform.JitCompilationResponse
         * @instance
         */
        JitCompilationResponse.prototype.operation = null;

        /**
         * JitCompilationResponse incrementalTable.
         * @member {dataform.IJitIncrementalTableResult|null|undefined} incrementalTable
         * @memberof dataform.JitCompilationResponse
         * @instance
         */
        JitCompilationResponse.prototype.incrementalTable = null;

        /**
         * JitCompilationResponse assertion.
         * @member {dataform.IJitAssertionResult|null|undefined} assertion
         * @memberof dataform.JitCompilationResponse
         * @instance
         */
        JitCompilationResponse.prototype.assertion = null;

        // OneOf field names bound to virtual getters and setters
        let $oneOfFields;

        /**
         * JitCompilationResponse response.
         * @member {"table"|"operation"|"incrementalTable"|"assertion"|undefined} response
         * @memberof dataform.JitCompilationResponse
         * @instance
         */
        Object.defineProperty(JitCompilationResponse.prototype, "response", {
            get: $util.oneOfGetter($oneOfFields = ["table", "operation", "incrementalTable", "assertion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        /**
         * Creates a new JitCompilationResponse instance using the specified properties.
         * @function create
         * @memberof dataform.JitCompilationResponse
         * @static
         * @param {dataform.IJitCompilationResponse=} [properties] Properties to set
         * @returns {dataform.JitCompilationResponse} JitCompilationResponse instance
         */
        JitCompilationResponse.create = function create(properties) {
            return new JitCompilationResponse(properties);
        };

        /**
         * Encodes the specified JitCompilationResponse message. Does not implicitly {@link dataform.JitCompilationResponse.verify|verify} messages.
         * @function encode
         * @memberof dataform.JitCompilationResponse
         * @static
         * @param {dataform.IJitCompilationResponse} message JitCompilationResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JitCompilationResponse.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.table != null && Object.hasOwnProperty.call(message, "table"))
                $root.dataform.JitTableResult.encode(message.table, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation"))
                $root.dataform.JitOperationResult.encode(message.operation, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            if (message.incrementalTable != null && Object.hasOwnProperty.call(message, "incrementalTable"))
                $root.dataform.JitIncrementalTableResult.encode(message.incrementalTable, writer.uint32(/* id 3, wireType 2 =*/26).fork(), q + 1).ldelim();
            if (message.assertion != null && Object.hasOwnProperty.call(message, "assertion"))
                $root.dataform.JitAssertionResult.encode(message.assertion, writer.uint32(/* id 4, wireType 2 =*/34).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified JitCompilationResponse message, length delimited. Does not implicitly {@link dataform.JitCompilationResponse.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.JitCompilationResponse
         * @static
         * @param {dataform.IJitCompilationResponse} message JitCompilationResponse message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JitCompilationResponse.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a JitCompilationResponse message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.JitCompilationResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.JitCompilationResponse} JitCompilationResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JitCompilationResponse.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.JitCompilationResponse();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.table = $root.dataform.JitTableResult.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.operation = $root.dataform.JitOperationResult.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 3: {
                        message.incrementalTable = $root.dataform.JitIncrementalTableResult.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 4: {
                        message.assertion = $root.dataform.JitAssertionResult.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JitCompilationResponse message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.JitCompilationResponse
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.JitCompilationResponse} JitCompilationResponse
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JitCompilationResponse.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JitCompilationResponse message.
         * @function verify
         * @memberof dataform.JitCompilationResponse
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JitCompilationResponse.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            let properties = {};
            if (message.table != null && Object.hasOwnProperty.call(message, "table")) {
                properties.response = 1;
                {
                    let error = $root.dataform.JitTableResult.verify(message.table, long + 1);
                    if (error)
                        return "table." + error;
                }
            }
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.dataform.JitOperationResult.verify(message.operation, long + 1);
                    if (error)
                        return "operation." + error;
                }
            }
            if (message.incrementalTable != null && Object.hasOwnProperty.call(message, "incrementalTable")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.dataform.JitIncrementalTableResult.verify(message.incrementalTable, long + 1);
                    if (error)
                        return "incrementalTable." + error;
                }
            }
            if (message.assertion != null && Object.hasOwnProperty.call(message, "assertion")) {
                if (properties.response === 1)
                    return "response: multiple values";
                properties.response = 1;
                {
                    let error = $root.dataform.JitAssertionResult.verify(message.assertion, long + 1);
                    if (error)
                        return "assertion." + error;
                }
            }
            return null;
        };

        /**
         * Creates a JitCompilationResponse message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.JitCompilationResponse
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.JitCompilationResponse} JitCompilationResponse
         */
        JitCompilationResponse.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.JitCompilationResponse)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.JitCompilationResponse: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.JitCompilationResponse();
            if (object.table != null) {
                if (!$util.isObject(object.table))
                    throw TypeError(".dataform.JitCompilationResponse.table: object expected");
                message.table = $root.dataform.JitTableResult.fromObject(object.table, long + 1);
            }
            if (object.operation != null) {
                if (!$util.isObject(object.operation))
                    throw TypeError(".dataform.JitCompilationResponse.operation: object expected");
                message.operation = $root.dataform.JitOperationResult.fromObject(object.operation, long + 1);
            }
            if (object.incrementalTable != null) {
                if (!$util.isObject(object.incrementalTable))
                    throw TypeError(".dataform.JitCompilationResponse.incrementalTable: object expected");
                message.incrementalTable = $root.dataform.JitIncrementalTableResult.fromObject(object.incrementalTable, long + 1);
            }
            if (object.assertion != null) {
                if (!$util.isObject(object.assertion))
                    throw TypeError(".dataform.JitCompilationResponse.assertion: object expected");
                message.assertion = $root.dataform.JitAssertionResult.fromObject(object.assertion, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a JitCompilationResponse message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.JitCompilationResponse
         * @static
         * @param {dataform.JitCompilationResponse} message JitCompilationResponse
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JitCompilationResponse.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (message.table != null && Object.hasOwnProperty.call(message, "table")) {
                object.table = $root.dataform.JitTableResult.toObject(message.table, options, q + 1);
                if (options.oneofs)
                    object.response = "table";
            }
            if (message.operation != null && Object.hasOwnProperty.call(message, "operation")) {
                object.operation = $root.dataform.JitOperationResult.toObject(message.operation, options, q + 1);
                if (options.oneofs)
                    object.response = "operation";
            }
            if (message.incrementalTable != null && Object.hasOwnProperty.call(message, "incrementalTable")) {
                object.incrementalTable = $root.dataform.JitIncrementalTableResult.toObject(message.incrementalTable, options, q + 1);
                if (options.oneofs)
                    object.response = "incrementalTable";
            }
            if (message.assertion != null && Object.hasOwnProperty.call(message, "assertion")) {
                object.assertion = $root.dataform.JitAssertionResult.toObject(message.assertion, options, q + 1);
                if (options.oneofs)
                    object.response = "assertion";
            }
            return object;
        };

        /**
         * Converts this JitCompilationResponse to JSON.
         * @function toJSON
         * @memberof dataform.JitCompilationResponse
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JitCompilationResponse.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JitCompilationResponse
         * @function getTypeUrl
         * @memberof dataform.JitCompilationResponse
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JitCompilationResponse.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.JitCompilationResponse";
        };

        return JitCompilationResponse;
    })();

    dataform.JitTableResult = (function() {

        /**
         * Properties of a JitTableResult.
         * @memberof dataform
         * @interface IJitTableResult
         * @property {string|null} [query] JitTableResult query
         * @property {Array.<string>|null} [preOps] JitTableResult preOps
         * @property {Array.<string>|null} [postOps] JitTableResult postOps
         */

        /**
         * Constructs a new JitTableResult.
         * @memberof dataform
         * @classdesc Represents a JitTableResult.
         * @implements IJitTableResult
         * @constructor
         * @param {dataform.IJitTableResult=} [properties] Properties to set
         */
        function JitTableResult(properties) {
            this.preOps = [];
            this.postOps = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JitTableResult query.
         * @member {string} query
         * @memberof dataform.JitTableResult
         * @instance
         */
        JitTableResult.prototype.query = "";

        /**
         * JitTableResult preOps.
         * @member {Array.<string>} preOps
         * @memberof dataform.JitTableResult
         * @instance
         */
        JitTableResult.prototype.preOps = $util.emptyArray;

        /**
         * JitTableResult postOps.
         * @member {Array.<string>} postOps
         * @memberof dataform.JitTableResult
         * @instance
         */
        JitTableResult.prototype.postOps = $util.emptyArray;

        /**
         * Creates a new JitTableResult instance using the specified properties.
         * @function create
         * @memberof dataform.JitTableResult
         * @static
         * @param {dataform.IJitTableResult=} [properties] Properties to set
         * @returns {dataform.JitTableResult} JitTableResult instance
         */
        JitTableResult.create = function create(properties) {
            return new JitTableResult(properties);
        };

        /**
         * Encodes the specified JitTableResult message. Does not implicitly {@link dataform.JitTableResult.verify|verify} messages.
         * @function encode
         * @memberof dataform.JitTableResult
         * @static
         * @param {dataform.IJitTableResult} message JitTableResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JitTableResult.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.query != null && Object.hasOwnProperty.call(message, "query"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.query);
            if (message.preOps != null && message.preOps.length)
                for (let i = 0; i < message.preOps.length; ++i)
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.preOps[i]);
            if (message.postOps != null && message.postOps.length)
                for (let i = 0; i < message.postOps.length; ++i)
                    writer.uint32(/* id 4, wireType 2 =*/34).string(message.postOps[i]);
            return writer;
        };

        /**
         * Encodes the specified JitTableResult message, length delimited. Does not implicitly {@link dataform.JitTableResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.JitTableResult
         * @static
         * @param {dataform.IJitTableResult} message JitTableResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JitTableResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a JitTableResult message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.JitTableResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.JitTableResult} JitTableResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JitTableResult.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.JitTableResult();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.query = reader.string();
                        break;
                    }
                case 3: {
                        if (!(message.preOps && message.preOps.length))
                            message.preOps = [];
                        message.preOps.push(reader.string());
                        break;
                    }
                case 4: {
                        if (!(message.postOps && message.postOps.length))
                            message.postOps = [];
                        message.postOps.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JitTableResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.JitTableResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.JitTableResult} JitTableResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JitTableResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JitTableResult message.
         * @function verify
         * @memberof dataform.JitTableResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JitTableResult.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.query != null && Object.hasOwnProperty.call(message, "query"))
                if (!$util.isString(message.query))
                    return "query: string expected";
            if (message.preOps != null && Object.hasOwnProperty.call(message, "preOps")) {
                if (!Array.isArray(message.preOps))
                    return "preOps: array expected";
                for (let i = 0; i < message.preOps.length; ++i)
                    if (!$util.isString(message.preOps[i]))
                        return "preOps: string[] expected";
            }
            if (message.postOps != null && Object.hasOwnProperty.call(message, "postOps")) {
                if (!Array.isArray(message.postOps))
                    return "postOps: array expected";
                for (let i = 0; i < message.postOps.length; ++i)
                    if (!$util.isString(message.postOps[i]))
                        return "postOps: string[] expected";
            }
            return null;
        };

        /**
         * Creates a JitTableResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.JitTableResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.JitTableResult} JitTableResult
         */
        JitTableResult.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.JitTableResult)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.JitTableResult: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.JitTableResult();
            if (object.query != null)
                message.query = String(object.query);
            if (object.preOps) {
                if (!Array.isArray(object.preOps))
                    throw TypeError(".dataform.JitTableResult.preOps: array expected");
                message.preOps = [];
                for (let i = 0; i < object.preOps.length; ++i)
                    message.preOps[i] = String(object.preOps[i]);
            }
            if (object.postOps) {
                if (!Array.isArray(object.postOps))
                    throw TypeError(".dataform.JitTableResult.postOps: array expected");
                message.postOps = [];
                for (let i = 0; i < object.postOps.length; ++i)
                    message.postOps[i] = String(object.postOps[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a JitTableResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.JitTableResult
         * @static
         * @param {dataform.JitTableResult} message JitTableResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JitTableResult.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults) {
                object.preOps = [];
                object.postOps = [];
            }
            if (options.defaults)
                object.query = "";
            if (message.query != null && Object.hasOwnProperty.call(message, "query"))
                object.query = message.query;
            if (message.preOps && message.preOps.length) {
                object.preOps = [];
                for (let j = 0; j < message.preOps.length; ++j)
                    object.preOps[j] = message.preOps[j];
            }
            if (message.postOps && message.postOps.length) {
                object.postOps = [];
                for (let j = 0; j < message.postOps.length; ++j)
                    object.postOps[j] = message.postOps[j];
            }
            return object;
        };

        /**
         * Converts this JitTableResult to JSON.
         * @function toJSON
         * @memberof dataform.JitTableResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JitTableResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JitTableResult
         * @function getTypeUrl
         * @memberof dataform.JitTableResult
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JitTableResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.JitTableResult";
        };

        return JitTableResult;
    })();

    dataform.JitIncrementalTableResult = (function() {

        /**
         * Properties of a JitIncrementalTableResult.
         * @memberof dataform
         * @interface IJitIncrementalTableResult
         * @property {dataform.IJitTableResult|null} [regular] JitIncrementalTableResult regular
         * @property {dataform.IJitTableResult|null} [incremental] JitIncrementalTableResult incremental
         */

        /**
         * Constructs a new JitIncrementalTableResult.
         * @memberof dataform
         * @classdesc Represents a JitIncrementalTableResult.
         * @implements IJitIncrementalTableResult
         * @constructor
         * @param {dataform.IJitIncrementalTableResult=} [properties] Properties to set
         */
        function JitIncrementalTableResult(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JitIncrementalTableResult regular.
         * @member {dataform.IJitTableResult|null|undefined} regular
         * @memberof dataform.JitIncrementalTableResult
         * @instance
         */
        JitIncrementalTableResult.prototype.regular = null;

        /**
         * JitIncrementalTableResult incremental.
         * @member {dataform.IJitTableResult|null|undefined} incremental
         * @memberof dataform.JitIncrementalTableResult
         * @instance
         */
        JitIncrementalTableResult.prototype.incremental = null;

        /**
         * Creates a new JitIncrementalTableResult instance using the specified properties.
         * @function create
         * @memberof dataform.JitIncrementalTableResult
         * @static
         * @param {dataform.IJitIncrementalTableResult=} [properties] Properties to set
         * @returns {dataform.JitIncrementalTableResult} JitIncrementalTableResult instance
         */
        JitIncrementalTableResult.create = function create(properties) {
            return new JitIncrementalTableResult(properties);
        };

        /**
         * Encodes the specified JitIncrementalTableResult message. Does not implicitly {@link dataform.JitIncrementalTableResult.verify|verify} messages.
         * @function encode
         * @memberof dataform.JitIncrementalTableResult
         * @static
         * @param {dataform.IJitIncrementalTableResult} message JitIncrementalTableResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JitIncrementalTableResult.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.regular != null && Object.hasOwnProperty.call(message, "regular"))
                $root.dataform.JitTableResult.encode(message.regular, writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
            if (message.incremental != null && Object.hasOwnProperty.call(message, "incremental"))
                $root.dataform.JitTableResult.encode(message.incremental, writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim();
            return writer;
        };

        /**
         * Encodes the specified JitIncrementalTableResult message, length delimited. Does not implicitly {@link dataform.JitIncrementalTableResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.JitIncrementalTableResult
         * @static
         * @param {dataform.IJitIncrementalTableResult} message JitIncrementalTableResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JitIncrementalTableResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a JitIncrementalTableResult message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.JitIncrementalTableResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.JitIncrementalTableResult} JitIncrementalTableResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JitIncrementalTableResult.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.JitIncrementalTableResult();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.regular = $root.dataform.JitTableResult.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                case 2: {
                        message.incremental = $root.dataform.JitTableResult.decode(reader, reader.uint32(), undefined, long + 1);
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JitIncrementalTableResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.JitIncrementalTableResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.JitIncrementalTableResult} JitIncrementalTableResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JitIncrementalTableResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JitIncrementalTableResult message.
         * @function verify
         * @memberof dataform.JitIncrementalTableResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JitIncrementalTableResult.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.regular != null && Object.hasOwnProperty.call(message, "regular")) {
                let error = $root.dataform.JitTableResult.verify(message.regular, long + 1);
                if (error)
                    return "regular." + error;
            }
            if (message.incremental != null && Object.hasOwnProperty.call(message, "incremental")) {
                let error = $root.dataform.JitTableResult.verify(message.incremental, long + 1);
                if (error)
                    return "incremental." + error;
            }
            return null;
        };

        /**
         * Creates a JitIncrementalTableResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.JitIncrementalTableResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.JitIncrementalTableResult} JitIncrementalTableResult
         */
        JitIncrementalTableResult.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.JitIncrementalTableResult)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.JitIncrementalTableResult: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.JitIncrementalTableResult();
            if (object.regular != null) {
                if (!$util.isObject(object.regular))
                    throw TypeError(".dataform.JitIncrementalTableResult.regular: object expected");
                message.regular = $root.dataform.JitTableResult.fromObject(object.regular, long + 1);
            }
            if (object.incremental != null) {
                if (!$util.isObject(object.incremental))
                    throw TypeError(".dataform.JitIncrementalTableResult.incremental: object expected");
                message.incremental = $root.dataform.JitTableResult.fromObject(object.incremental, long + 1);
            }
            return message;
        };

        /**
         * Creates a plain object from a JitIncrementalTableResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.JitIncrementalTableResult
         * @static
         * @param {dataform.JitIncrementalTableResult} message JitIncrementalTableResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JitIncrementalTableResult.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.regular = null;
                object.incremental = null;
            }
            if (message.regular != null && Object.hasOwnProperty.call(message, "regular"))
                object.regular = $root.dataform.JitTableResult.toObject(message.regular, options, q + 1);
            if (message.incremental != null && Object.hasOwnProperty.call(message, "incremental"))
                object.incremental = $root.dataform.JitTableResult.toObject(message.incremental, options, q + 1);
            return object;
        };

        /**
         * Converts this JitIncrementalTableResult to JSON.
         * @function toJSON
         * @memberof dataform.JitIncrementalTableResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JitIncrementalTableResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JitIncrementalTableResult
         * @function getTypeUrl
         * @memberof dataform.JitIncrementalTableResult
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JitIncrementalTableResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.JitIncrementalTableResult";
        };

        return JitIncrementalTableResult;
    })();

    dataform.JitOperationResult = (function() {

        /**
         * Properties of a JitOperationResult.
         * @memberof dataform
         * @interface IJitOperationResult
         * @property {Array.<string>|null} [queries] JitOperationResult queries
         */

        /**
         * Constructs a new JitOperationResult.
         * @memberof dataform
         * @classdesc Represents a JitOperationResult.
         * @implements IJitOperationResult
         * @constructor
         * @param {dataform.IJitOperationResult=} [properties] Properties to set
         */
        function JitOperationResult(properties) {
            this.queries = [];
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JitOperationResult queries.
         * @member {Array.<string>} queries
         * @memberof dataform.JitOperationResult
         * @instance
         */
        JitOperationResult.prototype.queries = $util.emptyArray;

        /**
         * Creates a new JitOperationResult instance using the specified properties.
         * @function create
         * @memberof dataform.JitOperationResult
         * @static
         * @param {dataform.IJitOperationResult=} [properties] Properties to set
         * @returns {dataform.JitOperationResult} JitOperationResult instance
         */
        JitOperationResult.create = function create(properties) {
            return new JitOperationResult(properties);
        };

        /**
         * Encodes the specified JitOperationResult message. Does not implicitly {@link dataform.JitOperationResult.verify|verify} messages.
         * @function encode
         * @memberof dataform.JitOperationResult
         * @static
         * @param {dataform.IJitOperationResult} message JitOperationResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JitOperationResult.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.queries != null && message.queries.length)
                for (let i = 0; i < message.queries.length; ++i)
                    writer.uint32(/* id 1, wireType 2 =*/10).string(message.queries[i]);
            return writer;
        };

        /**
         * Encodes the specified JitOperationResult message, length delimited. Does not implicitly {@link dataform.JitOperationResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.JitOperationResult
         * @static
         * @param {dataform.IJitOperationResult} message JitOperationResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JitOperationResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a JitOperationResult message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.JitOperationResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.JitOperationResult} JitOperationResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JitOperationResult.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.JitOperationResult();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        if (!(message.queries && message.queries.length))
                            message.queries = [];
                        message.queries.push(reader.string());
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JitOperationResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.JitOperationResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.JitOperationResult} JitOperationResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JitOperationResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JitOperationResult message.
         * @function verify
         * @memberof dataform.JitOperationResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JitOperationResult.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.queries != null && Object.hasOwnProperty.call(message, "queries")) {
                if (!Array.isArray(message.queries))
                    return "queries: array expected";
                for (let i = 0; i < message.queries.length; ++i)
                    if (!$util.isString(message.queries[i]))
                        return "queries: string[] expected";
            }
            return null;
        };

        /**
         * Creates a JitOperationResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.JitOperationResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.JitOperationResult} JitOperationResult
         */
        JitOperationResult.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.JitOperationResult)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.JitOperationResult: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.JitOperationResult();
            if (object.queries) {
                if (!Array.isArray(object.queries))
                    throw TypeError(".dataform.JitOperationResult.queries: array expected");
                message.queries = [];
                for (let i = 0; i < object.queries.length; ++i)
                    message.queries[i] = String(object.queries[i]);
            }
            return message;
        };

        /**
         * Creates a plain object from a JitOperationResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.JitOperationResult
         * @static
         * @param {dataform.JitOperationResult} message JitOperationResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JitOperationResult.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.arrays || options.defaults)
                object.queries = [];
            if (message.queries && message.queries.length) {
                object.queries = [];
                for (let j = 0; j < message.queries.length; ++j)
                    object.queries[j] = message.queries[j];
            }
            return object;
        };

        /**
         * Converts this JitOperationResult to JSON.
         * @function toJSON
         * @memberof dataform.JitOperationResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JitOperationResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JitOperationResult
         * @function getTypeUrl
         * @memberof dataform.JitOperationResult
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JitOperationResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.JitOperationResult";
        };

        return JitOperationResult;
    })();

    dataform.JitAssertionResult = (function() {

        /**
         * Properties of a JitAssertionResult.
         * @memberof dataform
         * @interface IJitAssertionResult
         * @property {string|null} [query] JitAssertionResult query
         */

        /**
         * Constructs a new JitAssertionResult.
         * @memberof dataform
         * @classdesc Represents a JitAssertionResult.
         * @implements IJitAssertionResult
         * @constructor
         * @param {dataform.IJitAssertionResult=} [properties] Properties to set
         */
        function JitAssertionResult(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * JitAssertionResult query.
         * @member {string} query
         * @memberof dataform.JitAssertionResult
         * @instance
         */
        JitAssertionResult.prototype.query = "";

        /**
         * Creates a new JitAssertionResult instance using the specified properties.
         * @function create
         * @memberof dataform.JitAssertionResult
         * @static
         * @param {dataform.IJitAssertionResult=} [properties] Properties to set
         * @returns {dataform.JitAssertionResult} JitAssertionResult instance
         */
        JitAssertionResult.create = function create(properties) {
            return new JitAssertionResult(properties);
        };

        /**
         * Encodes the specified JitAssertionResult message. Does not implicitly {@link dataform.JitAssertionResult.verify|verify} messages.
         * @function encode
         * @memberof dataform.JitAssertionResult
         * @static
         * @param {dataform.IJitAssertionResult} message JitAssertionResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JitAssertionResult.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.query != null && Object.hasOwnProperty.call(message, "query"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.query);
            return writer;
        };

        /**
         * Encodes the specified JitAssertionResult message, length delimited. Does not implicitly {@link dataform.JitAssertionResult.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.JitAssertionResult
         * @static
         * @param {dataform.IJitAssertionResult} message JitAssertionResult message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        JitAssertionResult.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a JitAssertionResult message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.JitAssertionResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.JitAssertionResult} JitAssertionResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JitAssertionResult.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.JitAssertionResult();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.query = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a JitAssertionResult message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.JitAssertionResult
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.JitAssertionResult} JitAssertionResult
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        JitAssertionResult.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a JitAssertionResult message.
         * @function verify
         * @memberof dataform.JitAssertionResult
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        JitAssertionResult.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.query != null && Object.hasOwnProperty.call(message, "query"))
                if (!$util.isString(message.query))
                    return "query: string expected";
            return null;
        };

        /**
         * Creates a JitAssertionResult message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.JitAssertionResult
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.JitAssertionResult} JitAssertionResult
         */
        JitAssertionResult.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.JitAssertionResult)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.JitAssertionResult: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.JitAssertionResult();
            if (object.query != null)
                message.query = String(object.query);
            return message;
        };

        /**
         * Creates a plain object from a JitAssertionResult message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.JitAssertionResult
         * @static
         * @param {dataform.JitAssertionResult} message JitAssertionResult
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        JitAssertionResult.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults)
                object.query = "";
            if (message.query != null && Object.hasOwnProperty.call(message, "query"))
                object.query = message.query;
            return object;
        };

        /**
         * Converts this JitAssertionResult to JSON.
         * @function toJSON
         * @memberof dataform.JitAssertionResult
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        JitAssertionResult.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for JitAssertionResult
         * @function getTypeUrl
         * @memberof dataform.JitAssertionResult
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        JitAssertionResult.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.JitAssertionResult";
        };

        return JitAssertionResult;
    })();

    dataform.BigQuery = (function() {

        /**
         * Properties of a BigQuery.
         * @memberof dataform
         * @interface IBigQuery
         * @property {string|null} [projectId] BigQuery projectId
         * @property {string|null} [credentials] BigQuery credentials
         * @property {string|null} [location] BigQuery location
         */

        /**
         * Constructs a new BigQuery.
         * @memberof dataform
         * @classdesc Represents a BigQuery.
         * @implements IBigQuery
         * @constructor
         * @param {dataform.IBigQuery=} [properties] Properties to set
         */
        function BigQuery(properties) {
            if (properties)
                for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                    if (properties[keys[i]] != null && keys[i] !== "__proto__")
                        this[keys[i]] = properties[keys[i]];
        }

        /**
         * BigQuery projectId.
         * @member {string} projectId
         * @memberof dataform.BigQuery
         * @instance
         */
        BigQuery.prototype.projectId = "";

        /**
         * BigQuery credentials.
         * @member {string} credentials
         * @memberof dataform.BigQuery
         * @instance
         */
        BigQuery.prototype.credentials = "";

        /**
         * BigQuery location.
         * @member {string} location
         * @memberof dataform.BigQuery
         * @instance
         */
        BigQuery.prototype.location = "";

        /**
         * Creates a new BigQuery instance using the specified properties.
         * @function create
         * @memberof dataform.BigQuery
         * @static
         * @param {dataform.IBigQuery=} [properties] Properties to set
         * @returns {dataform.BigQuery} BigQuery instance
         */
        BigQuery.create = function create(properties) {
            return new BigQuery(properties);
        };

        /**
         * Encodes the specified BigQuery message. Does not implicitly {@link dataform.BigQuery.verify|verify} messages.
         * @function encode
         * @memberof dataform.BigQuery
         * @static
         * @param {dataform.IBigQuery} message BigQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BigQuery.encode = function encode(message, writer, q) {
            if (!writer)
                writer = $Writer.create();
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            if (message.projectId != null && Object.hasOwnProperty.call(message, "projectId"))
                writer.uint32(/* id 1, wireType 2 =*/10).string(message.projectId);
            if (message.credentials != null && Object.hasOwnProperty.call(message, "credentials"))
                writer.uint32(/* id 3, wireType 2 =*/26).string(message.credentials);
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                writer.uint32(/* id 4, wireType 2 =*/34).string(message.location);
            return writer;
        };

        /**
         * Encodes the specified BigQuery message, length delimited. Does not implicitly {@link dataform.BigQuery.verify|verify} messages.
         * @function encodeDelimited
         * @memberof dataform.BigQuery
         * @static
         * @param {dataform.IBigQuery} message BigQuery message or plain object to encode
         * @param {$protobuf.Writer} [writer] Writer to encode to
         * @returns {$protobuf.Writer} Writer
         */
        BigQuery.encodeDelimited = function encodeDelimited(message, writer) {
            return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
        };

        /**
         * Decodes a BigQuery message from the specified reader or buffer.
         * @function decode
         * @memberof dataform.BigQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @param {number} [length] Message length if known beforehand
         * @returns {dataform.BigQuery} BigQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BigQuery.decode = function decode(reader, length, error, long) {
            if (!(reader instanceof $Reader))
                reader = $Reader.create(reader);
            if (long === undefined)
                long = 0;
            if (long > $Reader.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let end = length === undefined ? reader.len : reader.pos + length, message = new $root.dataform.BigQuery();
            while (reader.pos < end) {
                let tag = reader.uint32();
                if (tag === error)
                    break;
                switch (tag >>> 3) {
                case 1: {
                        message.projectId = reader.string();
                        break;
                    }
                case 3: {
                        message.credentials = reader.string();
                        break;
                    }
                case 4: {
                        message.location = reader.string();
                        break;
                    }
                default:
                    reader.skipType(tag & 7, long);
                    break;
                }
            }
            return message;
        };

        /**
         * Decodes a BigQuery message from the specified reader or buffer, length delimited.
         * @function decodeDelimited
         * @memberof dataform.BigQuery
         * @static
         * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
         * @returns {dataform.BigQuery} BigQuery
         * @throws {Error} If the payload is not a reader or valid buffer
         * @throws {$protobuf.util.ProtocolError} If required fields are missing
         */
        BigQuery.decodeDelimited = function decodeDelimited(reader) {
            if (!(reader instanceof $Reader))
                reader = new $Reader(reader);
            return this.decode(reader, reader.uint32());
        };

        /**
         * Verifies a BigQuery message.
         * @function verify
         * @memberof dataform.BigQuery
         * @static
         * @param {Object.<string,*>} message Plain object to verify
         * @returns {string|null} `null` if valid, otherwise the reason why it is not
         */
        BigQuery.verify = function verify(message, long) {
            if (typeof message !== "object" || message === null)
                return "object expected";
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                return "maximum nesting depth exceeded";
            if (message.projectId != null && Object.hasOwnProperty.call(message, "projectId"))
                if (!$util.isString(message.projectId))
                    return "projectId: string expected";
            if (message.credentials != null && Object.hasOwnProperty.call(message, "credentials"))
                if (!$util.isString(message.credentials))
                    return "credentials: string expected";
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                if (!$util.isString(message.location))
                    return "location: string expected";
            return null;
        };

        /**
         * Creates a BigQuery message from a plain object. Also converts values to their respective internal types.
         * @function fromObject
         * @memberof dataform.BigQuery
         * @static
         * @param {Object.<string,*>} object Plain object
         * @returns {dataform.BigQuery} BigQuery
         */
        BigQuery.fromObject = function fromObject(object, long) {
            if (object instanceof $root.dataform.BigQuery)
                return object;
            if (!$util.isObject(object))
                throw TypeError(".dataform.BigQuery: object expected");
            if (long === undefined)
                long = 0;
            if (long > $util.recursionLimit)
                throw Error("maximum nesting depth exceeded");
            let message = new $root.dataform.BigQuery();
            if (object.projectId != null)
                message.projectId = String(object.projectId);
            if (object.credentials != null)
                message.credentials = String(object.credentials);
            if (object.location != null)
                message.location = String(object.location);
            return message;
        };

        /**
         * Creates a plain object from a BigQuery message. Also converts values to other types if specified.
         * @function toObject
         * @memberof dataform.BigQuery
         * @static
         * @param {dataform.BigQuery} message BigQuery
         * @param {$protobuf.IConversionOptions} [options] Conversion options
         * @returns {Object.<string,*>} Plain object
         */
        BigQuery.toObject = function toObject(message, options, q) {
            if (!options)
                options = {};
            if (q === undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw Error("max depth exceeded");
            let object = {};
            if (options.defaults) {
                object.projectId = "";
                object.credentials = "";
                object.location = "";
            }
            if (message.projectId != null && Object.hasOwnProperty.call(message, "projectId"))
                object.projectId = message.projectId;
            if (message.credentials != null && Object.hasOwnProperty.call(message, "credentials"))
                object.credentials = message.credentials;
            if (message.location != null && Object.hasOwnProperty.call(message, "location"))
                object.location = message.location;
            return object;
        };

        /**
         * Converts this BigQuery to JSON.
         * @function toJSON
         * @memberof dataform.BigQuery
         * @instance
         * @returns {Object.<string,*>} JSON object
         */
        BigQuery.prototype.toJSON = function toJSON() {
            return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
        };

        /**
         * Gets the default type url for BigQuery
         * @function getTypeUrl
         * @memberof dataform.BigQuery
         * @static
         * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
         * @returns {string} The default type url
         */
        BigQuery.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
            if (typeUrlPrefix === undefined) {
                typeUrlPrefix = "type.googleapis.com";
            }
            return typeUrlPrefix + "/dataform.BigQuery";
        };

        return BigQuery;
    })();

    return dataform;
})();

const google = $root.google = (() => {

    /**
     * Namespace google.
     * @exports google
     * @namespace
     */
    const google = {};

    google.protobuf = (function() {

        /**
         * Namespace protobuf.
         * @memberof google
         * @namespace
         */
        const protobuf = {};

        protobuf.Struct = (function() {

            /**
             * Properties of a Struct.
             * @memberof google.protobuf
             * @interface IStruct
             * @property {Object.<string,google.protobuf.IValue>|null} [fields] Struct fields
             */

            /**
             * Constructs a new Struct.
             * @memberof google.protobuf
             * @classdesc Represents a Struct.
             * @implements IStruct
             * @constructor
             * @param {google.protobuf.IStruct=} [properties] Properties to set
             */
            function Struct(properties) {
                this.fields = {};
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Struct fields.
             * @member {Object.<string,google.protobuf.IValue>} fields
             * @memberof google.protobuf.Struct
             * @instance
             */
            Struct.prototype.fields = $util.emptyObject;

            /**
             * Creates a new Struct instance using the specified properties.
             * @function create
             * @memberof google.protobuf.Struct
             * @static
             * @param {google.protobuf.IStruct=} [properties] Properties to set
             * @returns {google.protobuf.Struct} Struct instance
             */
            Struct.create = function create(properties) {
                return new Struct(properties);
            };

            /**
             * Encodes the specified Struct message. Does not implicitly {@link google.protobuf.Struct.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.Struct
             * @static
             * @param {google.protobuf.IStruct} message Struct message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Struct.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.fields != null && Object.hasOwnProperty.call(message, "fields"))
                    for (let keys = Object.keys(message.fields), i = 0; i < keys.length; ++i) {
                        writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]);
                        $root.google.protobuf.Value.encode(message.fields[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), q + 1).ldelim().ldelim();
                    }
                return writer;
            };

            /**
             * Encodes the specified Struct message, length delimited. Does not implicitly {@link google.protobuf.Struct.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.Struct
             * @static
             * @param {google.protobuf.IStruct} message Struct message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Struct.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a Struct message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.Struct
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.Struct} Struct
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Struct.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.google.protobuf.Struct(), key, value;
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            if (message.fields === $util.emptyObject)
                                message.fields = {};
                            let end2 = reader.uint32() + reader.pos;
                            key = "";
                            value = null;
                            while (reader.pos < end2) {
                                let tag2 = reader.uint32();
                                switch (tag2 >>> 3) {
                                case 1:
                                    key = reader.string();
                                    break;
                                case 2:
                                    value = $root.google.protobuf.Value.decode(reader, reader.uint32(), undefined, long + 1);
                                    break;
                                default:
                                    reader.skipType(tag2 & 7, long);
                                    break;
                                }
                            }
                            if (key === "__proto__")
                                $util.makeProp(message.fields, key);
                            message.fields[key] = value;
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Struct message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.Struct
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.Struct} Struct
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Struct.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Struct message.
             * @function verify
             * @memberof google.protobuf.Struct
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Struct.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.fields != null && Object.hasOwnProperty.call(message, "fields")) {
                    if (!$util.isObject(message.fields))
                        return "fields: object expected";
                    let key = Object.keys(message.fields);
                    for (let i = 0; i < key.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.fields[key[i]], long + 1);
                        if (error)
                            return "fields." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Struct message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.Struct
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.Struct} Struct
             */
            Struct.fromObject = function fromObject(object, long) {
                if (object instanceof $root.google.protobuf.Struct)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".google.protobuf.Struct: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.google.protobuf.Struct();
                if (object.fields) {
                    if (!$util.isObject(object.fields))
                        throw TypeError(".google.protobuf.Struct.fields: object expected");
                    message.fields = {};
                    for (let keys = Object.keys(object.fields), i = 0; i < keys.length; ++i) {
                        if (keys[i] === "__proto__")
                            $util.makeProp(message.fields, keys[i]);
                        if (!$util.isObject(object.fields[keys[i]]))
                            throw TypeError(".google.protobuf.Struct.fields: object expected");
                        message.fields[keys[i]] = $root.google.protobuf.Value.fromObject(object.fields[keys[i]], long + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a Struct message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.Struct
             * @static
             * @param {google.protobuf.Struct} message Struct
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Struct.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.objects || options.defaults)
                    object.fields = {};
                let keys2;
                if (message.fields && (keys2 = Object.keys(message.fields)).length) {
                    object.fields = {};
                    for (let j = 0; j < keys2.length; ++j) {
                        if (keys2[j] === "__proto__")
                            $util.makeProp(object.fields, keys2[j]);
                        object.fields[keys2[j]] = $root.google.protobuf.Value.toObject(message.fields[keys2[j]], options, q + 1);
                    }
                }
                return object;
            };

            /**
             * Converts this Struct to JSON.
             * @function toJSON
             * @memberof google.protobuf.Struct
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Struct.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Struct
             * @function getTypeUrl
             * @memberof google.protobuf.Struct
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Struct.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/google.protobuf.Struct";
            };

            return Struct;
        })();

        protobuf.Value = (function() {

            /**
             * Properties of a Value.
             * @memberof google.protobuf
             * @interface IValue
             * @property {google.protobuf.NullValue|null} [nullValue] Value nullValue
             * @property {number|null} [numberValue] Value numberValue
             * @property {string|null} [stringValue] Value stringValue
             * @property {boolean|null} [boolValue] Value boolValue
             * @property {google.protobuf.IStruct|null} [structValue] Value structValue
             * @property {google.protobuf.IListValue|null} [listValue] Value listValue
             */

            /**
             * Constructs a new Value.
             * @memberof google.protobuf
             * @classdesc Represents a Value.
             * @implements IValue
             * @constructor
             * @param {google.protobuf.IValue=} [properties] Properties to set
             */
            function Value(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Value nullValue.
             * @member {google.protobuf.NullValue|null|undefined} nullValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.nullValue = null;

            /**
             * Value numberValue.
             * @member {number|null|undefined} numberValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.numberValue = null;

            /**
             * Value stringValue.
             * @member {string|null|undefined} stringValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.stringValue = null;

            /**
             * Value boolValue.
             * @member {boolean|null|undefined} boolValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.boolValue = null;

            /**
             * Value structValue.
             * @member {google.protobuf.IStruct|null|undefined} structValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.structValue = null;

            /**
             * Value listValue.
             * @member {google.protobuf.IListValue|null|undefined} listValue
             * @memberof google.protobuf.Value
             * @instance
             */
            Value.prototype.listValue = null;

            // OneOf field names bound to virtual getters and setters
            let $oneOfFields;

            /**
             * Value kind.
             * @member {"nullValue"|"numberValue"|"stringValue"|"boolValue"|"structValue"|"listValue"|undefined} kind
             * @memberof google.protobuf.Value
             * @instance
             */
            Object.defineProperty(Value.prototype, "kind", {
                get: $util.oneOfGetter($oneOfFields = ["nullValue", "numberValue", "stringValue", "boolValue", "structValue", "listValue"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            /**
             * Creates a new Value instance using the specified properties.
             * @function create
             * @memberof google.protobuf.Value
             * @static
             * @param {google.protobuf.IValue=} [properties] Properties to set
             * @returns {google.protobuf.Value} Value instance
             */
            Value.create = function create(properties) {
                return new Value(properties);
            };

            /**
             * Encodes the specified Value message. Does not implicitly {@link google.protobuf.Value.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.Value
             * @static
             * @param {google.protobuf.IValue} message Value message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Value.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.nullValue != null && Object.hasOwnProperty.call(message, "nullValue"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int32(message.nullValue);
                if (message.numberValue != null && Object.hasOwnProperty.call(message, "numberValue"))
                    writer.uint32(/* id 2, wireType 1 =*/17).double(message.numberValue);
                if (message.stringValue != null && Object.hasOwnProperty.call(message, "stringValue"))
                    writer.uint32(/* id 3, wireType 2 =*/26).string(message.stringValue);
                if (message.boolValue != null && Object.hasOwnProperty.call(message, "boolValue"))
                    writer.uint32(/* id 4, wireType 0 =*/32).bool(message.boolValue);
                if (message.structValue != null && Object.hasOwnProperty.call(message, "structValue"))
                    $root.google.protobuf.Struct.encode(message.structValue, writer.uint32(/* id 5, wireType 2 =*/42).fork(), q + 1).ldelim();
                if (message.listValue != null && Object.hasOwnProperty.call(message, "listValue"))
                    $root.google.protobuf.ListValue.encode(message.listValue, writer.uint32(/* id 6, wireType 2 =*/50).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified Value message, length delimited. Does not implicitly {@link google.protobuf.Value.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.Value
             * @static
             * @param {google.protobuf.IValue} message Value message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Value.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a Value message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.Value
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.Value} Value
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Value.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.google.protobuf.Value();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.nullValue = reader.int32();
                            break;
                        }
                    case 2: {
                            message.numberValue = reader.double();
                            break;
                        }
                    case 3: {
                            message.stringValue = reader.string();
                            break;
                        }
                    case 4: {
                            message.boolValue = reader.bool();
                            break;
                        }
                    case 5: {
                            message.structValue = $root.google.protobuf.Struct.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    case 6: {
                            message.listValue = $root.google.protobuf.ListValue.decode(reader, reader.uint32(), undefined, long + 1);
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Value message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.Value
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.Value} Value
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Value.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Value message.
             * @function verify
             * @memberof google.protobuf.Value
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Value.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                let properties = {};
                if (message.nullValue != null && Object.hasOwnProperty.call(message, "nullValue")) {
                    properties.kind = 1;
                    switch (message.nullValue) {
                    default:
                        return "nullValue: enum value expected";
                    case 0:
                        break;
                    }
                }
                if (message.numberValue != null && Object.hasOwnProperty.call(message, "numberValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    if (typeof message.numberValue !== "number")
                        return "numberValue: number expected";
                }
                if (message.stringValue != null && Object.hasOwnProperty.call(message, "stringValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    if (!$util.isString(message.stringValue))
                        return "stringValue: string expected";
                }
                if (message.boolValue != null && Object.hasOwnProperty.call(message, "boolValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    if (typeof message.boolValue !== "boolean")
                        return "boolValue: boolean expected";
                }
                if (message.structValue != null && Object.hasOwnProperty.call(message, "structValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    {
                        let error = $root.google.protobuf.Struct.verify(message.structValue, long + 1);
                        if (error)
                            return "structValue." + error;
                    }
                }
                if (message.listValue != null && Object.hasOwnProperty.call(message, "listValue")) {
                    if (properties.kind === 1)
                        return "kind: multiple values";
                    properties.kind = 1;
                    {
                        let error = $root.google.protobuf.ListValue.verify(message.listValue, long + 1);
                        if (error)
                            return "listValue." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a Value message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.Value
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.Value} Value
             */
            Value.fromObject = function fromObject(object, long) {
                if (object instanceof $root.google.protobuf.Value)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".google.protobuf.Value: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.google.protobuf.Value();
                switch (object.nullValue) {
                default:
                    if (typeof object.nullValue === "number") {
                        message.nullValue = object.nullValue;
                        break;
                    }
                    break;
                case "NULL_VALUE":
                case 0:
                    message.nullValue = 0;
                    break;
                }
                if (object.numberValue != null)
                    message.numberValue = Number(object.numberValue);
                if (object.stringValue != null)
                    message.stringValue = String(object.stringValue);
                if (object.boolValue != null)
                    message.boolValue = Boolean(object.boolValue);
                if (object.structValue != null) {
                    if (!$util.isObject(object.structValue))
                        throw TypeError(".google.protobuf.Value.structValue: object expected");
                    message.structValue = $root.google.protobuf.Struct.fromObject(object.structValue, long + 1);
                }
                if (object.listValue != null) {
                    if (!$util.isObject(object.listValue))
                        throw TypeError(".google.protobuf.Value.listValue: object expected");
                    message.listValue = $root.google.protobuf.ListValue.fromObject(object.listValue, long + 1);
                }
                return message;
            };

            /**
             * Creates a plain object from a Value message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.Value
             * @static
             * @param {google.protobuf.Value} message Value
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Value.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (message.nullValue != null && Object.hasOwnProperty.call(message, "nullValue")) {
                    object.nullValue = options.enums === String ? $root.google.protobuf.NullValue[message.nullValue] === undefined ? message.nullValue : $root.google.protobuf.NullValue[message.nullValue] : message.nullValue;
                    if (options.oneofs)
                        object.kind = "nullValue";
                }
                if (message.numberValue != null && Object.hasOwnProperty.call(message, "numberValue")) {
                    object.numberValue = options.json && !isFinite(message.numberValue) ? String(message.numberValue) : message.numberValue;
                    if (options.oneofs)
                        object.kind = "numberValue";
                }
                if (message.stringValue != null && Object.hasOwnProperty.call(message, "stringValue")) {
                    object.stringValue = message.stringValue;
                    if (options.oneofs)
                        object.kind = "stringValue";
                }
                if (message.boolValue != null && Object.hasOwnProperty.call(message, "boolValue")) {
                    object.boolValue = message.boolValue;
                    if (options.oneofs)
                        object.kind = "boolValue";
                }
                if (message.structValue != null && Object.hasOwnProperty.call(message, "structValue")) {
                    object.structValue = $root.google.protobuf.Struct.toObject(message.structValue, options, q + 1);
                    if (options.oneofs)
                        object.kind = "structValue";
                }
                if (message.listValue != null && Object.hasOwnProperty.call(message, "listValue")) {
                    object.listValue = $root.google.protobuf.ListValue.toObject(message.listValue, options, q + 1);
                    if (options.oneofs)
                        object.kind = "listValue";
                }
                return object;
            };

            /**
             * Converts this Value to JSON.
             * @function toJSON
             * @memberof google.protobuf.Value
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Value.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Value
             * @function getTypeUrl
             * @memberof google.protobuf.Value
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Value.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/google.protobuf.Value";
            };

            return Value;
        })();

        /**
         * NullValue enum.
         * @name google.protobuf.NullValue
         * @enum {number}
         * @property {number} NULL_VALUE=0 NULL_VALUE value
         */
        protobuf.NullValue = (function() {
            const valuesById = {}, values = Object.create(valuesById);
            values[valuesById[0] = "NULL_VALUE"] = 0;
            return values;
        })();

        protobuf.ListValue = (function() {

            /**
             * Properties of a ListValue.
             * @memberof google.protobuf
             * @interface IListValue
             * @property {Array.<google.protobuf.IValue>|null} [values] ListValue values
             */

            /**
             * Constructs a new ListValue.
             * @memberof google.protobuf
             * @classdesc Represents a ListValue.
             * @implements IListValue
             * @constructor
             * @param {google.protobuf.IListValue=} [properties] Properties to set
             */
            function ListValue(properties) {
                this.values = [];
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * ListValue values.
             * @member {Array.<google.protobuf.IValue>} values
             * @memberof google.protobuf.ListValue
             * @instance
             */
            ListValue.prototype.values = $util.emptyArray;

            /**
             * Creates a new ListValue instance using the specified properties.
             * @function create
             * @memberof google.protobuf.ListValue
             * @static
             * @param {google.protobuf.IListValue=} [properties] Properties to set
             * @returns {google.protobuf.ListValue} ListValue instance
             */
            ListValue.create = function create(properties) {
                return new ListValue(properties);
            };

            /**
             * Encodes the specified ListValue message. Does not implicitly {@link google.protobuf.ListValue.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.ListValue
             * @static
             * @param {google.protobuf.IListValue} message ListValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ListValue.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.values != null && message.values.length)
                    for (let i = 0; i < message.values.length; ++i)
                        $root.google.protobuf.Value.encode(message.values[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), q + 1).ldelim();
                return writer;
            };

            /**
             * Encodes the specified ListValue message, length delimited. Does not implicitly {@link google.protobuf.ListValue.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.ListValue
             * @static
             * @param {google.protobuf.IListValue} message ListValue message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            ListValue.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a ListValue message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.ListValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.ListValue} ListValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ListValue.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.google.protobuf.ListValue();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            if (!(message.values && message.values.length))
                                message.values = [];
                            message.values.push($root.google.protobuf.Value.decode(reader, reader.uint32(), undefined, long + 1));
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a ListValue message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.ListValue
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.ListValue} ListValue
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            ListValue.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a ListValue message.
             * @function verify
             * @memberof google.protobuf.ListValue
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            ListValue.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.values != null && Object.hasOwnProperty.call(message, "values")) {
                    if (!Array.isArray(message.values))
                        return "values: array expected";
                    for (let i = 0; i < message.values.length; ++i) {
                        let error = $root.google.protobuf.Value.verify(message.values[i], long + 1);
                        if (error)
                            return "values." + error;
                    }
                }
                return null;
            };

            /**
             * Creates a ListValue message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.ListValue
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.ListValue} ListValue
             */
            ListValue.fromObject = function fromObject(object, long) {
                if (object instanceof $root.google.protobuf.ListValue)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".google.protobuf.ListValue: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.google.protobuf.ListValue();
                if (object.values) {
                    if (!Array.isArray(object.values))
                        throw TypeError(".google.protobuf.ListValue.values: array expected");
                    message.values = [];
                    for (let i = 0; i < object.values.length; ++i) {
                        if (!$util.isObject(object.values[i]))
                            throw TypeError(".google.protobuf.ListValue.values: object expected");
                        message.values[i] = $root.google.protobuf.Value.fromObject(object.values[i], long + 1);
                    }
                }
                return message;
            };

            /**
             * Creates a plain object from a ListValue message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.ListValue
             * @static
             * @param {google.protobuf.ListValue} message ListValue
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            ListValue.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.arrays || options.defaults)
                    object.values = [];
                if (message.values && message.values.length) {
                    object.values = [];
                    for (let j = 0; j < message.values.length; ++j)
                        object.values[j] = $root.google.protobuf.Value.toObject(message.values[j], options, q + 1);
                }
                return object;
            };

            /**
             * Converts this ListValue to JSON.
             * @function toJSON
             * @memberof google.protobuf.ListValue
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            ListValue.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for ListValue
             * @function getTypeUrl
             * @memberof google.protobuf.ListValue
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            ListValue.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/google.protobuf.ListValue";
            };

            return ListValue;
        })();

        protobuf.Empty = (function() {

            /**
             * Properties of an Empty.
             * @memberof google.protobuf
             * @interface IEmpty
             */

            /**
             * Constructs a new Empty.
             * @memberof google.protobuf
             * @classdesc Represents an Empty.
             * @implements IEmpty
             * @constructor
             * @param {google.protobuf.IEmpty=} [properties] Properties to set
             */
            function Empty(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Creates a new Empty instance using the specified properties.
             * @function create
             * @memberof google.protobuf.Empty
             * @static
             * @param {google.protobuf.IEmpty=} [properties] Properties to set
             * @returns {google.protobuf.Empty} Empty instance
             */
            Empty.create = function create(properties) {
                return new Empty(properties);
            };

            /**
             * Encodes the specified Empty message. Does not implicitly {@link google.protobuf.Empty.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.Empty
             * @static
             * @param {google.protobuf.IEmpty} message Empty message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Empty.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                return writer;
            };

            /**
             * Encodes the specified Empty message, length delimited. Does not implicitly {@link google.protobuf.Empty.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.Empty
             * @static
             * @param {google.protobuf.IEmpty} message Empty message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Empty.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes an Empty message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.Empty
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.Empty} Empty
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Empty.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.google.protobuf.Empty();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes an Empty message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.Empty
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.Empty} Empty
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Empty.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies an Empty message.
             * @function verify
             * @memberof google.protobuf.Empty
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Empty.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                return null;
            };

            /**
             * Creates an Empty message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.Empty
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.Empty} Empty
             */
            Empty.fromObject = function fromObject(object, long) {
                if (object instanceof $root.google.protobuf.Empty)
                    return object;
                return new $root.google.protobuf.Empty();
            };

            /**
             * Creates a plain object from an Empty message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.Empty
             * @static
             * @param {google.protobuf.Empty} message Empty
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Empty.toObject = function toObject() {
                return {};
            };

            /**
             * Converts this Empty to JSON.
             * @function toJSON
             * @memberof google.protobuf.Empty
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Empty.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Empty
             * @function getTypeUrl
             * @memberof google.protobuf.Empty
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Empty.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/google.protobuf.Empty";
            };

            return Empty;
        })();

        protobuf.Timestamp = (function() {

            /**
             * Properties of a Timestamp.
             * @memberof google.protobuf
             * @interface ITimestamp
             * @property {Long|null} [seconds] Timestamp seconds
             * @property {number|null} [nanos] Timestamp nanos
             */

            /**
             * Constructs a new Timestamp.
             * @memberof google.protobuf
             * @classdesc Represents a Timestamp.
             * @implements ITimestamp
             * @constructor
             * @param {google.protobuf.ITimestamp=} [properties] Properties to set
             */
            function Timestamp(properties) {
                if (properties)
                    for (let keys = Object.keys(properties), i = 0; i < keys.length; ++i)
                        if (properties[keys[i]] != null && keys[i] !== "__proto__")
                            this[keys[i]] = properties[keys[i]];
            }

            /**
             * Timestamp seconds.
             * @member {Long} seconds
             * @memberof google.protobuf.Timestamp
             * @instance
             */
            Timestamp.prototype.seconds = $util.Long ? $util.Long.fromBits(0,0,false) : 0;

            /**
             * Timestamp nanos.
             * @member {number} nanos
             * @memberof google.protobuf.Timestamp
             * @instance
             */
            Timestamp.prototype.nanos = 0;

            /**
             * Creates a new Timestamp instance using the specified properties.
             * @function create
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {google.protobuf.ITimestamp=} [properties] Properties to set
             * @returns {google.protobuf.Timestamp} Timestamp instance
             */
            Timestamp.create = function create(properties) {
                return new Timestamp(properties);
            };

            /**
             * Encodes the specified Timestamp message. Does not implicitly {@link google.protobuf.Timestamp.verify|verify} messages.
             * @function encode
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {google.protobuf.ITimestamp} message Timestamp message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Timestamp.encode = function encode(message, writer, q) {
                if (!writer)
                    writer = $Writer.create();
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                if (message.seconds != null && Object.hasOwnProperty.call(message, "seconds"))
                    writer.uint32(/* id 1, wireType 0 =*/8).int64(message.seconds);
                if (message.nanos != null && Object.hasOwnProperty.call(message, "nanos"))
                    writer.uint32(/* id 2, wireType 0 =*/16).int32(message.nanos);
                return writer;
            };

            /**
             * Encodes the specified Timestamp message, length delimited. Does not implicitly {@link google.protobuf.Timestamp.verify|verify} messages.
             * @function encodeDelimited
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {google.protobuf.ITimestamp} message Timestamp message or plain object to encode
             * @param {$protobuf.Writer} [writer] Writer to encode to
             * @returns {$protobuf.Writer} Writer
             */
            Timestamp.encodeDelimited = function encodeDelimited(message, writer) {
                return this.encode(message, writer && writer.len ? writer.fork() : writer).ldelim();
            };

            /**
             * Decodes a Timestamp message from the specified reader or buffer.
             * @function decode
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @param {number} [length] Message length if known beforehand
             * @returns {google.protobuf.Timestamp} Timestamp
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Timestamp.decode = function decode(reader, length, error, long) {
                if (!(reader instanceof $Reader))
                    reader = $Reader.create(reader);
                if (long === undefined)
                    long = 0;
                if (long > $Reader.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let end = length === undefined ? reader.len : reader.pos + length, message = new $root.google.protobuf.Timestamp();
                while (reader.pos < end) {
                    let tag = reader.uint32();
                    if (tag === error)
                        break;
                    switch (tag >>> 3) {
                    case 1: {
                            message.seconds = reader.int64();
                            break;
                        }
                    case 2: {
                            message.nanos = reader.int32();
                            break;
                        }
                    default:
                        reader.skipType(tag & 7, long);
                        break;
                    }
                }
                return message;
            };

            /**
             * Decodes a Timestamp message from the specified reader or buffer, length delimited.
             * @function decodeDelimited
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
             * @returns {google.protobuf.Timestamp} Timestamp
             * @throws {Error} If the payload is not a reader or valid buffer
             * @throws {$protobuf.util.ProtocolError} If required fields are missing
             */
            Timestamp.decodeDelimited = function decodeDelimited(reader) {
                if (!(reader instanceof $Reader))
                    reader = new $Reader(reader);
                return this.decode(reader, reader.uint32());
            };

            /**
             * Verifies a Timestamp message.
             * @function verify
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {Object.<string,*>} message Plain object to verify
             * @returns {string|null} `null` if valid, otherwise the reason why it is not
             */
            Timestamp.verify = function verify(message, long) {
                if (typeof message !== "object" || message === null)
                    return "object expected";
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    return "maximum nesting depth exceeded";
                if (message.seconds != null && Object.hasOwnProperty.call(message, "seconds"))
                    if (!$util.isInteger(message.seconds) && !(message.seconds && $util.isInteger(message.seconds.low) && $util.isInteger(message.seconds.high)))
                        return "seconds: integer|Long expected";
                if (message.nanos != null && Object.hasOwnProperty.call(message, "nanos"))
                    if (!$util.isInteger(message.nanos))
                        return "nanos: integer expected";
                return null;
            };

            /**
             * Creates a Timestamp message from a plain object. Also converts values to their respective internal types.
             * @function fromObject
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {Object.<string,*>} object Plain object
             * @returns {google.protobuf.Timestamp} Timestamp
             */
            Timestamp.fromObject = function fromObject(object, long) {
                if (object instanceof $root.google.protobuf.Timestamp)
                    return object;
                if (!$util.isObject(object))
                    throw TypeError(".google.protobuf.Timestamp: object expected");
                if (long === undefined)
                    long = 0;
                if (long > $util.recursionLimit)
                    throw Error("maximum nesting depth exceeded");
                let message = new $root.google.protobuf.Timestamp();
                if (object.seconds != null)
                    if ($util.Long)
                        message.seconds = $util.Long.fromValue(object.seconds, false);
                    else if (typeof object.seconds === "string")
                        message.seconds = parseInt(object.seconds, 10);
                    else if (typeof object.seconds === "number")
                        message.seconds = object.seconds;
                    else if (typeof object.seconds === "object")
                        message.seconds = new $util.LongBits(object.seconds.low >>> 0, object.seconds.high >>> 0).toNumber();
                if (object.nanos != null)
                    message.nanos = object.nanos | 0;
                return message;
            };

            /**
             * Creates a plain object from a Timestamp message. Also converts values to other types if specified.
             * @function toObject
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {google.protobuf.Timestamp} message Timestamp
             * @param {$protobuf.IConversionOptions} [options] Conversion options
             * @returns {Object.<string,*>} Plain object
             */
            Timestamp.toObject = function toObject(message, options, q) {
                if (!options)
                    options = {};
                if (q === undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw Error("max depth exceeded");
                let object = {};
                if (options.defaults) {
                    if ($util.Long) {
                        let long = new $util.Long(0, 0, false);
                        object.seconds = options.longs === String ? long.toString() : options.longs === Number ? long.toNumber() : typeof BigInt !== "undefined" && options.longs === BigInt ? long.toBigInt() : long;
                    } else
                        object.seconds = options.longs === String ? "0" : typeof BigInt !== "undefined" && options.longs === BigInt ? BigInt("0") : 0;
                    object.nanos = 0;
                }
                if (message.seconds != null && Object.hasOwnProperty.call(message, "seconds"))
                    if (typeof BigInt !== "undefined" && options.longs === BigInt)
                        object.seconds = typeof message.seconds === "number" ? BigInt(message.seconds) : $util.Long.fromBits(message.seconds.low >>> 0, message.seconds.high >>> 0, false).toBigInt();
                    else if (typeof message.seconds === "number")
                        object.seconds = options.longs === String ? String(message.seconds) : message.seconds;
                    else
                        object.seconds = options.longs === String ? $util.Long.prototype.toString.call(message.seconds) : options.longs === Number ? new $util.LongBits(message.seconds.low >>> 0, message.seconds.high >>> 0).toNumber() : message.seconds;
                if (message.nanos != null && Object.hasOwnProperty.call(message, "nanos"))
                    object.nanos = message.nanos;
                return object;
            };

            /**
             * Converts this Timestamp to JSON.
             * @function toJSON
             * @memberof google.protobuf.Timestamp
             * @instance
             * @returns {Object.<string,*>} JSON object
             */
            Timestamp.prototype.toJSON = function toJSON() {
                return this.constructor.toObject(this, $protobuf__namespace.util.toJSONOptions);
            };

            /**
             * Gets the default type url for Timestamp
             * @function getTypeUrl
             * @memberof google.protobuf.Timestamp
             * @static
             * @param {string} [typeUrlPrefix] your custom typeUrlPrefix(default "type.googleapis.com")
             * @returns {string} The default type url
             */
            Timestamp.getTypeUrl = function getTypeUrl(typeUrlPrefix) {
                if (typeUrlPrefix === undefined) {
                    typeUrlPrefix = "type.googleapis.com";
                }
                return typeUrlPrefix + "/google.protobuf.Timestamp";
            };

            return Timestamp;
        })();

        return protobuf;
    })();

    return google;
})();

const CONFIGS_PROTO_DOCUMENTATION_URL = "https://dataform-co.github.io/dataform/docs/configs-reference";
const REPORT_ISSUE_URL = "https://github.com/dataform-co/dataform/issues";
var VerifyProtoErrorBehaviour;
(function (VerifyProtoErrorBehaviour) {
    VerifyProtoErrorBehaviour[VerifyProtoErrorBehaviour["DEFAULT"] = 0] = "DEFAULT";
    VerifyProtoErrorBehaviour[VerifyProtoErrorBehaviour["SUGGEST_REPORTING_TO_DATAFORM_TEAM"] = 1] = "SUGGEST_REPORTING_TO_DATAFORM_TEAM";
    VerifyProtoErrorBehaviour[VerifyProtoErrorBehaviour["SHOW_DOCS_LINK"] = 2] = "SHOW_DOCS_LINK";
})(VerifyProtoErrorBehaviour || (VerifyProtoErrorBehaviour = {}));
const Struct = google.protobuf.Struct;
const originalVerify = Struct.verify;
Struct.verify = function (object) {
    if (object && typeof object === "object" && !("fields" in object)) {
        const fields = {};
        for (const [k, v] of Object.entries(object)) {
            fields[k] = unknownToValueShallow(v);
        }
        Object.keys(object).forEach(key => delete object[key]);
        object.fields = fields;
    }
    return originalVerify.call(this, object);
};
function verifyObjectMatchesProto(protoType, object, errorBehaviour = VerifyProtoErrorBehaviour.DEFAULT) {
    if (Array.isArray(object)) {
        throw ReferenceError(`Expected a top-level object, but found an array`);
    }
    protoType.verify(object);
    const proto = protoType.create(object);
    const protoCastObject = protoType.toObject(proto);
    function checkFields(present, desired) {
        Object.entries(present).forEach(([presentKey, presentValue]) => {
            const desiredValue = desired[presentKey];
            if (typeof desiredValue !== typeof presentValue) {
                if (Array.isArray(presentValue) && presentValue.length === 0) {
                    return;
                }
                if (!presentValue) {
                    throw ReferenceError(`Unexpected empty value for "${presentKey}".` +
                        maybeGetDocsLinkPrefix(errorBehaviour, protoType));
                }
                if (typeof presentValue === "object" && Object.keys(presentValue).length === 0) {
                    return;
                }
                if (errorBehaviour === VerifyProtoErrorBehaviour.SUGGEST_REPORTING_TO_DATAFORM_TEAM) {
                    throw ReferenceError(`Unexpected property "${presentKey}" for "${protoType
                        .getTypeUrl("")
                        .replace("/", "")}", please report this to the Dataform team at ` +
                        `${REPORT_ISSUE_URL}.`);
                }
                throw ReferenceError(`Unexpected property "${presentKey}", or property value type of ` +
                    `"${typeof presentValue}" is incorrect.` +
                    maybeGetDocsLinkPrefix(errorBehaviour, protoType));
            }
            if (typeof presentValue === "object") {
                checkFields(presentValue, desiredValue);
            }
        });
    }
    checkFields(object, protoCastObject);
    return proto;
}
function maybeGetDocsLinkPrefix(errorBehaviour, protoType) {
    return errorBehaviour === VerifyProtoErrorBehaviour.SHOW_DOCS_LINK
        ? ` See ${CONFIGS_PROTO_DOCUMENTATION_URL}#${protoType
            .getTypeUrl("")
            .replace(/\./g, "-")
            .replace(/\//, "")} for allowed properties.`
        : "";
}
function encode64(protoType, value = {}) {
    return toBase64(protoType.encode(protoType.create(value)).finish());
}
function decode64(protoType, encodedValue) {
    if (!encodedValue) {
        return protoType.create();
    }
    return protoType.decode(fromBase64(encodedValue));
}
function equals(protoType, valueA, valueB) {
    return encode64(protoType, valueA) === encode64(protoType, valueB);
}
function deepClone(protoType, value) {
    return protoType.fromObject(protoType.toObject(protoType.create(value)));
}
function toBase64(value) {
    return protobufjs.util.base64.encode(value, 0, value.length);
}
function fromBase64(value) {
    const buf = new Uint8Array(protobufjs.util.base64.length(value));
    protobufjs.util.base64.decode(value, buf, 0);
    return buf;
}
function unknownToValueShallow(raw) {
    if (raw === null || typeof raw === "undefined") {
        return { nullValue: 0 };
    }
    if (typeof raw === "string") {
        return { stringValue: raw };
    }
    if (typeof raw === "number") {
        return { numberValue: raw };
    }
    if (typeof raw === "boolean") {
        return { boolValue: raw };
    }
    if (Array.isArray(raw)) {
        return { listValue: { values: raw.map(unknownToValueShallow) } };
    }
    if (typeof raw === "object") {
        return { structValue: raw };
    }
    throw new Error(`Unsupported value: ${raw}`);
}

(() => {
    if (typeof process !== "undefined") {
        return process.platform === "win32" ? "\\" : "/";
    }
    return "/";
})();

typeof __webpack_require__ === "function" ? __non_webpack_require__ : require;
function matchPatterns(patterns, values) {
    const fullyQualifiedActions = [];
    patterns.forEach(pattern => {
        if (pattern.includes(".")) {
            if (values.includes(pattern)) {
                fullyQualifiedActions.push(pattern);
            }
        }
        else {
            const matchingActions = values.filter(value => pattern === value.split(".").slice(-1)[0]);
            if (matchingActions.length === 0) {
                return;
            }
            if (matchingActions.length > 1) {
                throw new Error(ambiguousActionNameMsg(pattern, matchingActions));
            }
            fullyQualifiedActions.push(matchingActions[0]);
        }
    });
    return fullyQualifiedActions;
}
function graphHasErrors(graph) {
    var _a;
    return ((_a = graph.graphErrors) === null || _a === void 0 ? void 0 : _a.compilationErrors.length) > 0;
}
function stringifyResolvable(res) {
    return typeof res === "string" ? res : JSON.stringify(res);
}
function ambiguousActionNameMsg(act, allActs) {
    const allActNames = typeof allActs[0] === "string"
        ? allActs
        : allActs.map(r => `${r.getTarget().schema}.${r.getTarget().name}`);
    return `Ambiguous Action name: ${stringifyResolvable(act)}. Did you mean one of: ${allActNames.join(", ")}.`;
}
function validateConnectionFormat(connection) {
    const dotPattern = /^[a-zA-Z0-9_-]+\.[a-zA-Z0-9_-]+\.[a-zA-Z0-9_-]+$/;
    const resourcePattern = /^projects\/[^/]+\/locations\/[^/]+\/connections\/[^/]+$/;
    const isValidFormat = dotPattern.test(connection) || resourcePattern.test(connection);
    if (connection !== 'DEFAULT' && !isValidFormat) {
        throw new Error('The connection must be in the format `{project}.{location}.{connection_id}` or `projects/{project}/locations/{location}/connections/{connection_id}`, or be set to `DEFAULT`.');
    }
}
function tableTypeStringToEnum(type, throwIfUnknown) {
    switch (type) {
        case "table":
            return dataform.TableType.TABLE;
        case "incremental":
            return dataform.TableType.INCREMENTAL;
        case "view":
            return dataform.TableType.VIEW;
        default: {
            if (throwIfUnknown) {
                throw new Error(`Unexpected table type: ${type}`);
            }
            return dataform.TableType.UNKNOWN_TYPE;
        }
    }
}
function tableTypeEnumToString(enumType) {
    return dataform.TableType[enumType].toLowerCase();
}
function setOrValidateTableEnumType(table) {
    let enumTypeFromStr = null;
    if (table.type !== "" && table.type !== undefined) {
        enumTypeFromStr = tableTypeStringToEnum(table.type, true);
    }
    if (table.enumType === dataform.TableType.UNKNOWN_TYPE || table.enumType === undefined) {
        table.enumType = enumTypeFromStr;
    }
    else if (enumTypeFromStr !== null && table.enumType !== enumTypeFromStr) {
        throw new Error(`Table str type "${table.type}" and enumType "${tableTypeEnumToString(table.enumType)}" are not equivalent.`);
    }
}

function prune(compiledGraph, runConfig) {
    var _a;
    compiledGraph.tables.forEach(setOrValidateTableEnumType);
    const includedActionNames = computeIncludedActionNames(compiledGraph, runConfig);
    return Object.assign(Object.assign({}, compiledGraph), { tables: compiledGraph.tables.filter(action => includedActionNames.has(targetAsReadableString(action.target))), assertions: compiledGraph.assertions.filter(action => includedActionNames.has(targetAsReadableString(action.target))), operations: compiledGraph.operations.filter(action => includedActionNames.has(targetAsReadableString(action.target))), targets: (_a = compiledGraph.targets) === null || _a === void 0 ? void 0 : _a.filter(target => includedActionNames.has(targetAsReadableString(target))) });
}
function computeIncludedActionNames(compiledGraph, runConfig) {
    var _a, _b, _c;
    const allActions = [].concat(compiledGraph.tables, compiledGraph.operations, compiledGraph.assertions);
    const allActionNames = new Set(allActions.map(action => targetAsReadableString(action.target)));
    const allActionsByName = new Map(allActions.map(action => [targetAsReadableString(action.target), action]));
    const hasActionSelector = ((_a = runConfig.actions) === null || _a === void 0 ? void 0 : _a.length) > 0;
    const hasTagSelector = ((_b = runConfig.tags) === null || _b === void 0 ? void 0 : _b.length) > 0;
    if (!hasActionSelector && !hasTagSelector) {
        return allActionNames;
    }
    const includedActionNames = new Set();
    if (hasActionSelector) {
        matchPatterns(runConfig.actions, [...allActionNames])
            .forEach(actionName => includedActionNames.add(actionName));
    }
    if (hasTagSelector) {
        allActions
            .filter(action => action.tags.some(tag => runConfig.tags.includes(tag)))
            .forEach(action => includedActionNames.add(targetAsReadableString(action.target)));
    }
    if (runConfig.includeDependencies) {
        const queue = [...includedActionNames];
        while (queue.length > 0) {
            const actionName = queue.pop();
            const action = allActionsByName.get(actionName);
            const matchingDependencyNames = ((_c = action.dependencyTargets) === null || _c === void 0 ? void 0 : _c.length) > 0
                ? matchPatterns(action.dependencyTargets.map(dependency => targetAsReadableString(dependency)), [...allActionNames])
                : [];
            matchingDependencyNames.forEach(dependencyName => {
                if (!includedActionNames.has(dependencyName)) {
                    queue.push(dependencyName);
                    includedActionNames.add(dependencyName);
                }
            });
        }
    }
    if (runConfig.includeDependents) {
        const queue = [...includedActionNames];
        while (queue.length > 0) {
            const actionName = queue.pop();
            const matchingDependentNames = allActions
                .filter(compileAction => {
                var _a;
                return matchPatterns([actionName], ((_a = compileAction.dependencyTargets) === null || _a === void 0 ? void 0 : _a.map(dependency => targetAsReadableString(dependency))) || []).length >= 1;
            })
                .map(compileAction => targetAsReadableString(compileAction.target));
            matchingDependentNames.forEach(dependentName => {
                if (!includedActionNames.has(dependentName)) {
                    queue.push(dependentName);
                    includedActionNames.add(dependentName);
                }
            });
        }
    }
    return includedActionNames;
}

async function state(dbadapter, targets) {
    const allTables = await Promise.all(targets.map(async (target) => dbadapter.table(target)));
    const tablesWithValues = allTables.filter(table => {
        return !!table && !!table.type;
    });
    return { tables: tablesWithValues };
}

function formatStatement(statement, isLast, modifier) {
    const lastLineStart = statement.lastIndexOf("\n") + 1;
    const commentIndex = statement.indexOf("--", lastLineStart);
    let code;
    let comment;
    if (commentIndex === 0) {
        code = "";
        comment = statement;
    }
    else if (commentIndex > 0) {
        code = statement.substring(0, commentIndex).trimEnd();
        comment = statement.substring(commentIndex);
    }
    else {
        code = statement;
        comment = "";
    }
    if (modifier && code.length > 0) {
        code = modifier(code);
    }
    if (!isLast && code.length > 0 && !code.endsWith(";")) {
        code = code + ";";
    }
    if (code.length > 0 && comment.length > 0) {
        return code + " " + comment;
    }
    return code || comment;
}
function concatenateQueries(statements, modifier) {
    const processed = statements
        .filter(statement => !!statement)
        .map(statement => statement.trim())
        .filter(statement => statement.length > 0);
    const formattedStatements = processed.map((statement, index) => formatStatement(statement, index === processed.length - 1, modifier));
    return formattedStatements.join("\n");
}
class Tasks {
    constructor() {
        this.tasks = [];
    }
    add(task) {
        this.tasks.push(task);
        return this;
    }
    addAll(tasks) {
        this.tasks = this.tasks.concat(tasks.tasks);
        return this;
    }
    build() {
        return this.tasks.map(task => task.build());
    }
    concatenate() {
        return new Tasks().add(Task.statement(concatenateQueries(this.tasks.map(task => task.getStatement()))));
    }
}
class Task {
    constructor() {
        this.proto = dataform.ExecutionTask.create();
    }
    static statement(statement) {
        return new Task().type("statement").statement(statement);
    }
    static assertion(statement) {
        return new Task().type("assertion").statement(statement);
    }
    type(v) {
        this.proto.type = v;
        return this;
    }
    statement(v) {
        this.proto.statement = v;
        return this;
    }
    getStatement() {
        return this.proto.statement;
    }
    build() {
        return dataform.ExecutionTask.create(this.proto);
    }
}

class ErrorWithCause extends Error {
    constructor(message, cause) {
        super(typeof message === "string" ? message : undefined);
        this.cause = cause;
        if (typeof message !== "string") {
            this.cause = message;
        }
        if (this.cause) {
            this.stack += `\nCaused by: ${this.cause.stack}`;
        }
    }
}
function coerceAsError(errorLike) {
    if (errorLike instanceof Error) {
        return errorLike;
    }
    const error = errorLike;
    const message = error.message || error.error || (typeof errorLike === "string" ? errorLike : JSON.stringify(errorLike));
    const coercedError = new Error(String(message));
    if (error.stack) {
        coercedError.stack = error.stack;
    }
    if (error.name) {
        coercedError.name = error.name;
    }
    return coercedError;
}

class CompilationSql {
    constructor(project, dataformCoreVersion) {
        this.project = project;
        this.dataformCoreVersion = dataformCoreVersion;
    }
    resolveTarget(target) {
        const database = target.database || this.project.defaultDatabase;
        if (!database) {
            return `\`${target.schema || this.project.defaultSchema}.${target.name}\``;
        }
        return `\`${database}.${target.schema || this.project.defaultSchema}.${target.name}\``;
    }
    sqlString(stringContents) {
        return `'${stringContents.replace(/\\/g, "\\\\").replace(/'/g, "\\'")}'`;
    }
    indexAssertion(dataset, indexCols) {
        const commaSeparatedColumns = indexCols.join(", ");
        return `
SELECT
  *
FROM (
  SELECT
    ${commaSeparatedColumns},
    COUNT(1) AS index_row_count
  FROM ${dataset}
  GROUP BY ${commaSeparatedColumns}
  ) AS data
WHERE index_row_count > 1
`;
    }
    rowConditionsAssertion(dataset, rowConditions) {
        return rowConditions
            .map((rowCondition) => `
SELECT
  ${this.sqlString(rowCondition)} AS failing_row_condition,
  *
FROM ${dataset}
WHERE NOT (${rowCondition})
`)
            .join(`UNION ALL`);
    }
}

class ExecutionSql {
    constructor(project, dataformCoreVersion, uniqueIdGenerator = () => Math.random().toString(36).substring(2)) {
        this.project = project;
        this.dataformCoreVersion = dataformCoreVersion;
        this.uniqueIdGenerator = uniqueIdGenerator;
        this.CompilationSql = new CompilationSql(project, dataformCoreVersion);
    }
    baseTableType(enumType) {
        switch (enumType) {
            case dataform.TableType.TABLE:
            case dataform.TableType.INCREMENTAL:
                return dataform.TableMetadata.Type.TABLE;
            case dataform.TableType.VIEW:
                return dataform.TableMetadata.Type.VIEW;
            default:
                throw new Error(`Unexpected table type: ${tableTypeEnumToString(enumType)}`);
        }
    }
    tableTypeAsSql(type) {
        switch (type) {
            case dataform.TableMetadata.Type.TABLE:
                return "table";
            case dataform.TableMetadata.Type.VIEW:
                return "view";
            default:
                throw new Error(`Unexpected table type: ${type}`);
        }
    }
    insertInto(target, columns, query) {
        return `	
insert into ${this.resolveTarget(target)}	
(${columns.join(",")})	
select ${columns.join(",")}	
from (${query}) as insertions`;
    }
    oppositeTableType(type) {
        switch (type) {
            case dataform.TableMetadata.Type.TABLE:
                return dataform.TableMetadata.Type.VIEW;
            case dataform.TableMetadata.Type.VIEW:
                return dataform.TableMetadata.Type.TABLE;
            default:
                throw new Error(`Unexpected table type: ${type}`);
        }
    }
    where(query, where) {
        return where
            ? `
  select * from (${query}) as subquery
    where ${where}`
            : query;
    }
    shouldWriteIncrementally(table, runConfig, tableMetadata) {
        return ((!runConfig.fullRefresh || table.protected) &&
            tableMetadata &&
            tableMetadata.type !== dataform.TableMetadata.Type.VIEW);
    }
    preOps(table, runConfig, tableMetadata) {
        let preOps = table.preOps;
        if (semver__namespace.gt(this.dataformCoreVersion, "1.4.8") &&
            table.enumType === dataform.TableType.INCREMENTAL &&
            this.shouldWriteIncrementally(table, runConfig, tableMetadata)) {
            preOps = table.incrementalPreOps;
        }
        return (preOps || []).map(pre => Task.statement(pre));
    }
    postOps(table, runConfig, tableMetadata) {
        let postOps = table.postOps;
        if (semver__namespace.gt(this.dataformCoreVersion, "1.4.8") &&
            table.enumType === dataform.TableType.INCREMENTAL &&
            this.shouldWriteIncrementally(table, runConfig, tableMetadata)) {
            postOps = table.incrementalPostOps;
        }
        return (postOps || []).map(post => Task.statement(post));
    }
    resolveTarget(target) {
        return this.CompilationSql.resolveTarget(target);
    }
    getIncrementalQuery(table) {
        return this.where(table.incrementalQuery || table.query, table.where);
    }
    publishTasks(table, runConfig, tableMetadata) {
        var _a;
        const tasks = new Tasks();
        this.preOps(table, runConfig, tableMetadata).forEach(statement => tasks.add(statement));
        const baseTableType = this.baseTableType(table.enumType);
        if (tableMetadata && tableMetadata.type !== baseTableType) {
            tasks.add(Task.statement(this.dropIfExists(table.target, this.oppositeTableType(baseTableType))));
        }
        if (table.enumType === dataform.TableType.INCREMENTAL) {
            if (!this.shouldWriteIncrementally(table, runConfig, tableMetadata)) {
                tasks.add(Task.statement(this.createOrReplace(table)));
            }
            else {
                const onSchemaChange = (_a = table.onSchemaChange) !== null && _a !== void 0 ? _a : dataform.OnSchemaChange.IGNORE;
                switch (onSchemaChange) {
                    case dataform.OnSchemaChange.FAIL:
                    case dataform.OnSchemaChange.EXTEND:
                    case dataform.OnSchemaChange.SYNCHRONIZE:
                        this.buildIncrementalSchemaChangeTasks(tasks, table);
                    case dataform.OnSchemaChange.IGNORE:
                        const columns = (tableMetadata === null || tableMetadata === void 0 ? void 0 : tableMetadata.fields.map(f => f.name)) || [];
                        tasks.add(Task.statement(this.getIncrementalDmlStatement(table, columns)));
                        break;
                }
            }
        }
        else {
            tasks.add(Task.statement(this.createOrReplace(table)));
        }
        this.postOps(table, runConfig, tableMetadata).forEach(statement => tasks.add(statement));
        return tasks.concatenate();
    }
    createTableTasks(table, runConfig, tableMetadata) {
        return table.disabled ? [] : this.publishTasks(table, runConfig, tableMetadata).build();
    }
    createOperationTasks(operation) {
        return operation.disabled
            ? []
            : operation.queries.map(statement => dataform.ExecutionTask.create({ type: "statement", statement }));
    }
    createAssertionTasks(assertion) {
        return assertion.disabled ? [] : this.assertTasks(assertion, this.project).build();
    }
    assertTasks(assertion, projectConfig) {
        const tasks = new Tasks();
        const target = assertion.target;
        tasks.add(Task.statement(this.createOrReplaceView(target, assertion.query)));
        tasks.add(Task.assertion(`select sum(1) as row_count from ${this.resolveTarget(target)}`));
        return tasks;
    }
    dropIfExists(target, type) {
        return `drop ${this.tableTypeAsSql(type)} if exists ${this.resolveTarget(target)}`;
    }
    buildIncrementalPredicatesString(incrementalPredicates) {
        const validPredicates = incrementalPredicates ? incrementalPredicates.filter(p => p.trim() !== "") : [];
        if (validPredicates.length === 0) {
            return "";
        }
        return `and ${validPredicates.map(p => `(${p})`).join(" and ")}`;
    }
    buildIncrementalSchemaChangeTasks(tasks, table) {
        const uniqueId = this.uniqueIdGenerator();
        const emptyTempTableTarget = Object.assign(Object.assign({}, table.target), { name: `${table.target.name}_df_temp_${uniqueId}_empty` });
        const procedureName = this.createProcedureName(table.target, uniqueId);
        const procedureBody = this.incrementalSchemaChangeBody(table, this.resolveTarget(table.target), emptyTempTableTarget);
        const createProcedureSql = `CREATE OR REPLACE PROCEDURE ${procedureName}()
OPTIONS(strict_mode=false)
BEGIN
${procedureBody}
END;`;
        const callProcedureSql = this.safeCallAndDropProcedure(procedureName, this.resolveTarget(emptyTempTableTarget));
        tasks.add(Task.statement(createProcedureSql));
        tasks.add(Task.statement(callProcedureSql));
    }
    createProcedureName(target, uniqueId) {
        return this.resolveTarget(Object.assign(Object.assign({}, target), { name: `df_osc_${uniqueId}` }));
    }
    safeCallAndDropProcedure(procedureName, emptyTempTableName) {
        return `
BEGIN
  CALL ${procedureName}();
EXCEPTION WHEN ERROR THEN
  DROP TABLE IF EXISTS ${emptyTempTableName};
  DROP PROCEDURE IF EXISTS ${procedureName};
  RAISE;
END;
DROP PROCEDURE IF EXISTS ${procedureName};`;
    }
    createEmptyTempTableSql(emptyTempTableName, query) {
        return `
-- Create empty table to extract schema of new query.
CREATE OR REPLACE TABLE ${emptyTempTableName} AS (
  SELECT * FROM (${query}) AS insertions LIMIT 0
);`;
    }
    compareSchemasSql(target, emptyTempTableTarget) {
        return `
-- Compare schemas
DECLARE dataform_columns ARRAY<STRING>;
DECLARE temp_table_columns ARRAY<STRUCT<column_name STRING, data_type STRING>>;
DECLARE columns_added ARRAY<STRUCT<column_name STRING, data_type STRING>>;
DECLARE columns_removed ARRAY<STRING>;

SET dataform_columns = (
  SELECT IFNULL(ARRAY_AGG(DISTINCT column_name), [])
  FROM \`${target.database}.${target.schema}.INFORMATION_SCHEMA.COLUMNS\`
  WHERE table_name = '${target.name}'
);

SET temp_table_columns = (
  SELECT IFNULL(ARRAY_AGG(STRUCT(column_name, data_type)), [])
  FROM \`${emptyTempTableTarget.database}.${emptyTempTableTarget.schema}.INFORMATION_SCHEMA.COLUMNS\`
  WHERE table_name = '${emptyTempTableTarget.name}'
);

SET columns_added = (
  SELECT IFNULL(ARRAY_AGG(column_info), [])
  FROM UNNEST(temp_table_columns) AS column_info
  WHERE column_info.column_name NOT IN UNNEST(dataform_columns)
);
SET columns_removed = (
  SELECT IFNULL(ARRAY_AGG(column_name), [])
  FROM UNNEST(dataform_columns) AS column_name
  WHERE column_name NOT IN (SELECT col.column_name FROM UNNEST(temp_table_columns) AS col)
);`;
    }
    applySchemaChangeStrategySql(table, qualifiedTargetTableName) {
        const onSchemaChange = table.onSchemaChange || dataform.OnSchemaChange.IGNORE;
        let sql = `
-- Apply schema change strategy (${dataform.OnSchemaChange[onSchemaChange]}).`;
        switch (onSchemaChange) {
            case dataform.OnSchemaChange.FAIL:
                sql += `
IF ARRAY_LENGTH(columns_added) > 0 OR ARRAY_LENGTH(columns_removed) > 0 THEN
  RAISE USING MESSAGE = FORMAT(
    "Schema mismatch defined by on_schema_change = 'FAIL'. Added columns: %T, removed columns: %T",
    columns_added,
    columns_removed
  );
END IF;
`;
                break;
            case dataform.OnSchemaChange.EXTEND:
                sql += `
IF ARRAY_LENGTH(columns_removed) > 0 THEN
  RAISE USING MESSAGE = FORMAT(
    "Column removals are not allowed when on_schema_change = 'EXTEND'. Removed columns: %T",
    columns_removed
  );
END IF;

${this.alterTableAddColumnsSql(qualifiedTargetTableName)}
`;
                break;
            case dataform.OnSchemaChange.SYNCHRONIZE:
                const uniqueKeys = table.uniqueKey || [];
                sql += `
DECLARE invalid_removed_columns ARRAY<STRING>;
SET invalid_removed_columns = (
  SELECT IFNULL(ARRAY_AGG(col), []) FROM UNNEST(columns_removed) AS col WHERE col IN UNNEST(${JSON.stringify(uniqueKeys)})
);

IF ARRAY_LENGTH(invalid_removed_columns) > 0 THEN
  RAISE USING MESSAGE = FORMAT(
    "Cannot drop columns %T as they are part of the unique key for table ${qualifiedTargetTableName}",
    invalid_removed_columns
  );
END IF;

IF ARRAY_LENGTH(columns_removed) > 0 THEN
  EXECUTE IMMEDIATE (
    "ALTER TABLE ${qualifiedTargetTableName} " ||
    (
      SELECT STRING_AGG(FORMAT("DROP COLUMN IF EXISTS %s", col), ", ")
      FROM UNNEST(columns_removed) AS col
    )
  );
END IF;

${this.alterTableAddColumnsSql(qualifiedTargetTableName)}
`;
                break;
        }
        return sql;
    }
    alterTableAddColumnsSql(qualifiedTargetTableName) {
        return `IF ARRAY_LENGTH(columns_added) > 0 THEN
  EXECUTE IMMEDIATE (
    "ALTER TABLE ${qualifiedTargetTableName} " ||
    (
      SELECT STRING_AGG(FORMAT("ADD COLUMN IF NOT EXISTS %s %s", column_info.column_name, column_info.data_type), ", ")
      FROM UNNEST(columns_added) AS column_info
    )
  );
END IF;`;
    }
    cleanupSql(emptyTempTableName) {
        return `
-- Cleanup temporary tables.
DROP TABLE IF EXISTS ${emptyTempTableName};
    `;
    }
    incrementalSchemaChangeBody(table, qualifiedTargetTableName, emptyTempTableTarget) {
        const emptyTempTableName = this.resolveTarget(emptyTempTableTarget);
        const query = this.getIncrementalQuery(table);
        const statements = [
            this.createEmptyTempTableSql(emptyTempTableName, query),
            this.compareSchemasSql(table.target, emptyTempTableTarget),
            this.applySchemaChangeStrategySql(table, qualifiedTargetTableName),
            this.cleanupSql(emptyTempTableName)
        ];
        return statements.join("\n\n");
    }
    createOrReplace(table) {
        const options = [];
        if (table.bigquery && table.bigquery.partitionBy && table.bigquery.partitionExpirationDays) {
            options.push(`partition_expiration_days=${table.bigquery.partitionExpirationDays}`);
        }
        if (table.bigquery && table.bigquery.partitionBy && table.bigquery.requirePartitionFilter) {
            options.push(`require_partition_filter=${table.bigquery.requirePartitionFilter}`);
        }
        if (table.bigquery && table.bigquery.additionalOptions) {
            for (const [optionName, optionValue] of Object.entries(table.bigquery.additionalOptions)) {
                options.push(`${optionName}=${optionValue}`);
            }
        }
        return `create or replace ${table.materialized ? "materialized " : ""}${this.tableTypeAsSql(this.baseTableType(table.enumType))} ${this.resolveTarget(table.target)} ${table.bigquery && table.bigquery.partitionBy
            ? `partition by ${table.bigquery.partitionBy} `
            : ""}${table.bigquery && table.bigquery.clusterBy && table.bigquery.clusterBy.length > 0
            ? `cluster by ${table.bigquery.clusterBy.join(", ")} `
            : ""}${options.length > 0 ? `OPTIONS(${options.join(",")})` : ""}as ${table.query}`;
    }
    createOrReplaceView(target, query) {
        return `
      create or replace view ${this.resolveTarget(target)} as ${query}`;
    }
    mergeInto(target, columns, query, uniqueKey, bigquery) {
        const updatePartitionFilter = bigquery && bigquery.updatePartitionFilter;
        const incrementalPredicates = bigquery && bigquery.incrementalPredicates;
        const incrementalPredicatesString = this.buildIncrementalPredicatesString(incrementalPredicates);
        const backtickedColumns = columns.map(column => `\`${column}\``);
        return `
merge ${this.resolveTarget(target)} DATAFORM_DEST
using (${query}
) DATAFORM_SOURCE
on ${uniqueKey.map(uniqueKeyCol => `DATAFORM_DEST.${uniqueKeyCol} = DATAFORM_SOURCE.${uniqueKeyCol}`).join(` and `)} ${updatePartitionFilter ? `and DATAFORM_DEST.${updatePartitionFilter}` : ""}
${incrementalPredicatesString ? ` ${incrementalPredicatesString}` : ""}
when matched then
  update set ${columns.map(column => `\`${column}\` = DATAFORM_SOURCE.${column}`).join(",")}
when not matched then
  insert (${backtickedColumns.join(",")}) values (${backtickedColumns.join(",")})`;
    }
    insertOverwrite(target, columns, query, bigquery) {
        const partitionBy = bigquery && bigquery.partitionBy;
        const updatePartitionFilter = bigquery && bigquery.updatePartitionFilter;
        const incrementalPredicates = bigquery && bigquery.incrementalPredicates;
        const incrementalPredicatesString = this.buildIncrementalPredicatesString(incrementalPredicates);
        const uniqueId = this.uniqueIdGenerator();
        const stagingTableUnqualified = `staging_table_temp_${uniqueId}`;
        const backtickedColumns = columns.map(column => `\`${column}\``);
        const resolveTargetTable = this.resolveTarget(target);
        return `CREATE OR REPLACE TEMP TABLE \`${stagingTableUnqualified}\` AS (
  ${query}
);

BEGIN
  DECLARE partitions_for_replacement DEFAULT (
    ARRAY(
      SELECT DISTINCT ${partitionBy}
      FROM \`${stagingTableUnqualified}\`
      WHERE ${partitionBy} IS NOT NULL
    )
  );

  MERGE ${resolveTargetTable} DATAFORM_DEST
  USING \`${stagingTableUnqualified}\` DATAFORM_SOURCE
  ON FALSE
  WHEN NOT MATCHED BY SOURCE AND ${partitionBy} IN UNNEST(partitions_for_replacement) ${updatePartitionFilter ? `and DATAFORM_DEST.${updatePartitionFilter}` : ""}
  ${incrementalPredicatesString ? ` ${incrementalPredicatesString}` : ""}
  THEN
    DELETE
  WHEN NOT MATCHED BY TARGET THEN
    INSERT (${backtickedColumns.join(",")}) VALUES (${backtickedColumns.join(",")});
END;

DROP TABLE IF EXISTS \`${stagingTableUnqualified}\`;`;
    }
    getIncrementalDmlStatement(table, columns) {
        const incrementalQuery = this.getIncrementalQuery(table);
        switch (table.incrementalStrategy) {
            case dataform.IncrementalStrategy.INSERT_OVERWRITE:
                return this.insertOverwrite(table.target, columns, incrementalQuery, table.bigquery);
            case dataform.IncrementalStrategy.MERGE:
            default:
                if (table.uniqueKey && table.uniqueKey.length > 0) {
                    return this.mergeInto(table.target, columns, incrementalQuery, table.uniqueKey, table.bigquery);
                }
                return this.insertInto(table.target, columns.map(column => `\`${column}\``), incrementalQuery);
        }
    }
}
function collectEvaluationQueries(queryOrAction, concatenate, queryModifier = (q) => q) {
    const validationQueries = new Array();
    if (typeof queryOrAction === "string") {
        validationQueries.push({ query: queryModifier(queryOrAction) });
    }
    else {
        try {
            if (queryOrAction instanceof dataform.Table) {
                if (queryOrAction.enumType === dataform.TableType.INCREMENTAL) {
                    const incrementalTableQueries = queryOrAction.incrementalPreOps.concat(queryOrAction.incrementalQuery, queryOrAction.incrementalPostOps);
                    if (concatenate) {
                        validationQueries.push({
                            query: concatenateQueries(incrementalTableQueries, queryModifier),
                            incremental: true
                        });
                    }
                    else {
                        incrementalTableQueries.forEach(q => validationQueries.push({ query: queryModifier(q), incremental: true }));
                    }
                }
                const tableQueries = queryOrAction.preOps.concat(queryOrAction.query, queryOrAction.postOps);
                if (concatenate) {
                    validationQueries.push({
                        query: concatenateQueries(tableQueries, queryModifier)
                    });
                }
                else {
                    tableQueries.forEach(q => validationQueries.push({ query: queryModifier(q) }));
                }
            }
            else if (queryOrAction instanceof dataform.Operation) {
                if (concatenate) {
                    validationQueries.push({
                        query: concatenateQueries(queryOrAction.queries, queryModifier)
                    });
                }
                else {
                    queryOrAction.queries.forEach(q => validationQueries.push({ query: queryModifier(q) }));
                }
            }
            else if (queryOrAction instanceof dataform.Assertion) {
                validationQueries.push({ query: queryModifier(queryOrAction.query) });
            }
            else {
                throw new Error("Unrecognized evaluate type.");
            }
        }
        catch (e) {
            throw new ErrorWithCause(`Error building tasks for evaluation. ${e.message}`, e);
        }
    }
    return validationQueries
        .map(validationQuery => (Object.assign({ query: validationQuery.query.trim() }, validationQuery)))
        .filter(validationQuery => !!validationQuery.query);
}

async function build(compiledGraph, runConfig, dbadapter) {
    const prunedGraph = prune(compiledGraph, runConfig);
    const allInvolvedTargets = new Set(prunedGraph.tables.map(table => targetStringifier.stringify(table.target)));
    return new Builder(prunedGraph, runConfig, await state(dbadapter, Array.from(allInvolvedTargets).map(target => targetStringifier.parse(target)))).build();
}
class Builder {
    constructor(prunedGraph, runConfig, warehouseState) {
        this.prunedGraph = prunedGraph;
        this.runConfig = runConfig;
        this.warehouseState = warehouseState;
        this.executionSql = new ExecutionSql(prunedGraph.projectConfig, prunedGraph.dataformCoreVersion || "1.0.0");
        prunedGraph.tables.forEach(setOrValidateTableEnumType);
    }
    build() {
        if (graphHasErrors(this.prunedGraph)) {
            throw new Error(`Project has unresolved compilation or validation errors.`);
        }
        const tableMetadataByTarget = new Map();
        this.warehouseState.tables.forEach(tableState => {
            tableMetadataByTarget.set(targetStringifier.stringify(tableState.target), tableState);
        });
        const actions = [].concat(this.prunedGraph.tables.map(t => this.buildTable(t, tableMetadataByTarget.get(targetStringifier.stringify(t.target)), this.runConfig)), this.prunedGraph.operations.map(o => this.buildOperation(o)), this.prunedGraph.assertions.map(a => this.buildAssertion(a)));
        return dataform.ExecutionGraph.create({
            projectConfig: this.prunedGraph.projectConfig,
            runConfig: this.runConfig,
            warehouseState: this.warehouseState,
            declarationTargets: this.prunedGraph.declarations.map(declaration => declaration.target),
            actions
        });
    }
    buildTable(table, tableMetadata, runConfig) {
        return Object.assign(Object.assign({}, this.toPartialExecutionAction(table)), { type: "table", tableType: tableTypeEnumToString(table.enumType), tasks: this.executionSql.createTableTasks(table, runConfig, tableMetadata), hermeticity: table.hermeticity || dataform.ActionHermeticity.HERMETIC });
    }
    buildOperation(operation) {
        return Object.assign(Object.assign({}, this.toPartialExecutionAction(operation)), { type: "operation", tasks: this.executionSql.createOperationTasks(operation), hermeticity: operation.hermeticity || dataform.ActionHermeticity.NON_HERMETIC });
    }
    buildAssertion(assertion) {
        return Object.assign(Object.assign({}, this.toPartialExecutionAction(assertion)), { type: "assertion", tasks: this.executionSql.createAssertionTasks(assertion), hermeticity: assertion.hermeticity || dataform.ActionHermeticity.HERMETIC });
    }
    toPartialExecutionAction(action) {
        return dataform.ExecutionAction.create({
            target: action.target,
            fileName: action.fileName,
            dependencyTargets: action.dependencyTargets,
            actionDescriptor: action.actionDescriptor
        });
    }
}

class BaseWorker {
    constructor(loaderPath) {
        this.loaderPath = loaderPath;
    }
    async runWorker(timeoutMillis, onBoot, onMessage) {
        const forkScript = this.resolveScript();
        const child = childProcess.fork(forkScript, [], {
            stdio: [0, 1, 2, "ipc", "pipe"]
        });
        return new Promise((resolve, reject) => {
            let completed = false;
            let booted = false;
            const terminate = (fn) => {
                if (completed) {
                    return;
                }
                completed = true;
                clearTimeout(timeout);
                child.kill("SIGKILL");
                fn();
            };
            const timeout = setTimeout(() => {
                terminate(() => reject(new Error(`Compilation timed out after ${timeoutMillis / 1000} seconds. ` +
                    `To allow more time, re-run with a longer --timeout ` +
                    `(e.g. --timeout=2m, --timeout=1h).`)));
            }, timeoutMillis);
            child.on("message", (message) => {
                if (message.type === "worker_booted") {
                    if (!booted) {
                        booted = true;
                        onBoot(child);
                    }
                    return;
                }
                onMessage(message, child, (res) => terminate(() => resolve(res)), (err) => terminate(() => reject(err)));
            });
            child.on("error", err => {
                terminate(() => reject(err));
            });
            child.on("exit", (code, signal) => {
                if (!completed) {
                    const errorMsg = code !== 0 && code !== null
                        ? `Worker exited with code ${code} and signal ${signal}`
                        : "Worker exited without sending a response message";
                    terminate(() => reject(new Error(errorMsg)));
                }
            });
        });
    }
    resolveScript() {
        const pathsToTry = ["./worker_bundle.js", this.loaderPath];
        for (const p of pathsToTry) {
            try {
                return require.resolve(p);
            }
            catch (e) {
            }
        }
        throw new Error(`Could not resolve worker script. Tried: ${pathsToTry.join(", ")}`);
    }
}

function prettyJsonStringify(obj) {
    return JSON.stringify(obj, null, 4) + "\n";
}
function readDataformCoreVersionFromWorkflowSettings(resolvedProjectPath) {
    var _a;
    return (_a = readConfigFromWorkflowSettings(resolvedProjectPath)) === null || _a === void 0 ? void 0 : _a.dataformCoreVersion;
}
function readConfigFromWorkflowSettings(resolvedProjectPath) {
    const workflowSettingsPath = path__namespace.join(resolvedProjectPath, "workflow_settings.yaml");
    if (!fs__namespace.existsSync(workflowSettingsPath)) {
        return;
    }
    const workflowSettingsContent = fs__namespace.readFileSync(workflowSettingsPath, "utf-8");
    let workflowSettingsAsJson = {};
    try {
        workflowSettingsAsJson = jsYaml.load(workflowSettingsContent);
    }
    catch (e) {
        if (e instanceof jsYaml.YAMLException) {
            throw new Error(`${workflowSettingsPath} is not a valid YAML file: ${e}`);
        }
        throw e;
    }
    return dataform.WorkflowSettings.create(workflowSettingsAsJson);
}

const MISSING_CORE_VERSION_ERROR = "dataformCoreVersion must be specified either in workflow_settings.yaml or via a package.json";
async function install(projectPath) {
    const resolvedProjectPath = path__namespace.resolve(projectPath);
    const packageJsonPath = path__namespace.join(resolvedProjectPath, "package.json");
    const dataformCoreVersion = readDataformCoreVersionFromWorkflowSettings(resolvedProjectPath);
    if (dataformCoreVersion) {
        throw new Error("No installation is needed when using workflow_settings.yaml, as packages are installed at " +
            "runtime.");
    }
    if (!fs__namespace$1.existsSync(packageJsonPath)) {
        throw new Error(MISSING_CORE_VERSION_ERROR);
    }
    await util.promisify(childProcess__namespace.exec)("npm i --ignore-scripts", { cwd: resolvedProjectPath });
}

const DEFAULT_COMPILATION_TIMEOUT_MILLIS = 60000;

function print$1(text) {
    process.stderr.write(text);
}
async function compile(compileConfig = {}) {
    var _a;
    let compiledGraph = dataform.CompiledGraph.create();
    const resolvedProjectPath = path__namespace.resolve(compileConfig.projectDir);
    const packageJsonPath = path__namespace.join(resolvedProjectPath, "package.json");
    const packageLockJsonPath = path__namespace.join(resolvedProjectPath, "package-lock.json");
    const projectNodeModulesPath = path__namespace.join(resolvedProjectPath, "node_modules");
    const temporaryProjectPath = tmp__namespace.dirSync().name;
    const workflowSettings = readConfigFromWorkflowSettings(resolvedProjectPath);
    const workflowSettingsDataformCoreVersion = workflowSettings === null || workflowSettings === void 0 ? void 0 : workflowSettings.dataformCoreVersion;
    const workflowSettingsExtension = (_a = workflowSettings === null || workflowSettings === void 0 ? void 0 : workflowSettings.extension) !== null && _a !== void 0 ? _a : undefined;
    compileConfig.extension = workflowSettingsExtension;
    if (!workflowSettingsDataformCoreVersion && !fs__namespace.existsSync(packageJsonPath)) {
        throw new Error(MISSING_CORE_VERSION_ERROR);
    }
    if (workflowSettingsDataformCoreVersion) {
        [projectNodeModulesPath, packageJsonPath, packageLockJsonPath].forEach(npmPath => {
            if (fs__namespace.existsSync(npmPath)) {
                throw new Error(`'${npmPath}' unexpected; remove it and try again`);
            }
        });
        if (compileConfig.verbose) {
            print$1(`Using isolated environment for @dataform/core@${workflowSettingsDataformCoreVersion}\n`);
            print$1(`Copying project to temporary directory: ${temporaryProjectPath}\n`);
        }
        const copyStartTime = performance.now();
        fs__namespace.copySync(resolvedProjectPath, temporaryProjectPath);
        if (compileConfig.verbose) {
            print$1(`Project copy completed in ${performance.now() - copyStartTime}ms\n`);
        }
        if (compileConfig.verbose) {
            print$1(`Generating temporary package.json\n`);
        }
        fs__namespace.writeFileSync(path__namespace.join(temporaryProjectPath, "package.json"), `{
  "dependencies": {
  "@dataform/core": "${workflowSettingsDataformCoreVersion}"
  }
}`);
        const npmCommand = `npm i --ignore-scripts${compileConfig.verbose ? " --loglevel=http" : ""}`;
        if (compileConfig.verbose) {
            print$1(`Running '${npmCommand}' in temporary directory...\n`);
        }
        const npmStartTime = performance.now();
        const { stdout, stderr } = await util.promisify(childProcess.exec)(npmCommand, {
            cwd: temporaryProjectPath
        });
        if (compileConfig.verbose) {
            print$1(`NPM HTTP Logs:\n${stderr}\n`);
            print$1(`NPM install completed in ${performance.now() - npmStartTime}ms\n`);
        }
        compileConfig.projectDir = temporaryProjectPath;
    }
    const result = await new CompileChildProcess().compile(compileConfig);
    const decodedResult = decode64(dataform.CoreExecutionResponse, result);
    compiledGraph = dataform.CompiledGraph.create(decodedResult.compile.compiledGraph);
    if (workflowSettingsDataformCoreVersion) {
        fs__namespace.rmSync(temporaryProjectPath, { recursive: true });
    }
    return compiledGraph;
}
class CompileChildProcess extends BaseWorker {
    constructor() {
        super(path__namespace.resolve(__dirname, "../../vm/compile_loader"));
    }
    async compile(compileConfig) {
        const timeoutValue = compileConfig.timeoutMillis || DEFAULT_COMPILATION_TIMEOUT_MILLIS;
        return await this.runWorker(timeoutValue, child => child.send(compileConfig), (message, child, resolve, reject) => {
            if (typeof message === "string") {
                resolve(message);
                return;
            }
            reject(coerceAsError(message));
        });
    }
}

const CREDENTIALS_FILENAME = ".df-credentials.json";
function read(credentialsPath) {
    var _a;
    if (!fs__namespace$1.existsSync(credentialsPath)) {
        throw new Error(`Missing credentials JSON file; not found at path '${credentialsPath}'.`);
    }
    let credentialsAsJson;
    try {
        credentialsAsJson = JSON.parse(fs__namespace$1.readFileSync(credentialsPath, "utf8"));
    }
    catch (e) {
        throw new Error(`Error reading credentials file: ${e.message}`);
    }
    const credentials = verifyObjectMatchesProto(dataform.BigQuery, credentialsAsJson);
    if (!((_a = Object.keys(credentials).find(key => key === "projectId")) === null || _a === void 0 ? void 0 : _a.length)) {
        throw new Error(`Error reading credentials file: the projectId field is required`);
    }
    return credentials;
}
var TestResultStatus;
(function (TestResultStatus) {
    TestResultStatus[TestResultStatus["SUCCESSFUL"] = 0] = "SUCCESSFUL";
    TestResultStatus[TestResultStatus["TIMED_OUT"] = 1] = "TIMED_OUT";
    TestResultStatus[TestResultStatus["OTHER_ERROR"] = 2] = "OTHER_ERROR";
})(TestResultStatus || (TestResultStatus = {}));
async function test$1(dbadapter, timeoutMs = 10000) {
    let timer;
    try {
        const timeout = new Promise(resolve => (timer = setTimeout(() => resolve(TestResultStatus.TIMED_OUT), timeoutMs)));
        const executeQuery = dbadapter.execute("SELECT 1 AS x").then(() => TestResultStatus.SUCCESSFUL);
        return {
            status: await Promise.race([executeQuery, timeout])
        };
    }
    catch (e) {
        return {
            status: TestResultStatus.OTHER_ERROR,
            error: e
        };
    }
    finally {
        if (timer) {
            clearTimeout(timer);
        }
    }
}

const version = "3.0.63";

const gitIgnoreContents = `
${CREDENTIALS_FILENAME}
node_modules/
`;
async function init(projectDir, projectConfig) {
    const workflowSettingsYamlPath = path__namespace.join(projectDir, "workflow_settings.yaml");
    const packageJsonPath = path__namespace.join(projectDir, "package.json");
    const gitignorePath = path__namespace.join(projectDir, ".gitignore");
    const dataformJsonPath = path__namespace.join(projectDir, "dataform.json");
    if (fs__namespace$1.existsSync(workflowSettingsYamlPath) ||
        fs__namespace$1.existsSync(packageJsonPath) ||
        fs__namespace$1.existsSync(dataformJsonPath)) {
        throw new Error("Cannot init dataform project, this already appears to be an NPM or Dataform directory.");
    }
    const filesWritten = [];
    const dirsCreated = [];
    if (!fs__namespace$1.existsSync(projectDir)) {
        fs__namespace$1.mkdirSync(projectDir);
        dirsCreated.push(projectDir);
    }
    const workflowSettings = {
        dataformCoreVersion: version,
        defaultProject: projectConfig.defaultDatabase,
        defaultLocation: projectConfig.defaultLocation,
        defaultDataset: projectConfig.defaultSchema || "dataform",
        defaultAssertionDataset: projectConfig.assertionSchema || "dataform_assertions",
        defaultIcebergConfig: projectConfig.defaultIcebergConfig,
    };
    if (projectConfig.databaseSuffix) {
        workflowSettings.projectSuffix = projectConfig.databaseSuffix;
    }
    if (projectConfig.schemaSuffix) {
        workflowSettings.datasetSuffix = projectConfig.schemaSuffix;
    }
    if (projectConfig.tablePrefix) {
        workflowSettings.namePrefix = projectConfig.tablePrefix;
    }
    if (projectConfig.vars) {
        workflowSettings.vars = projectConfig.vars;
    }
    if (projectConfig.builtinAssertionNamePrefix) {
        workflowSettings.builtinAssertionNamePrefix = projectConfig.builtinAssertionNamePrefix;
    }
    if (projectConfig.defaultIcebergConfig) {
        workflowSettings.defaultIcebergConfig = projectConfig.defaultIcebergConfig;
    }
    fs__namespace$1.writeFileSync(workflowSettingsYamlPath, jsYaml.dump(workflowSettings));
    filesWritten.push(workflowSettingsYamlPath);
    fs__namespace$1.writeFileSync(gitignorePath, gitIgnoreContents);
    filesWritten.push(gitignorePath);
    const definitionsDir = path__namespace.join(projectDir, "definitions");
    fs__namespace$1.mkdirSync(definitionsDir);
    dirsCreated.push(definitionsDir);
    const includesDir = path__namespace.join(projectDir, "includes");
    fs__namespace$1.mkdirSync(includesDir);
    dirsCreated.push(includesDir);
    return {
        filesWritten,
        dirsCreated
    };
}

class Flags {
    static boolean(name, defaultValue = false) {
        return new SingleValueFlag(name, defaultValue, (stringValue) => {
            if (stringValue === "" || stringValue === "true") {
                return true;
            }
            if (stringValue === "false") {
                return false;
            }
            throw Flags.invalidFlagValueError(name);
        });
    }
    static string(name, defaultValue = "") {
        return new SingleValueFlag(name, defaultValue, (stringValue) => stringValue);
    }
    static number(name, defaultValue = 0) {
        return new SingleValueFlag(name, defaultValue, (stringValue) => parseFloat(stringValue));
    }
    static long(name, defaultValue = Long__default["default"].ZERO) {
        return new SingleValueFlag(name, defaultValue, Long__default["default"].fromString);
    }
    static stringSet(name, defaultValue = new Set()) {
        return new SetFlag(name, defaultValue, (singleValue) => singleValue);
    }
    static getRawFlagValue(flagName) {
        return Flags.parsedArgv[flagName];
    }
    static setRawFlagValueForTesting(flagName, value) {
        Flags.parsedArgv[flagName] = value;
    }
    static invalidFlagValueError(flagName) {
        return new Error(`Invalid flag value: ${Flags.getRawFlagValue(flagName)} [${flagName}]`);
    }
}
Flags.parsedArgv = parseArgv(process.argv);
function parseArgv(argv) {
    const parsedArgv = {};
    const splitArgv = [];
    for (let arg of argv) {
        if (arg.startsWith("--no-")) {
            arg = `--${arg.slice(5)}=false`;
        }
        if (arg.startsWith("--") && arg.includes("=")) {
            splitArgv.push(arg.slice(0, arg.indexOf("=")));
            splitArgv.push(arg.slice(arg.indexOf("=") + 1));
        }
        else {
            splitArgv.push(arg);
        }
    }
    let currentFlagName = "";
    for (const splitArg of splitArgv) {
        if (splitArg.startsWith("--")) {
            currentFlagName = splitArg.slice(2);
            parsedArgv[currentFlagName] = "";
        }
        else if (currentFlagName) {
            parsedArgv[currentFlagName] = splitArg;
            currentFlagName = "";
        }
    }
    return parsedArgv;
}
class AbstractFlag {
    constructor(name, defaultValue) {
        this.name = name;
        this.defaultValue = defaultValue;
        this.parsed = {};
    }
    get() {
        if (Flags.getRawFlagValue(this.name) === undefined) {
            return this.defaultValue;
        }
        const stringValue = Flags.getRawFlagValue(this.name);
        if (!(stringValue in this.parsed)) {
            this.parsed[stringValue] = this.parse(stringValue);
        }
        return this.parsed[stringValue];
    }
}
class SingleValueFlag extends AbstractFlag {
    constructor(name, defaultValue, parser) {
        super(name, defaultValue);
        this.parser = parser;
    }
    parse(stringValue) {
        return this.parser(stringValue);
    }
}
class SetFlag extends AbstractFlag {
    constructor(name, defaultValue, parser) {
        super(name, defaultValue);
        this.parser = parser;
    }
    parse(stringValue) {
        return new Set(stringValue
            .split(",")
            .filter(singleValue => singleValue.length > 0)
            .map(this.parser));
    }
}

async function retry(fn, maxAttempts = 1, matchesRetriableError = () => true) {
    let lastErr;
    for (let i = 0; i < maxAttempts; i++) {
        try {
            return await fn();
        }
        catch (e) {
            if (!matchesRetriableError(e)) {
                throw e;
            }
            lastErr = e;
        }
    }
    throw lastErr;
}

const CANCEL_EVENT = "jobCancel";
const flags = {
    runnerNotificationPeriodMillis: Flags.number("runner-notification-period-millis", 5000)
};
const isSuccessfulAction = (actionResult) => actionResult.status === dataform.ActionResult.ExecutionStatus.SUCCESSFUL ||
    actionResult.status === dataform.ActionResult.ExecutionStatus.DISABLED;
function run(dbadapter, graph, executionOptions = {}, partiallyExecutedRunResult = {}, runnerNotificationPeriodMillis = flags.runnerNotificationPeriodMillis.get()) {
    return new Runner(dbadapter, graph, executionOptions, partiallyExecutedRunResult, runnerNotificationPeriodMillis).execute();
}
class Runner {
    constructor(dbadapter, graph, executionOptions = {}, partiallyExecutedRunResult = {}, runnerNotificationPeriodMillis = flags.runnerNotificationPeriodMillis.get()) {
        var _a;
        this.dbadapter = dbadapter;
        this.graph = graph;
        this.runnerNotificationPeriodMillis = runnerNotificationPeriodMillis;
        this.changeListeners = [];
        this.lastNotificationTimestampMillis = 0;
        this.stopped = false;
        this.cancelled = false;
        this.timedOut = false;
        this.skipReason = "";
        this.executionOptions = Object.assign({ projectDir: "." }, executionOptions);
        this.runResult = Object.assign({ actions: [] }, partiallyExecutedRunResult);
        this.allActionTargets = new Set(graph.actions.map(action => targetStringifier.stringify(action.target)));
        this.warehouseStateByTarget = new Map();
        (_a = graph.warehouseState.tables) === null || _a === void 0 ? void 0 : _a.forEach(tableMetadata => this.warehouseStateByTarget.set(targetStringifier.stringify(tableMetadata.target), tableMetadata));
        this.executedActionTargets = new Set(this.runResult.actions
            .filter(action => action.status !== dataform.ActionResult.ExecutionStatus.RUNNING)
            .map(action => targetStringifier.stringify(action.target)));
        this.successfullyExecutedActionTargets = new Set(this.runResult.actions
            .filter(isSuccessfulAction)
            .map(action => targetStringifier.stringify(action.target)));
        this.pendingActions = graph.actions.filter(action => !this.executedActionTargets.has(targetStringifier.stringify(action.target)));
        this.eEmitter = new EventEmitter__default["default"]();
        this.eEmitter.setMaxListeners(0);
    }
    static create(dbadapter, graph, options = {}, runnerNotificationPeriodMillis = flags.runnerNotificationPeriodMillis.get()) {
        return new Runner(dbadapter, graph, options, {}, runnerNotificationPeriodMillis);
    }
    static resume(dbadapter, graph, runResult, runnerNotificationPeriodMillis = flags.runnerNotificationPeriodMillis.get()) {
        return new Runner(dbadapter, graph, {}, runResult, runnerNotificationPeriodMillis);
    }
    onChange(listener) {
        this.changeListeners.push(listener);
        return this;
    }
    execute() {
        var _a, _b, _c;
        if (!!this.executionTask) {
            throw new Error("Executor already started.");
        }
        this.executionTask = this.executeGraph();
        if (!!this.graph.runConfig && !!this.graph.runConfig.timeoutMillis) {
            const now = Date.now();
            const runStartMillis = ((_c = (_b = (_a = this.runResult.timing) === null || _a === void 0 ? void 0 : _a.startTimeMillis) === null || _b === void 0 ? void 0 : _b.toNumber) === null || _c === void 0 ? void 0 : _c.call(_b)) || now;
            const elapsedTimeMillis = now - runStartMillis;
            const timeoutMillis = this.graph.runConfig.timeoutMillis - elapsedTimeMillis;
            this.timeout = setTimeout(() => {
                this.timedOut = true;
                this.cancel("timeout");
            }, timeoutMillis);
        }
        return this;
    }
    stop() {
        this.stopped = true;
    }
    cancel(reason = "user") {
        var _a;
        if (!this.skipReason) {
            switch (reason) {
                case "timeout":
                    this.skipReason = ((_a = this.graph.runConfig) === null || _a === void 0 ? void 0 : _a.timeoutMillis) ? `Run timed out after ${this.graph.runConfig.timeoutMillis / 1000} seconds`
                        : "Run timed out";
                    break;
                case "user":
                    this.skipReason = "Run cancelled";
                    break;
                case "cancellation":
                    this.skipReason = "Run cancelled before action started";
                    break;
            }
        }
        this.cancelled = true;
        this.eEmitter.emit(CANCEL_EVENT, undefined, undefined);
    }
    async result() {
        try {
            return await this.executionTask;
        }
        finally {
            if (!!this.timeout) {
                clearTimeout(this.timeout);
            }
        }
    }
    notifyListeners() {
        if (Date.now() - this.runnerNotificationPeriodMillis < this.lastNotificationTimestampMillis) {
            return;
        }
        const runResultClone = deepClone(dataform.RunResult, this.runResult);
        this.lastNotificationTimestampMillis = Date.now();
        this.changeListeners.forEach(listener => listener(runResultClone));
    }
    async executeGraph() {
        const timer = Timer.start(this.runResult.timing);
        this.runResult.status = dataform.RunResult.ExecutionStatus.RUNNING;
        this.runResult.timing = timer.current();
        this.notifyListeners();
        if (this.runResult.actions.length === 0) {
            await this.prepareAllSchemas();
        }
        await this.executeAllActionsReadyForExecution();
        if (this.stopped) {
            return this.runResult;
        }
        this.runResult.timing = timer.end();
        this.runResult.status = dataform.RunResult.ExecutionStatus.SUCCESSFUL;
        if (this.timedOut) {
            this.runResult.status = dataform.RunResult.ExecutionStatus.TIMED_OUT;
        }
        else if (this.cancelled) {
            this.runResult.status = dataform.RunResult.ExecutionStatus.CANCELLED;
        }
        else if (this.runResult.actions.some(action => action.status === dataform.ActionResult.ExecutionStatus.FAILED)) {
            this.runResult.status = dataform.RunResult.ExecutionStatus.FAILED;
        }
        return this.runResult;
    }
    async prepareAllSchemas() {
        const databaseSchemas = new Map();
        this.graph.actions
            .filter(action => !!action.target && !!action.target.schema)
            .forEach(({ target }) => {
            const trueDatabase = target.database || this.graph.projectConfig.defaultDatabase;
            if (!databaseSchemas.has(trueDatabase)) {
                databaseSchemas.set(trueDatabase, new Set());
            }
            databaseSchemas.get(trueDatabase).add(target.schema);
        });
        await Promise.all(Array.from(databaseSchemas.entries()).map(async ([database, schemas]) => {
            const existingSchemas = new Set(await this.dbadapter.schemas(database));
            await Promise.all(Array.from(schemas)
                .filter(schema => !existingSchemas.has(schema))
                .map(schema => this.dbadapter.createSchema(database, schema)));
        }));
    }
    async executeAllActionsReadyForExecution() {
        if (this.stopped) {
            return;
        }
        if (this.cancelled) {
            const allPendingActions = this.pendingActions;
            this.pendingActions = [];
            const skipErrorMessage = this.skipReason || "Run cancelled before action started";
            allPendingActions.forEach(pendingAction => {
                let tasks;
                if (pendingAction.tasks && pendingAction.tasks.length > 0) {
                    tasks = pendingAction.tasks.map((_, i) => (Object.assign({ status: dataform.TaskResult.ExecutionStatus.SKIPPED }, (i === 0 ? { errorMessage: skipErrorMessage } : {}))));
                }
                else {
                    tasks = [
                        {
                            status: dataform.TaskResult.ExecutionStatus.SKIPPED,
                            errorMessage: skipErrorMessage
                        }
                    ];
                }
                this.runResult.actions.push({
                    target: pendingAction.target,
                    status: dataform.ActionResult.ExecutionStatus.SKIPPED,
                    tasks
                });
            });
            this.notifyListeners();
            return;
        }
        const executableActions = [];
        const skippableActions = [];
        const stillPendingActions = [];
        for (const pendingAction of this.pendingActions) {
            if (pendingAction.dependencyTargets.every(dependency => !this.allActionTargets.has(targetStringifier.stringify(dependency)) ||
                this.successfullyExecutedActionTargets.has(targetStringifier.stringify(dependency)))) {
                executableActions.push(pendingAction);
            }
            else if (pendingAction.dependencyTargets.every(dependency => !this.allActionTargets.has(targetStringifier.stringify(dependency)) ||
                this.executedActionTargets.has(targetStringifier.stringify(dependency)))) {
                skippableActions.push(pendingAction);
            }
            else {
                stillPendingActions.push(pendingAction);
            }
        }
        this.pendingActions = stillPendingActions;
        await Promise.all([
            (async () => {
                skippableActions.forEach(skippableAction => {
                    this.runResult.actions.push({
                        target: skippableAction.target,
                        status: dataform.ActionResult.ExecutionStatus.SKIPPED,
                        tasks: skippableAction.tasks.map(() => ({
                            status: dataform.TaskResult.ExecutionStatus.SKIPPED
                        }))
                    });
                });
                if (skippableActions.length > 0) {
                    this.notifyListeners();
                    await this.executeAllActionsReadyForExecution();
                }
            })(),
            Promise.all(executableActions.map(async (executableAction) => {
                const actionResult = await this.executeAction(executableAction);
                this.executedActionTargets.add(targetStringifier.stringify(executableAction.target));
                if (isSuccessfulAction(actionResult)) {
                    this.successfullyExecutedActionTargets.add(targetStringifier.stringify(executableAction.target));
                }
                await this.executeAllActionsReadyForExecution();
            }))
        ]);
    }
    async executeAction(action) {
        var _a;
        let actionResult = {
            target: action.target,
            tasks: []
        };
        if (action.tasks.length === 0) {
            actionResult.status = dataform.ActionResult.ExecutionStatus.DISABLED;
            this.runResult.actions.push(actionResult);
            this.notifyListeners();
            return actionResult;
        }
        const resumedActionResult = this.runResult.actions.find(existingActionResult => equals(dataform.Target, existingActionResult.target, action.target));
        if (resumedActionResult) {
            actionResult = resumedActionResult;
        }
        else {
            this.runResult.actions.push(actionResult);
        }
        actionResult.status = dataform.ActionResult.ExecutionStatus.RUNNING;
        const timer = Timer.start(resumedActionResult === null || resumedActionResult === void 0 ? void 0 : resumedActionResult.timing);
        actionResult.timing = timer.current();
        this.notifyListeners();
        await this.dbadapter.withClientLock(async (client) => {
            var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l;
            for (const task of action.tasks.slice(actionResult.tasks.length)) {
                if (this.stopped) {
                    return actionResult;
                }
                if (actionResult.status === dataform.ActionResult.ExecutionStatus.RUNNING &&
                    !this.cancelled) {
                    const taskStatus = await this.executeTask(client, task, actionResult, {
                        bigquery: {
                            labels: Object.assign(Object.assign({}, (((_b = (_a = this.executionOptions) === null || _a === void 0 ? void 0 : _a.bigquery) === null || _b === void 0 ? void 0 : _b.labels) || {})), (((_c = action.actionDescriptor) === null || _c === void 0 ? void 0 : _c.bigqueryLabels) || {})),
                            actionRetryLimit: (_e = (_d = this.executionOptions) === null || _d === void 0 ? void 0 : _d.bigquery) === null || _e === void 0 ? void 0 : _e.actionRetryLimit,
                            jobPrefix: (_g = (_f = this.executionOptions) === null || _f === void 0 ? void 0 : _f.bigquery) === null || _g === void 0 ? void 0 : _g.jobPrefix,
                            dryRun: (_j = (_h = this.executionOptions) === null || _h === void 0 ? void 0 : _h.bigquery) === null || _j === void 0 ? void 0 : _j.dryRun,
                            reservation: ((_k = action.actionDescriptor) === null || _k === void 0 ? void 0 : _k.reservation) || ((_l = this.graph.projectConfig) === null || _l === void 0 ? void 0 : _l.defaultReservation)
                        }
                    });
                    if (taskStatus === dataform.TaskResult.ExecutionStatus.FAILED) {
                        actionResult.status = dataform.ActionResult.ExecutionStatus.FAILED;
                    }
                    else if (taskStatus === dataform.TaskResult.ExecutionStatus.CANCELLED) {
                        actionResult.status = dataform.ActionResult.ExecutionStatus.CANCELLED;
                    }
                }
                else {
                    actionResult.tasks.push({
                        status: dataform.TaskResult.ExecutionStatus.SKIPPED,
                        errorMessage: this.skipReason || "Task skipped after cancellation"
                    });
                }
            }
        });
        if (this.stopped) {
            return actionResult;
        }
        if (action.actionDescriptor &&
            actionResult.status === dataform.ActionResult.ExecutionStatus.RUNNING &&
            !(this.graph.runConfig && this.graph.runConfig.disableSetMetadata) &&
            !((_a = this.executionOptions.bigquery) === null || _a === void 0 ? void 0 : _a.dryRun) &&
            action.type === "table") {
            try {
                await this.dbadapter.setMetadata(action);
            }
            catch (e) {
                if (actionResult.tasks.length > 0) {
                    actionResult.tasks[actionResult.tasks.length - 1].errorMessage = `Error setting metadata: ${e.message}`;
                    actionResult.tasks[actionResult.tasks.length - 1].status =
                        dataform.TaskResult.ExecutionStatus.FAILED;
                }
                actionResult.status = dataform.ActionResult.ExecutionStatus.FAILED;
            }
        }
        this.warehouseStateByTarget.delete(targetStringifier.stringify(action.target));
        if (actionResult.status === dataform.ActionResult.ExecutionStatus.RUNNING) {
            actionResult.status = dataform.ActionResult.ExecutionStatus.SUCCESSFUL;
        }
        actionResult.timing = timer.end();
        this.notifyListeners();
        return actionResult;
    }
    async executeTask(client, task, parentAction, options) {
        var _a, _b, _c, _d;
        const timer = Timer.start();
        const taskResult = {
            status: dataform.TaskResult.ExecutionStatus.RUNNING,
            timing: timer.current(),
            metadata: {}
        };
        parentAction.tasks.push(taskResult);
        this.notifyListeners();
        if (((_a = options.bigquery) === null || _a === void 0 ? void 0 : _a.dryRun) && task.type === "assertion") {
            taskResult.status = dataform.TaskResult.ExecutionStatus.SUCCESSFUL;
        }
        else {
            try {
                const { rows, metadata } = await retry(() => client.execute(task.statement, {
                    onCancel: handleCancel => this.eEmitter.on(CANCEL_EVENT, handleCancel),
                    rowLimit: 1,
                    bigquery: options.bigquery
                }), task.type === "operation" ? 1 : options.bigquery.actionRetryLimit + 1 || 1);
                taskResult.metadata = metadata;
                if (task.type === "assertion") {
                    const rowCount = (_b = rows[0]) === null || _b === void 0 ? void 0 : _b[Object.keys(rows[0])[0]];
                    if (rowCount > 0) {
                        throw new Error(`Assertion failed: query returned ${rowCount} row(s).`);
                    }
                }
                taskResult.status = dataform.TaskResult.ExecutionStatus.SUCCESSFUL;
            }
            catch (e) {
                taskResult.status = this.cancelled
                    ? dataform.TaskResult.ExecutionStatus.CANCELLED
                    : dataform.TaskResult.ExecutionStatus.FAILED;
                taskResult.errorMessage = `${this.graph.projectConfig.warehouse} error: ${e.message}`;
                if ((_d = (_c = e.metadata) === null || _c === void 0 ? void 0 : _c.bigquery) === null || _d === void 0 ? void 0 : _d.jobId) {
                    taskResult.metadata = {
                        bigquery: {
                            jobId: e.metadata.bigquery.jobId
                        }
                    };
                }
            }
        }
        taskResult.timing = timer.end();
        this.notifyListeners();
        return taskResult.status;
    }
}
class Timer {
    constructor(startTimeMillis) {
        this.startTimeMillis = startTimeMillis;
    }
    static start(existingTiming) {
        return new Timer((existingTiming === null || existingTiming === void 0 ? void 0 : existingTiming.startTimeMillis.toNumber()) || new Date().valueOf());
    }
    current() {
        return {
            startTimeMillis: Long__default["default"].fromNumber(this.startTimeMillis)
        };
    }
    end() {
        return {
            startTimeMillis: Long__default["default"].fromNumber(this.startTimeMillis),
            endTimeMillis: Long__default["default"].fromNumber(new Date().valueOf())
        };
    }
}

async function test(dbadapter, tests) {
    return await Promise.all(tests.map(testCase => runTest(dbadapter, testCase)));
}
async function runTest(dbadapter, testCase) {
    let actualResults;
    let expectedResults;
    try {
        [actualResults, expectedResults] = await Promise.all([
            dbadapter.execute(testCase.testQuery, { byteLimit: 1024 * 1024 }),
            dbadapter.execute(testCase.expectedOutputQuery, { byteLimit: 1024 * 1024 })
        ]);
    }
    catch (e) {
        return {
            name: testCase.name,
            successful: false,
            messages: [`Error thrown: ${e.message}.`]
        };
    }
    if (actualResults.rows.length !== expectedResults.rows.length) {
        return {
            name: testCase.name,
            successful: false,
            messages: [
                `Expected ${expectedResults.rows.length} rows, but saw ${actualResults.rows.length} rows.`
            ]
        };
    }
    if (actualResults.rows.length === 0) {
        return {
            name: testCase.name,
            successful: true
        };
    }
    const actualColumns = Object.keys(actualResults.rows[0]);
    const expectedColumns = Object.keys(expectedResults.rows[0]);
    if (actualColumns.length !== expectedColumns.length) {
        return {
            name: testCase.name,
            successful: false,
            messages: [`Expected columns "${expectedColumns}", but saw "${actualColumns}".`]
        };
    }
    for (const expectedColumn of expectedColumns) {
        if (!actualColumns.some(actualColumn => normalizeColumnName(actualColumn) === normalizeColumnName(expectedColumn))) {
            return {
                name: testCase.name,
                successful: false,
                messages: [`Expected columns "${expectedColumns}", but saw "${actualColumns}".`]
            };
        }
    }
    const rowMessages = [];
    for (let i = 0; i < actualResults.rows.length; i++) {
        const actualResultRow = normalizeRow(actualResults.rows[i]);
        const expectedResultRow = normalizeRow(expectedResults.rows[i]);
        for (const column of actualColumns) {
            const normalizedColumn = normalizeColumnName(column);
            const expectedValue = expectedResultRow[normalizedColumn];
            const actualValue = actualResultRow[normalizedColumn];
            if (expectedValue === null && actualValue !== null) {
                rowMessages.push(`For row ${i} and column "${column}": expected null, but saw "${actualValue}".`);
                break;
            }
            if (expectedValue !== null && actualValue === null) {
                rowMessages.push(`For row ${i} and column "${column}": expected "${expectedValue}", but saw null.`);
                break;
            }
            if (typeof expectedValue !== typeof actualValue) {
                rowMessages.push(`For row ${i} and column "${column}": expected type "${typeof expectedValue}", but saw type "${typeof actualValue}".`);
                break;
            }
            const comparableExpectedValue = typeof expectedValue === "object" ? JSON.stringify(expectedValue) : expectedValue;
            const comparableActualValue = typeof actualValue === "object" ? JSON.stringify(actualValue) : actualValue;
            if (comparableExpectedValue !== comparableActualValue) {
                rowMessages.push(`For row ${i} and column "${column}": expected "${comparableExpectedValue}", but saw "${comparableActualValue}".`);
            }
        }
    }
    if (rowMessages.length > 0) {
        return {
            name: testCase.name,
            successful: false,
            messages: rowMessages
        };
    }
    return {
        name: testCase.name,
        successful: true
    };
}
function normalizeColumnName(name) {
    return name.toUpperCase();
}
function normalizeRow(row) {
    const newRow = {};
    Object.keys(row).forEach(colName => {
        newRow[normalizeColumnName(colName)] = row[colName];
    });
    return newRow;
}

function parseBigqueryEvalError(error) {
    const evalError = dataform.QueryEvaluationError.create({
        message: String(error)
    });
    try {
        if (!error.message) {
            return evalError;
        }
        const bracketsString = error.message.split("[").slice(-1)[0];
        const [_, lineNumber, columnNumber] = bracketsString.match(/([0-9]*)[^0-9]*([0-9]*).*/);
        const line = Number(lineNumber);
        const column = Number(columnNumber);
        if (line || line === 0) {
            evalError.errorLocation = { line, column };
        }
    }
    catch (_) {
    }
    return evalError;
}

class LimitedResultSet {
    constructor(options) {
        this.options = options;
        this.results = [];
        this.currentBytes = 0;
    }
    push(row) {
        var _a, _b, _c, _d;
        this.currentBytes += sizeof__default["default"](row);
        if ((((_a = this.options) === null || _a === void 0 ? void 0 : _a.rowLimit) && this.results.length >= ((_b = this.options) === null || _b === void 0 ? void 0 : _b.rowLimit)) ||
            (((_c = this.options) === null || _c === void 0 ? void 0 : _c.byteLimit) && this.currentBytes >= ((_d = this.options) === null || _d === void 0 ? void 0 : _d.byteLimit))) {
            return false;
        }
        this.results.push(row);
        return true;
    }
    get rows() {
        return this.results;
    }
}

const EXTRA_GOOGLE_SCOPES = ["https://www.googleapis.com/auth/drive"];
const BIGQUERY_DATE_RELATED_FIELDS = [
    "BigQueryDate",
    "BigQueryTime",
    "BigQueryTimestamp",
    "BigQueryDatetime"
];
const BIGQUERY_INTERNAL_ERROR_JOB_MAX_ATTEMPTS = 3;
function createBigQueryClientProvider(credentials) {
    const clients = new Map();
    return (projectId) => {
        projectId = projectId || credentials.projectId;
        if (!clients.has(projectId)) {
            clients.set(projectId, new bigquery.BigQuery({
                projectId,
                scopes: EXTRA_GOOGLE_SCOPES,
                location: credentials.location,
                credentials: credentials.credentials && JSON.parse(credentials.credentials)
            }));
        }
        return clients.get(projectId);
    };
}
class BigQueryDbAdapter {
    constructor(credentials, options) {
        this.bigQueryCredentials = credentials;
        this.clientProvider = (options === null || options === void 0 ? void 0 : options.clientProvider) || createBigQueryClientProvider(credentials);
        this.pool = new promisePoolExecutor.PromisePoolExecutor({
            concurrencyLimit: options === null || options === void 0 ? void 0 : options.concurrencyLimit,
            frequencyWindow: 1000,
            frequencyLimit: 30
        });
    }
    async execute(statement, options = { interactive: false, rowLimit: 1000, byteLimit: 1024 * 1024 }) {
        var _a;
        if ((options === null || options === void 0 ? void 0 : options.interactive) && ((_a = options === null || options === void 0 ? void 0 : options.bigquery) === null || _a === void 0 ? void 0 : _a.labels)) {
            throw new Error("BigQuery job labels may not be set for interactive queries.");
        }
        if (!statement) {
            throw new Error("Query string cannot be empty");
        }
        return this.pool
            .addSingleTask({
            generator: () => {
                var _a, _b, _c, _d, _e, _f;
                return (options === null || options === void 0 ? void 0 : options.interactive) ? this.runQuery(statement, options === null || options === void 0 ? void 0 : options.params, options === null || options === void 0 ? void 0 : options.rowLimit, options === null || options === void 0 ? void 0 : options.byteLimit, (_a = options.bigquery) === null || _a === void 0 ? void 0 : _a.location)
                    : this.createQueryJob(statement, options === null || options === void 0 ? void 0 : options.params, options === null || options === void 0 ? void 0 : options.rowLimit, options === null || options === void 0 ? void 0 : options.byteLimit, options === null || options === void 0 ? void 0 : options.onCancel, (_b = options === null || options === void 0 ? void 0 : options.bigquery) === null || _b === void 0 ? void 0 : _b.labels, (_c = options === null || options === void 0 ? void 0 : options.bigquery) === null || _c === void 0 ? void 0 : _c.location, (_d = options === null || options === void 0 ? void 0 : options.bigquery) === null || _d === void 0 ? void 0 : _d.jobPrefix, (_e = options === null || options === void 0 ? void 0 : options.bigquery) === null || _e === void 0 ? void 0 : _e.dryRun, (_f = options === null || options === void 0 ? void 0 : options.bigquery) === null || _f === void 0 ? void 0 : _f.reservation);
            }
        })
            .promise();
    }
    async executeRaw(statement, options = { rowLimit: 1000 }) {
        if (!statement) {
            throw new Error("Query string cannot be empty");
        }
        return this.pool
            .addSingleTask({
            generator: async () => {
                var _a, _b;
                const [rows, , apiResponse] = await this.getClient().query(Object.assign(Object.assign({}, this.prepareQueryOptions(statement, options.rowLimit, options.bigquery, options.params)), { skipParsing: true }));
                const schema = (_b = (_a = apiResponse === null || apiResponse === void 0 ? void 0 : apiResponse.schema) === null || _a === void 0 ? void 0 : _a.fields) === null || _b === void 0 ? void 0 : _b.map((field) => convertField(field));
                return { rows, schema, metadata: {} };
            }
        })
            .promise();
    }
    async withClientLock(callback) {
        return await callback(this);
    }
    async evaluate(queryOrAction) {
        const validationQueries = collectEvaluationQueries(queryOrAction, true);
        return await Promise.all(validationQueries.map(async ({ query, incremental }) => {
            try {
                await this.pool
                    .addSingleTask({
                    generator: () => this.getClient().query({
                        useLegacySql: false,
                        query,
                        dryRun: true
                    })
                })
                    .promise();
                return dataform.QueryEvaluation.create({
                    status: dataform.QueryEvaluation.QueryEvaluationStatus.SUCCESS,
                    incremental,
                    query
                });
            }
            catch (e) {
                return {
                    status: dataform.QueryEvaluation.QueryEvaluationStatus.FAILURE,
                    error: parseBigqueryEvalError(e),
                    incremental,
                    query
                };
            }
        }));
    }
    async tables(database, schema) {
        const datasetIds = schema ? [schema] : await this.schemas(database);
        const tablesMetadata = [];
        await Promise.all(datasetIds.map(async (datasetId) => {
            const [tables] = await this.getClient(database)
                .dataset(datasetId)
                .getTables({ autoPaginate: true, maxResults: 1000 });
            await Promise.all(tables.map(async (table) => {
                const metadata = await this.table({
                    database,
                    schema: datasetId,
                    name: table.id
                });
                if (metadata) {
                    tablesMetadata.push(metadata);
                }
            }));
        }));
        return tablesMetadata;
    }
    async search(searchText, options = { limit: 1000 }) {
        const results = await this.execute(`select table_catalog, table_schema, table_name
       from region-${this.bigQueryCredentials.location}.INFORMATION_SCHEMA.COLUMN_FIELD_PATHS
       where regexp_contains(table_schema, @searchText) or regexp_contains(table_name, @searchText) or regexp_contains(field_path, @searchText)
       group by 1, 2, 3`, {
            params: {
                searchText: `(?i)${searchText}`
            },
            interactive: true,
            rowLimit: options.limit
        });
        return await Promise.all(results.rows.map(row => this.table({
            database: row.table_catalog,
            schema: row.table_schema,
            name: row.table_name
        })));
    }
    async table(target) {
        var _a;
        const metadata = await this.getMetadata(target);
        if (!metadata) {
            return null;
        }
        if (metadata.tableReference.datasetId !== target.schema ||
            metadata.tableReference.tableId !== target.name) {
            const metadataTarget = {
                database: metadata.tableReference.projectId,
                schema: metadata.tableReference.datasetId,
                name: metadata.tableReference.tableId
            };
            throw new Error(`Target ${JSON.stringify(metadataTarget)} does not match requested target ${JSON.stringify(target)}.`);
        }
        return dataform.TableMetadata.create({
            type: metadata.type === "TABLE"
                ? dataform.TableMetadata.Type.TABLE
                : metadata.type === "VIEW"
                    ? dataform.TableMetadata.Type.VIEW
                    : dataform.TableMetadata.Type.UNKNOWN,
            target,
            fields: (_a = metadata.schema.fields) === null || _a === void 0 ? void 0 : _a.map(field => convertField(field)),
            lastUpdatedMillis: Long__default["default"].fromString(metadata.lastModifiedTime),
            description: metadata.description,
            labels: metadata.labels,
            bigquery: {
                hasStreamingBuffer: !!metadata.streamingBuffer
            }
        });
    }
    async deleteTable(target) {
        await this.getClient(target.database)
            .dataset(target.schema)
            .table(target.name)
            .delete({ ignoreNotFound: true });
    }
    async schemas(database) {
        const data = await this.getClient(database).getDatasets({ autoPaginate: true, maxResults: 1000 });
        return data[0].map(dataset => dataset.id);
    }
    async createSchema(database, schema) {
        await this.execute(`create schema if not exists \`${database || this.bigQueryCredentials.projectId}.${schema}\``, { bigquery: { location: this.bigQueryCredentials.location || "US" } });
    }
    async setMetadata(action) {
        const { target, actionDescriptor } = action;
        const metadata = await this.getMetadata(target);
        const schemaWithDescription = addDescriptionToMetadata(actionDescriptor.columns, metadata.schema.fields);
        await this.getClient(target.database)
            .dataset(target.schema)
            .table(target.name)
            .setMetadata({
            description: actionDescriptor.description,
            schema: schemaWithDescription,
            labels: actionDescriptor.bigqueryLabels
        });
    }
    async getMetadata(target) {
        var _a, _b;
        try {
            const table = await this.getClient(target.database)
                .dataset(target.schema)
                .table(target.name)
                .getMetadata();
            return table === null || table === void 0 ? void 0 : table[0];
        }
        catch (e) {
            if (((_b = (_a = e === null || e === void 0 ? void 0 : e.errors) === null || _a === void 0 ? void 0 : _a[0]) === null || _b === void 0 ? void 0 : _b.reason) === "notFound") {
                return null;
            }
            throw coerceAsError(e);
        }
    }
    getClient(projectId) {
        return this.clientProvider(projectId);
    }
    async runQuery(query, params, rowLimit, byteLimit, location) {
        const results = await new Promise((resolve, reject) => {
            const allRows = new LimitedResultSet({
                rowLimit,
                byteLimit
            });
            const stream = this.getClient().createQueryStream({
                query,
                params,
                location
            });
            stream
                .on("error", e => reject(coerceAsError(e)))
                .on("data", row => {
                if (!allRows.push(row)) {
                    stream.end();
                }
            })
                .on("end", () => {
                resolve(allRows.rows);
            });
        });
        return { rows: cleanRows(results), metadata: {} };
    }
    prepareQueryOptions(query, rowLimit, bigqueryOptions, params) {
        return {
            query,
            useLegacySql: false,
            jobPrefix: "dataform-" + ((bigqueryOptions === null || bigqueryOptions === void 0 ? void 0 : bigqueryOptions.jobPrefix) ? `${bigqueryOptions.jobPrefix}-` : ""),
            location: bigqueryOptions === null || bigqueryOptions === void 0 ? void 0 : bigqueryOptions.location,
            maxResults: rowLimit,
            labels: bigqueryOptions === null || bigqueryOptions === void 0 ? void 0 : bigqueryOptions.labels,
            dryRun: bigqueryOptions === null || bigqueryOptions === void 0 ? void 0 : bigqueryOptions.dryRun,
            reservation: bigqueryOptions === null || bigqueryOptions === void 0 ? void 0 : bigqueryOptions.reservation,
            params
        };
    }
    async createQueryJob(query, params, rowLimit, byteLimit, onCancel, labels, location, jobPrefix, dryRun, reservation) {
        let isCancelled = false;
        onCancel === null || onCancel === void 0 ? void 0 : onCancel(() => (isCancelled = true));
        return retry(async () => {
            try {
                const job = await this.getClient().createQueryJob(this.prepareQueryOptions(query, rowLimit, {
                    labels,
                    location,
                    jobPrefix,
                    dryRun,
                    reservation
                }, params));
                const resultStream = job[0].getQueryResultsStream();
                return new Promise((resolve, reject) => {
                    if (isCancelled) {
                        resultStream.end();
                        reject(new Error("Query cancelled."));
                        return;
                    }
                    onCancel === null || onCancel === void 0 ? void 0 : onCancel(() => {
                        resultStream.end();
                        reject(new Error("Query cancelled."));
                    });
                    const results = new LimitedResultSet({
                        rowLimit,
                        byteLimit
                    });
                    resultStream
                        .on("error", async (e) => {
                        var _a, _b;
                        if (dryRun && ((_a = e.message) === null || _a === void 0 ? void 0 : _a.includes("Not found: Job"))) {
                            resolve({ rows: [], metadata: {} });
                            return;
                        }
                        try {
                            const [jobMetadata] = await job[0].getMetadata();
                            if (!!((_b = jobMetadata.status) === null || _b === void 0 ? void 0 : _b.errorResult)) {
                                const error = createBigQueryError(jobMetadata);
                                reject(error);
                            }
                        }
                        catch (metadataError) {
                            reject(metadataError);
                        }
                        reject(e);
                    })
                        .on("data", row => {
                        if (!results.push(row)) {
                            resultStream.end();
                        }
                    })
                        .on("end", async () => {
                        var _a;
                        try {
                            const [jobMetadata] = await job[0].getMetadata();
                            if (!!((_a = jobMetadata.status) === null || _a === void 0 ? void 0 : _a.errorResult)) {
                                const error = createBigQueryError(jobMetadata);
                                reject(error);
                                return;
                            }
                            resolve({
                                rows: results.rows,
                                metadata: {
                                    bigquery: {
                                        jobId: jobMetadata.jobReference.jobId,
                                        totalBytesBilled: jobMetadata.statistics.query.totalBytesBilled
                                            ? Long__default["default"].fromString(jobMetadata.statistics.query.totalBytesBilled)
                                            : Long__default["default"].ZERO,
                                        totalBytesProcessed: jobMetadata.statistics.query.totalBytesProcessed
                                            ? Long__default["default"].fromString(jobMetadata.statistics.query.totalBytesProcessed)
                                            : Long__default["default"].ZERO,
                                    }
                                }
                            });
                        }
                        catch (e) {
                            reject(e);
                        }
                    });
                });
            }
            catch (e) {
                throw coerceAsError(e);
            }
        }, BIGQUERY_INTERNAL_ERROR_JOB_MAX_ATTEMPTS, e => { var _a; return (_a = e.message) === null || _a === void 0 ? void 0 : _a.includes("Retrying the job may solve the problem"); });
    }
}
function cleanRows(rows) {
    if (rows.length === 0) {
        return rows;
    }
    const sampleData = rows[0];
    const fieldsWithBigQueryDates = Object.keys(sampleData).filter(key => sampleData[key] &&
        sampleData[key].constructor &&
        BIGQUERY_DATE_RELATED_FIELDS.includes(sampleData[key].constructor.name));
    fieldsWithBigQueryDates.forEach(dateField => {
        rows.forEach(row => (row[dateField] = row[dateField] ? row[dateField].value : row[dateField]));
    });
    return rows;
}
function convertField(field) {
    const result = {
        name: field.name,
        flags: field.mode === "REPEATED" ? [dataform.Field.Flag.REPEATED] : [],
        description: field.description
    };
    if (field.type === "RECORD" || field.type === "STRUCT") {
        result.struct = dataform.Fields.create({
            fields: field.fields.map(innerField => convertField(innerField))
        });
    }
    else {
        result.primitive = convertFieldType(field.type);
    }
    return dataform.Field.create(result);
}
function convertFieldType(type) {
    switch (String(type).toUpperCase()) {
        case "FLOAT":
        case "FLOAT64":
            return dataform.Field.Primitive.FLOAT;
        case "INTEGER":
        case "INT64":
            return dataform.Field.Primitive.INTEGER;
        case "NUMERIC":
        case "BIGNUMERIC":
            return dataform.Field.Primitive.NUMERIC;
        case "BOOL":
        case "BOOLEAN":
            return dataform.Field.Primitive.BOOLEAN;
        case "STRING":
        case "JSON":
        case "INTERVAL":
            return dataform.Field.Primitive.STRING;
        case "DATE":
            return dataform.Field.Primitive.DATE;
        case "DATETIME":
            return dataform.Field.Primitive.DATETIME;
        case "TIMESTAMP":
            return dataform.Field.Primitive.TIMESTAMP;
        case "TIME":
            return dataform.Field.Primitive.TIME;
        case "BYTES":
            return dataform.Field.Primitive.BYTES;
        case "GEOGRAPHY":
            return dataform.Field.Primitive.GEOGRAPHY;
        case "BIGNUMERIC":
            return dataform.Field.Primitive.BIGNUMERIC;
        case "JSON":
            return dataform.Field.Primitive.JSON;
        case "INTERVAL":
            return dataform.Field.Primitive.INTERVAL;
        default:
            return dataform.Field.Primitive.UNKNOWN;
    }
}
function addDescriptionToMetadata(columnDescriptions, metadataArray) {
    if (!columnDescriptions) {
        return metadataArray;
    }
    const findDescription = (path) => columnDescriptions.find(column => column.path.join("") === path.join(""));
    const mapDescriptionToMetadata = (metadata, path) => {
        var _a;
        const description = findDescription(path);
        if (description) {
            metadata.description = description.description;
            if (((_a = description.bigqueryPolicyTags) === null || _a === void 0 ? void 0 : _a.length) > 0) {
                metadata.policyTags = {
                    names: description.bigqueryPolicyTags
                };
            }
        }
        if (metadata.fields) {
            metadata.fields = metadata.fields.map(nestedMetadata => mapDescriptionToMetadata(nestedMetadata, [...path, nestedMetadata.name]));
        }
        return metadata;
    };
    const newMetadata = metadataArray.map(metaItem => mapDescriptionToMetadata(metaItem, [metaItem.name]));
    return newMetadata;
}
function createBigQueryError(jobMetadata) {
    const error = new Error(jobMetadata.status.errorResult.message);
    if (jobMetadata.jobReference && jobMetadata.jobReference.jobId) {
        error.metadata = {
            bigquery: {
                jobId: jobMetadata.jobReference.jobId,
            }
        };
    }
    return error;
}

function actuallyResolve(...filePaths) {
    return path__namespace.resolve(...filePaths.map(filePath => untildify__default["default"](filePath)));
}
function assertPathExists(checkPath) {
    if (!fs__namespace$1.existsSync(checkPath)) {
        throw new Error(`${checkPath} does not exist!`);
    }
}
function compiledGraphHasErrors(graph) {
    var _a, _b;
    return ((_b = (_a = graph.graphErrors) === null || _a === void 0 ? void 0 : _a.compilationErrors) === null || _b === void 0 ? void 0 : _b.length) > 0;
}
function formatExecutionSuffix(jobIds, bytesBilled) {
    const jobMetadataParts = [];
    if (jobIds.length > 0) {
        jobMetadataParts.push(`\n \t jobId: ${jobIds.join(", ")}`);
    }
    if (bytesBilled.length > 0) {
        jobMetadataParts.push(`\n \t Bytes billed: ${bytesBilled.join(", ")}`);
    }
    return jobMetadataParts.length > 0 ? ` ${jobMetadataParts}` : "";
}
function formatBytesInHumanReadableFormat(bytes) {
    if (bytes <= 0) {
        return '0 B';
    }
    const units = ['B', 'KiB', 'MiB', 'GiB', 'TiB', 'PiB'];
    const k = 1024;
    const i = Math.floor(Math.log(bytes) / Math.log(k));
    const value = (bytes / Math.pow(k, i)).toFixed(2);
    return `${value} ${units[i]}`;
}
function promptForIcebergConfig() {
    print(ICEBERG_CONFIG_PROMPT_TEXT);
    print(ICEBERG_CONFIG_PROMPT_HINT);
    const tempIcebergConfig = {};
    let bucketName;
    while (true) {
        print(ICEBERG_BUCKET_NAME_HINT);
        bucketName = interactiveQuestion(ICEBERG_BUCKET_NAME_PROMPT_QUESTION);
        try {
            if (bucketName) {
                validateIcebergConfigBucketName(bucketName);
                tempIcebergConfig.bucketName = bucketName;
            }
            break;
        }
        catch (e) {
            printError(`Validation Error: ${e.message}`);
            print("Please try again.");
        }
    }
    let tableFolderRoot;
    while (true) {
        print(ICEBERG_TABLE_FOLDER_ROOT_HINT);
        tableFolderRoot = interactiveQuestion(ICEBERG_TABLE_FOLDER_ROOT_PROMPT_QUESTION);
        try {
            if (tableFolderRoot) {
                validateIcebergConfigTableFolderRoot(tableFolderRoot);
                tempIcebergConfig.tableFolderRoot = tableFolderRoot;
            }
            break;
        }
        catch (e) {
            printError(`Validation Error: ${e.message}`);
            print("Please try again.");
        }
    }
    let tableFolderSubpath;
    while (true) {
        print(ICEBERG_TABLE_FOLDER_ROOT_SUBPATH_HINT);
        tableFolderSubpath = interactiveQuestion(ICEBERG_TABLE_FOLDER_SUBPATH_PROMPT_QUESTION);
        try {
            if (tableFolderSubpath) {
                validateIcebergConfigTableFolderSubpath(tableFolderSubpath);
                tempIcebergConfig.tableFolderSubpath = tableFolderSubpath;
            }
            break;
        }
        catch (e) {
            printError(`Validation Error: ${e.message}`);
            print("Please try again.");
        }
    }
    let connection;
    while (true) {
        print(ICEBERG_CONNECTION_HINT);
        connection = interactiveQuestion(ICEBERG_CONNECTION_QUESTION);
        try {
            if (connection) {
                validateConnectionFormat(connection);
                tempIcebergConfig.connection = connection;
            }
            break;
        }
        catch (e) {
            printError(`Validation Error: ${e.message}`);
            print("Please try again.");
        }
    }
    printSuccess(ICEBERG_CONFIG_COLLECTED_TEXT);
    if (Object.keys(tempIcebergConfig).length > 0) {
        return tempIcebergConfig;
    }
    return undefined;
}
function validateIcebergConfigBucketName(bucketName) {
    if (bucketName.length < 3 || bucketName.length > 63) {
        throw new Error("Bucket name must be between 3 and 63 characters long.");
    }
    if (!/^[a-z0-9][a-z0-9-._]{1,220}[a-z0-9]$/.test(bucketName) || bucketName.includes("..")) {
        throw new Error("Invalid bucket name. Must start and end with a letter or number, contain only lowercase letters, numbers, hyphens (-), underscores (_), and periods (.).");
    }
    if (bucketName.startsWith("goog") || bucketName.includes("--")) {
        throw new Error("Bucket name cannot start with 'goog' or contain '--'.");
    }
    if (bucketName.includes("google") || bucketName.includes("g00gle")) {
        throw new Error("Bucket name cannot contain 'google' or close misspellings such as 'g00gle'.");
    }
}
function validateIcebergConfigTableFolderRoot(tableFolderRoot) {
    if (!/^[a-zA-Z0-9][a-zA-Z0-9-._]{1,220}[a-zA-Z0-9]$/.test(tableFolderRoot) || tableFolderRoot.includes("..")) {
        throw new Error("Invalid input. Must start and end with a letter or number, contain only letters (a-z, A-Z), numbers, hyphens (-), underscores (_), and periods (.).");
    }
}
function validateIcebergConfigTableFolderSubpath(tableFolderSubpath) {
    if (!/^[a-zA-Z0-9][a-zA-Z0-9-._/]{1,220}[a-zA-Z0-9]$/.test(tableFolderSubpath) || tableFolderSubpath.includes("..")) {
        throw new Error("Invalid input. Must start and end with a letter or number, contain only letters (a-z, A-Z), numbers, hyphens (-), underscores (_), periods (.), and forward slashes (/). The sequence '..' is also disallowed.");
    }
    if (tableFolderSubpath.includes("./") || tableFolderSubpath.includes("../")) {
        throw new Error("Input cannot contain './' or '../'.");
    }
}
const ICEBERG_BUCKET_NAME_HINT = "The bucket name must comply with https://cloud.google.com/storage/docs/buckets#naming. If you do not want to provide a workflow-level default bucket name, leave this input empty.\n";
const ICEBERG_TABLE_FOLDER_ROOT_HINT = "Table folder root must start and end with a letter or a number. It can only contain letters, numbers, hyphens, underscores and periods. If you do not want to provide a workflow_level default table folder root, leave this input empty.\n";
const ICEBERG_TABLE_FOLDER_ROOT_SUBPATH_HINT = "Table folder subpath must start and end with a letter or a number. It can only contain letters, numbers, hyphens, underscores, periods and forward slashes. If you do not want to provide a workflow_level default table folder subpath, leave this input empty.\n";
const ICEBERG_CONFIG_PROMPT_HINT = "Set repository-level configuration for Iceberg bucket name, table folder root,  table folder subpath and connection. If you do not want to set a field, enter an empty string in response to the prompt.\n";
const ICEBERG_CONFIG_PROMPT_TEXT = "\n--- Iceberg Configuration ---\n";
const ICEBERG_CONFIG_COLLECTED_TEXT = "Iceberg configuration collected.\n";
const ICEBERG_BUCKET_NAME_PROMPT_QUESTION = "Enter the default Iceberg bucket name:";
const ICEBERG_TABLE_FOLDER_ROOT_PROMPT_QUESTION = "Enter the default Iceberg table folder root:";
const ICEBERG_TABLE_FOLDER_SUBPATH_PROMPT_QUESTION = "Enter the default Iceberg table folder subpath:";
const ICEBERG_CONNECTION_HINT = "The connection can have the form `{project}.{location}.{connection_id}` or `projects/{project}/locations/{location}/connections/{connection_id} or be set to DEFAULT. If you do not want to provide a workflow-level default connection, leave this input empty.\n";
const ICEBERG_CONNECTION_QUESTION = "Enter the default connection:";

const noColor = process.env.NO_COLOR && ["1", "true", "yes"].includes(process.env.NO_COLOR.toLowerCase());
const ansiColorCodes = Object.freeze({
    red: 91,
    green: 32,
    yellow: 93,
    cyan: 36
});
function output(text, ansiColorCode) {
    if (noColor) {
        return text;
    }
    return `\x1b[${ansiColorCode}m${text}\x1b[0m`;
}
const successOutput = (text) => output(text, ansiColorCodes.green);
const warningOutput = (text) => output(text, ansiColorCodes.yellow);
const errorOutput = (text) => output(text, ansiColorCodes.red);
const calloutOutput = (text) => output(text, ansiColorCodes.cyan);
const write = (stream, text, indentCount) => stream.write(`${"  ".repeat(indentCount)}${text}\n`);
const writeStdOut = (text, indentCount = 0) => write(process.stdout, text, indentCount);
const writeStdErr = (text, indentCount = 0) => write(process.stderr, text, indentCount);
const DEFAULT_PROMPT = "> ";
function question(questionText) {
    return prompt(questionText);
}
function interactiveQuestion(questionText) {
    if (!process.stdin.isTTY && process.env.DATAFORM_CLI_TEST_INPUTS !== undefined) {
        const testInput = getTestInput(questionText);
        print(`${questionText} ${testInput}\n`);
        return testInput;
    }
    print(`${questionText} `);
    return readlineSync__namespace.question("");
}
function getTestInput(questionText) {
    const envVar = process.env.DATAFORM_CLI_TEST_INPUTS;
    if (!envVar) {
        throw new Error("Environment variable DATAFORM_CLI_TEST_INPUTS not set.");
    }
    try {
        const parsed = JSON.parse(envVar);
        if (typeof parsed === 'object' && parsed !== null && !Array.isArray(parsed)) {
            const inputs = new Map(Object.entries(parsed).map(([key, value]) => [key, String(value)]));
            const trimmedQuestion = questionText.trim();
            const answer = inputs.get(trimmedQuestion);
            if (answer !== undefined) {
                print(`[TEST_INPUT for "${trimmedQuestion}"]: "${answer}"`);
                return answer;
            }
            else {
                throw new Error(`[MISSING TEST_INPUT for "${trimmedQuestion}"]`);
            }
        }
        else {
            throw new Error(`Failed to parse DATAFORM_CLI_TEST_INPUTS: Expected a JSON object, but got ${typeof parsed}.`);
        }
    }
    catch (e) {
        throw new Error(`Failed to parse DATAFORM_CLI_TEST_INPUTS: ${e.message || e}`);
    }
}
function selectionQuestion(questionText, options) {
    return readlineSync__namespace.keyInSelect(options, `${questionText}\n`, {
        cancel: false
    });
}
function prompt(questionText, options) {
    writeStdOut(questionText);
    return readlineSync__namespace.prompt(Object.assign(Object.assign({}, options), { prompt: (options && options.prompt && options.prompt + DEFAULT_PROMPT) || DEFAULT_PROMPT }));
}
function print(text) {
    writeStdOut(text);
}
function printSuccess(text) {
    writeStdOut(successOutput(text));
}
function printError(errorText, indentCount = 0) {
    writeStdErr(errorOutput(errorText), indentCount);
}
function printInitResult(result) {
    if (result.dirsCreated && result.dirsCreated.length) {
        writeStdOut(successOutput("Directories successfully created:"));
        result.dirsCreated.forEach(dir => writeStdOut(dir, 1));
    }
    if (result.filesWritten && result.filesWritten.length) {
        writeStdOut(successOutput("Files successfully written:"));
        result.filesWritten.forEach(file => writeStdOut(file, 1));
    }
}
function printInitCredsResult(writtenFilePath) {
    writeStdOut(successOutput("Credentials file successfully written:"));
    writeStdOut(writtenFilePath, 1);
    writeStdOut("To change connection settings, edit this file directly.");
}
function isInteractive({ stream = process.stdout } = {}) {
    return Boolean(stream && stream.isTTY &&
        process.env.TERM !== 'dumb' &&
        !('CI' in process.env));
}
var compiledGraphOutputType;
(function (compiledGraphOutputType) {
    compiledGraphOutputType["Json"] = "json";
    compiledGraphOutputType["Dot"] = "dot";
    compiledGraphOutputType["Summary"] = "summary";
})(compiledGraphOutputType || (compiledGraphOutputType = {}));
function printCompiledGraph(graph, outputType, quietCompilation) {
    const interactive = isInteractive();
    if (outputType === compiledGraphOutputType.Json) {
        writeStdOut(prettyJsonStringify(graph));
    }
    else if (outputType === compiledGraphOutputType.Dot) {
        writeStdOut(dotRepresentation(graph, interactive));
    }
    else {
        const actionCount = 0 +
            (graph.tables ? graph.tables.length : 0) +
            (graph.assertions ? graph.assertions.length : 0) +
            (graph.operations ? graph.operations.length : 0);
        writeStdOut(successOutput(`Compiled ${actionCount} action(s).`));
        if (graph.tables && graph.tables.length) {
            graph.tables.forEach(setOrValidateTableEnumType);
            writeStdOut(`${graph.tables.length} dataset(s)${quietCompilation ? "." : ":"}`);
            if (!quietCompilation) {
                graph.tables.forEach(compiledTable => {
                    writeStdOut(`${datasetString(compiledTable.target, tableTypeEnumToString(compiledTable.enumType), compiledTable.disabled)}`, 1);
                });
            }
        }
        if (graph.assertions && graph.assertions.length) {
            writeStdOut(`${graph.assertions.length} assertion(s)${quietCompilation ? "." : ":"}`);
            if (!quietCompilation) {
                graph.assertions.forEach(assertion => {
                    writeStdOut(assertionString(assertion.target, assertion.disabled), 1);
                });
            }
        }
        if (graph.operations && graph.operations.length) {
            writeStdOut(`${graph.operations.length} operation(s)${quietCompilation ? "." : ":"}`);
            if (!quietCompilation) {
                graph.operations.forEach(operation => {
                    writeStdOut(operationString(operation.target, operation.disabled), 1);
                });
            }
        }
    }
}
function formatStackTraceForQuietCompilation(compileError) {
    var _a, _b;
    if ((_a = compileError === null || compileError === void 0 ? void 0 : compileError.message) === null || _a === void 0 ? void 0 : _a.includes("Unexpected identifier")) {
        if (!compileError.stack) {
            return "";
        }
        const stackLines = ((_b = compileError.stack) === null || _b === void 0 ? void 0 : _b.split("\n")) || [];
        return stackLines.slice(0, Math.min(3, stackLines.length)).join("\n");
    }
    return "";
}
function printCompiledGraphErrors(graphErrors, quietCompilation) {
    if (graphErrors.compilationErrors && graphErrors.compilationErrors.length > 0) {
        printError("Compilation errors:", 1);
        graphErrors.compilationErrors.forEach(compileError => {
            writeStdErr(`${calloutOutput(compileError.fileName)}: ${errorOutput(quietCompilation ? (compileError.message + " " + formatStackTraceForQuietCompilation(compileError) || compileError.stack) : (compileError.stack || compileError.message))}`, 1);
        });
    }
}
function printTestResult(testResult) {
    writeStdOut(`${testResult.name}: ${testResult.successful ? successOutput("passed") : errorOutput("failed")}`);
    if (!testResult.successful) {
        testResult.messages.forEach(message => writeStdErr(message, 1));
    }
}
function printExecutionGraph(executionGraph, asJson) {
    if (asJson) {
        writeStdOut(prettyJsonStringify(executionGraph.toJSON()));
    }
    else {
        const actionsByType = {
            table: [],
            assertion: [],
            operation: []
        };
        executionGraph.actions.forEach(action => {
            if (!(action.type === "table" || action.type === "assertion" || action.type === "operation")) {
                throw new Error(`Unrecognized action type: ${action.type}`);
            }
            actionsByType[action.type].push(action);
        });
        const datasetActions = actionsByType.table;
        if (datasetActions && datasetActions.length) {
            writeStdOut(`${datasetActions.length} dataset(s):`);
            datasetActions.forEach(datasetAction => writeStdOut(datasetString(datasetAction.target, datasetAction.type, datasetAction.tasks.length === 0), 1));
        }
        const assertionActions = actionsByType.assertion;
        if (assertionActions && assertionActions.length) {
            writeStdOut(`${assertionActions.length} assertion(s):`);
            assertionActions.forEach(assertionAction => writeStdOut(assertionString(assertionAction.target, assertionAction.tasks.length === 0), 1));
        }
        const operationActions = actionsByType.operation;
        if (operationActions && operationActions.length) {
            writeStdOut(`${operationActions.length} operation(s):`);
            operationActions.forEach(operationAction => writeStdOut(operationString(operationAction.target, operationAction.tasks.length === 0), 1));
        }
    }
}
function printExecutedAction(executedAction, executionAction, dryRun) {
    const jobIds = executedAction.tasks
        .filter(task => { var _a, _b; return (_b = (_a = task.metadata) === null || _a === void 0 ? void 0 : _a.bigquery) === null || _b === void 0 ? void 0 : _b.jobId; })
        .map(task => task.metadata.bigquery.jobId);
    const bytesBilled = executedAction.tasks
        .filter(task => { var _a, _b; return (_b = (_a = task.metadata) === null || _a === void 0 ? void 0 : _a.bigquery) === null || _b === void 0 ? void 0 : _b.jobId; })
        .map(task => {
        var _a, _b, _c, _d;
        const bytes = (_d = (_c = (_b = (_a = task.metadata.bigquery) === null || _a === void 0 ? void 0 : _a.totalBytesBilled) === null || _b === void 0 ? void 0 : _b.toNumber) === null || _c === void 0 ? void 0 : _c.call(_b)) !== null && _d !== void 0 ? _d : 0;
        return formatBytesInHumanReadableFormat(bytes);
    });
    const executionSuffix = formatExecutionSuffix(jobIds, bytesBilled);
    switch (executedAction.status) {
        case dataform.ActionResult.ExecutionStatus.SUCCESSFUL: {
            switch (executionAction.type) {
                case "table": {
                    writeStdOut(`${successOutput(`Table ${dryRun ? "dry run success" : "created"}: `)} ${datasetString(executionAction.target, executionAction.tableType, executionAction.tasks.length === 0)}${executionSuffix}`);
                    return;
                }
                case "assertion": {
                    writeStdOut(`${successOutput(`Assertion ${dryRun ? "dry run success" : "passed"}: `)} ${assertionString(executionAction.target, executionAction.tasks.length === 0)}${executionSuffix}`);
                    return;
                }
                case "operation": {
                    writeStdOut(`${successOutput(`Operation ${dryRun ? "dry run success" : "completed successfully"}: `)} ${operationString(executionAction.target, executionAction.tasks.length === 0)}${executionSuffix}`);
                    return;
                }
            }
        }
        case dataform.ActionResult.ExecutionStatus.FAILED: {
            switch (executionAction.type) {
                case "table": {
                    writeStdErr(`${errorOutput("Dataset creation failed: ")} ${datasetString(executionAction.target, executionAction.tableType, executionAction.tasks.length === 0)}${executionSuffix}`);
                    break;
                }
                case "assertion": {
                    writeStdErr(`${errorOutput("Assertion failed: ")} ${assertionString(executionAction.target, executionAction.tasks.length === 0)}${executionSuffix}`);
                    break;
                }
                case "operation": {
                    writeStdErr(`${errorOutput("Operation failed: ")} ${operationString(executionAction.target, executionAction.tasks.length === 0)}${executionSuffix}`);
                    break;
                }
            }
            printExecutedActionErrors(executedAction, executionAction);
            return;
        }
        case dataform.ActionResult.ExecutionStatus.SKIPPED: {
            switch (executionAction.type) {
                case "table": {
                    writeStdOut(`${warningOutput("Skipping dataset creation: ")} ${datasetString(executionAction.target, executionAction.tableType, executionAction.tasks.length === 0)}`);
                    return;
                }
                case "assertion": {
                    writeStdOut(`${warningOutput("Skipping assertion execution: ")} ${assertionString(executionAction.target, executionAction.tasks.length === 0)}`);
                    return;
                }
                case "operation": {
                    writeStdOut(`${warningOutput("Skipping operation execution: ")} ${operationString(executionAction.target, executionAction.tasks.length === 0)}`);
                    return;
                }
            }
            return;
        }
        case dataform.ActionResult.ExecutionStatus.DISABLED: {
            switch (executionAction.type) {
                case "table": {
                    writeStdOut(`${warningOutput("Dataset creation disabled: ")} ${datasetString(executionAction.target, executionAction.tableType, executionAction.tasks.length === 0)}`);
                    return;
                }
                case "assertion": {
                    writeStdOut(`${warningOutput(`Assertion execution disabled: `)} ${assertionString(executionAction.target, executionAction.tasks.length === 0)}`);
                    return;
                }
                case "operation": {
                    writeStdOut(`${warningOutput(`Operation execution disabled: `)} ${operationString(executionAction.target, executionAction.tasks.length === 0)}`);
                    return;
                }
            }
            return;
        }
    }
}
function printFormatFilesResult(formatResults) {
    const sorted = formatResults.sort((a, b) => a.filename.localeCompare(b.filename));
    const successfulFormatResults = sorted.filter(result => !result.err && !result.needsFormatting);
    const needsFormattingResults = sorted.filter(result => !result.err && result.needsFormatting);
    const failedFormatResults = sorted.filter(result => !!result.err);
    if (successfulFormatResults.length > 0) {
        printSuccess("Successfully formatted:");
        successfulFormatResults.forEach(result => writeStdOut(result.filename, 1));
    }
    if (needsFormattingResults.length > 0) {
        printError("Files that need formatting:");
        needsFormattingResults.forEach(result => writeStdErr(result.filename, 1));
    }
    if (failedFormatResults.length > 0) {
        printError("Errors encountered during formatting:");
        failedFormatResults.forEach(result => writeStdOut(`${result.filename}: ${result.err.message}`, 1));
    }
}
function datasetString(target, datasetType, disabled) {
    return `${targetString(target)} [${datasetType}]${disabled ? " [disabled]" : ""}`;
}
function assertionString(target, disabled) {
    return `${targetString(target)}${disabled ? " [disabled]" : ""}`;
}
function operationString(target, disabled) {
    return `${targetString(target)}${disabled ? " [disabled]" : ""}`;
}
function plainTargetString(target) {
    return `${target.schema}.${target.name}`;
}
function targetString(target) {
    return calloutOutput(`${target.schema}.${target.name}`);
}
function dotRepresentation(graph, interactive) {
    var _a, _b, _c;
    const nodes = [];
    const edges = [];
    const formatTarget = interactive ? targetString : plainTargetString;
    (_a = graph.tables) === null || _a === void 0 ? void 0 : _a.forEach(table => {
        var _a;
        const nodeName = `${formatTarget(table.target)}`;
        nodes.push(`"${nodeName}" [label="${formatTarget(table.target)} [${tableTypeEnumToString(table.enumType)}]"]`);
        (_a = table.dependencyTargets) === null || _a === void 0 ? void 0 : _a.forEach(dependencyTarget => {
            edges.push(`"${formatTarget(dependencyTarget)}" -> "${nodeName}"`);
        });
    });
    (_b = graph.assertions) === null || _b === void 0 ? void 0 : _b.forEach(assertion => {
        var _a;
        const nodeName = `${formatTarget(assertion.target)}`;
        nodes.push(`"${nodeName}" [label="${formatTarget(assertion.target)}"]`);
        (_a = assertion.dependencyTargets) === null || _a === void 0 ? void 0 : _a.forEach(dependencyTarget => {
            edges.push(`"${formatTarget(dependencyTarget)}" -> "${nodeName}"`);
        });
    });
    (_c = graph.operations) === null || _c === void 0 ? void 0 : _c.forEach(operation => {
        var _a;
        const nodeName = `${formatTarget(operation.target)}`;
        nodes.push(`"${nodeName}" [label="${formatTarget(operation.target)}"`);
        (_a = operation.dependencyTargets) === null || _a === void 0 ? void 0 : _a.forEach(dependencyTarget => {
            edges.push(`"${formatTarget(dependencyTarget)}" -> "${nodeName}"`);
        });
    });
    return `digraph {\n${nodes.join(";\n")};\n${edges.join(";\n")};\n}`;
}
function printExecutedActionErrors(executedAction, executionAction) {
    const failingTasks = executedAction.tasks.filter(task => task.status === dataform.TaskResult.ExecutionStatus.FAILED);
    failingTasks.forEach((task, i) => {
        executionAction.tasks[i].statement.split("\n").forEach(line => {
            writeStdErr(`${DEFAULT_PROMPT}${line}`, 1);
        });
        printError(task.errorMessage, 1);
    });
}

function getBigQueryCredentials() {
    const locationIndex = selectionQuestion("Enter the location of your datasets:", [
        "US (default)",
        "EU",
        "other"
    ]);
    let location = locationIndex === 0 ? "US" : "EU";
    if (locationIndex === 2) {
        location = question("Enter the location's region name (e.g. 'asia-south1'):");
    }
    const isApplicationDefaultOrJSONKeyIndex = selectionQuestion("Do you wish to use Application Default Credentials or JSON Key:", ["ADC (default)", "JSON Key"]);
    if (isApplicationDefaultOrJSONKeyIndex === 0) {
        const projectId = question("Enter your billing project ID:");
        return {
            projectId,
            location
        };
    }
    const cloudCredentialsPath = actuallyResolve(question("Please follow the instructions at https://docs.dataform.co/dataform-cli#create-a-credentials-file/\n" +
        "to create and download a private key from the Google Cloud Console in JSON format.\n" +
        "(You can delete this file after credential initialization is complete.)\n\n" +
        "Enter the path to your Google Cloud private key file:"));
    if (!fs__namespace$1.existsSync(cloudCredentialsPath)) {
        throw new Error(`Google Cloud private key file "${cloudCredentialsPath}" does not exist!`);
    }
    const cloudCredentials = JSON.parse(fs__namespace$1.readFileSync(cloudCredentialsPath, "utf8"));
    return {
        projectId: cloudCredentials.project_id,
        credentials: fs__namespace$1.readFileSync(cloudCredentialsPath, "utf8"),
        location
    };
}

function createYargsCli(cli) {
    let yargsChain = yargs__default["default"](fixArgvForHelp());
    for (const command of cli.commands) {
        yargsChain = yargsChain.command(command.format, command.description, (yargsChainer) => createOptionsChain(yargsChainer, command), async (argv) => {
            const exitCode = await command.processFn(argv);
            process.exit(exitCode);
        });
    }
    return yargsChain;
}
function createOptionsChain(yargsChain, command) {
    const checks = [];
    for (const positionalOption of command.positionalOptions) {
        yargsChain = yargsChain.positional(positionalOption.name, positionalOption.option);
        if (positionalOption.check) {
            checks.push(positionalOption.check);
        }
    }
    for (const option of command.options) {
        yargsChain = yargsChain.option(option.name, option.option);
        if (option.check) {
            checks.push(option.check);
        }
    }
    yargsChain = yargsChain.check(argv => {
        checks.forEach(check => check(argv));
        return true;
    });
    return yargsChain;
}
function fixArgvForHelp() {
    const argvCopy = process.argv.slice(2);
    if (argvCopy[0] === "help") {
        argvCopy[0] = "--help";
    }
    return argvCopy;
}

const LEXER_STATE_NAMES = {
    SQL: "sql",
    JS_BLOCK: "jsBlock",
    JS_TEMPLATE_STRING: "jsTemplateString",
    INNER_SQL_BLOCK: "innerSqlBlock",
    SQL_SINGLE_QUOTE_STRING: "innerSingleQuote",
    SQL_DOUBLE_QUOTE_STRING: "innerDoubleQuote",
    SQL_TRIPLE_SINGLE_QUOTE_STRING: "innerTripleSingleQuote",
    SQL_TRIPLE_DOUBLE_QUOTE_STRING: "innerTripleDoubleQuote"
};
const SQL_LEXER_TOKEN_NAMES = {
    START_CONFIG: LEXER_STATE_NAMES.SQL + "_startConfig",
    START_JS: LEXER_STATE_NAMES.SQL + "_startJs",
    START_INCREMENTAL: LEXER_STATE_NAMES.SQL + "_startIncremental",
    START_PRE_OPERATIONS: LEXER_STATE_NAMES.SQL + "_startPreOperations",
    START_POST_OPERATIONS: LEXER_STATE_NAMES.SQL + "_startPostOperations",
    START_INPUT: LEXER_STATE_NAMES.SQL + "_startInput",
    STATEMENT_SEPERATOR: LEXER_STATE_NAMES.SQL + "_statementSeparator",
    SINGLE_LINE_COMMENT: LEXER_STATE_NAMES.SQL + "_singleLineComment",
    MULTI_LINE_COMMENT: LEXER_STATE_NAMES.SQL + "_multiLineComment",
    START_JS_PLACEHOLDER: LEXER_STATE_NAMES.SQL + "_startJsPlaceholder",
    BACKTICK: LEXER_STATE_NAMES.SQL + "_backtick",
    START_QUOTE_SINGLE: LEXER_STATE_NAMES.SQL + "_startQuoteSingle",
    START_QUOTE_DOUBLE: LEXER_STATE_NAMES.SQL + "_startQuoteDouble",
    START_TRIPLE_QUOTE_SINGLE: LEXER_STATE_NAMES.SQL + "_startTripleQuoteSingle",
    START_TRIPLE_QUOTE_DOUBLE: LEXER_STATE_NAMES.SQL + "_startTripleQuoteDouble",
    CAPTURE_EVERYTHING_ELSE: LEXER_STATE_NAMES.SQL + "_captureEverythingElse"
};
const JS_BLOCK_LEXER_TOKEN_NAMES = {
    SINGLE_LINE_COMMENT: LEXER_STATE_NAMES.JS_BLOCK + "_singleLineComment",
    MULTI_LINE_COMMENT: LEXER_STATE_NAMES.JS_BLOCK + "_multiLineComment",
    SINGLE_QUOTE_STRING: LEXER_STATE_NAMES.JS_BLOCK + "_singleQuoteString",
    DOUBLE_QUOTE_STRING: LEXER_STATE_NAMES.JS_BLOCK + "_doubleQuoteString",
    START_JS_TEMPLATE_STRING: LEXER_STATE_NAMES.JS_BLOCK + "_startJsTemplateString",
    START_JS_BLOCK: LEXER_STATE_NAMES.JS_BLOCK + "_startJsBlock",
    CLOSE_BLOCK: LEXER_STATE_NAMES.JS_BLOCK + "_closeBlock",
    CAPTURE_EVERYTHING_ELSE: LEXER_STATE_NAMES.JS_BLOCK + "_captureEverythingElse"
};
const JS_TEMPLATE_STRING_LEXER_TOKEN_NAMES = {
    ESCAPED_BACKSLASH: LEXER_STATE_NAMES.JS_TEMPLATE_STRING + "_escapedBackslash",
    ESCAPED_DOLLAR_BRACE: LEXER_STATE_NAMES.JS_TEMPLATE_STRING + "_escapedDollarBrace",
    START_JS_BLOCK: LEXER_STATE_NAMES.JS_TEMPLATE_STRING + "_startJsBlock",
    CLOSE_STRING: LEXER_STATE_NAMES.JS_TEMPLATE_STRING + "_closeString",
    CAPTURE_EVERYTHING_ELSE: LEXER_STATE_NAMES.JS_TEMPLATE_STRING + "_captureEverythingElse"
};
const INNER_SQL_BLOCK_LEXER_TOKEN_NAMES = {
    STATEMENT_SEPERATOR: LEXER_STATE_NAMES.INNER_SQL_BLOCK + "_statementSeparator",
    SINGLE_LINE_COMMENT: LEXER_STATE_NAMES.INNER_SQL_BLOCK + "_singleLineComment",
    MULTI_LINE_COMMENT: LEXER_STATE_NAMES.INNER_SQL_BLOCK + "_multiLineComment",
    START_JS_PLACEHOLDER: LEXER_STATE_NAMES.INNER_SQL_BLOCK + "_startJsPlaceholder",
    CLOSE_BLOCK: LEXER_STATE_NAMES.INNER_SQL_BLOCK + "_closeBlock",
    BACKTICK: LEXER_STATE_NAMES.INNER_SQL_BLOCK + "_backtick",
    START_QUOTE_SINGLE: LEXER_STATE_NAMES.INNER_SQL_BLOCK + "_startQuoteSingle",
    START_QUOTE_DOUBLE: LEXER_STATE_NAMES.INNER_SQL_BLOCK + "_startQuoteDouble",
    START_TRIPLE_QUOTE_SINGLE: LEXER_STATE_NAMES.INNER_SQL_BLOCK + "_startTripleQuoteSingle",
    START_TRIPLE_QUOTE_DOUBLE: LEXER_STATE_NAMES.INNER_SQL_BLOCK + "_startTripleQuoteDouble",
    CAPTURE_EVERYTHING_ELSE: LEXER_STATE_NAMES.INNER_SQL_BLOCK + "_captureEverythingElse"
};
const SQL_SINGLE_QUOTE_STRING_LEXER_TOKEN_NAMES = {
    ESCAPED_BACKSLASH: LEXER_STATE_NAMES.SQL_SINGLE_QUOTE_STRING + "_escapedBackslash",
    ESCAPED_QUOTE: LEXER_STATE_NAMES.SQL_SINGLE_QUOTE_STRING + "_escapedQuoteSingle",
    START_JS_PLACEHOLDER: LEXER_STATE_NAMES.SQL_SINGLE_QUOTE_STRING + "_startJsPlaceholder",
    CLOSE_QUOTE: LEXER_STATE_NAMES.SQL_SINGLE_QUOTE_STRING + "_closeQuoteSingle",
    CAPTURE_EVERYTHING_ELSE: LEXER_STATE_NAMES.SQL_SINGLE_QUOTE_STRING + "_captureEverythingElse"
};
const SQL_DOUBLE_QUOTE_STRING_LEXER_TOKEN_NAMES = {
    ESCAPED_BACKSLASH: LEXER_STATE_NAMES.SQL_DOUBLE_QUOTE_STRING + "_escapedBackslash",
    ESCAPED_QUOTE: LEXER_STATE_NAMES.SQL_DOUBLE_QUOTE_STRING + "_escapedQuoteDouble",
    START_JS_PLACEHOLDER: LEXER_STATE_NAMES.SQL_DOUBLE_QUOTE_STRING + "_startJsPlaceholder",
    CLOSE_QUOTE: LEXER_STATE_NAMES.SQL_DOUBLE_QUOTE_STRING + "_closeQuoteDouble",
    CAPTURE_EVERYTHING_ELSE: LEXER_STATE_NAMES.SQL_DOUBLE_QUOTE_STRING + "_captureEverythingElse"
};
const SQL_TRIPLE_SINGLE_QUOTE_STRING_LEXER_TOKEN_NAMES = {
    ESCAPED_BACKSLASH: LEXER_STATE_NAMES.SQL_TRIPLE_SINGLE_QUOTE_STRING + "_escapedBackslash",
    START_JS_PLACEHOLDER: LEXER_STATE_NAMES.SQL_TRIPLE_SINGLE_QUOTE_STRING + "_startJsPlaceholder",
    CLOSE_QUOTE: LEXER_STATE_NAMES.SQL_TRIPLE_SINGLE_QUOTE_STRING + "_closeTripleQuoteSingle",
    CAPTURE_EVERYTHING_ELSE: LEXER_STATE_NAMES.SQL_TRIPLE_SINGLE_QUOTE_STRING + "_captureEverythingElse"
};
const SQL_TRIPLE_DOUBLE_QUOTE_STRING_LEXER_TOKEN_NAMES = {
    ESCAPED_BACKSLASH: LEXER_STATE_NAMES.SQL_TRIPLE_DOUBLE_QUOTE_STRING + "_escapedBackslash",
    START_JS_PLACEHOLDER: LEXER_STATE_NAMES.SQL_TRIPLE_DOUBLE_QUOTE_STRING + "_startJsPlaceholder",
    CLOSE_QUOTE: LEXER_STATE_NAMES.SQL_TRIPLE_DOUBLE_QUOTE_STRING + "_closeTripleQuoteDouble",
    CAPTURE_EVERYTHING_ELSE: LEXER_STATE_NAMES.SQL_TRIPLE_DOUBLE_QUOTE_STRING + "_captureEverythingElse"
};
const lexer = moo__namespace.states(buildSqlxLexer());
var SyntaxTreeNodeType;
(function (SyntaxTreeNodeType) {
    SyntaxTreeNodeType[SyntaxTreeNodeType["JAVASCRIPT"] = 0] = "JAVASCRIPT";
    SyntaxTreeNodeType[SyntaxTreeNodeType["JAVASCRIPT_TEMPLATE_STRING_PLACEHOLDER"] = 1] = "JAVASCRIPT_TEMPLATE_STRING_PLACEHOLDER";
    SyntaxTreeNodeType[SyntaxTreeNodeType["SQL"] = 2] = "SQL";
    SyntaxTreeNodeType[SyntaxTreeNodeType["SQL_COMMENT"] = 3] = "SQL_COMMENT";
    SyntaxTreeNodeType[SyntaxTreeNodeType["SQL_LITERAL_STRING"] = 4] = "SQL_LITERAL_STRING";
    SyntaxTreeNodeType[SyntaxTreeNodeType["SQL_LITERAL_MULTILINE_STRING"] = 5] = "SQL_LITERAL_MULTILINE_STRING";
    SyntaxTreeNodeType[SyntaxTreeNodeType["SQL_STATEMENT_SEPARATOR"] = 6] = "SQL_STATEMENT_SEPARATOR";
})(SyntaxTreeNodeType || (SyntaxTreeNodeType = {}));
const START_TOKEN_NODE_MAPPINGS = new Map([
    [SQL_LEXER_TOKEN_NAMES.START_CONFIG, SyntaxTreeNodeType.JAVASCRIPT],
    [SQL_LEXER_TOKEN_NAMES.START_INCREMENTAL, SyntaxTreeNodeType.SQL],
    [SQL_LEXER_TOKEN_NAMES.START_INPUT, SyntaxTreeNodeType.SQL],
    [SQL_LEXER_TOKEN_NAMES.START_JS, SyntaxTreeNodeType.JAVASCRIPT],
    [
        SQL_LEXER_TOKEN_NAMES.START_JS_PLACEHOLDER,
        SyntaxTreeNodeType.JAVASCRIPT_TEMPLATE_STRING_PLACEHOLDER
    ],
    [SQL_LEXER_TOKEN_NAMES.START_POST_OPERATIONS, SyntaxTreeNodeType.SQL],
    [SQL_LEXER_TOKEN_NAMES.START_PRE_OPERATIONS, SyntaxTreeNodeType.SQL],
    [SQL_LEXER_TOKEN_NAMES.START_QUOTE_SINGLE, SyntaxTreeNodeType.SQL_LITERAL_STRING],
    [SQL_LEXER_TOKEN_NAMES.START_QUOTE_DOUBLE, SyntaxTreeNodeType.SQL_LITERAL_STRING],
    [
        SQL_LEXER_TOKEN_NAMES.START_TRIPLE_QUOTE_SINGLE,
        SyntaxTreeNodeType.SQL_LITERAL_MULTILINE_STRING
    ],
    [
        SQL_LEXER_TOKEN_NAMES.START_TRIPLE_QUOTE_DOUBLE,
        SyntaxTreeNodeType.SQL_LITERAL_MULTILINE_STRING
    ],
    [JS_BLOCK_LEXER_TOKEN_NAMES.START_JS_BLOCK, SyntaxTreeNodeType.JAVASCRIPT],
    [JS_TEMPLATE_STRING_LEXER_TOKEN_NAMES.START_JS_BLOCK, SyntaxTreeNodeType.JAVASCRIPT],
    [
        INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.START_JS_PLACEHOLDER,
        SyntaxTreeNodeType.JAVASCRIPT_TEMPLATE_STRING_PLACEHOLDER
    ],
    [INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.START_QUOTE_SINGLE, SyntaxTreeNodeType.SQL_LITERAL_STRING],
    [INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.START_QUOTE_DOUBLE, SyntaxTreeNodeType.SQL_LITERAL_STRING],
    [
        INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.START_TRIPLE_QUOTE_SINGLE,
        SyntaxTreeNodeType.SQL_LITERAL_MULTILINE_STRING
    ],
    [
        INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.START_TRIPLE_QUOTE_DOUBLE,
        SyntaxTreeNodeType.SQL_LITERAL_MULTILINE_STRING
    ],
    [
        SQL_SINGLE_QUOTE_STRING_LEXER_TOKEN_NAMES.START_JS_PLACEHOLDER,
        SyntaxTreeNodeType.JAVASCRIPT_TEMPLATE_STRING_PLACEHOLDER
    ],
    [
        SQL_DOUBLE_QUOTE_STRING_LEXER_TOKEN_NAMES.START_JS_PLACEHOLDER,
        SyntaxTreeNodeType.JAVASCRIPT_TEMPLATE_STRING_PLACEHOLDER
    ],
    [
        SQL_TRIPLE_SINGLE_QUOTE_STRING_LEXER_TOKEN_NAMES.START_JS_PLACEHOLDER,
        SyntaxTreeNodeType.JAVASCRIPT_TEMPLATE_STRING_PLACEHOLDER
    ],
    [
        SQL_TRIPLE_DOUBLE_QUOTE_STRING_LEXER_TOKEN_NAMES.START_JS_PLACEHOLDER,
        SyntaxTreeNodeType.JAVASCRIPT_TEMPLATE_STRING_PLACEHOLDER
    ]
]);
const CLOSE_TOKEN_TYPES = new Set([
    JS_BLOCK_LEXER_TOKEN_NAMES.CLOSE_BLOCK,
    INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.CLOSE_BLOCK,
    SQL_SINGLE_QUOTE_STRING_LEXER_TOKEN_NAMES.CLOSE_QUOTE,
    SQL_DOUBLE_QUOTE_STRING_LEXER_TOKEN_NAMES.CLOSE_QUOTE,
    SQL_TRIPLE_SINGLE_QUOTE_STRING_LEXER_TOKEN_NAMES.CLOSE_QUOTE,
    SQL_TRIPLE_DOUBLE_QUOTE_STRING_LEXER_TOKEN_NAMES.CLOSE_QUOTE
]);
const WHOLE_TOKEN_NODE_MAPPINGS = new Map([
    [SQL_LEXER_TOKEN_NAMES.MULTI_LINE_COMMENT, SyntaxTreeNodeType.SQL_COMMENT],
    [SQL_LEXER_TOKEN_NAMES.SINGLE_LINE_COMMENT, SyntaxTreeNodeType.SQL_COMMENT],
    [SQL_LEXER_TOKEN_NAMES.STATEMENT_SEPERATOR, SyntaxTreeNodeType.SQL_STATEMENT_SEPARATOR],
    [INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.MULTI_LINE_COMMENT, SyntaxTreeNodeType.SQL_COMMENT],
    [INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.SINGLE_LINE_COMMENT, SyntaxTreeNodeType.SQL_COMMENT],
    [
        INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.STATEMENT_SEPERATOR,
        SyntaxTreeNodeType.SQL_STATEMENT_SEPARATOR
    ]
]);
class SyntaxTreeNode {
    constructor(type, allChildren = []) {
        this.type = type;
        this.allChildren = allChildren;
    }
    static create(code) {
        const parentNode = new SyntaxTreeNode(SyntaxTreeNodeType.SQL);
        let currentNode = parentNode;
        const nodeStack = [currentNode];
        lexer.reset(code);
        for (const token of lexer) {
            if (!token.type) {
                throw new Error("Undefined token type encountered.");
            }
            if (START_TOKEN_NODE_MAPPINGS.has(token.type)) {
                const childType = START_TOKEN_NODE_MAPPINGS.get(token.type);
                if (childType === SyntaxTreeNodeType.SQL && currentNode.type !== SyntaxTreeNodeType.SQL) {
                    throw new Error("SQL syntax tree nodes may only be children of other SQL nodes.");
                }
                const newCurrentNode = new SyntaxTreeNode(childType, [token.value]);
                nodeStack.push(newCurrentNode);
                currentNode.push(newCurrentNode);
                currentNode = newCurrentNode;
            }
            else if (CLOSE_TOKEN_TYPES.has(token.type)) {
                currentNode.push(token.value);
                nodeStack.pop();
                currentNode = nodeStack[nodeStack.length - 1];
            }
            else if (WHOLE_TOKEN_NODE_MAPPINGS.has(token.type)) {
                currentNode.push(new SyntaxTreeNode(WHOLE_TOKEN_NODE_MAPPINGS.get(token.type)).push(token.value));
            }
            else {
                currentNode.push(token.value);
            }
        }
        return parentNode;
    }
    static isSyntaxTreeNode(node) {
        return typeof node !== "string";
    }
    children() {
        return this.allChildren.slice();
    }
    concatenate(mutators) {
        const mutator = (mutators === null || mutators === void 0 ? void 0 : mutators.has(this.type)) ? mutators.get(this.type) : (str) => str;
        return this.allChildren
            .map(child => {
            if (typeof child === "string") {
                return mutator(child);
            }
            return child.concatenate();
        })
            .join("");
    }
    push(child) {
        if (this.allChildren.length > 0 &&
            typeof child === "string" &&
            typeof this.allChildren[this.allChildren.length - 1] === "string") {
            this.allChildren[this.allChildren.length - 1] =
                this.allChildren[this.allChildren.length - 1] + child;
            return this;
        }
        this.allChildren.push(child);
        return this;
    }
    equals(other) {
        if (this.type !== other.type) {
            return false;
        }
        if (this.allChildren.length !== other.children().length) {
            return false;
        }
        const areEqual = (first, second) => {
            if (typeof first !== typeof second) {
                return false;
            }
            if (typeof first === "string" || typeof second === "string") {
                return first === second;
            }
            return first.equals(second);
        };
        if (this.allChildren.some((child, index) => !areEqual(child, other.children()[index]))) {
            return false;
        }
        return true;
    }
}
function buildSqlxLexer() {
    const sqlLexer = {};
    sqlLexer[SQL_LEXER_TOKEN_NAMES.START_CONFIG] = {
        match: "config {",
        push: LEXER_STATE_NAMES.JS_BLOCK
    };
    sqlLexer[SQL_LEXER_TOKEN_NAMES.START_JS] = {
        match: "js {",
        push: LEXER_STATE_NAMES.JS_BLOCK
    };
    sqlLexer[SQL_LEXER_TOKEN_NAMES.START_INCREMENTAL] = {
        match: "incremental_where {",
        push: LEXER_STATE_NAMES.INNER_SQL_BLOCK
    };
    sqlLexer[SQL_LEXER_TOKEN_NAMES.START_PRE_OPERATIONS] = {
        match: "pre_operations {",
        push: LEXER_STATE_NAMES.INNER_SQL_BLOCK
    };
    sqlLexer[SQL_LEXER_TOKEN_NAMES.START_POST_OPERATIONS] = {
        match: "post_operations {",
        push: LEXER_STATE_NAMES.INNER_SQL_BLOCK
    };
    sqlLexer[SQL_LEXER_TOKEN_NAMES.START_INPUT] = {
        match: /input "[a-zA-Z0-9_-]+"(?:,\s*"[a-zA-Z0-9_-]+")* {/,
        push: LEXER_STATE_NAMES.INNER_SQL_BLOCK
    };
    sqlLexer[SQL_LEXER_TOKEN_NAMES.STATEMENT_SEPERATOR] = /[^\S\r\n]*---[^\S\r\n]*$/;
    sqlLexer[SQL_LEXER_TOKEN_NAMES.SINGLE_LINE_COMMENT] = /--.*?$/;
    sqlLexer[SQL_LEXER_TOKEN_NAMES.MULTI_LINE_COMMENT] = /\/\*[\s\S]*?\*\//;
    sqlLexer[SQL_LEXER_TOKEN_NAMES.START_JS_PLACEHOLDER] = {
        match: "${",
        push: LEXER_STATE_NAMES.JS_BLOCK
    };
    sqlLexer[SQL_LEXER_TOKEN_NAMES.BACKTICK] = "`";
    sqlLexer[SQL_LEXER_TOKEN_NAMES.START_TRIPLE_QUOTE_SINGLE] = {
        match: "'''",
        push: LEXER_STATE_NAMES.SQL_TRIPLE_SINGLE_QUOTE_STRING
    };
    sqlLexer[SQL_LEXER_TOKEN_NAMES.START_TRIPLE_QUOTE_DOUBLE] = {
        match: '"""',
        push: LEXER_STATE_NAMES.SQL_TRIPLE_DOUBLE_QUOTE_STRING
    };
    sqlLexer[SQL_LEXER_TOKEN_NAMES.START_QUOTE_SINGLE] = {
        match: "'",
        push: LEXER_STATE_NAMES.SQL_SINGLE_QUOTE_STRING
    };
    sqlLexer[SQL_LEXER_TOKEN_NAMES.START_QUOTE_DOUBLE] = {
        match: '"',
        push: LEXER_STATE_NAMES.SQL_DOUBLE_QUOTE_STRING
    };
    sqlLexer[SQL_LEXER_TOKEN_NAMES.CAPTURE_EVERYTHING_ELSE] = {
        match: /[\s\S]+?/,
        lineBreaks: true
    };
    const jsBlockLexer = {};
    jsBlockLexer[JS_BLOCK_LEXER_TOKEN_NAMES.SINGLE_LINE_COMMENT] = /\/\/.*?$/;
    jsBlockLexer[JS_BLOCK_LEXER_TOKEN_NAMES.MULTI_LINE_COMMENT] = /\/\*[\s\S]*?\*\//;
    jsBlockLexer[JS_BLOCK_LEXER_TOKEN_NAMES.SINGLE_QUOTE_STRING] = /'(?:\\['\\]|[^\n'\\])*'/;
    jsBlockLexer[JS_BLOCK_LEXER_TOKEN_NAMES.DOUBLE_QUOTE_STRING] = /"(?:\\["\\]|[^\n"\\])*"/;
    jsBlockLexer[JS_BLOCK_LEXER_TOKEN_NAMES.START_JS_TEMPLATE_STRING] = {
        match: "`",
        push: LEXER_STATE_NAMES.JS_TEMPLATE_STRING
    };
    jsBlockLexer[JS_BLOCK_LEXER_TOKEN_NAMES.START_JS_BLOCK] = {
        match: "{",
        push: LEXER_STATE_NAMES.JS_BLOCK
    };
    jsBlockLexer[JS_BLOCK_LEXER_TOKEN_NAMES.CLOSE_BLOCK] = { match: "}", pop: 1 };
    jsBlockLexer[JS_BLOCK_LEXER_TOKEN_NAMES.CAPTURE_EVERYTHING_ELSE] = {
        match: /[\s\S]+?/,
        lineBreaks: true
    };
    const jsTemplateStringLexer = {};
    jsTemplateStringLexer[JS_TEMPLATE_STRING_LEXER_TOKEN_NAMES.ESCAPED_BACKSLASH] = /\\\\/;
    jsTemplateStringLexer[JS_TEMPLATE_STRING_LEXER_TOKEN_NAMES.ESCAPED_DOLLAR_BRACE] = /\\\${`/;
    jsTemplateStringLexer[JS_TEMPLATE_STRING_LEXER_TOKEN_NAMES.START_JS_BLOCK] = {
        match: "${",
        push: LEXER_STATE_NAMES.JS_BLOCK
    };
    jsTemplateStringLexer[JS_TEMPLATE_STRING_LEXER_TOKEN_NAMES.CLOSE_STRING] = { match: "`", pop: 1 };
    jsTemplateStringLexer[JS_TEMPLATE_STRING_LEXER_TOKEN_NAMES.CAPTURE_EVERYTHING_ELSE] = {
        match: /[\s\S]+?/,
        lineBreaks: true
    };
    const innerSqlBlockLexer = {};
    innerSqlBlockLexer[INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.STATEMENT_SEPERATOR] = /[^\S\r\n]*---[^\S\r\n]*$/;
    innerSqlBlockLexer[INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.SINGLE_LINE_COMMENT] = /--.*?$/;
    innerSqlBlockLexer[INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.MULTI_LINE_COMMENT] = /\/\*[\s\S]*?\*\//;
    innerSqlBlockLexer[INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.START_JS_PLACEHOLDER] = {
        match: "${",
        push: LEXER_STATE_NAMES.JS_BLOCK
    };
    innerSqlBlockLexer[INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.CLOSE_BLOCK] = {
        match: "}",
        pop: 1
    };
    innerSqlBlockLexer[INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.BACKTICK] = "`";
    innerSqlBlockLexer[INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.START_TRIPLE_QUOTE_SINGLE] = {
        match: "'''",
        push: LEXER_STATE_NAMES.SQL_TRIPLE_SINGLE_QUOTE_STRING
    };
    innerSqlBlockLexer[INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.START_TRIPLE_QUOTE_DOUBLE] = {
        match: '"""',
        push: LEXER_STATE_NAMES.SQL_TRIPLE_DOUBLE_QUOTE_STRING
    };
    innerSqlBlockLexer[INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.START_QUOTE_SINGLE] = {
        match: "'",
        push: LEXER_STATE_NAMES.SQL_SINGLE_QUOTE_STRING
    };
    innerSqlBlockLexer[INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.START_QUOTE_DOUBLE] = {
        match: '"',
        push: LEXER_STATE_NAMES.SQL_DOUBLE_QUOTE_STRING
    };
    innerSqlBlockLexer[INNER_SQL_BLOCK_LEXER_TOKEN_NAMES.CAPTURE_EVERYTHING_ELSE] = {
        match: /[\s\S]+?/,
        lineBreaks: true
    };
    const innerSingleQuoteLexer = {};
    innerSingleQuoteLexer[SQL_SINGLE_QUOTE_STRING_LEXER_TOKEN_NAMES.ESCAPED_BACKSLASH] = "\\\\";
    innerSingleQuoteLexer[SQL_SINGLE_QUOTE_STRING_LEXER_TOKEN_NAMES.ESCAPED_QUOTE] = "\\'";
    innerSingleQuoteLexer[SQL_SINGLE_QUOTE_STRING_LEXER_TOKEN_NAMES.START_JS_PLACEHOLDER] = {
        match: "${",
        push: LEXER_STATE_NAMES.JS_BLOCK
    };
    innerSingleQuoteLexer[SQL_SINGLE_QUOTE_STRING_LEXER_TOKEN_NAMES.CLOSE_QUOTE] = {
        match: "'",
        pop: 1
    };
    innerSingleQuoteLexer[SQL_SINGLE_QUOTE_STRING_LEXER_TOKEN_NAMES.CAPTURE_EVERYTHING_ELSE] = {
        match: /[\s\S]+?/,
        lineBreaks: true
    };
    const innerDoubleQuoteLexer = {};
    innerDoubleQuoteLexer[SQL_DOUBLE_QUOTE_STRING_LEXER_TOKEN_NAMES.ESCAPED_BACKSLASH] = "\\\\";
    innerDoubleQuoteLexer[SQL_DOUBLE_QUOTE_STRING_LEXER_TOKEN_NAMES.ESCAPED_QUOTE] = {
        match: '\\"'
    };
    innerSingleQuoteLexer[SQL_DOUBLE_QUOTE_STRING_LEXER_TOKEN_NAMES.START_JS_PLACEHOLDER] = {
        match: "${",
        push: LEXER_STATE_NAMES.JS_BLOCK
    };
    innerDoubleQuoteLexer[SQL_DOUBLE_QUOTE_STRING_LEXER_TOKEN_NAMES.CLOSE_QUOTE] = {
        match: '"',
        pop: 1
    };
    innerDoubleQuoteLexer[SQL_DOUBLE_QUOTE_STRING_LEXER_TOKEN_NAMES.CAPTURE_EVERYTHING_ELSE] = {
        match: /[\s\S]+?/,
        lineBreaks: true
    };
    const innerTripleSingleQuoteLexer = {};
    innerTripleSingleQuoteLexer[SQL_TRIPLE_SINGLE_QUOTE_STRING_LEXER_TOKEN_NAMES.ESCAPED_BACKSLASH] =
        "\\\\";
    innerTripleSingleQuoteLexer[SQL_TRIPLE_SINGLE_QUOTE_STRING_LEXER_TOKEN_NAMES.START_JS_PLACEHOLDER] = {
        match: "${",
        push: LEXER_STATE_NAMES.JS_BLOCK
    };
    innerTripleSingleQuoteLexer[SQL_TRIPLE_SINGLE_QUOTE_STRING_LEXER_TOKEN_NAMES.CLOSE_QUOTE] = {
        match: "'''",
        pop: 1
    };
    innerTripleSingleQuoteLexer[SQL_TRIPLE_SINGLE_QUOTE_STRING_LEXER_TOKEN_NAMES.CAPTURE_EVERYTHING_ELSE] = {
        match: /[\s\S]+?/,
        lineBreaks: true
    };
    const innerTripleDoubleQuoteLexer = {};
    innerTripleDoubleQuoteLexer[SQL_TRIPLE_DOUBLE_QUOTE_STRING_LEXER_TOKEN_NAMES.ESCAPED_BACKSLASH] =
        "\\\\";
    innerTripleDoubleQuoteLexer[SQL_TRIPLE_DOUBLE_QUOTE_STRING_LEXER_TOKEN_NAMES.START_JS_PLACEHOLDER] = {
        match: "${",
        push: LEXER_STATE_NAMES.JS_BLOCK
    };
    innerTripleDoubleQuoteLexer[SQL_TRIPLE_DOUBLE_QUOTE_STRING_LEXER_TOKEN_NAMES.CLOSE_QUOTE] = {
        match: '"""',
        pop: 1
    };
    innerTripleDoubleQuoteLexer[SQL_TRIPLE_DOUBLE_QUOTE_STRING_LEXER_TOKEN_NAMES.CAPTURE_EVERYTHING_ELSE] = {
        match: /[\s\S]+?/,
        lineBreaks: true
    };
    const lexerStates = {};
    lexerStates[LEXER_STATE_NAMES.SQL] = sqlLexer;
    lexerStates[LEXER_STATE_NAMES.JS_BLOCK] = jsBlockLexer;
    lexerStates[LEXER_STATE_NAMES.JS_TEMPLATE_STRING] = jsTemplateStringLexer;
    lexerStates[LEXER_STATE_NAMES.INNER_SQL_BLOCK] = innerSqlBlockLexer;
    lexerStates[LEXER_STATE_NAMES.SQL_SINGLE_QUOTE_STRING] = innerSingleQuoteLexer;
    lexerStates[LEXER_STATE_NAMES.SQL_DOUBLE_QUOTE_STRING] = innerDoubleQuoteLexer;
    lexerStates[LEXER_STATE_NAMES.SQL_TRIPLE_SINGLE_QUOTE_STRING] = innerTripleSingleQuoteLexer;
    lexerStates[LEXER_STATE_NAMES.SQL_TRIPLE_DOUBLE_QUOTE_STRING] = innerTripleDoubleQuoteLexer;
    return lexerStates;
}

const JS_BEAUTIFY_OPTIONS = {
    indent_size: 2,
    preserve_newlines: true,
    max_preserve_newlines: 2
};
const queryFormatter = new googleSqlSyntaxTs.QueryFormatter(new googleSqlSyntaxTs.GoogleSqlDefinition());
function format(text, fileExtension) {
    try {
        switch (fileExtension) {
            case "sqlx":
                return postProcessFormattedSqlx(formatSqlx(SyntaxTreeNode.create(text), ""));
            case "js":
                return `${formatJavaScript(text).trim()}\n`;
            default:
                return text;
        }
    }
    catch (e) {
        throw new ErrorWithCause(`Unable to format "${text === null || text === void 0 ? void 0 : text.substring(0, 20)}...".`, e);
    }
}
async function formatFile(filename, options) {
    const fileExtension = filename.split(".").slice(-1)[0];
    const originalFileContent = await util.promisify(fs__namespace$1.readFile)(filename, "utf8");
    const formattedText = format(originalFileContent, fileExtension);
    if (formattedText !== format(formattedText, fileExtension)) {
        throw new Error("Formatter unable to determine final formatted form.");
    }
    const noWhiteSpaceFormatted = formattedText.replace(/\s/g, "");
    const noWhiteSpaceOriginal = originalFileContent.replace(/\s/g, "");
    if (noWhiteSpaceFormatted.length !== noWhiteSpaceOriginal.length) {
        const isLonger = noWhiteSpaceFormatted.length > noWhiteSpaceOriginal.length;
        throw new Error(`Formatter ${isLonger ? "added" : "removed"} non-whitespace characters`);
    }
    if (options && options.overwriteFile) {
        await util.promisify(fs__namespace$1.writeFile)(filename, formattedText);
    }
    return formattedText;
}
function formatSqlx(node, indent = "") {
    const { sqlxStatements, javascriptBlocks, innerSqlBlocks } = separateSqlxIntoParts(node.children());
    const formattedJsCodeBlocks = javascriptBlocks.map(jsCodeBlock => formatJavaScript(jsCodeBlock.concatenate()));
    const formattedSqlxStatements = sqlxStatements.map(sqlxStatement => {
        const placeholders = {};
        const unformattedPlaceholderSql = stripUnformattableText(sqlxStatement, placeholders).join("");
        const formattedPlaceholderSql = queryFormatter.formatQuery(unformattedPlaceholderSql);
        return formatEveryLine(replacePlaceholders(formattedPlaceholderSql, placeholders), line => `${indent}${line}`);
    });
    const formattedSqlCodeBlocks = innerSqlBlocks.map((sqlCodeBlock) => {
        const firstPart = sqlCodeBlock.children()[0];
        const upToFirstBrace = firstPart.slice(0, firstPart.indexOf("{") + 1);
        const lastPart = sqlCodeBlock.children()[sqlCodeBlock.children().length - 1];
        const lastBraceOnwards = lastPart.slice(lastPart.lastIndexOf("}"));
        const sqlCodeBlockWithoutOuterBraces = sqlCodeBlock.children().length === 1
            ? new SyntaxTreeNode(SyntaxTreeNodeType.SQL, [
                firstPart.slice(firstPart.indexOf("{") + 1, firstPart.lastIndexOf("}"))
            ])
            : new SyntaxTreeNode(SyntaxTreeNodeType.SQL, [
                firstPart.slice(firstPart.indexOf("{") + 1),
                ...sqlCodeBlock.children().slice(1, -1),
                lastPart.slice(0, lastPart.lastIndexOf("}"))
            ]);
        return `${upToFirstBrace}
${formatSqlx(sqlCodeBlockWithoutOuterBraces, "  ")}
${lastBraceOnwards}`;
    });
    const finalText = `
${formattedJsCodeBlocks.join("\n\n")}

${formattedSqlxStatements.join(`\n\n${indent}---\n\n`)}

${formattedSqlCodeBlocks.join("\n\n")}
`;
    return `${indent}${finalText.trim()}`;
}
function separateSqlxIntoParts(nodeContents) {
    const sqlxStatements = [[]];
    const javascriptBlocks = [];
    const innerSqlBlocks = [];
    nodeContents.forEach(child => {
        if (typeof child !== "string") {
            switch (child.type) {
                case SyntaxTreeNodeType.JAVASCRIPT:
                    javascriptBlocks.push(child);
                    return;
                case SyntaxTreeNodeType.SQL:
                    innerSqlBlocks.push(child);
                    return;
                case SyntaxTreeNodeType.SQL_STATEMENT_SEPARATOR:
                    sqlxStatements.push([]);
                    return;
            }
        }
        sqlxStatements[sqlxStatements.length - 1].push(child);
    });
    return {
        sqlxStatements,
        javascriptBlocks,
        innerSqlBlocks
    };
}
function stripUnformattableText(sqlxStatementParts, placeholders) {
    return sqlxStatementParts.map(part => {
        if (typeof part !== "string") {
            const placeholderId = generatePlaceholderId();
            switch (part.type) {
                case SyntaxTreeNodeType.SQL_LITERAL_STRING:
                case SyntaxTreeNodeType.SQL_LITERAL_MULTILINE_STRING:
                case SyntaxTreeNodeType.JAVASCRIPT_TEMPLATE_STRING_PLACEHOLDER: {
                    placeholders[placeholderId] = part;
                    return placeholderId;
                }
                case SyntaxTreeNodeType.SQL_COMMENT: {
                    const commentPlaceholderId = part.concatenate().startsWith("--")
                        ? `--${placeholderId}`
                        : `/*${placeholderId}*/`;
                    placeholders[commentPlaceholderId] = part;
                    return commentPlaceholderId;
                }
                default:
                    throw new Error(`Misplaced SyntaxTreeNodeType inside SQLX: ${part.type}`);
            }
        }
        return part;
    });
}
function generatePlaceholderId() {
    const uuid = typeidJs.typeid("p")
        .toString()
        .replace(/-/g, "");
    return "_" + uuid.substring(uuid.length - 16);
}
function replacePlaceholders(formattedSql, placeholders) {
    return Object.keys(placeholders).reduce((partiallyFormattedSql, placeholderId) => {
        const placeholderValue = placeholders[placeholderId];
        if (typeof placeholderValue === "string") {
            return partiallyFormattedSql.replace(placeholderId, placeholderValue);
        }
        return formatPlaceholderInSqlx(placeholderId, placeholderValue, partiallyFormattedSql);
    }, formattedSql);
}
function formatJavaScript(text) {
    return jsBeautify__namespace.js(text, JS_BEAUTIFY_OPTIONS);
}
function formatPlaceholderInSqlx(placeholderId, placeholderSyntaxNode, sqlx) {
    const wholeLine = getWholeLineContainingPlaceholderId(placeholderId, sqlx);
    if (!wholeLine) {
        return sqlx;
    }
    const indent = " ".repeat(wholeLine.length - wholeLine.trimStart().length);
    const formattedPlaceholder = formatSqlQueryPlaceholder(placeholderSyntaxNode, indent);
    if (placeholderSyntaxNode.type !== SyntaxTreeNodeType.SQL_COMMENT &&
        !formattedPlaceholder.includes("\n")) {
        return sqlx.replace(placeholderId, () => formattedPlaceholder.trim());
    }
    if (placeholderSyntaxNode.type === SyntaxTreeNodeType.SQL_LITERAL_MULTILINE_STRING) {
        return sqlx.replace(placeholderId, () => formattedPlaceholder.trim());
    }
    const [textBeforePlaceholder, textAfterPlaceholder] = wholeLine.split(placeholderId);
    const newLines = [];
    if (textBeforePlaceholder.trim().length > 0) {
        newLines.push(`${indent}${textBeforePlaceholder.trim()}`);
    }
    newLines.push(formattedPlaceholder);
    if (textAfterPlaceholder.trim().length > 0) {
        newLines.push(`${indent}${textAfterPlaceholder.trim()}`);
    }
    return sqlx.replace(wholeLine, newLines.join("\n"));
}
function formatSqlQueryPlaceholder(node, jsIndent) {
    switch (node.type) {
        case SyntaxTreeNodeType.JAVASCRIPT_TEMPLATE_STRING_PLACEHOLDER:
            return formatJavaScriptPlaceholder(node, jsIndent);
        case SyntaxTreeNodeType.SQL_LITERAL_STRING:
        case SyntaxTreeNodeType.SQL_COMMENT:
            return formatEveryLine(node.concatenate(), line => `${jsIndent}${line.trimStart()}`);
        case SyntaxTreeNodeType.SQL_LITERAL_MULTILINE_STRING:
            return `${jsIndent}${node.concatenate().trimStart()}`;
        default:
            throw new Error(`Unrecognized SyntaxTreeNodeType: ${node.type}`);
    }
}
function formatJavaScriptPlaceholder(node, jsIndent) {
    const formattedJs = formatJavaScript(node.concatenate());
    const textInsideBraces = formattedJs.slice(formattedJs.indexOf("{") + 1, formattedJs.lastIndexOf("}"));
    const finalJs = textInsideBraces.trim().includes("\n")
        ? `\${${textInsideBraces}}`
        : `\${${textInsideBraces.trim()}}`;
    return formatEveryLine(finalJs, line => `${jsIndent}${line}`);
}
function formatEveryLine(text, mapFn) {
    return text
        .split("\n")
        .map(mapFn)
        .join("\n");
}
function getWholeLineContainingPlaceholderId(placeholderId, text) {
    var _a;
    const regexpEscapedPlaceholderId = placeholderId.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    return (_a = text.match(new RegExp(".*" + regexpEscapedPlaceholderId + ".*"))) === null || _a === void 0 ? void 0 : _a[0];
}
function postProcessFormattedSqlx(formattedSql) {
    let previousLineHadContent = false;
    formattedSql = formattedSql.split("\n").reduce((accumulatedSql, currentLine) => {
        const lineHasContent = currentLine.trim().length > 0;
        if (lineHasContent) {
            previousLineHadContent = true;
            return `${accumulatedSql}\n${currentLine.trimEnd()}`;
        }
        if (previousLineHadContent) {
            previousLineHadContent = false;
            return `${accumulatedSql}\n`;
        }
        return accumulatedSql;
    }, "");
    return `${formattedSql.trim()}\n`;
}

const RECOMPILE_DELAY = 500;
process.on("unhandledRejection", async (reason) => {
    printError(`Unhandled promise rejection: ${(reason === null || reason === void 0 ? void 0 : reason.stack) || reason}`);
});
const projectDirOption = {
    name: "project-dir",
    option: {
        describe: "The Dataform project directory.",
        default: ".",
        coerce: actuallyResolve
    }
};
const projectDirMustExistOption = Object.assign(Object.assign({}, projectDirOption), { check: (argv) => {
        assertPathExists(argv[projectDirOption.name]);
        const dataformJsonPath = path__namespace.resolve(argv[projectDirOption.name], "dataform.json");
        const workflowSettingsYamlPath = path__namespace.resolve(argv[projectDirOption.name], "workflow_settings.yaml");
        if (!fs__namespace$1.existsSync(dataformJsonPath) && !fs__namespace$1.existsSync(workflowSettingsYamlPath)) {
            throw new Error(`${argv[projectDirOption.name]} does not appear to be a dataform directory (missing workflow_settings.yaml file).`);
        }
    } });
const fullRefreshOption = {
    name: "full-refresh",
    option: {
        describe: "Forces incremental tables to be rebuilt from scratch.",
        type: "boolean",
        default: false
    }
};
const splitCommas = (raw) => raw.map(value => value.split(",")).flat();
const requiresSelection = (name, actions, tags) => (argv) => {
    if (argv[name] && !(argv[actions.name] || argv[tags.name])) {
        throw new Error(`The --${name} flag should only be supplied along with --${actions.name} or --${tags.name}.`);
    }
};
const actionsOption = {
    name: "actions",
    option: {
        describe: "A list of action names or patterns to run. Can include '*' wildcards.",
        type: "array",
        coerce: splitCommas
    }
};
const tagsOption = {
    name: "tags",
    option: {
        describe: "A list of tags to filter the actions to run.",
        type: "array",
        coerce: splitCommas
    }
};
const includeDepsOption = {
    name: "include-deps",
    option: {
        describe: "If set, dependencies for selected actions will also be run.",
        type: "boolean"
    },
    check: requiresSelection("include-deps", actionsOption, tagsOption)
};
const includeDependentsOption = {
    name: "include-dependents",
    option: {
        describe: "If set, dependents (downstream) for selected actions will also be run.",
        type: "boolean"
    },
    check: requiresSelection("include-dependents", actionsOption, tagsOption)
};
const outputActionsOption = {
    name: "output-actions",
    option: {
        describe: "A list of action names to filter the compiled output to.",
        type: "array",
        coerce: splitCommas
    }
};
const outputTagsOption = {
    name: "output-tags",
    option: {
        describe: "A list of tags to filter the compiled output to.",
        type: "array",
        coerce: splitCommas
    }
};
const outputIncludeDepsOption = {
    name: "output-include-deps",
    option: {
        describe: "If set, dependencies of the selected actions are also included in the output.",
        type: "boolean"
    },
    check: requiresSelection("output-include-deps", outputActionsOption, outputTagsOption)
};
const outputIncludeDependentsOption = {
    name: "output-include-dependents",
    option: {
        describe: "If set, dependents (downstream) of the selected actions are also included in the output.",
        type: "boolean"
    },
    check: requiresSelection("output-include-dependents", outputActionsOption, outputTagsOption)
};
const credentialsOption = {
    name: "credentials",
    option: {
        describe: "The location of the credentials JSON file to use.",
        default: CREDENTIALS_FILENAME
    },
    check: (argv) => getCredentialsPath(argv[projectDirOption.name], argv[credentialsOption.name])
};
const jsonOutputOption = {
    name: "json",
    option: {
        describe: "Outputs a JSON representation of the compiled project or test results.",
        type: "boolean",
        default: false
    }
};
const dotOutputOption = {
    name: "dot",
    option: {
        describe: "Outputs a dot representation of the compiled project.",
        type: "boolean",
        default: false,
    },
    check: (argv) => {
        if (argv.json && argv.dot) {
            throw new Error("Arguments --json and --dot are mutually exclusive.");
        }
    }
};
const timeoutOption = {
    name: "timeout",
    option: {
        describe: "Duration to allow project compilation to complete. Examples: '1s', '10m', etc.",
        type: "string",
        default: null,
        coerce: (rawTimeoutString) => rawTimeoutString ? parseDuration__default["default"](rawTimeoutString) : null
    }
};
const jobPrefixOption = {
    name: "job-prefix",
    option: {
        describe: "Adds an additional prefix in the form of `dataform-${jobPrefix}-`.",
        type: "string",
        default: null
    }
};
const bigqueryJobLabelsOption = {
    name: "job-labels",
    option: {
        describe: "Comma-separated list of labels to add to BigQuery jobs, e.g. 'key1=val1,key2=val2'.",
        type: "string",
        coerce: (raw) => {
            const labels = {};
            raw === null || raw === void 0 ? void 0 : raw.split(",").forEach(kv => {
                if (!kv) {
                    return;
                }
                const [key, ...rest] = kv.split("=");
                labels[key] = rest.join("=") || "";
            });
            return labels;
        }
    }
};
const quietCompileOption = {
    name: "quiet",
    option: {
        describe: "Less verbose compilation output. Example usage: 'dataform compile --quiet'",
        type: "boolean",
        default: false
    }
};
const icebergOption = {
    name: "iceberg",
    option: {
        describe: "Initialize the project with workflow-level Iceberg tables configuration.",
        type: "boolean",
        default: false,
    },
};
const testConnectionOptionName = "test-connection";
const watchOptionName = "watch";
const verboseOptionName = "verbose";
const dryRunOptionName = "dry-run";
const runTestsOptionName = "run-tests";
const checkOptionName = "check";
const actionRetryLimitName = "action-retry-limit";
function getCredentialsPath(projectDir, credentialsPath) {
    return actuallyResolve(projectDir, credentialsPath);
}
function runCli() {
    const builtYargs = createYargsCli({
        commands: [
            {
                format: "help [command]",
                description: "Show help. If [command] is specified, the help is for the given command.",
                positionalOptions: [],
                options: [],
                processFn: async (argv) => {
                    return 0;
                }
            },
            {
                format: `init [${projectDirOption.name}] [${ProjectConfigOptions.defaultDatabase.name}]` +
                    ` [${ProjectConfigOptions.defaultLocation.name}]`,
                description: "Create a new dataform project.",
                positionalOptions: [
                    projectDirOption,
                    {
                        name: ProjectConfigOptions.defaultDatabase.name,
                        option: {
                            describe: "The default database to use, equivalent to Google Cloud Project ID."
                        },
                        check: (argv) => {
                            if (!argv[ProjectConfigOptions.defaultDatabase.name]) {
                                throw new Error(`The ${ProjectConfigOptions.defaultDatabase.name} positional argument is ` +
                                    `required. Use "dataform help init" for more info.`);
                            }
                        }
                    },
                    {
                        name: ProjectConfigOptions.defaultLocation.name,
                        option: {
                            describe: "The default location to use. See " +
                                "https://cloud.google.com/bigquery/docs/locations for supported values."
                        },
                        check: (argv) => {
                            if (!argv[ProjectConfigOptions.defaultLocation.name]) {
                                throw new Error(`The ${ProjectConfigOptions.defaultLocation.name} positional argument is ` +
                                    `required. Use "dataform help init" for more info.`);
                            }
                        }
                    }
                ],
                options: [icebergOption],
                processFn: async (argv) => {
                    const projectDir = argv[projectDirOption.name];
                    const projectConfig = {
                        defaultDatabase: argv[ProjectConfigOptions.defaultDatabase.name],
                        defaultLocation: argv[ProjectConfigOptions.defaultLocation.name],
                    };
                    if (argv[icebergOption.name]) {
                        const icebergConfig = promptForIcebergConfig();
                        if (icebergConfig) {
                            projectConfig.defaultIcebergConfig = icebergConfig;
                        }
                    }
                    print("Writing project files...\n");
                    const initResult = await init(projectDir, projectConfig);
                    printInitResult(initResult);
                    return 0;
                }
            },
            {
                format: `install [${projectDirMustExistOption.name}]`,
                description: "Install a project's NPM dependencies.",
                positionalOptions: [projectDirMustExistOption],
                options: [],
                processFn: async (argv) => {
                    print("Installing NPM dependencies...\n");
                    await install(argv[projectDirMustExistOption.name]);
                    printSuccess("Project dependencies successfully installed.");
                    return 0;
                }
            },
            {
                format: `init-creds [${projectDirMustExistOption.name}]`,
                description: `Create a ${CREDENTIALS_FILENAME} file for Dataform to use when ` +
                    `accessing BigQuery.`,
                positionalOptions: [projectDirMustExistOption],
                options: [
                    {
                        name: testConnectionOptionName,
                        option: {
                            describe: "If true, a test query will be run using your final credentials.",
                            type: "boolean",
                            default: true
                        }
                    }
                ],
                processFn: async (argv) => {
                    const finalCredentials = getBigQueryCredentials();
                    if (argv[testConnectionOptionName]) {
                        print("\nRunning connection test...");
                        const dbadapter = new BigQueryDbAdapter(finalCredentials);
                        const testResult = await test$1(dbadapter);
                        switch (testResult.status) {
                            case TestResultStatus.SUCCESSFUL: {
                                printSuccess("\nCredentials test query completed successfully.\n");
                                break;
                            }
                            case TestResultStatus.TIMED_OUT: {
                                throw new Error("Credentials test connection timed out.");
                            }
                            case TestResultStatus.OTHER_ERROR: {
                                throw new Error(`Credentials test query failed: ${testResult.error.stack ||
                                    testResult.error.message}`);
                            }
                        }
                    }
                    else {
                        print("\nCredentials test query was not run.\n");
                    }
                    const filePath = path__namespace.resolve(argv[projectDirMustExistOption.name], CREDENTIALS_FILENAME);
                    fs__namespace$1.writeFileSync(filePath, prettyJsonStringify(finalCredentials));
                    printInitCredsResult(filePath);
                    return 0;
                }
            },
            {
                format: `compile [${projectDirMustExistOption.name}]`,
                description: "Compile the dataform project. Produces JSON output describing the non-executable graph.",
                positionalOptions: [projectDirMustExistOption],
                options: [
                    {
                        name: watchOptionName,
                        option: {
                            describe: "Whether to watch the changes in the project directory.",
                            type: "boolean",
                            default: false
                        }
                    },
                    jsonOutputOption,
                    dotOutputOption,
                    timeoutOption,
                    quietCompileOption,
                    outputActionsOption,
                    outputTagsOption,
                    outputIncludeDepsOption,
                    outputIncludeDependentsOption,
                    {
                        name: verboseOptionName,
                        option: {
                            describe: "Enable verbose compilation output. Example usage: 'dataform compile --verbose'",
                            type: "boolean",
                            default: false
                        },
                        check: (argv) => {
                            if (argv.quiet && argv.verbose) {
                                throw new Error("Arguments --verbose and --quiet are mutually exclusive.");
                            }
                        }
                    },
                    ...ProjectConfigOptions.allYargsOptions
                ],
                processFn: async (argv) => {
                    const projectDir = argv[projectDirMustExistOption.name];
                    async function compileAndPrint() {
                        var _a, _b;
                        let outputType = compiledGraphOutputType.Summary;
                        if (argv[jsonOutputOption.name]) {
                            outputType = compiledGraphOutputType.Json;
                        }
                        else if (argv[dotOutputOption.name]) {
                            outputType = compiledGraphOutputType.Dot;
                        }
                        if (outputType === compiledGraphOutputType.Summary) {
                            print("Compiling...\n");
                        }
                        const compiledGraph = await compile({
                            projectDir,
                            projectConfigOverride: ProjectConfigOptions.constructProjectConfigOverride(argv),
                            timeoutMillis: argv[timeoutOption.name] || undefined,
                            verbose: argv[verboseOptionName] || false
                        });
                        const hasSelector = ((_a = argv[outputActionsOption.name]) === null || _a === void 0 ? void 0 : _a.length) > 0 || ((_b = argv[outputTagsOption.name]) === null || _b === void 0 ? void 0 : _b.length) > 0;
                        const outputGraph = hasSelector && !compiledGraphHasErrors(compiledGraph)
                            ? prune(compiledGraph, {
                                actions: argv[outputActionsOption.name],
                                tags: argv[outputTagsOption.name],
                                includeDependencies: argv[outputIncludeDepsOption.name],
                                includeDependents: argv[outputIncludeDependentsOption.name]
                            })
                            : compiledGraph;
                        printCompiledGraph(outputGraph, outputType, argv[quietCompileOption.name]);
                        if (compiledGraphHasErrors(compiledGraph)) {
                            print("");
                            printCompiledGraphErrors(compiledGraph.graphErrors, argv[quietCompileOption.name]);
                            return true;
                        }
                        return false;
                    }
                    const graphHasErrors = await compileAndPrint();
                    if (!argv[watchOptionName]) {
                        return graphHasErrors ? 1 : 0;
                    }
                    let watching = true;
                    let timeoutID = null;
                    let isCompiling = false;
                    const watcher = chokidar__namespace.watch(projectDir, {
                        ignored: /node_modules/,
                        persistent: true,
                        ignoreInitial: true,
                        awaitWriteFinish: {
                            stabilityThreshold: 1000,
                            pollInterval: 200
                        }
                    });
                    const printReady = () => {
                        print("\nWatching for changes...\n");
                    };
                    watcher
                        .on("ready", printReady)
                        .on("error", error => {
                        printError(`Error: ${error}`);
                        process.exit(1);
                    })
                        .on("all", () => {
                        if (timeoutID || isCompiling) {
                            clearTimeout(timeoutID);
                        }
                        timeoutID = setTimeout(async () => {
                            clearTimeout(timeoutID);
                            if (!isCompiling) {
                                isCompiling = true;
                                await compileAndPrint();
                                printReady();
                                isCompiling = false;
                            }
                        }, RECOMPILE_DELAY);
                    });
                    process.on("SIGINT", async () => {
                        await watcher.close();
                        watching = false;
                        process.exit(1);
                    });
                    while (watching) {
                        await new Promise((resolve, reject) => setTimeout(() => resolve(), 100));
                    }
                }
            },
            {
                format: `test [${projectDirMustExistOption.name}]`,
                description: "Run the dataform project's unit tests.",
                positionalOptions: [projectDirMustExistOption],
                options: [credentialsOption, timeoutOption, jsonOutputOption, ...ProjectConfigOptions.allYargsOptions],
                processFn: async (argv) => {
                    if (!argv[jsonOutputOption.name]) {
                        print("Compiling...\n");
                    }
                    const compiledGraph = await compile({
                        projectDir: argv[projectDirMustExistOption.name],
                        projectConfigOverride: ProjectConfigOptions.constructProjectConfigOverride(argv),
                        timeoutMillis: argv[timeoutOption.name] || undefined
                    });
                    if (compiledGraphHasErrors(compiledGraph)) {
                        printCompiledGraphErrors(compiledGraph.graphErrors, argv[quietCompileOption.name]);
                        return 1;
                    }
                    if (!argv[jsonOutputOption.name]) {
                        printSuccess("Compiled successfully.\n");
                    }
                    const readCredentials = read(getCredentialsPath(argv[projectDirOption.name], argv[credentialsOption.name]));
                    if (!compiledGraph.tests.length) {
                        printError("No unit tests found.");
                        return 1;
                    }
                    if (!argv[jsonOutputOption.name]) {
                        print(`Running ${compiledGraph.tests.length} unit tests...\n`);
                    }
                    const dbadapter = new BigQueryDbAdapter(readCredentials);
                    const testResults = await test(dbadapter, compiledGraph.tests);
                    if (!argv[jsonOutputOption.name]) {
                        testResults.forEach(testResult => printTestResult(testResult));
                    }
                    else {
                        print(prettyJsonStringify(testResults));
                    }
                    return testResults.every(testResult => testResult.successful) ? 0 : 1;
                }
            },
            {
                format: `run [${projectDirMustExistOption.name}]`,
                description: "Run the dataform project.",
                positionalOptions: [projectDirMustExistOption],
                options: [
                    {
                        name: dryRunOptionName,
                        option: {
                            describe: "If set, BigQuery will validate the run SQL without applying changes to the warehouse.",
                            type: "boolean"
                        }
                    },
                    {
                        name: runTestsOptionName,
                        option: {
                            describe: "If set, the project's unit tests are required to pass before running the project.",
                            type: "boolean"
                        }
                    },
                    {
                        name: actionRetryLimitName,
                        option: {
                            describe: "If set, idempotent actions will be retried up to the limit.",
                            type: "number",
                            default: 0
                        }
                    },
                    actionsOption,
                    credentialsOption,
                    fullRefreshOption,
                    includeDepsOption,
                    includeDependentsOption,
                    credentialsOption,
                    jsonOutputOption,
                    timeoutOption,
                    tagsOption,
                    bigqueryJobLabelsOption,
                    ...ProjectConfigOptions.allYargsOptions
                ],
                processFn: async (argv) => {
                    if (argv[jsonOutputOption.name] && !argv[dryRunOptionName]) {
                        print(`For execution, the --${jsonOutputOption.name} option is only supported if the ` +
                            `--${dryRunOptionName} option is enabled`);
                        return;
                    }
                    if (!argv[jsonOutputOption.name]) {
                        print("Compiling...\n");
                    }
                    const compiledGraph = await compile({
                        projectDir: argv[projectDirOption.name],
                        projectConfigOverride: ProjectConfigOptions.constructProjectConfigOverride(argv),
                        timeoutMillis: argv[timeoutOption.name] || undefined
                    });
                    if (compiledGraphHasErrors(compiledGraph)) {
                        printCompiledGraphErrors(compiledGraph.graphErrors, argv[quietCompileOption.name]);
                        return 1;
                    }
                    if (!argv[jsonOutputOption.name]) {
                        printSuccess("Compiled successfully.\n");
                    }
                    const readCredentials = read(getCredentialsPath(argv[projectDirOption.name], argv[credentialsOption.name]));
                    const dbadapter = new BigQueryDbAdapter(readCredentials);
                    const executionGraph = await build(compiledGraph, {
                        fullRefresh: argv[fullRefreshOption.name],
                        actions: argv[actionsOption.name],
                        includeDependencies: argv[includeDepsOption.name],
                        includeDependents: argv[includeDependentsOption.name],
                        tags: argv[tagsOption.name]
                    }, dbadapter);
                    if (argv[dryRunOptionName] && argv[jsonOutputOption.name]) {
                        printExecutionGraph(executionGraph, argv[jsonOutputOption.name]);
                        return;
                    }
                    if (argv[runTestsOptionName]) {
                        print(`Running ${compiledGraph.tests.length} unit tests...\n`);
                        const testResults = await test(dbadapter, compiledGraph.tests);
                        testResults.forEach(testResult => printTestResult(testResult));
                        if (testResults.some(testResult => !testResult.successful)) {
                            printError("\nUnit tests did not pass; aborting run.");
                            return 1;
                        }
                        printSuccess("Unit tests completed successfully.\n");
                    }
                    let bigqueryOptions = {
                        actionRetryLimit: argv[actionRetryLimitName]
                    };
                    if (argv[dryRunOptionName]) {
                        bigqueryOptions = Object.assign(Object.assign({}, bigqueryOptions), { dryRun: argv[dryRunOptionName] });
                    }
                    if (argv[jobPrefixOption.name]) {
                        bigqueryOptions = Object.assign(Object.assign({}, bigqueryOptions), { jobPrefix: argv[jobPrefixOption.name] });
                    }
                    if (argv[bigqueryJobLabelsOption.name]) {
                        bigqueryOptions = Object.assign(Object.assign({}, bigqueryOptions), { labels: argv[bigqueryJobLabelsOption.name] });
                    }
                    const actionsByName = new Map();
                    executionGraph.actions.forEach(action => {
                        actionsByName.set(targetAsReadableString(action.target), action);
                    });
                    if (actionsByName.size === 0) {
                        print("No actions to run.\n");
                        return 0;
                    }
                    if (argv[dryRunOptionName]) {
                        print("Dry running (no changes to the warehouse will be applied)...");
                    }
                    else {
                        print("Running...\n");
                    }
                    const runner = run(dbadapter, executionGraph, { bigquery: bigqueryOptions });
                    process.on("SIGINT", () => {
                        runner.cancel();
                    });
                    const alreadyPrintedActions = new Set();
                    const printExecutedGraph = (executedGraph) => {
                        executedGraph.actions
                            .filter(actionResult => actionResult.status !== dataform.ActionResult.ExecutionStatus.RUNNING)
                            .filter(executedAction => !alreadyPrintedActions.has(targetAsReadableString(executedAction.target)))
                            .forEach(executedAction => {
                            printExecutedAction(executedAction, actionsByName.get(targetAsReadableString(executedAction.target)), argv[dryRunOptionName]);
                            alreadyPrintedActions.add(targetAsReadableString(executedAction.target));
                        });
                    };
                    runner.onChange(printExecutedGraph);
                    const runResult = await runner.result();
                    printExecutedGraph(runResult);
                    return runResult.status === dataform.RunResult.ExecutionStatus.SUCCESSFUL ? 0 : 1;
                }
            },
            {
                format: `format [${projectDirMustExistOption.name}]`,
                description: "Format the dataform project's files.",
                positionalOptions: [projectDirMustExistOption],
                options: [
                    actionsOption,
                    {
                        name: checkOptionName,
                        option: {
                            describe: "Check if files are formatted correctly without modifying them.",
                            type: "boolean",
                            default: false
                        }
                    }
                ],
                processFn: async (argv) => {
                    let actions = ["{definitions,includes}/**/*.{js,sqlx}"];
                    if (actionsOption.name in argv && argv[actionsOption.name].length > 0) {
                        actions = argv[actionsOption.name];
                    }
                    const filenames = actions
                        .map((action) => glob__namespace.sync(action, { cwd: argv[projectDirMustExistOption.name] }))
                        .flat();
                    const isCheckMode = argv[checkOptionName];
                    const results = await Promise.all(filenames.map(async (filename) => {
                        try {
                            const filePath = path__namespace.resolve(argv[projectDirMustExistOption.name], filename);
                            if (isCheckMode) {
                                const fileContent = fs__namespace$1.readFileSync(filePath).toString();
                                const formattedContent = await formatFile(filePath, {
                                    overwriteFile: false
                                });
                                return {
                                    filename,
                                    needsFormatting: fileContent !== formattedContent
                                };
                            }
                            else {
                                await formatFile(filePath, {
                                    overwriteFile: true
                                });
                                return {
                                    filename
                                };
                            }
                        }
                        catch (e) {
                            return {
                                filename,
                                err: e
                            };
                        }
                    }));
                    printFormatFilesResult(results);
                    const failedFormatResults = results.filter(result => !!result.err);
                    if (failedFormatResults.length > 0) {
                        printError(`${failedFormatResults.length} file(s) failed to format.`);
                        return 1;
                    }
                    if (isCheckMode) {
                        const filesNeedingFormatting = results.filter(result => result.needsFormatting);
                        if (filesNeedingFormatting.length > 0) {
                            printError(`${filesNeedingFormatting.length} file(s) would be reformatted. Run the format command without --check to update.`);
                            return 1;
                        }
                        printSuccess("All files are formatted correctly!");
                    }
                    return 0;
                }
            }
        ]
    })
        .scriptName("dataform")
        .strict()
        .wrap(null)
        .recommendCommands()
        .fail(async (msg, err) => {
        if (!!err && err.name === "VMError" && err.message.includes("Cannot find module")) {
            printError("Could not find NPM dependencies. Have you run 'dataform install'?");
        }
        else {
            const message = (err === null || err === void 0 ? void 0 : err.message) ? err.message.split("\n")[0] : msg;
            printError(`Dataform encountered an error: ${message}`);
            if (err === null || err === void 0 ? void 0 : err.stack) {
                printError(err.stack);
            }
        }
        process.exit(1);
    }).argv;
    if (!builtYargs._[0]) {
        yargs__default["default"].showHelp();
    }
}
class ProjectConfigOptions {
    static constructProjectConfigOverride(argv) {
        const projectConfigOptions = {};
        if (argv[ProjectConfigOptions.defaultDatabase.name]) {
            projectConfigOptions.defaultDatabase = argv[ProjectConfigOptions.defaultDatabase.name];
        }
        if (argv[ProjectConfigOptions.defaultSchema.name]) {
            projectConfigOptions.defaultSchema = argv[ProjectConfigOptions.defaultSchema.name];
        }
        if (argv[ProjectConfigOptions.defaultLocation.name]) {
            projectConfigOptions.defaultLocation = argv[ProjectConfigOptions.defaultLocation.name];
        }
        if (argv[ProjectConfigOptions.assertionSchema.name]) {
            projectConfigOptions.assertionSchema = argv[ProjectConfigOptions.assertionSchema.name];
        }
        if (argv[ProjectConfigOptions.vars.name]) {
            projectConfigOptions.vars = argv[ProjectConfigOptions.vars.name];
        }
        if (argv[ProjectConfigOptions.databaseSuffix.name]) {
            projectConfigOptions.databaseSuffix = argv[ProjectConfigOptions.databaseSuffix.name];
        }
        if (argv[ProjectConfigOptions.schemaSuffix.name]) {
            projectConfigOptions.schemaSuffix = argv[ProjectConfigOptions.schemaSuffix.name];
        }
        if (argv[ProjectConfigOptions.tablePrefix.name]) {
            projectConfigOptions.tablePrefix = argv[ProjectConfigOptions.tablePrefix.name];
        }
        if (argv[ProjectConfigOptions.disableAssertions.name]) {
            projectConfigOptions.disableAssertions = argv[ProjectConfigOptions.disableAssertions.name];
        }
        if (argv[ProjectConfigOptions.defaultReservation.name]) {
            projectConfigOptions.defaultReservation = argv[ProjectConfigOptions.defaultReservation.name];
        }
        return projectConfigOptions;
    }
}
ProjectConfigOptions.defaultDatabase = {
    name: "default-database",
    option: {
        describe: "The default database to use, equivalent to Google Cloud Project ID. If unset, " +
            "the value from workflow_settings.yaml is used.",
        type: "string"
    }
};
ProjectConfigOptions.defaultSchema = {
    name: "default-schema",
    option: {
        describe: "Override for the default schema name. If unset, the value from workflow_settings.yaml is used."
    }
};
ProjectConfigOptions.defaultLocation = {
    name: "default-location",
    option: {
        describe: "The default location to use. See " +
            "https://cloud.google.com/bigquery/docs/locations for supported values. If unset, the " +
            "value from workflow_settings.yaml is used."
    }
};
ProjectConfigOptions.assertionSchema = {
    name: "assertion-schema",
    option: {
        describe: "Default assertion schema. If unset, the value from workflow_settings.yaml is used."
    }
};
ProjectConfigOptions.databaseSuffix = {
    name: "database-suffix",
    option: {
        describe: "Default assertion schema. If unset, the value from workflow_settings.yaml is used."
    }
};
ProjectConfigOptions.vars = {
    name: "vars",
    option: {
        describe: "Override for variables to inject via '--vars=someKey=someValue,a=b', referenced by " +
            "`dataform.projectConfig.vars.someValue`.  If unset, the value from workflow_settings.yaml is used.",
        type: "string",
        default: null,
        coerce: (rawVarsString) => {
            const variables = {};
            rawVarsString === null || rawVarsString === void 0 ? void 0 : rawVarsString.split(",").forEach(keyValueStr => {
                const [key, value] = keyValueStr.split("=");
                variables[key] = value;
            });
            return variables;
        }
    }
};
ProjectConfigOptions.schemaSuffix = {
    name: "schema-suffix",
    option: {
        describe: "A suffix to be appended to output schema names. If unset, the value from workflow_settings.yaml " +
            "is used."
    },
    check: (argv) => {
        if (argv[ProjectConfigOptions.schemaSuffix.name] &&
            !/^[a-zA-Z_0-9]+$/.test(argv[ProjectConfigOptions.schemaSuffix.name])) {
            throw new Error(`--${ProjectConfigOptions.schemaSuffix.name} should contain only ` +
                `alphanumeric characters and/or underscores.`);
        }
    }
};
ProjectConfigOptions.tablePrefix = {
    name: "table-prefix",
    option: {
        describe: "Adds a prefix for all table names. If unset, the value from workflow_settings.yaml is used."
    }
};
ProjectConfigOptions.disableAssertions = {
    name: "disable-assertions",
    option: {
        describe: "Disables all assertions including built-in assertions (uniqueKey, nonNull, rowConditions) and manual assertions (type: assertion).",
        type: "boolean",
        default: false
    }
};
ProjectConfigOptions.defaultReservation = {
    name: "default-reservation",
    option: {
        describe: "The default BigQuery reservation to use for execution. If unset, the value from " +
            "workflow_settings.yaml is used. If neither is set, default BigQuery behavior applies.",
        type: "string"
    }
};
ProjectConfigOptions.allYargsOptions = [
    ProjectConfigOptions.defaultDatabase,
    ProjectConfigOptions.defaultSchema,
    ProjectConfigOptions.defaultLocation,
    ProjectConfigOptions.assertionSchema,
    ProjectConfigOptions.vars,
    ProjectConfigOptions.databaseSuffix,
    ProjectConfigOptions.schemaSuffix,
    ProjectConfigOptions.tablePrefix,
    ProjectConfigOptions.disableAssertions,
    ProjectConfigOptions.defaultReservation
];

runCli();
