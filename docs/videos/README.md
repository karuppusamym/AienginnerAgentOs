# UI videos

This folder contains short screen recordings of the DataPilot UI navigation flow.

The current environment does not expose a direct browser screen-recorder API from this session, so the repository includes a placeholder note until a local desktop recorder is used.
