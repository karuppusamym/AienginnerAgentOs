# DataPilot Agent OS — Implementation Status Matrix

**Purpose:** a single completed / partial / not-completed checklist, reconciled directly against the code (not just prior documentation), for presentation and tracking use.
**Method:** cross-checked against `apps/api/app/routers/*.py` (177 endpoints across 26 domain routers), `apps/api/app/models.py`, and `apps/web/app/components/*.tsx`. Backend test suite independently re-run from a clean install (venv at `apps/api/.venv`): **98/98 passing**, all four test files (`test_api.py`, `test_connection_guard.py`, `test_new_endpoints.py`, `test_vector_store.py`) genuinely isolated. Frontend `npx tsc --noEmit` and `next build`: both clean. `pyflakes app/` run across the whole backend package: zero undefined-name findings — the specific bug class ("symbol used but never imported") that broke 8 routers in an earlier pass does not currently exist anywhere in the codebase.
**Last reconciled:** August 16, 2026 (seventeenth reconciliation pass, this revision — see change log). **Note: this masthead itself was stale between the eleventh and the fourteenth pass** — two full reconciliation passes (twelfth: RBAC/gateway-search fixes, 93/93 passing; thirteenth: a Docker 404 fix) had already landed and were reflected in the change log below, but this masthead and several §2 rows still described the eleventh pass's state as current. Corrected in the fourteenth pass, and flagged as a process gap there: **update the masthead in the same edit as any change-log entry that changes test counts or closes a gap**, not as a separate follow-up pass. This revision follows that rule.

> **Read this before trusting any status row below that predates this revision:** the previous revision of this document (the one ending in the "Superset dashboard-collision fix" change-log entry) was reconciled against code that, as of this pass, **did not actually run** — 8 routers referenced `require_permission`/`project_permissions`/`current_membership` without importing them, and every write path through those routers raised a live `NameError`. That prior revision's "70/70 passing" masthead claim was written either just before this regression landed or without re-running the suite after it. This revision fixes the regression and re-verifies from an actual green test run, not from reading the previous revision's prose.

Status legend: ✅ Done · 🟡 Partial · 🔴 Not started/Not certified

> **Note on how to read this document:** multiple independent rounds of work have landed in this codebase between revisions of this doc — not all of them mine, and at least one landed *while this exact revision was being written* (confirmed by a file-changed-on-disk warning mid-edit). This pass found real, working code for three things an earlier revision of this document had called out as open gaps: bounded multi-hop lineage traversal (`/lineage/graph`), permission-map-backed write authorization (`require_permission`), and PII detection/masking on governed query results (`app/pii.py`). Each was verified by reading the actual implementation and, where practical, exercising it — not by trusting a changelog line. The methodology stays constant: cross-check against the code as it stands right now, not against any round's — including this document's own previous version's — self-description.

## 1. Core product workflows

| Capability | Status | Evidence |
| --- | --- | --- |
| Local auth, roles, project scoping | ✅ | `/auth/*`, `/admin/users`, `/projects/*` |
| File upload/profile/mapping/staging (CSV, JSON, Excel, Parquet) | ✅ | `/files/*`, versioned replace/append/key-merge |
| Bulk catalog metadata import (CSV, no live data required) | ✅ | `POST /datasets/import` — upload a `schema_name,table_name,column_name,...` CSV, creates/updates `DataAsset` rows with `metadata_status="manual_import"`; UI in Dataset explorer ("Import catalog CSV") |
| Catalog + hybrid search (Postgres keyword + Qdrant vector) | ✅ | `/search`, `/datasets`, `/recommendations` |
| Manual metadata edit/enrichment (description, tags, owner, sensitivity, freshness, status) | ✅ | `PUT /datasets/{asset_id}` — admin/engineer-gated, audited. The edit modal in `DatasetsView.tsx` exposes all six fields |
| Column-level business names/descriptions | ✅ **New this session** | `PUT /datasets/{asset_id}` accepts `column_notes: {column_name: {business_name?, description?}}`, merged into the asset's stored `columns` JSON with a diffed audit trail. Fed into both SQL-generation grounding text (`_column_sql_context_entry()` in `main.py`) and keyword-search relevance (`grounding.py`'s `project_asset_search()` haystack), so a business name genuinely changes what the model sees and what search matches — not just cosmetic. `DatasetsView.tsx` edit modal has a per-column business-name/description editor. Directly answers "how do we update business names for table or columns" |
| SOP / business-glossary document upload (PDF or text) for grounding | ✅ **New this session** | `POST /glossary` (title + either an uploaded `.pdf`/`.txt`/`.md` file or `text_content`), `GET /glossary`, `DELETE /glossary/{id}`. PDF text extracted via `pypdf`; extracted text is split into overlapping ~1,500-char chunks (`_chunk_glossary_text()` in `routers/workspace.py`) and each chunk indexed into the vector store tagged `source_type: "glossary"`. `grounding.py`'s new `glossary_matches()` retrieves and cross-checks hits against the current project's own `GlossaryDocument` rows (the vector store has no server-side project filter, so this prevents cross-project leakage), and both `grounding_context()`/`grounding_prompt_text()` now include a "Relevant business glossary / SOP passages" section — flowing automatically into both `/sql/generate` and the agent planner (`temporal_activities.py`), no call-site changes needed since both already call `grounding_prompt_text()`. Backend-only for now — no dedicated frontend upload UI yet, same as external extractions when they shipped API-only. Covered by `test_z_glossary_document_upload_list_grounding_delete` |
| Database connection via native driver **or MCP** | ✅ | `POST /connectors` with `connection_mode: "direct"` or `"mcp"` (`mcp_server_url` + per-tool `upstream_tool_name`). Both modes selectable in the connector creation form |
| Metadata scan for MCP-connected sources | ✅ | `discover_mcp_metadata()` in `connector_runtime.py` calls `tools/list` on the upstream MCP server and turns each tool's schema into a cataloged asset. Verified against a local MCP fixture server end-to-end |
| Catalog-grounded, dialect-aware SQL generation | ✅ | `/sql/generate`, `/sql/execute` — enforces single read-only `SELECT`/`WITH` |
| SQL-generation relevance for large catalogs (>40 tables) | ✅ **Real bug fixed this session** | `_catalog_sql_context()` (in `main.py`) caps its output at 40 assets. The generate-endpoint prompt path was building this section from the raw alphabetical `catalog` list instead of the grounding-prioritized (relevant-first) ordering that was *already being computed* for the local-mock code path — so for any project with more than 40 tables, an actually-relevant table sorting past position 40 alphabetically was silently dropped from this section of the prompt, leaving only the separate top-5 `grounding_prompt_text()` section to carry relevance. Fixed in `routers/sql.py` by threading `prioritized_catalog` through to the real-provider path too. Directly answers "is context attached to the model relevant enough" for large catalogs |
| SQL query history | ✅ | `GET /sql/history` — persisted per-user/per-project query log |
| **PII detection + output masking** | ✅ **Verified this session** | `app/pii.py` — pattern/heuristic PII classification (email, phone, government ID, payment card, bank account, DOB, address, name) wired into local SQL execution (`staging.py`), connector queries including MCP (`connector_runtime.py`, 2 call sites), and ingested-file column annotation (`routers/files.py`). Applies partial masking (e.g. `j***@domain`, `****1234`) to result rows before they reach a caller. This is application-level enforcement on governed result paths — not a substitute for a managed DLP tool or database-native column security, and it's pattern/heuristic-based (column-name + regex), so it will miss PII in oddly-named or free-text columns |
| Persistent conversations with memory/summary | ✅ | `/conversations/*` |
| Data quality rules, runs, quarantine, remediation | ✅ | `/quality/*` |
| Pipeline generation + PostgreSQL view deployment | ✅ | `/pipelines/*`, approval-gated deploy |
| dbt/Dataform package emission | 🟡 | Real code generation exists (`pipeline_codegen/{planner,emitters,exporters,validators}.py`, ~1,100 lines) with per-target archive, delivery-config, and validate endpoints. What's still missing: no test run against an actual `dbt` CLI / Dataform project to confirm the emitted package is directly runnable, only structural validation |
| Lineage — flat list/audit view | ✅ | `GET /lineage` — filterable list of `LineageEdge` rows for list/audit UI use |
| Lineage — multi-hop traversal | ✅ **Verified this session, upgraded from 🟡** | `GET /lineage/graph?relation=...&direction=upstream\|downstream\|both&depth=1-20` — genuine bounded BFS traversal over `LineageEdge` adjacency, returns nodes with hop depth and directioned edges, capped at `depth<=20` to prevent unbounded walks. Read the full implementation (not just the route signature) to confirm this is real traversal, not a relabeled flat query. Distinct from `/semantic/graph` (catalog/join relationships, not pipeline data-flow) |
| Semantic relationship graph (catalog/join relationships) | ✅ **Real cross-source bug fixed, then corrected further for MCP connectors — see seventeenth-pass change-log entry** | `GET /semantic/graph` — nodes from `DataAsset`, edges from governed `SemanticJoinPolicy` plus inferred column-name-matched suggestions flagged `governed: false` for review. **Was suggesting joins across completely different external connectors** (e.g. a Postgres warehouse and a SQL Server warehouse that both happen to have a `customer_id` column) purely because column names matched, with no notion that `/sql/generate`/`/sql/execute` are always scoped to one connector (or the local workspace) at a time — those joins can never actually execute. User caught this live, from the graph rendering as a dense, near-fully-connected mesh with no visible structure. Fixed (fifteenth pass): inferred edges confined to assets that share a `group_key()`, each node carries a new `group`/`source_label` field, and the frontend clusters nodes into per-source arcs with distinct colors and a legend instead of one flat ring; `POST/PUT /semantic/joins` (`validate_semantic_join_policy()`) rejects a governed policy across two different connectors (422). **User caught a second, deeper issue (seventeenth pass): that connector-level grouping is only correct for direct-driver connectors.** An MCP-backed connector's assets are individually-discovered *tools* (`discover_mcp_metadata()` hardcodes `schema_name="mcp"` regardless of what backend a tool actually fronts), MCP execution invokes exactly one named tool per call with zero cross-tool join path anywhere in the runtime, and a single MCP toolbox can front more than one physically distinct backend (confirmed via `infra/mcp-toolbox/toolbox.yaml`'s two separate `kind: source` blocks) — so two tools on the very same connector must never be treated as joinable either. `group_key()` and `validate_semantic_join_policy()` now branch on `connection_mode`: direct connectors keep one bucket per `connector_id`; MCP connectors key every asset individually. The graph UI still colors/legends by `source_label` (so an MCP toolbox reads as one color, not N), while arc placement uses the finer-grained group so its tools render as visually distinct, non-linked clusters. Retrieval/generation itself was independently verified NOT to have the original cross-connector problem — see the grouping row in §4 below |
| Recurring schedules + watermarks | ✅ | `/schedules/*`, Temporal-backed, 15s poll |
| External DB extraction to staging | ✅ | `staging.py` — real, tested, read-only-enforced staging path wired into `connectors.py` |
| Schema drift detection + acknowledgment | ✅ | `/schema-drift`, `/schema-drift/{id}/acknowledge` |
| Agent registry (versioned instructions, tools, publish) | ✅ | `/agents/*` |
| Agent execution of governed SQL query tools (not just the internal ToolDefinition registry) | ✅ **Fixed this session — was a real gap** | Previously, `AgentDefinition.tool_names` only ever referenced `ToolDefinition` (the internal registry); the governed `QueryTool` registry (SQL tools with business metadata, generated by the wizard, verified via `/test`, published) was reachable only by external accessors over `/external/v1` and `/mcp` — an internal agent run had no way to invoke a published SQL tool at all. Added `AgentDefinition`/`AgentVersion.query_tool_names` (validated against published, project-scoped `QueryTool` rows on `POST /agents` and `POST /agents/{id}/versions`), surfaced resolved bindings as `query_tools` on `GET /agents/{id}`, and wired execution into `temporal_activities._execute_bound_tools()` so a bounded agent run invokes bound query tools through the same `execute_connector_query`/`execute_parameterized_read_only` path (and therefore the same PII masking, read-only enforcement, and governance logging) the external gateway uses. Covered by a new backend test (`test_agent_can_be_bound_to_a_published_query_tool_and_executes_it`); full backend suite 62/62 (was 56/56 in the prior revision of this document — count grew from added coverage, not from unrelated endpoint growth); frontend `AgentsView.tsx` updated to bind/display query tools, `tsc --noEmit` clean. See `docs/specs/agents.md` and `docs/specs/query_tools.md`. |
| Agent scorecard + governed promotion recommendation | ✅ | `GET /agents/{id}/scorecard` — pass_rate ≥ 80% AND zero golden-failure rule, persists `evaluation_score`, returns `promotion_recommendation`; never auto-publishes |
| Bounded agent orchestration via Temporal (planner, ≤12 tool calls) | ✅ | `/agents/runs`, `temporal_activities.py` |
| Internal tool registry (built-in + allowlisted HTTP) | ✅ **Materially fixed this session** | `/tools/*`. Was 🟡: `seed.py` declared 10 tools but only 5 had a working `ToolVersion`/handler; `sql.generate`, `file.profile`, `quality.run`, `pipeline.stage`, `schedule.run` were named on the SQL Analyst/Pipeline/Quality agents with zero implementation behind them (the Pipeline agent could do literally nothing; Quality could only search, never run a rule; SQL Analyst could never draft SQL). All 5 now have real handlers in `tool_runtime.py`, reusing the same code paths as their REST counterparts (`/sql/generate`'s deterministic fallback, `/quality/rules/{id}/run`, `/files/{id}/stage`, `/schedules/{id}/run`). High-risk/write tools (`pipeline.stage`, `schedule.run`) stay `requires_approval=True` and are never auto-selected by the bounded orchestration loop, which only ever picks `risk_level == "low"` tools. See `docs/specs/tools.md`. |
| External query-tool gateway (REST/OpenAPI/MCP) | ✅ | `/query-tools/*`, `/external/v1/*`, `/mcp`, `/.well-known/mcp.json` |
| External gateway rate limiting | ✅ **New this session** | `app/rate_limit.py` — Redis-backed fixed-window limiter (`EXTERNAL_QUERY_TOOL_RATE_LIMIT_PER_MINUTE`, default 60/client/min), wired into `_invoke_external_query_tool()` in `main.py`, the single choke-point both the REST invoke endpoint and the MCP `tools/call` handler share. Fails open if Redis is unreachable. Also puts `REDIS_URL` to use — it was provisioned in `compose.yaml`/k8s but referenced by zero application code before this |
| Query-tool creation wizard | ✅ | `GET /query-tools/relation-options` + `POST /query-tools/wizard/preview` back a full guided UI: relation → template → column → reviewable draft |
| Query-tool lifecycle (draft → tested → published → retired) | ✅ | `/query-tools/{id}/test`, `/publish`, `/retire`; status shown via `StatusPill` in the gateway list |
| Jobs, cancel/retry/diagnose, incidents | ✅ | `/jobs/*`, `/incidents/*` |
| Approvals | ✅ | `/approvals/*` |
| Artifacts: versions, diffs, comments, reviews | ✅ | `/artifacts/*` |
| Governed notebooks (markdown/SQL/restricted Python) | ✅ | `/notebooks/*` |
| Evaluations, replay, red-team suite, baselines | ✅ | `/evaluations/*` |
| Prompt versioning + rollback | ✅ | `/prompts/*` |
| Feedback → human-review learning-suggestion queue, with trend detection | ✅ | `POST /feedback` folds repeats within a 30-day window into one suggestion with `occurrence_count`/`severity`, capped `recent_signals`. Full review UI (Admin → Governance → Learning Loop) with a review-target-aware deep link into the relevant editor (see §"Self-learning" below) |
| Role-based landing pages | ✅ **Verified this session** | `roleLanding[user.role]` in `lib/constants.tsx`, applied on login in `page.tsx` |
| SQL "Why this result?" explainability drawer | ✅ **Verified this session** | `SQLView.tsx` — shows source/connector, model provider, validation status/risk/row-limit, and catalog/semantic/join grounding evidence in a modal |
| Retention policies | ✅ | `/retention-policies/*` |
| Audit event log | ✅ | `/audit` |
| Semantic metrics + approved joins, metric-to-asset binding | ✅ | `/semantic/*`, `SemanticMetric.asset_id` |
| Optional embedded Superset (guest token + editor handoff) | ✅ | `/analytics/*`, `superset_client.py` |
| Superset linking for SQL/notebook query results ("publish a result, hotlink to it") | ✅ **Real bug fixed this session** | Requested via `POST /analytics/publish-sql` (approval-gated: SQL is copied into `Approval.evidence`, revalidated read-only, re-executed for a live preview, and only registered in Superset on approval). The publish path existed from concurrent work, but its dashboard identity collided with the project's primary dashboard: both called `_dashboard_slug()` with only `project_slug`, so publishing any query silently overwrote whatever the project's main "Governed analytics" dashboard was showing. Fixed by threading an explicit `dashboard_key` (derived from the source artifact id, not the project) through `superset_client.get_embed_configuration()`/`create_guest_token()`, and added a dedicated `SupersetQueryDashboard` table (unique per project+artifact) so each published query keeps its own dashboard, dataset, and charts. Added the missing hotlink: `GET /analytics/queries/{artifact_id}` (publish status) and `POST /analytics/queries/{artifact_id}/guest-token` (scoped guest token), with a new `PublishedQueryAnalyticsModal` component wired into both `SQLView.tsx` ("Save artifact" → "Publish to Superset" → becomes "Open in Superset" once approved) and `NotebooksView.tsx` (same, for a notebook's SQL cell). Covered by an extended `test_saved_sql_and_notebook_can_be_approved_for_superset_publication`, which now also asserts the dashboard key is artifact-derived and exercises both new endpoints. |
| **Security posture dashboard** (prompt-injection / PII / toxic-content scoring, trend, top risks) | ✅ | `/security-overview`, keyword-classified `Incident` records |
| Conversation/SQL-result quick actions (publish as tool, eject to notebook, rename, per-message feedback) | ✅ **Fixed this session — all four were broken on arrival** | `ConversationsView.tsx`/`SQLView.tsx` — publish-as-tool and eject-to-notebook now send payloads matching `QueryToolCreate`/`NotebookSave`'s real required fields; feedback routes to the real `POST /feedback` with the real `helpful`/`not_helpful` enum; rename now works against a new `PUT /conversations/{id}` endpoint. "Convert to pipeline" was removed rather than left pointing at a nonexistent endpoint — see the change log for why that one needs a product decision, not a payload fix |
| Dataset → "Start analysis" deep link | ✅ | `DatasetsView.tsx`'s new `onStartAnalysis` seeds `ConversationsView` with a pre-filled question and connector via `page.tsx`'s `analysisSeed` state |
| Search/filter boxes on Approvals, Artifacts, Jobs, Pipelines, Quality, Semantic | ✅ | Client-side filtering, reuses the existing `.toolbar-search` styling; no backend changes needed |
| Quality rule deletion | ✅ | `DELETE /quality/rules/{rule_id}` — endpoint already existed, now exposed in the UI |
| **Agent self-healing on tool-call failure — new, previously undocumented** | ✅ **Found this pass, not in any prior revision** | `_reflect_on_tool_error()` in `temporal_activities.py`: when a bound tool call raises inside `_execute_bound_tools()`, and a real (non-`local_mock`) provider is configured, the model is prompted with the tool's schema, the objective, the parameters that failed, and the error text, and asked for a corrected parameter set. The response goes through the exact same guard as the primary parameter-fill tier (§8): strict JSON, `validate_parameters()`, and every foreign-key-shaped field must resolve to a real project-scoped row or the whole correction is discarded. Covered by `tests/test_new_endpoints.py::test_agentic_self_healing_reflection` (see §5 for a caveat about that test file's isolation) |
| **Agent plan reviewer/critic loop — new, previously undocumented** | ✅ **Found this pass, not in any prior revision** | `_review_plan_outputs()` in `temporal_activities.py`: after a bounded run's planned steps execute, a "Reviewer" pass (same provider-gated pattern — skipped for `local_mock`) is shown the objective, the plan, and the last 3 steps' outputs, and may propose up to 2 additional steps drawn only from the run's already-enabled agent set if the objective looks unmet. This is additive plan extension, not automatic execution — appended steps still flow through the same tool-risk/approval gating as the original plan. Covered by `test_agentic_reviewer_loop` |
| **Graph-based multi-hop join pathfinding for grounding — new, previously undocumented** | ✅ **Found this pass, not in any prior revision** | `_find_graph_join_paths()` in `grounding.py`: given a set of target asset ids and the project's `SemanticJoinPolicy` edges, does a graph walk (not a single-hop lookup) to find the chain of join policies connecting them — e.g. A→B→C→D resolves to the 3 policies on that path — so SQL-generation grounding can surface an indirect join path between two tables that aren't directly joined. Covered by `test_graph_rag_pathfinding` (3 unit cases: 1-hop, 3-hop, disconnected) |

## 2. Enterprise readiness

| Capability | Status | Evidence / gap |
| --- | --- | --- |
| Live SQL Server / Oracle / Teradata / BigQuery certification | 🟡 **Unblocked for SQL Server this pass** | The host-TCP/IP blocker is gone: `compose.yaml`'s `sqlserver-demo` service is a self-contained, Dockerized SQL Server 2022 instance (see rewritten `docs/SQLSERVER_EXPRESS_DEMO.md`) reachable over the Compose network with no host configuration, seeded with real tables via `infra/sqlserver-init/init.sql`, plus an MCP-mode path through `mcp-toolbox`. Both direct and MCP connector scans can be run live against it via `docker compose up` — not yet executed from this sandbox (no Docker daemon here), so still 🟡 pending a live run confirmation, but the blocker itself is resolved. **Oracle, Teradata, BigQuery remain fully open** — no live demo/certification path for any of the three, drivers/adapters installed and unit-exercised only |
| PingFederate / SSO end-to-end | 🔴 | Admin config contract exists; full OIDC login/callback/logout/group-mapping not wired |
| Fine-grained RBAC | ✅ **Corrected this pass — was stale, already fixed as of the twelfth pass** | `require_permission()` (main.py) checks the same permission map used by `/auth/me`, threading project-membership role. `agents.py`, `approvals.py`, `connectors.py`, `projects.py`, `quality.py`, `query_tools.py`, `semantic.py`, and `tools.py` all call it. The one gap this row used to describe — `artifacts.py`'s `POST /artifacts/{id}/review` using a raw `user.role` check — **was actually closed in the twelfth pass** (see that change-log entry: migrated to `require_permission(user, db, "jobs:write", ...)`, verified live). This row and the masthead simply weren't updated to say so until now; read `app/routers/artifacts.py:385` yourself to confirm rather than trusting this line either |
| Production hardening (secrets, TLS/ingress, backup/restore, DR) | 🟡 **NetworkPolicy gap closed this pass (ingress only)** | Real progress carried over from prior passes: non-root Dockerfiles, `securityContext` on every Deployment, worker/web HPAs, worker PDB + liveness probe, tunable DB pool sizing, `ingress.example.yaml`. **New this pass:** `infra/kubernetes/network-policy.yaml` — default-deny-ingress across the namespace plus explicit allows (api from web/worker on 8000; web from web/worker and your ingress controller, placeholder namespaceSelector, on 3000), wired into `kustomization.yaml`. Deliberately does **not** restrict egress — connectors/MCP/model-provider calls need broad outbound reach by design, and a safe cluster-CIDR-aware egress allowlist can't be hardcoded generically here; see `infra/kubernetes/README.md`'s new NetworkPolicy section for the full reasoning. **Still missing:** no secrets-manager integration beyond "put it in a Secret," no backup/restore or DR drill has been run, no security review (see next row), egress still unrestricted |
| Security review (threat model, pen test, dependency scan) | 🔴 | Not performed — explicitly deferred by the user for this pass, staged for a future round |
| Self-learning / auto-adaptation | 🟡 (by design, now smarter) | **Direct answer verified against code: accepting a suggestion never changes runtime behavior automatically.** `review_learning_suggestion()` only flips status, records reviewer/note, and audits — confirmed by reading the function, not inferring from the UI copy. What's real and improved: repeated "not helpful" signals in the same category aggregate into one `LearningSuggestion` with `occurrence_count`/`severity` instead of one row per event; a full review UI exists (Admin → Governance → Learning Loop); as of this session, an accepted/reviewed suggestion also shows a "review_target"-aware deep link (e.g. a SQL-grounding suggestion links straight to the SQL workspace) so a reviewer doesn't have to remember which screen owns which fix — still zero automatic changes, purely navigation. See `ARCHITECTURE_DECISIONS.md` §2 for why autonomous self-modification is deliberately not built |
| Registry usability (marketplace view, certification lifecycle) | ✅ **Corrected this pass — was stale, already fixed as of the twelfth pass** | Lifecycle states (draft/tested/published/retired) and per-tool analytics are done. External consumers can already search published tools by purpose/source/LOB/owner/tags over `/external/v1/*` and MCP. The internal gap this row used to describe — no search/filter in the gateway's own admin list — **was actually closed in the twelfth pass** (`GatewayAdmin` in `admin.tsx` filters by name/purpose/owner/LOB/data source/tags, same `toolbar-search` pattern as every other registry screen). This row simply wasn't updated to say so until now |
| Per-connector concurrent-connection ceiling for external MCP/agent traffic | ✅ **New this session, closed a real gap** | Direct-driver connectors (`pymssql`/`psycopg`/`oracledb`/`teradatasql`) open a fresh physical connection per query with no pooling — under enough concurrent external MCP/agent calls against the same connector, this could open unbounded simultaneous connections against the source database. Added `app/connection_guard.py`: a `threading.BoundedSemaphore` per `connector_id` (matches the existing sync-threadpool concurrency model, not asyncio), capped by `CONNECTOR_MAX_CONCURRENT_CONNECTIONS` (default 8), wired around the driver-dispatch block in `connector_runtime.py`. Exceeding the budget raises `ConnectionLimitExceeded` → HTTP 503 with a clear retry message, not a silent hang or a connection-storm against the source. 6 dedicated unit tests (`tests/test_connection_guard.py`). Directly answers "is it able to handle any number of connections from external agents" — answer was previously "no explicit limit, relies on the source DB's own connection cap," now it's bounded and observable at the DataPilot layer too |
| Long-running external extraction (source → staging) durability | ✅ **New this session, closed a real inconsistency** | Every other ingestion path (`/schedules/*` recurring runs, metadata scans) already ran through Temporal for durability/retry/history; `POST /extractions/{id}/run` was the one exception — a long extraction ran synchronously in the request thread with no retry-on-worker-crash and no visibility into an in-flight run. Extracted the handler's logic into `extraction_runtime.run_external_extraction_now()`, added a matching `ExternalExtractionWorkflow`/`execute_external_extraction` activity pair (same pattern as `ScheduledIngestionWorkflow`), and made the endpoint Temporal-dispatch-first with a synchronous fallback when `TEMPORAL_ADDRESS` is unset (mirrors `POST /schedules/{id}/run` exactly). Response changed `201`→`202` to reflect the now-possibly-async nature; test updated accordingly. Directly answers "if there's a long-running bring-data-to-staging job, any impacts" — it's now retried/tracked the same way scheduled ingestion already was, not a special case that could silently die with the request |
| Message queue / Kafka | ✅ **Answered this session, not a gap** | Not needed today. Temporal already serves as the durable async execution engine (retries, history, visibility — better fit than a raw log for this workload); Redis (provisioned but previously unused — now wired for rate limiting, see §1) covers cross-request state needs. Kafka would earn its place only for genuine multi-consumer event fan-out, which nothing in the product needs yet. Full reasoning in `ARCHITECTURE_DECISIONS.md` §6 |
| Scalability | 🟡 **Answered this session** | Architecturally sound and not yet proven at scale: stateless API/web pods scale via HPA (api 2–8, web 2–6, worker 2–10, all CPU-triggered), Temporal workers scale independently of HTTP traffic. One concrete, documented, *unresolved* capacity risk: Postgres connection exhaustion at max replica counts — `(8 api + 10 worker) × 8 conns/process = 144` against a Postgres default `max_connections` of 100 (see `infra/kubernetes/README.md`). No load test has been run. Full reasoning in `ARCHITECTURE_DECISIONS.md` §7 |
| CI pipeline | ✅ | `.github/workflows/ci.yml` — `pytest` + `tsc --noEmit`/`next build` on every push/PR |

## 3. Engineering / codebase health

| Item | Status | Detail |
| --- | --- | --- |
| Backend test suite | ✅ Passing, fully isolated | **95/95**, re-verified from a clean venv against all four test files together and each standalone (`test_new_endpoints.py`'s import-order dependency is fixed — see fourteenth-pass entry; +2 new tests this pass covering the connector-group semantic-graph/join-policy fix — see fifteenth-pass entry). `pyflakes app/` clean (no undefined-name findings) |
| Backend architecture | ✅ Restructured | `apps/api/app/main.py` (was 7,069 lines / one file) is now a shared-schema/helper module plus 25 domain-scoped `apps/api/app/routers/*.py` files. Verified stable across four further rounds of concurrent changes — no regressions introduced by the split |
| Test coverage breadth | 🟡 | Against 177 endpoints, coverage is broader than one file now (`test_api.py`, `test_connection_guard.py`, `test_new_endpoints.py`, `test_vector_store.py`), but still not comprehensive. Still missing dedicated coverage: PII masking behavior specifically, the full RBAC permission-map matrix (which role can/can't do what per endpoint), the "staging" Postgres-schema code path (see the new Screenshots row below — every test in this repo runs against SQLite, where `stage_rows()` uses `schema=None` instead of `schema="staging"`, so the one code path a live demo run actually flagged as buggy has zero automated coverage) |
| Frontend architecture | ✅ Restructured | `apps/web/app/page.tsx` (was 2,714 lines) is now a composition root plus `types.ts`, `lib/constants.tsx`, and 20 files under `app/components/`. `tsc --noEmit` clean; `next build` compiles/typechecks/generates pages |
| Repo cleanliness | 🟡 Improved, one class of item still stuck | The stray root `docker-compose.yml` and `tmp_migrate_*.py` scripts flagged in an earlier pass are already gone (confirmed this pass). Still remaining: empty leftover scaffold dirs (`apps/web/db`, `apps/web/worker`, `apps/web/drizzle`, `apps/web/app/_sites-preview`) — every device-bridge tool available to this pass (including a direct `rmdir`) gets `Operation not permitted` on them; genuinely needs a human to delete from Windows Explorer, not a sandbox-access limitation that a smarter attempt would get around |
| Deployment security posture (OCP/K8s) | ✅ | Non-root Dockerfiles, `securityContext` on every Deployment, worker/web HPAs, worker PDB + liveness probe, tunable DB pool sizing, `ingress.example.yaml`, **NetworkPolicy (new this pass, ingress-only — see §2)** |
| Demo video | 🔴 Not recorded | `docs/videos/` is confirmed empty this pass (not even a placeholder file) |
| Screenshots | ✅ **Corrected this pass — were stale/wrong, not actually a gap** | Confirmed present on disk: 40 real PNGs under `docs/screenshots/workflows/` plus a packaged `DataPilot_Demo_Screenshots.zip`, contradicting this row's own prior "not confirmed present" claim. **More important than the correction: two of the 40 are literally named `06-ask-question-ERROR-bug.png` and `07-sql-generate-ERROR-bug.png`** — someone flagged a real bug in the two most central workflows (asking a natural-language question, generating SQL) during a live `docker compose up` demo run, and it was never triaged or referenced anywhere else in this document. See the fourteenth-pass change-log entry: this pass could not reproduce it (no Docker daemon reachable from here, and the one schema-naming difference found — `stage_rows()`'s `schema="staging"` only applies under real Postgres, never under the SQLite tests always run against — means this may be a real gap in automated coverage, not just an unlucky one-off). **Needs a live `docker compose up` re-run to confirm whether this is still broken before recording the demo video.** |

## 4. Metadata management — direct answers

| Question | Answer |
| --- | --- |
| How does DataPilot pull metadata from a source database? | Admin adds a `Connector` (direct driver or MCP mode), then calls `POST /connectors/{id}/scan`. Runs read-only `discover_metadata()` — driver-backed for direct connectors, `tools/list`-backed for MCP connectors — writing/updating `DataAsset` rows and raising a `SchemaDriftEvent` on column changes |
| Can metadata be scanned through an MCP-connected source? | ✅ Yes — `discover_mcp_metadata()` catalogs each upstream tool's schema as a queryable asset with inferred PII/date/id tags |
| Can I upload metadata instead of connecting live? | ✅ Yes — `POST /datasets/import` accepts a CSV and creates/updates `DataAsset` rows tagged `metadata_status="manual_import"`, no live connection required |
| Can I manually edit or review scanned metadata (description, tags, owner, sensitivity, freshness, status)? | ✅ `PUT /datasets/{asset_id}` — API and the `DatasetsView.tsx` edit modal both cover all six fields |
| Is discovered/ingested metadata flagged for PII? | ✅ `annotate_columns()` in `app/pii.py` tags columns with `sensitivity: "pii"` and a `pii_category` during file ingestion and connector metadata discovery |
| Is metadata scoped per project? | ✅ Yes — `DataAsset.project_id`/`Connector.project_id` are FKs, every route filters by `require_current_project()`. No cross-project catalog sharing |
| Can one project have multiple sets of joined tables, or multiple "data models"/groupings (vs. one flat catalog)? | 🟡 **Partially answered this pass — an implicit, enforced grouping exists; a named/first-class one still doesn't** | A project's catalog is still a flat list of `DataAsset` rows with no named "data model"/"domain"/"subject area" object — that part of the earlier finding stands. **What's new as of the fifteenth/seventeenth passes:** there IS now a real, enforced grouping boundary, just an implicit one: for a direct-driver connector, `connector_id` (one bucket per external connector, plus one shared bucket for everything locally staged/uploaded, since those really do live in one queryable local database); for an MCP-backed connector, each individually-discovered tool is its own atomic group instead, since MCP execution can only ever invoke one named tool per call — see the seventeenth-pass change-log entry and the Semantic relationship graph row in §1. This boundary is exactly "which datasets can be queried together in a single call" — `/sql/generate`/`/sql/execute` were already scoped this way via `allowed_asset_ids` (verified, see the retrieval-safety note below); `/semantic/graph`'s inferred suggestions and `/semantic/joins`' governed-policy validation now enforce the same boundary (see the Semantic relationship graph row in §1). So: **groups exist and are enforced for join-safety purposes, but they're anonymous (a connector, not a named domain) and there's still no way to subdivide one connector into multiple named domains, or deliberately group tables from different connectors under one federated model.** If a user-authored, named grouping is wanted — e.g. "Retail Banking core" spanning specific tables regardless of source, independent of the connector boundary — that's still the same unbuilt `DataModel`/`AssetGroup` entity this row originally flagged, now with a clearer articulation of what it would need to coexist with (the connector-scoped executability boundary can't be relaxed by a naming feature; a cross-connector "model" could group tables for documentation/search but could never enable a single executable cross-connector join without real query federation, which does not exist in this product). **Separately verified this pass, directly answering "is context maintained properly for generation and retrieval across sources with duplicate-looking table names":** yes — `grounding_context()`/`project_asset_search()` (`grounding.py`) filter both keyword and vector-search hits against the same connector-scoped `allowed_asset_ids` set `/sql/generate` computes, with an explicit post-hoc cross-check discarding any vector-search hit whose underlying asset isn't in that set (the same defensive pattern used for glossary cross-project leakage prevention) — so even though Qdrant itself has no server-side connector filter, a generation call for connector A can never surface or ground on connector B's identically-named table. This was read in the actual code this pass, not assumed from the fix above. See §9 for the recommendation |
| Where are business names for tables/columns stored, and do they reach the vector store? | ✅ Table-level `description`/`tags`/owner already existed; **column-level `business_name`/`description` added this session** (see §1). Both live in Postgres (`DataAsset.description`, `DataAsset.columns` JSON) as the source of truth — nothing is stored *only* in the vector store. They reach Qdrant indirectly: `index_document()` embeds `f"{title}\n{text}"` built from the asset's catalog text, and that text includes column business names via `_column_sql_context_entry()`. As of this pass, `embed_text()` calls the *active* `ModelProvider`'s real embedding API (Gemini, OpenAI, or an OpenAI-compatible endpoint) using `ModelProvider.embedding_model`, resolved per call via `provider_selection.default_embedding_provider(db)` — not just a hardcoded `GOOGLE_API_KEY`/`text-embedding-004` call as an earlier pass had it (see §5, §9). It still fails open to the deterministic hash-bag-of-tokens (blake2b) fallback when no provider/secret is configured, the provider type has no embeddings API (`claude` — Anthropic has no embeddings endpoint; `local_mock`), or the live call errors — so search quality genuinely depends on a healthy provider being configured, same caveat as chat generation |

## 5. Further functionality improvement ideas — status

| Idea | Status |
| --- | --- |
| Manual metadata edit + enrichment | ✅ Done |
| Owner / steward / sensitivity / freshness fields on `DataAsset` | ✅ Done |
| Bulk metadata import (CSV, no live data required) | ✅ Done |
| MCP-mode connector metadata scanning | ✅ Done |
| PII detection + result masking | ✅ Done this session's re-verification (built concurrently, verified real) |
| Multi-hop lineage traversal | ✅ Done this session's re-verification (built concurrently, verified real) |
| Fine-grained RBAC enforcement | 🟡 Real but partial — see §2 for exactly which routers still bypass the permission map, and the project-membership-role blind spot in `require_permission()` |
| External gateway rate limiting | ✅ Done this session |
| OCP/Kubernetes production hardening | 🟡 Meaningfully improved this session (non-root, securityContext, HPAs, PDB, pool sizing); NetworkPolicy, secrets-manager integration, backup/restore/DR drills still open |
| Async/Temporal-backed scan for large sources | 🟡 Still open — worth confirming the durable workflow path is exercised for a genuinely large schema, not just present in code |
| Registry marketplace UI (internal search/filter by tag/LOB/owner) | 🟡 Still open |
| Automated CI | ✅ Done |
| Test coverage for newer endpoints | 🟡 Partial, and the new file has a real isolation bug | `tests/test_new_endpoints.py` was added covering `/semantic/graph`, `/lineage/graph`, `/sql/history`, `/datasets/import`, RBAC role-blending (`project_permissions`), plus three previously-undocumented capabilities (see new §1 rows: self-healing tool-error reflection, plan-reviewer loop, graph-based join pathfinding). **Bug found this pass:** unlike `test_api.py`, this file never sets `DATABASE_URL` before importing `app.main`, so run in isolation it hits the real dev `datapilot.db` on a mounted filesystem and fails all 8 tests with a SQLite disk-I/O error; it only passes today because pytest imports `test_api.py` first in the same process and inherits its temp-database env var as a side effect. Not fixed this pass — needs its own isolated `DATABASE_URL` setup, same pattern as `test_api.py` |
| Per-connector concurrency limiting for external MCP/agent traffic | ✅ Done this session |
| Temporal-backed durability for external extraction | ✅ Done this session |
| SQL-generation catalog relevance bug (>40-table projects) | ✅ Fixed this session |
| Column-level business names/descriptions | ✅ Done this session |
| SOP/business-glossary upload (PDF or text) for grounding | ✅ Done this session — backend + tests, no dedicated frontend UI yet |
| Real semantic embeddings, actually driven by `ModelProvider.embedding_model` | ✅ Done this pass (corrects an overstated earlier claim) | A user asked directly: "is any model used, or is embedding chosen during model registration, or per-database?" Reading `vector_store.py` line by line showed the earlier pass's "✅ Verified this pass" claim below was materially incomplete: it *did* call a real Gemini embeddings endpoint, but the model string (`text-embedding-004`) and API key (`GOOGLE_API_KEY`) were both hardcoded — `ModelProvider.embedding_model`, the field an admin can actually set in the Provider registry UI, was written to the DB and returned by every CRUD endpoint but never read by anything. Every other provider type (`openai`, `openai_compatible`, `claude`, `local_mock`) always silently used the hash fallback regardless of configuration. Fixed properly: `embed_text(value, provider)` now dispatches on `provider.provider_type` — `gemini` and `openai`/`openai_compatible` call their real embeddings endpoints with `provider.embedding_model` and the provider's own `secret_reference`/`base_url`; `claude` (no Anthropic embeddings endpoint exists) and `local_mock` intentionally stay on the hash fallback. `provider_selection.default_embedding_provider(db)` resolves which provider is active (global, not per-project — the vector store has no per-project partition, see §4/glossary row), threaded through `index_document()`/`search_documents()` at all 9 real call sites (`main.py` startup reindex, `metadata_scan_runtime.py`, `schedule_runtime.py`, `routers/files.py` ×2, `routers/workspace.py` ×2, `grounding.py` ×2). Because different embedding models produce different-dimension vectors (Gemini 768, OpenAI `text-embedding-3-small` 1536, ...), `ensure_collection()` now detects a size mismatch against the live Qdrant collection and recreates it on the write path only (`index_document`, `allow_resize=True`) — the read path (`search_documents`) never destroys data, it just yields no results until reindexed. Added `vector_store.reindex_all(db)` + `POST /model-providers/reindex-embeddings` (admin-only) to re-embed every `DataAsset`/`GlossaryDocument` after switching providers or editing `embedding_model`, plus a "Reindex embeddings" button in the admin Provider registry screen. Full backend suite re-run: **78/79 passing, 1 skipped**, no regressions. **This corrects, not just supersedes, the entry below and the §4 caveat — the previous claim that this was already "done" was itself the gap** |
| Real semantic embeddings (vs. hash-based) in the vector store — *(superseded, see row above; kept for history)* | 🟡 Was incomplete | `embed_text()` in `vector_store.py` called Google's `text-embedding-004` via the `google-genai` SDK when `GOOGLE_API_KEY` was set, falling back to the hash implementation otherwise — real, but it ignored `ModelProvider.embedding_model` entirely and only ever supported Gemini. See the row above for the actual fix |
| First-class "multiple data models per project" grouping | 🔴 Confirmed not to exist this session — recommendation only, no code change |
| Agent/tool harness: LLM-driven tool-parameter construction | ✅ Implemented this session as a guarded second tier behind the deterministic heuristic — see §8 and `docs/specs/tools.md` |

## 6. Deployment verification

This sandbox has no Docker daemon and no path to the user's local Docker Desktop, so `docker compose up --build` cannot be run from here. Verified directly instead: API boots standalone; `POST /datasets/import` exercised live end-to-end; `discover_mcp_metadata()` exercised against a local MCP fixture server; the rate limiter's 429 path exercised live with `fakeredis` (not just unit-tested in isolation); full backend suite 56/56; frontend `tsc`/`next build` clean.

**To validate the full Compose stack (Postgres, Temporal, Qdrant, Redis, Superset) end-to-end, run on your machine:**

```powershell
cd C:\Users\karup\AGProjects\AienginnerAgentOs
docker compose up --build
```

Then check `http://localhost:3001` (web), `http://localhost:8000/health/ready`, `http://localhost:8233` (Temporal UI). For the SQL Server Express demo, see `docs/SQLSERVER_EXPRESS_DEMO.md` — live scanning needs SQL Server TCP/IP enabled on the host first.

## 7. Recommended priority order

1. **P0 for a good demo:** record the actual demo video (confirmed still just a placeholder); reconfirm screenshots exist on disk (confirmed this pass: none exist); run one live SQL Server scan end-to-end against the now-unblocked `sqlserver-demo` Docker container (direct + MCP) — the setup no longer needs host TCP/IP changes, just `docker compose up` and a connector added in Admin.
2. **P0 for enterprise credibility:** SSO completion, live connector certification beyond SQL Server Express, a real security review/pen test, a NetworkPolicy, a backup/restore drill.
3. **P1 for correctness/trust — ✅ done as of this pass:** `require_permission()` already consults project-membership role. The `artifacts.py` migration and the gateway-search item below were already done as of the twelfth pass (this document just hadn't caught up — corrected in §2 this pass). `tests/test_new_endpoints.py` now has its own isolated `DATABASE_URL` (fourteenth pass). Remaining: automated tests for PII masking specifically, and the "staging" Postgres-schema code path noted in §3's Screenshots row — genuinely not exercised by any test in this repo today.
4. **P1 for scale-readiness:** load-test against the documented Postgres connection-pool math before raising HPA `maxReplicas` in a real deployment; **NetworkPolicy added this pass (ingress only — see §2)**; consider a custom Temporal-queue-depth autoscaling metric for the worker if CPU-based HPA proves insufficient.
5. **P1 for usability:** ✅ done as of the twelfth pass — gateway search/filter exists (`admin.tsx`'s `GatewayAdmin`), this document just hadn't been updated to say so until this pass.
6. **P1 for maintainability:** ✅ done — `main.py` and `page.tsx` split into per-domain/per-view modules, verified stable across four further rounds of unrelated concurrent changes.
7. **P2:** ✅ done — `ModelProvider.embedding_model` is wired into `vector_store.embed_text()` for Gemini/OpenAI/OpenAI-compatible, with dimension-safe reindexing. Remaining open item: design a first-class grouping entity if multi-model-per-project becomes a real requirement (see §9).
8. **P0, newly surfaced this pass:** reproduce and triage the two `*-ERROR-bug.png` screenshots (`docs/screenshots/workflows/06-ask-question-ERROR-bug.png`, `07-sql-generate-ERROR-bug.png`) against a live `docker compose up` stack — this sandbox has no Docker daemon and could not reproduce them. These hit the two most central workflows (ask a question, generate SQL) and were apparently never triaged after being captured. Do this before recording the demo video.

## 8. Agent/tool harness — improvement assessment (this session)

Read `temporal_activities.py`'s `_execute_agent_plan()`/`_execute_bound_tools()` and `tool_runtime.py`'s `execute_tool()` directly (not the docs) to answer "anything need to improve over there." Findings, all confirmed against code:

| Area | Finding |
| --- | --- |
| Plan generation | Real when a non-`local_mock` provider is configured: the model is prompted with grounding context and must return 3–6 steps from the enabled, registry-configured agent set (with `Planner` and `Policy` retained as control-plane participants); it falls back to a deterministic plan on parse/validation failure. |
| Tool parameter construction | **Two-tier, both guarded.** Tier 1, `_parameters_for_tool()`: fills only grounded objective/query values, exact project-scoped UUIDs, known catalog relations, bounded limits, and declared defaults — no model call. Tier 2, `_llm_parameters_for_tool()` (new this session): only reached when tier 1 can't ground every required field, and only when a real (non-`local_mock`) provider is configured. The model gets one narrow, schema-only prompt; its output must pass the exact same `validate_parameters()` every other call is checked against, *and* any field shaped like a foreign key (`asset_id`, `file_id`, `mapping_id`, `rule_id`, `schedule_id`, `job_id`) must resolve to a real row scoped to the run's project or the whole fill is discarded — a well-formed but hallucinated id is rejected exactly like a missing one. Every model attempt is logged to `ModelCallLog` (`purpose="agent_tool_parameter_fill"`) whether accepted or rejected, and accepted evidence is tagged `parameter_source: "heuristic"` or `"model"` so a run stays explainable after the fact. Covered by `test_llm_parameter_fill_is_guarded_and_grounded`, which asserts both the acceptance path (grounded id, `dataset.profile` runs) and the rejection path (hallucinated id, tool is skipped, nothing executes). |
| Policy authority | **Implemented.** The configurable `Policy` agent can explain policy evidence, but deterministic approval keywords, project scope, risk level, and write/execute boundaries remain authoritative. A generated plan crossing a deterministic boundary is paused for approval before bound tools run. |
| Agent coverage | **Expanded.** Seeded registry now includes configurable `Policy` and `Analytics` agents in addition to Planner, Metadata, SQL Analyst, Pipeline, Quality, and Troubleshooter. All ten built-in tools have published contracts; agents can be versioned, enabled/disabled, and bound to internal tools or governed query tools. |
| Failure handling | A `ToolRuntimeError` on one tool logs a warning and moves to the next planned step. There's no reflection/replan step that feeds the failure back to the model and tries a different tool or adjusted parameters within the same run. |
| Run budget | A flat 12-tool-call ceiling per run, no per-step or per-tool-type budgeting, no cost/token accounting surfaced per run beyond the raw call count. |
| Model provider resilience | One provider is selected once per run; a planning failure falls back to the fixed deterministic plan, not a retry against a secondary configured provider. |
| Self-evaluation | No automated LLM-as-judge or benchmark scoring of a completed run beyond the existing `GET /agents/{id}/scorecard` (which scores evaluation-suite pass rate, not live-run quality). Historical benchmark dashboard / ablation testing were already flagged as not built in `ARCHITECTURE_DECISIONS.md` §2 — still true. |

The remaining harness gaps are reflection/replan after a failed call, per-tool cost/token accounting (beyond the per-attempt `ModelCallLog` rows the parameter-fill tier now writes), provider failover, and automated live-run quality judging.

## 9. Change log

- **August 16, 2026 (seventeenth pass — MCP tools aren't connector-level groups, this revision):** Direct follow-up from the user, pointing at the sixteenth pass's own clustered-graph screenshot: `mcp.sqlserver.*` tools and `mcp.postgres.*` tools were still shown connected/grouped together even though they should never be treated as one queryable unit. The fifteenth pass's `group_key()` fix (one bucket per `connector_id`) is correct for a **direct-driver** connector — its assets really do share one physical DB connection — but wrong for an **MCP-backed** connector, where each cataloged asset is a separately-discovered *tool* (`discover_mcp_metadata()` hardcodes `schema_name="mcp"` for every one, regardless of backend), and a single MCP toolbox can front more than one physically distinct backend with no structured signal in `tools/list` saying which tools share one — confirmed directly in this repo's own demo config, `infra/mcp-toolbox/toolbox.yaml`, which defines two separate `kind: source` blocks (a SQL Server source and a Postgres source) behind one connector. Traced every MCP execution path (`execute_connector_query`/`execute_mcp_tool` in `connector_runtime.py`, and both call sites in `temporal_activities.py`) to confirm there is no cross-tool join capability anywhere in the runtime — an MCP-backed call always invokes exactly one named tool. That makes the correct fix stronger than "assume different backends conservatively": **no two distinct MCP tools are ever joinable, even when they happen to share a backend**, so each one must be its own atomic group regardless of connector_id.
  - **Fixed, backend:** `group_key()` in `routers/semantic.py` now checks the asset's connector's `connection_mode`: direct connectors keep the existing one-bucket-per-`connector_id` behavior; MCP connectors instead key each asset by `f"{connector_id}:{asset_id}"`, so two tools on the same MCP connector never share a group. Inferred edges (already scoped to `group_key()` equality from the fifteenth pass) automatically inherit the fix. `validate_semantic_join_policy()` (`app/main.py`) had the identical gap on the governed-policy path — it only ever compared `connector_id`, which would have let a maintainer approve a policy between two different MCP tools on the same connector; now it separately checks `connection_mode == "mcp"` for a same-connector pair and rejects with a message naming the real reason (two different MCP tools, not "two different connectors"). Added `connection_mode` to `analysis_source_output()`'s response (the `source` object returned by `GET /datasets` and `/sql/generate`) so clients can tell direct and MCP sources apart without a second lookup.
  - **Fixed, frontend:** `Dataset.source.connection_mode` added to `types.ts`; `JoinPoliciesPanel`'s client-side `groupKey()` mirror updated to match the backend exactly (MCP assets keyed per-tool). `SemanticGraphPanel` still needed a second-order fix beyond just correctness: coloring/legending strictly by the new fine-grained `group` would have rendered a 6-tool MCP toolbox as six unrelated-looking colors with six single-row legend entries, which is a worse UX regression than the bug it fixes. Colors and the legend now key off `source_label` (so one MCP connector reads as one color and one legend row, annotated with a "N separately-queried tools" note when it spans more than one non-joinable group), while arc placement still keys off the fine-grained `group` (so the tools are visually separate, non-edge-connected clusters) and groups sharing a `source_label` are sorted adjacent on the ring so they still read as one visual cluster.
  - **Tests:** 2 new (`test_semantic_graph_treats_distinct_mcp_tools_as_non_joinable_even_under_one_connector`, `test_semantic_join_policy_rejects_two_mcp_tools_on_same_connector`), both creating a real `connection_mode="mcp"` connector via the live `/connectors` endpoint (not a mock) and asserting both the inferred-edge and governed-policy-creation paths correctly refuse to treat two of its tools as joinable, while confirming `source_label`/node identity still reads correctly. Full backend suite re-run at **98/98 passing** (up from 96); `tsc --noEmit` and `next build` both clean.
  - **Not changed, and correctly so:** an MCP connector's tools still all share the same `source_label` (the connector's name) — that's intentional, they genuinely are "the same source" from a human's perspective, just not jointly queryable. `dataset_category()`/`source_name` display elsewhere in the app were not touched; this pass only affects the join-safety grouping used by the semantic graph and join-policy validation.

- **August 16, 2026 (sixteenth pass — grandfathered cross-connector policies + a real dataset-picker bug, this revision):** Direct follow-up: the user shared a live screenshot of the fifteenth pass's fix actually running (clustered, colored graph, "2 governed / 0 inferred") and asked for analysis of the two visible governed edges. Investigating that surfaced two more real, previously-undiscovered issues rather than just confirming the fix worked:
  1. **The fifteenth pass's new creation-time guard (`validate_semantic_join_policy`) can't retroactively fix a policy that already existed before it, and `/semantic/graph` was still rendering any such policy identically to a real, executable one — no signal anything was wrong.** Added a `cross_connector` boolean to every governed edge in `/semantic/graph`'s response (computed the same way the creation-time guard is, by comparing each policy's two assets' connector groups), and surfaced it in the UI: the graph edge renders dashed red instead of the normal solid brand color, its tooltip explains why, and `JoinPoliciesPanel`'s list now shows a warning icon plus inline text on any flagged policy. New test `test_semantic_graph_flags_pre_existing_cross_connector_policies` inserts a policy directly via `SessionLocal` (bypassing the create endpoint, simulating exactly how stale pre-existing data would have gotten in) and confirms it's flagged while a same-connector policy isn't.
  2. **A second, independent, previously-undiscovered bug found while tracing why a cross-connector policy could exist at all: `JoinPoliciesPanel`'s dataset picker (`SemanticView.tsx`) deduplicated the `/datasets` response by bare `schema_name.table_name` before ever putting it in state** (`Array.from(new Map(datasetData.map(d => [key, d])))`) — a `Map` keyed this way silently keeps only whichever asset loaded last for a repeated name and drops the other entirely. Once two sources both catalog e.g. `core.accounts`, one of them became permanently unselectable in the "Add join policy" form, with no indication a collision even happened — a user intending to pick one specific `core.accounts` had no way to know or control which one the dropdown actually resolved to. This is the same duplicate-name scenario the graph's own node labels were already fixed for (see the fourteenth-pass-adjacent code comment in `routers/semantic.py`), but that fix never reached this separate component's own dataset list. **Fixed:** stopped deduplicating; added a `datasetLabel()` helper (same disambiguation pattern as the backend: only append the source name when a relation string actually collides) used in both dataset `<select>` dropdowns and the approved-paths list's relation display, so every distinct asset is both selectable and correctly labeled.
  - Whether the two edges in the user's screenshot were actually cross-connector could not be conclusively determined from the image alone (edge stroke color is a fixed brand color unrelated to node group colors, and node layout position alone isn't a reliable proxy for which edge connects to which node) — this is exactly what the new `cross_connector` flag now answers definitively, live, without needing to eyeball a screenshot.
  - Full backend suite re-run at **96/96**; frontend `tsc --noEmit` and `next build` both clean.

- **August 16, 2026 (fifteenth pass — cross-connector join graph, this revision):** Direct user report, with live screenshots of a real `docker compose up` run: the `/semantic/graph` relationship graph rendered as a dense, nearly-fully-connected mesh ("0 governed / 77 inferred") with no visible structure, joining tables across a SQL Server demo warehouse, a Postgres demo warehouse, MCP-discovered SQL Server and Postgres sources, and local files. The user's read was exactly right: inference should stay within a source/group, and the graph had no concept of "group" at all. Read `routers/semantic.py`'s `semantic_graph()` line by line to confirm rather than assume: the inferred-edge loop was a naive nested loop over every `DataAsset` in the project comparing column names, with zero awareness of which connector each asset came from.
  - **Why this matters beyond visual noise:** every `/sql/generate`/`/sql/execute` call is scoped to exactly one connector (or the local workspace) at a time (`allowed_asset_ids` in `routers/sql.py`), so a suggested join between two different external connectors' tables can never actually be executed as a single query — the graph was recommending joins the product cannot run.
  - **Fixed, backend:** `routers/semantic.py`'s inferred-edge computation now only compares assets within the same `group_key()` (one connector's `connector_id`, or a shared `"__local__"` bucket for every locally-staged/uploaded asset, since those genuinely share one queryable database). Each graph node now also carries `group` and `source_label` fields. Separately, `validate_semantic_join_policy()` (`app/main.py`) — used by both `POST` and `PUT /semantic/joins`, the *governed*, human-approved path — had the identical gap: nothing stopped a maintainer from approving a join policy between two different connectors' assets. Now rejected with a 422 explaining why, while same-connector and local-workspace pairs are unaffected. 2 new tests (`test_semantic_join_policy_rejects_cross_connector_datasets`, `test_semantic_graph_inferred_edges_stay_within_connector_groups`) cover both the rejection and the still-allowed cases; full suite re-run at **95/95**.
  - **Fixed, frontend:** `SemanticGraphPanel` (`SemanticView.tsx`) no longer scatters every node around one flat ring. Nodes are now clustered into a contiguous arc per source group, color-coded (one color per group, `GRAPH_GROUP_COLORS`), with a legend showing each source's label and dataset count above the graph. `tsc --noEmit` and `next build` both clean.
  - **Also directly answered, by reading the code rather than assuming the same bug applied everywhere:** does SQL generation/retrieval suffer the same cross-source confusion when duplicate-looking table names exist across sources (e.g. two different connectors both having a `core.accounts`)? **No — verified safe.** `grounding_context()`/`project_asset_search()` (`grounding.py`) filter every catalog match, and cross-check every vector-search hit, against the same connector-scoped `allowed_asset_ids` set `/sql/generate` computes; a hit for an asset outside that set is silently dropped even though the underlying Qdrant store itself has no server-side connector filter. This was the one place the same bug plausibly could have existed and didn't — reported as verified-safe rather than left unstated.
  - **Design question directly answered:** does this need a bigger design change (a first-class "data model"/group entity)? Partial answer — see the updated §4 grouping row: connector-scoped grouping is now real and enforced for join-safety, but it's still anonymous (a connector, not a named domain) and doesn't let a user hand-define a domain that spans multiple connectors. That remains a genuine, unbuilt product decision, not a bug — recorded honestly rather than either overstating this pass's fix or leaving the design question unanswered.

- **August 16, 2026 (fourteenth pass — code fixes/cleanup + doc re-verification, this revision):** Requested directly: fix open issues and improve core functionality; SSO and a formal pen test explicitly deferred to a future round by the user. This pass worked from a full clean-install re-verification (fresh Python venv, fresh `npm install`) rather than trusting any prior pass's masthead, per this document's own stated methodology.

  **Real bugs found and fixed, both with a passing regression test:**
  1. **Test-isolation leak breaking 2 unrelated tests.** `test_llm_parameter_fill_is_guarded_and_grounded` (`tests/test_api.py`) inserted a synthetic `DataAsset` (`analytics.llm_fill_target`) directly into the shared test-class catalog and never cleaned it up. Because `unittest` runs this `TestCase`'s methods alphabetically against one shared DB (by design, per `setUpClass`), the leaked asset silently changed which table `/sql/generate`'s deterministic local-mock path picked for later, alphabetically-subsequent, unrelated tests — breaking `test_postgres_connector_type_is_registered_with_postgres_dialect` (409 instead of 200) and `test_sql_generation_is_read_only` (returned SQL against `analytics.llm_fill_target` instead of `core.accounts`). Fixed by deleting the synthetic asset at the end of the test. Confirmed reproducible standalone (not an artifact of my environment) before fixing.
  2. **The relevance bug that leak exposed was real, not just a test artifact.** `generated_catalog_sql()` (`app/sql_generation.py`) only checked `assets[0]` for the well-known demo `core.accounts` table before returning its polished canned query — so *any* other asset sorting or scoring ahead of it (alphabetically-earlier `schema_name`, a higher grounding-relevance score for a given question, or, as found here, an unrelated leaked test asset) silently fell through to a generic, less useful templated query even though `core.accounts` was still right there in the catalog. Fixed to search the full asset list instead of trusting list position. This is the same bug *category* (SQL-generation relevance depending on catalog ordering) this document has recorded fixes for twice before — this is a third, previously-undetected instance of it.
  3. **`test_postgres_connector_type_is_registered_with_postgres_dialect` was independently broken, unrelated to the leak above.** It creates a fresh `postgres`-type connector and calls `/sql/generate` against it with zero scanned assets, expecting `200`. `generated_catalog_sql()` correctly 409s ("Ingest or scan a dataset before generating SQL") for a connector with an empty catalog — that guard is the right product behavior (generating SQL against a completely unscanned connector would mean guessing table/column names), so the *test* was wrong, not the code. The test's actual intent is verifying `connector_dialect()`'s type→dialect override (`postgres` connector forces `dialect="postgres"` regardless of the requested dialect), which doesn't need real catalog data — fixed by giving the connector one scanned `DataAsset` first (same pattern already used elsewhere in this file for the equivalent Oracle case), preserving the safety guard rather than weakening it.
  4. **`tests/test_new_endpoints.py` test-isolation bug (flagged, not fixed, in an earlier pass) — now fixed.** It imported `app.main` with no `DATABASE_URL` of its own, only ever passing because pytest imports `test_api.py` first in the same process and this file inherited its temp-DB env var as a side effect. Gave it the same isolated temp-SQLite-DB setup `test_api.py` uses; verified it now passes standalone in any import order.

  **Result: full backend suite genuinely 93/93 passing** (previously that count was only ever true by import-order accident for one of the four files) from a from-scratch `pip install`; frontend `tsc --noEmit` and `next build` both clean from a from-scratch `npm install`; `pyflakes app/` run across the whole backend package found zero undefined-name issues (the specific "symbol used but never imported" bug class that broke 8 routers in an earlier pass does not currently exist anywhere).

  **Closed the documented NetworkPolicy gap (ingress-only, by design — see §2/§7).** Added `infra/kubernetes/network-policy.yaml`: default-deny-ingress across the namespace, with explicit allows for `datapilot-api` (from web/worker, port 8000) and `datapilot-web` (from web/worker, plus a templated ingress-controller selector, port 3000); wired into `kustomization.yaml`; `infra/kubernetes/README.md` updated with the reasoning for leaving egress unrestricted (connectors/MCP/model-provider calls need broad outbound reach by design, and a safe cluster-CIDR-aware egress allowlist has no generic value to hardcode here).

  **Corrected three stale rows this document itself was carrying** (found by re-reading the actual code, not by trusting prior prose — per this document's own stated method): the RBAC gap in §2 and the gateway-search gap in §2 were both already closed in the twelfth pass but never marked as such here; the masthead itself (top of document) still described the eleventh pass as current even though the twelfth and thirteenth passes had already landed. All three corrected in place this pass.

  **Confirmed repo cleanliness partially self-resolved:** the stray root `docker-compose.yml` and `tmp_migrate_*.py` scripts flagged as cruft in an earlier pass are already gone. The three empty scaffold directories (`apps/web/db`, `apps/web/worker`, `apps/web/drizzle`) and `apps/web/app/_sites-preview` are still present — confirmed, again, that no tool available to this pass (including a direct `rmdir` from the device-bridge shell) can remove them; `Operation not permitted` on every attempt. This needs a human with Windows Explorer, not a smarter sandbox workaround.

  **New finding, not yet resolved — flagged honestly rather than silently left in a screenshot filename:** `docs/screenshots/workflows/` contains 40 real PNGs (this document's own prior "screenshots unconfirmed on disk" row was itself stale, also corrected this pass), two of which are named `06-ask-question-ERROR-bug.png` and `07-sql-generate-ERROR-bug.png` — someone captured a real bug in the two most central workflows (asking a natural-language question, generating SQL) during a live `docker compose up` run, and it was never referenced or triaged anywhere in this document afterward. This pass could not reproduce it: no Docker daemon is reachable from this sandbox or the connected device, and the one concrete lead found by reading the code — `stage_rows()` (`app/staging.py`) only uses schema `"staging"` under real Postgres (`schema = "staging" if is_postgres else None`) — means every automated test in this repo, which all run against SQLite, silently never exercises the exact schema-qualified-staged-table code path a human demo run apparently hit trouble in. Static review of `stage_rows()`'s Postgres path (including its `CREATE SCHEMA IF NOT EXISTS staging` handling) did not surface an obvious defect, so this is reported as an open, unreproduced lead, not diagnosed further. **Recommend: re-run `docker compose up` and either re-attempt those two exact steps or share what actually broke, before recording the demo video** — see the new §7 priority-8 item.

- **August 9, 2026 (thirteenth pass — post-login 404 under Docker, this revision):** Direct bug report: "getting a 404 error post login," reproduced against `docker compose up` (confirmed with the user, not assumed). Root cause was self-inflicted by the ninth pass's switch to a same-origin `/api` proxy: `apps/web/next.config.ts`'s new `rewrites()` (the only thing making `/api/*` reach the backend at all under Docker) is resolved by `next start` at runtime, but `apps/web/Dockerfile`'s runtime stage never copied `next.config.ts` into the container — only `.next`, `public`, `package.json`, and a pruned `node_modules` — so the shipped image ran with zero rewrites and every `/api/*` call 404'd immediately, starting with `/api/auth/login` on submit and `/api/auth/me` right after. Compounding it: even copying the `.ts` file as-is would not have been enough, because the runtime stage's `npm prune --omit=dev` strips `typescript`, which `next start` needs to load a `.ts` config at all. This gap was latent and harmless before the ninth pass (the old direct-`NEXT_PUBLIC_API_URL` approach never depended on rewrites), and became a real, deployment-breaking bug only once the proxy architecture shipped without a matching Dockerfile update. **Fixed:** converted `next.config.ts` → `next.config.mjs` (plain ESM, no TypeScript needed to load it at runtime — verified by importing it with bare `node`, no `tsx`/`ts-node`), deleted the old `.ts` file, and added `COPY --from=build /app/next.config.mjs ./next.config.mjs` to the Dockerfile's runtime stage before the `npm prune` step. Verified `tsc --noEmit` clean and a full `next build` succeeds picking up the `.mjs` config with no config-related warnings. Not yet verified against a live `docker compose up` run (no Docker daemon in this sandbox) — the user should rebuild the `web` image (`docker compose build web && docker compose up -d web`, or `--build` on the full stack) since the previously-built image predates this fix and a plain restart will not pick it up.

- **August 9, 2026 (twelfth pass — SQL Server demo correction + the three "quick" fixes, this revision):** Direct correction from the user: the SQL Server certification blocker described in the previous revision of `docs/SQLSERVER_EXPRESS_DEMO.md` (host-based SQL Server Express, blocked on enabling TCP/IP in SQL Server Configuration Manager) is obsolete — `compose.yaml` already provisions a self-contained `sqlserver-demo` container (SQL Server 2022, seeded via `infra/sqlserver-init/init.sql` with `demo.customers`/`demo.orders`/`demo.payments`, `sa`/`DataPilot#2026!`, healthchecked) that needs no host changes at all. Rewrote the doc around that container as the active path (direct connector registration steps, plus the existing `mcp-toolbox` MCP-mode variant), and corrected §2's certification row and the §7 priority order to point at the Docker path instead of the dead host-based one; Oracle/Teradata/BigQuery remain fully open and unchanged. SSO/PingFederate completion and the security review/pen test were explicitly deferred by the user this round and are untouched. Then closed the three "quick" items: (1) **Gateway search** — `GatewayAdmin` in `admin.tsx` was the one registry-style admin list still missing the search/filter box every other list (Approvals/Artifacts/Jobs/Pipelines/Quality/Semantic) already got in the ninth pass; added, filtering by name/purpose/owner/LOB/data source/tags. (2) **`tests/test_vector_store.py` (new, 14 tests)** — the multi-provider `embed_text()` fallback chain and `default_embedding_provider()`'s default-then-first-enabled resolution had zero test coverage despite being a genuinely complex, partly-destructive path (`ensure_collection(allow_resize=True)` drops and recreates the Qdrant collection on a dimension change). Covers: no provider, unsupported provider type (`claude`, `local_mock`), missing secret, missing `embedding_model`, live-call failure and success (Gemini + OpenAI-compatible, mocked), and missing-SDK resilience. (3) **`artifacts.py` RBAC gap closed** — `POST /artifacts/{id}/review`'s approval branch used a raw `user.role not in {"admin", "engineer"}` check, the same class of project-membership blind spot fixed everywhere else in the app via `require_permission`; migrated to `require_permission(user, db, "jobs:write", ...)`, matching `/approvals/*`. Verified live, not just unit-tested: an analyst is still correctly blocked from approving (403) and can still request changes (unchanged); a global-`viewer`-who-is-project-`owner` — previously hard-blocked regardless of project role — can now approve, which is the actual bug this closes. Full backend suite re-run at **93/93 passing** (up from 79; +14 from the new test file); `tsc --noEmit` clean.

- **August 9, 2026 (eleventh pass — embedding-provider wiring, this revision):** Direct user question: "`ModelProvider.embedding_model` — is any model used, or is it chosen during model registration, or is it per-database?" Answering it honestly required reading `vector_store.py` line by line rather than trusting this document's own prior claim (see the superseded §5 row and the corrected §4 caveat). Found the real gap: `embed_text()` called a real Gemini embeddings endpoint, but with a hardcoded model string and API key, never reading `ModelProvider.embedding_model`/`secret_reference` at all — every other provider type silently used the hash fallback regardless of what was configured. **Fixed:** `embed_text(value, provider)` now dispatches per `provider.provider_type` (`gemini`, `openai`, `openai_compatible` call real embeddings endpoints with `provider.embedding_model`; `claude` has no Anthropic embeddings endpoint and `local_mock` has no external API, so both intentionally keep the hash fallback), resolved via new `provider_selection.default_embedding_provider(db)` and threaded through `index_document()`/`search_documents()` at every real call site (9 across `main.py`, `metadata_scan_runtime.py`, `schedule_runtime.py`, `routers/files.py`, `routers/workspace.py`, `grounding.py`). Added dimension-mismatch handling in `ensure_collection()` (different models embed to different vector sizes — Gemini 768 vs. OpenAI `text-embedding-3-small` 1536, etc.): the write path recreates the Qdrant collection when the active model's dimension changes, the read path never does (a stale-dimension search just yields no results instead of destroying data). Added `vector_store.reindex_all(db)` + admin-only `POST /model-providers/reindex-embeddings` to re-embed every `DataAsset`/`GlossaryDocument` after a provider/model change, plus a "Reindex embeddings" button in the Provider registry admin screen. Moved `_chunk_glossary_text()` from `routers/workspace.py` into `vector_store.py` (as `chunk_glossary_text()`) so `reindex_all()` could reuse it without a circular import. Full backend suite re-run: **78/79 passing, 1 skipped**, `python -m py_compile` clean on every touched module. Not done this pass, flagged honestly: per-project embedding-provider selection (embeddings stay global, matching the existing single-collection design — see §4); automatic reindex-on-provider-change (the admin has to click "Reindex embeddings" — auto-triggering it inline on `PUT /model-providers/{id}` was judged too slow/surprising for a synchronous request).

- **August 9, 2026 (tenth pass — admin/UX rework, this revision):** Direct product feedback on the Admin screens, confirmed against the code before acting: (1) **Removed a confirmed literal duplication** — Admin's "Tool gateway" tab and the main nav's "Tool registry" → "External data tools" tab both rendered the exact same `GatewayAdmin` component to the exact same audience (admin/engineer only); the Admin tab was deleted with a one-line pointer left in its place. (2) **Split the Governance tab into Prompts / Retention / Learning loop sub-tabs** — all three were already real, working features (`/prompts`, `/retention-policies`, `/learning-suggestions`), just stacked as three dense panels on one screen; now selectable sub-tabs, same components, no behavior change. (3) **Reworked the Users screen** — confirmed the gap was real on the backend, not just the UI: `PUT /admin/users/{id}` only ever accepted `role`/`active`, with no way to edit name/email and no `DELETE` endpoint at all. Extended `UserUpdate` (`name`, `email`, with a case-insensitive uniqueness check reusing the same pattern as user creation) and added an edit modal; kept deactivate-only by design (hard-deleting a user would orphan `AuditEvent`/`ModelCallLog` foreign keys) but added a confirmation prompt, disabled the control for your own active account instead of letting the API 409, and added search + loading/empty states matching the other admin screens. Verified live with `TestClient`: create → rename/re-email → duplicate-email correctly rejected with 409. (4) **Answered "is local workspace just Postgres?"** — precisely: querying without selecting a connector runs against DataPilot's own control-plane database (`analysis_source_output()` in `main.py` labels it "PostgreSQL staging"), which is Postgres in the documented Compose stack but is not hardcoded — `staging.py`'s `stage_rows()` branches on `engine.dialect.name`, so the same code path also serves the SQLite fallback used by the non-Docker dev path. Not a separate engine; whatever `DATABASE_URL` resolves to. (5) **Built a visual catalog relationship graph** in `SemanticView.tsx` (`SemanticGraphPanel`) — a hand-rolled SVG node-link diagram (no new dependency; the app has zero charting/graph libraries installed) rendering `GET /semantic/graph`'s existing nodes/edges, circular layout, governed joins drawn solid, column-name-inferred suggestions drawn dashed and visually distinct, with a toggle matching the endpoint's `include_inferred` param and hover highlighting. This is a real UI gap closed with data the backend already computed but never surfaced visually. Full backend suite re-run at **79/79 passing**; `tsc --noEmit` and `next build` both clean after every step above.

- **August 9, 2026 (ninth pass — validating a further round of frontend additions, this revision):** A follow-up round of uncommitted frontend work added real, useful functionality to `ConversationsView.tsx`/`SQLView.tsx` (publish-a-result-as-a-tool, eject-to-notebook, rename analysis, per-message thumbs feedback, a "Start analysis" deep link from Datasets), plus search/filter boxes and dataset de-duplication across `ApprovalsView`/`ArtifactsView`/`JobsView`/`PipelinesView`/`QualityView`/`SemanticView`, a rule-delete action in Quality, and a switch to a same-origin `/api` proxy (`apps/web/app/lib/api.ts` + `next.config.ts`) instead of a build-time `NEXT_PUBLIC_API_URL`. Re-validating this against the actual backend contracts (not just reading the diff) found: (1) **A real compile-breaking bug** — `PipelinesView.tsx`'s new schedule search called `.toLowerCase()` on `IngestionSchedule.target_table`, which is optional; `tsc --noEmit` failed, which means `next build` would have failed too. Fixed with a null-safe fallback. (2) **Four real broken backend calls, all confirmed live-404/422 before the fix, all confirmed working after:** the new "Publish as Tool" action posted to `/query-tools` with only `name`/`sql_template`, omitting five other required fields (`description`, `purpose`, `data_source`, `line_of_business`, `owner`) and using a free-text name that fails the endpoint's slug pattern — fixed by deriving all required fields from context and slugifying the name. "Eject to Notebook" posted `{title, cells:[{type, content}]}` against a schema that actually requires `{name, cells:[{id, type, source}]}` — fixed to match. The per-message thumbs up/down called a `/conversations/{id}/messages/{id}/feedback` endpoint that does not exist, with a `rating: "positive"/"negative"` enum that doesn't match the real `/feedback` endpoint's `"helpful"/"not_helpful"` — rewired to the real endpoint with the real enum. "Rename analysis" called `PUT /conversations/{id}`, which also did not exist anywhere in the router — rather than leave the button non-functional, added the endpoint (`ConversationRename` model + `rename_conversation()` in `conversations.py`, same ownership rule as delete). All four verified with live `TestClient` calls using the exact payload shapes the frontend now sends, not just unit-level assertions. (3) **One action removed rather than patched:** "Convert to Pipeline" posted to a `/pipelines` endpoint that doesn't exist, with a raw-SQL payload shape (`sql_definition`) that has no equivalent anywhere in the pipeline system — real pipeline generation (`POST /pipelines/generate`) is objective- and source-asset-driven, not built to accept a pasted SQL string. Faking a mapping would have been dishonest about what the button does, so it was removed from `ConversationsView.tsx` pending a real product decision on whether "paste this SQL into a new pipeline" should become a real backend capability. (4) **A real deployment/docs-drift bug**, not code-breaking but demo-breaking: switching `API_URL` to a hardcoded same-origin `/api` (proxied via `next.config.ts` to `INTERNAL_API_URL`, defaulting to `http://api:8000`) is correct for `docker compose up` (the service is named `api`), but the repo's own documented non-Docker quick start (`README.md` "Run without Docker": `uvicorn` on `localhost:8000` + `npm run dev`) had no `INTERNAL_API_URL` set, and `api` does not resolve as a hostname outside the Compose network — every API call from a directly-run `npm run dev` would fail. Updated `README.md`, `apps/web/.env.example`, and `compose.yaml` to set/document `INTERNAL_API_URL` correctly for both paths, and removed the now-dead `NEXT_PUBLIC_API_URL` build arg the frontend no longer reads. Full backend suite re-run at **79/79 passing**; `tsc --noEmit` clean. A new `docs/OWASP_TOP_10_LLM_MAPPING.md` also landed this round — reviewed, reads as accurate against the architecture described elsewhere in this document, not yet cross-linked from here.

- **August 9, 2026 (post-migration regression fix — this revision, requested as "revalidate the code after changes from another model"):** A separate work session (uncommitted at the time of this pass) had run a scripted RBAC migration (`tmp_migrate_rbac.py`, `tmp_semantic_rbac.py` — regex-based find/replace scripts left in the repo root) that rewrote inline `user.role in {...}` checks into `require_permission(...)` calls across `agents.py`, `approvals.py`, `connectors.py`, `projects.py`, `quality.py`, `query_tools.py`, `semantic.py`, and `tools.py`. The scripts never added `require_permission`, `project_permissions`, or `current_membership` to each file's `from ..main import (...)` list. Result: every one of those routers raised a live `NameError` on the affected endpoints — creating a query tool, publishing an agent version, running quality rules, creating projects, managing semantic joins, registering tools, and listing connectors were all broken. Running the actual backend suite (not trusting the previous revision's masthead) surfaced this immediately: **16 of 79 tests failing**, not the "70/70 passing" the prior revision of this document claimed — that revision was written concurrently with the breaking change and never re-ran the suite against it. Separately, `require_semantic_maintainer()` in `main.py` called `require_permission(user, "semantic:write", "...")` with an argument missing — passing the permission string into the `db: Session` parameter slot — which would have thrown `AttributeError` on every semantic-join create/update/delete for any admin/engineer/project-maintainer, the only users who reach that line. **Fixed:** added the missing imports to all 8 router files, corrected the `require_semantic_maintainer` call, and re-ran the full suite: **79/79 passing.** Also found and documented, not fixed: `tests/test_new_endpoints.py` (new this session, covers 3 real previously-undocumented capabilities — see new §1 rows) only passes because it inherits `test_api.py`'s temp-database env var by import-order accident, not real isolation; a stray untracked `docker-compose.yml` at the repo root duplicates and conflicts with the documented `compose.yaml` (different Postgres version, port, and credentials) and should be removed or reconciled to avoid someone running the wrong stack; `docs/presentation/version 1/` is confirmed-by-timestamp to be the prior deck iteration, superseded by the top-level deck + `docs/presentation/diagrams/` (generated afterward) — safe to archive elsewhere or delete once confirmed no longer needed. This pass also found three real, working, previously-undocumented capabilities already in the code (agent self-healing tool-error reflection, a plan-reviewer/critic loop, and graph-based multi-hop join pathfinding for grounding — see new §1 rows) and confirmed that real Gemini embeddings (not just the hash fallback) are wired into `vector_store.py`, which retroactively supersedes the hash-only caveat and the "wire real embeddings" recommendation both still present elsewhere in this document's older prose.

- **August 9, 2026 (Superset dashboard-collision fix, fabricated-evidence fix, tool risk/approval invariant — independent gap-finding pass, this revision):** Requested directly as a from-scratch code review ("find gaps, dead code, unwritten code, fix them — even notebook/LLM/Superset isn't wired properly, how does a SQL/tool result actually link to Superset, hotlinked, approvals"), not a continuation of this document's own prior self-review. Three general-purpose sub-agents independently re-audited the backend, frontend, and approvals subsystem against the code as it stands (not against this document's claims). Found and fixed: (1) **The real Superset gap the request named.** `POST /analytics/publish-sql`'s approval-gated publish flow (built concurrently, present when this pass started) provisioned its Superset dashboard using `_dashboard_slug(project_slug)` — the *identical* slug the project's primary "Governed analytics" dashboard uses. Approving any published query would silently overwrite the project's main dashboard's title, dataset, and charts with the new query's. Fixed by threading an artifact-derived `dashboard_key` through `superset_client.py` and giving each published query its own `SupersetQueryDashboard` row/dashboard, and closed the missing other half — there was no UI path to actually reach a published query's dashboard. Added `GET/POST /analytics/queries/{artifact_id}[/guest-token]` and a `PublishedQueryAnalyticsModal`, wired as an "Open in Superset" hotlink in both `SQLView.tsx` and `NotebooksView.tsx` next to their existing "Publish to Superset" buttons. (2) `POST /agents/runs` showed human approvers a hardcoded, never-computed `"3 assets considered"` evidence line before any real grounding had run — replaced with an honest, cheaply-computed live count of the project's catalog, labeled to make clear real grounding happens post-approval. (3) `worker.py`'s `schedule_dispatch_loop` had a bare `except Exception: pass` with zero logging anywhere in the module — if the DB or Temporal connection started failing persistently, every scheduled ingestion would stop firing forever with no trace to diagnose it from; added `logging.exception(...)`. (4) `ToolDefinition.risk_level` and `.requires_approval` were independent fields with nothing enforcing they agree — any admin/engineer could register or edit a `risk_level="critical"` tool with `requires_approval=False`, and it would execute immediately with zero `Approval` ever created; `POST /tools` and `PUT /tools/{id}` now force `requires_approval=True` whenever `risk_level` is `high`/`critical`. Two new backend tests cover (2) and (4) directly against the real endpoints, and the existing Superset-publication test was extended to assert the dashboard-key fix and exercise both new endpoints end to end. Full backend suite re-run at **70/70 passing, 1 skipped**; frontend `tsc --noEmit` clean. **Confirmed, not fixed this pass, and still real:** the fine-grained-RBAC project-membership blind spot in `require_permission()` (§2) — the sub-agent audit independently re-confirmed it rather than re-discovering it, and the fix (thread `db` through ~32 call sites across 9 files) was judged too large to do safely alongside everything above in one pass; and file-stage-to-Postgres itself still has no approval step even though it's the top-priority candidate `resolve_superset_dataset()` picks for the *project's* primary dashboard — a real, narrower edge of "does anything ungoverned reach Superset," left open and documented rather than silently gated (see the new §1 Superset row for the reasoning on why the project dashboard is treated differently from a published query).

- **August 9, 2026 (guarded LLM parameter-fill, this revision):** Implemented the capability §8/§5 had identified as "not built" in the prior revision — a second, model-assisted tier behind `_parameters_for_tool()`'s deterministic heuristic, reached only when the heuristic can't ground every required parameter for an already-vetted low-risk, non-approval tool. `_llm_parameters_for_tool()` in `temporal_activities.py` prompts a real (non-`local_mock`) provider with only the tool's JSON Schema and the objective, then subjects the response to the same checks any other call gets: strict JSON, `validate_parameters()`, and — critically — every foreign-key-shaped field (`asset_id`, `file_id`, `mapping_id`, `rule_id`, `schedule_id`, `job_id`) must resolve to a real row scoped to the run's project or the entire fill is discarded, so a schema-valid but hallucinated id is rejected exactly like a missing one. Every attempt (accepted or rejected) is logged to `ModelCallLog` for cost visibility; accepted tool calls carry a `parameter_source: "heuristic"|"model"` tag on their evidence entry so a completed run stays explainable about which tier supplied which parameter. New test `test_llm_parameter_fill_is_guarded_and_grounded` exercises both outcomes directly against `_execute_bound_tools()` — the real bounded-run function, not a unit-level mock of the fill function alone — with a mocked model response, proving both that a grounded id is accepted and a hallucinated one is rejected. Also corrected two stale figures this pass introduced fresh eyes on: the document's own masthead claimed "64/64 passing" when the actual re-run (this session's own prior two entries had already added tests) was 67/68 passing + 1 skipped across `test_api.py` + `test_connection_guard.py` combined — fixed in the masthead and §3 rather than left stale for the next reconciliation to catch.

- **August 8, 2026 (orphaned internal-tool handlers, this revision):** Answering a direct question ("is the agent/tool system for SQL generation, metadata, quality solid, or do we need more agents/tools?") required verifying, not assuming. Found that of the 10 tools `seed.py` registers, only 5 had a working `ToolVersion`/handler — `sql.generate`, `file.profile`, `quality.run`, `pipeline.stage`, `schedule.run` were bound to agents by name but had no implementation at all (`tool_runtime.BUILTIN_HANDLERS` didn't contain them, and no `ToolVersion` existed to even publish). This meant the seeded Pipeline agent could execute nothing, Quality could only search the catalog, and SQL Analyst could never draft SQL — the real SQL generation users see comes from a standalone `/sql/generate` route the agent framework never touches. Implemented all 5 as real builtin handlers (extracted `generated_sql`/`generated_catalog_sql` into a new `sql_generation.py` to avoid a circular import into `tool_runtime.py`), reusing the exact same code paths their REST counterparts use, and registered their `ToolVersion` contracts in `seed.py` (idempotent — self-heals existing databases on next startup, verified directly against this repo's dev `datapilot.db`). High-risk tools (`pipeline.stage`, `schedule.run`) keep `requires_approval=True` and are confirmed to never auto-fire in the bounded orchestration loop. Added two new tests, one exercising all 5 handlers (including the approval-gated ones end to end) and one proving the seeded SQL Analyst agent now autonomously drafts SQL through the real bounded-run code path — not just a unit-level handler call. See `docs/specs/tools.md` for the full built-in handler table. This is complementary to, not a duplicate of, the "agent orchestration ceiling" findings in §8 above (the `_parameters_for_tool` heuristic) — that ceiling still applies to these 5 tools exactly as it did to the original 5: only `sql.generate` (required param `query`) is auto-fillable by the bounded loop, the other four still only run through explicit invocation, by design.
- **August 8, 2026 (configurable policy/analytics and schema-aware binding):** Added seeded `Policy` and `Analytics` agents, made planner agent selection follow enabled registry configuration, and kept deterministic policy enforcement authoritative over any model-generated plan. Added schema-aware, project-grounded parameter binding with enum/type/required validation and tests for safe refusal when required identifiers are not grounded. Existing databases self-heal the new agent rows on API startup; agents remain editable/versioned/publishable from the registry UI.
- **August 8, 2026 (agent ↔ SQL query-tool binding, this revision):** Closed a real architectural gap surfaced by direct code review, not by a prior version of this document: internal agents (`AgentDefinition`/`AgentVersion`) could only ever call the internal `ToolDefinition` registry — the governed `QueryTool` registry (SQL tools generated via the wizard, verified, published with business metadata) was only reachable by external accessors. Added `query_tool_names` binding, validated against published/project-scoped query tools, resolved on `GET /agents/{id}`, and executed inside the bounded orchestration loop through the same connector/PII-masking path the external gateway uses. New test added; full suite re-run at 62/62. Frontend updated and `tsc --noEmit` re-verified clean. See §1 "Agent execution of governed SQL query tools" and `docs/specs/agents.md`/`query_tools.md`.
- **August 9, 2026 (external-MCP resilience / SQL-relevance / business-naming / glossary iteration, concurrent):** Answered a four-part question about production concerns and closed the real gaps found along the way. (1) Added `app/connection_guard.py` — a per-connector `BoundedSemaphore` (default 8 concurrent, `CONNECTOR_MAX_CONCURRENT_CONNECTIONS`) around the no-pooling direct-driver dispatch path, so unbounded external MCP/agent traffic against one connector now gets a clear 503 instead of an unbounded connection storm against the source database. (2) Made `POST /extractions/{id}/run` Temporal-backed (`ExternalExtractionWorkflow`, sync-fallback when Temporal isn't configured) — it was the one ingestion path without durable retry/history, now consistent with scheduled ingestion. (3) Fixed a real SQL-generation relevance bug: the real-provider prompt path was building its 40-table-capped catalog section from the raw alphabetical list instead of the already-computed grounding-prioritized ordering, silently dropping relevant tables past position 40 for large catalogs. (4) Added column-level `business_name`/`description` (`column_notes` on `PUT /datasets/{asset_id}`), wired into both SQL-generation grounding text and catalog search relevance. (5) Added SOP/business-glossary upload (`POST/GET/DELETE /glossary`, PDF-or-text, chunked and indexed into the vector store, retrieved by both SQL generation and the agent planner via `grounding.py`'s new `glossary_matches()`). (6) Investigated and confirmed, rather than assumed: there is no first-class "multiple data models per project" grouping concept in the code today (flat catalog + free-text tags only) — recorded as an honest gap, not built. (7) Flagged that vector-store "embeddings" are a deterministic hash function, not real semantic embeddings — recorded as a gap, not fixed this round. (8) Assessed the agent/tool harness end-to-end (see §8) — the real ceiling found is that tool-parameter filling is a narrow, deliberately-conservative heuristic, not an LLM-driven step, which is a safety trade-off worth knowing about rather than a bug to silently patch. Full backend suite re-run at **64/64** (was 62/62 as of the previous revision of this document; new coverage this round includes `tests/test_connection_guard.py`'s 6 standalone concurrency-guard tests and `test_z_glossary_document_upload_list_grounding_delete`).
- **August 8, 2026 (rate limiting / OCP / self-learning-UX / re-verification iteration, this revision):** Added Redis-backed rate limiting for the external query-tool gateway (`app/rate_limit.py`, wired into the shared REST+MCP invoke path, tested with `fakeredis`) — the first real use of the previously-provisioned-but-unreferenced `REDIS_URL`. Hardened `infra/kubernetes` for OpenShift/vanilla-K8s parity: non-root Dockerfiles (group-0-writable pattern), `securityContext` on every Deployment, HPAs for web and worker (previously only the API had one), a worker PodDisruptionBudget and liveness probe, explicit/tunable SQLAlchemy pool sizing with documented connection-count math, and an `ingress.example.yaml` covering both platforms. Added a review-target-aware deep link on accepted/reviewed learning suggestions (pure navigation, verified no automatic behavior change). Re-verified every 🟡/🔴 row against current code and discovered three concurrently-built capabilities that had not yet been reflected in this document — multi-hop lineage traversal (`/lineage/graph`), permission-map-backed RBAC enforcement (`require_permission`, real but partial — see §2), and PII detection/masking (`app/pii.py`) — each confirmed by reading the actual implementation rather than trusting the commit message describing it. Documented direct, code-verified answers to: does an accepted learning suggestion change anything automatically (no), is DataPilot enterprise-ready (enterprise-shaped, not yet certified), is it scalable (architecturally yes, not load-tested), and do we need Kafka (no — see `ARCHITECTURE_DECISIONS.md` §6–§7).
- **August 8, 2026 (PII guardrails iteration, concurrent):** Shared PII classification for identity/contact/payment fields, sensitivity annotations on discovered/ingested catalog columns, centralized partial masking for local SQL, connector, MCP, query-tool, and notebook result rows.
- **August 8, 2026 (navigation/RBAC/explainability iteration, concurrent):** Grouped collapsible navigation, role-based landing defaults, permission-backed catalog/workspace/semantic write gates, SQL "Why this result?" drawer.
- **August 8, 2026 (lineage iteration, concurrent):** Bounded multi-hop `/lineage/graph` traversal for upstream/downstream/both directions.
- **August 8, 2026 (metadata/MCP/CI iteration):** MCP-mode metadata scanning, feedback trend detection + learning-loop review UI, bulk CSV catalog import + UI, GitHub Actions CI pipeline.
- **August 8, 2026 (governed metadata iteration, concurrent):** governed asset metadata fields (owner, sensitivity, freshness SLA, certification status), metric-to-asset binding, `/semantic/graph`, `/sql/history`, `/auth/me` permissions surface, query-tool tested/retired lifecycle, SQL Server Express demo provisioning.
